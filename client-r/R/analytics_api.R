#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Analytics operations
#' @description AnalyticsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  Activities  ####################
#'
#' library(openapi)
#' var_start <- 56 # integer | The start of the pagination
#' var_limit <- 56 # integer | The limit of the pagination
#' var_account_id <- 56 # integer | the account id of the user
#'
#' #Get User Activity
#' api_instance <- AnalyticsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$Activities(var_start, var_limit, var_account_iddata_file = "result.txt")
#' result <- api_instance$Activities(var_start, var_limit, var_account_id)
#' dput(result)
#'
#'
#' ####################  AggregatedFilteredUsage  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_application_id <- 56 # integer | This parameter is deprecated. (Optional)
#' var_app_key <- "app_key_example" # character | The application key used to filter results by application (Optional)
#' var_start_date <- 56 # integer | Used to search for results after or equal to this date (UNIX time-stamp in milliseconds) (Optional)
#' var_end_date <- 56 # integer | Used to search for results before or equal to this date (UNIX time-stamp in milliseconds) (Optional)
#' var_device_type <- "device_type_example" # character | The device type to filter results by (performs a LIKE search) (Optional)
#' var_device <- "device_example" # character | The device to filter results by (performs a LIKE search) (Optional)
#' var_device_os <- "device_os_example" # character | The device OS to filter results by (performs a LIKE search) (Optional)
#' var_gender <- "gender_example" # character | The gender to filter results by {MALE, FEMALE} (Optional)
#' var_age_group <- "age_group_example" # character | Comma separated list of age groups to filter by {AGE_0_13, AGE_14_17, AGE_18_22, AGE_23_30, AGE_31_54, AGE_55_PLUS} (Optional)
#' var_country <- "country_example" # character | The country to filter results by (performs a wild-card search) (Optional)
#' var_state <- "state_example" # character | The state to filter results by (performs a wild-card search) (Optional)
#' var_city <- "city_example" # character | The city to filter results by (performs a wild-card search) (Optional)
#' var_zip <- "zip_example" # character | The zip to filter results by (performs a wild-card search) (Optional)
#' var_model <- "model_example" # character | The model to filter results by (performs a wild-card search) (Optional)
#' var_tag <- "tag_example" # character | The tag to filter results by (performs a wild-card search) (Optional)
#' var_user_account_id <- 56 # integer | The account id to filter results for a particular user (Optional)
#' var_user_account_display <- "user_account_display_example" # character | The user display to filter results by (performs a wild-card search) (Optional)
#' var_user_account_username <- "user_account_username_example" # character | The username to filter results by (performs a wild-card search) (Optional)
#' var_group_by_root <- "group_by_root_example" # character | Determines how to group results. For example, passing in 'CREATED' will return results by date. Passing in 'ACCOUNT' will return results by user {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, APPLICATION_NAME_BY_ID} (Optional)
#' var_group_by <- "group_by_example" # character | Determines how to group the nested data. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, APPLICATION_NAME_BY_ID} (Optional)
#' var_distinct_count <- "distinct_count_example" # character | Performs a unique query on the specified column. For example, passing in 'ACCOUNT' will return the number of unique users. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, NEW_ACCOUNT} (Optional)
#' var_sort_field <- "sort_field_example" # character | Determines whether the results will be sorted by day or by number of hits {CREATED, TAG_COUNT} (Optional)
#' var_descending <- "descending_example" # character | Determines whether to return results in descending order (Optional)
#' var_hide_unknown <- "hide_unknown_example" # character | Determines whether to return data that has empty or unknown values (Optional)
#' var_response_format <- "response_format_example" # character | Determines whether to return a JOSN or XML representation of the graph results (Optional)
#' var__l <- 56 # integer | This parameter is deprecated. (Optional)
#' var_limit <- 56 # integer | Used to limit results to get a cleaner graph. The results that gets filtered out will be combined (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#'
#' #Get Aggregated Filtered Usage
#' api_instance <- AnalyticsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AggregatedFilteredUsage(device_id = var_device_id, account_id = var_account_id, application_id = var_application_id, app_key = var_app_key, start_date = var_start_date, end_date = var_end_date, device_type = var_device_type, device = var_device, device_os = var_device_os, gender = var_gender, age_group = var_age_group, country = var_country, state = var_state, city = var_city, zip = var_zip, model = var_model, tag = var_tag, user_account_id = var_user_account_id, user_account_display = var_user_account_display, user_account_username = var_user_account_username, group_by_root = var_group_by_root, group_by = var_group_by, distinct_count = var_distinct_count, sort_field = var_sort_field, descending = var_descending, hide_unknown = var_hide_unknown, response_format = var_response_format, _l = var__l, limit = var_limit, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$AggregatedFilteredUsage(device_id = var_device_id, account_id = var_account_id, application_id = var_application_id, app_key = var_app_key, start_date = var_start_date, end_date = var_end_date, device_type = var_device_type, device = var_device, device_os = var_device_os, gender = var_gender, age_group = var_age_group, country = var_country, state = var_state, city = var_city, zip = var_zip, model = var_model, tag = var_tag, user_account_id = var_user_account_id, user_account_display = var_user_account_display, user_account_username = var_user_account_username, group_by_root = var_group_by_root, group_by = var_group_by, distinct_count = var_distinct_count, sort_field = var_sort_field, descending = var_descending, hide_unknown = var_hide_unknown, response_format = var_response_format, _l = var__l, limit = var_limit, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  FilteredUsage  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_application_id <- 56 # integer | This parameter is deprecated. (Optional)
#' var_app_key <- "app_key_example" # character | The application key used to filter results by application (Optional)
#' var_start_date <- 56 # integer | Used to search for results after or equal to this date (UNIX time-stamp in milliseconds) (Optional)
#' var_end_date <- 56 # integer | Used to search for results before or equal to this date (UNIX time-stamp in milliseconds) (Optional)
#' var_device_type <- "device_type_example" # character | The device type to filter results by (performs a LIKE search) (Optional)
#' var_device <- "device_example" # character | The device to filter results by (performs a LIKE search) (Optional)
#' var_device_os <- "device_os_example" # character | The device OS to filter results by (performs a LIKE search) (Optional)
#' var_gender <- "gender_example" # character | The gender to filter results by {MALE, FEMALE} (Optional)
#' var_age_group <- "age_group_example" # character | Comma separated list of age groups to filter by {AGE_0_13, AGE_14_17, AGE_18_22, AGE_23_30, AGE_31_54, AGE_55_PLUS} (Optional)
#' var_country <- "country_example" # character | The country to filter results by (performs a wild-card search) (Optional)
#' var_state <- "state_example" # character | The state to filter results by (performs a wild-card search) (Optional)
#' var_city <- "city_example" # character | The city to filter results by (performs a wild-card search) (Optional)
#' var_zip <- "zip_example" # character | The zip to filter results by (performs a wild-card search) (Optional)
#' var_model <- "model_example" # character | The model to filter results by (performs a wild-card search) (Optional)
#' var_tag <- "tag_example" # character | The tag to filter results by (performs a wild-card search) (Optional)
#' var_user_account_id <- 56 # integer | The account id to filter results for a particular user (Optional)
#' var_user_account_display <- "user_account_display_example" # character | The user display to filter results by (performs a wild-card search) (Optional)
#' var_user_account_username <- "user_account_username_example" # character | The username to filter results by (performs a wild-card search) (Optional)
#' var_custom_id <- 56 # integer | The ID for the custom table column (Optional)
#' var_custom_type <- "custom_type_example" # character | The type defined for the custom table column (Optional)
#' var_custom_value <- 3.4 # numeric | The value for the custom table column (Optional)
#' var_custom_value2 <- 3.4 # numeric | The value for another custom table column (Optional)
#' var_custom_long <- 56 # integer | The value that supports Longs for custom table column (Optional)
#' var_custom_long2 <- 56 # integer | The value that supports Longs for custom table column (Optional)
#' var_custom_message <- "custom_message_example" # character | The message for the custom table column (Optional)
#' var_custom_message2 <- "custom_message2_example" # character | The message for the custom table column (Optional)
#' var_group_by <- "group_by_example" # character | Determines how to group results. For example, passing in 'CREATED' will return results by date. Passing in 'ACCOUNT' will return results by user {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID} (Optional)
#' var_distinct_count <- "distinct_count_example" # character | Performs a unique query on the specified column. For example, passing in 'ACCOUNT' will return the number of unique users. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID} (Optional)
#' var_sum_column <- "sum_column_example" # character | sum of the column values (Optional)
#' var_sort_field <- "sort_field_example" # character | Determines whether the results will be sorted by day or by number of hits {CREATED, TAG_COUNT} (Optional)
#' var_descending <- "descending_example" # character | Determines whether to return results in descending order (Optional)
#' var_hide_unknown <- "hide_unknown_example" # character | Determines whether to return data that has empty or unknown values (Optional)
#' var_response_format <- "response_format_example" # character | Determines whether to return a JOSN or XML representation of the graph results (Optional)
#' var__l <- 56 # integer | This parameter is deprecated. (Optional)
#' var_limit <- 56 # integer | Used to limit results to get a cleaner graph. The results that gets filtered out will be combined (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#'
#' #Get Filtered Usage
#' api_instance <- AnalyticsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FilteredUsage(device_id = var_device_id, account_id = var_account_id, application_id = var_application_id, app_key = var_app_key, start_date = var_start_date, end_date = var_end_date, device_type = var_device_type, device = var_device, device_os = var_device_os, gender = var_gender, age_group = var_age_group, country = var_country, state = var_state, city = var_city, zip = var_zip, model = var_model, tag = var_tag, user_account_id = var_user_account_id, user_account_display = var_user_account_display, user_account_username = var_user_account_username, custom_id = var_custom_id, custom_type = var_custom_type, custom_value = var_custom_value, custom_value2 = var_custom_value2, custom_long = var_custom_long, custom_long2 = var_custom_long2, custom_message = var_custom_message, custom_message2 = var_custom_message2, group_by = var_group_by, distinct_count = var_distinct_count, sum_column = var_sum_column, sort_field = var_sort_field, descending = var_descending, hide_unknown = var_hide_unknown, response_format = var_response_format, _l = var__l, limit = var_limit, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$FilteredUsage(device_id = var_device_id, account_id = var_account_id, application_id = var_application_id, app_key = var_app_key, start_date = var_start_date, end_date = var_end_date, device_type = var_device_type, device = var_device, device_os = var_device_os, gender = var_gender, age_group = var_age_group, country = var_country, state = var_state, city = var_city, zip = var_zip, model = var_model, tag = var_tag, user_account_id = var_user_account_id, user_account_display = var_user_account_display, user_account_username = var_user_account_username, custom_id = var_custom_id, custom_type = var_custom_type, custom_value = var_custom_value, custom_value2 = var_custom_value2, custom_long = var_custom_long, custom_long2 = var_custom_long2, custom_message = var_custom_message, custom_message2 = var_custom_message2, group_by = var_group_by, distinct_count = var_distinct_count, sum_column = var_sum_column, sort_field = var_sort_field, descending = var_descending, hide_unknown = var_hide_unknown, response_format = var_response_format, _l = var__l, limit = var_limit, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  Usage  ####################
#'
#' library(openapi)
#' var_tag <- "tag_example" # character | The tag to apply: the name of the action or thing being logged.
#' var_device_id <- "device_id_example" # character | The client deviceID (Optional)
#' var_account_id <- 56 # integer | The logged in user ID (Optional)
#' var_application_id <- 56 # integer | This parameter is deprecated. This is deprecated, use appKey. (Optional)
#' var_app_key <- "app_key_example" # character | The application key unique to each application. (Optional)
#' var_app_version <- "app_version_example" # character | The current build version of the application (Optional)
#' var_device <- "device_example" # character | The name of the device being used (iPhone5,1 , HTC Nexus One, x86_64, etc.) (Optional)
#' var_device_type <- "device_type_example" # character | The type of device (Handheld or Desktop) (Optional)
#' var_device_os <- "device_os_example" # character | The operating system that the device is using (iPhone OS 5.0, Android OS 2.3.6, Windows 7 Service Pack 1, etc) (Optional)
#' var_model <- "model_example" # character | The model of the device (iPhone5,1 , Nexus One, etc.) (Optional)
#' var_latitude <- 3.4 # numeric | The current position of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current position of the user (Optional)
#' var_custom_id <- 56 # integer | A customId used to associate the usage record with some other record in the system (Optional)
#' var_custom_type <- "custom_type_example" # character | The type of record the customId is recorded for (Optional)
#' var_achievement_increment <- 56 # integer | This parameter is deprecated. This is deprecated, use \"customValue\". Determines how many achievement points and/or ranking points the user gets if this analytic is associated with an achievement (Optional)
#' var_city <- "city_example" # character | The current city of the user (Optional)
#' var_state <- "state_example" # character | The current state of the user (Optional)
#' var_country <- "country_example" # character | The current country of the user (Optional)
#' var_zip <- "zip_example" # character | The current zip of the user (Optional)
#' var_location_description <- "location_description_example" # character | The general description of the user's location (Optional)
#' var_client_time <- 56 # integer | Timestamp sent from device (Optional)
#' var_error_message <- "error_message_example" # character | This parameter is deprecated. (Optional)
#' var_ip <- "ip_example" # character | the ip address of the client device (Optional)
#' var_user_agent <- "user_agent_example" # character | this is a description of the client (might contain the device's OS, browser version etc - its a common term) (Optional)
#' var_background_event <- "background_event_example" # character | This is deprecated. (Optional)
#' var_custom_message <- "custom_message_example" # character | a custom message for the usage record (Optional)
#' var_custom_message2 <- "custom_message2_example" # character | a custom message for the usage record (Optional)
#' var_custom_value <- 3.4 # numeric | a custom value for the usage record (Optional)
#' var_custom_value2 <- 3.4 # numeric | a custom value for the usage record (Optional)
#' var_custom_long <- 56 # integer | a custom long value for the usage record (Optional)
#' var_custom_long2 <- 56 # integer | a custom long value for the usage record (Optional)
#'
#' #Create Usage Record
#' api_instance <- AnalyticsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$Usage(var_tag, device_id = var_device_id, account_id = var_account_id, application_id = var_application_id, app_key = var_app_key, app_version = var_app_version, device = var_device, device_type = var_device_type, device_os = var_device_os, model = var_model, latitude = var_latitude, longitude = var_longitude, custom_id = var_custom_id, custom_type = var_custom_type, achievement_increment = var_achievement_increment, city = var_city, state = var_state, country = var_country, zip = var_zip, location_description = var_location_description, client_time = var_client_time, error_message = var_error_message, ip = var_ip, user_agent = var_user_agent, background_event = var_background_event, custom_message = var_custom_message, custom_message2 = var_custom_message2, custom_value = var_custom_value, custom_value2 = var_custom_value2, custom_long = var_custom_long, custom_long2 = var_custom_long2data_file = "result.txt")
#' result <- api_instance$Usage(var_tag, device_id = var_device_id, account_id = var_account_id, application_id = var_application_id, app_key = var_app_key, app_version = var_app_version, device = var_device, device_type = var_device_type, device_os = var_device_os, model = var_model, latitude = var_latitude, longitude = var_longitude, custom_id = var_custom_id, custom_type = var_custom_type, achievement_increment = var_achievement_increment, city = var_city, state = var_state, country = var_country, zip = var_zip, location_description = var_location_description, client_time = var_client_time, error_message = var_error_message, ip = var_ip, user_agent = var_user_agent, background_event = var_background_event, custom_message = var_custom_message, custom_message2 = var_custom_message2, custom_value = var_custom_value, custom_value2 = var_custom_value2, custom_long = var_custom_long, custom_long2 = var_custom_long2)
#' dput(result)
#'
#'
#' ####################  UsageBatch  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key unique to each application.
#' var_device <- "device_example" # character | The name of the device being used (iPhone5,1 , HTC Nexus One, x86_64, etc.)
#' var_data <- "data_example" # character | The analytic data AnalyticListResponse
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (Optional)
#' var_account_id <- 56 # integer | The account ID of the logged in user making the request (Optional)
#' var_app_version <- "app_version_example" # character | The current build version of the application (Optional)
#' var_device_type <- "device_type_example" # character | The type of device (Handheld or Desktop) (Optional)
#' var_device_os <- "device_os_example" # character | The operating system that the device is using (iPhone OS 5.0, Android OS 2.3.6, Windows 7 Service Pack 1, etc.) (Optional)
#' var_model <- "model_example" # character | The model of the device (iPhone5,1 , Nexus One, etc.) (Optional)
#' var_update_ranking <- "update_ranking_example" # character | Will create a leaderboard if one does not exist for the \"tag\" yet (Optional)
#' var_return_summary_response <- "return_summary_response_example" # character | Returns a summary response of the achievements that have been completed due to the analytics (Optional)
#'
#' #Create Multiple Usage Records
#' api_instance <- AnalyticsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UsageBatch(var_app_key, var_device, var_data, device_id = var_device_id, account_id = var_account_id, app_version = var_app_version, device_type = var_device_type, device_os = var_device_os, model = var_model, update_ranking = var_update_ranking, return_summary_response = var_return_summary_responsedata_file = "result.txt")
#' result <- api_instance$UsageBatch(var_app_key, var_device, var_data, device_id = var_device_id, account_id = var_account_id, app_version = var_app_version, device_type = var_device_type, device_os = var_device_os, model = var_model, update_ranking = var_update_ranking, return_summary_response = var_return_summary_response)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AnalyticsApi <- R6::R6Class(
  "AnalyticsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AnalyticsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get User Activity
    #'
    #' @param start The start of the pagination
    #' @param limit The limit of the pagination
    #' @param account_id the account id of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[UserActivityResponse]
    Activities = function(start, limit, account_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ActivitiesWithHttpInfo(start, limit, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get User Activity
    #'
    #' @param start The start of the pagination
    #' @param limit The limit of the pagination
    #' @param account_id the account id of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[UserActivityResponse]) with additional information such as HTTP status code, headers
    ActivitiesWithHttpInfo = function(start, limit, account_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AnalyticsApi$Activities, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AnalyticsApi$Activities, `limit` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AnalyticsApi$Activities, `account_id` is not nullable")
      }

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["accountId"]] <- `account_id`

      local_var_url_path <- "/analytics/useractivity"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[UserActivityResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Aggregated Filtered Usage
    #'
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param application_id (optional) This parameter is deprecated.
    #' @param app_key (optional) The application key used to filter results by application
    #' @param start_date (optional) Used to search for results after or equal to this date (UNIX time-stamp in milliseconds)
    #' @param end_date (optional) Used to search for results before or equal to this date (UNIX time-stamp in milliseconds)
    #' @param device_type (optional) The device type to filter results by (performs a LIKE search)
    #' @param device (optional) The device to filter results by (performs a LIKE search)
    #' @param device_os (optional) The device OS to filter results by (performs a LIKE search)
    #' @param gender (optional) The gender to filter results by {MALE, FEMALE}
    #' @param age_group (optional) Comma separated list of age groups to filter by {AGE_0_13, AGE_14_17, AGE_18_22, AGE_23_30, AGE_31_54, AGE_55_PLUS}
    #' @param country (optional) The country to filter results by (performs a wild-card search)
    #' @param state (optional) The state to filter results by (performs a wild-card search)
    #' @param city (optional) The city to filter results by (performs a wild-card search)
    #' @param zip (optional) The zip to filter results by (performs a wild-card search)
    #' @param model (optional) The model to filter results by (performs a wild-card search)
    #' @param tag (optional) The tag to filter results by (performs a wild-card search)
    #' @param user_account_id (optional) The account id to filter results for a particular user
    #' @param user_account_display (optional) The user display to filter results by (performs a wild-card search)
    #' @param user_account_username (optional) The username to filter results by (performs a wild-card search)
    #' @param group_by_root (optional) Determines how to group results. For example, passing in 'CREATED' will return results by date. Passing in 'ACCOUNT' will return results by user {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, APPLICATION_NAME_BY_ID}
    #' @param group_by (optional) Determines how to group the nested data. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, APPLICATION_NAME_BY_ID}
    #' @param distinct_count (optional) Performs a unique query on the specified column. For example, passing in 'ACCOUNT' will return the number of unique users. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, NEW_ACCOUNT}
    #' @param sort_field (optional) Determines whether the results will be sorted by day or by number of hits {CREATED, TAG_COUNT}
    #' @param descending (optional) Determines whether to return results in descending order
    #' @param hide_unknown (optional) Determines whether to return data that has empty or unknown values
    #' @param response_format (optional) Determines whether to return a JOSN or XML representation of the graph results
    #' @param _l (optional) This parameter is deprecated.
    #' @param limit (optional) Used to limit results to get a cleaner graph. The results that gets filtered out will be combined
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ChartData
    AggregatedFilteredUsage = function(device_id = NULL, account_id = NULL, application_id = NULL, app_key = NULL, start_date = NULL, end_date = NULL, device_type = NULL, device = NULL, device_os = NULL, gender = NULL, age_group = NULL, country = NULL, state = NULL, city = NULL, zip = NULL, model = NULL, tag = NULL, user_account_id = NULL, user_account_display = NULL, user_account_username = NULL, group_by_root = NULL, group_by = NULL, distinct_count = NULL, sort_field = NULL, descending = NULL, hide_unknown = NULL, response_format = NULL, _l = NULL, limit = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AggregatedFilteredUsageWithHttpInfo(device_id, account_id, application_id, app_key, start_date, end_date, device_type, device, device_os, gender, age_group, country, state, city, zip, model, tag, user_account_id, user_account_display, user_account_username, group_by_root, group_by, distinct_count, sort_field, descending, hide_unknown, response_format, _l, limit, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Aggregated Filtered Usage
    #'
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param application_id (optional) This parameter is deprecated.
    #' @param app_key (optional) The application key used to filter results by application
    #' @param start_date (optional) Used to search for results after or equal to this date (UNIX time-stamp in milliseconds)
    #' @param end_date (optional) Used to search for results before or equal to this date (UNIX time-stamp in milliseconds)
    #' @param device_type (optional) The device type to filter results by (performs a LIKE search)
    #' @param device (optional) The device to filter results by (performs a LIKE search)
    #' @param device_os (optional) The device OS to filter results by (performs a LIKE search)
    #' @param gender (optional) The gender to filter results by {MALE, FEMALE}
    #' @param age_group (optional) Comma separated list of age groups to filter by {AGE_0_13, AGE_14_17, AGE_18_22, AGE_23_30, AGE_31_54, AGE_55_PLUS}
    #' @param country (optional) The country to filter results by (performs a wild-card search)
    #' @param state (optional) The state to filter results by (performs a wild-card search)
    #' @param city (optional) The city to filter results by (performs a wild-card search)
    #' @param zip (optional) The zip to filter results by (performs a wild-card search)
    #' @param model (optional) The model to filter results by (performs a wild-card search)
    #' @param tag (optional) The tag to filter results by (performs a wild-card search)
    #' @param user_account_id (optional) The account id to filter results for a particular user
    #' @param user_account_display (optional) The user display to filter results by (performs a wild-card search)
    #' @param user_account_username (optional) The username to filter results by (performs a wild-card search)
    #' @param group_by_root (optional) Determines how to group results. For example, passing in 'CREATED' will return results by date. Passing in 'ACCOUNT' will return results by user {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, APPLICATION_NAME_BY_ID}
    #' @param group_by (optional) Determines how to group the nested data. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, APPLICATION_NAME_BY_ID}
    #' @param distinct_count (optional) Performs a unique query on the specified column. For example, passing in 'ACCOUNT' will return the number of unique users. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, NEW_ACCOUNT}
    #' @param sort_field (optional) Determines whether the results will be sorted by day or by number of hits {CREATED, TAG_COUNT}
    #' @param descending (optional) Determines whether to return results in descending order
    #' @param hide_unknown (optional) Determines whether to return data that has empty or unknown values
    #' @param response_format (optional) Determines whether to return a JOSN or XML representation of the graph results
    #' @param _l (optional) This parameter is deprecated.
    #' @param limit (optional) Used to limit results to get a cleaner graph. The results that gets filtered out will be combined
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ChartData) with additional information such as HTTP status code, headers
    AggregatedFilteredUsageWithHttpInfo = function(device_id = NULL, account_id = NULL, application_id = NULL, app_key = NULL, start_date = NULL, end_date = NULL, device_type = NULL, device = NULL, device_os = NULL, gender = NULL, age_group = NULL, country = NULL, state = NULL, city = NULL, zip = NULL, model = NULL, tag = NULL, user_account_id = NULL, user_account_display = NULL, user_account_username = NULL, group_by_root = NULL, group_by = NULL, distinct_count = NULL, sort_field = NULL, descending = NULL, hide_unknown = NULL, response_format = NULL, _l = NULL, limit = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AnalyticsApi$AggregatedFilteredUsage, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AnalyticsApi$AggregatedFilteredUsage, `account_id` is not nullable")
      }

      if (!missing(`application_id`) && is.null(`application_id`)) {
        stop("Invalid value for `application_id` when calling AnalyticsApi$AggregatedFilteredUsage, `application_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AnalyticsApi$AggregatedFilteredUsage, `app_key` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling AnalyticsApi$AggregatedFilteredUsage, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling AnalyticsApi$AggregatedFilteredUsage, `end_date` is not nullable")
      }

      if (!missing(`device_type`) && is.null(`device_type`)) {
        stop("Invalid value for `device_type` when calling AnalyticsApi$AggregatedFilteredUsage, `device_type` is not nullable")
      }

      if (!missing(`device`) && is.null(`device`)) {
        stop("Invalid value for `device` when calling AnalyticsApi$AggregatedFilteredUsage, `device` is not nullable")
      }

      if (!missing(`device_os`) && is.null(`device_os`)) {
        stop("Invalid value for `device_os` when calling AnalyticsApi$AggregatedFilteredUsage, `device_os` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling AnalyticsApi$AggregatedFilteredUsage, `gender` is not nullable")
      }

      if (!missing(`age_group`) && is.null(`age_group`)) {
        stop("Invalid value for `age_group` when calling AnalyticsApi$AggregatedFilteredUsage, `age_group` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling AnalyticsApi$AggregatedFilteredUsage, `country` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling AnalyticsApi$AggregatedFilteredUsage, `state` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling AnalyticsApi$AggregatedFilteredUsage, `city` is not nullable")
      }

      if (!missing(`zip`) && is.null(`zip`)) {
        stop("Invalid value for `zip` when calling AnalyticsApi$AggregatedFilteredUsage, `zip` is not nullable")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling AnalyticsApi$AggregatedFilteredUsage, `model` is not nullable")
      }

      if (!missing(`tag`) && is.null(`tag`)) {
        stop("Invalid value for `tag` when calling AnalyticsApi$AggregatedFilteredUsage, `tag` is not nullable")
      }

      if (!missing(`user_account_id`) && is.null(`user_account_id`)) {
        stop("Invalid value for `user_account_id` when calling AnalyticsApi$AggregatedFilteredUsage, `user_account_id` is not nullable")
      }

      if (!missing(`user_account_display`) && is.null(`user_account_display`)) {
        stop("Invalid value for `user_account_display` when calling AnalyticsApi$AggregatedFilteredUsage, `user_account_display` is not nullable")
      }

      if (!missing(`user_account_username`) && is.null(`user_account_username`)) {
        stop("Invalid value for `user_account_username` when calling AnalyticsApi$AggregatedFilteredUsage, `user_account_username` is not nullable")
      }

      if (!missing(`group_by_root`) && is.null(`group_by_root`)) {
        stop("Invalid value for `group_by_root` when calling AnalyticsApi$AggregatedFilteredUsage, `group_by_root` is not nullable")
      }

      if (!missing(`group_by`) && is.null(`group_by`)) {
        stop("Invalid value for `group_by` when calling AnalyticsApi$AggregatedFilteredUsage, `group_by` is not nullable")
      }

      if (!missing(`distinct_count`) && is.null(`distinct_count`)) {
        stop("Invalid value for `distinct_count` when calling AnalyticsApi$AggregatedFilteredUsage, `distinct_count` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AnalyticsApi$AggregatedFilteredUsage, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AnalyticsApi$AggregatedFilteredUsage, `descending` is not nullable")
      }

      if (!missing(`hide_unknown`) && is.null(`hide_unknown`)) {
        stop("Invalid value for `hide_unknown` when calling AnalyticsApi$AggregatedFilteredUsage, `hide_unknown` is not nullable")
      }

      if (!missing(`response_format`) && is.null(`response_format`)) {
        stop("Invalid value for `response_format` when calling AnalyticsApi$AggregatedFilteredUsage, `response_format` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling AnalyticsApi$AggregatedFilteredUsage, `_l` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AnalyticsApi$AggregatedFilteredUsage, `limit` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AnalyticsApi$AggregatedFilteredUsage, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AnalyticsApi$AggregatedFilteredUsage, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["applicationId"]] <- `application_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["deviceType"]] <- `device_type`

      query_params[["device"]] <- `device`

      query_params[["deviceOS"]] <- `device_os`

      query_params[["gender"]] <- `gender`

      query_params[["ageGroup"]] <- `age_group`

      query_params[["country"]] <- `country`

      query_params[["state"]] <- `state`

      query_params[["city"]] <- `city`

      query_params[["zip"]] <- `zip`

      query_params[["model"]] <- `model`

      query_params[["tag"]] <- `tag`

      query_params[["userAccountId"]] <- `user_account_id`

      query_params[["userAccountDisplay"]] <- `user_account_display`

      query_params[["userAccountUsername"]] <- `user_account_username`

      if (!is.null(`group_by_root`) && !(`group_by_root` %in% c("TAG_COUNT", "TAG", "TOKEN", "MODEL", "DEVICE_TYPE", "DEVICE", "DEVICE_OS", "DEVICE_ID", "IP_ADDRESS", "STATE", "CITY", "ZIP", "COUNTRY", "CREATED", "UPDATED", "LAST_UPDATED", "CLIENT_TIME", "ACTIVE", "CUSTOM_ID", "CUSTOM_TYPE", "CUSTOM_VALUE", "CUSTOM_VALUE2", "CUSTOM_LONG", "CUSTOM_LONG2", "CUSTOM_MESSAGE", "CUSTOM_MESSAGE2", "ACCOUNT_ID", "ACCOUNT_USERNAME", "ACCOUNT_DISPLAY", "ACCOUNT_CREATED", "ACCOUNT_GENDER", "ACCOUNT_AGE_GROUP", "APPLICATION_ID", "APPLICATION_KEY", "APPLICATION_NAME"))) {
        stop("Invalid value for group_by_root when calling AnalyticsApi$AggregatedFilteredUsage. Must be [TAG_COUNT, TAG, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, CREATED, UPDATED, LAST_UPDATED, CLIENT_TIME, ACTIVE, CUSTOM_ID, CUSTOM_TYPE, CUSTOM_VALUE, CUSTOM_VALUE2, CUSTOM_LONG, CUSTOM_LONG2, CUSTOM_MESSAGE, CUSTOM_MESSAGE2, ACCOUNT_ID, ACCOUNT_USERNAME, ACCOUNT_DISPLAY, ACCOUNT_CREATED, ACCOUNT_GENDER, ACCOUNT_AGE_GROUP, APPLICATION_ID, APPLICATION_KEY, APPLICATION_NAME].")
      }
      query_params[["groupByRoot"]] <- `group_by_root`

      if (!is.null(`group_by`) && !(`group_by` %in% c("TAG_COUNT", "TAG", "TOKEN", "MODEL", "DEVICE_TYPE", "DEVICE", "DEVICE_OS", "DEVICE_ID", "IP_ADDRESS", "STATE", "CITY", "ZIP", "COUNTRY", "CREATED", "UPDATED", "LAST_UPDATED", "CLIENT_TIME", "ACTIVE", "CUSTOM_ID", "CUSTOM_TYPE", "CUSTOM_VALUE", "CUSTOM_VALUE2", "CUSTOM_LONG", "CUSTOM_LONG2", "CUSTOM_MESSAGE", "CUSTOM_MESSAGE2", "ACCOUNT_ID", "ACCOUNT_USERNAME", "ACCOUNT_DISPLAY", "ACCOUNT_CREATED", "ACCOUNT_GENDER", "ACCOUNT_AGE_GROUP", "APPLICATION_ID", "APPLICATION_KEY", "APPLICATION_NAME"))) {
        stop("Invalid value for group_by when calling AnalyticsApi$AggregatedFilteredUsage. Must be [TAG_COUNT, TAG, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, CREATED, UPDATED, LAST_UPDATED, CLIENT_TIME, ACTIVE, CUSTOM_ID, CUSTOM_TYPE, CUSTOM_VALUE, CUSTOM_VALUE2, CUSTOM_LONG, CUSTOM_LONG2, CUSTOM_MESSAGE, CUSTOM_MESSAGE2, ACCOUNT_ID, ACCOUNT_USERNAME, ACCOUNT_DISPLAY, ACCOUNT_CREATED, ACCOUNT_GENDER, ACCOUNT_AGE_GROUP, APPLICATION_ID, APPLICATION_KEY, APPLICATION_NAME].")
      }
      query_params[["groupBy"]] <- `group_by`

      if (!is.null(`distinct_count`) && !(`distinct_count` %in% c("TAG_COUNT", "TAG", "TOKEN", "MODEL", "DEVICE_TYPE", "DEVICE", "DEVICE_OS", "DEVICE_ID", "IP_ADDRESS", "STATE", "CITY", "ZIP", "COUNTRY", "CREATED", "UPDATED", "LAST_UPDATED", "CLIENT_TIME", "ACTIVE", "CUSTOM_ID", "CUSTOM_TYPE", "CUSTOM_VALUE", "CUSTOM_VALUE2", "CUSTOM_LONG", "CUSTOM_LONG2", "CUSTOM_MESSAGE", "CUSTOM_MESSAGE2", "ACCOUNT_ID", "ACCOUNT_USERNAME", "ACCOUNT_DISPLAY", "ACCOUNT_CREATED", "ACCOUNT_GENDER", "ACCOUNT_AGE_GROUP", "APPLICATION_ID", "APPLICATION_KEY", "APPLICATION_NAME"))) {
        stop("Invalid value for distinct_count when calling AnalyticsApi$AggregatedFilteredUsage. Must be [TAG_COUNT, TAG, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, CREATED, UPDATED, LAST_UPDATED, CLIENT_TIME, ACTIVE, CUSTOM_ID, CUSTOM_TYPE, CUSTOM_VALUE, CUSTOM_VALUE2, CUSTOM_LONG, CUSTOM_LONG2, CUSTOM_MESSAGE, CUSTOM_MESSAGE2, ACCOUNT_ID, ACCOUNT_USERNAME, ACCOUNT_DISPLAY, ACCOUNT_CREATED, ACCOUNT_GENDER, ACCOUNT_AGE_GROUP, APPLICATION_ID, APPLICATION_KEY, APPLICATION_NAME].")
      }
      query_params[["distinctCount"]] <- `distinct_count`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("TAG_COUNT", "TAG", "TOKEN", "MODEL", "DEVICE_TYPE", "DEVICE", "DEVICE_OS", "DEVICE_ID", "IP_ADDRESS", "STATE", "CITY", "ZIP", "COUNTRY", "CREATED", "UPDATED", "LAST_UPDATED", "CLIENT_TIME", "ACTIVE", "CUSTOM_ID", "CUSTOM_TYPE", "CUSTOM_VALUE", "CUSTOM_VALUE2", "CUSTOM_LONG", "CUSTOM_LONG2", "CUSTOM_MESSAGE", "CUSTOM_MESSAGE2", "ACCOUNT_ID", "ACCOUNT_USERNAME", "ACCOUNT_DISPLAY", "ACCOUNT_CREATED", "ACCOUNT_GENDER", "ACCOUNT_AGE_GROUP", "APPLICATION_ID", "APPLICATION_KEY", "APPLICATION_NAME"))) {
        stop("Invalid value for sort_field when calling AnalyticsApi$AggregatedFilteredUsage. Must be [TAG_COUNT, TAG, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, CREATED, UPDATED, LAST_UPDATED, CLIENT_TIME, ACTIVE, CUSTOM_ID, CUSTOM_TYPE, CUSTOM_VALUE, CUSTOM_VALUE2, CUSTOM_LONG, CUSTOM_LONG2, CUSTOM_MESSAGE, CUSTOM_MESSAGE2, ACCOUNT_ID, ACCOUNT_USERNAME, ACCOUNT_DISPLAY, ACCOUNT_CREATED, ACCOUNT_GENDER, ACCOUNT_AGE_GROUP, APPLICATION_ID, APPLICATION_KEY, APPLICATION_NAME].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["hideUnknown"]] <- `hide_unknown`

      if (!is.null(`response_format`) && !(`response_format` %in% c("HTML", "XML", "JSON", "CSV"))) {
        stop("Invalid value for response_format when calling AnalyticsApi$AggregatedFilteredUsage. Must be [HTML, XML, JSON, CSV].")
      }
      query_params[["responseFormat"]] <- `response_format`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/analytics/aggregatedFilteredUsage"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ChartData"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Filtered Usage
    #'
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param application_id (optional) This parameter is deprecated.
    #' @param app_key (optional) The application key used to filter results by application
    #' @param start_date (optional) Used to search for results after or equal to this date (UNIX time-stamp in milliseconds)
    #' @param end_date (optional) Used to search for results before or equal to this date (UNIX time-stamp in milliseconds)
    #' @param device_type (optional) The device type to filter results by (performs a LIKE search)
    #' @param device (optional) The device to filter results by (performs a LIKE search)
    #' @param device_os (optional) The device OS to filter results by (performs a LIKE search)
    #' @param gender (optional) The gender to filter results by {MALE, FEMALE}
    #' @param age_group (optional) Comma separated list of age groups to filter by {AGE_0_13, AGE_14_17, AGE_18_22, AGE_23_30, AGE_31_54, AGE_55_PLUS}
    #' @param country (optional) The country to filter results by (performs a wild-card search)
    #' @param state (optional) The state to filter results by (performs a wild-card search)
    #' @param city (optional) The city to filter results by (performs a wild-card search)
    #' @param zip (optional) The zip to filter results by (performs a wild-card search)
    #' @param model (optional) The model to filter results by (performs a wild-card search)
    #' @param tag (optional) The tag to filter results by (performs a wild-card search)
    #' @param user_account_id (optional) The account id to filter results for a particular user
    #' @param user_account_display (optional) The user display to filter results by (performs a wild-card search)
    #' @param user_account_username (optional) The username to filter results by (performs a wild-card search)
    #' @param custom_id (optional) The ID for the custom table column
    #' @param custom_type (optional) The type defined for the custom table column
    #' @param custom_value (optional) The value for the custom table column
    #' @param custom_value2 (optional) The value for another custom table column
    #' @param custom_long (optional) The value that supports Longs for custom table column
    #' @param custom_long2 (optional) The value that supports Longs for custom table column
    #' @param custom_message (optional) The message for the custom table column
    #' @param custom_message2 (optional) The message for the custom table column
    #' @param group_by (optional) Determines how to group results. For example, passing in 'CREATED' will return results by date. Passing in 'ACCOUNT' will return results by user {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID}
    #' @param distinct_count (optional) Performs a unique query on the specified column. For example, passing in 'ACCOUNT' will return the number of unique users. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID}
    #' @param sum_column (optional) sum of the column values
    #' @param sort_field (optional) Determines whether the results will be sorted by day or by number of hits {CREATED, TAG_COUNT}
    #' @param descending (optional) Determines whether to return results in descending order
    #' @param hide_unknown (optional) Determines whether to return data that has empty or unknown values
    #' @param response_format (optional) Determines whether to return a JOSN or XML representation of the graph results
    #' @param _l (optional) This parameter is deprecated.
    #' @param limit (optional) Used to limit results to get a cleaner graph. The results that gets filtered out will be combined
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ChartData
    FilteredUsage = function(device_id = NULL, account_id = NULL, application_id = NULL, app_key = NULL, start_date = NULL, end_date = NULL, device_type = NULL, device = NULL, device_os = NULL, gender = NULL, age_group = NULL, country = NULL, state = NULL, city = NULL, zip = NULL, model = NULL, tag = NULL, user_account_id = NULL, user_account_display = NULL, user_account_username = NULL, custom_id = NULL, custom_type = NULL, custom_value = NULL, custom_value2 = NULL, custom_long = NULL, custom_long2 = NULL, custom_message = NULL, custom_message2 = NULL, group_by = NULL, distinct_count = NULL, sum_column = NULL, sort_field = NULL, descending = NULL, hide_unknown = NULL, response_format = NULL, _l = NULL, limit = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$FilteredUsageWithHttpInfo(device_id, account_id, application_id, app_key, start_date, end_date, device_type, device, device_os, gender, age_group, country, state, city, zip, model, tag, user_account_id, user_account_display, user_account_username, custom_id, custom_type, custom_value, custom_value2, custom_long, custom_long2, custom_message, custom_message2, group_by, distinct_count, sum_column, sort_field, descending, hide_unknown, response_format, _l, limit, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Filtered Usage
    #'
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param application_id (optional) This parameter is deprecated.
    #' @param app_key (optional) The application key used to filter results by application
    #' @param start_date (optional) Used to search for results after or equal to this date (UNIX time-stamp in milliseconds)
    #' @param end_date (optional) Used to search for results before or equal to this date (UNIX time-stamp in milliseconds)
    #' @param device_type (optional) The device type to filter results by (performs a LIKE search)
    #' @param device (optional) The device to filter results by (performs a LIKE search)
    #' @param device_os (optional) The device OS to filter results by (performs a LIKE search)
    #' @param gender (optional) The gender to filter results by {MALE, FEMALE}
    #' @param age_group (optional) Comma separated list of age groups to filter by {AGE_0_13, AGE_14_17, AGE_18_22, AGE_23_30, AGE_31_54, AGE_55_PLUS}
    #' @param country (optional) The country to filter results by (performs a wild-card search)
    #' @param state (optional) The state to filter results by (performs a wild-card search)
    #' @param city (optional) The city to filter results by (performs a wild-card search)
    #' @param zip (optional) The zip to filter results by (performs a wild-card search)
    #' @param model (optional) The model to filter results by (performs a wild-card search)
    #' @param tag (optional) The tag to filter results by (performs a wild-card search)
    #' @param user_account_id (optional) The account id to filter results for a particular user
    #' @param user_account_display (optional) The user display to filter results by (performs a wild-card search)
    #' @param user_account_username (optional) The username to filter results by (performs a wild-card search)
    #' @param custom_id (optional) The ID for the custom table column
    #' @param custom_type (optional) The type defined for the custom table column
    #' @param custom_value (optional) The value for the custom table column
    #' @param custom_value2 (optional) The value for another custom table column
    #' @param custom_long (optional) The value that supports Longs for custom table column
    #' @param custom_long2 (optional) The value that supports Longs for custom table column
    #' @param custom_message (optional) The message for the custom table column
    #' @param custom_message2 (optional) The message for the custom table column
    #' @param group_by (optional) Determines how to group results. For example, passing in 'CREATED' will return results by date. Passing in 'ACCOUNT' will return results by user {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID}
    #' @param distinct_count (optional) Performs a unique query on the specified column. For example, passing in 'ACCOUNT' will return the number of unique users. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID}
    #' @param sum_column (optional) sum of the column values
    #' @param sort_field (optional) Determines whether the results will be sorted by day or by number of hits {CREATED, TAG_COUNT}
    #' @param descending (optional) Determines whether to return results in descending order
    #' @param hide_unknown (optional) Determines whether to return data that has empty or unknown values
    #' @param response_format (optional) Determines whether to return a JOSN or XML representation of the graph results
    #' @param _l (optional) This parameter is deprecated.
    #' @param limit (optional) Used to limit results to get a cleaner graph. The results that gets filtered out will be combined
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ChartData) with additional information such as HTTP status code, headers
    FilteredUsageWithHttpInfo = function(device_id = NULL, account_id = NULL, application_id = NULL, app_key = NULL, start_date = NULL, end_date = NULL, device_type = NULL, device = NULL, device_os = NULL, gender = NULL, age_group = NULL, country = NULL, state = NULL, city = NULL, zip = NULL, model = NULL, tag = NULL, user_account_id = NULL, user_account_display = NULL, user_account_username = NULL, custom_id = NULL, custom_type = NULL, custom_value = NULL, custom_value2 = NULL, custom_long = NULL, custom_long2 = NULL, custom_message = NULL, custom_message2 = NULL, group_by = NULL, distinct_count = NULL, sum_column = NULL, sort_field = NULL, descending = NULL, hide_unknown = NULL, response_format = NULL, _l = NULL, limit = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AnalyticsApi$FilteredUsage, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AnalyticsApi$FilteredUsage, `account_id` is not nullable")
      }

      if (!missing(`application_id`) && is.null(`application_id`)) {
        stop("Invalid value for `application_id` when calling AnalyticsApi$FilteredUsage, `application_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AnalyticsApi$FilteredUsage, `app_key` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling AnalyticsApi$FilteredUsage, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling AnalyticsApi$FilteredUsage, `end_date` is not nullable")
      }

      if (!missing(`device_type`) && is.null(`device_type`)) {
        stop("Invalid value for `device_type` when calling AnalyticsApi$FilteredUsage, `device_type` is not nullable")
      }

      if (!missing(`device`) && is.null(`device`)) {
        stop("Invalid value for `device` when calling AnalyticsApi$FilteredUsage, `device` is not nullable")
      }

      if (!missing(`device_os`) && is.null(`device_os`)) {
        stop("Invalid value for `device_os` when calling AnalyticsApi$FilteredUsage, `device_os` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling AnalyticsApi$FilteredUsage, `gender` is not nullable")
      }

      if (!missing(`age_group`) && is.null(`age_group`)) {
        stop("Invalid value for `age_group` when calling AnalyticsApi$FilteredUsage, `age_group` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling AnalyticsApi$FilteredUsage, `country` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling AnalyticsApi$FilteredUsage, `state` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling AnalyticsApi$FilteredUsage, `city` is not nullable")
      }

      if (!missing(`zip`) && is.null(`zip`)) {
        stop("Invalid value for `zip` when calling AnalyticsApi$FilteredUsage, `zip` is not nullable")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling AnalyticsApi$FilteredUsage, `model` is not nullable")
      }

      if (!missing(`tag`) && is.null(`tag`)) {
        stop("Invalid value for `tag` when calling AnalyticsApi$FilteredUsage, `tag` is not nullable")
      }

      if (!missing(`user_account_id`) && is.null(`user_account_id`)) {
        stop("Invalid value for `user_account_id` when calling AnalyticsApi$FilteredUsage, `user_account_id` is not nullable")
      }

      if (!missing(`user_account_display`) && is.null(`user_account_display`)) {
        stop("Invalid value for `user_account_display` when calling AnalyticsApi$FilteredUsage, `user_account_display` is not nullable")
      }

      if (!missing(`user_account_username`) && is.null(`user_account_username`)) {
        stop("Invalid value for `user_account_username` when calling AnalyticsApi$FilteredUsage, `user_account_username` is not nullable")
      }

      if (!missing(`custom_id`) && is.null(`custom_id`)) {
        stop("Invalid value for `custom_id` when calling AnalyticsApi$FilteredUsage, `custom_id` is not nullable")
      }

      if (!missing(`custom_type`) && is.null(`custom_type`)) {
        stop("Invalid value for `custom_type` when calling AnalyticsApi$FilteredUsage, `custom_type` is not nullable")
      }

      if (!missing(`custom_value`) && is.null(`custom_value`)) {
        stop("Invalid value for `custom_value` when calling AnalyticsApi$FilteredUsage, `custom_value` is not nullable")
      }

      if (!missing(`custom_value2`) && is.null(`custom_value2`)) {
        stop("Invalid value for `custom_value2` when calling AnalyticsApi$FilteredUsage, `custom_value2` is not nullable")
      }

      if (!missing(`custom_long`) && is.null(`custom_long`)) {
        stop("Invalid value for `custom_long` when calling AnalyticsApi$FilteredUsage, `custom_long` is not nullable")
      }

      if (!missing(`custom_long2`) && is.null(`custom_long2`)) {
        stop("Invalid value for `custom_long2` when calling AnalyticsApi$FilteredUsage, `custom_long2` is not nullable")
      }

      if (!missing(`custom_message`) && is.null(`custom_message`)) {
        stop("Invalid value for `custom_message` when calling AnalyticsApi$FilteredUsage, `custom_message` is not nullable")
      }

      if (!missing(`custom_message2`) && is.null(`custom_message2`)) {
        stop("Invalid value for `custom_message2` when calling AnalyticsApi$FilteredUsage, `custom_message2` is not nullable")
      }

      if (!missing(`group_by`) && is.null(`group_by`)) {
        stop("Invalid value for `group_by` when calling AnalyticsApi$FilteredUsage, `group_by` is not nullable")
      }

      if (!missing(`distinct_count`) && is.null(`distinct_count`)) {
        stop("Invalid value for `distinct_count` when calling AnalyticsApi$FilteredUsage, `distinct_count` is not nullable")
      }

      if (!missing(`sum_column`) && is.null(`sum_column`)) {
        stop("Invalid value for `sum_column` when calling AnalyticsApi$FilteredUsage, `sum_column` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AnalyticsApi$FilteredUsage, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AnalyticsApi$FilteredUsage, `descending` is not nullable")
      }

      if (!missing(`hide_unknown`) && is.null(`hide_unknown`)) {
        stop("Invalid value for `hide_unknown` when calling AnalyticsApi$FilteredUsage, `hide_unknown` is not nullable")
      }

      if (!missing(`response_format`) && is.null(`response_format`)) {
        stop("Invalid value for `response_format` when calling AnalyticsApi$FilteredUsage, `response_format` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling AnalyticsApi$FilteredUsage, `_l` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AnalyticsApi$FilteredUsage, `limit` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AnalyticsApi$FilteredUsage, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AnalyticsApi$FilteredUsage, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["applicationId"]] <- `application_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["deviceType"]] <- `device_type`

      query_params[["device"]] <- `device`

      query_params[["deviceOS"]] <- `device_os`

      query_params[["gender"]] <- `gender`

      query_params[["ageGroup"]] <- `age_group`

      query_params[["country"]] <- `country`

      query_params[["state"]] <- `state`

      query_params[["city"]] <- `city`

      query_params[["zip"]] <- `zip`

      query_params[["model"]] <- `model`

      query_params[["tag"]] <- `tag`

      query_params[["userAccountId"]] <- `user_account_id`

      query_params[["userAccountDisplay"]] <- `user_account_display`

      query_params[["userAccountUsername"]] <- `user_account_username`

      query_params[["customId"]] <- `custom_id`

      query_params[["customType"]] <- `custom_type`

      query_params[["customValue"]] <- `custom_value`

      query_params[["customValue2"]] <- `custom_value2`

      query_params[["customLong"]] <- `custom_long`

      query_params[["customLong2"]] <- `custom_long2`

      query_params[["customMessage"]] <- `custom_message`

      query_params[["customMessage2"]] <- `custom_message2`

      if (!is.null(`group_by`) && !(`group_by` %in% c("TAG_COUNT", "TAG", "TOKEN", "MODEL", "DEVICE_TYPE", "DEVICE", "DEVICE_OS", "DEVICE_ID", "IP_ADDRESS", "STATE", "CITY", "ZIP", "COUNTRY", "CREATED", "UPDATED", "LAST_UPDATED", "CLIENT_TIME", "ACTIVE", "CUSTOM_ID", "CUSTOM_TYPE", "CUSTOM_VALUE", "CUSTOM_VALUE2", "CUSTOM_LONG", "CUSTOM_LONG2", "CUSTOM_MESSAGE", "CUSTOM_MESSAGE2", "ACCOUNT_ID", "ACCOUNT_USERNAME", "ACCOUNT_DISPLAY", "ACCOUNT_CREATED", "ACCOUNT_GENDER", "ACCOUNT_AGE_GROUP", "APPLICATION_ID", "APPLICATION_KEY", "APPLICATION_NAME"))) {
        stop("Invalid value for group_by when calling AnalyticsApi$FilteredUsage. Must be [TAG_COUNT, TAG, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, CREATED, UPDATED, LAST_UPDATED, CLIENT_TIME, ACTIVE, CUSTOM_ID, CUSTOM_TYPE, CUSTOM_VALUE, CUSTOM_VALUE2, CUSTOM_LONG, CUSTOM_LONG2, CUSTOM_MESSAGE, CUSTOM_MESSAGE2, ACCOUNT_ID, ACCOUNT_USERNAME, ACCOUNT_DISPLAY, ACCOUNT_CREATED, ACCOUNT_GENDER, ACCOUNT_AGE_GROUP, APPLICATION_ID, APPLICATION_KEY, APPLICATION_NAME].")
      }
      query_params[["groupBy"]] <- `group_by`

      if (!is.null(`distinct_count`) && !(`distinct_count` %in% c("TAG_COUNT", "TAG", "TOKEN", "MODEL", "DEVICE_TYPE", "DEVICE", "DEVICE_OS", "DEVICE_ID", "IP_ADDRESS", "STATE", "CITY", "ZIP", "COUNTRY", "CREATED", "UPDATED", "LAST_UPDATED", "CLIENT_TIME", "ACTIVE", "CUSTOM_ID", "CUSTOM_TYPE", "CUSTOM_VALUE", "CUSTOM_VALUE2", "CUSTOM_LONG", "CUSTOM_LONG2", "CUSTOM_MESSAGE", "CUSTOM_MESSAGE2", "ACCOUNT_ID", "ACCOUNT_USERNAME", "ACCOUNT_DISPLAY", "ACCOUNT_CREATED", "ACCOUNT_GENDER", "ACCOUNT_AGE_GROUP", "APPLICATION_ID", "APPLICATION_KEY", "APPLICATION_NAME"))) {
        stop("Invalid value for distinct_count when calling AnalyticsApi$FilteredUsage. Must be [TAG_COUNT, TAG, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, CREATED, UPDATED, LAST_UPDATED, CLIENT_TIME, ACTIVE, CUSTOM_ID, CUSTOM_TYPE, CUSTOM_VALUE, CUSTOM_VALUE2, CUSTOM_LONG, CUSTOM_LONG2, CUSTOM_MESSAGE, CUSTOM_MESSAGE2, ACCOUNT_ID, ACCOUNT_USERNAME, ACCOUNT_DISPLAY, ACCOUNT_CREATED, ACCOUNT_GENDER, ACCOUNT_AGE_GROUP, APPLICATION_ID, APPLICATION_KEY, APPLICATION_NAME].")
      }
      query_params[["distinctCount"]] <- `distinct_count`

      if (!is.null(`sum_column`) && !(`sum_column` %in% c("TAG_COUNT", "TAG", "TOKEN", "MODEL", "DEVICE_TYPE", "DEVICE", "DEVICE_OS", "DEVICE_ID", "IP_ADDRESS", "STATE", "CITY", "ZIP", "COUNTRY", "CREATED", "UPDATED", "LAST_UPDATED", "CLIENT_TIME", "ACTIVE", "CUSTOM_ID", "CUSTOM_TYPE", "CUSTOM_VALUE", "CUSTOM_VALUE2", "CUSTOM_LONG", "CUSTOM_LONG2", "CUSTOM_MESSAGE", "CUSTOM_MESSAGE2", "ACCOUNT_ID", "ACCOUNT_USERNAME", "ACCOUNT_DISPLAY", "ACCOUNT_CREATED", "ACCOUNT_GENDER", "ACCOUNT_AGE_GROUP", "APPLICATION_ID", "APPLICATION_KEY", "APPLICATION_NAME"))) {
        stop("Invalid value for sum_column when calling AnalyticsApi$FilteredUsage. Must be [TAG_COUNT, TAG, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, CREATED, UPDATED, LAST_UPDATED, CLIENT_TIME, ACTIVE, CUSTOM_ID, CUSTOM_TYPE, CUSTOM_VALUE, CUSTOM_VALUE2, CUSTOM_LONG, CUSTOM_LONG2, CUSTOM_MESSAGE, CUSTOM_MESSAGE2, ACCOUNT_ID, ACCOUNT_USERNAME, ACCOUNT_DISPLAY, ACCOUNT_CREATED, ACCOUNT_GENDER, ACCOUNT_AGE_GROUP, APPLICATION_ID, APPLICATION_KEY, APPLICATION_NAME].")
      }
      query_params[["sumColumn"]] <- `sum_column`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("TAG_COUNT", "TAG", "TOKEN", "MODEL", "DEVICE_TYPE", "DEVICE", "DEVICE_OS", "DEVICE_ID", "IP_ADDRESS", "STATE", "CITY", "ZIP", "COUNTRY", "CREATED", "UPDATED", "LAST_UPDATED", "CLIENT_TIME", "ACTIVE", "CUSTOM_ID", "CUSTOM_TYPE", "CUSTOM_VALUE", "CUSTOM_VALUE2", "CUSTOM_LONG", "CUSTOM_LONG2", "CUSTOM_MESSAGE", "CUSTOM_MESSAGE2", "ACCOUNT_ID", "ACCOUNT_USERNAME", "ACCOUNT_DISPLAY", "ACCOUNT_CREATED", "ACCOUNT_GENDER", "ACCOUNT_AGE_GROUP", "APPLICATION_ID", "APPLICATION_KEY", "APPLICATION_NAME"))) {
        stop("Invalid value for sort_field when calling AnalyticsApi$FilteredUsage. Must be [TAG_COUNT, TAG, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, CREATED, UPDATED, LAST_UPDATED, CLIENT_TIME, ACTIVE, CUSTOM_ID, CUSTOM_TYPE, CUSTOM_VALUE, CUSTOM_VALUE2, CUSTOM_LONG, CUSTOM_LONG2, CUSTOM_MESSAGE, CUSTOM_MESSAGE2, ACCOUNT_ID, ACCOUNT_USERNAME, ACCOUNT_DISPLAY, ACCOUNT_CREATED, ACCOUNT_GENDER, ACCOUNT_AGE_GROUP, APPLICATION_ID, APPLICATION_KEY, APPLICATION_NAME].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["hideUnknown"]] <- `hide_unknown`

      if (!is.null(`response_format`) && !(`response_format` %in% c("HTML", "XML", "JSON", "CSV"))) {
        stop("Invalid value for response_format when calling AnalyticsApi$FilteredUsage. Must be [HTML, XML, JSON, CSV].")
      }
      query_params[["responseFormat"]] <- `response_format`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/analytics/filteredUsage"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ChartData"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Usage Record
    #'
    #' @param tag The tag to apply: the name of the action or thing being logged.
    #' @param device_id (optional) The client deviceID
    #' @param account_id (optional) The logged in user ID
    #' @param application_id (optional) This parameter is deprecated. This is deprecated, use appKey.
    #' @param app_key (optional) The application key unique to each application.
    #' @param app_version (optional) The current build version of the application
    #' @param device (optional) The name of the device being used (iPhone5,1 , HTC Nexus One, x86_64, etc.)
    #' @param device_type (optional) The type of device (Handheld or Desktop)
    #' @param device_os (optional) The operating system that the device is using (iPhone OS 5.0, Android OS 2.3.6, Windows 7 Service Pack 1, etc)
    #' @param model (optional) The model of the device (iPhone5,1 , Nexus One, etc.)
    #' @param latitude (optional) The current position of the user
    #' @param longitude (optional) The current position of the user
    #' @param custom_id (optional) A customId used to associate the usage record with some other record in the system
    #' @param custom_type (optional) The type of record the customId is recorded for
    #' @param achievement_increment (optional) This parameter is deprecated. This is deprecated, use \"customValue\". Determines how many achievement points and/or ranking points the user gets if this analytic is associated with an achievement
    #' @param city (optional) The current city of the user
    #' @param state (optional) The current state of the user
    #' @param country (optional) The current country of the user
    #' @param zip (optional) The current zip of the user
    #' @param location_description (optional) The general description of the user's location
    #' @param client_time (optional) Timestamp sent from device
    #' @param error_message (optional) This parameter is deprecated.
    #' @param ip (optional) the ip address of the client device
    #' @param user_agent (optional) this is a description of the client (might contain the device's OS, browser version etc - its a common term)
    #' @param background_event (optional) This is deprecated.
    #' @param custom_message (optional) a custom message for the usage record
    #' @param custom_message2 (optional) a custom message for the usage record
    #' @param custom_value (optional) a custom value for the usage record
    #' @param custom_value2 (optional) a custom value for the usage record
    #' @param custom_long (optional) a custom long value for the usage record
    #' @param custom_long2 (optional) a custom long value for the usage record
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    Usage = function(tag, device_id = NULL, account_id = NULL, application_id = NULL, app_key = NULL, app_version = NULL, device = NULL, device_type = NULL, device_os = NULL, model = NULL, latitude = NULL, longitude = NULL, custom_id = NULL, custom_type = NULL, achievement_increment = NULL, city = NULL, state = NULL, country = NULL, zip = NULL, location_description = NULL, client_time = NULL, error_message = NULL, ip = NULL, user_agent = NULL, background_event = NULL, custom_message = NULL, custom_message2 = NULL, custom_value = NULL, custom_value2 = NULL, custom_long = NULL, custom_long2 = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UsageWithHttpInfo(tag, device_id, account_id, application_id, app_key, app_version, device, device_type, device_os, model, latitude, longitude, custom_id, custom_type, achievement_increment, city, state, country, zip, location_description, client_time, error_message, ip, user_agent, background_event, custom_message, custom_message2, custom_value, custom_value2, custom_long, custom_long2, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Usage Record
    #'
    #' @param tag The tag to apply: the name of the action or thing being logged.
    #' @param device_id (optional) The client deviceID
    #' @param account_id (optional) The logged in user ID
    #' @param application_id (optional) This parameter is deprecated. This is deprecated, use appKey.
    #' @param app_key (optional) The application key unique to each application.
    #' @param app_version (optional) The current build version of the application
    #' @param device (optional) The name of the device being used (iPhone5,1 , HTC Nexus One, x86_64, etc.)
    #' @param device_type (optional) The type of device (Handheld or Desktop)
    #' @param device_os (optional) The operating system that the device is using (iPhone OS 5.0, Android OS 2.3.6, Windows 7 Service Pack 1, etc)
    #' @param model (optional) The model of the device (iPhone5,1 , Nexus One, etc.)
    #' @param latitude (optional) The current position of the user
    #' @param longitude (optional) The current position of the user
    #' @param custom_id (optional) A customId used to associate the usage record with some other record in the system
    #' @param custom_type (optional) The type of record the customId is recorded for
    #' @param achievement_increment (optional) This parameter is deprecated. This is deprecated, use \"customValue\". Determines how many achievement points and/or ranking points the user gets if this analytic is associated with an achievement
    #' @param city (optional) The current city of the user
    #' @param state (optional) The current state of the user
    #' @param country (optional) The current country of the user
    #' @param zip (optional) The current zip of the user
    #' @param location_description (optional) The general description of the user's location
    #' @param client_time (optional) Timestamp sent from device
    #' @param error_message (optional) This parameter is deprecated.
    #' @param ip (optional) the ip address of the client device
    #' @param user_agent (optional) this is a description of the client (might contain the device's OS, browser version etc - its a common term)
    #' @param background_event (optional) This is deprecated.
    #' @param custom_message (optional) a custom message for the usage record
    #' @param custom_message2 (optional) a custom message for the usage record
    #' @param custom_value (optional) a custom value for the usage record
    #' @param custom_value2 (optional) a custom value for the usage record
    #' @param custom_long (optional) a custom long value for the usage record
    #' @param custom_long2 (optional) a custom long value for the usage record
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UsageWithHttpInfo = function(tag, device_id = NULL, account_id = NULL, application_id = NULL, app_key = NULL, app_version = NULL, device = NULL, device_type = NULL, device_os = NULL, model = NULL, latitude = NULL, longitude = NULL, custom_id = NULL, custom_type = NULL, achievement_increment = NULL, city = NULL, state = NULL, country = NULL, zip = NULL, location_description = NULL, client_time = NULL, error_message = NULL, ip = NULL, user_agent = NULL, background_event = NULL, custom_message = NULL, custom_message2 = NULL, custom_value = NULL, custom_value2 = NULL, custom_long = NULL, custom_long2 = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`tag`)) {
        stop("Missing required parameter `tag`.")
      }

      if (!missing(`tag`) && is.null(`tag`)) {
        stop("Invalid value for `tag` when calling AnalyticsApi$Usage, `tag` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AnalyticsApi$Usage, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AnalyticsApi$Usage, `account_id` is not nullable")
      }

      if (!missing(`application_id`) && is.null(`application_id`)) {
        stop("Invalid value for `application_id` when calling AnalyticsApi$Usage, `application_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AnalyticsApi$Usage, `app_key` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling AnalyticsApi$Usage, `app_version` is not nullable")
      }

      if (!missing(`device`) && is.null(`device`)) {
        stop("Invalid value for `device` when calling AnalyticsApi$Usage, `device` is not nullable")
      }

      if (!missing(`device_type`) && is.null(`device_type`)) {
        stop("Invalid value for `device_type` when calling AnalyticsApi$Usage, `device_type` is not nullable")
      }

      if (!missing(`device_os`) && is.null(`device_os`)) {
        stop("Invalid value for `device_os` when calling AnalyticsApi$Usage, `device_os` is not nullable")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling AnalyticsApi$Usage, `model` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AnalyticsApi$Usage, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AnalyticsApi$Usage, `longitude` is not nullable")
      }

      if (!missing(`custom_id`) && is.null(`custom_id`)) {
        stop("Invalid value for `custom_id` when calling AnalyticsApi$Usage, `custom_id` is not nullable")
      }

      if (!missing(`custom_type`) && is.null(`custom_type`)) {
        stop("Invalid value for `custom_type` when calling AnalyticsApi$Usage, `custom_type` is not nullable")
      }

      if (!missing(`achievement_increment`) && is.null(`achievement_increment`)) {
        stop("Invalid value for `achievement_increment` when calling AnalyticsApi$Usage, `achievement_increment` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling AnalyticsApi$Usage, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling AnalyticsApi$Usage, `state` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling AnalyticsApi$Usage, `country` is not nullable")
      }

      if (!missing(`zip`) && is.null(`zip`)) {
        stop("Invalid value for `zip` when calling AnalyticsApi$Usage, `zip` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling AnalyticsApi$Usage, `location_description` is not nullable")
      }

      if (!missing(`client_time`) && is.null(`client_time`)) {
        stop("Invalid value for `client_time` when calling AnalyticsApi$Usage, `client_time` is not nullable")
      }

      if (!missing(`error_message`) && is.null(`error_message`)) {
        stop("Invalid value for `error_message` when calling AnalyticsApi$Usage, `error_message` is not nullable")
      }

      if (!missing(`ip`) && is.null(`ip`)) {
        stop("Invalid value for `ip` when calling AnalyticsApi$Usage, `ip` is not nullable")
      }

      if (!missing(`user_agent`) && is.null(`user_agent`)) {
        stop("Invalid value for `user_agent` when calling AnalyticsApi$Usage, `user_agent` is not nullable")
      }

      if (!missing(`background_event`) && is.null(`background_event`)) {
        stop("Invalid value for `background_event` when calling AnalyticsApi$Usage, `background_event` is not nullable")
      }

      if (!missing(`custom_message`) && is.null(`custom_message`)) {
        stop("Invalid value for `custom_message` when calling AnalyticsApi$Usage, `custom_message` is not nullable")
      }

      if (!missing(`custom_message2`) && is.null(`custom_message2`)) {
        stop("Invalid value for `custom_message2` when calling AnalyticsApi$Usage, `custom_message2` is not nullable")
      }

      if (!missing(`custom_value`) && is.null(`custom_value`)) {
        stop("Invalid value for `custom_value` when calling AnalyticsApi$Usage, `custom_value` is not nullable")
      }

      if (!missing(`custom_value2`) && is.null(`custom_value2`)) {
        stop("Invalid value for `custom_value2` when calling AnalyticsApi$Usage, `custom_value2` is not nullable")
      }

      if (!missing(`custom_long`) && is.null(`custom_long`)) {
        stop("Invalid value for `custom_long` when calling AnalyticsApi$Usage, `custom_long` is not nullable")
      }

      if (!missing(`custom_long2`) && is.null(`custom_long2`)) {
        stop("Invalid value for `custom_long2` when calling AnalyticsApi$Usage, `custom_long2` is not nullable")
      }

      query_params[["tag"]] <- `tag`

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["applicationId"]] <- `application_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["appVersion"]] <- `app_version`

      query_params[["device"]] <- `device`

      query_params[["deviceType"]] <- `device_type`

      query_params[["deviceOS"]] <- `device_os`

      query_params[["model"]] <- `model`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["customId"]] <- `custom_id`

      query_params[["customType"]] <- `custom_type`

      query_params[["achievementIncrement"]] <- `achievement_increment`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["country"]] <- `country`

      query_params[["zip"]] <- `zip`

      query_params[["locationDescription"]] <- `location_description`

      query_params[["clientTime"]] <- `client_time`

      query_params[["errorMessage"]] <- `error_message`

      query_params[["ip"]] <- `ip`

      query_params[["userAgent"]] <- `user_agent`

      query_params[["backgroundEvent"]] <- `background_event`

      query_params[["customMessage"]] <- `custom_message`

      query_params[["customMessage2"]] <- `custom_message2`

      query_params[["customValue"]] <- `custom_value`

      query_params[["customValue2"]] <- `custom_value2`

      query_params[["customLong"]] <- `custom_long`

      query_params[["customLong2"]] <- `custom_long2`

      local_var_url_path <- "/analytics/usage"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Multiple Usage Records
    #'
    #' @param app_key The application key unique to each application.
    #' @param device The name of the device being used (iPhone5,1 , HTC Nexus One, x86_64, etc.)
    #' @param data The analytic data AnalyticListResponse
    #' @param device_id (optional) The unique id of the device making the request
    #' @param account_id (optional) The account ID of the logged in user making the request
    #' @param app_version (optional) The current build version of the application
    #' @param device_type (optional) The type of device (Handheld or Desktop)
    #' @param device_os (optional) The operating system that the device is using (iPhone OS 5.0, Android OS 2.3.6, Windows 7 Service Pack 1, etc.)
    #' @param model (optional) The model of the device (iPhone5,1 , Nexus One, etc.)
    #' @param update_ranking (optional) Will create a leaderboard if one does not exist for the \"tag\" yet
    #' @param return_summary_response (optional) Returns a summary response of the achievements that have been completed due to the analytics
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UsageBatch = function(app_key, device, data, device_id = NULL, account_id = NULL, app_version = NULL, device_type = NULL, device_os = NULL, model = NULL, update_ranking = NULL, return_summary_response = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UsageBatchWithHttpInfo(app_key, device, data, device_id, account_id, app_version, device_type, device_os, model, update_ranking, return_summary_response, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Multiple Usage Records
    #'
    #' @param app_key The application key unique to each application.
    #' @param device The name of the device being used (iPhone5,1 , HTC Nexus One, x86_64, etc.)
    #' @param data The analytic data AnalyticListResponse
    #' @param device_id (optional) The unique id of the device making the request
    #' @param account_id (optional) The account ID of the logged in user making the request
    #' @param app_version (optional) The current build version of the application
    #' @param device_type (optional) The type of device (Handheld or Desktop)
    #' @param device_os (optional) The operating system that the device is using (iPhone OS 5.0, Android OS 2.3.6, Windows 7 Service Pack 1, etc.)
    #' @param model (optional) The model of the device (iPhone5,1 , Nexus One, etc.)
    #' @param update_ranking (optional) Will create a leaderboard if one does not exist for the \"tag\" yet
    #' @param return_summary_response (optional) Returns a summary response of the achievements that have been completed due to the analytics
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UsageBatchWithHttpInfo = function(app_key, device, data, device_id = NULL, account_id = NULL, app_version = NULL, device_type = NULL, device_os = NULL, model = NULL, update_ranking = NULL, return_summary_response = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`device`)) {
        stop("Missing required parameter `device`.")
      }

      if (missing(`data`)) {
        stop("Missing required parameter `data`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AnalyticsApi$UsageBatch, `app_key` is not nullable")
      }

      if (!missing(`device`) && is.null(`device`)) {
        stop("Invalid value for `device` when calling AnalyticsApi$UsageBatch, `device` is not nullable")
      }

      if (!missing(`data`) && is.null(`data`)) {
        stop("Invalid value for `data` when calling AnalyticsApi$UsageBatch, `data` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AnalyticsApi$UsageBatch, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AnalyticsApi$UsageBatch, `account_id` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling AnalyticsApi$UsageBatch, `app_version` is not nullable")
      }

      if (!missing(`device_type`) && is.null(`device_type`)) {
        stop("Invalid value for `device_type` when calling AnalyticsApi$UsageBatch, `device_type` is not nullable")
      }

      if (!missing(`device_os`) && is.null(`device_os`)) {
        stop("Invalid value for `device_os` when calling AnalyticsApi$UsageBatch, `device_os` is not nullable")
      }

      if (!missing(`model`) && is.null(`model`)) {
        stop("Invalid value for `model` when calling AnalyticsApi$UsageBatch, `model` is not nullable")
      }

      if (!missing(`update_ranking`) && is.null(`update_ranking`)) {
        stop("Invalid value for `update_ranking` when calling AnalyticsApi$UsageBatch, `update_ranking` is not nullable")
      }

      if (!missing(`return_summary_response`) && is.null(`return_summary_response`)) {
        stop("Invalid value for `return_summary_response` when calling AnalyticsApi$UsageBatch, `return_summary_response` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["appVersion"]] <- `app_version`

      query_params[["device"]] <- `device`

      query_params[["deviceType"]] <- `device_type`

      query_params[["deviceOS"]] <- `device_os`

      query_params[["model"]] <- `model`

      query_params[["data"]] <- `data`

      query_params[["updateRanking"]] <- `update_ranking`

      query_params[["returnSummaryResponse"]] <- `return_summary_response`

      local_var_url_path <- "/analytics/usage/batch"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
