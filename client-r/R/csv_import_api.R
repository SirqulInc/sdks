#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title CSVImport operations
#' @description CSVImportApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GetStatusCSV  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_batch_id <- 56 # integer | the id of the batch
#' var_response_group <- "response_group_example" # character | The group of categories to return: SUMMARY, DETAILS, ERRORS, OR ALL
#' var_start <- 56 # integer | the start of the pagination
#' var_limit <- 56 # integer | the limit of the pagination
#'
#' #Detail Status
#' api_instance <- CSVImportApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetStatusCSV(var_account_id, var_batch_id, var_response_group, var_start, var_limitdata_file = "result.txt")
#' result <- api_instance$GetStatusCSV(var_account_id, var_batch_id, var_response_group, var_start, var_limit)
#' dput(result)
#'
#'
#' ####################  ListStatusCSV  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the account
#' var_start <- 56 # integer | the start of the pagination
#' var_limit <- 56 # integer | the limit of the pagination
#'
#' #Search Status
#' api_instance <- CSVImportApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListStatusCSV(var_account_id, var_start, var_limitdata_file = "result.txt")
#' result <- api_instance$ListStatusCSV(var_account_id, var_start, var_limit)
#' dput(result)
#'
#'
#' ####################  StatusCSV  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the account
#' var_batch_id <- 56 # integer | the id of the batch to get its status
#'
#' #Batch Status
#' api_instance <- CSVImportApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$StatusCSV(var_account_id, var_batch_iddata_file = "result.txt")
#' result <- api_instance$StatusCSV(var_account_id, var_batch_id)
#' dput(result)
#'
#'
#' ####################  UploadCSV  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the account
#' var_upload_type <- "upload_type_example" # character | the upload type: OFFERS, RETAILERS, RETAILERLOCATIONS, CATEGORIES, OR FILTERS
#' var_import_file <- File.new('/path/to/file') # data.frame | the import file to reference
#' var_file_format <- "file_format_example" # character | the format of the file
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#'
#' #Upload CSV
#' api_instance <- CSVImportApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UploadCSV(var_account_id, var_upload_type, var_import_file, var_file_format, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$UploadCSV(var_account_id, var_upload_type, var_import_file, var_file_format, app_key = var_app_key)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CSVImportApi <- R6::R6Class(
  "CSVImportApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CSVImportApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Detail Status
    #'
    #' @param account_id the id of the logged in user
    #' @param batch_id the id of the batch
    #' @param response_group The group of categories to return: SUMMARY, DETAILS, ERRORS, OR ALL
    #' @param start the start of the pagination
    #' @param limit the limit of the pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    GetStatusCSV = function(account_id, batch_id, response_group, start, limit, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetStatusCSVWithHttpInfo(account_id, batch_id, response_group, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Detail Status
    #'
    #' @param account_id the id of the logged in user
    #' @param batch_id the id of the batch
    #' @param response_group The group of categories to return: SUMMARY, DETAILS, ERRORS, OR ALL
    #' @param start the start of the pagination
    #' @param limit the limit of the pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    GetStatusCSVWithHttpInfo = function(account_id, batch_id, response_group, start, limit, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`batch_id`)) {
        stop("Missing required parameter `batch_id`.")
      }

      if (missing(`response_group`)) {
        stop("Missing required parameter `response_group`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CSVImportApi$GetStatusCSV, `account_id` is not nullable")
      }

      if (!missing(`batch_id`) && is.null(`batch_id`)) {
        stop("Invalid value for `batch_id` when calling CSVImportApi$GetStatusCSV, `batch_id` is not nullable")
      }

      if (!missing(`response_group`) && is.null(`response_group`)) {
        stop("Invalid value for `response_group` when calling CSVImportApi$GetStatusCSV, `response_group` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling CSVImportApi$GetStatusCSV, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling CSVImportApi$GetStatusCSV, `limit` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["batchId"]] <- `batch_id`

      if (!is.null(`response_group`) && !(`response_group` %in% c("SUMMARY", "DETAILS", "ERRORS", "ALL"))) {
        stop("Invalid value for response_group when calling CSVImportApi$GetStatusCSV. Must be [SUMMARY, DETAILS, ERRORS, ALL].")
      }
      query_params[["responseGroup"]] <- `response_group`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/csvimport/batch/status/details"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Status
    #'
    #' @param account_id the id of the account
    #' @param start the start of the pagination
    #' @param limit the limit of the pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CsvImportResponse
    ListStatusCSV = function(account_id, start, limit, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ListStatusCSVWithHttpInfo(account_id, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Status
    #'
    #' @param account_id the id of the account
    #' @param start the start of the pagination
    #' @param limit the limit of the pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CsvImportResponse) with additional information such as HTTP status code, headers
    ListStatusCSVWithHttpInfo = function(account_id, start, limit, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CSVImportApi$ListStatusCSV, `account_id` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling CSVImportApi$ListStatusCSV, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling CSVImportApi$ListStatusCSV, `limit` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/csvimport/batch/list"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CsvImportResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Batch Status
    #'
    #' @param account_id the id of the account
    #' @param batch_id the id of the batch to get its status
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CsvImportResponse
    StatusCSV = function(account_id, batch_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$StatusCSVWithHttpInfo(account_id, batch_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Batch Status
    #'
    #' @param account_id the id of the account
    #' @param batch_id the id of the batch to get its status
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CsvImportResponse) with additional information such as HTTP status code, headers
    StatusCSVWithHttpInfo = function(account_id, batch_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`batch_id`)) {
        stop("Missing required parameter `batch_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CSVImportApi$StatusCSV, `account_id` is not nullable")
      }

      if (!missing(`batch_id`) && is.null(`batch_id`)) {
        stop("Invalid value for `batch_id` when calling CSVImportApi$StatusCSV, `batch_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["batchId"]] <- `batch_id`

      local_var_url_path <- "/csvimport/batch/status"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CsvImportResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Upload CSV
    #'
    #' @param account_id the id of the account
    #' @param upload_type the upload type: OFFERS, RETAILERS, RETAILERLOCATIONS, CATEGORIES, OR FILTERS
    #' @param import_file the import file to reference
    #' @param file_format the format of the file
    #' @param app_key (optional) the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CsvImportResponse
    UploadCSV = function(account_id, upload_type, import_file, file_format, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UploadCSVWithHttpInfo(account_id, upload_type, import_file, file_format, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Upload CSV
    #'
    #' @param account_id the id of the account
    #' @param upload_type the upload type: OFFERS, RETAILERS, RETAILERLOCATIONS, CATEGORIES, OR FILTERS
    #' @param import_file the import file to reference
    #' @param file_format the format of the file
    #' @param app_key (optional) the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CsvImportResponse) with additional information such as HTTP status code, headers
    UploadCSVWithHttpInfo = function(account_id, upload_type, import_file, file_format, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`upload_type`)) {
        stop("Missing required parameter `upload_type`.")
      }

      if (missing(`import_file`)) {
        stop("Missing required parameter `import_file`.")
      }

      if (missing(`file_format`)) {
        stop("Missing required parameter `file_format`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CSVImportApi$UploadCSV, `account_id` is not nullable")
      }

      if (!missing(`upload_type`) && is.null(`upload_type`)) {
        stop("Invalid value for `upload_type` when calling CSVImportApi$UploadCSV, `upload_type` is not nullable")
      }

      if (!missing(`import_file`) && is.null(`import_file`)) {
        stop("Invalid value for `import_file` when calling CSVImportApi$UploadCSV, `import_file` is not nullable")
      }

      if (!missing(`file_format`) && is.null(`file_format`)) {
        stop("Invalid value for `file_format` when calling CSVImportApi$UploadCSV, `file_format` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling CSVImportApi$UploadCSV, `app_key` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      if (!is.null(`upload_type`) && !(`upload_type` %in% c("OFFERS", "RETAILERS", "RETAILERLOCATIONS", "CATEGORIES", "FILTERS"))) {
        stop("Invalid value for upload_type when calling CSVImportApi$UploadCSV. Must be [OFFERS, RETAILERS, RETAILERLOCATIONS, CATEGORIES, FILTERS].")
      }
      query_params[["uploadType"]] <- `upload_type`

      query_params[["importFile"]] <- `import_file`

      query_params[["appKey"]] <- `app_key`

      if (!is.null(`file_format`) && !(`file_format` %in% c("EXCEL", "MYSQL", "RFC4180", "TDF"))) {
        stop("Invalid value for file_format when calling CSVImportApi$UploadCSV. Must be [EXCEL, MYSQL, RFC4180, TDF].")
      }
      query_params[["fileFormat"]] <- `file_format`

      local_var_url_path <- "/csvimport/upload"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CsvImportResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
