#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ApplicationConfig operations
#' @description ApplicationConfigApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateApplicationConfig  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account ID of the user
#' var_app_key <- "app_key_example" # character | The application key that the newly created applicationConfig will be associated to
#' var_config_version <- "config_version_example" # character | The application configuration, has to be unique within the application
#' var_asset_id <- 56 # integer | The json assetId that stores the configuration detail.
#' var_retailer_id <- 56 # integer | The retailer id for retailer specific configurations (Optional)
#' var_retailer_location_id <- 56 # integer | The retailer location id for retailer location specific configurations (Optional)
#' var_udid <- "udid_example" # character | The device udid for device specific configurations (Optional)
#'
#' #Create AppConfig
#' api_instance <- ApplicationConfigApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateApplicationConfig(var_account_id, var_app_key, var_config_version, var_asset_id, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, udid = var_udiddata_file = "result.txt")
#' result <- api_instance$CreateApplicationConfig(var_account_id, var_app_key, var_config_version, var_asset_id, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, udid = var_udid)
#' dput(result)
#'
#'
#' ####################  DeleteApplicationConfig  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account ID of the user
#' var_config_id <- 56 # integer | The config ID of the application configuration to delete
#'
#' #Delete AppConfig
#' api_instance <- ApplicationConfigApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteApplicationConfig(var_account_id, var_config_iddata_file = "result.txt")
#' result <- api_instance$DeleteApplicationConfig(var_account_id, var_config_id)
#' dput(result)
#'
#'
#' ####################  GetApplicationConfig  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account ID of the user
#' var_config_id <- 56 # integer | The config ID of the application configuration
#'
#' #Get AppConfig
#' api_instance <- ApplicationConfigApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetApplicationConfig(var_account_id, var_config_iddata_file = "result.txt")
#' result <- api_instance$GetApplicationConfig(var_account_id, var_config_id)
#' dput(result)
#'
#'
#' ####################  GetApplicationConfigByConfigVersion  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key
#' var_config_version <- "config_version_example" # character | The version of the application configuration
#' var_retailer_id <- 56 # integer | Only returns the config that matches the given retailer (Optional)
#' var_retailer_location_id <- 56 # integer | Only returns the config that matches the given retailer location (Optional)
#' var_udid <- "udid_example" # character | Only returns only returns the config that matches the given device udid (Optional)
#' var_allow_older_versions <- FALSE # character | Determines whether to return older config versions if the exact version is not found. If this happens, will try to return the latest version. (Optional)
#'
#' #Get AppConfig by Version
#' api_instance <- ApplicationConfigApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetApplicationConfigByConfigVersion(var_app_key, var_config_version, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, udid = var_udid, allow_older_versions = var_allow_older_versionsdata_file = "result.txt")
#' result <- api_instance$GetApplicationConfigByConfigVersion(var_app_key, var_config_version, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, udid = var_udid, allow_older_versions = var_allow_older_versions)
#' dput(result)
#'
#'
#' ####################  SearchApplicationConfig  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account ID of the user
#' var_app_key <- "app_key_example" # character | The application key to filter results by application Leaving this empty will return all application configurations for all applications (executive user only) (Optional)
#' var_retailer_id <- 56 # integer | Only returns the configs that matches the given retailer (Optional)
#' var_retailer_location_id <- 56 # integer | Only returns the configs that matches the given retailer location (Optional)
#' var_udid <- "udid_example" # character | Only returns only returns the configs that matches the given device udid (Optional)
#' var_config_version <- "config_version_example" # character | Config Version (Optional)
#' var_sort_field <- "CONFIG_VERSION_INDEX" # character | Orders the results by: CREATED, UPDATED, ACTIVE, APPLICATION_ID,  APPLICATION_KEY, APPLICATION_NAME, RETAILER_NAME, RETAILER_ID, RETAILER_LOCATION_NAME,  RETAILER_LOCATION_ID, OFFER_TITLE, OFFER_ID, OFFER_LOCATION_ID, CONFIG_VERSION_MINOR,  CONFIG_VERSION_MAJOR, CONFIG_VERSION_INDEX  (Optional)
#' var_descending <- TRUE # character | Determines whether the results are in descending or ascending order (Optional)
#' var_start <- 0 # integer | The start index for pagination (Optional)
#' var_limit <- 20 # integer | The limit for pagination (There is a hard limit of 100) (Optional)
#'
#' #Search AppConfigs
#' api_instance <- ApplicationConfigApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchApplicationConfig(var_account_id, app_key = var_app_key, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, udid = var_udid, config_version = var_config_version, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchApplicationConfig(var_account_id, app_key = var_app_key, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, udid = var_udid, config_version = var_config_version, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  UpdateApplicationConfig  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account ID of the user
#' var_config_id <- 56 # integer | The config ID of the application configuration to update
#' var_app_key <- "app_key_example" # character | The application key that the updated applicationConfig will be associated to (Optional)
#' var_config_version <- "config_version_example" # character | The application configuration, has to be unique within the application (Optional)
#' var_asset_id <- 56 # integer | The json assetId that stores the configuration detail. (Optional)
#' var_retailer_id <- 56 # integer | The retailer id for retailer specific configurations (Optional)
#' var_retailer_location_id <- 56 # integer | The retailer location id for retailer location specific configurations (Optional)
#' var_udid <- "udid_example" # character | The device udid for device specific configurations (Optional)
#'
#' #Update AppConfig
#' api_instance <- ApplicationConfigApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateApplicationConfig(var_account_id, var_config_id, app_key = var_app_key, config_version = var_config_version, asset_id = var_asset_id, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, udid = var_udiddata_file = "result.txt")
#' result <- api_instance$UpdateApplicationConfig(var_account_id, var_config_id, app_key = var_app_key, config_version = var_config_version, asset_id = var_asset_id, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, udid = var_udid)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ApplicationConfigApi <- R6::R6Class(
  "ApplicationConfigApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ApplicationConfigApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create AppConfig
    #'
    #' @param account_id The account ID of the user
    #' @param app_key The application key that the newly created applicationConfig will be associated to
    #' @param config_version The application configuration, has to be unique within the application
    #' @param asset_id The json assetId that stores the configuration detail.
    #' @param retailer_id (optional) The retailer id for retailer specific configurations
    #' @param retailer_location_id (optional) The retailer location id for retailer location specific configurations
    #' @param udid (optional) The device udid for device specific configurations
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ApplicationConfigResponse
    CreateApplicationConfig = function(account_id, app_key, config_version, asset_id, retailer_id = NULL, retailer_location_id = NULL, udid = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateApplicationConfigWithHttpInfo(account_id, app_key, config_version, asset_id, retailer_id, retailer_location_id, udid, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create AppConfig
    #'
    #' @param account_id The account ID of the user
    #' @param app_key The application key that the newly created applicationConfig will be associated to
    #' @param config_version The application configuration, has to be unique within the application
    #' @param asset_id The json assetId that stores the configuration detail.
    #' @param retailer_id (optional) The retailer id for retailer specific configurations
    #' @param retailer_location_id (optional) The retailer location id for retailer location specific configurations
    #' @param udid (optional) The device udid for device specific configurations
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ApplicationConfigResponse) with additional information such as HTTP status code, headers
    CreateApplicationConfigWithHttpInfo = function(account_id, app_key, config_version, asset_id, retailer_id = NULL, retailer_location_id = NULL, udid = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`config_version`)) {
        stop("Missing required parameter `config_version`.")
      }

      if (missing(`asset_id`)) {
        stop("Missing required parameter `asset_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationConfigApi$CreateApplicationConfig, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationConfigApi$CreateApplicationConfig, `app_key` is not nullable")
      }

      if (!missing(`config_version`) && is.null(`config_version`)) {
        stop("Invalid value for `config_version` when calling ApplicationConfigApi$CreateApplicationConfig, `config_version` is not nullable")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling ApplicationConfigApi$CreateApplicationConfig, `asset_id` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling ApplicationConfigApi$CreateApplicationConfig, `retailer_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling ApplicationConfigApi$CreateApplicationConfig, `retailer_location_id` is not nullable")
      }

      if (!missing(`udid`) && is.null(`udid`)) {
        stop("Invalid value for `udid` when calling ApplicationConfigApi$CreateApplicationConfig, `udid` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["configVersion"]] <- `config_version`

      query_params[["assetId"]] <- `asset_id`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["udid"]] <- `udid`

      local_var_url_path <- "/appconfig/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ApplicationConfigResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete AppConfig
    #'
    #' @param account_id The account ID of the user
    #' @param config_id The config ID of the application configuration to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteApplicationConfig = function(account_id, config_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteApplicationConfigWithHttpInfo(account_id, config_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete AppConfig
    #'
    #' @param account_id The account ID of the user
    #' @param config_id The config ID of the application configuration to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteApplicationConfigWithHttpInfo = function(account_id, config_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`config_id`)) {
        stop("Missing required parameter `config_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationConfigApi$DeleteApplicationConfig, `account_id` is not nullable")
      }

      if (!missing(`config_id`) && is.null(`config_id`)) {
        stop("Invalid value for `config_id` when calling ApplicationConfigApi$DeleteApplicationConfig, `config_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["configId"]] <- `config_id`

      local_var_url_path <- "/appconfig/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get AppConfig
    #'
    #' @param account_id The account ID of the user
    #' @param config_id The config ID of the application configuration
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ApplicationConfigResponse
    GetApplicationConfig = function(account_id, config_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetApplicationConfigWithHttpInfo(account_id, config_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get AppConfig
    #'
    #' @param account_id The account ID of the user
    #' @param config_id The config ID of the application configuration
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ApplicationConfigResponse) with additional information such as HTTP status code, headers
    GetApplicationConfigWithHttpInfo = function(account_id, config_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`config_id`)) {
        stop("Missing required parameter `config_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationConfigApi$GetApplicationConfig, `account_id` is not nullable")
      }

      if (!missing(`config_id`) && is.null(`config_id`)) {
        stop("Invalid value for `config_id` when calling ApplicationConfigApi$GetApplicationConfig, `config_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["configId"]] <- `config_id`

      local_var_url_path <- "/appconfig/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ApplicationConfigResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get AppConfig by Version
    #'
    #' @param app_key The application key
    #' @param config_version The version of the application configuration
    #' @param retailer_id (optional) Only returns the config that matches the given retailer
    #' @param retailer_location_id (optional) Only returns the config that matches the given retailer location
    #' @param udid (optional) Only returns only returns the config that matches the given device udid
    #' @param allow_older_versions (optional) Determines whether to return older config versions if the exact version is not found. If this happens, will try to return the latest version. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ApplicationConfigResponse
    GetApplicationConfigByConfigVersion = function(app_key, config_version, retailer_id = NULL, retailer_location_id = NULL, udid = NULL, allow_older_versions = FALSE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetApplicationConfigByConfigVersionWithHttpInfo(app_key, config_version, retailer_id, retailer_location_id, udid, allow_older_versions, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get AppConfig by Version
    #'
    #' @param app_key The application key
    #' @param config_version The version of the application configuration
    #' @param retailer_id (optional) Only returns the config that matches the given retailer
    #' @param retailer_location_id (optional) Only returns the config that matches the given retailer location
    #' @param udid (optional) Only returns only returns the config that matches the given device udid
    #' @param allow_older_versions (optional) Determines whether to return older config versions if the exact version is not found. If this happens, will try to return the latest version. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ApplicationConfigResponse) with additional information such as HTTP status code, headers
    GetApplicationConfigByConfigVersionWithHttpInfo = function(app_key, config_version, retailer_id = NULL, retailer_location_id = NULL, udid = NULL, allow_older_versions = FALSE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`config_version`)) {
        stop("Missing required parameter `config_version`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationConfigApi$GetApplicationConfigByConfigVersion, `app_key` is not nullable")
      }

      if (!missing(`config_version`) && is.null(`config_version`)) {
        stop("Invalid value for `config_version` when calling ApplicationConfigApi$GetApplicationConfigByConfigVersion, `config_version` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling ApplicationConfigApi$GetApplicationConfigByConfigVersion, `retailer_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling ApplicationConfigApi$GetApplicationConfigByConfigVersion, `retailer_location_id` is not nullable")
      }

      if (!missing(`udid`) && is.null(`udid`)) {
        stop("Invalid value for `udid` when calling ApplicationConfigApi$GetApplicationConfigByConfigVersion, `udid` is not nullable")
      }

      if (!missing(`allow_older_versions`) && is.null(`allow_older_versions`)) {
        stop("Invalid value for `allow_older_versions` when calling ApplicationConfigApi$GetApplicationConfigByConfigVersion, `allow_older_versions` is not nullable")
      }

      query_params[["appKey"]] <- `app_key`

      query_params[["configVersion"]] <- `config_version`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["udid"]] <- `udid`

      query_params[["allowOlderVersions"]] <- `allow_older_versions`

      local_var_url_path <- "/appconfig/getbyversion"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ApplicationConfigResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search AppConfigs
    #'
    #' @param account_id The account ID of the user
    #' @param app_key (optional) The application key to filter results by application Leaving this empty will return all application configurations for all applications (executive user only)
    #' @param retailer_id (optional) Only returns the configs that matches the given retailer
    #' @param retailer_location_id (optional) Only returns the configs that matches the given retailer location
    #' @param udid (optional) Only returns only returns the configs that matches the given device udid
    #' @param config_version (optional) Config Version
    #' @param sort_field (optional) Orders the results by: CREATED, UPDATED, ACTIVE, APPLICATION_ID,  APPLICATION_KEY, APPLICATION_NAME, RETAILER_NAME, RETAILER_ID, RETAILER_LOCATION_NAME,  RETAILER_LOCATION_ID, OFFER_TITLE, OFFER_ID, OFFER_LOCATION_ID, CONFIG_VERSION_MINOR,  CONFIG_VERSION_MAJOR, CONFIG_VERSION_INDEX  (default value: "CONFIG_VERSION_INDEX")
    #' @param descending (optional) Determines whether the results are in descending or ascending order (default value: TRUE)
    #' @param start (optional) The start index for pagination (default value: 0)
    #' @param limit (optional) The limit for pagination (There is a hard limit of 100) (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ApplicationConfigResponse]
    SearchApplicationConfig = function(account_id, app_key = NULL, retailer_id = NULL, retailer_location_id = NULL, udid = NULL, config_version = NULL, sort_field = "CONFIG_VERSION_INDEX", descending = TRUE, start = 0, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchApplicationConfigWithHttpInfo(account_id, app_key, retailer_id, retailer_location_id, udid, config_version, sort_field, descending, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search AppConfigs
    #'
    #' @param account_id The account ID of the user
    #' @param app_key (optional) The application key to filter results by application Leaving this empty will return all application configurations for all applications (executive user only)
    #' @param retailer_id (optional) Only returns the configs that matches the given retailer
    #' @param retailer_location_id (optional) Only returns the configs that matches the given retailer location
    #' @param udid (optional) Only returns only returns the configs that matches the given device udid
    #' @param config_version (optional) Config Version
    #' @param sort_field (optional) Orders the results by: CREATED, UPDATED, ACTIVE, APPLICATION_ID,  APPLICATION_KEY, APPLICATION_NAME, RETAILER_NAME, RETAILER_ID, RETAILER_LOCATION_NAME,  RETAILER_LOCATION_ID, OFFER_TITLE, OFFER_ID, OFFER_LOCATION_ID, CONFIG_VERSION_MINOR,  CONFIG_VERSION_MAJOR, CONFIG_VERSION_INDEX  (default value: "CONFIG_VERSION_INDEX")
    #' @param descending (optional) Determines whether the results are in descending or ascending order (default value: TRUE)
    #' @param start (optional) The start index for pagination (default value: 0)
    #' @param limit (optional) The limit for pagination (There is a hard limit of 100) (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ApplicationConfigResponse]) with additional information such as HTTP status code, headers
    SearchApplicationConfigWithHttpInfo = function(account_id, app_key = NULL, retailer_id = NULL, retailer_location_id = NULL, udid = NULL, config_version = NULL, sort_field = "CONFIG_VERSION_INDEX", descending = TRUE, start = 0, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationConfigApi$SearchApplicationConfig, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationConfigApi$SearchApplicationConfig, `app_key` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling ApplicationConfigApi$SearchApplicationConfig, `retailer_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling ApplicationConfigApi$SearchApplicationConfig, `retailer_location_id` is not nullable")
      }

      if (!missing(`udid`) && is.null(`udid`)) {
        stop("Invalid value for `udid` when calling ApplicationConfigApi$SearchApplicationConfig, `udid` is not nullable")
      }

      if (!missing(`config_version`) && is.null(`config_version`)) {
        stop("Invalid value for `config_version` when calling ApplicationConfigApi$SearchApplicationConfig, `config_version` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling ApplicationConfigApi$SearchApplicationConfig, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling ApplicationConfigApi$SearchApplicationConfig, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ApplicationConfigApi$SearchApplicationConfig, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ApplicationConfigApi$SearchApplicationConfig, `limit` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["udid"]] <- `udid`

      query_params[["configVersion"]] <- `config_version`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/appconfig/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ApplicationConfigResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update AppConfig
    #'
    #' @param account_id The account ID of the user
    #' @param config_id The config ID of the application configuration to update
    #' @param app_key (optional) The application key that the updated applicationConfig will be associated to
    #' @param config_version (optional) The application configuration, has to be unique within the application
    #' @param asset_id (optional) The json assetId that stores the configuration detail.
    #' @param retailer_id (optional) The retailer id for retailer specific configurations
    #' @param retailer_location_id (optional) The retailer location id for retailer location specific configurations
    #' @param udid (optional) The device udid for device specific configurations
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ApplicationConfigResponse
    UpdateApplicationConfig = function(account_id, config_id, app_key = NULL, config_version = NULL, asset_id = NULL, retailer_id = NULL, retailer_location_id = NULL, udid = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateApplicationConfigWithHttpInfo(account_id, config_id, app_key, config_version, asset_id, retailer_id, retailer_location_id, udid, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update AppConfig
    #'
    #' @param account_id The account ID of the user
    #' @param config_id The config ID of the application configuration to update
    #' @param app_key (optional) The application key that the updated applicationConfig will be associated to
    #' @param config_version (optional) The application configuration, has to be unique within the application
    #' @param asset_id (optional) The json assetId that stores the configuration detail.
    #' @param retailer_id (optional) The retailer id for retailer specific configurations
    #' @param retailer_location_id (optional) The retailer location id for retailer location specific configurations
    #' @param udid (optional) The device udid for device specific configurations
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ApplicationConfigResponse) with additional information such as HTTP status code, headers
    UpdateApplicationConfigWithHttpInfo = function(account_id, config_id, app_key = NULL, config_version = NULL, asset_id = NULL, retailer_id = NULL, retailer_location_id = NULL, udid = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`config_id`)) {
        stop("Missing required parameter `config_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationConfigApi$UpdateApplicationConfig, `account_id` is not nullable")
      }

      if (!missing(`config_id`) && is.null(`config_id`)) {
        stop("Invalid value for `config_id` when calling ApplicationConfigApi$UpdateApplicationConfig, `config_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationConfigApi$UpdateApplicationConfig, `app_key` is not nullable")
      }

      if (!missing(`config_version`) && is.null(`config_version`)) {
        stop("Invalid value for `config_version` when calling ApplicationConfigApi$UpdateApplicationConfig, `config_version` is not nullable")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling ApplicationConfigApi$UpdateApplicationConfig, `asset_id` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling ApplicationConfigApi$UpdateApplicationConfig, `retailer_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling ApplicationConfigApi$UpdateApplicationConfig, `retailer_location_id` is not nullable")
      }

      if (!missing(`udid`) && is.null(`udid`)) {
        stop("Invalid value for `udid` when calling ApplicationConfigApi$UpdateApplicationConfig, `udid` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["configId"]] <- `config_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["configVersion"]] <- `config_version`

      query_params[["assetId"]] <- `asset_id`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["udid"]] <- `udid`

      local_var_url_path <- "/appconfig/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ApplicationConfigResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
