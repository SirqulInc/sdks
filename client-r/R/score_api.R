#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Score operations
#' @description ScoreApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateScore  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_app_key <- "app_key_example" # character | The game application key to save the score for.
#' var_points <- 56 # integer | The score
#' var_mission_id <- 56 # integer | The missionId to score for, ignore if not playing mission. (Optional)
#' var_game_id <- 56 # integer | The gameId to score for, ignore if not playing mission. (Optional)
#' var_pack_id <- 56 # integer | The packId to score for, send -2 if playing community levels. (Optional)
#' var_game_level_id <- 56 # integer | The gameLevelId to score for. (Optional)
#' var_game_object_id <- 56 # integer | The gameObjectId to score for, ignore if level based scoring. (Optional)
#' var_time_taken <- 56 # integer | The time taken to complete task (Optional)
#' var_highest <- "highest_example" # character |  (Optional)
#'
#' #Create Score
#' api_instance <- ScoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateScore(var_account_id, var_app_key, var_points, mission_id = var_mission_id, game_id = var_game_id, pack_id = var_pack_id, game_level_id = var_game_level_id, game_object_id = var_game_object_id, time_taken = var_time_taken, highest = var_highestdata_file = "result.txt")
#' result <- api_instance$CreateScore(var_account_id, var_app_key, var_points, mission_id = var_mission_id, game_id = var_game_id, pack_id = var_pack_id, game_level_id = var_game_level_id, game_object_id = var_game_object_id, time_taken = var_time_taken, highest = var_highest)
#' dput(result)
#'
#'
#' ####################  GetScore  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_app_key <- "app_key_example" # character | The game application key to get the level for.
#' var_mission_id <- 56 # integer | The missionId to score for, null if not playing mission. (Optional)
#' var_game_id <- 56 # integer | The gameId to score for, null if not playing mission. (Optional)
#' var_pack_id <- 56 # integer | The packId to score for, null if playing community levels. (Optional)
#' var_game_level_id <- 56 # integer | The gameLevelId to score for. (Optional)
#' var_game_object_id <- 56 # integer | The gameObjectId to score for, null if level based scoring. (Optional)
#' var_score_object_type <- "score_object_type_example" # character | The object type to filter scores by (TicketObjectType) (Optional)
#' var_score_status <- "score_status_example" # character | The status of the score to filter (ScoreStatus) (Optional)
#'
#' #Get Score
#' api_instance <- ScoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetScore(var_account_id, var_app_key, mission_id = var_mission_id, game_id = var_game_id, pack_id = var_pack_id, game_level_id = var_game_level_id, game_object_id = var_game_object_id, score_object_type = var_score_object_type, score_status = var_score_statusdata_file = "result.txt")
#' result <- api_instance$GetScore(var_account_id, var_app_key, mission_id = var_mission_id, game_id = var_game_id, pack_id = var_pack_id, game_level_id = var_game_level_id, game_object_id = var_game_object_id, score_object_type = var_score_object_type, score_status = var_score_status)
#' dput(result)
#'
#'
#' ####################  SearchScores  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_app_key <- "app_key_example" # character | The game application key to get the level for.
#' var_mission_id <- 56 # integer | The missionId to score for, null if not playing mission. (Optional)
#' var_game_id <- 56 # integer | The gameId to score for, null if not playing mission. (Optional)
#' var_pack_id <- 56 # integer | The packId to score for, null if playing community levels. (Optional)
#' var_game_level_id <- 56 # integer | The gameLevelId to score for. (Optional)
#' var_game_object_id <- 56 # integer | The gameObjectId to score for, null if level based scoring. (Optional)
#'
#' #Search Score
#' api_instance <- ScoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchScores(var_account_id, var_app_key, mission_id = var_mission_id, game_id = var_game_id, pack_id = var_pack_id, game_level_id = var_game_level_id, game_object_id = var_game_object_iddata_file = "result.txt")
#' result <- api_instance$SearchScores(var_account_id, var_app_key, mission_id = var_mission_id, game_id = var_game_id, pack_id = var_pack_id, game_level_id = var_game_level_id, game_object_id = var_game_object_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ScoreApi <- R6::R6Class(
  "ScoreApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ScoreApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Score
    #'
    #' @param account_id The logged in user.
    #' @param app_key The game application key to save the score for.
    #' @param points The score
    #' @param mission_id (optional) The missionId to score for, ignore if not playing mission.
    #' @param game_id (optional) The gameId to score for, ignore if not playing mission.
    #' @param pack_id (optional) The packId to score for, send -2 if playing community levels.
    #' @param game_level_id (optional) The gameLevelId to score for.
    #' @param game_object_id (optional) The gameObjectId to score for, ignore if level based scoring.
    #' @param time_taken (optional) The time taken to complete task
    #' @param highest (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ScoreResponse
    CreateScore = function(account_id, app_key, points, mission_id = NULL, game_id = NULL, pack_id = NULL, game_level_id = NULL, game_object_id = NULL, time_taken = NULL, highest = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateScoreWithHttpInfo(account_id, app_key, points, mission_id, game_id, pack_id, game_level_id, game_object_id, time_taken, highest, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Score
    #'
    #' @param account_id The logged in user.
    #' @param app_key The game application key to save the score for.
    #' @param points The score
    #' @param mission_id (optional) The missionId to score for, ignore if not playing mission.
    #' @param game_id (optional) The gameId to score for, ignore if not playing mission.
    #' @param pack_id (optional) The packId to score for, send -2 if playing community levels.
    #' @param game_level_id (optional) The gameLevelId to score for.
    #' @param game_object_id (optional) The gameObjectId to score for, ignore if level based scoring.
    #' @param time_taken (optional) The time taken to complete task
    #' @param highest (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ScoreResponse) with additional information such as HTTP status code, headers
    CreateScoreWithHttpInfo = function(account_id, app_key, points, mission_id = NULL, game_id = NULL, pack_id = NULL, game_level_id = NULL, game_object_id = NULL, time_taken = NULL, highest = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`points`)) {
        stop("Missing required parameter `points`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ScoreApi$CreateScore, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ScoreApi$CreateScore, `app_key` is not nullable")
      }

      if (!missing(`points`) && is.null(`points`)) {
        stop("Invalid value for `points` when calling ScoreApi$CreateScore, `points` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling ScoreApi$CreateScore, `mission_id` is not nullable")
      }

      if (!missing(`game_id`) && is.null(`game_id`)) {
        stop("Invalid value for `game_id` when calling ScoreApi$CreateScore, `game_id` is not nullable")
      }

      if (!missing(`pack_id`) && is.null(`pack_id`)) {
        stop("Invalid value for `pack_id` when calling ScoreApi$CreateScore, `pack_id` is not nullable")
      }

      if (!missing(`game_level_id`) && is.null(`game_level_id`)) {
        stop("Invalid value for `game_level_id` when calling ScoreApi$CreateScore, `game_level_id` is not nullable")
      }

      if (!missing(`game_object_id`) && is.null(`game_object_id`)) {
        stop("Invalid value for `game_object_id` when calling ScoreApi$CreateScore, `game_object_id` is not nullable")
      }

      if (!missing(`time_taken`) && is.null(`time_taken`)) {
        stop("Invalid value for `time_taken` when calling ScoreApi$CreateScore, `time_taken` is not nullable")
      }

      if (!missing(`highest`) && is.null(`highest`)) {
        stop("Invalid value for `highest` when calling ScoreApi$CreateScore, `highest` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["missionId"]] <- `mission_id`

      query_params[["gameId"]] <- `game_id`

      query_params[["packId"]] <- `pack_id`

      query_params[["gameLevelId"]] <- `game_level_id`

      query_params[["gameObjectId"]] <- `game_object_id`

      query_params[["points"]] <- `points`

      query_params[["timeTaken"]] <- `time_taken`

      query_params[["highest"]] <- `highest`

      local_var_url_path <- "/score/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ScoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Score
    #'
    #' @param account_id The logged in user.
    #' @param app_key The game application key to get the level for.
    #' @param mission_id (optional) The missionId to score for, null if not playing mission.
    #' @param game_id (optional) The gameId to score for, null if not playing mission.
    #' @param pack_id (optional) The packId to score for, null if playing community levels.
    #' @param game_level_id (optional) The gameLevelId to score for.
    #' @param game_object_id (optional) The gameObjectId to score for, null if level based scoring.
    #' @param score_object_type (optional) The object type to filter scores by (TicketObjectType)
    #' @param score_status (optional) The status of the score to filter (ScoreStatus)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ScoreResponse
    GetScore = function(account_id, app_key, mission_id = NULL, game_id = NULL, pack_id = NULL, game_level_id = NULL, game_object_id = NULL, score_object_type = NULL, score_status = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetScoreWithHttpInfo(account_id, app_key, mission_id, game_id, pack_id, game_level_id, game_object_id, score_object_type, score_status, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Score
    #'
    #' @param account_id The logged in user.
    #' @param app_key The game application key to get the level for.
    #' @param mission_id (optional) The missionId to score for, null if not playing mission.
    #' @param game_id (optional) The gameId to score for, null if not playing mission.
    #' @param pack_id (optional) The packId to score for, null if playing community levels.
    #' @param game_level_id (optional) The gameLevelId to score for.
    #' @param game_object_id (optional) The gameObjectId to score for, null if level based scoring.
    #' @param score_object_type (optional) The object type to filter scores by (TicketObjectType)
    #' @param score_status (optional) The status of the score to filter (ScoreStatus)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ScoreResponse) with additional information such as HTTP status code, headers
    GetScoreWithHttpInfo = function(account_id, app_key, mission_id = NULL, game_id = NULL, pack_id = NULL, game_level_id = NULL, game_object_id = NULL, score_object_type = NULL, score_status = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ScoreApi$GetScore, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ScoreApi$GetScore, `app_key` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling ScoreApi$GetScore, `mission_id` is not nullable")
      }

      if (!missing(`game_id`) && is.null(`game_id`)) {
        stop("Invalid value for `game_id` when calling ScoreApi$GetScore, `game_id` is not nullable")
      }

      if (!missing(`pack_id`) && is.null(`pack_id`)) {
        stop("Invalid value for `pack_id` when calling ScoreApi$GetScore, `pack_id` is not nullable")
      }

      if (!missing(`game_level_id`) && is.null(`game_level_id`)) {
        stop("Invalid value for `game_level_id` when calling ScoreApi$GetScore, `game_level_id` is not nullable")
      }

      if (!missing(`game_object_id`) && is.null(`game_object_id`)) {
        stop("Invalid value for `game_object_id` when calling ScoreApi$GetScore, `game_object_id` is not nullable")
      }

      if (!missing(`score_object_type`) && is.null(`score_object_type`)) {
        stop("Invalid value for `score_object_type` when calling ScoreApi$GetScore, `score_object_type` is not nullable")
      }

      if (!missing(`score_status`) && is.null(`score_status`)) {
        stop("Invalid value for `score_status` when calling ScoreApi$GetScore, `score_status` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["missionId"]] <- `mission_id`

      query_params[["gameId"]] <- `game_id`

      query_params[["packId"]] <- `pack_id`

      query_params[["gameLevelId"]] <- `game_level_id`

      query_params[["gameObjectId"]] <- `game_object_id`

      query_params[["scoreObjectType"]] <- `score_object_type`

      query_params[["scoreStatus"]] <- `score_status`

      local_var_url_path <- "/score/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ScoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Score
    #'
    #' @param account_id The logged in user.
    #' @param app_key The game application key to get the level for.
    #' @param mission_id (optional) The missionId to score for, null if not playing mission.
    #' @param game_id (optional) The gameId to score for, null if not playing mission.
    #' @param pack_id (optional) The packId to score for, null if playing community levels.
    #' @param game_level_id (optional) The gameLevelId to score for.
    #' @param game_object_id (optional) The gameObjectId to score for, null if level based scoring.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ScoreResponse]
    SearchScores = function(account_id, app_key, mission_id = NULL, game_id = NULL, pack_id = NULL, game_level_id = NULL, game_object_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchScoresWithHttpInfo(account_id, app_key, mission_id, game_id, pack_id, game_level_id, game_object_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Score
    #'
    #' @param account_id The logged in user.
    #' @param app_key The game application key to get the level for.
    #' @param mission_id (optional) The missionId to score for, null if not playing mission.
    #' @param game_id (optional) The gameId to score for, null if not playing mission.
    #' @param pack_id (optional) The packId to score for, null if playing community levels.
    #' @param game_level_id (optional) The gameLevelId to score for.
    #' @param game_object_id (optional) The gameObjectId to score for, null if level based scoring.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ScoreResponse]) with additional information such as HTTP status code, headers
    SearchScoresWithHttpInfo = function(account_id, app_key, mission_id = NULL, game_id = NULL, pack_id = NULL, game_level_id = NULL, game_object_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ScoreApi$SearchScores, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ScoreApi$SearchScores, `app_key` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling ScoreApi$SearchScores, `mission_id` is not nullable")
      }

      if (!missing(`game_id`) && is.null(`game_id`)) {
        stop("Invalid value for `game_id` when calling ScoreApi$SearchScores, `game_id` is not nullable")
      }

      if (!missing(`pack_id`) && is.null(`pack_id`)) {
        stop("Invalid value for `pack_id` when calling ScoreApi$SearchScores, `pack_id` is not nullable")
      }

      if (!missing(`game_level_id`) && is.null(`game_level_id`)) {
        stop("Invalid value for `game_level_id` when calling ScoreApi$SearchScores, `game_level_id` is not nullable")
      }

      if (!missing(`game_object_id`) && is.null(`game_object_id`)) {
        stop("Invalid value for `game_object_id` when calling ScoreApi$SearchScores, `game_object_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["missionId"]] <- `mission_id`

      query_params[["gameId"]] <- `game_id`

      query_params[["packId"]] <- `pack_id`

      query_params[["gameLevelId"]] <- `game_level_id`

      query_params[["gameObjectId"]] <- `game_object_id`

      local_var_url_path <- "/score/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ScoreResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
