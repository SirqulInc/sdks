#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title OfferStatus operations
#' @description OfferStatusApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateOfferTransactionStatus  ####################
#'
#' library(openapi)
#' var_name <- "name_example" # character | The name of the status
#' var_code <- 56 # integer | The status code, must be unique 
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_description <- "description_example" # character | The description of the status (Optional)
#' var_role <- "ANY" # character | The actor that uses the status (Optional)
#' var_active <- TRUE # character | The active status (Optional)
#' var_application_ids <- "application_ids_example" # character | The applications to associate the status with, if null then for all. (Optional)
#'
#' #Create Offer Status
#' api_instance <- OfferStatusApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateOfferTransactionStatus(var_name, var_code, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, description = var_description, role = var_role, active = var_active, application_ids = var_application_idsdata_file = "result.txt")
#' result <- api_instance$CreateOfferTransactionStatus(var_name, var_code, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, description = var_description, role = var_role, active = var_active, application_ids = var_application_ids)
#' dput(result)
#'
#'
#' ####################  DeleteOfferTransactionStatus  ####################
#'
#' library(openapi)
#' var_status_id <- 56 # integer | The id of the record to delete
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#'
#' #Delete Offer Status
#' api_instance <- OfferStatusApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteOfferTransactionStatus(var_status_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$DeleteOfferTransactionStatus(var_status_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  GetOfferTransactionStatus  ####################
#'
#' library(openapi)
#' var_status_id <- 56 # integer | The id of the record to get 
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#'
#' #Get Offer Status
#' api_instance <- OfferStatusApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOfferTransactionStatus(var_status_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$GetOfferTransactionStatus(var_status_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  SearchOfferTransactionStatuses  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_keyword <- "keyword_example" # character | Search the name and description fields (Optional)
#' var_role <- "role_example" # character | The actors role, possible values are: CUSTOMER, RETAILER, or ANY (Optional)
#' var_app_key <- "app_key_example" # character | The application making the request (Optional)
#' var_sort_field <- "CODE" # character | The field to sort on, possible values are: ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, NAME, DESCRIPTION, CODE, ROLE (Optional)
#' var_descending <- TRUE # character | Determines whether the results are in descending order (Optional)
#' var_start <- 0 # integer | The start index for pagination (Optional)
#' var_limit <- 20 # integer | The limit for pagination (Optional)
#' var_include_inactive <- FALSE # character | If true include inactive items (Optional)
#'
#' #Search Offer Status
#' api_instance <- OfferStatusApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchOfferTransactionStatuses(device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, keyword = var_keyword, role = var_role, app_key = var_app_key, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, include_inactive = var_include_inactivedata_file = "result.txt")
#' result <- api_instance$SearchOfferTransactionStatuses(device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, keyword = var_keyword, role = var_role, app_key = var_app_key, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, include_inactive = var_include_inactive)
#' dput(result)
#'
#'
#' ####################  UpdateOfferTransactionStatus  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_status_id <- 56 # integer | The id of the record to update  (Optional)
#' var_name <- "name_example" # character | The name of th status (Optional)
#' var_description <- "description_example" # character | The description of the status (Optional)
#' var_code <- 56 # integer | The status code, must be unique  (Optional)
#' var_role <- "role_example" # character | The actor that uses the status (Optional)
#' var_active <- "active_example" # character | The active status (Optional)
#' var_application_ids <- "application_ids_example" # character | The applications to associate the status with, if null then for all. (Optional)
#'
#' #Update Offer Status
#' api_instance <- OfferStatusApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateOfferTransactionStatus(device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, status_id = var_status_id, name = var_name, description = var_description, code = var_code, role = var_role, active = var_active, application_ids = var_application_idsdata_file = "result.txt")
#' result <- api_instance$UpdateOfferTransactionStatus(device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, status_id = var_status_id, name = var_name, description = var_description, code = var_code, role = var_role, active = var_active, application_ids = var_application_ids)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OfferStatusApi <- R6::R6Class(
  "OfferStatusApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new OfferStatusApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Offer Status
    #'
    #' @param name The name of the status
    #' @param code The status code, must be unique 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param description (optional) The description of the status
    #' @param role (optional) The actor that uses the status (default value: "ANY")
    #' @param active (optional) The active status (default value: TRUE)
    #' @param application_ids (optional) The applications to associate the status with, if null then for all.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferTransactionStatusResponse
    CreateOfferTransactionStatus = function(name, code, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, description = NULL, role = "ANY", active = TRUE, application_ids = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateOfferTransactionStatusWithHttpInfo(name, code, device_id, account_id, latitude, longitude, description, role, active, application_ids, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Offer Status
    #'
    #' @param name The name of the status
    #' @param code The status code, must be unique 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param description (optional) The description of the status
    #' @param role (optional) The actor that uses the status (default value: "ANY")
    #' @param active (optional) The active status (default value: TRUE)
    #' @param application_ids (optional) The applications to associate the status with, if null then for all.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferTransactionStatusResponse) with additional information such as HTTP status code, headers
    CreateOfferTransactionStatusWithHttpInfo = function(name, code, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, description = NULL, role = "ANY", active = TRUE, application_ids = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (missing(`code`)) {
        stop("Missing required parameter `code`.")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling OfferStatusApi$CreateOfferTransactionStatus, `name` is not nullable")
      }

      if (!missing(`code`) && is.null(`code`)) {
        stop("Invalid value for `code` when calling OfferStatusApi$CreateOfferTransactionStatus, `code` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferStatusApi$CreateOfferTransactionStatus, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferStatusApi$CreateOfferTransactionStatus, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling OfferStatusApi$CreateOfferTransactionStatus, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling OfferStatusApi$CreateOfferTransactionStatus, `longitude` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling OfferStatusApi$CreateOfferTransactionStatus, `description` is not nullable")
      }

      if (!missing(`role`) && is.null(`role`)) {
        stop("Invalid value for `role` when calling OfferStatusApi$CreateOfferTransactionStatus, `role` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling OfferStatusApi$CreateOfferTransactionStatus, `active` is not nullable")
      }

      if (!missing(`application_ids`) && is.null(`application_ids`)) {
        stop("Invalid value for `application_ids` when calling OfferStatusApi$CreateOfferTransactionStatus, `application_ids` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["code"]] <- `code`

      query_params[["role"]] <- `role`

      query_params[["active"]] <- `active`

      query_params[["applicationIds"]] <- `application_ids`

      local_var_url_path <- "/offer/status/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferTransactionStatusResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Offer Status
    #'
    #' @param status_id The id of the record to delete
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteOfferTransactionStatus = function(status_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteOfferTransactionStatusWithHttpInfo(status_id, device_id, account_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Offer Status
    #'
    #' @param status_id The id of the record to delete
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteOfferTransactionStatusWithHttpInfo = function(status_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`status_id`)) {
        stop("Missing required parameter `status_id`.")
      }

      if (!missing(`status_id`) && is.null(`status_id`)) {
        stop("Invalid value for `status_id` when calling OfferStatusApi$DeleteOfferTransactionStatus, `status_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferStatusApi$DeleteOfferTransactionStatus, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferStatusApi$DeleteOfferTransactionStatus, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling OfferStatusApi$DeleteOfferTransactionStatus, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling OfferStatusApi$DeleteOfferTransactionStatus, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["statusId"]] <- `status_id`

      local_var_url_path <- "/offer/status/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Offer Status
    #'
    #' @param status_id The id of the record to get 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferTransactionStatusResponse
    GetOfferTransactionStatus = function(status_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetOfferTransactionStatusWithHttpInfo(status_id, device_id, account_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Offer Status
    #'
    #' @param status_id The id of the record to get 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferTransactionStatusResponse) with additional information such as HTTP status code, headers
    GetOfferTransactionStatusWithHttpInfo = function(status_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`status_id`)) {
        stop("Missing required parameter `status_id`.")
      }

      if (!missing(`status_id`) && is.null(`status_id`)) {
        stop("Invalid value for `status_id` when calling OfferStatusApi$GetOfferTransactionStatus, `status_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferStatusApi$GetOfferTransactionStatus, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferStatusApi$GetOfferTransactionStatus, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling OfferStatusApi$GetOfferTransactionStatus, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling OfferStatusApi$GetOfferTransactionStatus, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["statusId"]] <- `status_id`

      local_var_url_path <- "/offer/status/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferTransactionStatusResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Offer Status
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param keyword (optional) Search the name and description fields
    #' @param role (optional) The actors role, possible values are: CUSTOMER, RETAILER, or ANY
    #' @param app_key (optional) The application making the request
    #' @param sort_field (optional) The field to sort on, possible values are: ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, NAME, DESCRIPTION, CODE, ROLE (default value: "CODE")
    #' @param descending (optional) Determines whether the results are in descending order (default value: TRUE)
    #' @param start (optional) The start index for pagination (default value: 0)
    #' @param limit (optional) The limit for pagination (default value: 20)
    #' @param include_inactive (optional) If true include inactive items (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[OfferTransactionStatusResponse]
    SearchOfferTransactionStatuses = function(device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, keyword = NULL, role = NULL, app_key = NULL, sort_field = "CODE", descending = TRUE, start = 0, limit = 20, include_inactive = FALSE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchOfferTransactionStatusesWithHttpInfo(device_id, account_id, latitude, longitude, keyword, role, app_key, sort_field, descending, start, limit, include_inactive, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Offer Status
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param keyword (optional) Search the name and description fields
    #' @param role (optional) The actors role, possible values are: CUSTOMER, RETAILER, or ANY
    #' @param app_key (optional) The application making the request
    #' @param sort_field (optional) The field to sort on, possible values are: ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, NAME, DESCRIPTION, CODE, ROLE (default value: "CODE")
    #' @param descending (optional) Determines whether the results are in descending order (default value: TRUE)
    #' @param start (optional) The start index for pagination (default value: 0)
    #' @param limit (optional) The limit for pagination (default value: 20)
    #' @param include_inactive (optional) If true include inactive items (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[OfferTransactionStatusResponse]) with additional information such as HTTP status code, headers
    SearchOfferTransactionStatusesWithHttpInfo = function(device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, keyword = NULL, role = NULL, app_key = NULL, sort_field = "CODE", descending = TRUE, start = 0, limit = 20, include_inactive = FALSE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferStatusApi$SearchOfferTransactionStatuses, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferStatusApi$SearchOfferTransactionStatuses, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling OfferStatusApi$SearchOfferTransactionStatuses, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling OfferStatusApi$SearchOfferTransactionStatuses, `longitude` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling OfferStatusApi$SearchOfferTransactionStatuses, `keyword` is not nullable")
      }

      if (!missing(`role`) && is.null(`role`)) {
        stop("Invalid value for `role` when calling OfferStatusApi$SearchOfferTransactionStatuses, `role` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling OfferStatusApi$SearchOfferTransactionStatuses, `app_key` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling OfferStatusApi$SearchOfferTransactionStatuses, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling OfferStatusApi$SearchOfferTransactionStatuses, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling OfferStatusApi$SearchOfferTransactionStatuses, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling OfferStatusApi$SearchOfferTransactionStatuses, `limit` is not nullable")
      }

      if (!missing(`include_inactive`) && is.null(`include_inactive`)) {
        stop("Invalid value for `include_inactive` when calling OfferStatusApi$SearchOfferTransactionStatuses, `include_inactive` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["keyword"]] <- `keyword`

      query_params[["role"]] <- `role`

      query_params[["appKey"]] <- `app_key`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "NAME", "DESCRIPTION", "CODE", "ROLE"))) {
        stop("Invalid value for sort_field when calling OfferStatusApi$SearchOfferTransactionStatuses. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, NAME, DESCRIPTION, CODE, ROLE].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["includeInactive"]] <- `include_inactive`

      local_var_url_path <- "/offer/status/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[OfferTransactionStatusResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Offer Status
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param status_id (optional) The id of the record to update 
    #' @param name (optional) The name of th status
    #' @param description (optional) The description of the status
    #' @param code (optional) The status code, must be unique 
    #' @param role (optional) The actor that uses the status
    #' @param active (optional) The active status
    #' @param application_ids (optional) The applications to associate the status with, if null then for all.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferTransactionStatusResponse
    UpdateOfferTransactionStatus = function(device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, status_id = NULL, name = NULL, description = NULL, code = NULL, role = NULL, active = NULL, application_ids = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateOfferTransactionStatusWithHttpInfo(device_id, account_id, latitude, longitude, status_id, name, description, code, role, active, application_ids, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Offer Status
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param status_id (optional) The id of the record to update 
    #' @param name (optional) The name of th status
    #' @param description (optional) The description of the status
    #' @param code (optional) The status code, must be unique 
    #' @param role (optional) The actor that uses the status
    #' @param active (optional) The active status
    #' @param application_ids (optional) The applications to associate the status with, if null then for all.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferTransactionStatusResponse) with additional information such as HTTP status code, headers
    UpdateOfferTransactionStatusWithHttpInfo = function(device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, status_id = NULL, name = NULL, description = NULL, code = NULL, role = NULL, active = NULL, application_ids = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferStatusApi$UpdateOfferTransactionStatus, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferStatusApi$UpdateOfferTransactionStatus, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling OfferStatusApi$UpdateOfferTransactionStatus, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling OfferStatusApi$UpdateOfferTransactionStatus, `longitude` is not nullable")
      }

      if (!missing(`status_id`) && is.null(`status_id`)) {
        stop("Invalid value for `status_id` when calling OfferStatusApi$UpdateOfferTransactionStatus, `status_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling OfferStatusApi$UpdateOfferTransactionStatus, `name` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling OfferStatusApi$UpdateOfferTransactionStatus, `description` is not nullable")
      }

      if (!missing(`code`) && is.null(`code`)) {
        stop("Invalid value for `code` when calling OfferStatusApi$UpdateOfferTransactionStatus, `code` is not nullable")
      }

      if (!missing(`role`) && is.null(`role`)) {
        stop("Invalid value for `role` when calling OfferStatusApi$UpdateOfferTransactionStatus, `role` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling OfferStatusApi$UpdateOfferTransactionStatus, `active` is not nullable")
      }

      if (!missing(`application_ids`) && is.null(`application_ids`)) {
        stop("Invalid value for `application_ids` when calling OfferStatusApi$UpdateOfferTransactionStatus, `application_ids` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["statusId"]] <- `status_id`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["code"]] <- `code`

      query_params[["role"]] <- `role`

      query_params[["active"]] <- `active`

      query_params[["applicationIds"]] <- `application_ids`

      local_var_url_path <- "/offer/status/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferTransactionStatusResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
