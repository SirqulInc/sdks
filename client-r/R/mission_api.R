#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Mission operations
#' @description MissionApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateMission  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_title <- "title_example" # character | The title of the mission
#' var_description <- "description_example" # character | The description of the mission (Optional)
#' var_sub_type <- "sub_type_example" # character | Custom string client apps can use for searching/filtering missions (Optional)
#' var_start_date <- 56 # integer | The date/time to start the mission, send 0 to unset (Optional)
#' var_end_date <- 56 # integer | The date/time to end the mission, send 0 to unset (Optional)
#' var_active <- "active_example" # character | Activate/deactivate the mission (Optional)
#' var_game_level_ids <- "game_level_ids_example" # character | the game level ids to include in the mission, comma separated (Optional)
#' var_creative_ids <- "creative_ids_example" # character | creatives to associate with the mission (Optional)
#' var_audience_ids <- "audience_ids_example" # character | audiences to associate with the mission (Optional)
#' var_mission_task <- "mission_task_example" # character | This parameter is deprecated. (Optional)
#' var_format_type <- "format_type_example" # character | The string identifier for a mission format (this is not the missionFormatId) (Optional)
#' var_offer_id <- 56 # integer | The offerId to give as a reward (Optional)
#' var_balance <- 3.4 # numeric | Set the amount of money available to spend, once 0 deactivate mission. Set to a negative value for unlimited. (Optional)
#' var_advanced_reporting <- "advanced_reporting_example" # character | Flag to mark the mission as eligible for advanced reporting (Optional)
#' var_allocate_tickets <- "allocate_tickets_example" # character | Flag to indicate owner should receive tickets for completed missions (Optional)
#' var_ticket_count <- 56 # integer | The number of tickets to reward (Optional)
#' var_ticket_type <- "ticket_type_example" # character | The type of ticket to reward, null means default type (Optional)
#' var_points <- 56 # integer | The number of points to award for completing a mission (Optional)
#' var_meta_data <- "meta_data_example" # character | external custom client defined data (Optional)
#' var_application_ids <- "application_ids_example" # character | Comma separated list of application ids (Optional)
#' var_devices <- "devices_example" # character | Deprecated parameter for device names (Optional)
#' var_device_ids <- "device_ids_example" # character | Comma separated list of device ids (Optional)
#' var_device_versions <- "device_versions_example" # character | Comma separated list of device version ranges (Optional)
#' var_locations <- "locations_example" # character | List of lat/long pairs for mission locations (Optional)
#' var_radius <- "radius_example" # character | Comma separated list of radii for locations (Optional)
#'
#' #Create Mission
#' api_instance <- MissionApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateMission(var_account_id, var_title, description = var_description, sub_type = var_sub_type, start_date = var_start_date, end_date = var_end_date, active = var_active, game_level_ids = var_game_level_ids, creative_ids = var_creative_ids, audience_ids = var_audience_ids, mission_task = var_mission_task, format_type = var_format_type, offer_id = var_offer_id, balance = var_balance, advanced_reporting = var_advanced_reporting, allocate_tickets = var_allocate_tickets, ticket_count = var_ticket_count, ticket_type = var_ticket_type, points = var_points, meta_data = var_meta_data, application_ids = var_application_ids, devices = var_devices, device_ids = var_device_ids, device_versions = var_device_versions, locations = var_locations, radius = var_radiusdata_file = "result.txt")
#' result <- api_instance$CreateMission(var_account_id, var_title, description = var_description, sub_type = var_sub_type, start_date = var_start_date, end_date = var_end_date, active = var_active, game_level_ids = var_game_level_ids, creative_ids = var_creative_ids, audience_ids = var_audience_ids, mission_task = var_mission_task, format_type = var_format_type, offer_id = var_offer_id, balance = var_balance, advanced_reporting = var_advanced_reporting, allocate_tickets = var_allocate_tickets, ticket_count = var_ticket_count, ticket_type = var_ticket_type, points = var_points, meta_data = var_meta_data, application_ids = var_application_ids, devices = var_devices, device_ids = var_device_ids, device_versions = var_device_versions, locations = var_locations, radius = var_radius)
#' dput(result)
#'
#'
#' ####################  DeleteMission  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_mission_id <- 56 # integer | the id of the mission to delete
#'
#' #Delete Mission
#' api_instance <- MissionApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteMission(var_account_id, var_mission_iddata_file = "result.txt")
#' result <- api_instance$DeleteMission(var_account_id, var_mission_id)
#' dput(result)
#'
#'
#' ####################  FindMissions  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key, if provided return missions specific for the app. Will always return mission levels that are app agnostic.
#' var_suffix <- "suffix_example" # character | The type of mission to get, possible values are: click_banner, click_leaderboard, click_skyscraper, click_full, click_video, or click_zip (Optional)
#' var_type <- "type_example" # character | The type of ads to get, possible values are: BANNER, LEADERBOARD, SKYSCRAPER, FULL, VIDEO, ZIP, CONFIG. Use this instead of suffix. (Optional)
#' var_account_id <- 56 # integer | The logged in user. (Optional)
#' var_app_version <- "app_version_example" # character | The version of the application, will not return levels newer than the appVersion. (Optional)
#' var_latitude <- 3.4 # numeric | The current location of the requesting device (Optional)
#' var_longitude <- 3.4 # numeric | The current location of the requesting device (Optional)
#' var_device <- "device_example" # character | Should use deviceId if possible. The name of the requesting device; possible values are: Android, iPhone, iPad, iPod, etc. use /audience/devices for list. (Optional)
#' var_device_identifier <- 56 # integer | The device ID of the requesting device, use /audience/devices for list (Optional)
#' var_device_version <- "device_version_example" # character | The requesting device version; examples are: 2.3, 5.1.4, 6.1.4. Supports X, X.X, or X.X.X formated string. (Optional)
#' var_start <- 56 # integer | The index into the record set to start with. (Optional)
#' var_limit <- 56 # integer | The total number of record to return. (Optional)
#' var_include_game_data <- "include_game_data_example" # character | Include the game level data with the mission. (Optional)
#' var_include_audiences <- "include_audiences_example" # character | If true then return the audience data in the response. Default is false. (Optional)
#' var_allocates_tickets <- "allocates_tickets_example" # character | If true/false only return missions whose game levels allocate (or don't allocate) tickets. Do not provide a value to return both. (Optional)
#' var_randomize <- "randomize_example" # character | return a random set of results, default is true. If false returns in natural order. (Optional)
#' var_targeted_ads_only <- "targeted_ads_only_example" # character | return only ads targeted to the specific app, no global ads. (Optional)
#' var_mission_ids <- "mission_ids_example" # character | return only ads from the specified campaigns. (Optional)
#' var_audience_operator <- "audience_operator_example" # character | will return the items that have at least 1 or all of their audiences exist in the logged in user’s audiences, depending if the value is OR or AND (Optional)
#'
#' #Find Missions
#' api_instance <- MissionApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FindMissions(var_app_key, suffix = var_suffix, type = var_type, account_id = var_account_id, app_version = var_app_version, latitude = var_latitude, longitude = var_longitude, device = var_device, device_identifier = var_device_identifier, device_version = var_device_version, start = var_start, limit = var_limit, include_game_data = var_include_game_data, include_audiences = var_include_audiences, allocates_tickets = var_allocates_tickets, randomize = var_randomize, targeted_ads_only = var_targeted_ads_only, mission_ids = var_mission_ids, audience_operator = var_audience_operatordata_file = "result.txt")
#' result <- api_instance$FindMissions(var_app_key, suffix = var_suffix, type = var_type, account_id = var_account_id, app_version = var_app_version, latitude = var_latitude, longitude = var_longitude, device = var_device, device_identifier = var_device_identifier, device_version = var_device_version, start = var_start, limit = var_limit, include_game_data = var_include_game_data, include_audiences = var_include_audiences, allocates_tickets = var_allocates_tickets, randomize = var_randomize, targeted_ads_only = var_targeted_ads_only, mission_ids = var_mission_ids, audience_operator = var_audience_operator)
#' dput(result)
#'
#'
#' ####################  GetMission  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_mission_id <- 56 # integer | The id of the mission to return.
#' var_return_creative <- "return_creative_example" # character | Return creatives associated with the mission when true (Optional)
#'
#' #Get Mission
#' api_instance <- MissionApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetMission(var_account_id, var_mission_id, return_creative = var_return_creativedata_file = "result.txt")
#' result <- api_instance$GetMission(var_account_id, var_mission_id, return_creative = var_return_creative)
#' dput(result)
#'
#'
#' ####################  ImportMission  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_latitude <- 3.4 # numeric | The current location of the requesting device
#' var_longitude <- 3.4 # numeric | The current location of the requesting device
#' var_app_key <- "app_key_example" # character | the application key
#' var_keyword <- "keyword_example" # character | the keyword of the mission (Optional)
#' var_start <- 56 # integer | The starting index in the result set to return. Default is 0. (Optional)
#' var_limit <- 56 # integer | The total number of records to return. Default is 20. (Optional)
#' var_ad_size <- "ad_size_example" # character | the size of the ad (Optional)
#'
#' #Import Mission
#' api_instance <- MissionApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ImportMission(var_account_id, var_latitude, var_longitude, var_app_key, keyword = var_keyword, start = var_start, limit = var_limit, ad_size = var_ad_sizedata_file = "result.txt")
#' result <- api_instance$ImportMission(var_account_id, var_latitude, var_longitude, var_app_key, keyword = var_keyword, start = var_start, limit = var_limit, ad_size = var_ad_size)
#' dput(result)
#'
#'
#' ####################  SearchMissionFormats  ####################
#'
#' library(openapi)
#' var_start <- 56 # integer | The starting index in the result set to return. Default is 0.
#' var_limit <- 56 # integer | The total number of records to return. Default is 20.
#' var_active_only <- "active_only_example" # character | Determines whether to return only active results. Default is false.
#'
#' #Search Mission Formats
#' api_instance <- MissionApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchMissionFormats(var_start, var_limit, var_active_onlydata_file = "result.txt")
#' result <- api_instance$SearchMissionFormats(var_start, var_limit, var_active_only)
#' dput(result)
#'
#'
#' ####################  SearchMissions  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_keyword <- "keyword_example" # character | Filter by keyword (Optional)
#' var_sub_type <- "sub_type_example" # character | Custom string client apps can use for searching/filtering missions (Optional)
#' var_start <- 56 # integer | The index into the record set to start with. Default is 0. (Optional)
#' var_limit <- 56 # integer | The total number of record to return. Default is 20, maximum is 100. (Optional)
#' var_include_game_data <- "include_game_data_example" # character | If true then return the game level data in the response. Default is false. (Optional)
#' var_include_audiences <- "include_audiences_example" # character | If true then return the audience data in the response. Default is false. (Optional)
#' var_include_inactive <- "include_inactive_example" # character | If true then include inactive missions. Default is false. (Optional)
#' var_suffix <- "suffix_example" # character | A list of game types to include; click_leaderboard, click_banner, click_skyscraper, click_full, click_video, click_zip, create, vote, question, invite, share, json, xml (Optional)
#' var_sort_field <- "sort_field_example" # character | The field to sort the search on (for example TITLE) (Optional)
#' var_descending <- "descending_example" # character | Whether to sort in descending order (default true) (Optional)
#'
#' #Search Missions
#' api_instance <- MissionApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchMissions(var_account_id, keyword = var_keyword, sub_type = var_sub_type, start = var_start, limit = var_limit, include_game_data = var_include_game_data, include_audiences = var_include_audiences, include_inactive = var_include_inactive, suffix = var_suffix, sort_field = var_sort_field, descending = var_descendingdata_file = "result.txt")
#' result <- api_instance$SearchMissions(var_account_id, keyword = var_keyword, sub_type = var_sub_type, start = var_start, limit = var_limit, include_game_data = var_include_game_data, include_audiences = var_include_audiences, include_inactive = var_include_inactive, suffix = var_suffix, sort_field = var_sort_field, descending = var_descending)
#' dput(result)
#'
#'
#' ####################  SearchMissionsByBillableEntity  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_keyword <- "keyword_example" # character | Filter by keyword (Optional)
#' var_start <- 56 # integer | The index into the record set to start with. Default is 0. (Optional)
#' var_limit <- 56 # integer | The total number of record to return. Default id 20. (Optional)
#' var_include_game_data <- "include_game_data_example" # character | If true then return the game level data in the response. Default is false. (Optional)
#' var_include_audiences <- "include_audiences_example" # character | If true then return the audience data in the response. Default is false. (Optional)
#' var_include_inactive <- "include_inactive_example" # character | If true then include inactive missions. Default is false. (Optional)
#' var_suffix <- "suffix_example" # character | A list of game types to include; click_leaderboard, click_banner, click_skyscraper, click_full, click_video, click_zip, create, vote, question, invite, share, json, xml (Optional)
#' var_sort_field <- "sort_field_example" # character | The field to sort the search on (for example TITLE) (Optional)
#' var_descending <- "descending_example" # character | Whether to sort in descending order (default true) (Optional)
#'
#' #Search Missions by Billable Entity
#' api_instance <- MissionApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchMissionsByBillableEntity(var_account_id, keyword = var_keyword, start = var_start, limit = var_limit, include_game_data = var_include_game_data, include_audiences = var_include_audiences, include_inactive = var_include_inactive, suffix = var_suffix, sort_field = var_sort_field, descending = var_descendingdata_file = "result.txt")
#' result <- api_instance$SearchMissionsByBillableEntity(var_account_id, keyword = var_keyword, start = var_start, limit = var_limit, include_game_data = var_include_game_data, include_audiences = var_include_audiences, include_inactive = var_include_inactive, suffix = var_suffix, sort_field = var_sort_field, descending = var_descending)
#' dput(result)
#'
#'
#' ####################  UpdateMission  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_mission_id <- 56 # integer | The id of the mission to update.
#' var_title <- "title_example" # character | The title of the mission (Optional)
#' var_description <- "description_example" # character | The description of the mission (Optional)
#' var_sub_type <- "sub_type_example" # character | Custom string client apps can use for searching/filtering missions (Optional)
#' var_meta_data <- "meta_data_example" # character | external custom client defined data (Optional)
#' var_start_date <- 56 # integer | The date/time to start the mission, send 0 to unset (Optional)
#' var_end_date <- 56 # integer | The date/time to end the mission, send 0 to unset (Optional)
#' var_active <- "active_example" # character | Activate/deactivate the mission (Optional)
#' var_game_level_ids <- "game_level_ids_example" # character | the game level ids to include in the mission, comma separated (Optional)
#' var_creative_ids <- "creative_ids_example" # character |  (Optional)
#' var_audience_ids <- "audience_ids_example" # character |  (Optional)
#' var_offer_id <- 56 # integer | The offerId to give as a reward (Optional)
#' var_balance <- 3.4 # numeric | Replace the amount of money available to spend, once 0 deactivate mission. Set to a negative value for unlimited. (Optional)
#' var_advanced_reporting <- "advanced_reporting_example" # character | Flag to mark the mission as eligible for advanced reporting (Optional)
#' var_allocate_tickets <- "allocate_tickets_example" # character | Flag to indicate owner should receive tickets for completed missions (Optional)
#' var_ticket_count <- 56 # integer | The number of tickets to reward (Optional)
#' var_ticket_type <- "ticket_type_example" # character | The type of ticket to reward, null means default type (Optional)
#' var_points <- 56 # integer | The number of points to award for completing a mission (Optional)
#' var_application_ids <- "application_ids_example" # character | Comma separated list of application ids (Optional)
#' var_devices <- "devices_example" # character | Deprecated parameter for device names (Optional)
#' var_device_ids <- "device_ids_example" # character | Comma separated list of device ids (Optional)
#' var_device_versions <- "device_versions_example" # character | Comma separated list of device version ranges (Optional)
#' var_locations <- "locations_example" # character | List of lat/long pairs for mission locations (Optional)
#' var_radius <- "radius_example" # character | Comma separated list of radii for locations (Optional)
#'
#' #Update Mission
#' api_instance <- MissionApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateMission(var_account_id, var_mission_id, title = var_title, description = var_description, sub_type = var_sub_type, meta_data = var_meta_data, start_date = var_start_date, end_date = var_end_date, active = var_active, game_level_ids = var_game_level_ids, creative_ids = var_creative_ids, audience_ids = var_audience_ids, offer_id = var_offer_id, balance = var_balance, advanced_reporting = var_advanced_reporting, allocate_tickets = var_allocate_tickets, ticket_count = var_ticket_count, ticket_type = var_ticket_type, points = var_points, application_ids = var_application_ids, devices = var_devices, device_ids = var_device_ids, device_versions = var_device_versions, locations = var_locations, radius = var_radiusdata_file = "result.txt")
#' result <- api_instance$UpdateMission(var_account_id, var_mission_id, title = var_title, description = var_description, sub_type = var_sub_type, meta_data = var_meta_data, start_date = var_start_date, end_date = var_end_date, active = var_active, game_level_ids = var_game_level_ids, creative_ids = var_creative_ids, audience_ids = var_audience_ids, offer_id = var_offer_id, balance = var_balance, advanced_reporting = var_advanced_reporting, allocate_tickets = var_allocate_tickets, ticket_count = var_ticket_count, ticket_type = var_ticket_type, points = var_points, application_ids = var_application_ids, devices = var_devices, device_ids = var_device_ids, device_versions = var_device_versions, locations = var_locations, radius = var_radius)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
MissionApi <- R6::R6Class(
  "MissionApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new MissionApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Mission
    #'
    #' @param account_id The logged in user.
    #' @param title The title of the mission
    #' @param description (optional) The description of the mission
    #' @param sub_type (optional) Custom string client apps can use for searching/filtering missions
    #' @param start_date (optional) The date/time to start the mission, send 0 to unset
    #' @param end_date (optional) The date/time to end the mission, send 0 to unset
    #' @param active (optional) Activate/deactivate the mission
    #' @param game_level_ids (optional) the game level ids to include in the mission, comma separated
    #' @param creative_ids (optional) creatives to associate with the mission
    #' @param audience_ids (optional) audiences to associate with the mission
    #' @param mission_task (optional) This parameter is deprecated.
    #' @param format_type (optional) The string identifier for a mission format (this is not the missionFormatId)
    #' @param offer_id (optional) The offerId to give as a reward
    #' @param balance (optional) Set the amount of money available to spend, once 0 deactivate mission. Set to a negative value for unlimited.
    #' @param advanced_reporting (optional) Flag to mark the mission as eligible for advanced reporting
    #' @param allocate_tickets (optional) Flag to indicate owner should receive tickets for completed missions
    #' @param ticket_count (optional) The number of tickets to reward
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a mission
    #' @param meta_data (optional) external custom client defined data
    #' @param application_ids (optional) Comma separated list of application ids
    #' @param devices (optional) Deprecated parameter for device names
    #' @param device_ids (optional) Comma separated list of device ids
    #' @param device_versions (optional) Comma separated list of device version ranges
    #' @param locations (optional) List of lat/long pairs for mission locations
    #' @param radius (optional) Comma separated list of radii for locations
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MissionResponse
    CreateMission = function(account_id, title, description = NULL, sub_type = NULL, start_date = NULL, end_date = NULL, active = NULL, game_level_ids = NULL, creative_ids = NULL, audience_ids = NULL, mission_task = NULL, format_type = NULL, offer_id = NULL, balance = NULL, advanced_reporting = NULL, allocate_tickets = NULL, ticket_count = NULL, ticket_type = NULL, points = NULL, meta_data = NULL, application_ids = NULL, devices = NULL, device_ids = NULL, device_versions = NULL, locations = NULL, radius = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateMissionWithHttpInfo(account_id, title, description, sub_type, start_date, end_date, active, game_level_ids, creative_ids, audience_ids, mission_task, format_type, offer_id, balance, advanced_reporting, allocate_tickets, ticket_count, ticket_type, points, meta_data, application_ids, devices, device_ids, device_versions, locations, radius, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Mission
    #'
    #' @param account_id The logged in user.
    #' @param title The title of the mission
    #' @param description (optional) The description of the mission
    #' @param sub_type (optional) Custom string client apps can use for searching/filtering missions
    #' @param start_date (optional) The date/time to start the mission, send 0 to unset
    #' @param end_date (optional) The date/time to end the mission, send 0 to unset
    #' @param active (optional) Activate/deactivate the mission
    #' @param game_level_ids (optional) the game level ids to include in the mission, comma separated
    #' @param creative_ids (optional) creatives to associate with the mission
    #' @param audience_ids (optional) audiences to associate with the mission
    #' @param mission_task (optional) This parameter is deprecated.
    #' @param format_type (optional) The string identifier for a mission format (this is not the missionFormatId)
    #' @param offer_id (optional) The offerId to give as a reward
    #' @param balance (optional) Set the amount of money available to spend, once 0 deactivate mission. Set to a negative value for unlimited.
    #' @param advanced_reporting (optional) Flag to mark the mission as eligible for advanced reporting
    #' @param allocate_tickets (optional) Flag to indicate owner should receive tickets for completed missions
    #' @param ticket_count (optional) The number of tickets to reward
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a mission
    #' @param meta_data (optional) external custom client defined data
    #' @param application_ids (optional) Comma separated list of application ids
    #' @param devices (optional) Deprecated parameter for device names
    #' @param device_ids (optional) Comma separated list of device ids
    #' @param device_versions (optional) Comma separated list of device version ranges
    #' @param locations (optional) List of lat/long pairs for mission locations
    #' @param radius (optional) Comma separated list of radii for locations
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MissionResponse) with additional information such as HTTP status code, headers
    CreateMissionWithHttpInfo = function(account_id, title, description = NULL, sub_type = NULL, start_date = NULL, end_date = NULL, active = NULL, game_level_ids = NULL, creative_ids = NULL, audience_ids = NULL, mission_task = NULL, format_type = NULL, offer_id = NULL, balance = NULL, advanced_reporting = NULL, allocate_tickets = NULL, ticket_count = NULL, ticket_type = NULL, points = NULL, meta_data = NULL, application_ids = NULL, devices = NULL, device_ids = NULL, device_versions = NULL, locations = NULL, radius = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`title`)) {
        stop("Missing required parameter `title`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionApi$CreateMission, `account_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling MissionApi$CreateMission, `title` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling MissionApi$CreateMission, `description` is not nullable")
      }

      if (!missing(`sub_type`) && is.null(`sub_type`)) {
        stop("Invalid value for `sub_type` when calling MissionApi$CreateMission, `sub_type` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling MissionApi$CreateMission, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling MissionApi$CreateMission, `end_date` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling MissionApi$CreateMission, `active` is not nullable")
      }

      if (!missing(`game_level_ids`) && is.null(`game_level_ids`)) {
        stop("Invalid value for `game_level_ids` when calling MissionApi$CreateMission, `game_level_ids` is not nullable")
      }

      if (!missing(`creative_ids`) && is.null(`creative_ids`)) {
        stop("Invalid value for `creative_ids` when calling MissionApi$CreateMission, `creative_ids` is not nullable")
      }

      if (!missing(`audience_ids`) && is.null(`audience_ids`)) {
        stop("Invalid value for `audience_ids` when calling MissionApi$CreateMission, `audience_ids` is not nullable")
      }

      if (!missing(`mission_task`) && is.null(`mission_task`)) {
        stop("Invalid value for `mission_task` when calling MissionApi$CreateMission, `mission_task` is not nullable")
      }

      if (!missing(`format_type`) && is.null(`format_type`)) {
        stop("Invalid value for `format_type` when calling MissionApi$CreateMission, `format_type` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling MissionApi$CreateMission, `offer_id` is not nullable")
      }

      if (!missing(`balance`) && is.null(`balance`)) {
        stop("Invalid value for `balance` when calling MissionApi$CreateMission, `balance` is not nullable")
      }

      if (!missing(`advanced_reporting`) && is.null(`advanced_reporting`)) {
        stop("Invalid value for `advanced_reporting` when calling MissionApi$CreateMission, `advanced_reporting` is not nullable")
      }

      if (!missing(`allocate_tickets`) && is.null(`allocate_tickets`)) {
        stop("Invalid value for `allocate_tickets` when calling MissionApi$CreateMission, `allocate_tickets` is not nullable")
      }

      if (!missing(`ticket_count`) && is.null(`ticket_count`)) {
        stop("Invalid value for `ticket_count` when calling MissionApi$CreateMission, `ticket_count` is not nullable")
      }

      if (!missing(`ticket_type`) && is.null(`ticket_type`)) {
        stop("Invalid value for `ticket_type` when calling MissionApi$CreateMission, `ticket_type` is not nullable")
      }

      if (!missing(`points`) && is.null(`points`)) {
        stop("Invalid value for `points` when calling MissionApi$CreateMission, `points` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling MissionApi$CreateMission, `meta_data` is not nullable")
      }

      if (!missing(`application_ids`) && is.null(`application_ids`)) {
        stop("Invalid value for `application_ids` when calling MissionApi$CreateMission, `application_ids` is not nullable")
      }

      if (!missing(`devices`) && is.null(`devices`)) {
        stop("Invalid value for `devices` when calling MissionApi$CreateMission, `devices` is not nullable")
      }

      if (!missing(`device_ids`) && is.null(`device_ids`)) {
        stop("Invalid value for `device_ids` when calling MissionApi$CreateMission, `device_ids` is not nullable")
      }

      if (!missing(`device_versions`) && is.null(`device_versions`)) {
        stop("Invalid value for `device_versions` when calling MissionApi$CreateMission, `device_versions` is not nullable")
      }

      if (!missing(`locations`) && is.null(`locations`)) {
        stop("Invalid value for `locations` when calling MissionApi$CreateMission, `locations` is not nullable")
      }

      if (!missing(`radius`) && is.null(`radius`)) {
        stop("Invalid value for `radius` when calling MissionApi$CreateMission, `radius` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["subType"]] <- `sub_type`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["active"]] <- `active`

      query_params[["gameLevelIds"]] <- `game_level_ids`

      query_params[["creativeIds"]] <- `creative_ids`

      query_params[["audienceIds"]] <- `audience_ids`

      query_params[["missionTask"]] <- `mission_task`

      query_params[["formatType"]] <- `format_type`

      query_params[["offerId"]] <- `offer_id`

      query_params[["balance"]] <- `balance`

      query_params[["advancedReporting"]] <- `advanced_reporting`

      query_params[["allocateTickets"]] <- `allocate_tickets`

      query_params[["ticketCount"]] <- `ticket_count`

      query_params[["ticketType"]] <- `ticket_type`

      query_params[["points"]] <- `points`

      query_params[["metaData"]] <- `meta_data`

      query_params[["applicationIds"]] <- `application_ids`

      query_params[["devices"]] <- `devices`

      query_params[["deviceIds"]] <- `device_ids`

      query_params[["deviceVersions"]] <- `device_versions`

      query_params[["locations"]] <- `locations`

      query_params[["radius"]] <- `radius`

      local_var_url_path <- "/mission/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MissionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Mission
    #'
    #' @param account_id the id of the logged in user
    #' @param mission_id the id of the mission to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteMission = function(account_id, mission_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteMissionWithHttpInfo(account_id, mission_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Mission
    #'
    #' @param account_id the id of the logged in user
    #' @param mission_id the id of the mission to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteMissionWithHttpInfo = function(account_id, mission_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`mission_id`)) {
        stop("Missing required parameter `mission_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionApi$DeleteMission, `account_id` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling MissionApi$DeleteMission, `mission_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["missionId"]] <- `mission_id`

      local_var_url_path <- "/mission/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Find Missions
    #'
    #' @param app_key The application key, if provided return missions specific for the app. Will always return mission levels that are app agnostic.
    #' @param suffix (optional) The type of mission to get, possible values are: click_banner, click_leaderboard, click_skyscraper, click_full, click_video, or click_zip
    #' @param type (optional) The type of ads to get, possible values are: BANNER, LEADERBOARD, SKYSCRAPER, FULL, VIDEO, ZIP, CONFIG. Use this instead of suffix.
    #' @param account_id (optional) The logged in user.
    #' @param app_version (optional) The version of the application, will not return levels newer than the appVersion.
    #' @param latitude (optional) The current location of the requesting device
    #' @param longitude (optional) The current location of the requesting device
    #' @param device (optional) Should use deviceId if possible. The name of the requesting device; possible values are: Android, iPhone, iPad, iPod, etc. use /audience/devices for list.
    #' @param device_identifier (optional) The device ID of the requesting device, use /audience/devices for list
    #' @param device_version (optional) The requesting device version; examples are: 2.3, 5.1.4, 6.1.4. Supports X, X.X, or X.X.X formated string.
    #' @param start (optional) The index into the record set to start with.
    #' @param limit (optional) The total number of record to return.
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param include_audiences (optional) If true then return the audience data in the response. Default is false.
    #' @param allocates_tickets (optional) If true/false only return missions whose game levels allocate (or don't allocate) tickets. Do not provide a value to return both.
    #' @param randomize (optional) return a random set of results, default is true. If false returns in natural order.
    #' @param targeted_ads_only (optional) return only ads targeted to the specific app, no global ads.
    #' @param mission_ids (optional) return only ads from the specified campaigns.
    #' @param audience_operator (optional) will return the items that have at least 1 or all of their audiences exist in the logged in user’s audiences, depending if the value is OR or AND
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MissionResponse
    FindMissions = function(app_key, suffix = NULL, type = NULL, account_id = NULL, app_version = NULL, latitude = NULL, longitude = NULL, device = NULL, device_identifier = NULL, device_version = NULL, start = NULL, limit = NULL, include_game_data = NULL, include_audiences = NULL, allocates_tickets = NULL, randomize = NULL, targeted_ads_only = NULL, mission_ids = NULL, audience_operator = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$FindMissionsWithHttpInfo(app_key, suffix, type, account_id, app_version, latitude, longitude, device, device_identifier, device_version, start, limit, include_game_data, include_audiences, allocates_tickets, randomize, targeted_ads_only, mission_ids, audience_operator, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Find Missions
    #'
    #' @param app_key The application key, if provided return missions specific for the app. Will always return mission levels that are app agnostic.
    #' @param suffix (optional) The type of mission to get, possible values are: click_banner, click_leaderboard, click_skyscraper, click_full, click_video, or click_zip
    #' @param type (optional) The type of ads to get, possible values are: BANNER, LEADERBOARD, SKYSCRAPER, FULL, VIDEO, ZIP, CONFIG. Use this instead of suffix.
    #' @param account_id (optional) The logged in user.
    #' @param app_version (optional) The version of the application, will not return levels newer than the appVersion.
    #' @param latitude (optional) The current location of the requesting device
    #' @param longitude (optional) The current location of the requesting device
    #' @param device (optional) Should use deviceId if possible. The name of the requesting device; possible values are: Android, iPhone, iPad, iPod, etc. use /audience/devices for list.
    #' @param device_identifier (optional) The device ID of the requesting device, use /audience/devices for list
    #' @param device_version (optional) The requesting device version; examples are: 2.3, 5.1.4, 6.1.4. Supports X, X.X, or X.X.X formated string.
    #' @param start (optional) The index into the record set to start with.
    #' @param limit (optional) The total number of record to return.
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param include_audiences (optional) If true then return the audience data in the response. Default is false.
    #' @param allocates_tickets (optional) If true/false only return missions whose game levels allocate (or don't allocate) tickets. Do not provide a value to return both.
    #' @param randomize (optional) return a random set of results, default is true. If false returns in natural order.
    #' @param targeted_ads_only (optional) return only ads targeted to the specific app, no global ads.
    #' @param mission_ids (optional) return only ads from the specified campaigns.
    #' @param audience_operator (optional) will return the items that have at least 1 or all of their audiences exist in the logged in user’s audiences, depending if the value is OR or AND
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MissionResponse) with additional information such as HTTP status code, headers
    FindMissionsWithHttpInfo = function(app_key, suffix = NULL, type = NULL, account_id = NULL, app_version = NULL, latitude = NULL, longitude = NULL, device = NULL, device_identifier = NULL, device_version = NULL, start = NULL, limit = NULL, include_game_data = NULL, include_audiences = NULL, allocates_tickets = NULL, randomize = NULL, targeted_ads_only = NULL, mission_ids = NULL, audience_operator = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling MissionApi$FindMissions, `app_key` is not nullable")
      }

      if (!missing(`suffix`) && is.null(`suffix`)) {
        stop("Invalid value for `suffix` when calling MissionApi$FindMissions, `suffix` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling MissionApi$FindMissions, `type` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionApi$FindMissions, `account_id` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling MissionApi$FindMissions, `app_version` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling MissionApi$FindMissions, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling MissionApi$FindMissions, `longitude` is not nullable")
      }

      if (!missing(`device`) && is.null(`device`)) {
        stop("Invalid value for `device` when calling MissionApi$FindMissions, `device` is not nullable")
      }

      if (!missing(`device_identifier`) && is.null(`device_identifier`)) {
        stop("Invalid value for `device_identifier` when calling MissionApi$FindMissions, `device_identifier` is not nullable")
      }

      if (!missing(`device_version`) && is.null(`device_version`)) {
        stop("Invalid value for `device_version` when calling MissionApi$FindMissions, `device_version` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling MissionApi$FindMissions, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling MissionApi$FindMissions, `limit` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling MissionApi$FindMissions, `include_game_data` is not nullable")
      }

      if (!missing(`include_audiences`) && is.null(`include_audiences`)) {
        stop("Invalid value for `include_audiences` when calling MissionApi$FindMissions, `include_audiences` is not nullable")
      }

      if (!missing(`allocates_tickets`) && is.null(`allocates_tickets`)) {
        stop("Invalid value for `allocates_tickets` when calling MissionApi$FindMissions, `allocates_tickets` is not nullable")
      }

      if (!missing(`randomize`) && is.null(`randomize`)) {
        stop("Invalid value for `randomize` when calling MissionApi$FindMissions, `randomize` is not nullable")
      }

      if (!missing(`targeted_ads_only`) && is.null(`targeted_ads_only`)) {
        stop("Invalid value for `targeted_ads_only` when calling MissionApi$FindMissions, `targeted_ads_only` is not nullable")
      }

      if (!missing(`mission_ids`) && is.null(`mission_ids`)) {
        stop("Invalid value for `mission_ids` when calling MissionApi$FindMissions, `mission_ids` is not nullable")
      }

      if (!missing(`audience_operator`) && is.null(`audience_operator`)) {
        stop("Invalid value for `audience_operator` when calling MissionApi$FindMissions, `audience_operator` is not nullable")
      }

      query_params[["appKey"]] <- `app_key`

      query_params[["suffix"]] <- `suffix`

      query_params[["type"]] <- `type`

      query_params[["accountId"]] <- `account_id`

      query_params[["appVersion"]] <- `app_version`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["device"]] <- `device`

      query_params[["deviceIdentifier"]] <- `device_identifier`

      query_params[["deviceVersion"]] <- `device_version`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["includeGameData"]] <- `include_game_data`

      query_params[["includeAudiences"]] <- `include_audiences`

      query_params[["allocatesTickets"]] <- `allocates_tickets`

      query_params[["randomize"]] <- `randomize`

      query_params[["targetedAdsOnly"]] <- `targeted_ads_only`

      query_params[["missionIds"]] <- `mission_ids`

      query_params[["audienceOperator"]] <- `audience_operator`

      local_var_url_path <- "/mission/find"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MissionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Mission
    #'
    #' @param account_id The logged in user.
    #' @param mission_id The id of the mission to return.
    #' @param return_creative (optional) Return creatives associated with the mission when true
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MissionResponse
    GetMission = function(account_id, mission_id, return_creative = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetMissionWithHttpInfo(account_id, mission_id, return_creative, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Mission
    #'
    #' @param account_id The logged in user.
    #' @param mission_id The id of the mission to return.
    #' @param return_creative (optional) Return creatives associated with the mission when true
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MissionResponse) with additional information such as HTTP status code, headers
    GetMissionWithHttpInfo = function(account_id, mission_id, return_creative = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`mission_id`)) {
        stop("Missing required parameter `mission_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionApi$GetMission, `account_id` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling MissionApi$GetMission, `mission_id` is not nullable")
      }

      if (!missing(`return_creative`) && is.null(`return_creative`)) {
        stop("Invalid value for `return_creative` when calling MissionApi$GetMission, `return_creative` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["missionId"]] <- `mission_id`

      query_params[["returnCreative"]] <- `return_creative`

      local_var_url_path <- "/mission/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MissionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Import Mission
    #'
    #' @param account_id The logged in user.
    #' @param latitude The current location of the requesting device
    #' @param longitude The current location of the requesting device
    #' @param app_key the application key
    #' @param keyword (optional) the keyword of the mission
    #' @param start (optional) The starting index in the result set to return. Default is 0.
    #' @param limit (optional) The total number of records to return. Default is 20.
    #' @param ad_size (optional) the size of the ad
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    ImportMission = function(account_id, latitude, longitude, app_key, keyword = NULL, start = NULL, limit = NULL, ad_size = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ImportMissionWithHttpInfo(account_id, latitude, longitude, app_key, keyword, start, limit, ad_size, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Import Mission
    #'
    #' @param account_id The logged in user.
    #' @param latitude The current location of the requesting device
    #' @param longitude The current location of the requesting device
    #' @param app_key the application key
    #' @param keyword (optional) the keyword of the mission
    #' @param start (optional) The starting index in the result set to return. Default is 0.
    #' @param limit (optional) The total number of records to return. Default is 20.
    #' @param ad_size (optional) the size of the ad
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    ImportMissionWithHttpInfo = function(account_id, latitude, longitude, app_key, keyword = NULL, start = NULL, limit = NULL, ad_size = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`latitude`)) {
        stop("Missing required parameter `latitude`.")
      }

      if (missing(`longitude`)) {
        stop("Missing required parameter `longitude`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionApi$ImportMission, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling MissionApi$ImportMission, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling MissionApi$ImportMission, `longitude` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling MissionApi$ImportMission, `app_key` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling MissionApi$ImportMission, `keyword` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling MissionApi$ImportMission, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling MissionApi$ImportMission, `limit` is not nullable")
      }

      if (!missing(`ad_size`) && is.null(`ad_size`)) {
        stop("Invalid value for `ad_size` when calling MissionApi$ImportMission, `ad_size` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["keyword"]] <- `keyword`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["appKey"]] <- `app_key`

      if (!is.null(`ad_size`) && !(`ad_size` %in% c("CONFIG", "BANNER", "LEADERBOARD", "SKYSCRAPER", "VIDEO", "ZIP", "INTERSTITIAL", "CUSTOM1", "CUSTOM2", "CUSTOM3", "CUSTOM4", "CUSTOM5", "CUSTOM6", "CUSTOM7", "CUSTOM8", "CUSTOM9", "CUSTOM10"))) {
        stop("Invalid value for ad_size when calling MissionApi$ImportMission. Must be [CONFIG, BANNER, LEADERBOARD, SKYSCRAPER, VIDEO, ZIP, INTERSTITIAL, CUSTOM1, CUSTOM2, CUSTOM3, CUSTOM4, CUSTOM5, CUSTOM6, CUSTOM7, CUSTOM8, CUSTOM9, CUSTOM10].")
      }
      query_params[["adSize"]] <- `ad_size`

      local_var_url_path <- "/mission/import"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Mission Formats
    #'
    #' @param start The starting index in the result set to return. Default is 0.
    #' @param limit The total number of records to return. Default is 20.
    #' @param active_only Determines whether to return only active results. Default is false.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[MissionFormatResponse]
    SearchMissionFormats = function(start, limit, active_only, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchMissionFormatsWithHttpInfo(start, limit, active_only, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Mission Formats
    #'
    #' @param start The starting index in the result set to return. Default is 0.
    #' @param limit The total number of records to return. Default is 20.
    #' @param active_only Determines whether to return only active results. Default is false.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[MissionFormatResponse]) with additional information such as HTTP status code, headers
    SearchMissionFormatsWithHttpInfo = function(start, limit, active_only, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`active_only`)) {
        stop("Missing required parameter `active_only`.")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling MissionApi$SearchMissionFormats, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling MissionApi$SearchMissionFormats, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling MissionApi$SearchMissionFormats, `active_only` is not nullable")
      }

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      local_var_url_path <- "/mission/format/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[MissionFormatResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Missions
    #'
    #' @param account_id The logged in user.
    #' @param keyword (optional) Filter by keyword
    #' @param sub_type (optional) Custom string client apps can use for searching/filtering missions
    #' @param start (optional) The index into the record set to start with. Default is 0.
    #' @param limit (optional) The total number of record to return. Default is 20, maximum is 100.
    #' @param include_game_data (optional) If true then return the game level data in the response. Default is false.
    #' @param include_audiences (optional) If true then return the audience data in the response. Default is false.
    #' @param include_inactive (optional) If true then include inactive missions. Default is false.
    #' @param suffix (optional) A list of game types to include; click_leaderboard, click_banner, click_skyscraper, click_full, click_video, click_zip, create, vote, question, invite, share, json, xml
    #' @param sort_field (optional) The field to sort the search on (for example TITLE)
    #' @param descending (optional) Whether to sort in descending order (default true)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[MissionResponse]
    SearchMissions = function(account_id, keyword = NULL, sub_type = NULL, start = NULL, limit = NULL, include_game_data = NULL, include_audiences = NULL, include_inactive = NULL, suffix = NULL, sort_field = NULL, descending = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchMissionsWithHttpInfo(account_id, keyword, sub_type, start, limit, include_game_data, include_audiences, include_inactive, suffix, sort_field, descending, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Missions
    #'
    #' @param account_id The logged in user.
    #' @param keyword (optional) Filter by keyword
    #' @param sub_type (optional) Custom string client apps can use for searching/filtering missions
    #' @param start (optional) The index into the record set to start with. Default is 0.
    #' @param limit (optional) The total number of record to return. Default is 20, maximum is 100.
    #' @param include_game_data (optional) If true then return the game level data in the response. Default is false.
    #' @param include_audiences (optional) If true then return the audience data in the response. Default is false.
    #' @param include_inactive (optional) If true then include inactive missions. Default is false.
    #' @param suffix (optional) A list of game types to include; click_leaderboard, click_banner, click_skyscraper, click_full, click_video, click_zip, create, vote, question, invite, share, json, xml
    #' @param sort_field (optional) The field to sort the search on (for example TITLE)
    #' @param descending (optional) Whether to sort in descending order (default true)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[MissionResponse]) with additional information such as HTTP status code, headers
    SearchMissionsWithHttpInfo = function(account_id, keyword = NULL, sub_type = NULL, start = NULL, limit = NULL, include_game_data = NULL, include_audiences = NULL, include_inactive = NULL, suffix = NULL, sort_field = NULL, descending = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionApi$SearchMissions, `account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling MissionApi$SearchMissions, `keyword` is not nullable")
      }

      if (!missing(`sub_type`) && is.null(`sub_type`)) {
        stop("Invalid value for `sub_type` when calling MissionApi$SearchMissions, `sub_type` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling MissionApi$SearchMissions, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling MissionApi$SearchMissions, `limit` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling MissionApi$SearchMissions, `include_game_data` is not nullable")
      }

      if (!missing(`include_audiences`) && is.null(`include_audiences`)) {
        stop("Invalid value for `include_audiences` when calling MissionApi$SearchMissions, `include_audiences` is not nullable")
      }

      if (!missing(`include_inactive`) && is.null(`include_inactive`)) {
        stop("Invalid value for `include_inactive` when calling MissionApi$SearchMissions, `include_inactive` is not nullable")
      }

      if (!missing(`suffix`) && is.null(`suffix`)) {
        stop("Invalid value for `suffix` when calling MissionApi$SearchMissions, `suffix` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling MissionApi$SearchMissions, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling MissionApi$SearchMissions, `descending` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["subType"]] <- `sub_type`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["includeGameData"]] <- `include_game_data`

      query_params[["includeAudiences"]] <- `include_audiences`

      query_params[["includeInactive"]] <- `include_inactive`

      query_params[["suffix"]] <- `suffix`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      local_var_url_path <- "/mission/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[MissionResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Missions by Billable Entity
    #'
    #' @param account_id The logged in user.
    #' @param keyword (optional) Filter by keyword
    #' @param start (optional) The index into the record set to start with. Default is 0.
    #' @param limit (optional) The total number of record to return. Default id 20.
    #' @param include_game_data (optional) If true then return the game level data in the response. Default is false.
    #' @param include_audiences (optional) If true then return the audience data in the response. Default is false.
    #' @param include_inactive (optional) If true then include inactive missions. Default is false.
    #' @param suffix (optional) A list of game types to include; click_leaderboard, click_banner, click_skyscraper, click_full, click_video, click_zip, create, vote, question, invite, share, json, xml
    #' @param sort_field (optional) The field to sort the search on (for example TITLE)
    #' @param descending (optional) Whether to sort in descending order (default true)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[MissionResponse]
    SearchMissionsByBillableEntity = function(account_id, keyword = NULL, start = NULL, limit = NULL, include_game_data = NULL, include_audiences = NULL, include_inactive = NULL, suffix = NULL, sort_field = NULL, descending = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchMissionsByBillableEntityWithHttpInfo(account_id, keyword, start, limit, include_game_data, include_audiences, include_inactive, suffix, sort_field, descending, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Missions by Billable Entity
    #'
    #' @param account_id The logged in user.
    #' @param keyword (optional) Filter by keyword
    #' @param start (optional) The index into the record set to start with. Default is 0.
    #' @param limit (optional) The total number of record to return. Default id 20.
    #' @param include_game_data (optional) If true then return the game level data in the response. Default is false.
    #' @param include_audiences (optional) If true then return the audience data in the response. Default is false.
    #' @param include_inactive (optional) If true then include inactive missions. Default is false.
    #' @param suffix (optional) A list of game types to include; click_leaderboard, click_banner, click_skyscraper, click_full, click_video, click_zip, create, vote, question, invite, share, json, xml
    #' @param sort_field (optional) The field to sort the search on (for example TITLE)
    #' @param descending (optional) Whether to sort in descending order (default true)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[MissionResponse]) with additional information such as HTTP status code, headers
    SearchMissionsByBillableEntityWithHttpInfo = function(account_id, keyword = NULL, start = NULL, limit = NULL, include_game_data = NULL, include_audiences = NULL, include_inactive = NULL, suffix = NULL, sort_field = NULL, descending = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionApi$SearchMissionsByBillableEntity, `account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling MissionApi$SearchMissionsByBillableEntity, `keyword` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling MissionApi$SearchMissionsByBillableEntity, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling MissionApi$SearchMissionsByBillableEntity, `limit` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling MissionApi$SearchMissionsByBillableEntity, `include_game_data` is not nullable")
      }

      if (!missing(`include_audiences`) && is.null(`include_audiences`)) {
        stop("Invalid value for `include_audiences` when calling MissionApi$SearchMissionsByBillableEntity, `include_audiences` is not nullable")
      }

      if (!missing(`include_inactive`) && is.null(`include_inactive`)) {
        stop("Invalid value for `include_inactive` when calling MissionApi$SearchMissionsByBillableEntity, `include_inactive` is not nullable")
      }

      if (!missing(`suffix`) && is.null(`suffix`)) {
        stop("Invalid value for `suffix` when calling MissionApi$SearchMissionsByBillableEntity, `suffix` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling MissionApi$SearchMissionsByBillableEntity, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling MissionApi$SearchMissionsByBillableEntity, `descending` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["includeGameData"]] <- `include_game_data`

      query_params[["includeAudiences"]] <- `include_audiences`

      query_params[["includeInactive"]] <- `include_inactive`

      query_params[["suffix"]] <- `suffix`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      local_var_url_path <- "/mission/searchByBillableEntity"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[MissionResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Mission
    #'
    #' @param account_id The logged in user.
    #' @param mission_id The id of the mission to update.
    #' @param title (optional) The title of the mission
    #' @param description (optional) The description of the mission
    #' @param sub_type (optional) Custom string client apps can use for searching/filtering missions
    #' @param meta_data (optional) external custom client defined data
    #' @param start_date (optional) The date/time to start the mission, send 0 to unset
    #' @param end_date (optional) The date/time to end the mission, send 0 to unset
    #' @param active (optional) Activate/deactivate the mission
    #' @param game_level_ids (optional) the game level ids to include in the mission, comma separated
    #' @param creative_ids (optional) 
    #' @param audience_ids (optional) 
    #' @param offer_id (optional) The offerId to give as a reward
    #' @param balance (optional) Replace the amount of money available to spend, once 0 deactivate mission. Set to a negative value for unlimited.
    #' @param advanced_reporting (optional) Flag to mark the mission as eligible for advanced reporting
    #' @param allocate_tickets (optional) Flag to indicate owner should receive tickets for completed missions
    #' @param ticket_count (optional) The number of tickets to reward
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a mission
    #' @param application_ids (optional) Comma separated list of application ids
    #' @param devices (optional) Deprecated parameter for device names
    #' @param device_ids (optional) Comma separated list of device ids
    #' @param device_versions (optional) Comma separated list of device version ranges
    #' @param locations (optional) List of lat/long pairs for mission locations
    #' @param radius (optional) Comma separated list of radii for locations
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MissionResponse
    UpdateMission = function(account_id, mission_id, title = NULL, description = NULL, sub_type = NULL, meta_data = NULL, start_date = NULL, end_date = NULL, active = NULL, game_level_ids = NULL, creative_ids = NULL, audience_ids = NULL, offer_id = NULL, balance = NULL, advanced_reporting = NULL, allocate_tickets = NULL, ticket_count = NULL, ticket_type = NULL, points = NULL, application_ids = NULL, devices = NULL, device_ids = NULL, device_versions = NULL, locations = NULL, radius = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateMissionWithHttpInfo(account_id, mission_id, title, description, sub_type, meta_data, start_date, end_date, active, game_level_ids, creative_ids, audience_ids, offer_id, balance, advanced_reporting, allocate_tickets, ticket_count, ticket_type, points, application_ids, devices, device_ids, device_versions, locations, radius, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Mission
    #'
    #' @param account_id The logged in user.
    #' @param mission_id The id of the mission to update.
    #' @param title (optional) The title of the mission
    #' @param description (optional) The description of the mission
    #' @param sub_type (optional) Custom string client apps can use for searching/filtering missions
    #' @param meta_data (optional) external custom client defined data
    #' @param start_date (optional) The date/time to start the mission, send 0 to unset
    #' @param end_date (optional) The date/time to end the mission, send 0 to unset
    #' @param active (optional) Activate/deactivate the mission
    #' @param game_level_ids (optional) the game level ids to include in the mission, comma separated
    #' @param creative_ids (optional) 
    #' @param audience_ids (optional) 
    #' @param offer_id (optional) The offerId to give as a reward
    #' @param balance (optional) Replace the amount of money available to spend, once 0 deactivate mission. Set to a negative value for unlimited.
    #' @param advanced_reporting (optional) Flag to mark the mission as eligible for advanced reporting
    #' @param allocate_tickets (optional) Flag to indicate owner should receive tickets for completed missions
    #' @param ticket_count (optional) The number of tickets to reward
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a mission
    #' @param application_ids (optional) Comma separated list of application ids
    #' @param devices (optional) Deprecated parameter for device names
    #' @param device_ids (optional) Comma separated list of device ids
    #' @param device_versions (optional) Comma separated list of device version ranges
    #' @param locations (optional) List of lat/long pairs for mission locations
    #' @param radius (optional) Comma separated list of radii for locations
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MissionResponse) with additional information such as HTTP status code, headers
    UpdateMissionWithHttpInfo = function(account_id, mission_id, title = NULL, description = NULL, sub_type = NULL, meta_data = NULL, start_date = NULL, end_date = NULL, active = NULL, game_level_ids = NULL, creative_ids = NULL, audience_ids = NULL, offer_id = NULL, balance = NULL, advanced_reporting = NULL, allocate_tickets = NULL, ticket_count = NULL, ticket_type = NULL, points = NULL, application_ids = NULL, devices = NULL, device_ids = NULL, device_versions = NULL, locations = NULL, radius = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`mission_id`)) {
        stop("Missing required parameter `mission_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionApi$UpdateMission, `account_id` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling MissionApi$UpdateMission, `mission_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling MissionApi$UpdateMission, `title` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling MissionApi$UpdateMission, `description` is not nullable")
      }

      if (!missing(`sub_type`) && is.null(`sub_type`)) {
        stop("Invalid value for `sub_type` when calling MissionApi$UpdateMission, `sub_type` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling MissionApi$UpdateMission, `meta_data` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling MissionApi$UpdateMission, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling MissionApi$UpdateMission, `end_date` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling MissionApi$UpdateMission, `active` is not nullable")
      }

      if (!missing(`game_level_ids`) && is.null(`game_level_ids`)) {
        stop("Invalid value for `game_level_ids` when calling MissionApi$UpdateMission, `game_level_ids` is not nullable")
      }

      if (!missing(`creative_ids`) && is.null(`creative_ids`)) {
        stop("Invalid value for `creative_ids` when calling MissionApi$UpdateMission, `creative_ids` is not nullable")
      }

      if (!missing(`audience_ids`) && is.null(`audience_ids`)) {
        stop("Invalid value for `audience_ids` when calling MissionApi$UpdateMission, `audience_ids` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling MissionApi$UpdateMission, `offer_id` is not nullable")
      }

      if (!missing(`balance`) && is.null(`balance`)) {
        stop("Invalid value for `balance` when calling MissionApi$UpdateMission, `balance` is not nullable")
      }

      if (!missing(`advanced_reporting`) && is.null(`advanced_reporting`)) {
        stop("Invalid value for `advanced_reporting` when calling MissionApi$UpdateMission, `advanced_reporting` is not nullable")
      }

      if (!missing(`allocate_tickets`) && is.null(`allocate_tickets`)) {
        stop("Invalid value for `allocate_tickets` when calling MissionApi$UpdateMission, `allocate_tickets` is not nullable")
      }

      if (!missing(`ticket_count`) && is.null(`ticket_count`)) {
        stop("Invalid value for `ticket_count` when calling MissionApi$UpdateMission, `ticket_count` is not nullable")
      }

      if (!missing(`ticket_type`) && is.null(`ticket_type`)) {
        stop("Invalid value for `ticket_type` when calling MissionApi$UpdateMission, `ticket_type` is not nullable")
      }

      if (!missing(`points`) && is.null(`points`)) {
        stop("Invalid value for `points` when calling MissionApi$UpdateMission, `points` is not nullable")
      }

      if (!missing(`application_ids`) && is.null(`application_ids`)) {
        stop("Invalid value for `application_ids` when calling MissionApi$UpdateMission, `application_ids` is not nullable")
      }

      if (!missing(`devices`) && is.null(`devices`)) {
        stop("Invalid value for `devices` when calling MissionApi$UpdateMission, `devices` is not nullable")
      }

      if (!missing(`device_ids`) && is.null(`device_ids`)) {
        stop("Invalid value for `device_ids` when calling MissionApi$UpdateMission, `device_ids` is not nullable")
      }

      if (!missing(`device_versions`) && is.null(`device_versions`)) {
        stop("Invalid value for `device_versions` when calling MissionApi$UpdateMission, `device_versions` is not nullable")
      }

      if (!missing(`locations`) && is.null(`locations`)) {
        stop("Invalid value for `locations` when calling MissionApi$UpdateMission, `locations` is not nullable")
      }

      if (!missing(`radius`) && is.null(`radius`)) {
        stop("Invalid value for `radius` when calling MissionApi$UpdateMission, `radius` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["missionId"]] <- `mission_id`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["subType"]] <- `sub_type`

      query_params[["metaData"]] <- `meta_data`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["active"]] <- `active`

      query_params[["gameLevelIds"]] <- `game_level_ids`

      query_params[["creativeIds"]] <- `creative_ids`

      query_params[["audienceIds"]] <- `audience_ids`

      query_params[["offerId"]] <- `offer_id`

      query_params[["balance"]] <- `balance`

      query_params[["advancedReporting"]] <- `advanced_reporting`

      query_params[["allocateTickets"]] <- `allocate_tickets`

      query_params[["ticketCount"]] <- `ticket_count`

      query_params[["ticketType"]] <- `ticket_type`

      query_params[["points"]] <- `points`

      query_params[["applicationIds"]] <- `application_ids`

      query_params[["devices"]] <- `devices`

      query_params[["deviceIds"]] <- `device_ids`

      query_params[["deviceVersions"]] <- `device_versions`

      query_params[["locations"]] <- `locations`

      query_params[["radius"]] <- `radius`

      local_var_url_path <- "/mission/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MissionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
