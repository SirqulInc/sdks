#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Account operations
#' @description AccountApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AccountLocationSearch  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_q <- "q_example" # character | Deprecated - legacy query parameter (Optional)
#' var_keyword <- "keyword_example" # character | An optional keyword to search on, will be ignore if empty (NOT implemented yet) (Optional)
#' var_postal_code <- "postal_code_example" # character | The postal code to search on, either postalCode or the user's exact location is required (Optional)
#' var_latitude <- 3.4 # numeric | The latitude of the user, either postalCode or the user's exact location is required (Optional)
#' var_longitude <- 3.4 # numeric | The longitude of the user, either postalCode or the user's exact location is required (Optional)
#' var_app_key <- "app_key_example" # character | The application key (Optional)
#' var_range <- 10 # numeric | The range to search on (Optional)
#' var_location_last_updated <- 56 # integer | Searches for user's that has updated their location since this date (Optional)
#' var_gender <- "gender_example" # character | The preferred gender (Optional)
#' var_min_age <- 56 # integer | The preferred min age (Optional)
#' var_max_age <- 56 # integer | The preferred max age (Optional)
#' var_companionship_index <- 56 # integer | The preferred companionship index (Optional)
#' var__i <- 56 # integer | this is the start index of a query (Optional)
#' var_start <- 0 # integer | Start of the pagination (Optional)
#' var__l <- 56 # integer | this is the limit index of a query (Optional)
#' var_limit <- 100 # integer | Limit of the pagination (Optional)
#' var_search_mode <- "search_mode_example" # character | Search mode to use for index searches (e.g. CLOUDINDEX, OPENSEARCH) (Optional)
#' var_sort_field <- "sort_field_example" # character | Sorting field for results (default: DISTANCE) (Optional)
#' var_descending <- "descending_example" # character | Whether to sort descending (default: false) (Optional)
#' var_roles <- "roles_example" # character | Roles to filter on (Optional)
#' var_tags <- "tags_example" # character | Tags to filter on (Optional)
#' var_experience <- "experience_example" # character | The experience to filter on (Optional)
#' var_category_ids <- "category_ids_example" # character | The category ids to filter on (comma separated) (Optional)
#' var_audience_ids <- "audience_ids_example" # character | The audience ids to filter on (comma separated) (Optional)
#' var_audience_operator <- "AND" # character | Operator used to combine audience filters (default: AND) (Optional)
#' var_update_current_location <- FALSE # character | Whether to use the given lat & long to update the user's current location (Optional)
#' var_update_preferred_settings <- FALSE # character | Whether to use the given parameters to update the user's preferred settings (Optional)
#' var_show_exact_locations <- TRUE # character | Determines whether to always display user exact locations (Optional)
#' var_show_connection_to_searcher <- FALSE # character | Return connection of the accounts in the result to the passed in account if there exists any (Optional)
#' var_flag_count_minimum <- 56 # integer | Return any results that have a minimum of the specified flag count (even ones that have met the flag threshold) (Optional)
#' var_verified_user_only <- "verified_user_only_example" # character | Returns only verified users (Optional)
#' var_content_admin_only <- "content_admin_only_example" # character | Returns only content admin users (Optional)
#'
#' #Search Accounts by Location
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AccountLocationSearch(device_id = var_device_id, account_id = var_account_id, q = var_q, keyword = var_keyword, postal_code = var_postal_code, latitude = var_latitude, longitude = var_longitude, app_key = var_app_key, range = var_range, location_last_updated = var_location_last_updated, gender = var_gender, min_age = var_min_age, max_age = var_max_age, companionship_index = var_companionship_index, _i = var__i, start = var_start, _l = var__l, limit = var_limit, search_mode = var_search_mode, sort_field = var_sort_field, descending = var_descending, roles = var_roles, tags = var_tags, experience = var_experience, category_ids = var_category_ids, audience_ids = var_audience_ids, audience_operator = var_audience_operator, update_current_location = var_update_current_location, update_preferred_settings = var_update_preferred_settings, show_exact_locations = var_show_exact_locations, show_connection_to_searcher = var_show_connection_to_searcher, flag_count_minimum = var_flag_count_minimum, verified_user_only = var_verified_user_only, content_admin_only = var_content_admin_onlydata_file = "result.txt")
#' result <- api_instance$AccountLocationSearch(device_id = var_device_id, account_id = var_account_id, q = var_q, keyword = var_keyword, postal_code = var_postal_code, latitude = var_latitude, longitude = var_longitude, app_key = var_app_key, range = var_range, location_last_updated = var_location_last_updated, gender = var_gender, min_age = var_min_age, max_age = var_max_age, companionship_index = var_companionship_index, _i = var__i, start = var_start, _l = var__l, limit = var_limit, search_mode = var_search_mode, sort_field = var_sort_field, descending = var_descending, roles = var_roles, tags = var_tags, experience = var_experience, category_ids = var_category_ids, audience_ids = var_audience_ids, audience_operator = var_audience_operator, update_current_location = var_update_current_location, update_preferred_settings = var_update_preferred_settings, show_exact_locations = var_show_exact_locations, show_connection_to_searcher = var_show_connection_to_searcher, flag_count_minimum = var_flag_count_minimum, verified_user_only = var_verified_user_only, content_admin_only = var_content_admin_only)
#' dput(result)
#'
#'
#' ####################  BlockAccount  ####################
#'
#' library(openapi)
#' var_account_id_being_blocked <- 56 # integer | The id of the account to be blocked/unblocked
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_block_flag_value <- TRUE # character | Determines whether the account is blocked or unblocked (Optional)
#' var_remove_from_groups_if_blocked <- FALSE # character | Determines whether the account is removed from all other groups if blocked (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#'
#' #Block Account
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BlockAccount(var_account_id_being_blocked, device_id = var_device_id, account_id = var_account_id, block_flag_value = var_block_flag_value, remove_from_groups_if_blocked = var_remove_from_groups_if_blocked, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$BlockAccount(var_account_id_being_blocked, device_id = var_device_id, account_id = var_account_id, block_flag_value = var_block_flag_value, remove_from_groups_if_blocked = var_remove_from_groups_if_blocked, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  CreateAccount  ####################
#'
#' library(openapi)
#' var_username <- "username_example" # character | The access token to authenticate with (ex: username)
#' var_password <- "password_example" # character | The secret to authenticate with (ex: password)
#' var_name <- "name_example" # character | The full name of the user. If this parameter is NOT empty, the following parameters will be ignored: prefixName, firstName, middleName, lastName, and suffixName (Optional)
#' var_prefix_name <- "prefix_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the prefix of the user's name (Optional)
#' var_first_name <- "first_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the user's first name (Optional)
#' var_middle_name <- "middle_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the user's middle name (Optional)
#' var_last_name <- "last_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the user's last name (Optional)
#' var_suffix_name <- "suffix_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the suffix of the user's name (Optional)
#' var_title <- "title_example" # character | This field will be used to set the user's job title (Optional)
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (Optional)
#' var_device_id_type <- "device_id_type_example" # character | The type of device id (this is defined by the client), ie. MAC_ADDRESS, APPLE_UDID, etc (Optional)
#' var_email_address <- "email_address_example" # character | The user's contact email address (NOT the username) (Optional)
#' var_asset_id <- 56 # integer | The asset id to set the user's profile image (Optional)
#' var_street_address <- "street_address_example" # character | The street address of the user's contact location (Optional)
#' var_zipcode <- "zipcode_example" # character | The zipcode of the user's contact location (Optional)
#' var_gender <- "gender_example" # character | The gender of the user (AudienceGender) (Optional)
#' var_birthday <- 56 # integer | The birthday date of the user in UTC milliseconds (Optional)
#' var_home_phone <- "home_phone_example" # character | The home phone number (Optional)
#' var_cell_phone <- "cell_phone_example" # character | The cellular phone number (Optional)
#' var_cell_phone_carrier <- "cell_phone_carrier_example" # character | The cellular service provider (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone number (Optional)
#' var_role <- "role_example" # character | The account role (default: MEMBER) (Optional)
#' var_platforms <- "platforms_example" # character | Comma separated list of development platforms (Optional)
#' var_tags <- "tags_example" # character | Search tags (Optional)
#' var_about_us <- "about_us_example" # character | About us information (Optional)
#' var_game_experience <- "game_experience_example" # character | Game experience of the user (Optional)
#' var_category_ids <- "category_ids_example" # character | A list of category ids that represent interests and associations (Optional)
#' var_hometown <- "hometown_example" # character | The user's hometown (Optional)
#' var_height <- "height_example" # character | The user's height (Optional)
#' var_height_index <- 56 # integer | The user's height in a numerical value that can be used for ordering/searching (Optional)
#' var_ethnicity <- "ethnicity_example" # character | The user's ethnicity (Optional)
#' var_body_type <- "body_type_example" # character | The user's body type (Optional)
#' var_marital_status <- "marital_status_example" # character | The user's marital status (Optional)
#' var_children <- "children_example" # character | The user's children status (Optional)
#' var_religion <- "religion_example" # character | The user's religion (Optional)
#' var_education <- "education_example" # character | The user's education (Optional)
#' var_education_index <- 56 # integer | The user's education in a numerical value that can be used for ordering/searching (Optional)
#' var_smoke <- "smoke_example" # character | The user's smoke status (Optional)
#' var_drink <- "drink_example" # character | The user's drink status (Optional)
#' var_companionship <- "companionship_example" # character | The user's companionship status (Optional)
#' var_companionship_index <- 56 # integer | The user's companionship index (Optional)
#' var_preferred_min_age <- 56 # integer | The preferred minimum age in the account location search (Optional)
#' var_preferred_max_age <- 56 # integer | The preferred maximum age in the account location search (Optional)
#' var_preferred_min_height <- 56 # integer | The preferred minimum height in the account location search (Optional)
#' var_preferred_max_height <- 56 # integer | The preferred maximum height in the account location search (Optional)
#' var_preferred_gender <- "preferred_gender_example" # character | The preferred gender in the account location search (Optional)
#' var_preferred_education <- "preferred_education_example" # character | The preferred education in the account location search (Optional)
#' var_preferred_education_index <- 56 # integer | The preferred education in a numerical value that can be used for ordering/searching (Optional)
#' var_preferred_body_type <- "preferred_body_type_example" # character | The preferred body type in the account location search (Optional)
#' var_preferred_ethnicity <- "preferred_ethnicity_example" # character | The preferred ethnicity in the account location search (Optional)
#' var_preferred_location <- "preferred_location_example" # character | The preferred location in the account location search (Optional)
#' var_preferred_location_range <- 3.4 # numeric | The preferred location range in the account location search (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#' var_accepted_terms <- "accepted_terms_example" # character | Accepted Terms (Optional)
#' var_invite_token <- "invite_token_example" # character | The inviteToken that the referrer used for this account to sign up (Optional)
#' var_referral_account_id <- 56 # integer | The accountId of the referrer (used if there is no inviteToken) (Optional)
#' var_send_validation <- "send_validation_example" # character | Whether to send validation email (Optional)
#' var_game_type <- "game_type_example" # character | Deprecated: use appKey (Optional)
#' var_app_key <- "app_key_example" # character | The application key (Optional)
#' var_app_version <- "app_version_example" # character | The application version (Optional)
#' var_response_type <- "response_type_example" # character | Returns an AccountLoginResponse if \"AccountLoginResponse\" is passed in (Optional)
#' var_audience_ids_to_add <- "audience_ids_to_add_example" # character | Comma separated list of audience ids to assign to the user (Optional)
#' var_app_blob <- "app_blob_example" # character | Application blob data (Optional)
#' var_app_enable_push <- "app_enable_push_example" # character | Enable push for the app (Optional)
#' var_app_enable_sms <- "app_enable_sms_example" # character | Enable SMS for the app (Optional)
#' var_app_enable_email <- "app_enable_email_example" # character | Enable email for the app (Optional)
#' var_location_visibility <- "location_visibility_example" # character | Location visibility setting (Optional)
#' var_home_latitude <- 3.4 # numeric | Home latitude (Optional)
#' var_home_longitude <- 3.4 # numeric | Home longitude (Optional)
#' var_app_nickname <- "app_nickname_example" # character | The nickname used in the application for this account (Optional)
#' var_personal_audience_id <- 56 # integer | Personal audience id to associate with this account (Optional)
#'
#' #Create Account
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateAccount(var_username, var_password, name = var_name, prefix_name = var_prefix_name, first_name = var_first_name, middle_name = var_middle_name, last_name = var_last_name, suffix_name = var_suffix_name, title = var_title, device_id = var_device_id, device_id_type = var_device_id_type, email_address = var_email_address, asset_id = var_asset_id, street_address = var_street_address, zipcode = var_zipcode, gender = var_gender, birthday = var_birthday, home_phone = var_home_phone, cell_phone = var_cell_phone, cell_phone_carrier = var_cell_phone_carrier, business_phone = var_business_phone, role = var_role, platforms = var_platforms, tags = var_tags, about_us = var_about_us, game_experience = var_game_experience, category_ids = var_category_ids, hometown = var_hometown, height = var_height, height_index = var_height_index, ethnicity = var_ethnicity, body_type = var_body_type, marital_status = var_marital_status, children = var_children, religion = var_religion, education = var_education, education_index = var_education_index, smoke = var_smoke, drink = var_drink, companionship = var_companionship, companionship_index = var_companionship_index, preferred_min_age = var_preferred_min_age, preferred_max_age = var_preferred_max_age, preferred_min_height = var_preferred_min_height, preferred_max_height = var_preferred_max_height, preferred_gender = var_preferred_gender, preferred_education = var_preferred_education, preferred_education_index = var_preferred_education_index, preferred_body_type = var_preferred_body_type, preferred_ethnicity = var_preferred_ethnicity, preferred_location = var_preferred_location, preferred_location_range = var_preferred_location_range, latitude = var_latitude, longitude = var_longitude, accepted_terms = var_accepted_terms, invite_token = var_invite_token, referral_account_id = var_referral_account_id, send_validation = var_send_validation, game_type = var_game_type, app_key = var_app_key, app_version = var_app_version, response_type = var_response_type, audience_ids_to_add = var_audience_ids_to_add, app_blob = var_app_blob, app_enable_push = var_app_enable_push, app_enable_sms = var_app_enable_sms, app_enable_email = var_app_enable_email, location_visibility = var_location_visibility, home_latitude = var_home_latitude, home_longitude = var_home_longitude, app_nickname = var_app_nickname, personal_audience_id = var_personal_audience_iddata_file = "result.txt")
#' result <- api_instance$CreateAccount(var_username, var_password, name = var_name, prefix_name = var_prefix_name, first_name = var_first_name, middle_name = var_middle_name, last_name = var_last_name, suffix_name = var_suffix_name, title = var_title, device_id = var_device_id, device_id_type = var_device_id_type, email_address = var_email_address, asset_id = var_asset_id, street_address = var_street_address, zipcode = var_zipcode, gender = var_gender, birthday = var_birthday, home_phone = var_home_phone, cell_phone = var_cell_phone, cell_phone_carrier = var_cell_phone_carrier, business_phone = var_business_phone, role = var_role, platforms = var_platforms, tags = var_tags, about_us = var_about_us, game_experience = var_game_experience, category_ids = var_category_ids, hometown = var_hometown, height = var_height, height_index = var_height_index, ethnicity = var_ethnicity, body_type = var_body_type, marital_status = var_marital_status, children = var_children, religion = var_religion, education = var_education, education_index = var_education_index, smoke = var_smoke, drink = var_drink, companionship = var_companionship, companionship_index = var_companionship_index, preferred_min_age = var_preferred_min_age, preferred_max_age = var_preferred_max_age, preferred_min_height = var_preferred_min_height, preferred_max_height = var_preferred_max_height, preferred_gender = var_preferred_gender, preferred_education = var_preferred_education, preferred_education_index = var_preferred_education_index, preferred_body_type = var_preferred_body_type, preferred_ethnicity = var_preferred_ethnicity, preferred_location = var_preferred_location, preferred_location_range = var_preferred_location_range, latitude = var_latitude, longitude = var_longitude, accepted_terms = var_accepted_terms, invite_token = var_invite_token, referral_account_id = var_referral_account_id, send_validation = var_send_validation, game_type = var_game_type, app_key = var_app_key, app_version = var_app_version, response_type = var_response_type, audience_ids_to_add = var_audience_ids_to_add, app_blob = var_app_blob, app_enable_push = var_app_enable_push, app_enable_sms = var_app_enable_sms, app_enable_email = var_app_enable_email, location_visibility = var_location_visibility, home_latitude = var_home_latitude, home_longitude = var_home_longitude, app_nickname = var_app_nickname, personal_audience_id = var_personal_audience_id)
#' dput(result)
#'
#'
#' ####################  EditAccount  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_connection_account_id <- 56 # integer | The account id used to edit another person's account (Optional)
#' var_role <- "role_example" # character | The account role to change to (Optional)
#' var_asset_id <- 56 # integer | The asset id to set the user's profile image (Optional)
#' var_name <- "name_example" # character | The full name of the user. If this parameter is NOT empty, the  following parameters will be ignored: prefixName, firstName, middleName,  lastName, and suffixName  (Optional)
#' var_prefix_name <- "prefix_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the prefix of the user's name (Optional)
#' var_first_name <- "first_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the user's first name (Optional)
#' var_middle_name <- "middle_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the user's middle name (Optional)
#' var_last_name <- "last_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the user's last name (Optional)
#' var_suffix_name <- "suffix_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the suffix of the user's name (Optional)
#' var_title <- "title_example" # character | This field will be used to set the user's job title (Optional)
#' var_gender <- "gender_example" # character | The gender of the user AudienceGender (Optional)
#' var_age <- 56 # integer | This is deperecated, use the birthday parameter (Optional)
#' var_birthday <- 56 # integer | The birthday date of the user in UTC milliseconds (Optional)
#' var_home_phone <- "home_phone_example" # character | The home phone number (Optional)
#' var_cell_phone <- "cell_phone_example" # character | The cellular phone number (Optional)
#' var_cell_phone_carrier <- "cell_phone_carrier_example" # character | The cellular service provider (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone number (Optional)
#' var_email_address <- "email_address_example" # character | The user's contact email address (NOT the username) (Optional)
#' var_street_address <- "street_address_example" # character | The street address of the user's contact location (Optional)
#' var_street_address2 <- "street_address2_example" # character | Additional address information (such as a suite number, floor number, building name, or PO Box) (Optional)
#' var_city <- "city_example" # character | The city of the user's contact location (Optional)
#' var_state <- "state_example" # character | The state of the user's contact location (Optional)
#' var_zipcode <- "zipcode_example" # character | The zipcode of the user's contact location (Optional)
#' var_country <- "country_example" # character | The country of the user's contact location (Optional)
#' var_make_profile_info_public <- "make_profile_info_public_example" # character | Allow anyone to view the user's personal profile (Optional)
#' var_make_game_info_public <- "make_game_info_public_example" # character | Allow anyone to view the user's game/app info (Optional)
#' var_make_friends_info_public <- "make_friends_info_public_example" # character | Allow anyone to view the user's friends list (Optional)
#' var_hometown <- "hometown_example" # character | The user's hometown (Optional)
#' var_height <- "height_example" # character | The user's height (Optional)
#' var_height_index <- 56 # integer | The user's height in a numerical value that can be used for ordering/searching (Optional)
#' var_ethnicity <- "ethnicity_example" # character | The user's ethnicity (Optional)
#' var_body_type <- "body_type_example" # character | The user's body type (Optional)
#' var_marital_status <- "marital_status_example" # character | The user's marital status (Optional)
#' var_children <- "children_example" # character | The user's children status (Optional)
#' var_religion <- "religion_example" # character | The user's religion (Optional)
#' var_education <- "education_example" # character | The user's education (Optional)
#' var_education_index <- 56 # integer | The user's education in a numerical value that can be used for ordering/searching (Optional)
#' var_smoke <- "smoke_example" # character | The user's smoke status (Optional)
#' var_drink <- "drink_example" # character | The user's drink status (Optional)
#' var_companionship <- "companionship_example" # character | The user's companionship status (Optional)
#' var_companionship_index <- 56 # integer | The user's companionship index (Optional)
#' var_preferred_min_age <- 56 # integer | The preferred minimum age in the account location search (Optional)
#' var_preferred_max_age <- 56 # integer | The preferred maximum age in the account location search (Optional)
#' var_preferred_min_height <- 56 # integer | The preferred minimum height in the account location search (Optional)
#' var_preferred_max_height <- 56 # integer | The preferred maximum height in the account location search (Optional)
#' var_preferred_gender <- "preferred_gender_example" # character | The preferred gender in the account location search (Optional)
#' var_preferred_education <- "preferred_education_example" # character | The preferred education in the account location search (Optional)
#' var_preferred_education_index <- 56 # integer | The preferred education in a numerical value that can be used for ordering/searching (Optional)
#' var_preferred_body_type <- "preferred_body_type_example" # character | The preferred body type in the account location search (Optional)
#' var_preferred_ethnicity <- "preferred_ethnicity_example" # character | The preferred ethnicity in the account location search (Optional)
#' var_preferred_location <- "preferred_location_example" # character | The preferred education in the account location search (Optional)
#' var_preferred_location_range <- 3.4 # numeric | The preferred location range in the account location search (Optional)
#' var_platforms <- "platforms_example" # character | Platforms (Optional)
#' var_tags <- "tags_example" # character | Tags (Optional)
#' var_about_us <- "about_us_example" # character | About Us (Optional)
#' var_match_token <- "match_token_example" # character | Match Token (Optional)
#' var_game_experience <- "game_experience_example" # character | Game Experience (Optional)
#' var_categories <- "categories_example" # character | Deprecated use categoryIds (Optional)
#' var_category_ids <- "category_ids_example" # character | A list of category ids that represent interests and associations (Optional)
#' var_response_filters <- "PROFILE,PROFILE_CONTACT" # character | A comma separated list of ProfileFilters for filtering the returned response data (Optional)
#' var_show_as_zipcode <- "show_as_zipcode_example" # character | The user's preference if they want to be shown by zipcode on a map (Optional)
#' var_show_exact_location <- "show_exact_location_example" # character | The user's preference if they want to be shown by their exact location on a map (Optional)
#' var_show_others_exact_location <- "show_others_exact_location_example" # character | The user's preference if they want to see others exact location on a map (Optional)
#' var_accepted_terms <- "accepted_terms_example" # character | Accepted Terms (Optional)
#' var_location_visibility <- "location_visibility_example" # character | Location Visibility (Optional)
#' var_app_blob <- "app_blob_example" # character | App Blob (Optional)
#' var_app_enable_push <- "app_enable_push_example" # character | App Enable Push (Optional)
#' var_app_enable_sms <- "app_enable_sms_example" # character | App Enable SMS (Optional)
#' var_app_enable_email <- "app_enable_email_example" # character | App Enable Email (Optional)
#' var_game_type <- "game_type_example" # character | Game Type (Optional)
#' var_app_key <- "app_key_example" # character | The application key (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#' var_return_profile <- "return_profile_example" # character | Return Profile (Optional)
#' var_audience_ids_to_add <- "audience_ids_to_add_example" # character | Audience Ids to add (Optional)
#' var_audience_ids_to_remove <- "audience_ids_to_remove_example" # character | Audience Ids to remove (Optional)
#' var_referral_account_id <- 56 # integer | The account id of the referrer (Optional)
#' var_app_nickname <- "app_nickname_example" # character | App nickname (Optional)
#' var_personal_audience_id <- 56 # integer | Personal Audience (Optional)
#' var_non_guest_username <- "non_guest_username_example" # character | The user's username to update with if they currently have a guest username (Optional)
#'
#' #Update Account
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$EditAccount(device_id = var_device_id, account_id = var_account_id, connection_account_id = var_connection_account_id, role = var_role, asset_id = var_asset_id, name = var_name, prefix_name = var_prefix_name, first_name = var_first_name, middle_name = var_middle_name, last_name = var_last_name, suffix_name = var_suffix_name, title = var_title, gender = var_gender, age = var_age, birthday = var_birthday, home_phone = var_home_phone, cell_phone = var_cell_phone, cell_phone_carrier = var_cell_phone_carrier, business_phone = var_business_phone, email_address = var_email_address, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, zipcode = var_zipcode, country = var_country, make_profile_info_public = var_make_profile_info_public, make_game_info_public = var_make_game_info_public, make_friends_info_public = var_make_friends_info_public, hometown = var_hometown, height = var_height, height_index = var_height_index, ethnicity = var_ethnicity, body_type = var_body_type, marital_status = var_marital_status, children = var_children, religion = var_religion, education = var_education, education_index = var_education_index, smoke = var_smoke, drink = var_drink, companionship = var_companionship, companionship_index = var_companionship_index, preferred_min_age = var_preferred_min_age, preferred_max_age = var_preferred_max_age, preferred_min_height = var_preferred_min_height, preferred_max_height = var_preferred_max_height, preferred_gender = var_preferred_gender, preferred_education = var_preferred_education, preferred_education_index = var_preferred_education_index, preferred_body_type = var_preferred_body_type, preferred_ethnicity = var_preferred_ethnicity, preferred_location = var_preferred_location, preferred_location_range = var_preferred_location_range, platforms = var_platforms, tags = var_tags, about_us = var_about_us, match_token = var_match_token, game_experience = var_game_experience, categories = var_categories, category_ids = var_category_ids, response_filters = var_response_filters, show_as_zipcode = var_show_as_zipcode, show_exact_location = var_show_exact_location, show_others_exact_location = var_show_others_exact_location, accepted_terms = var_accepted_terms, location_visibility = var_location_visibility, app_blob = var_app_blob, app_enable_push = var_app_enable_push, app_enable_sms = var_app_enable_sms, app_enable_email = var_app_enable_email, game_type = var_game_type, app_key = var_app_key, latitude = var_latitude, longitude = var_longitude, return_profile = var_return_profile, audience_ids_to_add = var_audience_ids_to_add, audience_ids_to_remove = var_audience_ids_to_remove, referral_account_id = var_referral_account_id, app_nickname = var_app_nickname, personal_audience_id = var_personal_audience_id, non_guest_username = var_non_guest_usernamedata_file = "result.txt")
#' result <- api_instance$EditAccount(device_id = var_device_id, account_id = var_account_id, connection_account_id = var_connection_account_id, role = var_role, asset_id = var_asset_id, name = var_name, prefix_name = var_prefix_name, first_name = var_first_name, middle_name = var_middle_name, last_name = var_last_name, suffix_name = var_suffix_name, title = var_title, gender = var_gender, age = var_age, birthday = var_birthday, home_phone = var_home_phone, cell_phone = var_cell_phone, cell_phone_carrier = var_cell_phone_carrier, business_phone = var_business_phone, email_address = var_email_address, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, zipcode = var_zipcode, country = var_country, make_profile_info_public = var_make_profile_info_public, make_game_info_public = var_make_game_info_public, make_friends_info_public = var_make_friends_info_public, hometown = var_hometown, height = var_height, height_index = var_height_index, ethnicity = var_ethnicity, body_type = var_body_type, marital_status = var_marital_status, children = var_children, religion = var_religion, education = var_education, education_index = var_education_index, smoke = var_smoke, drink = var_drink, companionship = var_companionship, companionship_index = var_companionship_index, preferred_min_age = var_preferred_min_age, preferred_max_age = var_preferred_max_age, preferred_min_height = var_preferred_min_height, preferred_max_height = var_preferred_max_height, preferred_gender = var_preferred_gender, preferred_education = var_preferred_education, preferred_education_index = var_preferred_education_index, preferred_body_type = var_preferred_body_type, preferred_ethnicity = var_preferred_ethnicity, preferred_location = var_preferred_location, preferred_location_range = var_preferred_location_range, platforms = var_platforms, tags = var_tags, about_us = var_about_us, match_token = var_match_token, game_experience = var_game_experience, categories = var_categories, category_ids = var_category_ids, response_filters = var_response_filters, show_as_zipcode = var_show_as_zipcode, show_exact_location = var_show_exact_location, show_others_exact_location = var_show_others_exact_location, accepted_terms = var_accepted_terms, location_visibility = var_location_visibility, app_blob = var_app_blob, app_enable_push = var_app_enable_push, app_enable_sms = var_app_enable_sms, app_enable_email = var_app_enable_email, game_type = var_game_type, app_key = var_app_key, latitude = var_latitude, longitude = var_longitude, return_profile = var_return_profile, audience_ids_to_add = var_audience_ids_to_add, audience_ids_to_remove = var_audience_ids_to_remove, referral_account_id = var_referral_account_id, app_nickname = var_app_nickname, personal_audience_id = var_personal_audience_id, non_guest_username = var_non_guest_username)
#' dput(result)
#'
#'
#' ####################  EditUsername  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_email_address <- "email_address_example" # character | the user's contact email address (NOT the username) which is also used for email validation (Optional)
#' var_username <- "username_example" # character | the user's username to update with if they currently have a guest username (Optional)
#'
#' #Update Username and Email
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$EditUsername(device_id = var_device_id, account_id = var_account_id, email_address = var_email_address, username = var_usernamedata_file = "result.txt")
#' result <- api_instance$EditUsername(device_id = var_device_id, account_id = var_account_id, email_address = var_email_address, username = var_username)
#' dput(result)
#'
#'
#' ####################  GetAccount  ####################
#'
#' library(openapi)
#' var_return_nulls <- FALSE # character | Return Nulls (Optional)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_connection_account_email <- "connection_account_email_example" # character | Connection Account Email (Optional)
#' var_connection_account_id <- 0 # integer | The account id used to view another person's account (Optional)
#' var_response_filters <- "PROFILE" # character | A comma separated list of ProfileFilters for filtering the returned response data (Optional)
#' var_game_type <- "game_type_example" # character | Game Type (Optional)
#' var_app_key <- "app_key_example" # character | The application key (Optional)
#' var_purchase_type <- "SIRQUL" # character | Purchase Type (Optional)
#' var_update_viewed_date <- FALSE # character | Determines whether to track if a person has viewed someone's profile (Optional)
#' var_latitude <- 3.4 # numeric | Latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | Longitude used to update the user's current location (Optional)
#'
#' #Get Account
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAccount(return_nulls = var_return_nulls, device_id = var_device_id, account_id = var_account_id, connection_account_email = var_connection_account_email, connection_account_id = var_connection_account_id, response_filters = var_response_filters, game_type = var_game_type, app_key = var_app_key, purchase_type = var_purchase_type, update_viewed_date = var_update_viewed_date, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$GetAccount(return_nulls = var_return_nulls, device_id = var_device_id, account_id = var_account_id, connection_account_email = var_connection_account_email, connection_account_id = var_connection_account_id, response_filters = var_response_filters, game_type = var_game_type, app_key = var_app_key, purchase_type = var_purchase_type, update_viewed_date = var_update_viewed_date, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  GetProfileAssets  ####################
#'
#' library(openapi)
#' var_return_nulls <- FALSE # character | Determines whether to return null fields in the response (Optional)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_owner_id <- 56 # integer | The account id of the person the user wants to view (Optional)
#' var_media_types <- "media_types_example" # character | Comma separated list of MediaType (Optional)
#' var_mime_types <- "mime_types_example" # character | Comma separated list of mime types (Optional)
#' var_sort_field <- "CREATED" # character | Determines what the returning list will be sorted by (see AssetApiMap) (Optional)
#' var_descending <- TRUE # character | Determines whether to return the resulting list in descending or ascending order (Optional)
#' var_latitude <- 3.4 # numeric | Latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | Longitude used to update the user's current location (Optional)
#' var__i <- 56 # integer | _i (Optional)
#' var_start <- 0 # integer | Start of the pagination (Optional)
#' var__l <- 56 # integer | _l (Optional)
#' var_limit <- 0 # integer | Limit of the pagination (Optional)
#'
#' #Get Profile Assets
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProfileAssets(return_nulls = var_return_nulls, device_id = var_device_id, account_id = var_account_id, owner_id = var_owner_id, media_types = var_media_types, mime_types = var_mime_types, sort_field = var_sort_field, descending = var_descending, latitude = var_latitude, longitude = var_longitude, _i = var__i, start = var_start, _l = var__l, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GetProfileAssets(return_nulls = var_return_nulls, device_id = var_device_id, account_id = var_account_id, owner_id = var_owner_id, media_types = var_media_types, mime_types = var_mime_types, sort_field = var_sort_field, descending = var_descending, latitude = var_latitude, longitude = var_longitude, _i = var__i, start = var_start, _l = var__l, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  GetReferralList  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_app_key <- "app_key_example" # character | The application key (Optional)
#' var_retrieve_type <- "retrieve_type_example" # character | one of these option - GET_CHILDREN will get all accounts that had signed up using the current account invite link - GET_ANCESTOR will get all accounts that referred the current account and it's parents, recursively - GET_ALL will get all of the above (Optional)
#' var_level_limit <- 3.4 # numeric | level limit for children and ancestors of current account, starts from current account (Optional)
#' var_ancestor_level_limit <- 3.4 # numeric | level limit for ancestors, will override levelLimit if this is set (Optional)
#' var_children_level_limit <- 3.4 # numeric | level limit for children, will override levelLimit if this is set (Optional)
#' var_ancestor_list_start <- 3.4 # numeric | pagination start for children list (Optional)
#' var_ancestor_list_limit <- 3.4 # numeric | pagination limit for children list (Optional)
#' var_children_list_start <- 3.4 # numeric | pagination start for children list (Optional)
#' var_children_list_limit <- 3.4 # numeric | pagination limit for children list (Optional)
#' var_children_children <- TRUE # character | if true, on each item in ancestor and children list, return the childrenTotalNumber and ancestorTotalNumber for that item (Optional)
#'
#' #Search Accounts
#' api_instance <- AccountApi$new()
#'
#' api_instance$GetReferralList(account_id = var_account_id, app_key = var_app_key, retrieve_type = var_retrieve_type, level_limit = var_level_limit, ancestor_level_limit = var_ancestor_level_limit, children_level_limit = var_children_level_limit, ancestor_list_start = var_ancestor_list_start, ancestor_list_limit = var_ancestor_list_limit, children_list_start = var_children_list_start, children_list_limit = var_children_list_limit, children_children = var_children_children)
#'
#'
#' ####################  GetSettings  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#'
#' #Get Account Settings
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSettings(device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$GetSettings(device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  LoginDelegate  ####################
#'
#' library(openapi)
#' var_access_token <- "access_token_example" # character | 
#' var_app_key <- "app_key_example" # character | 
#' var_device_id <- "device_id_example" # character |  (Optional)
#' var_access_token_secret <- "access_token_secret_example" # character |  (Optional)
#' var_delegated_account_id <- 56 # integer |  (Optional)
#' var_delegated_username <- "delegated_username_example" # character |  (Optional)
#' var_network_uid <- "USERNAME" # character | The access provider to authenticate against (default: USERNAME). Supported values: FACEBOOK, TWITTER, USERNAME, PHONE (Optional)
#' var_age_restriction <- 0 # integer | Checks user's birthday to see if they meet an age requirement. If the user is under age, an error message is returned. (Optional)
#' var_response_filters <- "PROFILE" # character | This determines how much of the profile should be returned, see ProfileFilters (Optional)
#' var_latitude <- 3.4 # numeric |  (Optional)
#' var_longitude <- 3.4 # numeric |  (Optional)
#'
#' #Login as Account
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LoginDelegate(var_access_token, var_app_key, device_id = var_device_id, access_token_secret = var_access_token_secret, delegated_account_id = var_delegated_account_id, delegated_username = var_delegated_username, network_uid = var_network_uid, age_restriction = var_age_restriction, response_filters = var_response_filters, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$LoginDelegate(var_access_token, var_app_key, device_id = var_device_id, access_token_secret = var_access_token_secret, delegated_account_id = var_delegated_account_id, delegated_username = var_delegated_username, network_uid = var_network_uid, age_restriction = var_age_restriction, response_filters = var_response_filters, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  LoginGeneral  ####################
#'
#' library(openapi)
#' var_access_token <- "access_token_example" # character | The access token to authenticate with (ex: username or fb token)
#' var_network_uid <- "USERNAME" # character | The access provider to authenticate against. This can be custom  networks created using the ThirdPartyApi as well. Supported values by default  include: FACEBOOK, TWITTER, USERNAME, PHONE 
#' var_app_key <- "app_key_example" # character | The application key
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (Optional)
#' var_device_id_type <- "device_id_type_example" # character | The type of device id (this is defined by the client), ie. MAC_ADDRESS, APPLE_UDID, etc (Optional)
#' var_access_token_secret <- "access_token_secret_example" # character | The secret to authenticate with (ex: password) (Optional)
#' var_age_restriction <- 0 # integer | Checks user's birthday to see if they meet an age requirement. If the user is under age, an error message is returned. (Optional)
#' var_response_filters <- "PROFILE" # character | This determines how much of the profile should be returned, see ProfileFilters (Optional)
#' var_latitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_email_match <- FALSE # character | Option to check for email if username doesn't match, also support multiple accounts (Optional)
#' var_chosen_account_id <- 0 # integer | Chosen account Id sent from the app - pass in the 2nd request to choose an account from multiple accounts matching the email - use one of the account id from the previous request (Optional)
#' var_third_party_credential_id <- 0 # integer | Third-party credential Id, pass in the 2nd request to choose an account from multiple accounts matching the email - use the id from the previous call ThirdPartyCredential object (Optional)
#'
#' #Login Account
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LoginGeneral(var_access_token, var_network_uid, var_app_key, device_id = var_device_id, device_id_type = var_device_id_type, access_token_secret = var_access_token_secret, age_restriction = var_age_restriction, response_filters = var_response_filters, latitude = var_latitude, longitude = var_longitude, email_match = var_email_match, chosen_account_id = var_chosen_account_id, third_party_credential_id = var_third_party_credential_iddata_file = "result.txt")
#' result <- api_instance$LoginGeneral(var_access_token, var_network_uid, var_app_key, device_id = var_device_id, device_id_type = var_device_id_type, access_token_secret = var_access_token_secret, age_restriction = var_age_restriction, response_filters = var_response_filters, latitude = var_latitude, longitude = var_longitude, email_match = var_email_match, chosen_account_id = var_chosen_account_id, third_party_credential_id = var_third_party_credential_id)
#' dput(result)
#'
#'
#' ####################  LoginUsername  ####################
#'
#' library(openapi)
#' var_username <- "username_example" # character | the user's email address they used to sign-up
#' var_password <- "password_example" # character | the password
#' var_device_id <- "device_id_example" # character | the device id (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#' var_app <- "app_example" # character | the app (Optional)
#' var_game_type <- "game_type_example" # character | This parameter is deprecated. This is deprecated, use appKey. (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_return_profile <- "return_profile_example" # character | the profile to return (Optional)
#' var_response_filters <- "response_filters_example" # character | a comma separated list of ProfileFilters for filtering the returned response data (Optional)
#'
#' #Login Account (Username)
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LoginUsername(var_username, var_password, device_id = var_device_id, latitude = var_latitude, longitude = var_longitude, app = var_app, game_type = var_game_type, app_key = var_app_key, return_profile = var_return_profile, response_filters = var_response_filtersdata_file = "result.txt")
#' result <- api_instance$LoginUsername(var_username, var_password, device_id = var_device_id, latitude = var_latitude, longitude = var_longitude, app = var_app, game_type = var_game_type, app_key = var_app_key, return_profile = var_return_profile, response_filters = var_response_filters)
#' dput(result)
#'
#'
#' ####################  Logout  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_device_id_type <- "device_id_type_example" # character | Device Id Type (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#'
#' #Logout Account
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$Logout(device_id = var_device_id, device_id_type = var_device_id_type, account_id = var_account_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$Logout(device_id = var_device_id, device_id_type = var_device_id_type, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  MergeAccount  ####################
#'
#' library(openapi)
#' var_merge_account_id <- 56 # integer | The id of the account to being merged
#' var_app_key <- "app_key_example" # character | The application key
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#'
#' #Merge Account
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MergeAccount(var_merge_account_id, var_app_key, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$MergeAccount(var_merge_account_id, var_app_key, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  PasswordChange  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account to update
#' var_old_password <- "old_password_example" # character | The current password, used to validate access
#' var_new_password <- "new_password_example" # character | The new password to set, cannot be empty
#' var_confirm_password <- "confirm_password_example" # character | The new password to confirm, must match newPassword
#'
#' #Update Password
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PasswordChange(var_account_id, var_old_password, var_new_password, var_confirm_passworddata_file = "result.txt")
#' result <- api_instance$PasswordChange(var_account_id, var_old_password, var_new_password, var_confirm_password)
#' dput(result)
#'
#'
#' ####################  PasswordReset  ####################
#'
#' library(openapi)
#' var_token <- "token_example" # character | The token associated with the account to update, good for 24 hours
#' var_password <- "password_example" # character | The new password to set, cannot be empty
#' var_confirm <- "confirm_example" # character | The new password to confirm, must match newPassword
#'
#' #Reset Password
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PasswordReset(var_token, var_password, var_confirmdata_file = "result.txt")
#' result <- api_instance$PasswordReset(var_token, var_password, var_confirm)
#' dput(result)
#'
#'
#' ####################  RequestPasswordReset  ####################
#'
#' library(openapi)
#' var_email <- "email_example" # character | The email/username of the account
#' var_from <- "Sirqul" # character | this is the sender email (Optional)
#' var_domain <- "domain_example" # character | this is the domain (like dev.sirqul.com) used to generate the password reset link (Optional)
#' var_sub_url <- "sub_url_example" # character | this is the the subUrl (like resetpassword) used to generate a password reset link (Optional)
#' var_referer <- "http://dev.sirqul.com/resetpassword" # character | this is used to generate a password reset link (Optional)
#'
#' #Request Password Reset
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RequestPasswordReset(var_email, from = var_from, domain = var_domain, sub_url = var_sub_url, referer = var_refererdata_file = "result.txt")
#' result <- api_instance$RequestPasswordReset(var_email, from = var_from, domain = var_domain, sub_url = var_sub_url, referer = var_referer)
#' dput(result)
#'
#'
#' ####################  RequestValidateAccount  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user
#'
#' #Send Validation Request
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RequestValidateAccount(var_account_iddata_file = "result.txt")
#' result <- api_instance$RequestValidateAccount(var_account_id)
#' dput(result)
#'
#'
#' ####################  SearchAccounts  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The id of the account requesting
#' var_app_key <- "app_key_example" # character | The application key
#' var_keyword <- "keyword_example" # character | The keyword for for querying the account (Optional)
#' var_latitude <- 3.4 # numeric | the latitude (Optional)
#' var_longitude <- 3.4 # numeric | the longitude (Optional)
#' var_radius <- 5 # numeric | the radius (Optional)
#' var_gender <- "gender_example" # character | the user's gender (Optional)
#' var_game_experience <- "game_experience_example" # character | the user's Game Experience (Optional)
#' var_age <- 56 # integer | the user's age (Optional)
#' var_category_ids <- "category_ids_example" # character | the user's Category Ids (Optional)
#' var_return_nulls <- TRUE # character | Return Nulls (Optional)
#' var_response_filters <- "PROFILE" # character | A comma separated list of ProfileFilters for filtering the returned response data (Optional)
#' var_purchase_type <- "SIRQUL" # character | A comma separated list of PurchaseType (Optional)
#' var_sort_field <- "id" # character | The field to sort by (Optional)
#' var_descending <- FALSE # character | The order to return the results. Default is false, which will return the results in ascending order. (Optional)
#' var_start <- 0 # integer | The index into the record set to start with. (Optional)
#' var_limit <- 20 # integer | The total number of record to return. (Optional)
#' var_active_only <- FALSE # character | Determines whether to return only active results. Default is false. (Optional)
#'
#' #Search Accounts
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchAccounts(var_account_id, var_app_key, keyword = var_keyword, latitude = var_latitude, longitude = var_longitude, radius = var_radius, gender = var_gender, game_experience = var_game_experience, age = var_age, category_ids = var_category_ids, return_nulls = var_return_nulls, response_filters = var_response_filters, purchase_type = var_purchase_type, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, active_only = var_active_onlydata_file = "result.txt")
#' result <- api_instance$SearchAccounts(var_account_id, var_app_key, keyword = var_keyword, latitude = var_latitude, longitude = var_longitude, radius = var_radius, gender = var_gender, game_experience = var_game_experience, age = var_age, category_ids = var_category_ids, return_nulls = var_return_nulls, response_filters = var_response_filters, purchase_type = var_purchase_type, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, active_only = var_active_only)
#' dput(result)
#'
#'
#' ####################  SecureLogin  ####################
#'
#' library(openapi)
#' var_username <- "username_example" # character | The user's encrypted email address they used to sign-up
#' var_password <- "password_example" # character | The encrypted password
#' var_game_type <- "game_type_example" # character | The application key
#' var_device_id <- "device_id_example" # character | The device id (Optional)
#' var_charset_name <- "UTF-8" # character | Charset Name (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#' var_return_profile <- FALSE # character | Return Profile (Optional)
#' var_response_filters <- "PROFILE" # character | A comma separated list of ProfileFilters for filtering the returned response data (Optional)
#'
#' #Login Account (Encrypted Username)
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SecureLogin(var_username, var_password, var_game_type, device_id = var_device_id, charset_name = var_charset_name, latitude = var_latitude, longitude = var_longitude, return_profile = var_return_profile, response_filters = var_response_filtersdata_file = "result.txt")
#' result <- api_instance$SecureLogin(var_username, var_password, var_game_type, device_id = var_device_id, charset_name = var_charset_name, latitude = var_latitude, longitude = var_longitude, return_profile = var_return_profile, response_filters = var_response_filters)
#' dput(result)
#'
#'
#' ####################  SecureSignup  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id
#' var_username <- "username_example" # character | The encrypted email of the user, this is what will be used when they login
#' var_password <- "password_example" # character | The encrypted password of the user
#' var_name <- "name_example" # character | The full name of the user. If this parameter is not empty, the  following parameters will be ignored: prefixName, firstName, middleName,  lastName, and suffixName  (Optional)
#' var_invite_token <- "invite_token_example" # character | the inviteToken that the referrer use for this account to sign up (Optional)
#' var_prefix_name <- "prefix_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the prefix of the user's name (Optional)
#' var_first_name <- "first_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the user's first name (Optional)
#' var_middle_name <- "middle_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the user's middle name (Optional)
#' var_last_name <- "last_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the user's last name (Optional)
#' var_suffix_name <- "suffix_name_example" # character | If the parameter 'name' is empty or not present, this field will be used to set the suffix of the user's name (Optional)
#' var_title <- "title_example" # character | Title (Optional)
#' var_device_id_type <- "device_id_type_example" # character | Device Id Type (Optional)
#' var_email_address <- "email_address_example" # character | The user's contact email address (NOT the username) which is also used for email validation (Optional)
#' var_asset_id <- 56 # integer | The asset id to set the user's profile image (Optional)
#' var_address <- "address_example" # character | the user's address (Optional)
#' var_zipcode <- "zipcode_example" # character | The street zipcode of the user's contact location (Optional)
#' var_gender <- "gender_example" # character | The gender of the user AudienceGender (Optional)
#' var_birthday <- 56 # integer | The birthday date of the user in milliseconds (Optional)
#' var_home_phone <- "home_phone_example" # character | the user's home phone number (Optional)
#' var_cell_phone <- "cell_phone_example" # character | the user's cell phone number (Optional)
#' var_cell_phone_carrier <- "cell_phone_carrier_example" # character | the user's Cell Phone Carrier (Optional)
#' var_business_phone <- "business_phone_example" # character | the user's Business Phone Number (Optional)
#' var_role <- "MEMBER" # character | The type of account being created {RETAILER, MEMBER, DEVELOPER, GUEST (Optional)
#' var_platforms <- "platforms_example" # character | Comma separated list of development platforms: MAC, WINDOWS, IOS, ANDROID, WINDOWSPHONE, KINDLE, UNITY3D, COCOS2D, HTML5, FACEBOOK (Optional)
#' var_tags <- "tags_example" # character | Search tags (Optional)
#' var_about_us <- "about_us_example" # character | About Us information (Optional)
#' var_game_experience <- "game_experience_example" # character | Game experience level of the user {ANY, NEW, BEGINNER, INTERMEDIATE, EXPERT (Optional)
#' var_category_ids <- "category_ids_example" # character | A list of category ids that represent interests and associations (Optional)
#' var_hometown <- "hometown_example" # character | The user's hometown (Optional)
#' var_height <- "height_example" # character | The user's height (Optional)
#' var_height_index <- 56 # integer | The user's height in a numerical value that can be used for ordering/searching (Optional)
#' var_ethnicity <- "ethnicity_example" # character | The user's ethnicity (Optional)
#' var_body_type <- "body_type_example" # character | The user's body type (Optional)
#' var_marital_status <- "marital_status_example" # character | The user's maritial status (Optional)
#' var_children <- "children_example" # character | The user's children status (Optional)
#' var_religion <- "religion_example" # character | The user's religion (Optional)
#' var_education <- "education_example" # character | The user's education (Optional)
#' var_education_index <- 56 # integer | The user's education in a numerical value that can be used for ordering/searching (Optional)
#' var_smoke <- "smoke_example" # character | The user's smoke status (Optional)
#' var_drink <- "drink_example" # character | The user's drink status (Optional)
#' var_companionship <- "companionship_example" # character | The user's companionship status (Optional)
#' var_companionship_index <- 56 # integer | The user's companionship index (Optional)
#' var_preferred_min_age <- 56 # integer | The preferred minimum age in the account location search (Optional)
#' var_preferred_max_age <- 56 # integer | The preferred maximum age in the account location search (Optional)
#' var_preferred_min_height <- 56 # integer | The preferred minimum height in the account location search (Optional)
#' var_preferred_max_height <- 56 # integer | The preferred maximum height in the account location search (Optional)
#' var_preferred_gender <- "preferred_gender_example" # character | The preferred gender in the account location search (Optional)
#' var_preferred_education <- "preferred_education_example" # character | The preferred education in the account location search (Optional)
#' var_preferred_education_index <- 56 # integer | The preferred education in a numerical value that can be used for ordering/searching (Optional)
#' var_preferred_body_type <- "preferred_body_type_example" # character | The preferred body type in the account location search (Optional)
#' var_preferred_ethnicity <- "preferred_ethnicity_example" # character | The preferred ethnicity in the account location search (Optional)
#' var_preferred_location <- "preferred_location_example" # character | The preferred education in the account location search (Optional)
#' var_preferred_location_range <- 3.4 # numeric | The preferred location range in the account location search (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#' var_accepted_terms <- TRUE # character | Accepted Terms (Optional)
#' var_charset_name <- "UTF-8" # character | Charset Name (Optional)
#' var_game_type <- "game_type_example" # character | Game Type (Optional)
#' var_app_key <- "app_key_example" # character | The application key (Optional)
#' var_app_version <- "app_version_example" # character | App Version (Optional)
#' var_response_type <- "response_type_example" # character | Response Type (Optional)
#'
#' #Create Account (Encrypted Username)
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SecureSignup(var_device_id, var_username, var_password, name = var_name, invite_token = var_invite_token, prefix_name = var_prefix_name, first_name = var_first_name, middle_name = var_middle_name, last_name = var_last_name, suffix_name = var_suffix_name, title = var_title, device_id_type = var_device_id_type, email_address = var_email_address, asset_id = var_asset_id, address = var_address, zipcode = var_zipcode, gender = var_gender, birthday = var_birthday, home_phone = var_home_phone, cell_phone = var_cell_phone, cell_phone_carrier = var_cell_phone_carrier, business_phone = var_business_phone, role = var_role, platforms = var_platforms, tags = var_tags, about_us = var_about_us, game_experience = var_game_experience, category_ids = var_category_ids, hometown = var_hometown, height = var_height, height_index = var_height_index, ethnicity = var_ethnicity, body_type = var_body_type, marital_status = var_marital_status, children = var_children, religion = var_religion, education = var_education, education_index = var_education_index, smoke = var_smoke, drink = var_drink, companionship = var_companionship, companionship_index = var_companionship_index, preferred_min_age = var_preferred_min_age, preferred_max_age = var_preferred_max_age, preferred_min_height = var_preferred_min_height, preferred_max_height = var_preferred_max_height, preferred_gender = var_preferred_gender, preferred_education = var_preferred_education, preferred_education_index = var_preferred_education_index, preferred_body_type = var_preferred_body_type, preferred_ethnicity = var_preferred_ethnicity, preferred_location = var_preferred_location, preferred_location_range = var_preferred_location_range, latitude = var_latitude, longitude = var_longitude, accepted_terms = var_accepted_terms, charset_name = var_charset_name, game_type = var_game_type, app_key = var_app_key, app_version = var_app_version, response_type = var_response_typedata_file = "result.txt")
#' result <- api_instance$SecureSignup(var_device_id, var_username, var_password, name = var_name, invite_token = var_invite_token, prefix_name = var_prefix_name, first_name = var_first_name, middle_name = var_middle_name, last_name = var_last_name, suffix_name = var_suffix_name, title = var_title, device_id_type = var_device_id_type, email_address = var_email_address, asset_id = var_asset_id, address = var_address, zipcode = var_zipcode, gender = var_gender, birthday = var_birthday, home_phone = var_home_phone, cell_phone = var_cell_phone, cell_phone_carrier = var_cell_phone_carrier, business_phone = var_business_phone, role = var_role, platforms = var_platforms, tags = var_tags, about_us = var_about_us, game_experience = var_game_experience, category_ids = var_category_ids, hometown = var_hometown, height = var_height, height_index = var_height_index, ethnicity = var_ethnicity, body_type = var_body_type, marital_status = var_marital_status, children = var_children, religion = var_religion, education = var_education, education_index = var_education_index, smoke = var_smoke, drink = var_drink, companionship = var_companionship, companionship_index = var_companionship_index, preferred_min_age = var_preferred_min_age, preferred_max_age = var_preferred_max_age, preferred_min_height = var_preferred_min_height, preferred_max_height = var_preferred_max_height, preferred_gender = var_preferred_gender, preferred_education = var_preferred_education, preferred_education_index = var_preferred_education_index, preferred_body_type = var_preferred_body_type, preferred_ethnicity = var_preferred_ethnicity, preferred_location = var_preferred_location, preferred_location_range = var_preferred_location_range, latitude = var_latitude, longitude = var_longitude, accepted_terms = var_accepted_terms, charset_name = var_charset_name, game_type = var_game_type, app_key = var_app_key, app_version = var_app_version, response_type = var_response_type)
#' dput(result)
#'
#'
#' ####################  SetMatchToken  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_match_token <- "match_token_example" # character | A string of numbers (Optional)
#' var_game_type <- "BOOPY" # character | Game Type (deprecated) (Optional)
#' var_app_key <- "app_key_example" # character | The application key (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#'
#' #Save Match Token
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SetMatchToken(device_id = var_device_id, account_id = var_account_id, match_token = var_match_token, game_type = var_game_type, app_key = var_app_key, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$SetMatchToken(device_id = var_device_id, account_id = var_account_id, match_token = var_match_token, game_type = var_game_type, app_key = var_app_key, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  UpdateActveStatus  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required)
#' var_connection_account_id <- 56 # integer | The account id of the user you want to modify (if this is not set, then the accountId parameter will be used instead)
#' var_active <- "active_example" # character | true will activate the user and false will deactivate
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required) (Optional)
#' var_app_key <- "app_key_example" # character | the application key that the user belongs to (Optional)
#'
#' #Update Account Active Status
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateActveStatus(var_account_id, var_connection_account_id, var_active, device_id = var_device_id, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$UpdateActveStatus(var_account_id, var_connection_account_id, var_active, device_id = var_device_id, app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  UpdateLocation  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#' var_client_time <- 56 # integer | The time of the update (Optional)
#'
#' #Update Location
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateLocation(device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, client_time = var_client_timedata_file = "result.txt")
#' result <- api_instance$UpdateLocation(device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, client_time = var_client_time)
#' dput(result)
#'
#'
#' ####################  UpdateSettings  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_blocked_notifications <- "blocked_notifications_example" # character | The notifications to be blocked (Optional)
#' var_suggestion_method <- "suggestion_method_example" # character | How suggestions are to be sent (APNS, MOBILE_NOTIFICATION, SMS) (Optional)
#' var_suggestion_count <- 56 # integer | How many suggestions to receive per time frame (Optional)
#' var_suggestion_time_frame <- 56 # integer | The time frame in seconds, 3600 would be a 1 hour time frame (Optional)
#' var_show_others_exact_location <- "show_others_exact_location_example" # character | Show Others Exact Location (Optional)
#' var_show_as_zipcode <- "show_as_zipcode_example" # character | Show As Zipcode (Optional)
#' var_show_exact_location <- "show_exact_location_example" # character | Show Exact Location (Optional)
#' var_favorite_visibility <- "favorite_visibility_example" # character | Show favorites (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#'
#' #Update Account Settings
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateSettings(device_id = var_device_id, account_id = var_account_id, blocked_notifications = var_blocked_notifications, suggestion_method = var_suggestion_method, suggestion_count = var_suggestion_count, suggestion_time_frame = var_suggestion_time_frame, show_others_exact_location = var_show_others_exact_location, show_as_zipcode = var_show_as_zipcode, show_exact_location = var_show_exact_location, favorite_visibility = var_favorite_visibility, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$UpdateSettings(device_id = var_device_id, account_id = var_account_id, blocked_notifications = var_blocked_notifications, suggestion_method = var_suggestion_method, suggestion_count = var_suggestion_count, suggestion_time_frame = var_suggestion_time_frame, show_others_exact_location = var_show_others_exact_location, show_as_zipcode = var_show_as_zipcode, show_exact_location = var_show_exact_location, favorite_visibility = var_favorite_visibility, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  ValidateAccountSignup  ####################
#'
#' library(openapi)
#' var_token <- "token_example" # character | The token associated with the account to update, good for 24 hours
#'
#' #Save Validation Status
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ValidateAccountSignup(var_tokendata_file = "result.txt")
#' result <- api_instance$ValidateAccountSignup(var_token)
#' dput(result)
#'
#'
#' ####################  ValidatePasswordReset  ####################
#'
#' library(openapi)
#' var_token <- "token_example" # character | The token associated with the account to update, good for 24 hours
#'
#' #Validate Password Reset Token
#' api_instance <- AccountApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ValidatePasswordReset(var_tokendata_file = "result.txt")
#' result <- api_instance$ValidatePasswordReset(var_token)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AccountApi <- R6::R6Class(
  "AccountApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AccountApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Search Accounts by Location
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param q (optional) Deprecated - legacy query parameter
    #' @param keyword (optional) An optional keyword to search on, will be ignore if empty (NOT implemented yet)
    #' @param postal_code (optional) The postal code to search on, either postalCode or the user's exact location is required
    #' @param latitude (optional) The latitude of the user, either postalCode or the user's exact location is required
    #' @param longitude (optional) The longitude of the user, either postalCode or the user's exact location is required
    #' @param app_key (optional) The application key
    #' @param range (optional) The range to search on (default value: 10)
    #' @param location_last_updated (optional) Searches for user's that has updated their location since this date
    #' @param gender (optional) The preferred gender
    #' @param min_age (optional) The preferred min age
    #' @param max_age (optional) The preferred max age
    #' @param companionship_index (optional) The preferred companionship index
    #' @param _i (optional) this is the start index of a query
    #' @param start (optional) Start of the pagination (default value: 0)
    #' @param _l (optional) this is the limit index of a query
    #' @param limit (optional) Limit of the pagination (default value: 100)
    #' @param search_mode (optional) Search mode to use for index searches (e.g. CLOUDINDEX, OPENSEARCH)
    #' @param sort_field (optional) Sorting field for results (default: DISTANCE)
    #' @param descending (optional) Whether to sort descending (default: false)
    #' @param roles (optional) Roles to filter on
    #' @param tags (optional) Tags to filter on
    #' @param experience (optional) The experience to filter on
    #' @param category_ids (optional) The category ids to filter on (comma separated)
    #' @param audience_ids (optional) The audience ids to filter on (comma separated)
    #' @param audience_operator (optional) Operator used to combine audience filters (default: AND) (default value: "AND")
    #' @param update_current_location (optional) Whether to use the given lat & long to update the user's current location (default value: FALSE)
    #' @param update_preferred_settings (optional) Whether to use the given parameters to update the user's preferred settings (default value: FALSE)
    #' @param show_exact_locations (optional) Determines whether to always display user exact locations (default value: TRUE)
    #' @param show_connection_to_searcher (optional) Return connection of the accounts in the result to the passed in account if there exists any (default value: FALSE)
    #' @param flag_count_minimum (optional) Return any results that have a minimum of the specified flag count (even ones that have met the flag threshold)
    #' @param verified_user_only (optional) Returns only verified users
    #' @param content_admin_only (optional) Returns only content admin users
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return UserLocationSearchResponse
    AccountLocationSearch = function(device_id = NULL, account_id = NULL, q = NULL, keyword = NULL, postal_code = NULL, latitude = NULL, longitude = NULL, app_key = NULL, range = 10, location_last_updated = NULL, gender = NULL, min_age = NULL, max_age = NULL, companionship_index = NULL, _i = NULL, start = 0, _l = NULL, limit = 100, search_mode = NULL, sort_field = NULL, descending = NULL, roles = NULL, tags = NULL, experience = NULL, category_ids = NULL, audience_ids = NULL, audience_operator = "AND", update_current_location = FALSE, update_preferred_settings = FALSE, show_exact_locations = TRUE, show_connection_to_searcher = FALSE, flag_count_minimum = NULL, verified_user_only = NULL, content_admin_only = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AccountLocationSearchWithHttpInfo(device_id, account_id, q, keyword, postal_code, latitude, longitude, app_key, range, location_last_updated, gender, min_age, max_age, companionship_index, _i, start, _l, limit, search_mode, sort_field, descending, roles, tags, experience, category_ids, audience_ids, audience_operator, update_current_location, update_preferred_settings, show_exact_locations, show_connection_to_searcher, flag_count_minimum, verified_user_only, content_admin_only, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Accounts by Location
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param q (optional) Deprecated - legacy query parameter
    #' @param keyword (optional) An optional keyword to search on, will be ignore if empty (NOT implemented yet)
    #' @param postal_code (optional) The postal code to search on, either postalCode or the user's exact location is required
    #' @param latitude (optional) The latitude of the user, either postalCode or the user's exact location is required
    #' @param longitude (optional) The longitude of the user, either postalCode or the user's exact location is required
    #' @param app_key (optional) The application key
    #' @param range (optional) The range to search on (default value: 10)
    #' @param location_last_updated (optional) Searches for user's that has updated their location since this date
    #' @param gender (optional) The preferred gender
    #' @param min_age (optional) The preferred min age
    #' @param max_age (optional) The preferred max age
    #' @param companionship_index (optional) The preferred companionship index
    #' @param _i (optional) this is the start index of a query
    #' @param start (optional) Start of the pagination (default value: 0)
    #' @param _l (optional) this is the limit index of a query
    #' @param limit (optional) Limit of the pagination (default value: 100)
    #' @param search_mode (optional) Search mode to use for index searches (e.g. CLOUDINDEX, OPENSEARCH)
    #' @param sort_field (optional) Sorting field for results (default: DISTANCE)
    #' @param descending (optional) Whether to sort descending (default: false)
    #' @param roles (optional) Roles to filter on
    #' @param tags (optional) Tags to filter on
    #' @param experience (optional) The experience to filter on
    #' @param category_ids (optional) The category ids to filter on (comma separated)
    #' @param audience_ids (optional) The audience ids to filter on (comma separated)
    #' @param audience_operator (optional) Operator used to combine audience filters (default: AND) (default value: "AND")
    #' @param update_current_location (optional) Whether to use the given lat & long to update the user's current location (default value: FALSE)
    #' @param update_preferred_settings (optional) Whether to use the given parameters to update the user's preferred settings (default value: FALSE)
    #' @param show_exact_locations (optional) Determines whether to always display user exact locations (default value: TRUE)
    #' @param show_connection_to_searcher (optional) Return connection of the accounts in the result to the passed in account if there exists any (default value: FALSE)
    #' @param flag_count_minimum (optional) Return any results that have a minimum of the specified flag count (even ones that have met the flag threshold)
    #' @param verified_user_only (optional) Returns only verified users
    #' @param content_admin_only (optional) Returns only content admin users
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (UserLocationSearchResponse) with additional information such as HTTP status code, headers
    AccountLocationSearchWithHttpInfo = function(device_id = NULL, account_id = NULL, q = NULL, keyword = NULL, postal_code = NULL, latitude = NULL, longitude = NULL, app_key = NULL, range = 10, location_last_updated = NULL, gender = NULL, min_age = NULL, max_age = NULL, companionship_index = NULL, _i = NULL, start = 0, _l = NULL, limit = 100, search_mode = NULL, sort_field = NULL, descending = NULL, roles = NULL, tags = NULL, experience = NULL, category_ids = NULL, audience_ids = NULL, audience_operator = "AND", update_current_location = FALSE, update_preferred_settings = FALSE, show_exact_locations = TRUE, show_connection_to_searcher = FALSE, flag_count_minimum = NULL, verified_user_only = NULL, content_admin_only = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$AccountLocationSearch, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$AccountLocationSearch, `account_id` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling AccountApi$AccountLocationSearch, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling AccountApi$AccountLocationSearch, `keyword` is not nullable")
      }

      if (!missing(`postal_code`) && is.null(`postal_code`)) {
        stop("Invalid value for `postal_code` when calling AccountApi$AccountLocationSearch, `postal_code` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$AccountLocationSearch, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$AccountLocationSearch, `longitude` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$AccountLocationSearch, `app_key` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling AccountApi$AccountLocationSearch, `range` is not nullable")
      }

      if (!missing(`location_last_updated`) && is.null(`location_last_updated`)) {
        stop("Invalid value for `location_last_updated` when calling AccountApi$AccountLocationSearch, `location_last_updated` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling AccountApi$AccountLocationSearch, `gender` is not nullable")
      }

      if (!missing(`min_age`) && is.null(`min_age`)) {
        stop("Invalid value for `min_age` when calling AccountApi$AccountLocationSearch, `min_age` is not nullable")
      }

      if (!missing(`max_age`) && is.null(`max_age`)) {
        stop("Invalid value for `max_age` when calling AccountApi$AccountLocationSearch, `max_age` is not nullable")
      }

      if (!missing(`companionship_index`) && is.null(`companionship_index`)) {
        stop("Invalid value for `companionship_index` when calling AccountApi$AccountLocationSearch, `companionship_index` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling AccountApi$AccountLocationSearch, `_i` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AccountApi$AccountLocationSearch, `start` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling AccountApi$AccountLocationSearch, `_l` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AccountApi$AccountLocationSearch, `limit` is not nullable")
      }

      if (!missing(`search_mode`) && is.null(`search_mode`)) {
        stop("Invalid value for `search_mode` when calling AccountApi$AccountLocationSearch, `search_mode` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AccountApi$AccountLocationSearch, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AccountApi$AccountLocationSearch, `descending` is not nullable")
      }

      if (!missing(`roles`) && is.null(`roles`)) {
        stop("Invalid value for `roles` when calling AccountApi$AccountLocationSearch, `roles` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling AccountApi$AccountLocationSearch, `tags` is not nullable")
      }

      if (!missing(`experience`) && is.null(`experience`)) {
        stop("Invalid value for `experience` when calling AccountApi$AccountLocationSearch, `experience` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling AccountApi$AccountLocationSearch, `category_ids` is not nullable")
      }

      if (!missing(`audience_ids`) && is.null(`audience_ids`)) {
        stop("Invalid value for `audience_ids` when calling AccountApi$AccountLocationSearch, `audience_ids` is not nullable")
      }

      if (!missing(`audience_operator`) && is.null(`audience_operator`)) {
        stop("Invalid value for `audience_operator` when calling AccountApi$AccountLocationSearch, `audience_operator` is not nullable")
      }

      if (!missing(`update_current_location`) && is.null(`update_current_location`)) {
        stop("Invalid value for `update_current_location` when calling AccountApi$AccountLocationSearch, `update_current_location` is not nullable")
      }

      if (!missing(`update_preferred_settings`) && is.null(`update_preferred_settings`)) {
        stop("Invalid value for `update_preferred_settings` when calling AccountApi$AccountLocationSearch, `update_preferred_settings` is not nullable")
      }

      if (!missing(`show_exact_locations`) && is.null(`show_exact_locations`)) {
        stop("Invalid value for `show_exact_locations` when calling AccountApi$AccountLocationSearch, `show_exact_locations` is not nullable")
      }

      if (!missing(`show_connection_to_searcher`) && is.null(`show_connection_to_searcher`)) {
        stop("Invalid value for `show_connection_to_searcher` when calling AccountApi$AccountLocationSearch, `show_connection_to_searcher` is not nullable")
      }

      if (!missing(`flag_count_minimum`) && is.null(`flag_count_minimum`)) {
        stop("Invalid value for `flag_count_minimum` when calling AccountApi$AccountLocationSearch, `flag_count_minimum` is not nullable")
      }

      if (!missing(`verified_user_only`) && is.null(`verified_user_only`)) {
        stop("Invalid value for `verified_user_only` when calling AccountApi$AccountLocationSearch, `verified_user_only` is not nullable")
      }

      if (!missing(`content_admin_only`) && is.null(`content_admin_only`)) {
        stop("Invalid value for `content_admin_only` when calling AccountApi$AccountLocationSearch, `content_admin_only` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["postalCode"]] <- `postal_code`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["appKey"]] <- `app_key`

      query_params[["range"]] <- `range`

      query_params[["locationLastUpdated"]] <- `location_last_updated`

      query_params[["gender"]] <- `gender`

      query_params[["minAge"]] <- `min_age`

      query_params[["maxAge"]] <- `max_age`

      query_params[["companionshipIndex"]] <- `companionship_index`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["searchMode"]] <- `search_mode`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["roles"]] <- `roles`

      query_params[["tags"]] <- `tags`

      query_params[["experience"]] <- `experience`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["audienceIds"]] <- `audience_ids`

      query_params[["audienceOperator"]] <- `audience_operator`

      query_params[["updateCurrentLocation"]] <- `update_current_location`

      query_params[["updatePreferredSettings"]] <- `update_preferred_settings`

      query_params[["showExactLocations"]] <- `show_exact_locations`

      query_params[["showConnectionToSearcher"]] <- `show_connection_to_searcher`

      query_params[["flagCountMinimum"]] <- `flag_count_minimum`

      query_params[["verifiedUserOnly"]] <- `verified_user_only`

      query_params[["contentAdminOnly"]] <- `content_admin_only`

      local_var_url_path <- "/account/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UserLocationSearchResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Block Account
    #'
    #' @param account_id_being_blocked The id of the account to be blocked/unblocked
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param block_flag_value (optional) Determines whether the account is blocked or unblocked (default value: TRUE)
    #' @param remove_from_groups_if_blocked (optional) Determines whether the account is removed from all other groups if blocked (default value: FALSE)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    BlockAccount = function(account_id_being_blocked, device_id = NULL, account_id = NULL, block_flag_value = TRUE, remove_from_groups_if_blocked = FALSE, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BlockAccountWithHttpInfo(account_id_being_blocked, device_id, account_id, block_flag_value, remove_from_groups_if_blocked, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Block Account
    #'
    #' @param account_id_being_blocked The id of the account to be blocked/unblocked
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param block_flag_value (optional) Determines whether the account is blocked or unblocked (default value: TRUE)
    #' @param remove_from_groups_if_blocked (optional) Determines whether the account is removed from all other groups if blocked (default value: FALSE)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    BlockAccountWithHttpInfo = function(account_id_being_blocked, device_id = NULL, account_id = NULL, block_flag_value = TRUE, remove_from_groups_if_blocked = FALSE, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id_being_blocked`)) {
        stop("Missing required parameter `account_id_being_blocked`.")
      }

      if (!missing(`account_id_being_blocked`) && is.null(`account_id_being_blocked`)) {
        stop("Invalid value for `account_id_being_blocked` when calling AccountApi$BlockAccount, `account_id_being_blocked` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$BlockAccount, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$BlockAccount, `account_id` is not nullable")
      }

      if (!missing(`block_flag_value`) && is.null(`block_flag_value`)) {
        stop("Invalid value for `block_flag_value` when calling AccountApi$BlockAccount, `block_flag_value` is not nullable")
      }

      if (!missing(`remove_from_groups_if_blocked`) && is.null(`remove_from_groups_if_blocked`)) {
        stop("Invalid value for `remove_from_groups_if_blocked` when calling AccountApi$BlockAccount, `remove_from_groups_if_blocked` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$BlockAccount, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$BlockAccount, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["accountIdBeingBlocked"]] <- `account_id_being_blocked`

      query_params[["blockFlagValue"]] <- `block_flag_value`

      query_params[["removeFromGroupsIfBlocked"]] <- `remove_from_groups_if_blocked`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/account/block"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Account
    #'
    #' @param username The access token to authenticate with (ex: username)
    #' @param password The secret to authenticate with (ex: password)
    #' @param name (optional) The full name of the user. If this parameter is NOT empty, the following parameters will be ignored: prefixName, firstName, middleName, lastName, and suffixName
    #' @param prefix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the prefix of the user's name
    #' @param first_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's first name
    #' @param middle_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's middle name
    #' @param last_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's last name
    #' @param suffix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the suffix of the user's name
    #' @param title (optional) This field will be used to set the user's job title
    #' @param device_id (optional) The unique id of the device making the request
    #' @param device_id_type (optional) The type of device id (this is defined by the client), ie. MAC_ADDRESS, APPLE_UDID, etc
    #' @param email_address (optional) The user's contact email address (NOT the username)
    #' @param asset_id (optional) The asset id to set the user's profile image
    #' @param street_address (optional) The street address of the user's contact location
    #' @param zipcode (optional) The zipcode of the user's contact location
    #' @param gender (optional) The gender of the user (AudienceGender)
    #' @param birthday (optional) The birthday date of the user in UTC milliseconds
    #' @param home_phone (optional) The home phone number
    #' @param cell_phone (optional) The cellular phone number
    #' @param cell_phone_carrier (optional) The cellular service provider
    #' @param business_phone (optional) The business phone number
    #' @param role (optional) The account role (default: MEMBER)
    #' @param platforms (optional) Comma separated list of development platforms
    #' @param tags (optional) Search tags
    #' @param about_us (optional) About us information
    #' @param game_experience (optional) Game experience of the user
    #' @param category_ids (optional) A list of category ids that represent interests and associations
    #' @param hometown (optional) The user's hometown
    #' @param height (optional) The user's height
    #' @param height_index (optional) The user's height in a numerical value that can be used for ordering/searching
    #' @param ethnicity (optional) The user's ethnicity
    #' @param body_type (optional) The user's body type
    #' @param marital_status (optional) The user's marital status
    #' @param children (optional) The user's children status
    #' @param religion (optional) The user's religion
    #' @param education (optional) The user's education
    #' @param education_index (optional) The user's education in a numerical value that can be used for ordering/searching
    #' @param smoke (optional) The user's smoke status
    #' @param drink (optional) The user's drink status
    #' @param companionship (optional) The user's companionship status
    #' @param companionship_index (optional) The user's companionship index
    #' @param preferred_min_age (optional) The preferred minimum age in the account location search
    #' @param preferred_max_age (optional) The preferred maximum age in the account location search
    #' @param preferred_min_height (optional) The preferred minimum height in the account location search
    #' @param preferred_max_height (optional) The preferred maximum height in the account location search
    #' @param preferred_gender (optional) The preferred gender in the account location search
    #' @param preferred_education (optional) The preferred education in the account location search
    #' @param preferred_education_index (optional) The preferred education in a numerical value that can be used for ordering/searching
    #' @param preferred_body_type (optional) The preferred body type in the account location search
    #' @param preferred_ethnicity (optional) The preferred ethnicity in the account location search
    #' @param preferred_location (optional) The preferred location in the account location search
    #' @param preferred_location_range (optional) The preferred location range in the account location search
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param accepted_terms (optional) Accepted Terms
    #' @param invite_token (optional) The inviteToken that the referrer used for this account to sign up
    #' @param referral_account_id (optional) The accountId of the referrer (used if there is no inviteToken)
    #' @param send_validation (optional) Whether to send validation email
    #' @param game_type (optional) Deprecated: use appKey
    #' @param app_key (optional) The application key
    #' @param app_version (optional) The application version
    #' @param response_type (optional) Returns an AccountLoginResponse if \"AccountLoginResponse\" is passed in
    #' @param audience_ids_to_add (optional) Comma separated list of audience ids to assign to the user
    #' @param app_blob (optional) Application blob data
    #' @param app_enable_push (optional) Enable push for the app
    #' @param app_enable_sms (optional) Enable SMS for the app
    #' @param app_enable_email (optional) Enable email for the app
    #' @param location_visibility (optional) Location visibility setting
    #' @param home_latitude (optional) Home latitude
    #' @param home_longitude (optional) Home longitude
    #' @param app_nickname (optional) The nickname used in the application for this account
    #' @param personal_audience_id (optional) Personal audience id to associate with this account
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AccountLoginResponse
    CreateAccount = function(username, password, name = NULL, prefix_name = NULL, first_name = NULL, middle_name = NULL, last_name = NULL, suffix_name = NULL, title = NULL, device_id = NULL, device_id_type = NULL, email_address = NULL, asset_id = NULL, street_address = NULL, zipcode = NULL, gender = NULL, birthday = NULL, home_phone = NULL, cell_phone = NULL, cell_phone_carrier = NULL, business_phone = NULL, role = NULL, platforms = NULL, tags = NULL, about_us = NULL, game_experience = NULL, category_ids = NULL, hometown = NULL, height = NULL, height_index = NULL, ethnicity = NULL, body_type = NULL, marital_status = NULL, children = NULL, religion = NULL, education = NULL, education_index = NULL, smoke = NULL, drink = NULL, companionship = NULL, companionship_index = NULL, preferred_min_age = NULL, preferred_max_age = NULL, preferred_min_height = NULL, preferred_max_height = NULL, preferred_gender = NULL, preferred_education = NULL, preferred_education_index = NULL, preferred_body_type = NULL, preferred_ethnicity = NULL, preferred_location = NULL, preferred_location_range = NULL, latitude = NULL, longitude = NULL, accepted_terms = NULL, invite_token = NULL, referral_account_id = NULL, send_validation = NULL, game_type = NULL, app_key = NULL, app_version = NULL, response_type = NULL, audience_ids_to_add = NULL, app_blob = NULL, app_enable_push = NULL, app_enable_sms = NULL, app_enable_email = NULL, location_visibility = NULL, home_latitude = NULL, home_longitude = NULL, app_nickname = NULL, personal_audience_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateAccountWithHttpInfo(username, password, name, prefix_name, first_name, middle_name, last_name, suffix_name, title, device_id, device_id_type, email_address, asset_id, street_address, zipcode, gender, birthday, home_phone, cell_phone, cell_phone_carrier, business_phone, role, platforms, tags, about_us, game_experience, category_ids, hometown, height, height_index, ethnicity, body_type, marital_status, children, religion, education, education_index, smoke, drink, companionship, companionship_index, preferred_min_age, preferred_max_age, preferred_min_height, preferred_max_height, preferred_gender, preferred_education, preferred_education_index, preferred_body_type, preferred_ethnicity, preferred_location, preferred_location_range, latitude, longitude, accepted_terms, invite_token, referral_account_id, send_validation, game_type, app_key, app_version, response_type, audience_ids_to_add, app_blob, app_enable_push, app_enable_sms, app_enable_email, location_visibility, home_latitude, home_longitude, app_nickname, personal_audience_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Account
    #'
    #' @param username The access token to authenticate with (ex: username)
    #' @param password The secret to authenticate with (ex: password)
    #' @param name (optional) The full name of the user. If this parameter is NOT empty, the following parameters will be ignored: prefixName, firstName, middleName, lastName, and suffixName
    #' @param prefix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the prefix of the user's name
    #' @param first_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's first name
    #' @param middle_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's middle name
    #' @param last_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's last name
    #' @param suffix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the suffix of the user's name
    #' @param title (optional) This field will be used to set the user's job title
    #' @param device_id (optional) The unique id of the device making the request
    #' @param device_id_type (optional) The type of device id (this is defined by the client), ie. MAC_ADDRESS, APPLE_UDID, etc
    #' @param email_address (optional) The user's contact email address (NOT the username)
    #' @param asset_id (optional) The asset id to set the user's profile image
    #' @param street_address (optional) The street address of the user's contact location
    #' @param zipcode (optional) The zipcode of the user's contact location
    #' @param gender (optional) The gender of the user (AudienceGender)
    #' @param birthday (optional) The birthday date of the user in UTC milliseconds
    #' @param home_phone (optional) The home phone number
    #' @param cell_phone (optional) The cellular phone number
    #' @param cell_phone_carrier (optional) The cellular service provider
    #' @param business_phone (optional) The business phone number
    #' @param role (optional) The account role (default: MEMBER)
    #' @param platforms (optional) Comma separated list of development platforms
    #' @param tags (optional) Search tags
    #' @param about_us (optional) About us information
    #' @param game_experience (optional) Game experience of the user
    #' @param category_ids (optional) A list of category ids that represent interests and associations
    #' @param hometown (optional) The user's hometown
    #' @param height (optional) The user's height
    #' @param height_index (optional) The user's height in a numerical value that can be used for ordering/searching
    #' @param ethnicity (optional) The user's ethnicity
    #' @param body_type (optional) The user's body type
    #' @param marital_status (optional) The user's marital status
    #' @param children (optional) The user's children status
    #' @param religion (optional) The user's religion
    #' @param education (optional) The user's education
    #' @param education_index (optional) The user's education in a numerical value that can be used for ordering/searching
    #' @param smoke (optional) The user's smoke status
    #' @param drink (optional) The user's drink status
    #' @param companionship (optional) The user's companionship status
    #' @param companionship_index (optional) The user's companionship index
    #' @param preferred_min_age (optional) The preferred minimum age in the account location search
    #' @param preferred_max_age (optional) The preferred maximum age in the account location search
    #' @param preferred_min_height (optional) The preferred minimum height in the account location search
    #' @param preferred_max_height (optional) The preferred maximum height in the account location search
    #' @param preferred_gender (optional) The preferred gender in the account location search
    #' @param preferred_education (optional) The preferred education in the account location search
    #' @param preferred_education_index (optional) The preferred education in a numerical value that can be used for ordering/searching
    #' @param preferred_body_type (optional) The preferred body type in the account location search
    #' @param preferred_ethnicity (optional) The preferred ethnicity in the account location search
    #' @param preferred_location (optional) The preferred location in the account location search
    #' @param preferred_location_range (optional) The preferred location range in the account location search
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param accepted_terms (optional) Accepted Terms
    #' @param invite_token (optional) The inviteToken that the referrer used for this account to sign up
    #' @param referral_account_id (optional) The accountId of the referrer (used if there is no inviteToken)
    #' @param send_validation (optional) Whether to send validation email
    #' @param game_type (optional) Deprecated: use appKey
    #' @param app_key (optional) The application key
    #' @param app_version (optional) The application version
    #' @param response_type (optional) Returns an AccountLoginResponse if \"AccountLoginResponse\" is passed in
    #' @param audience_ids_to_add (optional) Comma separated list of audience ids to assign to the user
    #' @param app_blob (optional) Application blob data
    #' @param app_enable_push (optional) Enable push for the app
    #' @param app_enable_sms (optional) Enable SMS for the app
    #' @param app_enable_email (optional) Enable email for the app
    #' @param location_visibility (optional) Location visibility setting
    #' @param home_latitude (optional) Home latitude
    #' @param home_longitude (optional) Home longitude
    #' @param app_nickname (optional) The nickname used in the application for this account
    #' @param personal_audience_id (optional) Personal audience id to associate with this account
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AccountLoginResponse) with additional information such as HTTP status code, headers
    CreateAccountWithHttpInfo = function(username, password, name = NULL, prefix_name = NULL, first_name = NULL, middle_name = NULL, last_name = NULL, suffix_name = NULL, title = NULL, device_id = NULL, device_id_type = NULL, email_address = NULL, asset_id = NULL, street_address = NULL, zipcode = NULL, gender = NULL, birthday = NULL, home_phone = NULL, cell_phone = NULL, cell_phone_carrier = NULL, business_phone = NULL, role = NULL, platforms = NULL, tags = NULL, about_us = NULL, game_experience = NULL, category_ids = NULL, hometown = NULL, height = NULL, height_index = NULL, ethnicity = NULL, body_type = NULL, marital_status = NULL, children = NULL, religion = NULL, education = NULL, education_index = NULL, smoke = NULL, drink = NULL, companionship = NULL, companionship_index = NULL, preferred_min_age = NULL, preferred_max_age = NULL, preferred_min_height = NULL, preferred_max_height = NULL, preferred_gender = NULL, preferred_education = NULL, preferred_education_index = NULL, preferred_body_type = NULL, preferred_ethnicity = NULL, preferred_location = NULL, preferred_location_range = NULL, latitude = NULL, longitude = NULL, accepted_terms = NULL, invite_token = NULL, referral_account_id = NULL, send_validation = NULL, game_type = NULL, app_key = NULL, app_version = NULL, response_type = NULL, audience_ids_to_add = NULL, app_blob = NULL, app_enable_push = NULL, app_enable_sms = NULL, app_enable_email = NULL, location_visibility = NULL, home_latitude = NULL, home_longitude = NULL, app_nickname = NULL, personal_audience_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`username`)) {
        stop("Missing required parameter `username`.")
      }

      if (missing(`password`)) {
        stop("Missing required parameter `password`.")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling AccountApi$CreateAccount, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling AccountApi$CreateAccount, `password` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling AccountApi$CreateAccount, `name` is not nullable")
      }

      if (!missing(`prefix_name`) && is.null(`prefix_name`)) {
        stop("Invalid value for `prefix_name` when calling AccountApi$CreateAccount, `prefix_name` is not nullable")
      }

      if (!missing(`first_name`) && is.null(`first_name`)) {
        stop("Invalid value for `first_name` when calling AccountApi$CreateAccount, `first_name` is not nullable")
      }

      if (!missing(`middle_name`) && is.null(`middle_name`)) {
        stop("Invalid value for `middle_name` when calling AccountApi$CreateAccount, `middle_name` is not nullable")
      }

      if (!missing(`last_name`) && is.null(`last_name`)) {
        stop("Invalid value for `last_name` when calling AccountApi$CreateAccount, `last_name` is not nullable")
      }

      if (!missing(`suffix_name`) && is.null(`suffix_name`)) {
        stop("Invalid value for `suffix_name` when calling AccountApi$CreateAccount, `suffix_name` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling AccountApi$CreateAccount, `title` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$CreateAccount, `device_id` is not nullable")
      }

      if (!missing(`device_id_type`) && is.null(`device_id_type`)) {
        stop("Invalid value for `device_id_type` when calling AccountApi$CreateAccount, `device_id_type` is not nullable")
      }

      if (!missing(`email_address`) && is.null(`email_address`)) {
        stop("Invalid value for `email_address` when calling AccountApi$CreateAccount, `email_address` is not nullable")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling AccountApi$CreateAccount, `asset_id` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling AccountApi$CreateAccount, `street_address` is not nullable")
      }

      if (!missing(`zipcode`) && is.null(`zipcode`)) {
        stop("Invalid value for `zipcode` when calling AccountApi$CreateAccount, `zipcode` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling AccountApi$CreateAccount, `gender` is not nullable")
      }

      if (!missing(`birthday`) && is.null(`birthday`)) {
        stop("Invalid value for `birthday` when calling AccountApi$CreateAccount, `birthday` is not nullable")
      }

      if (!missing(`home_phone`) && is.null(`home_phone`)) {
        stop("Invalid value for `home_phone` when calling AccountApi$CreateAccount, `home_phone` is not nullable")
      }

      if (!missing(`cell_phone`) && is.null(`cell_phone`)) {
        stop("Invalid value for `cell_phone` when calling AccountApi$CreateAccount, `cell_phone` is not nullable")
      }

      if (!missing(`cell_phone_carrier`) && is.null(`cell_phone_carrier`)) {
        stop("Invalid value for `cell_phone_carrier` when calling AccountApi$CreateAccount, `cell_phone_carrier` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling AccountApi$CreateAccount, `business_phone` is not nullable")
      }

      if (!missing(`role`) && is.null(`role`)) {
        stop("Invalid value for `role` when calling AccountApi$CreateAccount, `role` is not nullable")
      }

      if (!missing(`platforms`) && is.null(`platforms`)) {
        stop("Invalid value for `platforms` when calling AccountApi$CreateAccount, `platforms` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling AccountApi$CreateAccount, `tags` is not nullable")
      }

      if (!missing(`about_us`) && is.null(`about_us`)) {
        stop("Invalid value for `about_us` when calling AccountApi$CreateAccount, `about_us` is not nullable")
      }

      if (!missing(`game_experience`) && is.null(`game_experience`)) {
        stop("Invalid value for `game_experience` when calling AccountApi$CreateAccount, `game_experience` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling AccountApi$CreateAccount, `category_ids` is not nullable")
      }

      if (!missing(`hometown`) && is.null(`hometown`)) {
        stop("Invalid value for `hometown` when calling AccountApi$CreateAccount, `hometown` is not nullable")
      }

      if (!missing(`height`) && is.null(`height`)) {
        stop("Invalid value for `height` when calling AccountApi$CreateAccount, `height` is not nullable")
      }

      if (!missing(`height_index`) && is.null(`height_index`)) {
        stop("Invalid value for `height_index` when calling AccountApi$CreateAccount, `height_index` is not nullable")
      }

      if (!missing(`ethnicity`) && is.null(`ethnicity`)) {
        stop("Invalid value for `ethnicity` when calling AccountApi$CreateAccount, `ethnicity` is not nullable")
      }

      if (!missing(`body_type`) && is.null(`body_type`)) {
        stop("Invalid value for `body_type` when calling AccountApi$CreateAccount, `body_type` is not nullable")
      }

      if (!missing(`marital_status`) && is.null(`marital_status`)) {
        stop("Invalid value for `marital_status` when calling AccountApi$CreateAccount, `marital_status` is not nullable")
      }

      if (!missing(`children`) && is.null(`children`)) {
        stop("Invalid value for `children` when calling AccountApi$CreateAccount, `children` is not nullable")
      }

      if (!missing(`religion`) && is.null(`religion`)) {
        stop("Invalid value for `religion` when calling AccountApi$CreateAccount, `religion` is not nullable")
      }

      if (!missing(`education`) && is.null(`education`)) {
        stop("Invalid value for `education` when calling AccountApi$CreateAccount, `education` is not nullable")
      }

      if (!missing(`education_index`) && is.null(`education_index`)) {
        stop("Invalid value for `education_index` when calling AccountApi$CreateAccount, `education_index` is not nullable")
      }

      if (!missing(`smoke`) && is.null(`smoke`)) {
        stop("Invalid value for `smoke` when calling AccountApi$CreateAccount, `smoke` is not nullable")
      }

      if (!missing(`drink`) && is.null(`drink`)) {
        stop("Invalid value for `drink` when calling AccountApi$CreateAccount, `drink` is not nullable")
      }

      if (!missing(`companionship`) && is.null(`companionship`)) {
        stop("Invalid value for `companionship` when calling AccountApi$CreateAccount, `companionship` is not nullable")
      }

      if (!missing(`companionship_index`) && is.null(`companionship_index`)) {
        stop("Invalid value for `companionship_index` when calling AccountApi$CreateAccount, `companionship_index` is not nullable")
      }

      if (!missing(`preferred_min_age`) && is.null(`preferred_min_age`)) {
        stop("Invalid value for `preferred_min_age` when calling AccountApi$CreateAccount, `preferred_min_age` is not nullable")
      }

      if (!missing(`preferred_max_age`) && is.null(`preferred_max_age`)) {
        stop("Invalid value for `preferred_max_age` when calling AccountApi$CreateAccount, `preferred_max_age` is not nullable")
      }

      if (!missing(`preferred_min_height`) && is.null(`preferred_min_height`)) {
        stop("Invalid value for `preferred_min_height` when calling AccountApi$CreateAccount, `preferred_min_height` is not nullable")
      }

      if (!missing(`preferred_max_height`) && is.null(`preferred_max_height`)) {
        stop("Invalid value for `preferred_max_height` when calling AccountApi$CreateAccount, `preferred_max_height` is not nullable")
      }

      if (!missing(`preferred_gender`) && is.null(`preferred_gender`)) {
        stop("Invalid value for `preferred_gender` when calling AccountApi$CreateAccount, `preferred_gender` is not nullable")
      }

      if (!missing(`preferred_education`) && is.null(`preferred_education`)) {
        stop("Invalid value for `preferred_education` when calling AccountApi$CreateAccount, `preferred_education` is not nullable")
      }

      if (!missing(`preferred_education_index`) && is.null(`preferred_education_index`)) {
        stop("Invalid value for `preferred_education_index` when calling AccountApi$CreateAccount, `preferred_education_index` is not nullable")
      }

      if (!missing(`preferred_body_type`) && is.null(`preferred_body_type`)) {
        stop("Invalid value for `preferred_body_type` when calling AccountApi$CreateAccount, `preferred_body_type` is not nullable")
      }

      if (!missing(`preferred_ethnicity`) && is.null(`preferred_ethnicity`)) {
        stop("Invalid value for `preferred_ethnicity` when calling AccountApi$CreateAccount, `preferred_ethnicity` is not nullable")
      }

      if (!missing(`preferred_location`) && is.null(`preferred_location`)) {
        stop("Invalid value for `preferred_location` when calling AccountApi$CreateAccount, `preferred_location` is not nullable")
      }

      if (!missing(`preferred_location_range`) && is.null(`preferred_location_range`)) {
        stop("Invalid value for `preferred_location_range` when calling AccountApi$CreateAccount, `preferred_location_range` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$CreateAccount, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$CreateAccount, `longitude` is not nullable")
      }

      if (!missing(`accepted_terms`) && is.null(`accepted_terms`)) {
        stop("Invalid value for `accepted_terms` when calling AccountApi$CreateAccount, `accepted_terms` is not nullable")
      }

      if (!missing(`invite_token`) && is.null(`invite_token`)) {
        stop("Invalid value for `invite_token` when calling AccountApi$CreateAccount, `invite_token` is not nullable")
      }

      if (!missing(`referral_account_id`) && is.null(`referral_account_id`)) {
        stop("Invalid value for `referral_account_id` when calling AccountApi$CreateAccount, `referral_account_id` is not nullable")
      }

      if (!missing(`send_validation`) && is.null(`send_validation`)) {
        stop("Invalid value for `send_validation` when calling AccountApi$CreateAccount, `send_validation` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling AccountApi$CreateAccount, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$CreateAccount, `app_key` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling AccountApi$CreateAccount, `app_version` is not nullable")
      }

      if (!missing(`response_type`) && is.null(`response_type`)) {
        stop("Invalid value for `response_type` when calling AccountApi$CreateAccount, `response_type` is not nullable")
      }

      if (!missing(`audience_ids_to_add`) && is.null(`audience_ids_to_add`)) {
        stop("Invalid value for `audience_ids_to_add` when calling AccountApi$CreateAccount, `audience_ids_to_add` is not nullable")
      }

      if (!missing(`app_blob`) && is.null(`app_blob`)) {
        stop("Invalid value for `app_blob` when calling AccountApi$CreateAccount, `app_blob` is not nullable")
      }

      if (!missing(`app_enable_push`) && is.null(`app_enable_push`)) {
        stop("Invalid value for `app_enable_push` when calling AccountApi$CreateAccount, `app_enable_push` is not nullable")
      }

      if (!missing(`app_enable_sms`) && is.null(`app_enable_sms`)) {
        stop("Invalid value for `app_enable_sms` when calling AccountApi$CreateAccount, `app_enable_sms` is not nullable")
      }

      if (!missing(`app_enable_email`) && is.null(`app_enable_email`)) {
        stop("Invalid value for `app_enable_email` when calling AccountApi$CreateAccount, `app_enable_email` is not nullable")
      }

      if (!missing(`location_visibility`) && is.null(`location_visibility`)) {
        stop("Invalid value for `location_visibility` when calling AccountApi$CreateAccount, `location_visibility` is not nullable")
      }

      if (!missing(`home_latitude`) && is.null(`home_latitude`)) {
        stop("Invalid value for `home_latitude` when calling AccountApi$CreateAccount, `home_latitude` is not nullable")
      }

      if (!missing(`home_longitude`) && is.null(`home_longitude`)) {
        stop("Invalid value for `home_longitude` when calling AccountApi$CreateAccount, `home_longitude` is not nullable")
      }

      if (!missing(`app_nickname`) && is.null(`app_nickname`)) {
        stop("Invalid value for `app_nickname` when calling AccountApi$CreateAccount, `app_nickname` is not nullable")
      }

      if (!missing(`personal_audience_id`) && is.null(`personal_audience_id`)) {
        stop("Invalid value for `personal_audience_id` when calling AccountApi$CreateAccount, `personal_audience_id` is not nullable")
      }

      query_params[["name"]] <- `name`

      query_params[["prefixName"]] <- `prefix_name`

      query_params[["firstName"]] <- `first_name`

      query_params[["middleName"]] <- `middle_name`

      query_params[["lastName"]] <- `last_name`

      query_params[["suffixName"]] <- `suffix_name`

      query_params[["title"]] <- `title`

      query_params[["deviceId"]] <- `device_id`

      query_params[["deviceIdType"]] <- `device_id_type`

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      query_params[["emailAddress"]] <- `email_address`

      query_params[["assetId"]] <- `asset_id`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["zipcode"]] <- `zipcode`

      query_params[["gender"]] <- `gender`

      query_params[["birthday"]] <- `birthday`

      query_params[["homePhone"]] <- `home_phone`

      query_params[["cellPhone"]] <- `cell_phone`

      query_params[["cellPhoneCarrier"]] <- `cell_phone_carrier`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["role"]] <- `role`

      query_params[["platforms"]] <- `platforms`

      query_params[["tags"]] <- `tags`

      query_params[["aboutUs"]] <- `about_us`

      query_params[["gameExperience"]] <- `game_experience`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["hometown"]] <- `hometown`

      query_params[["height"]] <- `height`

      query_params[["heightIndex"]] <- `height_index`

      query_params[["ethnicity"]] <- `ethnicity`

      query_params[["bodyType"]] <- `body_type`

      query_params[["maritalStatus"]] <- `marital_status`

      query_params[["children"]] <- `children`

      query_params[["religion"]] <- `religion`

      query_params[["education"]] <- `education`

      query_params[["educationIndex"]] <- `education_index`

      query_params[["smoke"]] <- `smoke`

      query_params[["drink"]] <- `drink`

      query_params[["companionship"]] <- `companionship`

      query_params[["companionshipIndex"]] <- `companionship_index`

      query_params[["preferredMinAge"]] <- `preferred_min_age`

      query_params[["preferredMaxAge"]] <- `preferred_max_age`

      query_params[["preferredMinHeight"]] <- `preferred_min_height`

      query_params[["preferredMaxHeight"]] <- `preferred_max_height`

      query_params[["preferredGender"]] <- `preferred_gender`

      query_params[["preferredEducation"]] <- `preferred_education`

      query_params[["preferredEducationIndex"]] <- `preferred_education_index`

      query_params[["preferredBodyType"]] <- `preferred_body_type`

      query_params[["preferredEthnicity"]] <- `preferred_ethnicity`

      query_params[["preferredLocation"]] <- `preferred_location`

      query_params[["preferredLocationRange"]] <- `preferred_location_range`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["acceptedTerms"]] <- `accepted_terms`

      query_params[["inviteToken"]] <- `invite_token`

      query_params[["referralAccountId"]] <- `referral_account_id`

      query_params[["sendValidation"]] <- `send_validation`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["appVersion"]] <- `app_version`

      query_params[["responseType"]] <- `response_type`

      query_params[["audienceIdsToAdd"]] <- `audience_ids_to_add`

      query_params[["appBlob"]] <- `app_blob`

      query_params[["appEnablePush"]] <- `app_enable_push`

      query_params[["appEnableSMS"]] <- `app_enable_sms`

      query_params[["appEnableEmail"]] <- `app_enable_email`

      query_params[["locationVisibility"]] <- `location_visibility`

      query_params[["homeLatitude"]] <- `home_latitude`

      query_params[["homeLongitude"]] <- `home_longitude`

      query_params[["appNickname"]] <- `app_nickname`

      query_params[["personalAudienceId"]] <- `personal_audience_id`

      local_var_url_path <- "/account/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AccountLoginResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Account
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param connection_account_id (optional) The account id used to edit another person's account
    #' @param role (optional) The account role to change to
    #' @param asset_id (optional) The asset id to set the user's profile image
    #' @param name (optional) The full name of the user. If this parameter is NOT empty, the  following parameters will be ignored: prefixName, firstName, middleName,  lastName, and suffixName 
    #' @param prefix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the prefix of the user's name
    #' @param first_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's first name
    #' @param middle_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's middle name
    #' @param last_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's last name
    #' @param suffix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the suffix of the user's name
    #' @param title (optional) This field will be used to set the user's job title
    #' @param gender (optional) The gender of the user AudienceGender
    #' @param age (optional) This is deperecated, use the birthday parameter
    #' @param birthday (optional) The birthday date of the user in UTC milliseconds
    #' @param home_phone (optional) The home phone number
    #' @param cell_phone (optional) The cellular phone number
    #' @param cell_phone_carrier (optional) The cellular service provider
    #' @param business_phone (optional) The business phone number
    #' @param email_address (optional) The user's contact email address (NOT the username)
    #' @param street_address (optional) The street address of the user's contact location
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the user's contact location
    #' @param state (optional) The state of the user's contact location
    #' @param zipcode (optional) The zipcode of the user's contact location
    #' @param country (optional) The country of the user's contact location
    #' @param make_profile_info_public (optional) Allow anyone to view the user's personal profile
    #' @param make_game_info_public (optional) Allow anyone to view the user's game/app info
    #' @param make_friends_info_public (optional) Allow anyone to view the user's friends list
    #' @param hometown (optional) The user's hometown
    #' @param height (optional) The user's height
    #' @param height_index (optional) The user's height in a numerical value that can be used for ordering/searching
    #' @param ethnicity (optional) The user's ethnicity
    #' @param body_type (optional) The user's body type
    #' @param marital_status (optional) The user's marital status
    #' @param children (optional) The user's children status
    #' @param religion (optional) The user's religion
    #' @param education (optional) The user's education
    #' @param education_index (optional) The user's education in a numerical value that can be used for ordering/searching
    #' @param smoke (optional) The user's smoke status
    #' @param drink (optional) The user's drink status
    #' @param companionship (optional) The user's companionship status
    #' @param companionship_index (optional) The user's companionship index
    #' @param preferred_min_age (optional) The preferred minimum age in the account location search
    #' @param preferred_max_age (optional) The preferred maximum age in the account location search
    #' @param preferred_min_height (optional) The preferred minimum height in the account location search
    #' @param preferred_max_height (optional) The preferred maximum height in the account location search
    #' @param preferred_gender (optional) The preferred gender in the account location search
    #' @param preferred_education (optional) The preferred education in the account location search
    #' @param preferred_education_index (optional) The preferred education in a numerical value that can be used for ordering/searching
    #' @param preferred_body_type (optional) The preferred body type in the account location search
    #' @param preferred_ethnicity (optional) The preferred ethnicity in the account location search
    #' @param preferred_location (optional) The preferred education in the account location search
    #' @param preferred_location_range (optional) The preferred location range in the account location search
    #' @param platforms (optional) Platforms
    #' @param tags (optional) Tags
    #' @param about_us (optional) About Us
    #' @param match_token (optional) Match Token
    #' @param game_experience (optional) Game Experience
    #' @param categories (optional) Deprecated use categoryIds
    #' @param category_ids (optional) A list of category ids that represent interests and associations
    #' @param response_filters (optional) A comma separated list of ProfileFilters for filtering the returned response data (default value: "PROFILE,PROFILE_CONTACT")
    #' @param show_as_zipcode (optional) The user's preference if they want to be shown by zipcode on a map
    #' @param show_exact_location (optional) The user's preference if they want to be shown by their exact location on a map
    #' @param show_others_exact_location (optional) The user's preference if they want to see others exact location on a map
    #' @param accepted_terms (optional) Accepted Terms
    #' @param location_visibility (optional) Location Visibility
    #' @param app_blob (optional) App Blob
    #' @param app_enable_push (optional) App Enable Push
    #' @param app_enable_sms (optional) App Enable SMS
    #' @param app_enable_email (optional) App Enable Email
    #' @param game_type (optional) Game Type
    #' @param app_key (optional) The application key
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param return_profile (optional) Return Profile
    #' @param audience_ids_to_add (optional) Audience Ids to add
    #' @param audience_ids_to_remove (optional) Audience Ids to remove
    #' @param referral_account_id (optional) The account id of the referrer
    #' @param app_nickname (optional) App nickname
    #' @param personal_audience_id (optional) Personal Audience
    #' @param non_guest_username (optional) The user's username to update with if they currently have a guest username
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileInfoResponse
    EditAccount = function(device_id = NULL, account_id = NULL, connection_account_id = NULL, role = NULL, asset_id = NULL, name = NULL, prefix_name = NULL, first_name = NULL, middle_name = NULL, last_name = NULL, suffix_name = NULL, title = NULL, gender = NULL, age = NULL, birthday = NULL, home_phone = NULL, cell_phone = NULL, cell_phone_carrier = NULL, business_phone = NULL, email_address = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, zipcode = NULL, country = NULL, make_profile_info_public = NULL, make_game_info_public = NULL, make_friends_info_public = NULL, hometown = NULL, height = NULL, height_index = NULL, ethnicity = NULL, body_type = NULL, marital_status = NULL, children = NULL, religion = NULL, education = NULL, education_index = NULL, smoke = NULL, drink = NULL, companionship = NULL, companionship_index = NULL, preferred_min_age = NULL, preferred_max_age = NULL, preferred_min_height = NULL, preferred_max_height = NULL, preferred_gender = NULL, preferred_education = NULL, preferred_education_index = NULL, preferred_body_type = NULL, preferred_ethnicity = NULL, preferred_location = NULL, preferred_location_range = NULL, platforms = NULL, tags = NULL, about_us = NULL, match_token = NULL, game_experience = NULL, categories = NULL, category_ids = NULL, response_filters = "PROFILE,PROFILE_CONTACT", show_as_zipcode = NULL, show_exact_location = NULL, show_others_exact_location = NULL, accepted_terms = NULL, location_visibility = NULL, app_blob = NULL, app_enable_push = NULL, app_enable_sms = NULL, app_enable_email = NULL, game_type = NULL, app_key = NULL, latitude = NULL, longitude = NULL, return_profile = NULL, audience_ids_to_add = NULL, audience_ids_to_remove = NULL, referral_account_id = NULL, app_nickname = NULL, personal_audience_id = NULL, non_guest_username = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$EditAccountWithHttpInfo(device_id, account_id, connection_account_id, role, asset_id, name, prefix_name, first_name, middle_name, last_name, suffix_name, title, gender, age, birthday, home_phone, cell_phone, cell_phone_carrier, business_phone, email_address, street_address, street_address2, city, state, zipcode, country, make_profile_info_public, make_game_info_public, make_friends_info_public, hometown, height, height_index, ethnicity, body_type, marital_status, children, religion, education, education_index, smoke, drink, companionship, companionship_index, preferred_min_age, preferred_max_age, preferred_min_height, preferred_max_height, preferred_gender, preferred_education, preferred_education_index, preferred_body_type, preferred_ethnicity, preferred_location, preferred_location_range, platforms, tags, about_us, match_token, game_experience, categories, category_ids, response_filters, show_as_zipcode, show_exact_location, show_others_exact_location, accepted_terms, location_visibility, app_blob, app_enable_push, app_enable_sms, app_enable_email, game_type, app_key, latitude, longitude, return_profile, audience_ids_to_add, audience_ids_to_remove, referral_account_id, app_nickname, personal_audience_id, non_guest_username, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Account
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param connection_account_id (optional) The account id used to edit another person's account
    #' @param role (optional) The account role to change to
    #' @param asset_id (optional) The asset id to set the user's profile image
    #' @param name (optional) The full name of the user. If this parameter is NOT empty, the  following parameters will be ignored: prefixName, firstName, middleName,  lastName, and suffixName 
    #' @param prefix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the prefix of the user's name
    #' @param first_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's first name
    #' @param middle_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's middle name
    #' @param last_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's last name
    #' @param suffix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the suffix of the user's name
    #' @param title (optional) This field will be used to set the user's job title
    #' @param gender (optional) The gender of the user AudienceGender
    #' @param age (optional) This is deperecated, use the birthday parameter
    #' @param birthday (optional) The birthday date of the user in UTC milliseconds
    #' @param home_phone (optional) The home phone number
    #' @param cell_phone (optional) The cellular phone number
    #' @param cell_phone_carrier (optional) The cellular service provider
    #' @param business_phone (optional) The business phone number
    #' @param email_address (optional) The user's contact email address (NOT the username)
    #' @param street_address (optional) The street address of the user's contact location
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the user's contact location
    #' @param state (optional) The state of the user's contact location
    #' @param zipcode (optional) The zipcode of the user's contact location
    #' @param country (optional) The country of the user's contact location
    #' @param make_profile_info_public (optional) Allow anyone to view the user's personal profile
    #' @param make_game_info_public (optional) Allow anyone to view the user's game/app info
    #' @param make_friends_info_public (optional) Allow anyone to view the user's friends list
    #' @param hometown (optional) The user's hometown
    #' @param height (optional) The user's height
    #' @param height_index (optional) The user's height in a numerical value that can be used for ordering/searching
    #' @param ethnicity (optional) The user's ethnicity
    #' @param body_type (optional) The user's body type
    #' @param marital_status (optional) The user's marital status
    #' @param children (optional) The user's children status
    #' @param religion (optional) The user's religion
    #' @param education (optional) The user's education
    #' @param education_index (optional) The user's education in a numerical value that can be used for ordering/searching
    #' @param smoke (optional) The user's smoke status
    #' @param drink (optional) The user's drink status
    #' @param companionship (optional) The user's companionship status
    #' @param companionship_index (optional) The user's companionship index
    #' @param preferred_min_age (optional) The preferred minimum age in the account location search
    #' @param preferred_max_age (optional) The preferred maximum age in the account location search
    #' @param preferred_min_height (optional) The preferred minimum height in the account location search
    #' @param preferred_max_height (optional) The preferred maximum height in the account location search
    #' @param preferred_gender (optional) The preferred gender in the account location search
    #' @param preferred_education (optional) The preferred education in the account location search
    #' @param preferred_education_index (optional) The preferred education in a numerical value that can be used for ordering/searching
    #' @param preferred_body_type (optional) The preferred body type in the account location search
    #' @param preferred_ethnicity (optional) The preferred ethnicity in the account location search
    #' @param preferred_location (optional) The preferred education in the account location search
    #' @param preferred_location_range (optional) The preferred location range in the account location search
    #' @param platforms (optional) Platforms
    #' @param tags (optional) Tags
    #' @param about_us (optional) About Us
    #' @param match_token (optional) Match Token
    #' @param game_experience (optional) Game Experience
    #' @param categories (optional) Deprecated use categoryIds
    #' @param category_ids (optional) A list of category ids that represent interests and associations
    #' @param response_filters (optional) A comma separated list of ProfileFilters for filtering the returned response data (default value: "PROFILE,PROFILE_CONTACT")
    #' @param show_as_zipcode (optional) The user's preference if they want to be shown by zipcode on a map
    #' @param show_exact_location (optional) The user's preference if they want to be shown by their exact location on a map
    #' @param show_others_exact_location (optional) The user's preference if they want to see others exact location on a map
    #' @param accepted_terms (optional) Accepted Terms
    #' @param location_visibility (optional) Location Visibility
    #' @param app_blob (optional) App Blob
    #' @param app_enable_push (optional) App Enable Push
    #' @param app_enable_sms (optional) App Enable SMS
    #' @param app_enable_email (optional) App Enable Email
    #' @param game_type (optional) Game Type
    #' @param app_key (optional) The application key
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param return_profile (optional) Return Profile
    #' @param audience_ids_to_add (optional) Audience Ids to add
    #' @param audience_ids_to_remove (optional) Audience Ids to remove
    #' @param referral_account_id (optional) The account id of the referrer
    #' @param app_nickname (optional) App nickname
    #' @param personal_audience_id (optional) Personal Audience
    #' @param non_guest_username (optional) The user's username to update with if they currently have a guest username
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileInfoResponse) with additional information such as HTTP status code, headers
    EditAccountWithHttpInfo = function(device_id = NULL, account_id = NULL, connection_account_id = NULL, role = NULL, asset_id = NULL, name = NULL, prefix_name = NULL, first_name = NULL, middle_name = NULL, last_name = NULL, suffix_name = NULL, title = NULL, gender = NULL, age = NULL, birthday = NULL, home_phone = NULL, cell_phone = NULL, cell_phone_carrier = NULL, business_phone = NULL, email_address = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, zipcode = NULL, country = NULL, make_profile_info_public = NULL, make_game_info_public = NULL, make_friends_info_public = NULL, hometown = NULL, height = NULL, height_index = NULL, ethnicity = NULL, body_type = NULL, marital_status = NULL, children = NULL, religion = NULL, education = NULL, education_index = NULL, smoke = NULL, drink = NULL, companionship = NULL, companionship_index = NULL, preferred_min_age = NULL, preferred_max_age = NULL, preferred_min_height = NULL, preferred_max_height = NULL, preferred_gender = NULL, preferred_education = NULL, preferred_education_index = NULL, preferred_body_type = NULL, preferred_ethnicity = NULL, preferred_location = NULL, preferred_location_range = NULL, platforms = NULL, tags = NULL, about_us = NULL, match_token = NULL, game_experience = NULL, categories = NULL, category_ids = NULL, response_filters = "PROFILE,PROFILE_CONTACT", show_as_zipcode = NULL, show_exact_location = NULL, show_others_exact_location = NULL, accepted_terms = NULL, location_visibility = NULL, app_blob = NULL, app_enable_push = NULL, app_enable_sms = NULL, app_enable_email = NULL, game_type = NULL, app_key = NULL, latitude = NULL, longitude = NULL, return_profile = NULL, audience_ids_to_add = NULL, audience_ids_to_remove = NULL, referral_account_id = NULL, app_nickname = NULL, personal_audience_id = NULL, non_guest_username = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$EditAccount, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$EditAccount, `account_id` is not nullable")
      }

      if (!missing(`connection_account_id`) && is.null(`connection_account_id`)) {
        stop("Invalid value for `connection_account_id` when calling AccountApi$EditAccount, `connection_account_id` is not nullable")
      }

      if (!missing(`role`) && is.null(`role`)) {
        stop("Invalid value for `role` when calling AccountApi$EditAccount, `role` is not nullable")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling AccountApi$EditAccount, `asset_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling AccountApi$EditAccount, `name` is not nullable")
      }

      if (!missing(`prefix_name`) && is.null(`prefix_name`)) {
        stop("Invalid value for `prefix_name` when calling AccountApi$EditAccount, `prefix_name` is not nullable")
      }

      if (!missing(`first_name`) && is.null(`first_name`)) {
        stop("Invalid value for `first_name` when calling AccountApi$EditAccount, `first_name` is not nullable")
      }

      if (!missing(`middle_name`) && is.null(`middle_name`)) {
        stop("Invalid value for `middle_name` when calling AccountApi$EditAccount, `middle_name` is not nullable")
      }

      if (!missing(`last_name`) && is.null(`last_name`)) {
        stop("Invalid value for `last_name` when calling AccountApi$EditAccount, `last_name` is not nullable")
      }

      if (!missing(`suffix_name`) && is.null(`suffix_name`)) {
        stop("Invalid value for `suffix_name` when calling AccountApi$EditAccount, `suffix_name` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling AccountApi$EditAccount, `title` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling AccountApi$EditAccount, `gender` is not nullable")
      }

      if (!missing(`age`) && is.null(`age`)) {
        stop("Invalid value for `age` when calling AccountApi$EditAccount, `age` is not nullable")
      }

      if (!missing(`birthday`) && is.null(`birthday`)) {
        stop("Invalid value for `birthday` when calling AccountApi$EditAccount, `birthday` is not nullable")
      }

      if (!missing(`home_phone`) && is.null(`home_phone`)) {
        stop("Invalid value for `home_phone` when calling AccountApi$EditAccount, `home_phone` is not nullable")
      }

      if (!missing(`cell_phone`) && is.null(`cell_phone`)) {
        stop("Invalid value for `cell_phone` when calling AccountApi$EditAccount, `cell_phone` is not nullable")
      }

      if (!missing(`cell_phone_carrier`) && is.null(`cell_phone_carrier`)) {
        stop("Invalid value for `cell_phone_carrier` when calling AccountApi$EditAccount, `cell_phone_carrier` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling AccountApi$EditAccount, `business_phone` is not nullable")
      }

      if (!missing(`email_address`) && is.null(`email_address`)) {
        stop("Invalid value for `email_address` when calling AccountApi$EditAccount, `email_address` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling AccountApi$EditAccount, `street_address` is not nullable")
      }

      if (!missing(`street_address2`) && is.null(`street_address2`)) {
        stop("Invalid value for `street_address2` when calling AccountApi$EditAccount, `street_address2` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling AccountApi$EditAccount, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling AccountApi$EditAccount, `state` is not nullable")
      }

      if (!missing(`zipcode`) && is.null(`zipcode`)) {
        stop("Invalid value for `zipcode` when calling AccountApi$EditAccount, `zipcode` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling AccountApi$EditAccount, `country` is not nullable")
      }

      if (!missing(`make_profile_info_public`) && is.null(`make_profile_info_public`)) {
        stop("Invalid value for `make_profile_info_public` when calling AccountApi$EditAccount, `make_profile_info_public` is not nullable")
      }

      if (!missing(`make_game_info_public`) && is.null(`make_game_info_public`)) {
        stop("Invalid value for `make_game_info_public` when calling AccountApi$EditAccount, `make_game_info_public` is not nullable")
      }

      if (!missing(`make_friends_info_public`) && is.null(`make_friends_info_public`)) {
        stop("Invalid value for `make_friends_info_public` when calling AccountApi$EditAccount, `make_friends_info_public` is not nullable")
      }

      if (!missing(`hometown`) && is.null(`hometown`)) {
        stop("Invalid value for `hometown` when calling AccountApi$EditAccount, `hometown` is not nullable")
      }

      if (!missing(`height`) && is.null(`height`)) {
        stop("Invalid value for `height` when calling AccountApi$EditAccount, `height` is not nullable")
      }

      if (!missing(`height_index`) && is.null(`height_index`)) {
        stop("Invalid value for `height_index` when calling AccountApi$EditAccount, `height_index` is not nullable")
      }

      if (!missing(`ethnicity`) && is.null(`ethnicity`)) {
        stop("Invalid value for `ethnicity` when calling AccountApi$EditAccount, `ethnicity` is not nullable")
      }

      if (!missing(`body_type`) && is.null(`body_type`)) {
        stop("Invalid value for `body_type` when calling AccountApi$EditAccount, `body_type` is not nullable")
      }

      if (!missing(`marital_status`) && is.null(`marital_status`)) {
        stop("Invalid value for `marital_status` when calling AccountApi$EditAccount, `marital_status` is not nullable")
      }

      if (!missing(`children`) && is.null(`children`)) {
        stop("Invalid value for `children` when calling AccountApi$EditAccount, `children` is not nullable")
      }

      if (!missing(`religion`) && is.null(`religion`)) {
        stop("Invalid value for `religion` when calling AccountApi$EditAccount, `religion` is not nullable")
      }

      if (!missing(`education`) && is.null(`education`)) {
        stop("Invalid value for `education` when calling AccountApi$EditAccount, `education` is not nullable")
      }

      if (!missing(`education_index`) && is.null(`education_index`)) {
        stop("Invalid value for `education_index` when calling AccountApi$EditAccount, `education_index` is not nullable")
      }

      if (!missing(`smoke`) && is.null(`smoke`)) {
        stop("Invalid value for `smoke` when calling AccountApi$EditAccount, `smoke` is not nullable")
      }

      if (!missing(`drink`) && is.null(`drink`)) {
        stop("Invalid value for `drink` when calling AccountApi$EditAccount, `drink` is not nullable")
      }

      if (!missing(`companionship`) && is.null(`companionship`)) {
        stop("Invalid value for `companionship` when calling AccountApi$EditAccount, `companionship` is not nullable")
      }

      if (!missing(`companionship_index`) && is.null(`companionship_index`)) {
        stop("Invalid value for `companionship_index` when calling AccountApi$EditAccount, `companionship_index` is not nullable")
      }

      if (!missing(`preferred_min_age`) && is.null(`preferred_min_age`)) {
        stop("Invalid value for `preferred_min_age` when calling AccountApi$EditAccount, `preferred_min_age` is not nullable")
      }

      if (!missing(`preferred_max_age`) && is.null(`preferred_max_age`)) {
        stop("Invalid value for `preferred_max_age` when calling AccountApi$EditAccount, `preferred_max_age` is not nullable")
      }

      if (!missing(`preferred_min_height`) && is.null(`preferred_min_height`)) {
        stop("Invalid value for `preferred_min_height` when calling AccountApi$EditAccount, `preferred_min_height` is not nullable")
      }

      if (!missing(`preferred_max_height`) && is.null(`preferred_max_height`)) {
        stop("Invalid value for `preferred_max_height` when calling AccountApi$EditAccount, `preferred_max_height` is not nullable")
      }

      if (!missing(`preferred_gender`) && is.null(`preferred_gender`)) {
        stop("Invalid value for `preferred_gender` when calling AccountApi$EditAccount, `preferred_gender` is not nullable")
      }

      if (!missing(`preferred_education`) && is.null(`preferred_education`)) {
        stop("Invalid value for `preferred_education` when calling AccountApi$EditAccount, `preferred_education` is not nullable")
      }

      if (!missing(`preferred_education_index`) && is.null(`preferred_education_index`)) {
        stop("Invalid value for `preferred_education_index` when calling AccountApi$EditAccount, `preferred_education_index` is not nullable")
      }

      if (!missing(`preferred_body_type`) && is.null(`preferred_body_type`)) {
        stop("Invalid value for `preferred_body_type` when calling AccountApi$EditAccount, `preferred_body_type` is not nullable")
      }

      if (!missing(`preferred_ethnicity`) && is.null(`preferred_ethnicity`)) {
        stop("Invalid value for `preferred_ethnicity` when calling AccountApi$EditAccount, `preferred_ethnicity` is not nullable")
      }

      if (!missing(`preferred_location`) && is.null(`preferred_location`)) {
        stop("Invalid value for `preferred_location` when calling AccountApi$EditAccount, `preferred_location` is not nullable")
      }

      if (!missing(`preferred_location_range`) && is.null(`preferred_location_range`)) {
        stop("Invalid value for `preferred_location_range` when calling AccountApi$EditAccount, `preferred_location_range` is not nullable")
      }

      if (!missing(`platforms`) && is.null(`platforms`)) {
        stop("Invalid value for `platforms` when calling AccountApi$EditAccount, `platforms` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling AccountApi$EditAccount, `tags` is not nullable")
      }

      if (!missing(`about_us`) && is.null(`about_us`)) {
        stop("Invalid value for `about_us` when calling AccountApi$EditAccount, `about_us` is not nullable")
      }

      if (!missing(`match_token`) && is.null(`match_token`)) {
        stop("Invalid value for `match_token` when calling AccountApi$EditAccount, `match_token` is not nullable")
      }

      if (!missing(`game_experience`) && is.null(`game_experience`)) {
        stop("Invalid value for `game_experience` when calling AccountApi$EditAccount, `game_experience` is not nullable")
      }

      if (!missing(`categories`) && is.null(`categories`)) {
        stop("Invalid value for `categories` when calling AccountApi$EditAccount, `categories` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling AccountApi$EditAccount, `category_ids` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling AccountApi$EditAccount, `response_filters` is not nullable")
      }

      if (!missing(`show_as_zipcode`) && is.null(`show_as_zipcode`)) {
        stop("Invalid value for `show_as_zipcode` when calling AccountApi$EditAccount, `show_as_zipcode` is not nullable")
      }

      if (!missing(`show_exact_location`) && is.null(`show_exact_location`)) {
        stop("Invalid value for `show_exact_location` when calling AccountApi$EditAccount, `show_exact_location` is not nullable")
      }

      if (!missing(`show_others_exact_location`) && is.null(`show_others_exact_location`)) {
        stop("Invalid value for `show_others_exact_location` when calling AccountApi$EditAccount, `show_others_exact_location` is not nullable")
      }

      if (!missing(`accepted_terms`) && is.null(`accepted_terms`)) {
        stop("Invalid value for `accepted_terms` when calling AccountApi$EditAccount, `accepted_terms` is not nullable")
      }

      if (!missing(`location_visibility`) && is.null(`location_visibility`)) {
        stop("Invalid value for `location_visibility` when calling AccountApi$EditAccount, `location_visibility` is not nullable")
      }

      if (!missing(`app_blob`) && is.null(`app_blob`)) {
        stop("Invalid value for `app_blob` when calling AccountApi$EditAccount, `app_blob` is not nullable")
      }

      if (!missing(`app_enable_push`) && is.null(`app_enable_push`)) {
        stop("Invalid value for `app_enable_push` when calling AccountApi$EditAccount, `app_enable_push` is not nullable")
      }

      if (!missing(`app_enable_sms`) && is.null(`app_enable_sms`)) {
        stop("Invalid value for `app_enable_sms` when calling AccountApi$EditAccount, `app_enable_sms` is not nullable")
      }

      if (!missing(`app_enable_email`) && is.null(`app_enable_email`)) {
        stop("Invalid value for `app_enable_email` when calling AccountApi$EditAccount, `app_enable_email` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling AccountApi$EditAccount, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$EditAccount, `app_key` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$EditAccount, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$EditAccount, `longitude` is not nullable")
      }

      if (!missing(`return_profile`) && is.null(`return_profile`)) {
        stop("Invalid value for `return_profile` when calling AccountApi$EditAccount, `return_profile` is not nullable")
      }

      if (!missing(`audience_ids_to_add`) && is.null(`audience_ids_to_add`)) {
        stop("Invalid value for `audience_ids_to_add` when calling AccountApi$EditAccount, `audience_ids_to_add` is not nullable")
      }

      if (!missing(`audience_ids_to_remove`) && is.null(`audience_ids_to_remove`)) {
        stop("Invalid value for `audience_ids_to_remove` when calling AccountApi$EditAccount, `audience_ids_to_remove` is not nullable")
      }

      if (!missing(`referral_account_id`) && is.null(`referral_account_id`)) {
        stop("Invalid value for `referral_account_id` when calling AccountApi$EditAccount, `referral_account_id` is not nullable")
      }

      if (!missing(`app_nickname`) && is.null(`app_nickname`)) {
        stop("Invalid value for `app_nickname` when calling AccountApi$EditAccount, `app_nickname` is not nullable")
      }

      if (!missing(`personal_audience_id`) && is.null(`personal_audience_id`)) {
        stop("Invalid value for `personal_audience_id` when calling AccountApi$EditAccount, `personal_audience_id` is not nullable")
      }

      if (!missing(`non_guest_username`) && is.null(`non_guest_username`)) {
        stop("Invalid value for `non_guest_username` when calling AccountApi$EditAccount, `non_guest_username` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["connectionAccountId"]] <- `connection_account_id`

      query_params[["role"]] <- `role`

      query_params[["assetId"]] <- `asset_id`

      query_params[["name"]] <- `name`

      query_params[["prefixName"]] <- `prefix_name`

      query_params[["firstName"]] <- `first_name`

      query_params[["middleName"]] <- `middle_name`

      query_params[["lastName"]] <- `last_name`

      query_params[["suffixName"]] <- `suffix_name`

      query_params[["title"]] <- `title`

      query_params[["gender"]] <- `gender`

      query_params[["age"]] <- `age`

      query_params[["birthday"]] <- `birthday`

      query_params[["homePhone"]] <- `home_phone`

      query_params[["cellPhone"]] <- `cell_phone`

      query_params[["cellPhoneCarrier"]] <- `cell_phone_carrier`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["emailAddress"]] <- `email_address`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["streetAddress2"]] <- `street_address2`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["zipcode"]] <- `zipcode`

      query_params[["country"]] <- `country`

      query_params[["makeProfileInfoPublic"]] <- `make_profile_info_public`

      query_params[["makeGameInfoPublic"]] <- `make_game_info_public`

      query_params[["makeFriendsInfoPublic"]] <- `make_friends_info_public`

      query_params[["hometown"]] <- `hometown`

      query_params[["height"]] <- `height`

      query_params[["heightIndex"]] <- `height_index`

      query_params[["ethnicity"]] <- `ethnicity`

      query_params[["bodyType"]] <- `body_type`

      query_params[["maritalStatus"]] <- `marital_status`

      query_params[["children"]] <- `children`

      query_params[["religion"]] <- `religion`

      query_params[["education"]] <- `education`

      query_params[["educationIndex"]] <- `education_index`

      query_params[["smoke"]] <- `smoke`

      query_params[["drink"]] <- `drink`

      query_params[["companionship"]] <- `companionship`

      query_params[["companionshipIndex"]] <- `companionship_index`

      query_params[["preferredMinAge"]] <- `preferred_min_age`

      query_params[["preferredMaxAge"]] <- `preferred_max_age`

      query_params[["preferredMinHeight"]] <- `preferred_min_height`

      query_params[["preferredMaxHeight"]] <- `preferred_max_height`

      query_params[["preferredGender"]] <- `preferred_gender`

      query_params[["preferredEducation"]] <- `preferred_education`

      query_params[["preferredEducationIndex"]] <- `preferred_education_index`

      query_params[["preferredBodyType"]] <- `preferred_body_type`

      query_params[["preferredEthnicity"]] <- `preferred_ethnicity`

      query_params[["preferredLocation"]] <- `preferred_location`

      query_params[["preferredLocationRange"]] <- `preferred_location_range`

      query_params[["platforms"]] <- `platforms`

      query_params[["tags"]] <- `tags`

      query_params[["aboutUs"]] <- `about_us`

      query_params[["matchToken"]] <- `match_token`

      query_params[["gameExperience"]] <- `game_experience`

      query_params[["categories"]] <- `categories`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["responseFilters"]] <- `response_filters`

      query_params[["showAsZipcode"]] <- `show_as_zipcode`

      query_params[["showExactLocation"]] <- `show_exact_location`

      query_params[["showOthersExactLocation"]] <- `show_others_exact_location`

      query_params[["acceptedTerms"]] <- `accepted_terms`

      query_params[["locationVisibility"]] <- `location_visibility`

      query_params[["appBlob"]] <- `app_blob`

      query_params[["appEnablePush"]] <- `app_enable_push`

      query_params[["appEnableSMS"]] <- `app_enable_sms`

      query_params[["appEnableEmail"]] <- `app_enable_email`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["returnProfile"]] <- `return_profile`

      query_params[["audienceIdsToAdd"]] <- `audience_ids_to_add`

      query_params[["audienceIdsToRemove"]] <- `audience_ids_to_remove`

      query_params[["referralAccountId"]] <- `referral_account_id`

      query_params[["appNickname"]] <- `app_nickname`

      query_params[["personalAudienceId"]] <- `personal_audience_id`

      query_params[["nonGuestUsername"]] <- `non_guest_username`

      local_var_url_path <- "/account/profile/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileInfoResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Username and Email
    #'
    #' @param device_id (optional) The device id
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param email_address (optional) the user's contact email address (NOT the username) which is also used for email validation
    #' @param username (optional) the user's username to update with if they currently have a guest username
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    EditUsername = function(device_id = NULL, account_id = NULL, email_address = NULL, username = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$EditUsernameWithHttpInfo(device_id, account_id, email_address, username, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Username and Email
    #'
    #' @param device_id (optional) The device id
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param email_address (optional) the user's contact email address (NOT the username) which is also used for email validation
    #' @param username (optional) the user's username to update with if they currently have a guest username
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    EditUsernameWithHttpInfo = function(device_id = NULL, account_id = NULL, email_address = NULL, username = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$EditUsername, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$EditUsername, `account_id` is not nullable")
      }

      if (!missing(`email_address`) && is.null(`email_address`)) {
        stop("Invalid value for `email_address` when calling AccountApi$EditUsername, `email_address` is not nullable")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling AccountApi$EditUsername, `username` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["emailAddress"]] <- `email_address`

      query_params[["username"]] <- `username`

      local_var_url_path <- "/account/username/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Account
    #'
    #' @param return_nulls (optional) Return Nulls (default value: FALSE)
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param connection_account_email (optional) Connection Account Email
    #' @param connection_account_id (optional) The account id used to view another person's account (default value: 0)
    #' @param response_filters (optional) A comma separated list of ProfileFilters for filtering the returned response data (default value: "PROFILE")
    #' @param game_type (optional) Game Type
    #' @param app_key (optional) The application key
    #' @param purchase_type (optional) Purchase Type (default value: "SIRQUL")
    #' @param update_viewed_date (optional) Determines whether to track if a person has viewed someone's profile (default value: FALSE)
    #' @param latitude (optional) Latitude used to update the user's current location
    #' @param longitude (optional) Longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    GetAccount = function(return_nulls = FALSE, device_id = NULL, account_id = NULL, connection_account_email = NULL, connection_account_id = 0, response_filters = "PROFILE", game_type = NULL, app_key = NULL, purchase_type = "SIRQUL", update_viewed_date = FALSE, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetAccountWithHttpInfo(return_nulls, device_id, account_id, connection_account_email, connection_account_id, response_filters, game_type, app_key, purchase_type, update_viewed_date, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Account
    #'
    #' @param return_nulls (optional) Return Nulls (default value: FALSE)
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param connection_account_email (optional) Connection Account Email
    #' @param connection_account_id (optional) The account id used to view another person's account (default value: 0)
    #' @param response_filters (optional) A comma separated list of ProfileFilters for filtering the returned response data (default value: "PROFILE")
    #' @param game_type (optional) Game Type
    #' @param app_key (optional) The application key
    #' @param purchase_type (optional) Purchase Type (default value: "SIRQUL")
    #' @param update_viewed_date (optional) Determines whether to track if a person has viewed someone's profile (default value: FALSE)
    #' @param latitude (optional) Latitude used to update the user's current location
    #' @param longitude (optional) Longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    GetAccountWithHttpInfo = function(return_nulls = FALSE, device_id = NULL, account_id = NULL, connection_account_email = NULL, connection_account_id = 0, response_filters = "PROFILE", game_type = NULL, app_key = NULL, purchase_type = "SIRQUL", update_viewed_date = FALSE, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`return_nulls`) && is.null(`return_nulls`)) {
        stop("Invalid value for `return_nulls` when calling AccountApi$GetAccount, `return_nulls` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$GetAccount, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$GetAccount, `account_id` is not nullable")
      }

      if (!missing(`connection_account_email`) && is.null(`connection_account_email`)) {
        stop("Invalid value for `connection_account_email` when calling AccountApi$GetAccount, `connection_account_email` is not nullable")
      }

      if (!missing(`connection_account_id`) && is.null(`connection_account_id`)) {
        stop("Invalid value for `connection_account_id` when calling AccountApi$GetAccount, `connection_account_id` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling AccountApi$GetAccount, `response_filters` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling AccountApi$GetAccount, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$GetAccount, `app_key` is not nullable")
      }

      if (!missing(`purchase_type`) && is.null(`purchase_type`)) {
        stop("Invalid value for `purchase_type` when calling AccountApi$GetAccount, `purchase_type` is not nullable")
      }

      if (!missing(`update_viewed_date`) && is.null(`update_viewed_date`)) {
        stop("Invalid value for `update_viewed_date` when calling AccountApi$GetAccount, `update_viewed_date` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$GetAccount, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$GetAccount, `longitude` is not nullable")
      }

      query_params[["returnNulls"]] <- `return_nulls`

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["connectionAccountEmail"]] <- `connection_account_email`

      query_params[["connectionAccountId"]] <- `connection_account_id`

      query_params[["responseFilters"]] <- `response_filters`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["purchaseType"]] <- `purchase_type`

      query_params[["updateViewedDate"]] <- `update_viewed_date`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/account/profile/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Profile Assets
    #'
    #' @param return_nulls (optional) Determines whether to return null fields in the response (default value: FALSE)
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param owner_id (optional) The account id of the person the user wants to view
    #' @param media_types (optional) Comma separated list of MediaType
    #' @param mime_types (optional) Comma separated list of mime types
    #' @param sort_field (optional) Determines what the returning list will be sorted by (see AssetApiMap) (default value: "CREATED")
    #' @param descending (optional) Determines whether to return the resulting list in descending or ascending order (default value: TRUE)
    #' @param latitude (optional) Latitude used to update the user's current location
    #' @param longitude (optional) Longitude used to update the user's current location
    #' @param _i (optional) _i
    #' @param start (optional) Start of the pagination (default value: 0)
    #' @param _l (optional) _l
    #' @param limit (optional) Limit of the pagination (default value: 0)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AssetListResponse
    GetProfileAssets = function(return_nulls = FALSE, device_id = NULL, account_id = NULL, owner_id = NULL, media_types = NULL, mime_types = NULL, sort_field = "CREATED", descending = TRUE, latitude = NULL, longitude = NULL, _i = NULL, start = 0, _l = NULL, limit = 0, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetProfileAssetsWithHttpInfo(return_nulls, device_id, account_id, owner_id, media_types, mime_types, sort_field, descending, latitude, longitude, _i, start, _l, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Profile Assets
    #'
    #' @param return_nulls (optional) Determines whether to return null fields in the response (default value: FALSE)
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param owner_id (optional) The account id of the person the user wants to view
    #' @param media_types (optional) Comma separated list of MediaType
    #' @param mime_types (optional) Comma separated list of mime types
    #' @param sort_field (optional) Determines what the returning list will be sorted by (see AssetApiMap) (default value: "CREATED")
    #' @param descending (optional) Determines whether to return the resulting list in descending or ascending order (default value: TRUE)
    #' @param latitude (optional) Latitude used to update the user's current location
    #' @param longitude (optional) Longitude used to update the user's current location
    #' @param _i (optional) _i
    #' @param start (optional) Start of the pagination (default value: 0)
    #' @param _l (optional) _l
    #' @param limit (optional) Limit of the pagination (default value: 0)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AssetListResponse) with additional information such as HTTP status code, headers
    GetProfileAssetsWithHttpInfo = function(return_nulls = FALSE, device_id = NULL, account_id = NULL, owner_id = NULL, media_types = NULL, mime_types = NULL, sort_field = "CREATED", descending = TRUE, latitude = NULL, longitude = NULL, _i = NULL, start = 0, _l = NULL, limit = 0, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`return_nulls`) && is.null(`return_nulls`)) {
        stop("Invalid value for `return_nulls` when calling AccountApi$GetProfileAssets, `return_nulls` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$GetProfileAssets, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$GetProfileAssets, `account_id` is not nullable")
      }

      if (!missing(`owner_id`) && is.null(`owner_id`)) {
        stop("Invalid value for `owner_id` when calling AccountApi$GetProfileAssets, `owner_id` is not nullable")
      }

      if (!missing(`media_types`) && is.null(`media_types`)) {
        stop("Invalid value for `media_types` when calling AccountApi$GetProfileAssets, `media_types` is not nullable")
      }

      if (!missing(`mime_types`) && is.null(`mime_types`)) {
        stop("Invalid value for `mime_types` when calling AccountApi$GetProfileAssets, `mime_types` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AccountApi$GetProfileAssets, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AccountApi$GetProfileAssets, `descending` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$GetProfileAssets, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$GetProfileAssets, `longitude` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling AccountApi$GetProfileAssets, `_i` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AccountApi$GetProfileAssets, `start` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling AccountApi$GetProfileAssets, `_l` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AccountApi$GetProfileAssets, `limit` is not nullable")
      }

      query_params[["returnNulls"]] <- `return_nulls`

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["ownerId"]] <- `owner_id`

      query_params[["mediaTypes"]] <- `media_types`

      query_params[["mimeTypes"]] <- `mime_types`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/account/profile/assets"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AssetListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Accounts
    #'
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param app_key (optional) The application key
    #' @param retrieve_type (optional) one of these option - GET_CHILDREN will get all accounts that had signed up using the current account invite link - GET_ANCESTOR will get all accounts that referred the current account and it's parents, recursively - GET_ALL will get all of the above
    #' @param level_limit (optional) level limit for children and ancestors of current account, starts from current account
    #' @param ancestor_level_limit (optional) level limit for ancestors, will override levelLimit if this is set
    #' @param children_level_limit (optional) level limit for children, will override levelLimit if this is set
    #' @param ancestor_list_start (optional) pagination start for children list
    #' @param ancestor_list_limit (optional) pagination limit for children list
    #' @param children_list_start (optional) pagination start for children list
    #' @param children_list_limit (optional) pagination limit for children list
    #' @param children_children (optional) if true, on each item in ancestor and children list, return the childrenTotalNumber and ancestorTotalNumber for that item (default value: TRUE)
    #' @param ... Other optional arguments
    #'
    #' @return void
    GetReferralList = function(account_id = NULL, app_key = NULL, retrieve_type = NULL, level_limit = NULL, ancestor_level_limit = NULL, children_level_limit = NULL, ancestor_list_start = NULL, ancestor_list_limit = NULL, children_list_start = NULL, children_list_limit = NULL, children_children = TRUE, ...) {
      local_var_response <- self$GetReferralListWithHttpInfo(account_id, app_key, retrieve_type, level_limit, ancestor_level_limit, children_level_limit, ancestor_list_start, ancestor_list_limit, children_list_start, children_list_limit, children_children, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Accounts
    #'
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param app_key (optional) The application key
    #' @param retrieve_type (optional) one of these option - GET_CHILDREN will get all accounts that had signed up using the current account invite link - GET_ANCESTOR will get all accounts that referred the current account and it's parents, recursively - GET_ALL will get all of the above
    #' @param level_limit (optional) level limit for children and ancestors of current account, starts from current account
    #' @param ancestor_level_limit (optional) level limit for ancestors, will override levelLimit if this is set
    #' @param children_level_limit (optional) level limit for children, will override levelLimit if this is set
    #' @param ancestor_list_start (optional) pagination start for children list
    #' @param ancestor_list_limit (optional) pagination limit for children list
    #' @param children_list_start (optional) pagination start for children list
    #' @param children_list_limit (optional) pagination limit for children list
    #' @param children_children (optional) if true, on each item in ancestor and children list, return the childrenTotalNumber and ancestorTotalNumber for that item (default value: TRUE)
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    GetReferralListWithHttpInfo = function(account_id = NULL, app_key = NULL, retrieve_type = NULL, level_limit = NULL, ancestor_level_limit = NULL, children_level_limit = NULL, ancestor_list_start = NULL, ancestor_list_limit = NULL, children_list_start = NULL, children_list_limit = NULL, children_children = TRUE, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$GetReferralList, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$GetReferralList, `app_key` is not nullable")
      }

      if (!missing(`retrieve_type`) && is.null(`retrieve_type`)) {
        stop("Invalid value for `retrieve_type` when calling AccountApi$GetReferralList, `retrieve_type` is not nullable")
      }

      if (!missing(`level_limit`) && is.null(`level_limit`)) {
        stop("Invalid value for `level_limit` when calling AccountApi$GetReferralList, `level_limit` is not nullable")
      }

      if (!missing(`ancestor_level_limit`) && is.null(`ancestor_level_limit`)) {
        stop("Invalid value for `ancestor_level_limit` when calling AccountApi$GetReferralList, `ancestor_level_limit` is not nullable")
      }

      if (!missing(`children_level_limit`) && is.null(`children_level_limit`)) {
        stop("Invalid value for `children_level_limit` when calling AccountApi$GetReferralList, `children_level_limit` is not nullable")
      }

      if (!missing(`ancestor_list_start`) && is.null(`ancestor_list_start`)) {
        stop("Invalid value for `ancestor_list_start` when calling AccountApi$GetReferralList, `ancestor_list_start` is not nullable")
      }

      if (!missing(`ancestor_list_limit`) && is.null(`ancestor_list_limit`)) {
        stop("Invalid value for `ancestor_list_limit` when calling AccountApi$GetReferralList, `ancestor_list_limit` is not nullable")
      }

      if (!missing(`children_list_start`) && is.null(`children_list_start`)) {
        stop("Invalid value for `children_list_start` when calling AccountApi$GetReferralList, `children_list_start` is not nullable")
      }

      if (!missing(`children_list_limit`) && is.null(`children_list_limit`)) {
        stop("Invalid value for `children_list_limit` when calling AccountApi$GetReferralList, `children_list_limit` is not nullable")
      }

      if (!missing(`children_children`) && is.null(`children_children`)) {
        stop("Invalid value for `children_children` when calling AccountApi$GetReferralList, `children_children` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["retrieveType"]] <- `retrieve_type`

      query_params[["levelLimit"]] <- `level_limit`

      query_params[["ancestorLevelLimit"]] <- `ancestor_level_limit`

      query_params[["childrenLevelLimit"]] <- `children_level_limit`

      query_params[["ancestorListStart"]] <- `ancestor_list_start`

      query_params[["ancestorListLimit"]] <- `ancestor_list_limit`

      query_params[["childrenListStart"]] <- `children_list_start`

      query_params[["childrenListLimit"]] <- `children_list_limit`

      query_params[["childrenChildren"]] <- `children_children`

      local_var_url_path <- "/account/referral/list"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Account Settings
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return UserSettingsResponse
    GetSettings = function(device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetSettingsWithHttpInfo(device_id, account_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Account Settings
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (UserSettingsResponse) with additional information such as HTTP status code, headers
    GetSettingsWithHttpInfo = function(device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$GetSettings, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$GetSettings, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$GetSettings, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$GetSettings, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/account/settings/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UserSettingsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Login as Account
    #'
    #' @param access_token 
    #' @param app_key 
    #' @param device_id (optional) No description
    #' @param access_token_secret (optional) No description
    #' @param delegated_account_id (optional) No description
    #' @param delegated_username (optional) No description
    #' @param network_uid (optional) The access provider to authenticate against (default: USERNAME). Supported values: FACEBOOK, TWITTER, USERNAME, PHONE (default value: "USERNAME")
    #' @param age_restriction (optional) Checks user's birthday to see if they meet an age requirement. If the user is under age, an error message is returned. (default value: 0)
    #' @param response_filters (optional) This determines how much of the profile should be returned, see ProfileFilters (default value: "PROFILE")
    #' @param latitude (optional) No description
    #' @param longitude (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    LoginDelegate = function(access_token, app_key, device_id = NULL, access_token_secret = NULL, delegated_account_id = NULL, delegated_username = NULL, network_uid = "USERNAME", age_restriction = 0, response_filters = "PROFILE", latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$LoginDelegateWithHttpInfo(access_token, app_key, device_id, access_token_secret, delegated_account_id, delegated_username, network_uid, age_restriction, response_filters, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Login as Account
    #'
    #' @param access_token 
    #' @param app_key 
    #' @param device_id (optional) No description
    #' @param access_token_secret (optional) No description
    #' @param delegated_account_id (optional) No description
    #' @param delegated_username (optional) No description
    #' @param network_uid (optional) The access provider to authenticate against (default: USERNAME). Supported values: FACEBOOK, TWITTER, USERNAME, PHONE (default value: "USERNAME")
    #' @param age_restriction (optional) Checks user's birthday to see if they meet an age requirement. If the user is under age, an error message is returned. (default value: 0)
    #' @param response_filters (optional) This determines how much of the profile should be returned, see ProfileFilters (default value: "PROFILE")
    #' @param latitude (optional) No description
    #' @param longitude (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    LoginDelegateWithHttpInfo = function(access_token, app_key, device_id = NULL, access_token_secret = NULL, delegated_account_id = NULL, delegated_username = NULL, network_uid = "USERNAME", age_restriction = 0, response_filters = "PROFILE", latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`access_token`)) {
        stop("Missing required parameter `access_token`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`access_token`) && is.null(`access_token`)) {
        stop("Invalid value for `access_token` when calling AccountApi$LoginDelegate, `access_token` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$LoginDelegate, `app_key` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$LoginDelegate, `device_id` is not nullable")
      }

      if (!missing(`access_token_secret`) && is.null(`access_token_secret`)) {
        stop("Invalid value for `access_token_secret` when calling AccountApi$LoginDelegate, `access_token_secret` is not nullable")
      }

      if (!missing(`delegated_account_id`) && is.null(`delegated_account_id`)) {
        stop("Invalid value for `delegated_account_id` when calling AccountApi$LoginDelegate, `delegated_account_id` is not nullable")
      }

      if (!missing(`delegated_username`) && is.null(`delegated_username`)) {
        stop("Invalid value for `delegated_username` when calling AccountApi$LoginDelegate, `delegated_username` is not nullable")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling AccountApi$LoginDelegate, `network_uid` is not nullable")
      }

      if (!missing(`age_restriction`) && is.null(`age_restriction`)) {
        stop("Invalid value for `age_restriction` when calling AccountApi$LoginDelegate, `age_restriction` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling AccountApi$LoginDelegate, `response_filters` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$LoginDelegate, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$LoginDelegate, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accessToken"]] <- `access_token`

      query_params[["accessTokenSecret"]] <- `access_token_secret`

      query_params[["delegatedAccountId"]] <- `delegated_account_id`

      query_params[["delegatedUsername"]] <- `delegated_username`

      query_params[["networkUID"]] <- `network_uid`

      query_params[["appKey"]] <- `app_key`

      query_params[["ageRestriction"]] <- `age_restriction`

      query_params[["responseFilters"]] <- `response_filters`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/account/login/delegate"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Login Account
    #'
    #' @param access_token The access token to authenticate with (ex: username or fb token)
    #' @param network_uid The access provider to authenticate against. This can be custom  networks created using the ThirdPartyApi as well. Supported values by default  include: FACEBOOK, TWITTER, USERNAME, PHONE 
    #' @param app_key The application key
    #' @param device_id (optional) The unique id of the device making the request
    #' @param device_id_type (optional) The type of device id (this is defined by the client), ie. MAC_ADDRESS, APPLE_UDID, etc
    #' @param access_token_secret (optional) The secret to authenticate with (ex: password)
    #' @param age_restriction (optional) Checks user's birthday to see if they meet an age requirement. If the user is under age, an error message is returned. (default value: 0)
    #' @param response_filters (optional) This determines how much of the profile should be returned, see ProfileFilters (default value: "PROFILE")
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param email_match (optional) Option to check for email if username doesn't match, also support multiple accounts (default value: FALSE)
    #' @param chosen_account_id (optional) Chosen account Id sent from the app - pass in the 2nd request to choose an account from multiple accounts matching the email - use one of the account id from the previous request (default value: 0)
    #' @param third_party_credential_id (optional) Third-party credential Id, pass in the 2nd request to choose an account from multiple accounts matching the email - use the id from the previous call ThirdPartyCredential object (default value: 0)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    LoginGeneral = function(access_token, network_uid, app_key, device_id = NULL, device_id_type = NULL, access_token_secret = NULL, age_restriction = 0, response_filters = "PROFILE", latitude = NULL, longitude = NULL, email_match = FALSE, chosen_account_id = 0, third_party_credential_id = 0, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$LoginGeneralWithHttpInfo(access_token, network_uid, app_key, device_id, device_id_type, access_token_secret, age_restriction, response_filters, latitude, longitude, email_match, chosen_account_id, third_party_credential_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Login Account
    #'
    #' @param access_token The access token to authenticate with (ex: username or fb token)
    #' @param network_uid The access provider to authenticate against. This can be custom  networks created using the ThirdPartyApi as well. Supported values by default  include: FACEBOOK, TWITTER, USERNAME, PHONE 
    #' @param app_key The application key
    #' @param device_id (optional) The unique id of the device making the request
    #' @param device_id_type (optional) The type of device id (this is defined by the client), ie. MAC_ADDRESS, APPLE_UDID, etc
    #' @param access_token_secret (optional) The secret to authenticate with (ex: password)
    #' @param age_restriction (optional) Checks user's birthday to see if they meet an age requirement. If the user is under age, an error message is returned. (default value: 0)
    #' @param response_filters (optional) This determines how much of the profile should be returned, see ProfileFilters (default value: "PROFILE")
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param email_match (optional) Option to check for email if username doesn't match, also support multiple accounts (default value: FALSE)
    #' @param chosen_account_id (optional) Chosen account Id sent from the app - pass in the 2nd request to choose an account from multiple accounts matching the email - use one of the account id from the previous request (default value: 0)
    #' @param third_party_credential_id (optional) Third-party credential Id, pass in the 2nd request to choose an account from multiple accounts matching the email - use the id from the previous call ThirdPartyCredential object (default value: 0)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    LoginGeneralWithHttpInfo = function(access_token, network_uid, app_key, device_id = NULL, device_id_type = NULL, access_token_secret = NULL, age_restriction = 0, response_filters = "PROFILE", latitude = NULL, longitude = NULL, email_match = FALSE, chosen_account_id = 0, third_party_credential_id = 0, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`access_token`)) {
        stop("Missing required parameter `access_token`.")
      }

      if (missing(`network_uid`)) {
        stop("Missing required parameter `network_uid`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`access_token`) && is.null(`access_token`)) {
        stop("Invalid value for `access_token` when calling AccountApi$LoginGeneral, `access_token` is not nullable")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling AccountApi$LoginGeneral, `network_uid` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$LoginGeneral, `app_key` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$LoginGeneral, `device_id` is not nullable")
      }

      if (!missing(`device_id_type`) && is.null(`device_id_type`)) {
        stop("Invalid value for `device_id_type` when calling AccountApi$LoginGeneral, `device_id_type` is not nullable")
      }

      if (!missing(`access_token_secret`) && is.null(`access_token_secret`)) {
        stop("Invalid value for `access_token_secret` when calling AccountApi$LoginGeneral, `access_token_secret` is not nullable")
      }

      if (!missing(`age_restriction`) && is.null(`age_restriction`)) {
        stop("Invalid value for `age_restriction` when calling AccountApi$LoginGeneral, `age_restriction` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling AccountApi$LoginGeneral, `response_filters` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$LoginGeneral, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$LoginGeneral, `longitude` is not nullable")
      }

      if (!missing(`email_match`) && is.null(`email_match`)) {
        stop("Invalid value for `email_match` when calling AccountApi$LoginGeneral, `email_match` is not nullable")
      }

      if (!missing(`chosen_account_id`) && is.null(`chosen_account_id`)) {
        stop("Invalid value for `chosen_account_id` when calling AccountApi$LoginGeneral, `chosen_account_id` is not nullable")
      }

      if (!missing(`third_party_credential_id`) && is.null(`third_party_credential_id`)) {
        stop("Invalid value for `third_party_credential_id` when calling AccountApi$LoginGeneral, `third_party_credential_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["deviceIdType"]] <- `device_id_type`

      query_params[["accessToken"]] <- `access_token`

      query_params[["accessTokenSecret"]] <- `access_token_secret`

      query_params[["networkUID"]] <- `network_uid`

      query_params[["appKey"]] <- `app_key`

      query_params[["ageRestriction"]] <- `age_restriction`

      query_params[["responseFilters"]] <- `response_filters`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["emailMatch"]] <- `email_match`

      query_params[["chosenAccountId"]] <- `chosen_account_id`

      query_params[["thirdPartyCredentialId"]] <- `third_party_credential_id`

      local_var_url_path <- "/account/login"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Login Account (Username)
    #'
    #' @param username the user's email address they used to sign-up
    #' @param password the password
    #' @param device_id (optional) the device id
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param app (optional) the app
    #' @param game_type (optional) This parameter is deprecated. This is deprecated, use appKey.
    #' @param app_key (optional) the application key
    #' @param return_profile (optional) the profile to return
    #' @param response_filters (optional) a comma separated list of ProfileFilters for filtering the returned response data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    LoginUsername = function(username, password, device_id = NULL, latitude = NULL, longitude = NULL, app = NULL, game_type = NULL, app_key = NULL, return_profile = NULL, response_filters = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$LoginUsernameWithHttpInfo(username, password, device_id, latitude, longitude, app, game_type, app_key, return_profile, response_filters, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Login Account (Username)
    #'
    #' @param username the user's email address they used to sign-up
    #' @param password the password
    #' @param device_id (optional) the device id
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param app (optional) the app
    #' @param game_type (optional) This parameter is deprecated. This is deprecated, use appKey.
    #' @param app_key (optional) the application key
    #' @param return_profile (optional) the profile to return
    #' @param response_filters (optional) a comma separated list of ProfileFilters for filtering the returned response data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    LoginUsernameWithHttpInfo = function(username, password, device_id = NULL, latitude = NULL, longitude = NULL, app = NULL, game_type = NULL, app_key = NULL, return_profile = NULL, response_filters = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`username`)) {
        stop("Missing required parameter `username`.")
      }

      if (missing(`password`)) {
        stop("Missing required parameter `password`.")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling AccountApi$LoginUsername, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling AccountApi$LoginUsername, `password` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$LoginUsername, `device_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$LoginUsername, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$LoginUsername, `longitude` is not nullable")
      }

      if (!missing(`app`) && is.null(`app`)) {
        stop("Invalid value for `app` when calling AccountApi$LoginUsername, `app` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling AccountApi$LoginUsername, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$LoginUsername, `app_key` is not nullable")
      }

      if (!missing(`return_profile`) && is.null(`return_profile`)) {
        stop("Invalid value for `return_profile` when calling AccountApi$LoginUsername, `return_profile` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling AccountApi$LoginUsername, `response_filters` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["app"]] <- `app`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["returnProfile"]] <- `return_profile`

      query_params[["responseFilters"]] <- `response_filters`

      local_var_url_path <- "/account/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Logout Account
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param device_id_type (optional) Device Id Type
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    Logout = function(device_id = NULL, device_id_type = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$LogoutWithHttpInfo(device_id, device_id_type, account_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Logout Account
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param device_id_type (optional) Device Id Type
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    LogoutWithHttpInfo = function(device_id = NULL, device_id_type = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$Logout, `device_id` is not nullable")
      }

      if (!missing(`device_id_type`) && is.null(`device_id_type`)) {
        stop("Invalid value for `device_id_type` when calling AccountApi$Logout, `device_id_type` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$Logout, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$Logout, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$Logout, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["deviceIdType"]] <- `device_id_type`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/account/logout"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Merge Account
    #'
    #' @param merge_account_id The id of the account to being merged
    #' @param app_key The application key
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    MergeAccount = function(merge_account_id, app_key, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$MergeAccountWithHttpInfo(merge_account_id, app_key, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Merge Account
    #'
    #' @param merge_account_id The id of the account to being merged
    #' @param app_key The application key
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    MergeAccountWithHttpInfo = function(merge_account_id, app_key, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`merge_account_id`)) {
        stop("Missing required parameter `merge_account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`merge_account_id`) && is.null(`merge_account_id`)) {
        stop("Invalid value for `merge_account_id` when calling AccountApi$MergeAccount, `merge_account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$MergeAccount, `app_key` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$MergeAccount, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$MergeAccount, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["mergeAccountId"]] <- `merge_account_id`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/account/merge"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Password
    #'
    #' @param account_id The account to update
    #' @param old_password The current password, used to validate access
    #' @param new_password The new password to set, cannot be empty
    #' @param confirm_password The new password to confirm, must match newPassword
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    PasswordChange = function(account_id, old_password, new_password, confirm_password, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$PasswordChangeWithHttpInfo(account_id, old_password, new_password, confirm_password, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Password
    #'
    #' @param account_id The account to update
    #' @param old_password The current password, used to validate access
    #' @param new_password The new password to set, cannot be empty
    #' @param confirm_password The new password to confirm, must match newPassword
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    PasswordChangeWithHttpInfo = function(account_id, old_password, new_password, confirm_password, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`old_password`)) {
        stop("Missing required parameter `old_password`.")
      }

      if (missing(`new_password`)) {
        stop("Missing required parameter `new_password`.")
      }

      if (missing(`confirm_password`)) {
        stop("Missing required parameter `confirm_password`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$PasswordChange, `account_id` is not nullable")
      }

      if (!missing(`old_password`) && is.null(`old_password`)) {
        stop("Invalid value for `old_password` when calling AccountApi$PasswordChange, `old_password` is not nullable")
      }

      if (!missing(`new_password`) && is.null(`new_password`)) {
        stop("Invalid value for `new_password` when calling AccountApi$PasswordChange, `new_password` is not nullable")
      }

      if (!missing(`confirm_password`) && is.null(`confirm_password`)) {
        stop("Invalid value for `confirm_password` when calling AccountApi$PasswordChange, `confirm_password` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["oldPassword"]] <- `old_password`

      query_params[["newPassword"]] <- `new_password`

      query_params[["confirmPassword"]] <- `confirm_password`

      local_var_url_path <- "/account/passwordchange"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Reset Password
    #'
    #' @param token The token associated with the account to update, good for 24 hours
    #' @param password The new password to set, cannot be empty
    #' @param confirm The new password to confirm, must match newPassword
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    PasswordReset = function(token, password, confirm, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$PasswordResetWithHttpInfo(token, password, confirm, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Reset Password
    #'
    #' @param token The token associated with the account to update, good for 24 hours
    #' @param password The new password to set, cannot be empty
    #' @param confirm The new password to confirm, must match newPassword
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    PasswordResetWithHttpInfo = function(token, password, confirm, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`token`)) {
        stop("Missing required parameter `token`.")
      }

      if (missing(`password`)) {
        stop("Missing required parameter `password`.")
      }

      if (missing(`confirm`)) {
        stop("Missing required parameter `confirm`.")
      }

      if (!missing(`token`) && is.null(`token`)) {
        stop("Invalid value for `token` when calling AccountApi$PasswordReset, `token` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling AccountApi$PasswordReset, `password` is not nullable")
      }

      if (!missing(`confirm`) && is.null(`confirm`)) {
        stop("Invalid value for `confirm` when calling AccountApi$PasswordReset, `confirm` is not nullable")
      }

      query_params[["token"]] <- `token`

      query_params[["password"]] <- `password`

      query_params[["confirm"]] <- `confirm`

      local_var_url_path <- "/account/passwordreset"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Request Password Reset
    #'
    #' @param email The email/username of the account
    #' @param from (optional) this is the sender email (default value: "Sirqul")
    #' @param domain (optional) this is the domain (like dev.sirqul.com) used to generate the password reset link
    #' @param sub_url (optional) this is the the subUrl (like resetpassword) used to generate a password reset link
    #' @param referer (optional) this is used to generate a password reset link (default value: "http://dev.sirqul.com/resetpassword")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    RequestPasswordReset = function(email, from = "Sirqul", domain = NULL, sub_url = NULL, referer = "http://dev.sirqul.com/resetpassword", data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RequestPasswordResetWithHttpInfo(email, from, domain, sub_url, referer, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Request Password Reset
    #'
    #' @param email The email/username of the account
    #' @param from (optional) this is the sender email (default value: "Sirqul")
    #' @param domain (optional) this is the domain (like dev.sirqul.com) used to generate the password reset link
    #' @param sub_url (optional) this is the the subUrl (like resetpassword) used to generate a password reset link
    #' @param referer (optional) this is used to generate a password reset link (default value: "http://dev.sirqul.com/resetpassword")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    RequestPasswordResetWithHttpInfo = function(email, from = "Sirqul", domain = NULL, sub_url = NULL, referer = "http://dev.sirqul.com/resetpassword", data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`email`)) {
        stop("Missing required parameter `email`.")
      }

      if (!missing(`email`) && is.null(`email`)) {
        stop("Invalid value for `email` when calling AccountApi$RequestPasswordReset, `email` is not nullable")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling AccountApi$RequestPasswordReset, `from` is not nullable")
      }

      if (!missing(`domain`) && is.null(`domain`)) {
        stop("Invalid value for `domain` when calling AccountApi$RequestPasswordReset, `domain` is not nullable")
      }

      if (!missing(`sub_url`) && is.null(`sub_url`)) {
        stop("Invalid value for `sub_url` when calling AccountApi$RequestPasswordReset, `sub_url` is not nullable")
      }

      if (!missing(`referer`) && is.null(`referer`)) {
        stop("Invalid value for `referer` when calling AccountApi$RequestPasswordReset, `referer` is not nullable")
      }

      query_params[["email"]] <- `email`

      query_params[["from"]] <- `from`

      query_params[["domain"]] <- `domain`

      query_params[["subUrl"]] <- `sub_url`

      query_params[["referer"]] <- `referer`

      local_var_url_path <- "/account/requestpasswordreset"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Send Validation Request
    #'
    #' @param account_id The account id of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    RequestValidateAccount = function(account_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RequestValidateAccountWithHttpInfo(account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Send Validation Request
    #'
    #' @param account_id The account id of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    RequestValidateAccountWithHttpInfo = function(account_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$RequestValidateAccount, `account_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      local_var_url_path <- "/account/requestValidateAccount"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Accounts
    #'
    #' @param account_id The id of the account requesting
    #' @param app_key The application key
    #' @param keyword (optional) The keyword for for querying the account
    #' @param latitude (optional) the latitude
    #' @param longitude (optional) the longitude
    #' @param radius (optional) the radius (default value: 5)
    #' @param gender (optional) the user's gender
    #' @param game_experience (optional) the user's Game Experience
    #' @param age (optional) the user's age
    #' @param category_ids (optional) the user's Category Ids
    #' @param return_nulls (optional) Return Nulls (default value: TRUE)
    #' @param response_filters (optional) A comma separated list of ProfileFilters for filtering the returned response data (default value: "PROFILE")
    #' @param purchase_type (optional) A comma separated list of PurchaseType (default value: "SIRQUL")
    #' @param sort_field (optional) The field to sort by (default value: "id")
    #' @param descending (optional) The order to return the results. Default is false, which will return the results in ascending order. (default value: FALSE)
    #' @param start (optional) The index into the record set to start with. (default value: 0)
    #' @param limit (optional) The total number of record to return. (default value: 20)
    #' @param active_only (optional) Determines whether to return only active results. Default is false. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ProfileResponse]
    SearchAccounts = function(account_id, app_key, keyword = NULL, latitude = NULL, longitude = NULL, radius = 5, gender = NULL, game_experience = NULL, age = NULL, category_ids = NULL, return_nulls = TRUE, response_filters = "PROFILE", purchase_type = "SIRQUL", sort_field = "id", descending = FALSE, start = 0, limit = 20, active_only = FALSE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchAccountsWithHttpInfo(account_id, app_key, keyword, latitude, longitude, radius, gender, game_experience, age, category_ids, return_nulls, response_filters, purchase_type, sort_field, descending, start, limit, active_only, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Accounts
    #'
    #' @param account_id The id of the account requesting
    #' @param app_key The application key
    #' @param keyword (optional) The keyword for for querying the account
    #' @param latitude (optional) the latitude
    #' @param longitude (optional) the longitude
    #' @param radius (optional) the radius (default value: 5)
    #' @param gender (optional) the user's gender
    #' @param game_experience (optional) the user's Game Experience
    #' @param age (optional) the user's age
    #' @param category_ids (optional) the user's Category Ids
    #' @param return_nulls (optional) Return Nulls (default value: TRUE)
    #' @param response_filters (optional) A comma separated list of ProfileFilters for filtering the returned response data (default value: "PROFILE")
    #' @param purchase_type (optional) A comma separated list of PurchaseType (default value: "SIRQUL")
    #' @param sort_field (optional) The field to sort by (default value: "id")
    #' @param descending (optional) The order to return the results. Default is false, which will return the results in ascending order. (default value: FALSE)
    #' @param start (optional) The index into the record set to start with. (default value: 0)
    #' @param limit (optional) The total number of record to return. (default value: 20)
    #' @param active_only (optional) Determines whether to return only active results. Default is false. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ProfileResponse]) with additional information such as HTTP status code, headers
    SearchAccountsWithHttpInfo = function(account_id, app_key, keyword = NULL, latitude = NULL, longitude = NULL, radius = 5, gender = NULL, game_experience = NULL, age = NULL, category_ids = NULL, return_nulls = TRUE, response_filters = "PROFILE", purchase_type = "SIRQUL", sort_field = "id", descending = FALSE, start = 0, limit = 20, active_only = FALSE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$SearchAccounts, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$SearchAccounts, `app_key` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling AccountApi$SearchAccounts, `keyword` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$SearchAccounts, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$SearchAccounts, `longitude` is not nullable")
      }

      if (!missing(`radius`) && is.null(`radius`)) {
        stop("Invalid value for `radius` when calling AccountApi$SearchAccounts, `radius` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling AccountApi$SearchAccounts, `gender` is not nullable")
      }

      if (!missing(`game_experience`) && is.null(`game_experience`)) {
        stop("Invalid value for `game_experience` when calling AccountApi$SearchAccounts, `game_experience` is not nullable")
      }

      if (!missing(`age`) && is.null(`age`)) {
        stop("Invalid value for `age` when calling AccountApi$SearchAccounts, `age` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling AccountApi$SearchAccounts, `category_ids` is not nullable")
      }

      if (!missing(`return_nulls`) && is.null(`return_nulls`)) {
        stop("Invalid value for `return_nulls` when calling AccountApi$SearchAccounts, `return_nulls` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling AccountApi$SearchAccounts, `response_filters` is not nullable")
      }

      if (!missing(`purchase_type`) && is.null(`purchase_type`)) {
        stop("Invalid value for `purchase_type` when calling AccountApi$SearchAccounts, `purchase_type` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AccountApi$SearchAccounts, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AccountApi$SearchAccounts, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AccountApi$SearchAccounts, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AccountApi$SearchAccounts, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling AccountApi$SearchAccounts, `active_only` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["keyword"]] <- `keyword`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["radius"]] <- `radius`

      if (!is.null(`gender`) && !(`gender` %in% c("MALE", "FEMALE", "ANY"))) {
        stop("Invalid value for gender when calling AccountApi$SearchAccounts. Must be [MALE, FEMALE, ANY].")
      }
      query_params[["gender"]] <- `gender`

      if (!is.null(`game_experience`) && !(`game_experience` %in% c("ANY", "NEW", "BEGINNER", "INTERMEDIATE", "EXPERT"))) {
        stop("Invalid value for game_experience when calling AccountApi$SearchAccounts. Must be [ANY, NEW, BEGINNER, INTERMEDIATE, EXPERT].")
      }
      query_params[["gameExperience"]] <- `game_experience`

      query_params[["age"]] <- `age`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["returnNulls"]] <- `return_nulls`

      query_params[["responseFilters"]] <- `response_filters`

      query_params[["purchaseType"]] <- `purchase_type`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      local_var_url_path <- "/account/profile/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ProfileResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Login Account (Encrypted Username)
    #'
    #' @param username The user's encrypted email address they used to sign-up
    #' @param password The encrypted password
    #' @param game_type The application key
    #' @param device_id (optional) The device id
    #' @param charset_name (optional) Charset Name (default value: "UTF-8")
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param return_profile (optional) Return Profile (default value: FALSE)
    #' @param response_filters (optional) A comma separated list of ProfileFilters for filtering the returned response data (default value: "PROFILE")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    SecureLogin = function(username, password, game_type, device_id = NULL, charset_name = "UTF-8", latitude = NULL, longitude = NULL, return_profile = FALSE, response_filters = "PROFILE", data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SecureLoginWithHttpInfo(username, password, game_type, device_id, charset_name, latitude, longitude, return_profile, response_filters, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Login Account (Encrypted Username)
    #'
    #' @param username The user's encrypted email address they used to sign-up
    #' @param password The encrypted password
    #' @param game_type The application key
    #' @param device_id (optional) The device id
    #' @param charset_name (optional) Charset Name (default value: "UTF-8")
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param return_profile (optional) Return Profile (default value: FALSE)
    #' @param response_filters (optional) A comma separated list of ProfileFilters for filtering the returned response data (default value: "PROFILE")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    SecureLoginWithHttpInfo = function(username, password, game_type, device_id = NULL, charset_name = "UTF-8", latitude = NULL, longitude = NULL, return_profile = FALSE, response_filters = "PROFILE", data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`username`)) {
        stop("Missing required parameter `username`.")
      }

      if (missing(`password`)) {
        stop("Missing required parameter `password`.")
      }

      if (missing(`game_type`)) {
        stop("Missing required parameter `game_type`.")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling AccountApi$SecureLogin, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling AccountApi$SecureLogin, `password` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling AccountApi$SecureLogin, `game_type` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$SecureLogin, `device_id` is not nullable")
      }

      if (!missing(`charset_name`) && is.null(`charset_name`)) {
        stop("Invalid value for `charset_name` when calling AccountApi$SecureLogin, `charset_name` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$SecureLogin, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$SecureLogin, `longitude` is not nullable")
      }

      if (!missing(`return_profile`) && is.null(`return_profile`)) {
        stop("Invalid value for `return_profile` when calling AccountApi$SecureLogin, `return_profile` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling AccountApi$SecureLogin, `response_filters` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      query_params[["gameType"]] <- `game_type`

      query_params[["charsetName"]] <- `charset_name`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["returnProfile"]] <- `return_profile`

      query_params[["responseFilters"]] <- `response_filters`

      local_var_url_path <- "/account/login/validate"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Account (Encrypted Username)
    #'
    #' @param device_id The device id
    #' @param username The encrypted email of the user, this is what will be used when they login
    #' @param password The encrypted password of the user
    #' @param name (optional) The full name of the user. If this parameter is not empty, the  following parameters will be ignored: prefixName, firstName, middleName,  lastName, and suffixName 
    #' @param invite_token (optional) the inviteToken that the referrer use for this account to sign up
    #' @param prefix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the prefix of the user's name
    #' @param first_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's first name
    #' @param middle_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's middle name
    #' @param last_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's last name
    #' @param suffix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the suffix of the user's name
    #' @param title (optional) Title
    #' @param device_id_type (optional) Device Id Type
    #' @param email_address (optional) The user's contact email address (NOT the username) which is also used for email validation
    #' @param asset_id (optional) The asset id to set the user's profile image
    #' @param address (optional) the user's address
    #' @param zipcode (optional) The street zipcode of the user's contact location
    #' @param gender (optional) The gender of the user AudienceGender
    #' @param birthday (optional) The birthday date of the user in milliseconds
    #' @param home_phone (optional) the user's home phone number
    #' @param cell_phone (optional) the user's cell phone number
    #' @param cell_phone_carrier (optional) the user's Cell Phone Carrier
    #' @param business_phone (optional) the user's Business Phone Number
    #' @param role (optional) The type of account being created {RETAILER, MEMBER, DEVELOPER, GUEST (default value: "MEMBER")
    #' @param platforms (optional) Comma separated list of development platforms: MAC, WINDOWS, IOS, ANDROID, WINDOWSPHONE, KINDLE, UNITY3D, COCOS2D, HTML5, FACEBOOK
    #' @param tags (optional) Search tags
    #' @param about_us (optional) About Us information
    #' @param game_experience (optional) Game experience level of the user {ANY, NEW, BEGINNER, INTERMEDIATE, EXPERT
    #' @param category_ids (optional) A list of category ids that represent interests and associations
    #' @param hometown (optional) The user's hometown
    #' @param height (optional) The user's height
    #' @param height_index (optional) The user's height in a numerical value that can be used for ordering/searching
    #' @param ethnicity (optional) The user's ethnicity
    #' @param body_type (optional) The user's body type
    #' @param marital_status (optional) The user's maritial status
    #' @param children (optional) The user's children status
    #' @param religion (optional) The user's religion
    #' @param education (optional) The user's education
    #' @param education_index (optional) The user's education in a numerical value that can be used for ordering/searching
    #' @param smoke (optional) The user's smoke status
    #' @param drink (optional) The user's drink status
    #' @param companionship (optional) The user's companionship status
    #' @param companionship_index (optional) The user's companionship index
    #' @param preferred_min_age (optional) The preferred minimum age in the account location search
    #' @param preferred_max_age (optional) The preferred maximum age in the account location search
    #' @param preferred_min_height (optional) The preferred minimum height in the account location search
    #' @param preferred_max_height (optional) The preferred maximum height in the account location search
    #' @param preferred_gender (optional) The preferred gender in the account location search
    #' @param preferred_education (optional) The preferred education in the account location search
    #' @param preferred_education_index (optional) The preferred education in a numerical value that can be used for ordering/searching
    #' @param preferred_body_type (optional) The preferred body type in the account location search
    #' @param preferred_ethnicity (optional) The preferred ethnicity in the account location search
    #' @param preferred_location (optional) The preferred education in the account location search
    #' @param preferred_location_range (optional) The preferred location range in the account location search
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param accepted_terms (optional) Accepted Terms (default value: TRUE)
    #' @param charset_name (optional) Charset Name (default value: "UTF-8")
    #' @param game_type (optional) Game Type
    #' @param app_key (optional) The application key
    #' @param app_version (optional) App Version
    #' @param response_type (optional) Response Type
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileInfoResponse
    SecureSignup = function(device_id, username, password, name = NULL, invite_token = NULL, prefix_name = NULL, first_name = NULL, middle_name = NULL, last_name = NULL, suffix_name = NULL, title = NULL, device_id_type = NULL, email_address = NULL, asset_id = NULL, address = NULL, zipcode = NULL, gender = NULL, birthday = NULL, home_phone = NULL, cell_phone = NULL, cell_phone_carrier = NULL, business_phone = NULL, role = "MEMBER", platforms = NULL, tags = NULL, about_us = NULL, game_experience = NULL, category_ids = NULL, hometown = NULL, height = NULL, height_index = NULL, ethnicity = NULL, body_type = NULL, marital_status = NULL, children = NULL, religion = NULL, education = NULL, education_index = NULL, smoke = NULL, drink = NULL, companionship = NULL, companionship_index = NULL, preferred_min_age = NULL, preferred_max_age = NULL, preferred_min_height = NULL, preferred_max_height = NULL, preferred_gender = NULL, preferred_education = NULL, preferred_education_index = NULL, preferred_body_type = NULL, preferred_ethnicity = NULL, preferred_location = NULL, preferred_location_range = NULL, latitude = NULL, longitude = NULL, accepted_terms = TRUE, charset_name = "UTF-8", game_type = NULL, app_key = NULL, app_version = NULL, response_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SecureSignupWithHttpInfo(device_id, username, password, name, invite_token, prefix_name, first_name, middle_name, last_name, suffix_name, title, device_id_type, email_address, asset_id, address, zipcode, gender, birthday, home_phone, cell_phone, cell_phone_carrier, business_phone, role, platforms, tags, about_us, game_experience, category_ids, hometown, height, height_index, ethnicity, body_type, marital_status, children, religion, education, education_index, smoke, drink, companionship, companionship_index, preferred_min_age, preferred_max_age, preferred_min_height, preferred_max_height, preferred_gender, preferred_education, preferred_education_index, preferred_body_type, preferred_ethnicity, preferred_location, preferred_location_range, latitude, longitude, accepted_terms, charset_name, game_type, app_key, app_version, response_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Account (Encrypted Username)
    #'
    #' @param device_id The device id
    #' @param username The encrypted email of the user, this is what will be used when they login
    #' @param password The encrypted password of the user
    #' @param name (optional) The full name of the user. If this parameter is not empty, the  following parameters will be ignored: prefixName, firstName, middleName,  lastName, and suffixName 
    #' @param invite_token (optional) the inviteToken that the referrer use for this account to sign up
    #' @param prefix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the prefix of the user's name
    #' @param first_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's first name
    #' @param middle_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's middle name
    #' @param last_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the user's last name
    #' @param suffix_name (optional) If the parameter 'name' is empty or not present, this field will be used to set the suffix of the user's name
    #' @param title (optional) Title
    #' @param device_id_type (optional) Device Id Type
    #' @param email_address (optional) The user's contact email address (NOT the username) which is also used for email validation
    #' @param asset_id (optional) The asset id to set the user's profile image
    #' @param address (optional) the user's address
    #' @param zipcode (optional) The street zipcode of the user's contact location
    #' @param gender (optional) The gender of the user AudienceGender
    #' @param birthday (optional) The birthday date of the user in milliseconds
    #' @param home_phone (optional) the user's home phone number
    #' @param cell_phone (optional) the user's cell phone number
    #' @param cell_phone_carrier (optional) the user's Cell Phone Carrier
    #' @param business_phone (optional) the user's Business Phone Number
    #' @param role (optional) The type of account being created {RETAILER, MEMBER, DEVELOPER, GUEST (default value: "MEMBER")
    #' @param platforms (optional) Comma separated list of development platforms: MAC, WINDOWS, IOS, ANDROID, WINDOWSPHONE, KINDLE, UNITY3D, COCOS2D, HTML5, FACEBOOK
    #' @param tags (optional) Search tags
    #' @param about_us (optional) About Us information
    #' @param game_experience (optional) Game experience level of the user {ANY, NEW, BEGINNER, INTERMEDIATE, EXPERT
    #' @param category_ids (optional) A list of category ids that represent interests and associations
    #' @param hometown (optional) The user's hometown
    #' @param height (optional) The user's height
    #' @param height_index (optional) The user's height in a numerical value that can be used for ordering/searching
    #' @param ethnicity (optional) The user's ethnicity
    #' @param body_type (optional) The user's body type
    #' @param marital_status (optional) The user's maritial status
    #' @param children (optional) The user's children status
    #' @param religion (optional) The user's religion
    #' @param education (optional) The user's education
    #' @param education_index (optional) The user's education in a numerical value that can be used for ordering/searching
    #' @param smoke (optional) The user's smoke status
    #' @param drink (optional) The user's drink status
    #' @param companionship (optional) The user's companionship status
    #' @param companionship_index (optional) The user's companionship index
    #' @param preferred_min_age (optional) The preferred minimum age in the account location search
    #' @param preferred_max_age (optional) The preferred maximum age in the account location search
    #' @param preferred_min_height (optional) The preferred minimum height in the account location search
    #' @param preferred_max_height (optional) The preferred maximum height in the account location search
    #' @param preferred_gender (optional) The preferred gender in the account location search
    #' @param preferred_education (optional) The preferred education in the account location search
    #' @param preferred_education_index (optional) The preferred education in a numerical value that can be used for ordering/searching
    #' @param preferred_body_type (optional) The preferred body type in the account location search
    #' @param preferred_ethnicity (optional) The preferred ethnicity in the account location search
    #' @param preferred_location (optional) The preferred education in the account location search
    #' @param preferred_location_range (optional) The preferred location range in the account location search
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param accepted_terms (optional) Accepted Terms (default value: TRUE)
    #' @param charset_name (optional) Charset Name (default value: "UTF-8")
    #' @param game_type (optional) Game Type
    #' @param app_key (optional) The application key
    #' @param app_version (optional) App Version
    #' @param response_type (optional) Response Type
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileInfoResponse) with additional information such as HTTP status code, headers
    SecureSignupWithHttpInfo = function(device_id, username, password, name = NULL, invite_token = NULL, prefix_name = NULL, first_name = NULL, middle_name = NULL, last_name = NULL, suffix_name = NULL, title = NULL, device_id_type = NULL, email_address = NULL, asset_id = NULL, address = NULL, zipcode = NULL, gender = NULL, birthday = NULL, home_phone = NULL, cell_phone = NULL, cell_phone_carrier = NULL, business_phone = NULL, role = "MEMBER", platforms = NULL, tags = NULL, about_us = NULL, game_experience = NULL, category_ids = NULL, hometown = NULL, height = NULL, height_index = NULL, ethnicity = NULL, body_type = NULL, marital_status = NULL, children = NULL, religion = NULL, education = NULL, education_index = NULL, smoke = NULL, drink = NULL, companionship = NULL, companionship_index = NULL, preferred_min_age = NULL, preferred_max_age = NULL, preferred_min_height = NULL, preferred_max_height = NULL, preferred_gender = NULL, preferred_education = NULL, preferred_education_index = NULL, preferred_body_type = NULL, preferred_ethnicity = NULL, preferred_location = NULL, preferred_location_range = NULL, latitude = NULL, longitude = NULL, accepted_terms = TRUE, charset_name = "UTF-8", game_type = NULL, app_key = NULL, app_version = NULL, response_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`device_id`)) {
        stop("Missing required parameter `device_id`.")
      }

      if (missing(`username`)) {
        stop("Missing required parameter `username`.")
      }

      if (missing(`password`)) {
        stop("Missing required parameter `password`.")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$SecureSignup, `device_id` is not nullable")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling AccountApi$SecureSignup, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling AccountApi$SecureSignup, `password` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling AccountApi$SecureSignup, `name` is not nullable")
      }

      if (!missing(`invite_token`) && is.null(`invite_token`)) {
        stop("Invalid value for `invite_token` when calling AccountApi$SecureSignup, `invite_token` is not nullable")
      }

      if (!missing(`prefix_name`) && is.null(`prefix_name`)) {
        stop("Invalid value for `prefix_name` when calling AccountApi$SecureSignup, `prefix_name` is not nullable")
      }

      if (!missing(`first_name`) && is.null(`first_name`)) {
        stop("Invalid value for `first_name` when calling AccountApi$SecureSignup, `first_name` is not nullable")
      }

      if (!missing(`middle_name`) && is.null(`middle_name`)) {
        stop("Invalid value for `middle_name` when calling AccountApi$SecureSignup, `middle_name` is not nullable")
      }

      if (!missing(`last_name`) && is.null(`last_name`)) {
        stop("Invalid value for `last_name` when calling AccountApi$SecureSignup, `last_name` is not nullable")
      }

      if (!missing(`suffix_name`) && is.null(`suffix_name`)) {
        stop("Invalid value for `suffix_name` when calling AccountApi$SecureSignup, `suffix_name` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling AccountApi$SecureSignup, `title` is not nullable")
      }

      if (!missing(`device_id_type`) && is.null(`device_id_type`)) {
        stop("Invalid value for `device_id_type` when calling AccountApi$SecureSignup, `device_id_type` is not nullable")
      }

      if (!missing(`email_address`) && is.null(`email_address`)) {
        stop("Invalid value for `email_address` when calling AccountApi$SecureSignup, `email_address` is not nullable")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling AccountApi$SecureSignup, `asset_id` is not nullable")
      }

      if (!missing(`address`) && is.null(`address`)) {
        stop("Invalid value for `address` when calling AccountApi$SecureSignup, `address` is not nullable")
      }

      if (!missing(`zipcode`) && is.null(`zipcode`)) {
        stop("Invalid value for `zipcode` when calling AccountApi$SecureSignup, `zipcode` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling AccountApi$SecureSignup, `gender` is not nullable")
      }

      if (!missing(`birthday`) && is.null(`birthday`)) {
        stop("Invalid value for `birthday` when calling AccountApi$SecureSignup, `birthday` is not nullable")
      }

      if (!missing(`home_phone`) && is.null(`home_phone`)) {
        stop("Invalid value for `home_phone` when calling AccountApi$SecureSignup, `home_phone` is not nullable")
      }

      if (!missing(`cell_phone`) && is.null(`cell_phone`)) {
        stop("Invalid value for `cell_phone` when calling AccountApi$SecureSignup, `cell_phone` is not nullable")
      }

      if (!missing(`cell_phone_carrier`) && is.null(`cell_phone_carrier`)) {
        stop("Invalid value for `cell_phone_carrier` when calling AccountApi$SecureSignup, `cell_phone_carrier` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling AccountApi$SecureSignup, `business_phone` is not nullable")
      }

      if (!missing(`role`) && is.null(`role`)) {
        stop("Invalid value for `role` when calling AccountApi$SecureSignup, `role` is not nullable")
      }

      if (!missing(`platforms`) && is.null(`platforms`)) {
        stop("Invalid value for `platforms` when calling AccountApi$SecureSignup, `platforms` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling AccountApi$SecureSignup, `tags` is not nullable")
      }

      if (!missing(`about_us`) && is.null(`about_us`)) {
        stop("Invalid value for `about_us` when calling AccountApi$SecureSignup, `about_us` is not nullable")
      }

      if (!missing(`game_experience`) && is.null(`game_experience`)) {
        stop("Invalid value for `game_experience` when calling AccountApi$SecureSignup, `game_experience` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling AccountApi$SecureSignup, `category_ids` is not nullable")
      }

      if (!missing(`hometown`) && is.null(`hometown`)) {
        stop("Invalid value for `hometown` when calling AccountApi$SecureSignup, `hometown` is not nullable")
      }

      if (!missing(`height`) && is.null(`height`)) {
        stop("Invalid value for `height` when calling AccountApi$SecureSignup, `height` is not nullable")
      }

      if (!missing(`height_index`) && is.null(`height_index`)) {
        stop("Invalid value for `height_index` when calling AccountApi$SecureSignup, `height_index` is not nullable")
      }

      if (!missing(`ethnicity`) && is.null(`ethnicity`)) {
        stop("Invalid value for `ethnicity` when calling AccountApi$SecureSignup, `ethnicity` is not nullable")
      }

      if (!missing(`body_type`) && is.null(`body_type`)) {
        stop("Invalid value for `body_type` when calling AccountApi$SecureSignup, `body_type` is not nullable")
      }

      if (!missing(`marital_status`) && is.null(`marital_status`)) {
        stop("Invalid value for `marital_status` when calling AccountApi$SecureSignup, `marital_status` is not nullable")
      }

      if (!missing(`children`) && is.null(`children`)) {
        stop("Invalid value for `children` when calling AccountApi$SecureSignup, `children` is not nullable")
      }

      if (!missing(`religion`) && is.null(`religion`)) {
        stop("Invalid value for `religion` when calling AccountApi$SecureSignup, `religion` is not nullable")
      }

      if (!missing(`education`) && is.null(`education`)) {
        stop("Invalid value for `education` when calling AccountApi$SecureSignup, `education` is not nullable")
      }

      if (!missing(`education_index`) && is.null(`education_index`)) {
        stop("Invalid value for `education_index` when calling AccountApi$SecureSignup, `education_index` is not nullable")
      }

      if (!missing(`smoke`) && is.null(`smoke`)) {
        stop("Invalid value for `smoke` when calling AccountApi$SecureSignup, `smoke` is not nullable")
      }

      if (!missing(`drink`) && is.null(`drink`)) {
        stop("Invalid value for `drink` when calling AccountApi$SecureSignup, `drink` is not nullable")
      }

      if (!missing(`companionship`) && is.null(`companionship`)) {
        stop("Invalid value for `companionship` when calling AccountApi$SecureSignup, `companionship` is not nullable")
      }

      if (!missing(`companionship_index`) && is.null(`companionship_index`)) {
        stop("Invalid value for `companionship_index` when calling AccountApi$SecureSignup, `companionship_index` is not nullable")
      }

      if (!missing(`preferred_min_age`) && is.null(`preferred_min_age`)) {
        stop("Invalid value for `preferred_min_age` when calling AccountApi$SecureSignup, `preferred_min_age` is not nullable")
      }

      if (!missing(`preferred_max_age`) && is.null(`preferred_max_age`)) {
        stop("Invalid value for `preferred_max_age` when calling AccountApi$SecureSignup, `preferred_max_age` is not nullable")
      }

      if (!missing(`preferred_min_height`) && is.null(`preferred_min_height`)) {
        stop("Invalid value for `preferred_min_height` when calling AccountApi$SecureSignup, `preferred_min_height` is not nullable")
      }

      if (!missing(`preferred_max_height`) && is.null(`preferred_max_height`)) {
        stop("Invalid value for `preferred_max_height` when calling AccountApi$SecureSignup, `preferred_max_height` is not nullable")
      }

      if (!missing(`preferred_gender`) && is.null(`preferred_gender`)) {
        stop("Invalid value for `preferred_gender` when calling AccountApi$SecureSignup, `preferred_gender` is not nullable")
      }

      if (!missing(`preferred_education`) && is.null(`preferred_education`)) {
        stop("Invalid value for `preferred_education` when calling AccountApi$SecureSignup, `preferred_education` is not nullable")
      }

      if (!missing(`preferred_education_index`) && is.null(`preferred_education_index`)) {
        stop("Invalid value for `preferred_education_index` when calling AccountApi$SecureSignup, `preferred_education_index` is not nullable")
      }

      if (!missing(`preferred_body_type`) && is.null(`preferred_body_type`)) {
        stop("Invalid value for `preferred_body_type` when calling AccountApi$SecureSignup, `preferred_body_type` is not nullable")
      }

      if (!missing(`preferred_ethnicity`) && is.null(`preferred_ethnicity`)) {
        stop("Invalid value for `preferred_ethnicity` when calling AccountApi$SecureSignup, `preferred_ethnicity` is not nullable")
      }

      if (!missing(`preferred_location`) && is.null(`preferred_location`)) {
        stop("Invalid value for `preferred_location` when calling AccountApi$SecureSignup, `preferred_location` is not nullable")
      }

      if (!missing(`preferred_location_range`) && is.null(`preferred_location_range`)) {
        stop("Invalid value for `preferred_location_range` when calling AccountApi$SecureSignup, `preferred_location_range` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$SecureSignup, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$SecureSignup, `longitude` is not nullable")
      }

      if (!missing(`accepted_terms`) && is.null(`accepted_terms`)) {
        stop("Invalid value for `accepted_terms` when calling AccountApi$SecureSignup, `accepted_terms` is not nullable")
      }

      if (!missing(`charset_name`) && is.null(`charset_name`)) {
        stop("Invalid value for `charset_name` when calling AccountApi$SecureSignup, `charset_name` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling AccountApi$SecureSignup, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$SecureSignup, `app_key` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling AccountApi$SecureSignup, `app_version` is not nullable")
      }

      if (!missing(`response_type`) && is.null(`response_type`)) {
        stop("Invalid value for `response_type` when calling AccountApi$SecureSignup, `response_type` is not nullable")
      }

      query_params[["name"]] <- `name`

      query_params[["inviteToken"]] <- `invite_token`

      query_params[["prefixName"]] <- `prefix_name`

      query_params[["firstName"]] <- `first_name`

      query_params[["middleName"]] <- `middle_name`

      query_params[["lastName"]] <- `last_name`

      query_params[["suffixName"]] <- `suffix_name`

      query_params[["title"]] <- `title`

      query_params[["deviceId"]] <- `device_id`

      query_params[["deviceIdType"]] <- `device_id_type`

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      query_params[["emailAddress"]] <- `email_address`

      query_params[["assetId"]] <- `asset_id`

      query_params[["address"]] <- `address`

      query_params[["zipcode"]] <- `zipcode`

      query_params[["gender"]] <- `gender`

      query_params[["birthday"]] <- `birthday`

      query_params[["homePhone"]] <- `home_phone`

      query_params[["cellPhone"]] <- `cell_phone`

      query_params[["cellPhoneCarrier"]] <- `cell_phone_carrier`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["role"]] <- `role`

      query_params[["platforms"]] <- `platforms`

      query_params[["tags"]] <- `tags`

      query_params[["aboutUs"]] <- `about_us`

      query_params[["gameExperience"]] <- `game_experience`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["hometown"]] <- `hometown`

      query_params[["height"]] <- `height`

      query_params[["heightIndex"]] <- `height_index`

      query_params[["ethnicity"]] <- `ethnicity`

      query_params[["bodyType"]] <- `body_type`

      query_params[["maritalStatus"]] <- `marital_status`

      query_params[["children"]] <- `children`

      query_params[["religion"]] <- `religion`

      query_params[["education"]] <- `education`

      query_params[["educationIndex"]] <- `education_index`

      query_params[["smoke"]] <- `smoke`

      query_params[["drink"]] <- `drink`

      query_params[["companionship"]] <- `companionship`

      query_params[["companionshipIndex"]] <- `companionship_index`

      query_params[["preferredMinAge"]] <- `preferred_min_age`

      query_params[["preferredMaxAge"]] <- `preferred_max_age`

      query_params[["preferredMinHeight"]] <- `preferred_min_height`

      query_params[["preferredMaxHeight"]] <- `preferred_max_height`

      query_params[["preferredGender"]] <- `preferred_gender`

      query_params[["preferredEducation"]] <- `preferred_education`

      query_params[["preferredEducationIndex"]] <- `preferred_education_index`

      query_params[["preferredBodyType"]] <- `preferred_body_type`

      query_params[["preferredEthnicity"]] <- `preferred_ethnicity`

      query_params[["preferredLocation"]] <- `preferred_location`

      query_params[["preferredLocationRange"]] <- `preferred_location_range`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["acceptedTerms"]] <- `accepted_terms`

      query_params[["charsetName"]] <- `charset_name`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["appVersion"]] <- `app_version`

      query_params[["responseType"]] <- `response_type`

      local_var_url_path <- "/account/create/validate"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileInfoResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Save Match Token
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param match_token (optional) A string of numbers
    #' @param game_type (optional) Game Type (deprecated) (default value: "BOOPY")
    #' @param app_key (optional) The application key
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    SetMatchToken = function(device_id = NULL, account_id = NULL, match_token = NULL, game_type = "BOOPY", app_key = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SetMatchTokenWithHttpInfo(device_id, account_id, match_token, game_type, app_key, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Save Match Token
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param match_token (optional) A string of numbers
    #' @param game_type (optional) Game Type (deprecated) (default value: "BOOPY")
    #' @param app_key (optional) The application key
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    SetMatchTokenWithHttpInfo = function(device_id = NULL, account_id = NULL, match_token = NULL, game_type = "BOOPY", app_key = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$SetMatchToken, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$SetMatchToken, `account_id` is not nullable")
      }

      if (!missing(`match_token`) && is.null(`match_token`)) {
        stop("Invalid value for `match_token` when calling AccountApi$SetMatchToken, `match_token` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling AccountApi$SetMatchToken, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$SetMatchToken, `app_key` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$SetMatchToken, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$SetMatchToken, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["matchToken"]] <- `match_token`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/consumer/profile/matchToken"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Account Active Status
    #'
    #' @param account_id the account id of the user (deviceId or accountId required)
    #' @param connection_account_id The account id of the user you want to modify (if this is not set, then the accountId parameter will be used instead)
    #' @param active true will activate the user and false will deactivate
    #' @param device_id (optional) the device id (deviceId or accountId required)
    #' @param app_key (optional) the application key that the user belongs to
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UpdateActveStatus = function(account_id, connection_account_id, active, device_id = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateActveStatusWithHttpInfo(account_id, connection_account_id, active, device_id, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Account Active Status
    #'
    #' @param account_id the account id of the user (deviceId or accountId required)
    #' @param connection_account_id The account id of the user you want to modify (if this is not set, then the accountId parameter will be used instead)
    #' @param active true will activate the user and false will deactivate
    #' @param device_id (optional) the device id (deviceId or accountId required)
    #' @param app_key (optional) the application key that the user belongs to
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UpdateActveStatusWithHttpInfo = function(account_id, connection_account_id, active, device_id = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`connection_account_id`)) {
        stop("Missing required parameter `connection_account_id`.")
      }

      if (missing(`active`)) {
        stop("Missing required parameter `active`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$UpdateActveStatus, `account_id` is not nullable")
      }

      if (!missing(`connection_account_id`) && is.null(`connection_account_id`)) {
        stop("Invalid value for `connection_account_id` when calling AccountApi$UpdateActveStatus, `connection_account_id` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling AccountApi$UpdateActveStatus, `active` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$UpdateActveStatus, `device_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AccountApi$UpdateActveStatus, `app_key` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["connectionAccountId"]] <- `connection_account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["active"]] <- `active`

      local_var_url_path <- "/account/active/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Location
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param client_time (optional) The time of the update
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UpdateLocation = function(device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, client_time = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateLocationWithHttpInfo(device_id, account_id, latitude, longitude, client_time, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Location
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param client_time (optional) The time of the update
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UpdateLocationWithHttpInfo = function(device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, client_time = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$UpdateLocation, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$UpdateLocation, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$UpdateLocation, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$UpdateLocation, `longitude` is not nullable")
      }

      if (!missing(`client_time`) && is.null(`client_time`)) {
        stop("Invalid value for `client_time` when calling AccountApi$UpdateLocation, `client_time` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["clientTime"]] <- `client_time`

      local_var_url_path <- "/account/location/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Account Settings
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param blocked_notifications (optional) The notifications to be blocked
    #' @param suggestion_method (optional) How suggestions are to be sent (APNS, MOBILE_NOTIFICATION, SMS)
    #' @param suggestion_count (optional) How many suggestions to receive per time frame
    #' @param suggestion_time_frame (optional) The time frame in seconds, 3600 would be a 1 hour time frame
    #' @param show_others_exact_location (optional) Show Others Exact Location
    #' @param show_as_zipcode (optional) Show As Zipcode
    #' @param show_exact_location (optional) Show Exact Location
    #' @param favorite_visibility (optional) Show favorites
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return UserSettingsResponse
    UpdateSettings = function(device_id = NULL, account_id = NULL, blocked_notifications = NULL, suggestion_method = NULL, suggestion_count = NULL, suggestion_time_frame = NULL, show_others_exact_location = NULL, show_as_zipcode = NULL, show_exact_location = NULL, favorite_visibility = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateSettingsWithHttpInfo(device_id, account_id, blocked_notifications, suggestion_method, suggestion_count, suggestion_time_frame, show_others_exact_location, show_as_zipcode, show_exact_location, favorite_visibility, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Account Settings
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param blocked_notifications (optional) The notifications to be blocked
    #' @param suggestion_method (optional) How suggestions are to be sent (APNS, MOBILE_NOTIFICATION, SMS)
    #' @param suggestion_count (optional) How many suggestions to receive per time frame
    #' @param suggestion_time_frame (optional) The time frame in seconds, 3600 would be a 1 hour time frame
    #' @param show_others_exact_location (optional) Show Others Exact Location
    #' @param show_as_zipcode (optional) Show As Zipcode
    #' @param show_exact_location (optional) Show Exact Location
    #' @param favorite_visibility (optional) Show favorites
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (UserSettingsResponse) with additional information such as HTTP status code, headers
    UpdateSettingsWithHttpInfo = function(device_id = NULL, account_id = NULL, blocked_notifications = NULL, suggestion_method = NULL, suggestion_count = NULL, suggestion_time_frame = NULL, show_others_exact_location = NULL, show_as_zipcode = NULL, show_exact_location = NULL, favorite_visibility = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AccountApi$UpdateSettings, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AccountApi$UpdateSettings, `account_id` is not nullable")
      }

      if (!missing(`blocked_notifications`) && is.null(`blocked_notifications`)) {
        stop("Invalid value for `blocked_notifications` when calling AccountApi$UpdateSettings, `blocked_notifications` is not nullable")
      }

      if (!missing(`suggestion_method`) && is.null(`suggestion_method`)) {
        stop("Invalid value for `suggestion_method` when calling AccountApi$UpdateSettings, `suggestion_method` is not nullable")
      }

      if (!missing(`suggestion_count`) && is.null(`suggestion_count`)) {
        stop("Invalid value for `suggestion_count` when calling AccountApi$UpdateSettings, `suggestion_count` is not nullable")
      }

      if (!missing(`suggestion_time_frame`) && is.null(`suggestion_time_frame`)) {
        stop("Invalid value for `suggestion_time_frame` when calling AccountApi$UpdateSettings, `suggestion_time_frame` is not nullable")
      }

      if (!missing(`show_others_exact_location`) && is.null(`show_others_exact_location`)) {
        stop("Invalid value for `show_others_exact_location` when calling AccountApi$UpdateSettings, `show_others_exact_location` is not nullable")
      }

      if (!missing(`show_as_zipcode`) && is.null(`show_as_zipcode`)) {
        stop("Invalid value for `show_as_zipcode` when calling AccountApi$UpdateSettings, `show_as_zipcode` is not nullable")
      }

      if (!missing(`show_exact_location`) && is.null(`show_exact_location`)) {
        stop("Invalid value for `show_exact_location` when calling AccountApi$UpdateSettings, `show_exact_location` is not nullable")
      }

      if (!missing(`favorite_visibility`) && is.null(`favorite_visibility`)) {
        stop("Invalid value for `favorite_visibility` when calling AccountApi$UpdateSettings, `favorite_visibility` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AccountApi$UpdateSettings, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AccountApi$UpdateSettings, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["blockedNotifications"]] <- `blocked_notifications`

      query_params[["suggestionMethod"]] <- `suggestion_method`

      query_params[["suggestionCount"]] <- `suggestion_count`

      query_params[["suggestionTimeFrame"]] <- `suggestion_time_frame`

      query_params[["showOthersExactLocation"]] <- `show_others_exact_location`

      query_params[["showAsZipcode"]] <- `show_as_zipcode`

      query_params[["showExactLocation"]] <- `show_exact_location`

      query_params[["favoriteVisibility"]] <- `favorite_visibility`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/account/settings/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UserSettingsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Save Validation Status
    #'
    #' @param token The token associated with the account to update, good for 24 hours
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AccountLoginResponse
    ValidateAccountSignup = function(token, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ValidateAccountSignupWithHttpInfo(token, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Save Validation Status
    #'
    #' @param token The token associated with the account to update, good for 24 hours
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AccountLoginResponse) with additional information such as HTTP status code, headers
    ValidateAccountSignupWithHttpInfo = function(token, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`token`)) {
        stop("Missing required parameter `token`.")
      }

      if (!missing(`token`) && is.null(`token`)) {
        stop("Invalid value for `token` when calling AccountApi$ValidateAccountSignup, `token` is not nullable")
      }

      query_params[["token"]] <- `token`

      local_var_url_path <- "/account/validateAccountSignup"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AccountLoginResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Validate Password Reset Token
    #'
    #' @param token The token associated with the account to update, good for 24 hours
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    ValidatePasswordReset = function(token, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ValidatePasswordResetWithHttpInfo(token, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Validate Password Reset Token
    #'
    #' @param token The token associated with the account to update, good for 24 hours
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    ValidatePasswordResetWithHttpInfo = function(token, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`token`)) {
        stop("Missing required parameter `token`.")
      }

      if (!missing(`token`) && is.null(`token`)) {
        stop("Invalid value for `token` when calling AccountApi$ValidatePasswordReset, `token` is not nullable")
      }

      query_params[["token"]] <- `token`

      local_var_url_path <- "/account/validatepasswordreset"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
