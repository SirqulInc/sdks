#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Category operations
#' @description CategoryApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CategoryDistanceSearch  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user (Optional)
#' var_keyword <- "keyword_example" # character | The keyword string to search on (Optional)
#' var_app_key <- "app_key_example" # character | the appKey of the application to retrieve categories for, if not specified then search on the global application. (Optional)
#' var_category_ids <- "category_ids_example" # character | Restrict the search by specific categories (Optional)
#' var_parent_category_ids <- "parent_category_ids_example" # character | Restrict the search by specific parent categories so that only its sub children are searched. (Optional)
#' var_root_only <- "root_only_example" # character | Restrict the search to only those categories with no parent category assigned. (Optional)
#' var_sort_field <- "DISPLAY" # character | The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, NAME, DISPLAY (Optional)
#' var_response_group <- "response_group_example" # character | The group of categories to return: ALL, GLOBAL or MINE. ALL returns both global and application specific matches, GLOBAL only returns global matches, and MINE only returns the application specific matches. If not provided search on the application provided. (Optional)
#' var_descending <- FALSE # character | The order to return the search results (Optional)
#' var_start <- 0 # integer | The record to begin the return set on (Optional)
#' var_limit <- 20 # integer | The number of records to return (Optional)
#' var_active_only <- TRUE # character | Determines whether to return only active categories (Optional)
#' var_return_external <- "return_external_example" # character | Determines whether to return extra info about the category's \"Participant\" reference (Optional)
#' var_exact_match <- "exact_match_example" # character | If true search categories using the exact keyword, if false then do a partial match (like) search. (Optional)
#' var_type <- "type_example" # character | Filters results by the Category's type (Optional)
#' var_external_type <- "external_type_example" # character | Filters results by externalType (Optional)
#' var_min_offer_count <- 56 # integer | Filters results to only return Categories that have been referenced by a minimum number of Offers (Optional)
#' var_latitude <- 3.4 # numeric | the latitude of where the search is centered on (Optional)
#' var_longitude <- 3.4 # numeric | the longitude of where the search is centered on (Optional)
#' var_range <- 3.4 # numeric | the maximum range the category can be from the center (Optional)
#'
#' #Search Categories by Distance
#' api_instance <- CategoryApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CategoryDistanceSearch(account_id = var_account_id, keyword = var_keyword, app_key = var_app_key, category_ids = var_category_ids, parent_category_ids = var_parent_category_ids, root_only = var_root_only, sort_field = var_sort_field, response_group = var_response_group, descending = var_descending, start = var_start, limit = var_limit, active_only = var_active_only, return_external = var_return_external, exact_match = var_exact_match, type = var_type, external_type = var_external_type, min_offer_count = var_min_offer_count, latitude = var_latitude, longitude = var_longitude, range = var_rangedata_file = "result.txt")
#' result <- api_instance$CategoryDistanceSearch(account_id = var_account_id, keyword = var_keyword, app_key = var_app_key, category_ids = var_category_ids, parent_category_ids = var_parent_category_ids, root_only = var_root_only, sort_field = var_sort_field, response_group = var_response_group, descending = var_descending, start = var_start, limit = var_limit, active_only = var_active_only, return_external = var_return_external, exact_match = var_exact_match, type = var_type, external_type = var_external_type, min_offer_count = var_min_offer_count, latitude = var_latitude, longitude = var_longitude, range = var_range)
#' dput(result)
#'
#'
#' ####################  CreateCategory  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user (must have permissions to the target application)
#' var_name <- "name_example" # character | The name of the category
#' var_app_key <- "app_key_example" # character | The appKey of the application to assign the category to, if not provided then the category will be applied to the global application (if the account has permissions) (Optional)
#' var_parent_category_id <- 56 # integer | The ID of the parent category, if not provided then the parent category will be null (Optional)
#' var_description <- "description_example" # character | The description of the category (Optional)
#' var_type <- "type_example" # character | The type of the category (Optional)
#' var_asset_id <- 56 # integer | The ID of the image asset previously uploaded using the media service (Optional)
#' var_external_id <- "external_id_example" # character | A string identifier used by client applications to store external information (Optional)
#' var_external_type <- "external_type_example" # character | A string type used by client applications to store external information (Optional)
#' var_external_category_slug <- "external_category_slug_example" # character | external category slug (Optional)
#' var_sqoot_slug <- "sqoot_slug_example" # character | sqoot slug (Optional)
#' var_active <- "active_example" # character | Sets whether the category is active or inactive (hidden from consumers) (Optional)
#' var_meta_data <- "meta_data_example" # character | external custom client defined data (Optional)
#' var_search_tags <- "search_tags_example" # character | user defined strings for searching (Optional)
#'
#' #Create Category
#' api_instance <- CategoryApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateCategory(var_account_id, var_name, app_key = var_app_key, parent_category_id = var_parent_category_id, description = var_description, type = var_type, asset_id = var_asset_id, external_id = var_external_id, external_type = var_external_type, external_category_slug = var_external_category_slug, sqoot_slug = var_sqoot_slug, active = var_active, meta_data = var_meta_data, search_tags = var_search_tagsdata_file = "result.txt")
#' result <- api_instance$CreateCategory(var_account_id, var_name, app_key = var_app_key, parent_category_id = var_parent_category_id, description = var_description, type = var_type, asset_id = var_asset_id, external_id = var_external_id, external_type = var_external_type, external_category_slug = var_external_category_slug, sqoot_slug = var_sqoot_slug, active = var_active, meta_data = var_meta_data, search_tags = var_search_tags)
#' dput(result)
#'
#'
#' ####################  DeleteCategory  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the ID of the account
#' var_category_id <- 56 # integer | the ID of the category
#'
#' #Delete Category
#' api_instance <- CategoryApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteCategory(var_account_id, var_category_iddata_file = "result.txt")
#' result <- api_instance$DeleteCategory(var_account_id, var_category_id)
#' dput(result)
#'
#'
#' ####################  DuplicateCategory  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user (must have permissions to the target application)
#' var_category_id <- 56 # integer | The category ID to duplicate (includes all children)
#' var_app_key <- "app_key_example" # character | The application to assign the new category to, may be different then the application the source category is assigned to (Optional)
#' var_parent_category_id <- 56 # integer | The parent category ID to add the target category to. (Optional)
#'
#' #Duplicate Category
#' api_instance <- CategoryApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DuplicateCategory(var_account_id, var_category_id, app_key = var_app_key, parent_category_id = var_parent_category_iddata_file = "result.txt")
#' result <- api_instance$DuplicateCategory(var_account_id, var_category_id, app_key = var_app_key, parent_category_id = var_parent_category_id)
#' dput(result)
#'
#'
#' ####################  GetCategory  ####################
#'
#' library(openapi)
#' var_category_id <- 56 # integer | the ID of the category
#' var_return_external <- TRUE # character | Determines whether to return extra info about the category's \"Participant\" reference (Optional)
#'
#' #Get Category
#' api_instance <- CategoryApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCategory(var_category_id, return_external = var_return_externaldata_file = "result.txt")
#' result <- api_instance$GetCategory(var_category_id, return_external = var_return_external)
#' dput(result)
#'
#'
#' ####################  SearchCategories  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user (Optional)
#' var_keyword <- "keyword_example" # character | The string to search on (Optional)
#' var_app_key <- "app_key_example" # character | the appKey of the application to retrieve categories for, if not specified then search on the global application. (Optional)
#' var_category_id <- "category_id_example" # character | @Deprecated, use parentCategoryIds instead (Optional)
#' var_category_ids <- "category_ids_example" # character | Restrict the search by specific categories (Optional)
#' var_parent_category_ids <- "parent_category_ids_example" # character | Restrict the search by specific parent categories so that only its sub children are searched. (Optional)
#' var_root_only <- "root_only_example" # character | Restrict the search to only those categories with no parent category assigned. (Optional)
#' var_sort_field <- "DISPLAY" # character | The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, NAME, DISPLAY (Optional)
#' var_response_group <- "response_group_example" # character | The group of categories to return: ALL, GLOBAL or MINE. ALL returns both global and application specific matches, GLOBAL only returns global matches, and MINE only returns the application specific matches. If not provided search on the application provided. (Optional)
#' var_descending <- FALSE # character | The order to return the search results (Optional)
#' var_start <- 0 # integer | The record to begin the return set on (Optional)
#' var_limit <- 20 # integer | The number of records to return (Optional)
#' var_active_only <- TRUE # character | Determines whether to return only active categories (Optional)
#' var_return_external <- TRUE # character | Determines whether to return extra info about the category's \"Participant\" reference (Optional)
#' var_exact_match <- FALSE # character | If true search categories using the exact keyword, if false then do a partial match (like) search. Default is false. (Optional)
#' var_type <- "type_example" # character | Filters results by the Category's type (Optional)
#' var_external_type <- "external_type_example" # character | Filters results by externalType (Optional)
#' var_exclude_external_type <- "exclude_external_type_example" # character | Determines whether the \"externalType\" param is excluded from the search results (Optional)
#' var_min_offer_count <- 56 # integer | Filters results to only return Categories that have been referenced by a minimum number of Offers (Optional)
#' var_search_depth <- 4 # integer | When searching by a specific parent category (to return sub children), this determines the number of child layers to search in. The minimum is 1, the maximum is 4. (Optional)
#' var_search_mode <- "search_mode_example" # character | The search index mode to use (e.g. OPENSEARCH or RDS) (Optional)
#'
#' #Search Categories
#' api_instance <- CategoryApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchCategories(account_id = var_account_id, keyword = var_keyword, app_key = var_app_key, category_id = var_category_id, category_ids = var_category_ids, parent_category_ids = var_parent_category_ids, root_only = var_root_only, sort_field = var_sort_field, response_group = var_response_group, descending = var_descending, start = var_start, limit = var_limit, active_only = var_active_only, return_external = var_return_external, exact_match = var_exact_match, type = var_type, external_type = var_external_type, exclude_external_type = var_exclude_external_type, min_offer_count = var_min_offer_count, search_depth = var_search_depth, search_mode = var_search_modedata_file = "result.txt")
#' result <- api_instance$SearchCategories(account_id = var_account_id, keyword = var_keyword, app_key = var_app_key, category_id = var_category_id, category_ids = var_category_ids, parent_category_ids = var_parent_category_ids, root_only = var_root_only, sort_field = var_sort_field, response_group = var_response_group, descending = var_descending, start = var_start, limit = var_limit, active_only = var_active_only, return_external = var_return_external, exact_match = var_exact_match, type = var_type, external_type = var_external_type, exclude_external_type = var_exclude_external_type, min_offer_count = var_min_offer_count, search_depth = var_search_depth, search_mode = var_search_mode)
#' dput(result)
#'
#'
#' ####################  UpdateCategory  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user
#' var_category_id <- 56 # integer | The ID of the category to edit
#' var_parent_category_id <- 56 # integer | The ID of the parent category, if not provided then the parent category will be null (Optional)
#' var_name <- "name_example" # character | The name of the category (Optional)
#' var_description <- "description_example" # character | The description of the category (Optional)
#' var_type <- "type_example" # character | The type of the category (Optional)
#' var_asset_id <- 56 # integer | The ID of the image asset previously uploaded using the media service (Optional)
#' var_external_id <- "external_id_example" # character | A string identifier used by client applications to store external information (Optional)
#' var_external_type <- "external_type_example" # character | A string type used by client applications to store external information (Optional)
#' var_external_category_slug <- "external_category_slug_example" # character | external category slug (Optional)
#' var_sqoot_slug <- "sqoot_slug_example" # character | sqoot slug (Optional)
#' var_active <- "active_example" # character | Sets whether the category is active or inactive (hidden from consumers) (Optional)
#' var_meta_data <- "meta_data_example" # character | external custom client defined data (Optional)
#' var_search_tags <- "search_tags_example" # character | user defined strings for searching (Optional)
#'
#' #Update Category
#' api_instance <- CategoryApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateCategory(var_account_id, var_category_id, parent_category_id = var_parent_category_id, name = var_name, description = var_description, type = var_type, asset_id = var_asset_id, external_id = var_external_id, external_type = var_external_type, external_category_slug = var_external_category_slug, sqoot_slug = var_sqoot_slug, active = var_active, meta_data = var_meta_data, search_tags = var_search_tagsdata_file = "result.txt")
#' result <- api_instance$UpdateCategory(var_account_id, var_category_id, parent_category_id = var_parent_category_id, name = var_name, description = var_description, type = var_type, asset_id = var_asset_id, external_id = var_external_id, external_type = var_external_type, external_category_slug = var_external_category_slug, sqoot_slug = var_sqoot_slug, active = var_active, meta_data = var_meta_data, search_tags = var_search_tags)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CategoryApi <- R6::R6Class(
  "CategoryApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CategoryApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Search Categories by Distance
    #'
    #' @param account_id (optional) The account id of the user
    #' @param keyword (optional) The keyword string to search on
    #' @param app_key (optional) the appKey of the application to retrieve categories for, if not specified then search on the global application.
    #' @param category_ids (optional) Restrict the search by specific categories
    #' @param parent_category_ids (optional) Restrict the search by specific parent categories so that only its sub children are searched.
    #' @param root_only (optional) Restrict the search to only those categories with no parent category assigned.
    #' @param sort_field (optional) The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, NAME, DISPLAY (default value: "DISPLAY")
    #' @param response_group (optional) The group of categories to return: ALL, GLOBAL or MINE. ALL returns both global and application specific matches, GLOBAL only returns global matches, and MINE only returns the application specific matches. If not provided search on the application provided.
    #' @param descending (optional) The order to return the search results (default value: FALSE)
    #' @param start (optional) The record to begin the return set on (default value: 0)
    #' @param limit (optional) The number of records to return (default value: 20)
    #' @param active_only (optional) Determines whether to return only active categories (default value: TRUE)
    #' @param return_external (optional) Determines whether to return extra info about the category's \"Participant\" reference
    #' @param exact_match (optional) If true search categories using the exact keyword, if false then do a partial match (like) search.
    #' @param type (optional) Filters results by the Category's type
    #' @param external_type (optional) Filters results by externalType
    #' @param min_offer_count (optional) Filters results to only return Categories that have been referenced by a minimum number of Offers
    #' @param latitude (optional) the latitude of where the search is centered on
    #' @param longitude (optional) the longitude of where the search is centered on
    #' @param range (optional) the maximum range the category can be from the center
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[CategoryResponse]
    CategoryDistanceSearch = function(account_id = NULL, keyword = NULL, app_key = NULL, category_ids = NULL, parent_category_ids = NULL, root_only = NULL, sort_field = "DISPLAY", response_group = NULL, descending = FALSE, start = 0, limit = 20, active_only = TRUE, return_external = NULL, exact_match = NULL, type = NULL, external_type = NULL, min_offer_count = NULL, latitude = NULL, longitude = NULL, range = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CategoryDistanceSearchWithHttpInfo(account_id, keyword, app_key, category_ids, parent_category_ids, root_only, sort_field, response_group, descending, start, limit, active_only, return_external, exact_match, type, external_type, min_offer_count, latitude, longitude, range, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Categories by Distance
    #'
    #' @param account_id (optional) The account id of the user
    #' @param keyword (optional) The keyword string to search on
    #' @param app_key (optional) the appKey of the application to retrieve categories for, if not specified then search on the global application.
    #' @param category_ids (optional) Restrict the search by specific categories
    #' @param parent_category_ids (optional) Restrict the search by specific parent categories so that only its sub children are searched.
    #' @param root_only (optional) Restrict the search to only those categories with no parent category assigned.
    #' @param sort_field (optional) The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, NAME, DISPLAY (default value: "DISPLAY")
    #' @param response_group (optional) The group of categories to return: ALL, GLOBAL or MINE. ALL returns both global and application specific matches, GLOBAL only returns global matches, and MINE only returns the application specific matches. If not provided search on the application provided.
    #' @param descending (optional) The order to return the search results (default value: FALSE)
    #' @param start (optional) The record to begin the return set on (default value: 0)
    #' @param limit (optional) The number of records to return (default value: 20)
    #' @param active_only (optional) Determines whether to return only active categories (default value: TRUE)
    #' @param return_external (optional) Determines whether to return extra info about the category's \"Participant\" reference
    #' @param exact_match (optional) If true search categories using the exact keyword, if false then do a partial match (like) search.
    #' @param type (optional) Filters results by the Category's type
    #' @param external_type (optional) Filters results by externalType
    #' @param min_offer_count (optional) Filters results to only return Categories that have been referenced by a minimum number of Offers
    #' @param latitude (optional) the latitude of where the search is centered on
    #' @param longitude (optional) the longitude of where the search is centered on
    #' @param range (optional) the maximum range the category can be from the center
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[CategoryResponse]) with additional information such as HTTP status code, headers
    CategoryDistanceSearchWithHttpInfo = function(account_id = NULL, keyword = NULL, app_key = NULL, category_ids = NULL, parent_category_ids = NULL, root_only = NULL, sort_field = "DISPLAY", response_group = NULL, descending = FALSE, start = 0, limit = 20, active_only = TRUE, return_external = NULL, exact_match = NULL, type = NULL, external_type = NULL, min_offer_count = NULL, latitude = NULL, longitude = NULL, range = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CategoryApi$CategoryDistanceSearch, `account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling CategoryApi$CategoryDistanceSearch, `keyword` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling CategoryApi$CategoryDistanceSearch, `app_key` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling CategoryApi$CategoryDistanceSearch, `category_ids` is not nullable")
      }

      if (!missing(`parent_category_ids`) && is.null(`parent_category_ids`)) {
        stop("Invalid value for `parent_category_ids` when calling CategoryApi$CategoryDistanceSearch, `parent_category_ids` is not nullable")
      }

      if (!missing(`root_only`) && is.null(`root_only`)) {
        stop("Invalid value for `root_only` when calling CategoryApi$CategoryDistanceSearch, `root_only` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling CategoryApi$CategoryDistanceSearch, `sort_field` is not nullable")
      }

      if (!missing(`response_group`) && is.null(`response_group`)) {
        stop("Invalid value for `response_group` when calling CategoryApi$CategoryDistanceSearch, `response_group` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling CategoryApi$CategoryDistanceSearch, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling CategoryApi$CategoryDistanceSearch, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling CategoryApi$CategoryDistanceSearch, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling CategoryApi$CategoryDistanceSearch, `active_only` is not nullable")
      }

      if (!missing(`return_external`) && is.null(`return_external`)) {
        stop("Invalid value for `return_external` when calling CategoryApi$CategoryDistanceSearch, `return_external` is not nullable")
      }

      if (!missing(`exact_match`) && is.null(`exact_match`)) {
        stop("Invalid value for `exact_match` when calling CategoryApi$CategoryDistanceSearch, `exact_match` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling CategoryApi$CategoryDistanceSearch, `type` is not nullable")
      }

      if (!missing(`external_type`) && is.null(`external_type`)) {
        stop("Invalid value for `external_type` when calling CategoryApi$CategoryDistanceSearch, `external_type` is not nullable")
      }

      if (!missing(`min_offer_count`) && is.null(`min_offer_count`)) {
        stop("Invalid value for `min_offer_count` when calling CategoryApi$CategoryDistanceSearch, `min_offer_count` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling CategoryApi$CategoryDistanceSearch, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling CategoryApi$CategoryDistanceSearch, `longitude` is not nullable")
      }

      if (!missing(`range`) && is.null(`range`)) {
        stop("Invalid value for `range` when calling CategoryApi$CategoryDistanceSearch, `range` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["appKey"]] <- `app_key`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["parentCategoryIds"]] <- `parent_category_ids`

      query_params[["rootOnly"]] <- `root_only`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "NAME", "DISPLAY"))) {
        stop("Invalid value for sort_field when calling CategoryApi$CategoryDistanceSearch. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, NAME, DISPLAY].")
      }
      query_params[["sortField"]] <- `sort_field`

      if (!is.null(`response_group`) && !(`response_group` %in% c("ALL", "GLOBAL", "MINE"))) {
        stop("Invalid value for response_group when calling CategoryApi$CategoryDistanceSearch. Must be [ALL, GLOBAL, MINE].")
      }
      query_params[["responseGroup"]] <- `response_group`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["returnExternal"]] <- `return_external`

      query_params[["exactMatch"]] <- `exact_match`

      query_params[["type"]] <- `type`

      query_params[["externalType"]] <- `external_type`

      query_params[["minOfferCount"]] <- `min_offer_count`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["range"]] <- `range`

      local_var_url_path <- "/category/distancesearch"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[CategoryResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Category
    #'
    #' @param account_id The account id of the user (must have permissions to the target application)
    #' @param name The name of the category
    #' @param app_key (optional) The appKey of the application to assign the category to, if not provided then the category will be applied to the global application (if the account has permissions)
    #' @param parent_category_id (optional) The ID of the parent category, if not provided then the parent category will be null
    #' @param description (optional) The description of the category
    #' @param type (optional) The type of the category
    #' @param asset_id (optional) The ID of the image asset previously uploaded using the media service
    #' @param external_id (optional) A string identifier used by client applications to store external information
    #' @param external_type (optional) A string type used by client applications to store external information
    #' @param external_category_slug (optional) external category slug
    #' @param sqoot_slug (optional) sqoot slug
    #' @param active (optional) Sets whether the category is active or inactive (hidden from consumers)
    #' @param meta_data (optional) external custom client defined data
    #' @param search_tags (optional) user defined strings for searching
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CategoryTreeResponse
    CreateCategory = function(account_id, name, app_key = NULL, parent_category_id = NULL, description = NULL, type = NULL, asset_id = NULL, external_id = NULL, external_type = NULL, external_category_slug = NULL, sqoot_slug = NULL, active = NULL, meta_data = NULL, search_tags = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateCategoryWithHttpInfo(account_id, name, app_key, parent_category_id, description, type, asset_id, external_id, external_type, external_category_slug, sqoot_slug, active, meta_data, search_tags, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Category
    #'
    #' @param account_id The account id of the user (must have permissions to the target application)
    #' @param name The name of the category
    #' @param app_key (optional) The appKey of the application to assign the category to, if not provided then the category will be applied to the global application (if the account has permissions)
    #' @param parent_category_id (optional) The ID of the parent category, if not provided then the parent category will be null
    #' @param description (optional) The description of the category
    #' @param type (optional) The type of the category
    #' @param asset_id (optional) The ID of the image asset previously uploaded using the media service
    #' @param external_id (optional) A string identifier used by client applications to store external information
    #' @param external_type (optional) A string type used by client applications to store external information
    #' @param external_category_slug (optional) external category slug
    #' @param sqoot_slug (optional) sqoot slug
    #' @param active (optional) Sets whether the category is active or inactive (hidden from consumers)
    #' @param meta_data (optional) external custom client defined data
    #' @param search_tags (optional) user defined strings for searching
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CategoryTreeResponse) with additional information such as HTTP status code, headers
    CreateCategoryWithHttpInfo = function(account_id, name, app_key = NULL, parent_category_id = NULL, description = NULL, type = NULL, asset_id = NULL, external_id = NULL, external_type = NULL, external_category_slug = NULL, sqoot_slug = NULL, active = NULL, meta_data = NULL, search_tags = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CategoryApi$CreateCategory, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling CategoryApi$CreateCategory, `name` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling CategoryApi$CreateCategory, `app_key` is not nullable")
      }

      if (!missing(`parent_category_id`) && is.null(`parent_category_id`)) {
        stop("Invalid value for `parent_category_id` when calling CategoryApi$CreateCategory, `parent_category_id` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling CategoryApi$CreateCategory, `description` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling CategoryApi$CreateCategory, `type` is not nullable")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling CategoryApi$CreateCategory, `asset_id` is not nullable")
      }

      if (!missing(`external_id`) && is.null(`external_id`)) {
        stop("Invalid value for `external_id` when calling CategoryApi$CreateCategory, `external_id` is not nullable")
      }

      if (!missing(`external_type`) && is.null(`external_type`)) {
        stop("Invalid value for `external_type` when calling CategoryApi$CreateCategory, `external_type` is not nullable")
      }

      if (!missing(`external_category_slug`) && is.null(`external_category_slug`)) {
        stop("Invalid value for `external_category_slug` when calling CategoryApi$CreateCategory, `external_category_slug` is not nullable")
      }

      if (!missing(`sqoot_slug`) && is.null(`sqoot_slug`)) {
        stop("Invalid value for `sqoot_slug` when calling CategoryApi$CreateCategory, `sqoot_slug` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling CategoryApi$CreateCategory, `active` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling CategoryApi$CreateCategory, `meta_data` is not nullable")
      }

      if (!missing(`search_tags`) && is.null(`search_tags`)) {
        stop("Invalid value for `search_tags` when calling CategoryApi$CreateCategory, `search_tags` is not nullable")
      }

      query_params[["appKey"]] <- `app_key`

      query_params[["accountId"]] <- `account_id`

      query_params[["parentCategoryId"]] <- `parent_category_id`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["type"]] <- `type`

      query_params[["assetId"]] <- `asset_id`

      query_params[["externalId"]] <- `external_id`

      query_params[["externalType"]] <- `external_type`

      query_params[["externalCategorySlug"]] <- `external_category_slug`

      query_params[["sqootSlug"]] <- `sqoot_slug`

      query_params[["active"]] <- `active`

      query_params[["metaData"]] <- `meta_data`

      query_params[["searchTags"]] <- `search_tags`

      local_var_url_path <- "/category/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CategoryTreeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Category
    #'
    #' @param account_id the ID of the account
    #' @param category_id the ID of the category
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteCategory = function(account_id, category_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteCategoryWithHttpInfo(account_id, category_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Category
    #'
    #' @param account_id the ID of the account
    #' @param category_id the ID of the category
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteCategoryWithHttpInfo = function(account_id, category_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`category_id`)) {
        stop("Missing required parameter `category_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CategoryApi$DeleteCategory, `account_id` is not nullable")
      }

      if (!missing(`category_id`) && is.null(`category_id`)) {
        stop("Invalid value for `category_id` when calling CategoryApi$DeleteCategory, `category_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["categoryId"]] <- `category_id`

      local_var_url_path <- "/category/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Duplicate Category
    #'
    #' @param account_id The account id of the user (must have permissions to the target application)
    #' @param category_id The category ID to duplicate (includes all children)
    #' @param app_key (optional) The application to assign the new category to, may be different then the application the source category is assigned to
    #' @param parent_category_id (optional) The parent category ID to add the target category to.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CategoryTreeResponse
    DuplicateCategory = function(account_id, category_id, app_key = NULL, parent_category_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DuplicateCategoryWithHttpInfo(account_id, category_id, app_key, parent_category_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Duplicate Category
    #'
    #' @param account_id The account id of the user (must have permissions to the target application)
    #' @param category_id The category ID to duplicate (includes all children)
    #' @param app_key (optional) The application to assign the new category to, may be different then the application the source category is assigned to
    #' @param parent_category_id (optional) The parent category ID to add the target category to.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CategoryTreeResponse) with additional information such as HTTP status code, headers
    DuplicateCategoryWithHttpInfo = function(account_id, category_id, app_key = NULL, parent_category_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`category_id`)) {
        stop("Missing required parameter `category_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CategoryApi$DuplicateCategory, `account_id` is not nullable")
      }

      if (!missing(`category_id`) && is.null(`category_id`)) {
        stop("Invalid value for `category_id` when calling CategoryApi$DuplicateCategory, `category_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling CategoryApi$DuplicateCategory, `app_key` is not nullable")
      }

      if (!missing(`parent_category_id`) && is.null(`parent_category_id`)) {
        stop("Invalid value for `parent_category_id` when calling CategoryApi$DuplicateCategory, `parent_category_id` is not nullable")
      }

      query_params[["appKey"]] <- `app_key`

      query_params[["accountId"]] <- `account_id`

      query_params[["categoryId"]] <- `category_id`

      query_params[["parentCategoryId"]] <- `parent_category_id`

      local_var_url_path <- "/category/duplicate"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CategoryTreeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Category
    #'
    #' @param category_id the ID of the category
    #' @param return_external (optional) Determines whether to return extra info about the category's \"Participant\" reference (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CategoryTreeResponse
    GetCategory = function(category_id, return_external = TRUE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetCategoryWithHttpInfo(category_id, return_external, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Category
    #'
    #' @param category_id the ID of the category
    #' @param return_external (optional) Determines whether to return extra info about the category's \"Participant\" reference (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CategoryTreeResponse) with additional information such as HTTP status code, headers
    GetCategoryWithHttpInfo = function(category_id, return_external = TRUE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`category_id`)) {
        stop("Missing required parameter `category_id`.")
      }

      if (!missing(`category_id`) && is.null(`category_id`)) {
        stop("Invalid value for `category_id` when calling CategoryApi$GetCategory, `category_id` is not nullable")
      }

      if (!missing(`return_external`) && is.null(`return_external`)) {
        stop("Invalid value for `return_external` when calling CategoryApi$GetCategory, `return_external` is not nullable")
      }

      query_params[["categoryId"]] <- `category_id`

      query_params[["returnExternal"]] <- `return_external`

      local_var_url_path <- "/category/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CategoryTreeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Categories
    #'
    #' @param account_id (optional) The account id of the user
    #' @param keyword (optional) The string to search on
    #' @param app_key (optional) the appKey of the application to retrieve categories for, if not specified then search on the global application.
    #' @param category_id (optional) @Deprecated, use parentCategoryIds instead
    #' @param category_ids (optional) Restrict the search by specific categories
    #' @param parent_category_ids (optional) Restrict the search by specific parent categories so that only its sub children are searched.
    #' @param root_only (optional) Restrict the search to only those categories with no parent category assigned.
    #' @param sort_field (optional) The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, NAME, DISPLAY (default value: "DISPLAY")
    #' @param response_group (optional) The group of categories to return: ALL, GLOBAL or MINE. ALL returns both global and application specific matches, GLOBAL only returns global matches, and MINE only returns the application specific matches. If not provided search on the application provided.
    #' @param descending (optional) The order to return the search results (default value: FALSE)
    #' @param start (optional) The record to begin the return set on (default value: 0)
    #' @param limit (optional) The number of records to return (default value: 20)
    #' @param active_only (optional) Determines whether to return only active categories (default value: TRUE)
    #' @param return_external (optional) Determines whether to return extra info about the category's \"Participant\" reference (default value: TRUE)
    #' @param exact_match (optional) If true search categories using the exact keyword, if false then do a partial match (like) search. Default is false. (default value: FALSE)
    #' @param type (optional) Filters results by the Category's type
    #' @param external_type (optional) Filters results by externalType
    #' @param exclude_external_type (optional) Determines whether the \"externalType\" param is excluded from the search results
    #' @param min_offer_count (optional) Filters results to only return Categories that have been referenced by a minimum number of Offers
    #' @param search_depth (optional) When searching by a specific parent category (to return sub children), this determines the number of child layers to search in. The minimum is 1, the maximum is 4. (default value: 4)
    #' @param search_mode (optional) The search index mode to use (e.g. OPENSEARCH or RDS)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[CategoryResponse]
    SearchCategories = function(account_id = NULL, keyword = NULL, app_key = NULL, category_id = NULL, category_ids = NULL, parent_category_ids = NULL, root_only = NULL, sort_field = "DISPLAY", response_group = NULL, descending = FALSE, start = 0, limit = 20, active_only = TRUE, return_external = TRUE, exact_match = FALSE, type = NULL, external_type = NULL, exclude_external_type = NULL, min_offer_count = NULL, search_depth = 4, search_mode = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchCategoriesWithHttpInfo(account_id, keyword, app_key, category_id, category_ids, parent_category_ids, root_only, sort_field, response_group, descending, start, limit, active_only, return_external, exact_match, type, external_type, exclude_external_type, min_offer_count, search_depth, search_mode, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Categories
    #'
    #' @param account_id (optional) The account id of the user
    #' @param keyword (optional) The string to search on
    #' @param app_key (optional) the appKey of the application to retrieve categories for, if not specified then search on the global application.
    #' @param category_id (optional) @Deprecated, use parentCategoryIds instead
    #' @param category_ids (optional) Restrict the search by specific categories
    #' @param parent_category_ids (optional) Restrict the search by specific parent categories so that only its sub children are searched.
    #' @param root_only (optional) Restrict the search to only those categories with no parent category assigned.
    #' @param sort_field (optional) The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, NAME, DISPLAY (default value: "DISPLAY")
    #' @param response_group (optional) The group of categories to return: ALL, GLOBAL or MINE. ALL returns both global and application specific matches, GLOBAL only returns global matches, and MINE only returns the application specific matches. If not provided search on the application provided.
    #' @param descending (optional) The order to return the search results (default value: FALSE)
    #' @param start (optional) The record to begin the return set on (default value: 0)
    #' @param limit (optional) The number of records to return (default value: 20)
    #' @param active_only (optional) Determines whether to return only active categories (default value: TRUE)
    #' @param return_external (optional) Determines whether to return extra info about the category's \"Participant\" reference (default value: TRUE)
    #' @param exact_match (optional) If true search categories using the exact keyword, if false then do a partial match (like) search. Default is false. (default value: FALSE)
    #' @param type (optional) Filters results by the Category's type
    #' @param external_type (optional) Filters results by externalType
    #' @param exclude_external_type (optional) Determines whether the \"externalType\" param is excluded from the search results
    #' @param min_offer_count (optional) Filters results to only return Categories that have been referenced by a minimum number of Offers
    #' @param search_depth (optional) When searching by a specific parent category (to return sub children), this determines the number of child layers to search in. The minimum is 1, the maximum is 4. (default value: 4)
    #' @param search_mode (optional) The search index mode to use (e.g. OPENSEARCH or RDS)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[CategoryResponse]) with additional information such as HTTP status code, headers
    SearchCategoriesWithHttpInfo = function(account_id = NULL, keyword = NULL, app_key = NULL, category_id = NULL, category_ids = NULL, parent_category_ids = NULL, root_only = NULL, sort_field = "DISPLAY", response_group = NULL, descending = FALSE, start = 0, limit = 20, active_only = TRUE, return_external = TRUE, exact_match = FALSE, type = NULL, external_type = NULL, exclude_external_type = NULL, min_offer_count = NULL, search_depth = 4, search_mode = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CategoryApi$SearchCategories, `account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling CategoryApi$SearchCategories, `keyword` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling CategoryApi$SearchCategories, `app_key` is not nullable")
      }

      if (!missing(`category_id`) && is.null(`category_id`)) {
        stop("Invalid value for `category_id` when calling CategoryApi$SearchCategories, `category_id` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling CategoryApi$SearchCategories, `category_ids` is not nullable")
      }

      if (!missing(`parent_category_ids`) && is.null(`parent_category_ids`)) {
        stop("Invalid value for `parent_category_ids` when calling CategoryApi$SearchCategories, `parent_category_ids` is not nullable")
      }

      if (!missing(`root_only`) && is.null(`root_only`)) {
        stop("Invalid value for `root_only` when calling CategoryApi$SearchCategories, `root_only` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling CategoryApi$SearchCategories, `sort_field` is not nullable")
      }

      if (!missing(`response_group`) && is.null(`response_group`)) {
        stop("Invalid value for `response_group` when calling CategoryApi$SearchCategories, `response_group` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling CategoryApi$SearchCategories, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling CategoryApi$SearchCategories, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling CategoryApi$SearchCategories, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling CategoryApi$SearchCategories, `active_only` is not nullable")
      }

      if (!missing(`return_external`) && is.null(`return_external`)) {
        stop("Invalid value for `return_external` when calling CategoryApi$SearchCategories, `return_external` is not nullable")
      }

      if (!missing(`exact_match`) && is.null(`exact_match`)) {
        stop("Invalid value for `exact_match` when calling CategoryApi$SearchCategories, `exact_match` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling CategoryApi$SearchCategories, `type` is not nullable")
      }

      if (!missing(`external_type`) && is.null(`external_type`)) {
        stop("Invalid value for `external_type` when calling CategoryApi$SearchCategories, `external_type` is not nullable")
      }

      if (!missing(`exclude_external_type`) && is.null(`exclude_external_type`)) {
        stop("Invalid value for `exclude_external_type` when calling CategoryApi$SearchCategories, `exclude_external_type` is not nullable")
      }

      if (!missing(`min_offer_count`) && is.null(`min_offer_count`)) {
        stop("Invalid value for `min_offer_count` when calling CategoryApi$SearchCategories, `min_offer_count` is not nullable")
      }

      if (!missing(`search_depth`) && is.null(`search_depth`)) {
        stop("Invalid value for `search_depth` when calling CategoryApi$SearchCategories, `search_depth` is not nullable")
      }

      if (!missing(`search_mode`) && is.null(`search_mode`)) {
        stop("Invalid value for `search_mode` when calling CategoryApi$SearchCategories, `search_mode` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["appKey"]] <- `app_key`

      query_params[["categoryId"]] <- `category_id`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["parentCategoryIds"]] <- `parent_category_ids`

      query_params[["rootOnly"]] <- `root_only`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "NAME", "DISPLAY"))) {
        stop("Invalid value for sort_field when calling CategoryApi$SearchCategories. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, NAME, DISPLAY].")
      }
      query_params[["sortField"]] <- `sort_field`

      if (!is.null(`response_group`) && !(`response_group` %in% c("ALL", "GLOBAL", "MINE"))) {
        stop("Invalid value for response_group when calling CategoryApi$SearchCategories. Must be [ALL, GLOBAL, MINE].")
      }
      query_params[["responseGroup"]] <- `response_group`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["returnExternal"]] <- `return_external`

      query_params[["exactMatch"]] <- `exact_match`

      query_params[["type"]] <- `type`

      query_params[["externalType"]] <- `external_type`

      query_params[["excludeExternalType"]] <- `exclude_external_type`

      query_params[["minOfferCount"]] <- `min_offer_count`

      query_params[["searchDepth"]] <- `search_depth`

      query_params[["searchMode"]] <- `search_mode`

      local_var_url_path <- "/category/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[CategoryResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Category
    #'
    #' @param account_id The account id of the user
    #' @param category_id The ID of the category to edit
    #' @param parent_category_id (optional) The ID of the parent category, if not provided then the parent category will be null
    #' @param name (optional) The name of the category
    #' @param description (optional) The description of the category
    #' @param type (optional) The type of the category
    #' @param asset_id (optional) The ID of the image asset previously uploaded using the media service
    #' @param external_id (optional) A string identifier used by client applications to store external information
    #' @param external_type (optional) A string type used by client applications to store external information
    #' @param external_category_slug (optional) external category slug
    #' @param sqoot_slug (optional) sqoot slug
    #' @param active (optional) Sets whether the category is active or inactive (hidden from consumers)
    #' @param meta_data (optional) external custom client defined data
    #' @param search_tags (optional) user defined strings for searching
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CategoryTreeResponse
    UpdateCategory = function(account_id, category_id, parent_category_id = NULL, name = NULL, description = NULL, type = NULL, asset_id = NULL, external_id = NULL, external_type = NULL, external_category_slug = NULL, sqoot_slug = NULL, active = NULL, meta_data = NULL, search_tags = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateCategoryWithHttpInfo(account_id, category_id, parent_category_id, name, description, type, asset_id, external_id, external_type, external_category_slug, sqoot_slug, active, meta_data, search_tags, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Category
    #'
    #' @param account_id The account id of the user
    #' @param category_id The ID of the category to edit
    #' @param parent_category_id (optional) The ID of the parent category, if not provided then the parent category will be null
    #' @param name (optional) The name of the category
    #' @param description (optional) The description of the category
    #' @param type (optional) The type of the category
    #' @param asset_id (optional) The ID of the image asset previously uploaded using the media service
    #' @param external_id (optional) A string identifier used by client applications to store external information
    #' @param external_type (optional) A string type used by client applications to store external information
    #' @param external_category_slug (optional) external category slug
    #' @param sqoot_slug (optional) sqoot slug
    #' @param active (optional) Sets whether the category is active or inactive (hidden from consumers)
    #' @param meta_data (optional) external custom client defined data
    #' @param search_tags (optional) user defined strings for searching
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CategoryTreeResponse) with additional information such as HTTP status code, headers
    UpdateCategoryWithHttpInfo = function(account_id, category_id, parent_category_id = NULL, name = NULL, description = NULL, type = NULL, asset_id = NULL, external_id = NULL, external_type = NULL, external_category_slug = NULL, sqoot_slug = NULL, active = NULL, meta_data = NULL, search_tags = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`category_id`)) {
        stop("Missing required parameter `category_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CategoryApi$UpdateCategory, `account_id` is not nullable")
      }

      if (!missing(`category_id`) && is.null(`category_id`)) {
        stop("Invalid value for `category_id` when calling CategoryApi$UpdateCategory, `category_id` is not nullable")
      }

      if (!missing(`parent_category_id`) && is.null(`parent_category_id`)) {
        stop("Invalid value for `parent_category_id` when calling CategoryApi$UpdateCategory, `parent_category_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling CategoryApi$UpdateCategory, `name` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling CategoryApi$UpdateCategory, `description` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling CategoryApi$UpdateCategory, `type` is not nullable")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling CategoryApi$UpdateCategory, `asset_id` is not nullable")
      }

      if (!missing(`external_id`) && is.null(`external_id`)) {
        stop("Invalid value for `external_id` when calling CategoryApi$UpdateCategory, `external_id` is not nullable")
      }

      if (!missing(`external_type`) && is.null(`external_type`)) {
        stop("Invalid value for `external_type` when calling CategoryApi$UpdateCategory, `external_type` is not nullable")
      }

      if (!missing(`external_category_slug`) && is.null(`external_category_slug`)) {
        stop("Invalid value for `external_category_slug` when calling CategoryApi$UpdateCategory, `external_category_slug` is not nullable")
      }

      if (!missing(`sqoot_slug`) && is.null(`sqoot_slug`)) {
        stop("Invalid value for `sqoot_slug` when calling CategoryApi$UpdateCategory, `sqoot_slug` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling CategoryApi$UpdateCategory, `active` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling CategoryApi$UpdateCategory, `meta_data` is not nullable")
      }

      if (!missing(`search_tags`) && is.null(`search_tags`)) {
        stop("Invalid value for `search_tags` when calling CategoryApi$UpdateCategory, `search_tags` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["categoryId"]] <- `category_id`

      query_params[["parentCategoryId"]] <- `parent_category_id`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["type"]] <- `type`

      query_params[["assetId"]] <- `asset_id`

      query_params[["externalId"]] <- `external_id`

      query_params[["externalType"]] <- `external_type`

      query_params[["externalCategorySlug"]] <- `external_category_slug`

      query_params[["sqootSlug"]] <- `sqoot_slug`

      query_params[["active"]] <- `active`

      query_params[["metaData"]] <- `meta_data`

      query_params[["searchTags"]] <- `search_tags`

      local_var_url_path <- "/category/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CategoryTreeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
