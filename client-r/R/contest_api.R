#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Contest operations
#' @description ContestApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AddOrUpdateAlbumContest  ####################
#'
#' library(openapi)
#' var_public_read <- "public_read_example" # character | determines whether the contest's participants has read permissions
#' var_public_write <- "public_write_example" # character | determines whether the contest's participants has write permissions
#' var_public_delete <- "public_delete_example" # character | determines whether the contest's participants has delete permissions
#' var_public_add <- "public_add_example" # character | determines whether the contest's participants has add permissions
#' var_visibility <- "visibility_example" # character | the determines the album's participants (PUBLIC - includes everyone in the system as a potential participant, PRIVATE - only considers people who have been invited as participants)
#' var_include_friend_group <- "include_friend_group_example" # character | determines whether to include all friends as participants
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_game_type <- "game_type_example" # character | This parameter is deprecated. the application key (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_contest_type <- "contest_type_example" # character | a custom field used for aggregation and searching (Optional)
#' var_album_contest_id <- 56 # integer | the album contest ID for updating (don't pass in if creating) (Optional)
#' var_title <- "title_example" # character | the title of the contest (Optional)
#' var_description <- "description_example" # character | the description of the contest (Optional)
#' var_album_id1 <- 56 # integer | the album ID for the first album (Optional)
#' var_remove_album1 <- "remove_album1_example" # character | removes album1 from the contest (Optional)
#' var_album_id2 <- 56 # integer | the album ID for the second album (Optional)
#' var_remove_album2 <- "remove_album2_example" # character | removes album2 from the contest (Optional)
#' var_start_date <- 56 # integer | the start date of the contest (time-stamp in milliseconds) (Optional)
#' var_end_date <- 56 # integer | the end date of the contest (time-stamp in milliseconds) (Optional)
#' var_location_description <- "location_description_example" # character | the location description of the contest taking place (Optional)
#' var_connection_ids_to_add <- "connection_ids_to_add_example" # character | comma separated list of connection IDs (Optional)
#' var_connection_group_ids_to_add <- "connection_group_ids_to_add_example" # character | comma separated list of connection group IDs (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Create or Update Contest
#' api_instance <- ContestApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AddOrUpdateAlbumContest(var_public_read, var_public_write, var_public_delete, var_public_add, var_visibility, var_include_friend_group, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, app_key = var_app_key, contest_type = var_contest_type, album_contest_id = var_album_contest_id, title = var_title, description = var_description, album_id1 = var_album_id1, remove_album1 = var_remove_album1, album_id2 = var_album_id2, remove_album2 = var_remove_album2, start_date = var_start_date, end_date = var_end_date, location_description = var_location_description, connection_ids_to_add = var_connection_ids_to_add, connection_group_ids_to_add = var_connection_group_ids_to_add, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$AddOrUpdateAlbumContest(var_public_read, var_public_write, var_public_delete, var_public_add, var_visibility, var_include_friend_group, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, app_key = var_app_key, contest_type = var_contest_type, album_contest_id = var_album_contest_id, title = var_title, description = var_description, album_id1 = var_album_id1, remove_album1 = var_remove_album1, album_id2 = var_album_id2, remove_album2 = var_remove_album2, start_date = var_start_date, end_date = var_end_date, location_description = var_location_description, connection_ids_to_add = var_connection_ids_to_add, connection_group_ids_to_add = var_connection_group_ids_to_add, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  ApproveAlbumContest  ####################
#'
#' library(openapi)
#' var_album_contest_id <- 56 # integer | The ID of the album contest
#' var_approval_status <- "approval_status_example" # character | The approval status to set {PENDING, REJECTED, APPROVED, FEATURED}
#' var_device_id <- "device_id_example" # character | A unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account ID of the user (deviceId or accountId required) (Optional)
#'
#' #Approve Contest
#' api_instance <- ContestApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ApproveAlbumContest(var_album_contest_id, var_approval_status, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$ApproveAlbumContest(var_album_contest_id, var_approval_status, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  DeleteContest  ####################
#'
#' library(openapi)
#' var_album_contest_id <- 56 # integer | the album contest ID
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Delete Contest
#' api_instance <- ContestApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteContest(var_album_contest_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$DeleteContest(var_album_contest_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  GetAlbumContest  ####################
#'
#' library(openapi)
#' var_album_contest_id <- 56 # integer | the album contest ID
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Get Contest
#' api_instance <- ContestApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAlbumContest(var_album_contest_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$GetAlbumContest(var_album_contest_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  GetAlbumContests  ####################
#'
#' library(openapi)
#' var_filter <- "filter_example" # character | a comma separated list of Ownership
#' var_sort_field <- "sort_field_example" # character | the field to sort by. See AlbumContestApiMap
#' var_descending <- "descending_example" # character | determines whether the sorted list is in descending or ascending order
#' var_start <- 56 # integer | the start index for pagination
#' var_limit <- 56 # integer | the limit for pagination (there is a hard limit of 30)
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_game_type <- "game_type_example" # character | This parameter is deprecated. (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_app_type <- "app_type_example" # character | the application type (Optional)
#' var_contest_type <- "contest_type_example" # character | filter contests with this contest type (Optional)
#' var_owner_id <- 56 # integer | search on contests that have been created by this account (that the user has permissions to) (Optional)
#' var_q <- "q_example" # character | This parameter is deprecated. (Optional)
#' var_keyword <- "keyword_example" # character | keyword search string (Optional)
#' var__i <- 56 # integer | This parameter is deprecated. (Optional)
#' var__l <- 56 # integer | This parameter is deprecated. (Optional)
#' var_date_created <- 56 # integer | filter on items that have been created before this date (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Search Contests
#' api_instance <- ContestApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAlbumContests(var_filter, var_sort_field, var_descending, var_start, var_limit, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, app_key = var_app_key, app_type = var_app_type, contest_type = var_contest_type, owner_id = var_owner_id, q = var_q, keyword = var_keyword, _i = var__i, _l = var__l, date_created = var_date_created, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$GetAlbumContests(var_filter, var_sort_field, var_descending, var_start, var_limit, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, app_key = var_app_key, app_type = var_app_type, contest_type = var_contest_type, owner_id = var_owner_id, q = var_q, keyword = var_keyword, _i = var__i, _l = var__l, date_created = var_date_created, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  VoteOnAlbumContest  ####################
#'
#' library(openapi)
#' var_album_contest_id <- 56 # integer | the album contest ID
#' var_album_id <- 56 # integer | the ID of the album to vote on
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_contest_type <- "contest_type_example" # character | a custom field used for aggregation and searching (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Vote on Contest
#' api_instance <- ContestApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$VoteOnAlbumContest(var_album_contest_id, var_album_id, device_id = var_device_id, account_id = var_account_id, contest_type = var_contest_type, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$VoteOnAlbumContest(var_album_contest_id, var_album_id, device_id = var_device_id, account_id = var_account_id, contest_type = var_contest_type, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ContestApi <- R6::R6Class(
  "ContestApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ContestApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create or Update Contest
    #'
    #' @param public_read determines whether the contest's participants has read permissions
    #' @param public_write determines whether the contest's participants has write permissions
    #' @param public_delete determines whether the contest's participants has delete permissions
    #' @param public_add determines whether the contest's participants has add permissions
    #' @param visibility the determines the album's participants (PUBLIC - includes everyone in the system as a potential participant, PRIVATE - only considers people who have been invited as participants)
    #' @param include_friend_group determines whether to include all friends as participants
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param game_type (optional) This parameter is deprecated. the application key
    #' @param app_key (optional) the application key
    #' @param contest_type (optional) a custom field used for aggregation and searching
    #' @param album_contest_id (optional) the album contest ID for updating (don't pass in if creating)
    #' @param title (optional) the title of the contest
    #' @param description (optional) the description of the contest
    #' @param album_id1 (optional) the album ID for the first album
    #' @param remove_album1 (optional) removes album1 from the contest
    #' @param album_id2 (optional) the album ID for the second album
    #' @param remove_album2 (optional) removes album2 from the contest
    #' @param start_date (optional) the start date of the contest (time-stamp in milliseconds)
    #' @param end_date (optional) the end date of the contest (time-stamp in milliseconds)
    #' @param location_description (optional) the location description of the contest taking place
    #' @param connection_ids_to_add (optional) comma separated list of connection IDs
    #' @param connection_group_ids_to_add (optional) comma separated list of connection group IDs
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AlbumContestResponse
    AddOrUpdateAlbumContest = function(public_read, public_write, public_delete, public_add, visibility, include_friend_group, device_id = NULL, account_id = NULL, game_type = NULL, app_key = NULL, contest_type = NULL, album_contest_id = NULL, title = NULL, description = NULL, album_id1 = NULL, remove_album1 = NULL, album_id2 = NULL, remove_album2 = NULL, start_date = NULL, end_date = NULL, location_description = NULL, connection_ids_to_add = NULL, connection_group_ids_to_add = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AddOrUpdateAlbumContestWithHttpInfo(public_read, public_write, public_delete, public_add, visibility, include_friend_group, device_id, account_id, game_type, app_key, contest_type, album_contest_id, title, description, album_id1, remove_album1, album_id2, remove_album2, start_date, end_date, location_description, connection_ids_to_add, connection_group_ids_to_add, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create or Update Contest
    #'
    #' @param public_read determines whether the contest's participants has read permissions
    #' @param public_write determines whether the contest's participants has write permissions
    #' @param public_delete determines whether the contest's participants has delete permissions
    #' @param public_add determines whether the contest's participants has add permissions
    #' @param visibility the determines the album's participants (PUBLIC - includes everyone in the system as a potential participant, PRIVATE - only considers people who have been invited as participants)
    #' @param include_friend_group determines whether to include all friends as participants
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param game_type (optional) This parameter is deprecated. the application key
    #' @param app_key (optional) the application key
    #' @param contest_type (optional) a custom field used for aggregation and searching
    #' @param album_contest_id (optional) the album contest ID for updating (don't pass in if creating)
    #' @param title (optional) the title of the contest
    #' @param description (optional) the description of the contest
    #' @param album_id1 (optional) the album ID for the first album
    #' @param remove_album1 (optional) removes album1 from the contest
    #' @param album_id2 (optional) the album ID for the second album
    #' @param remove_album2 (optional) removes album2 from the contest
    #' @param start_date (optional) the start date of the contest (time-stamp in milliseconds)
    #' @param end_date (optional) the end date of the contest (time-stamp in milliseconds)
    #' @param location_description (optional) the location description of the contest taking place
    #' @param connection_ids_to_add (optional) comma separated list of connection IDs
    #' @param connection_group_ids_to_add (optional) comma separated list of connection group IDs
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AlbumContestResponse) with additional information such as HTTP status code, headers
    AddOrUpdateAlbumContestWithHttpInfo = function(public_read, public_write, public_delete, public_add, visibility, include_friend_group, device_id = NULL, account_id = NULL, game_type = NULL, app_key = NULL, contest_type = NULL, album_contest_id = NULL, title = NULL, description = NULL, album_id1 = NULL, remove_album1 = NULL, album_id2 = NULL, remove_album2 = NULL, start_date = NULL, end_date = NULL, location_description = NULL, connection_ids_to_add = NULL, connection_group_ids_to_add = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`public_read`)) {
        stop("Missing required parameter `public_read`.")
      }

      if (missing(`public_write`)) {
        stop("Missing required parameter `public_write`.")
      }

      if (missing(`public_delete`)) {
        stop("Missing required parameter `public_delete`.")
      }

      if (missing(`public_add`)) {
        stop("Missing required parameter `public_add`.")
      }

      if (missing(`visibility`)) {
        stop("Missing required parameter `visibility`.")
      }

      if (missing(`include_friend_group`)) {
        stop("Missing required parameter `include_friend_group`.")
      }

      if (!missing(`public_read`) && is.null(`public_read`)) {
        stop("Invalid value for `public_read` when calling ContestApi$AddOrUpdateAlbumContest, `public_read` is not nullable")
      }

      if (!missing(`public_write`) && is.null(`public_write`)) {
        stop("Invalid value for `public_write` when calling ContestApi$AddOrUpdateAlbumContest, `public_write` is not nullable")
      }

      if (!missing(`public_delete`) && is.null(`public_delete`)) {
        stop("Invalid value for `public_delete` when calling ContestApi$AddOrUpdateAlbumContest, `public_delete` is not nullable")
      }

      if (!missing(`public_add`) && is.null(`public_add`)) {
        stop("Invalid value for `public_add` when calling ContestApi$AddOrUpdateAlbumContest, `public_add` is not nullable")
      }

      if (!missing(`visibility`) && is.null(`visibility`)) {
        stop("Invalid value for `visibility` when calling ContestApi$AddOrUpdateAlbumContest, `visibility` is not nullable")
      }

      if (!missing(`include_friend_group`) && is.null(`include_friend_group`)) {
        stop("Invalid value for `include_friend_group` when calling ContestApi$AddOrUpdateAlbumContest, `include_friend_group` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ContestApi$AddOrUpdateAlbumContest, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ContestApi$AddOrUpdateAlbumContest, `account_id` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling ContestApi$AddOrUpdateAlbumContest, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ContestApi$AddOrUpdateAlbumContest, `app_key` is not nullable")
      }

      if (!missing(`contest_type`) && is.null(`contest_type`)) {
        stop("Invalid value for `contest_type` when calling ContestApi$AddOrUpdateAlbumContest, `contest_type` is not nullable")
      }

      if (!missing(`album_contest_id`) && is.null(`album_contest_id`)) {
        stop("Invalid value for `album_contest_id` when calling ContestApi$AddOrUpdateAlbumContest, `album_contest_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling ContestApi$AddOrUpdateAlbumContest, `title` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling ContestApi$AddOrUpdateAlbumContest, `description` is not nullable")
      }

      if (!missing(`album_id1`) && is.null(`album_id1`)) {
        stop("Invalid value for `album_id1` when calling ContestApi$AddOrUpdateAlbumContest, `album_id1` is not nullable")
      }

      if (!missing(`remove_album1`) && is.null(`remove_album1`)) {
        stop("Invalid value for `remove_album1` when calling ContestApi$AddOrUpdateAlbumContest, `remove_album1` is not nullable")
      }

      if (!missing(`album_id2`) && is.null(`album_id2`)) {
        stop("Invalid value for `album_id2` when calling ContestApi$AddOrUpdateAlbumContest, `album_id2` is not nullable")
      }

      if (!missing(`remove_album2`) && is.null(`remove_album2`)) {
        stop("Invalid value for `remove_album2` when calling ContestApi$AddOrUpdateAlbumContest, `remove_album2` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling ContestApi$AddOrUpdateAlbumContest, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling ContestApi$AddOrUpdateAlbumContest, `end_date` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling ContestApi$AddOrUpdateAlbumContest, `location_description` is not nullable")
      }

      if (!missing(`connection_ids_to_add`) && is.null(`connection_ids_to_add`)) {
        stop("Invalid value for `connection_ids_to_add` when calling ContestApi$AddOrUpdateAlbumContest, `connection_ids_to_add` is not nullable")
      }

      if (!missing(`connection_group_ids_to_add`) && is.null(`connection_group_ids_to_add`)) {
        stop("Invalid value for `connection_group_ids_to_add` when calling ContestApi$AddOrUpdateAlbumContest, `connection_group_ids_to_add` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ContestApi$AddOrUpdateAlbumContest, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ContestApi$AddOrUpdateAlbumContest, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["contestType"]] <- `contest_type`

      query_params[["albumContestId"]] <- `album_contest_id`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["albumId1"]] <- `album_id1`

      query_params[["removeAlbum1"]] <- `remove_album1`

      query_params[["albumId2"]] <- `album_id2`

      query_params[["removeAlbum2"]] <- `remove_album2`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["publicRead"]] <- `public_read`

      query_params[["publicWrite"]] <- `public_write`

      query_params[["publicDelete"]] <- `public_delete`

      query_params[["publicAdd"]] <- `public_add`

      query_params[["locationDescription"]] <- `location_description`

      if (!is.null(`visibility`) && !(`visibility` %in% c("PUBLIC", "PRIVATE", "FRIENDS"))) {
        stop("Invalid value for visibility when calling ContestApi$AddOrUpdateAlbumContest. Must be [PUBLIC, PRIVATE, FRIENDS].")
      }
      query_params[["visibility"]] <- `visibility`

      query_params[["connectionIdsToAdd"]] <- `connection_ids_to_add`

      query_params[["connectionGroupIdsToAdd"]] <- `connection_group_ids_to_add`

      query_params[["includeFriendGroup"]] <- `include_friend_group`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/consumer/album/contest"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AlbumContestResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Approve Contest
    #'
    #' @param album_contest_id The ID of the album contest
    #' @param approval_status The approval status to set {PENDING, REJECTED, APPROVED, FEATURED}
    #' @param device_id (optional) A unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    ApproveAlbumContest = function(album_contest_id, approval_status, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ApproveAlbumContestWithHttpInfo(album_contest_id, approval_status, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Approve Contest
    #'
    #' @param album_contest_id The ID of the album contest
    #' @param approval_status The approval status to set {PENDING, REJECTED, APPROVED, FEATURED}
    #' @param device_id (optional) A unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    ApproveAlbumContestWithHttpInfo = function(album_contest_id, approval_status, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`album_contest_id`)) {
        stop("Missing required parameter `album_contest_id`.")
      }

      if (missing(`approval_status`)) {
        stop("Missing required parameter `approval_status`.")
      }

      if (!missing(`album_contest_id`) && is.null(`album_contest_id`)) {
        stop("Invalid value for `album_contest_id` when calling ContestApi$ApproveAlbumContest, `album_contest_id` is not nullable")
      }

      if (!missing(`approval_status`) && is.null(`approval_status`)) {
        stop("Invalid value for `approval_status` when calling ContestApi$ApproveAlbumContest, `approval_status` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ContestApi$ApproveAlbumContest, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ContestApi$ApproveAlbumContest, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["albumContestId"]] <- `album_contest_id`

      if (!is.null(`approval_status`) && !(`approval_status` %in% c("PENDING", "REJECTED", "APPROVED", "FEATURED"))) {
        stop("Invalid value for approval_status when calling ContestApi$ApproveAlbumContest. Must be [PENDING, REJECTED, APPROVED, FEATURED].")
      }
      query_params[["approvalStatus"]] <- `approval_status`

      local_var_url_path <- "/consumer/album/contest/approve"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Contest
    #'
    #' @param album_contest_id the album contest ID
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteContest = function(album_contest_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteContestWithHttpInfo(album_contest_id, device_id, account_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Contest
    #'
    #' @param album_contest_id the album contest ID
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteContestWithHttpInfo = function(album_contest_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`album_contest_id`)) {
        stop("Missing required parameter `album_contest_id`.")
      }

      if (!missing(`album_contest_id`) && is.null(`album_contest_id`)) {
        stop("Invalid value for `album_contest_id` when calling ContestApi$DeleteContest, `album_contest_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ContestApi$DeleteContest, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ContestApi$DeleteContest, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ContestApi$DeleteContest, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ContestApi$DeleteContest, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["albumContestId"]] <- `album_contest_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/consumer/album/contest/remove"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Contest
    #'
    #' @param album_contest_id the album contest ID
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AlbumContestResponse
    GetAlbumContest = function(album_contest_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetAlbumContestWithHttpInfo(album_contest_id, device_id, account_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Contest
    #'
    #' @param album_contest_id the album contest ID
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AlbumContestResponse) with additional information such as HTTP status code, headers
    GetAlbumContestWithHttpInfo = function(album_contest_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`album_contest_id`)) {
        stop("Missing required parameter `album_contest_id`.")
      }

      if (!missing(`album_contest_id`) && is.null(`album_contest_id`)) {
        stop("Invalid value for `album_contest_id` when calling ContestApi$GetAlbumContest, `album_contest_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ContestApi$GetAlbumContest, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ContestApi$GetAlbumContest, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ContestApi$GetAlbumContest, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ContestApi$GetAlbumContest, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["albumContestId"]] <- `album_contest_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/consumer/album/contest/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AlbumContestResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Contests
    #'
    #' @param filter a comma separated list of Ownership
    #' @param sort_field the field to sort by. See AlbumContestApiMap
    #' @param descending determines whether the sorted list is in descending or ascending order
    #' @param start the start index for pagination
    #' @param limit the limit for pagination (there is a hard limit of 30)
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param game_type (optional) This parameter is deprecated.
    #' @param app_key (optional) the application key
    #' @param app_type (optional) the application type
    #' @param contest_type (optional) filter contests with this contest type
    #' @param owner_id (optional) search on contests that have been created by this account (that the user has permissions to)
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) keyword search string
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param date_created (optional) filter on items that have been created before this date
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AlbumContestListResponse
    GetAlbumContests = function(filter, sort_field, descending, start, limit, device_id = NULL, account_id = NULL, game_type = NULL, app_key = NULL, app_type = NULL, contest_type = NULL, owner_id = NULL, q = NULL, keyword = NULL, _i = NULL, _l = NULL, date_created = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetAlbumContestsWithHttpInfo(filter, sort_field, descending, start, limit, device_id, account_id, game_type, app_key, app_type, contest_type, owner_id, q, keyword, _i, _l, date_created, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Contests
    #'
    #' @param filter a comma separated list of Ownership
    #' @param sort_field the field to sort by. See AlbumContestApiMap
    #' @param descending determines whether the sorted list is in descending or ascending order
    #' @param start the start index for pagination
    #' @param limit the limit for pagination (there is a hard limit of 30)
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param game_type (optional) This parameter is deprecated.
    #' @param app_key (optional) the application key
    #' @param app_type (optional) the application type
    #' @param contest_type (optional) filter contests with this contest type
    #' @param owner_id (optional) search on contests that have been created by this account (that the user has permissions to)
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) keyword search string
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param date_created (optional) filter on items that have been created before this date
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AlbumContestListResponse) with additional information such as HTTP status code, headers
    GetAlbumContestsWithHttpInfo = function(filter, sort_field, descending, start, limit, device_id = NULL, account_id = NULL, game_type = NULL, app_key = NULL, app_type = NULL, contest_type = NULL, owner_id = NULL, q = NULL, keyword = NULL, _i = NULL, _l = NULL, date_created = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`filter`)) {
        stop("Missing required parameter `filter`.")
      }

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`filter`) && is.null(`filter`)) {
        stop("Invalid value for `filter` when calling ContestApi$GetAlbumContests, `filter` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling ContestApi$GetAlbumContests, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling ContestApi$GetAlbumContests, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ContestApi$GetAlbumContests, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ContestApi$GetAlbumContests, `limit` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ContestApi$GetAlbumContests, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ContestApi$GetAlbumContests, `account_id` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling ContestApi$GetAlbumContests, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ContestApi$GetAlbumContests, `app_key` is not nullable")
      }

      if (!missing(`app_type`) && is.null(`app_type`)) {
        stop("Invalid value for `app_type` when calling ContestApi$GetAlbumContests, `app_type` is not nullable")
      }

      if (!missing(`contest_type`) && is.null(`contest_type`)) {
        stop("Invalid value for `contest_type` when calling ContestApi$GetAlbumContests, `contest_type` is not nullable")
      }

      if (!missing(`owner_id`) && is.null(`owner_id`)) {
        stop("Invalid value for `owner_id` when calling ContestApi$GetAlbumContests, `owner_id` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling ContestApi$GetAlbumContests, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ContestApi$GetAlbumContests, `keyword` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling ContestApi$GetAlbumContests, `_i` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling ContestApi$GetAlbumContests, `_l` is not nullable")
      }

      if (!missing(`date_created`) && is.null(`date_created`)) {
        stop("Invalid value for `date_created` when calling ContestApi$GetAlbumContests, `date_created` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ContestApi$GetAlbumContests, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ContestApi$GetAlbumContests, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["appType"]] <- `app_type`

      query_params[["contestType"]] <- `contest_type`

      query_params[["ownerId"]] <- `owner_id`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["filter"]] <- `filter`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["dateCreated"]] <- `date_created`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/consumer/album/contest/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AlbumContestListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Vote on Contest
    #'
    #' @param album_contest_id the album contest ID
    #' @param album_id the ID of the album to vote on
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param contest_type (optional) a custom field used for aggregation and searching
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AlbumContestResponse
    VoteOnAlbumContest = function(album_contest_id, album_id, device_id = NULL, account_id = NULL, contest_type = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$VoteOnAlbumContestWithHttpInfo(album_contest_id, album_id, device_id, account_id, contest_type, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Vote on Contest
    #'
    #' @param album_contest_id the album contest ID
    #' @param album_id the ID of the album to vote on
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param contest_type (optional) a custom field used for aggregation and searching
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AlbumContestResponse) with additional information such as HTTP status code, headers
    VoteOnAlbumContestWithHttpInfo = function(album_contest_id, album_id, device_id = NULL, account_id = NULL, contest_type = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`album_contest_id`)) {
        stop("Missing required parameter `album_contest_id`.")
      }

      if (missing(`album_id`)) {
        stop("Missing required parameter `album_id`.")
      }

      if (!missing(`album_contest_id`) && is.null(`album_contest_id`)) {
        stop("Invalid value for `album_contest_id` when calling ContestApi$VoteOnAlbumContest, `album_contest_id` is not nullable")
      }

      if (!missing(`album_id`) && is.null(`album_id`)) {
        stop("Invalid value for `album_id` when calling ContestApi$VoteOnAlbumContest, `album_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ContestApi$VoteOnAlbumContest, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ContestApi$VoteOnAlbumContest, `account_id` is not nullable")
      }

      if (!missing(`contest_type`) && is.null(`contest_type`)) {
        stop("Invalid value for `contest_type` when calling ContestApi$VoteOnAlbumContest, `contest_type` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ContestApi$VoteOnAlbumContest, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ContestApi$VoteOnAlbumContest, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["albumContestId"]] <- `album_contest_id`

      query_params[["albumId"]] <- `album_id`

      query_params[["contestType"]] <- `contest_type`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/consumer/album/contest/vote"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AlbumContestResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
