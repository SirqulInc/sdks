#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title SecureApp operations
#' @description SecureAppApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateSecureApplication  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The unique id of the user making the request
#' var_app_key <- "app_key_example" # character | The application to secure
#' var_key_cert <- File.new('/path/to/file') # data.frame | 
#' var_trust_store <- File.new('/path/to/file') # data.frame | 
#' var_username <- "username_example" # character | 
#' var_password <- "password_example" # character | 
#' var_active <- TRUE # character |  (Optional)
#' var_biometric_type <- "FACIAL" # character | The type of biometric, options are: - FINGERPRINT - IRIS - FACIAL (Optional)
#' var_biometric_position <- "UNKNOWN" # character | The position for the biometric file uploaded (Optional)
#' var_biometric_position2 <- "UNKNOWN" # character | The position for each the biometric2 file uploaded (Optional)
#'
#' #Create Secure Application
#' api_instance <- SecureAppApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateSecureApplication(var_account_id, var_app_key, var_key_cert, var_trust_store, var_username, var_password, active = var_active, biometric_type = var_biometric_type, biometric_position = var_biometric_position, biometric_position2 = var_biometric_position2data_file = "result.txt")
#' result <- api_instance$CreateSecureApplication(var_account_id, var_app_key, var_key_cert, var_trust_store, var_username, var_password, active = var_active, biometric_type = var_biometric_type, biometric_position = var_biometric_position, biometric_position2 = var_biometric_position2)
#' dput(result)
#'
#'
#' ####################  DeleteSecureApplication  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The unique id of the user making the request
#' var_app_key <- "app_key_example" # character | The application to secure
#'
#' #Delete Secure Application
#' api_instance <- SecureAppApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteSecureApplication(var_account_id, var_app_keydata_file = "result.txt")
#' result <- api_instance$DeleteSecureApplication(var_account_id, var_app_key)
#' dput(result)
#'
#'
#' ####################  LoginSecure  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application making the request, defines what type and position is required to make a secure login the request.
#' var_biometric_file <- File.new('/path/to/file') # data.frame | The data file used to perform authentication
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (Optional)
#' var_biometric_file2 <- File.new('/path/to/file') # data.frame | The data file used to perform authentication (Optional)
#' var_age_restriction <- 0 # integer | Checks user's birthday to see if they meet an age requirement. If the user is under age, an error message is returned. (Optional)
#' var_return_profile <- FALSE # character | Determines whether to return a ProfileResponse on success, or a PaymentTypesResponse (Optional)
#' var_response_filters <- "PROFILE" # character | If returnProfile is set to true, this determines how much of the profile should be returned, see ProfileFilters (Optional)
#' var_latitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | Used to update the user's current location (Optional)
#'
#' #Login Clear
#' api_instance <- SecureAppApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LoginSecure(var_app_key, var_biometric_file, device_id = var_device_id, biometric_file2 = var_biometric_file2, age_restriction = var_age_restriction, return_profile = var_return_profile, response_filters = var_response_filters, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$LoginSecure(var_app_key, var_biometric_file, device_id = var_device_id, biometric_file2 = var_biometric_file2, age_restriction = var_age_restriction, return_profile = var_return_profile, response_filters = var_response_filters, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  PurchaseSecure  ####################
#'
#' library(openapi)
#' var_body <- PaymentRequest$new("appKey_example", "clientPaymentId_example", 123, Invoice$new("location_example", 123, 123, c(LineItem$new("description_example", "item_example", 123, 123, 123, 123))), BiometricRequest$new(BiometricImage$new("UNKNOWN", "data_example"), c(FingerprintBiometricImage$new("UNKNOWN", "data_example", "imageFormat_example"))), c(OrderItemRequest$new("OFFER", 123, "offerName_example", "orderCustomType_example", "orderCustomId_example", 123, 123, 123, 123, 123, 123, "message_example")), "acceptedBilling_example") # PaymentRequest | The payment request object
#'
#' #Purchase Clear
#' api_instance <- SecureAppApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PurchaseSecure(var_bodydata_file = "result.txt")
#' result <- api_instance$PurchaseSecure(var_body)
#' dput(result)
#'
#'
#' ####################  ResetSecure  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The unique id of the user making the request
#' var_app_key <- "app_key_example" # character | The application to secure
#'
#' #Rest Secure Application
#' api_instance <- SecureAppApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ResetSecure(var_account_id, var_app_keydata_file = "result.txt")
#' result <- api_instance$ResetSecure(var_account_id, var_app_key)
#' dput(result)
#'
#'
#' ####################  UpdateSecureApplication  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The unique id of the user making the request
#' var_app_key <- "app_key_example" # character | The application to secure
#' var_active <- "active_example" # character |  (Optional)
#' var_key_cert <- File.new('/path/to/file') # data.frame |  (Optional)
#' var_trust_store <- File.new('/path/to/file') # data.frame |  (Optional)
#' var_username <- "username_example" # character |  (Optional)
#' var_password <- "password_example" # character |  (Optional)
#' var_biometric_type <- "biometric_type_example" # character | The type of biometric, options are: - FINGERPRINT - IRIS - FACIAL (Optional)
#' var_biometric_position <- "biometric_position_example" # character | The position for the biometric file uploaded (Optional)
#' var_biometric_position2 <- "biometric_position2_example" # character | The position for each the biometric2 file uploaded (Optional)
#'
#' #Update Secure Application
#' api_instance <- SecureAppApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateSecureApplication(var_account_id, var_app_key, active = var_active, key_cert = var_key_cert, trust_store = var_trust_store, username = var_username, password = var_password, biometric_type = var_biometric_type, biometric_position = var_biometric_position, biometric_position2 = var_biometric_position2data_file = "result.txt")
#' result <- api_instance$UpdateSecureApplication(var_account_id, var_app_key, active = var_active, key_cert = var_key_cert, trust_store = var_trust_store, username = var_username, password = var_password, biometric_type = var_biometric_type, biometric_position = var_biometric_position, biometric_position2 = var_biometric_position2)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SecureAppApi <- R6::R6Class(
  "SecureAppApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new SecureAppApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Secure Application
    #'
    #' @param account_id The unique id of the user making the request
    #' @param app_key The application to secure
    #' @param key_cert 
    #' @param trust_store 
    #' @param username 
    #' @param password 
    #' @param active (optional)  (default value: TRUE)
    #' @param biometric_type (optional) The type of biometric, options are: - FINGERPRINT - IRIS - FACIAL (default value: "FACIAL")
    #' @param biometric_position (optional) The position for the biometric file uploaded (default value: "UNKNOWN")
    #' @param biometric_position2 (optional) The position for each the biometric2 file uploaded (default value: "UNKNOWN")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    CreateSecureApplication = function(account_id, app_key, key_cert, trust_store, username, password, active = TRUE, biometric_type = "FACIAL", biometric_position = "UNKNOWN", biometric_position2 = "UNKNOWN", data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateSecureApplicationWithHttpInfo(account_id, app_key, key_cert, trust_store, username, password, active, biometric_type, biometric_position, biometric_position2, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Secure Application
    #'
    #' @param account_id The unique id of the user making the request
    #' @param app_key The application to secure
    #' @param key_cert 
    #' @param trust_store 
    #' @param username 
    #' @param password 
    #' @param active (optional)  (default value: TRUE)
    #' @param biometric_type (optional) The type of biometric, options are: - FINGERPRINT - IRIS - FACIAL (default value: "FACIAL")
    #' @param biometric_position (optional) The position for the biometric file uploaded (default value: "UNKNOWN")
    #' @param biometric_position2 (optional) The position for each the biometric2 file uploaded (default value: "UNKNOWN")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    CreateSecureApplicationWithHttpInfo = function(account_id, app_key, key_cert, trust_store, username, password, active = TRUE, biometric_type = "FACIAL", biometric_position = "UNKNOWN", biometric_position2 = "UNKNOWN", data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`key_cert`)) {
        stop("Missing required parameter `key_cert`.")
      }

      if (missing(`trust_store`)) {
        stop("Missing required parameter `trust_store`.")
      }

      if (missing(`username`)) {
        stop("Missing required parameter `username`.")
      }

      if (missing(`password`)) {
        stop("Missing required parameter `password`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling SecureAppApi$CreateSecureApplication, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling SecureAppApi$CreateSecureApplication, `app_key` is not nullable")
      }

      if (!missing(`key_cert`) && is.null(`key_cert`)) {
        stop("Invalid value for `key_cert` when calling SecureAppApi$CreateSecureApplication, `key_cert` is not nullable")
      }

      if (!missing(`trust_store`) && is.null(`trust_store`)) {
        stop("Invalid value for `trust_store` when calling SecureAppApi$CreateSecureApplication, `trust_store` is not nullable")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling SecureAppApi$CreateSecureApplication, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling SecureAppApi$CreateSecureApplication, `password` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling SecureAppApi$CreateSecureApplication, `active` is not nullable")
      }

      if (!missing(`biometric_type`) && is.null(`biometric_type`)) {
        stop("Invalid value for `biometric_type` when calling SecureAppApi$CreateSecureApplication, `biometric_type` is not nullable")
      }

      if (!missing(`biometric_position`) && is.null(`biometric_position`)) {
        stop("Invalid value for `biometric_position` when calling SecureAppApi$CreateSecureApplication, `biometric_position` is not nullable")
      }

      if (!missing(`biometric_position2`) && is.null(`biometric_position2`)) {
        stop("Invalid value for `biometric_position2` when calling SecureAppApi$CreateSecureApplication, `biometric_position2` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["active"]] <- `active`

      query_params[["keyCert"]] <- `key_cert`

      query_params[["trustStore"]] <- `trust_store`

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      if (!is.null(`biometric_type`) && !(`biometric_type` %in% c("FINGERPRINT", "IRIS", "FACIAL"))) {
        stop("Invalid value for biometric_type when calling SecureAppApi$CreateSecureApplication. Must be [FINGERPRINT, IRIS, FACIAL].")
      }
      query_params[["biometricType"]] <- `biometric_type`

      if (!is.null(`biometric_position`) && !(`biometric_position` %in% c("UNKNOWN", "LEFT_INDEX", "LEFT_MIDDLE", "LEFT_RING", "LEFT_LITTLE", "LEFT_THUMB", "RIGHT_INDEX", "RIGHT_MIDDLE", "RIGHT_RING", "RIGHT_LITTLE", "RIGHT_THUMB", "LEFT", "RIGHT"))) {
        stop("Invalid value for biometric_position when calling SecureAppApi$CreateSecureApplication. Must be [UNKNOWN, LEFT_INDEX, LEFT_MIDDLE, LEFT_RING, LEFT_LITTLE, LEFT_THUMB, RIGHT_INDEX, RIGHT_MIDDLE, RIGHT_RING, RIGHT_LITTLE, RIGHT_THUMB, LEFT, RIGHT].")
      }
      query_params[["biometricPosition"]] <- `biometric_position`

      if (!is.null(`biometric_position2`) && !(`biometric_position2` %in% c("UNKNOWN", "LEFT_INDEX", "LEFT_MIDDLE", "LEFT_RING", "LEFT_LITTLE", "LEFT_THUMB", "RIGHT_INDEX", "RIGHT_MIDDLE", "RIGHT_RING", "RIGHT_LITTLE", "RIGHT_THUMB", "LEFT", "RIGHT"))) {
        stop("Invalid value for biometric_position2 when calling SecureAppApi$CreateSecureApplication. Must be [UNKNOWN, LEFT_INDEX, LEFT_MIDDLE, LEFT_RING, LEFT_LITTLE, LEFT_THUMB, RIGHT_INDEX, RIGHT_MIDDLE, RIGHT_RING, RIGHT_LITTLE, RIGHT_THUMB, LEFT, RIGHT].")
      }
      query_params[["biometricPosition2"]] <- `biometric_position2`

      local_var_url_path <- "/secure/application/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Secure Application
    #'
    #' @param account_id The unique id of the user making the request
    #' @param app_key The application to secure
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteSecureApplication = function(account_id, app_key, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteSecureApplicationWithHttpInfo(account_id, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Secure Application
    #'
    #' @param account_id The unique id of the user making the request
    #' @param app_key The application to secure
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteSecureApplicationWithHttpInfo = function(account_id, app_key, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling SecureAppApi$DeleteSecureApplication, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling SecureAppApi$DeleteSecureApplication, `app_key` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/secure/application/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Login Clear
    #'
    #' @param app_key The application making the request, defines what type and position is required to make a secure login the request.
    #' @param biometric_file The data file used to perform authentication
    #' @param device_id (optional) The unique id of the device making the request
    #' @param biometric_file2 (optional) The data file used to perform authentication
    #' @param age_restriction (optional) Checks user's birthday to see if they meet an age requirement. If the user is under age, an error message is returned. (default value: 0)
    #' @param return_profile (optional) Determines whether to return a ProfileResponse on success, or a PaymentTypesResponse (default value: FALSE)
    #' @param response_filters (optional) If returnProfile is set to true, this determines how much of the profile should be returned, see ProfileFilters (default value: "PROFILE")
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    LoginSecure = function(app_key, biometric_file, device_id = NULL, biometric_file2 = NULL, age_restriction = 0, return_profile = FALSE, response_filters = "PROFILE", latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$LoginSecureWithHttpInfo(app_key, biometric_file, device_id, biometric_file2, age_restriction, return_profile, response_filters, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Login Clear
    #'
    #' @param app_key The application making the request, defines what type and position is required to make a secure login the request.
    #' @param biometric_file The data file used to perform authentication
    #' @param device_id (optional) The unique id of the device making the request
    #' @param biometric_file2 (optional) The data file used to perform authentication
    #' @param age_restriction (optional) Checks user's birthday to see if they meet an age requirement. If the user is under age, an error message is returned. (default value: 0)
    #' @param return_profile (optional) Determines whether to return a ProfileResponse on success, or a PaymentTypesResponse (default value: FALSE)
    #' @param response_filters (optional) If returnProfile is set to true, this determines how much of the profile should be returned, see ProfileFilters (default value: "PROFILE")
    #' @param latitude (optional) Used to update the user's current location
    #' @param longitude (optional) Used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    LoginSecureWithHttpInfo = function(app_key, biometric_file, device_id = NULL, biometric_file2 = NULL, age_restriction = 0, return_profile = FALSE, response_filters = "PROFILE", latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`biometric_file`)) {
        stop("Missing required parameter `biometric_file`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling SecureAppApi$LoginSecure, `app_key` is not nullable")
      }

      if (!missing(`biometric_file`) && is.null(`biometric_file`)) {
        stop("Invalid value for `biometric_file` when calling SecureAppApi$LoginSecure, `biometric_file` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling SecureAppApi$LoginSecure, `device_id` is not nullable")
      }

      if (!missing(`biometric_file2`) && is.null(`biometric_file2`)) {
        stop("Invalid value for `biometric_file2` when calling SecureAppApi$LoginSecure, `biometric_file2` is not nullable")
      }

      if (!missing(`age_restriction`) && is.null(`age_restriction`)) {
        stop("Invalid value for `age_restriction` when calling SecureAppApi$LoginSecure, `age_restriction` is not nullable")
      }

      if (!missing(`return_profile`) && is.null(`return_profile`)) {
        stop("Invalid value for `return_profile` when calling SecureAppApi$LoginSecure, `return_profile` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling SecureAppApi$LoginSecure, `response_filters` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling SecureAppApi$LoginSecure, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling SecureAppApi$LoginSecure, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["biometricFile"]] <- `biometric_file`

      query_params[["biometricFile2"]] <- `biometric_file2`

      query_params[["ageRestriction"]] <- `age_restriction`

      query_params[["returnProfile"]] <- `return_profile`

      query_params[["responseFilters"]] <- `response_filters`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/secure/login"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Purchase Clear
    #'
    #' @param body The payment request object
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    PurchaseSecure = function(body, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$PurchaseSecureWithHttpInfo(body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Purchase Clear
    #'
    #' @param body The payment request object
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    PurchaseSecureWithHttpInfo = function(body, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`) && is.null(`body`)) {
        stop("Invalid value for `body` when calling SecureAppApi$PurchaseSecure, `body` is not nullable")
      }

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/secure/purchase"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Rest Secure Application
    #'
    #' @param account_id The unique id of the user making the request
    #' @param app_key The application to secure
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    ResetSecure = function(account_id, app_key, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ResetSecureWithHttpInfo(account_id, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Rest Secure Application
    #'
    #' @param account_id The unique id of the user making the request
    #' @param app_key The application to secure
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    ResetSecureWithHttpInfo = function(account_id, app_key, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling SecureAppApi$ResetSecure, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling SecureAppApi$ResetSecure, `app_key` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/secure/application/reset"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Secure Application
    #'
    #' @param account_id The unique id of the user making the request
    #' @param app_key The application to secure
    #' @param active (optional) 
    #' @param key_cert (optional) 
    #' @param trust_store (optional) 
    #' @param username (optional) 
    #' @param password (optional) 
    #' @param biometric_type (optional) The type of biometric, options are: - FINGERPRINT - IRIS - FACIAL
    #' @param biometric_position (optional) The position for the biometric file uploaded
    #' @param biometric_position2 (optional) The position for each the biometric2 file uploaded
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UpdateSecureApplication = function(account_id, app_key, active = NULL, key_cert = NULL, trust_store = NULL, username = NULL, password = NULL, biometric_type = NULL, biometric_position = NULL, biometric_position2 = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateSecureApplicationWithHttpInfo(account_id, app_key, active, key_cert, trust_store, username, password, biometric_type, biometric_position, biometric_position2, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Secure Application
    #'
    #' @param account_id The unique id of the user making the request
    #' @param app_key The application to secure
    #' @param active (optional) 
    #' @param key_cert (optional) 
    #' @param trust_store (optional) 
    #' @param username (optional) 
    #' @param password (optional) 
    #' @param biometric_type (optional) The type of biometric, options are: - FINGERPRINT - IRIS - FACIAL
    #' @param biometric_position (optional) The position for the biometric file uploaded
    #' @param biometric_position2 (optional) The position for each the biometric2 file uploaded
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UpdateSecureApplicationWithHttpInfo = function(account_id, app_key, active = NULL, key_cert = NULL, trust_store = NULL, username = NULL, password = NULL, biometric_type = NULL, biometric_position = NULL, biometric_position2 = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling SecureAppApi$UpdateSecureApplication, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling SecureAppApi$UpdateSecureApplication, `app_key` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling SecureAppApi$UpdateSecureApplication, `active` is not nullable")
      }

      if (!missing(`key_cert`) && is.null(`key_cert`)) {
        stop("Invalid value for `key_cert` when calling SecureAppApi$UpdateSecureApplication, `key_cert` is not nullable")
      }

      if (!missing(`trust_store`) && is.null(`trust_store`)) {
        stop("Invalid value for `trust_store` when calling SecureAppApi$UpdateSecureApplication, `trust_store` is not nullable")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling SecureAppApi$UpdateSecureApplication, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling SecureAppApi$UpdateSecureApplication, `password` is not nullable")
      }

      if (!missing(`biometric_type`) && is.null(`biometric_type`)) {
        stop("Invalid value for `biometric_type` when calling SecureAppApi$UpdateSecureApplication, `biometric_type` is not nullable")
      }

      if (!missing(`biometric_position`) && is.null(`biometric_position`)) {
        stop("Invalid value for `biometric_position` when calling SecureAppApi$UpdateSecureApplication, `biometric_position` is not nullable")
      }

      if (!missing(`biometric_position2`) && is.null(`biometric_position2`)) {
        stop("Invalid value for `biometric_position2` when calling SecureAppApi$UpdateSecureApplication, `biometric_position2` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["active"]] <- `active`

      query_params[["keyCert"]] <- `key_cert`

      query_params[["trustStore"]] <- `trust_store`

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      if (!is.null(`biometric_type`) && !(`biometric_type` %in% c("FINGERPRINT", "IRIS", "FACIAL"))) {
        stop("Invalid value for biometric_type when calling SecureAppApi$UpdateSecureApplication. Must be [FINGERPRINT, IRIS, FACIAL].")
      }
      query_params[["biometricType"]] <- `biometric_type`

      if (!is.null(`biometric_position`) && !(`biometric_position` %in% c("UNKNOWN", "LEFT_INDEX", "LEFT_MIDDLE", "LEFT_RING", "LEFT_LITTLE", "LEFT_THUMB", "RIGHT_INDEX", "RIGHT_MIDDLE", "RIGHT_RING", "RIGHT_LITTLE", "RIGHT_THUMB", "LEFT", "RIGHT"))) {
        stop("Invalid value for biometric_position when calling SecureAppApi$UpdateSecureApplication. Must be [UNKNOWN, LEFT_INDEX, LEFT_MIDDLE, LEFT_RING, LEFT_LITTLE, LEFT_THUMB, RIGHT_INDEX, RIGHT_MIDDLE, RIGHT_RING, RIGHT_LITTLE, RIGHT_THUMB, LEFT, RIGHT].")
      }
      query_params[["biometricPosition"]] <- `biometric_position`

      if (!is.null(`biometric_position2`) && !(`biometric_position2` %in% c("UNKNOWN", "LEFT_INDEX", "LEFT_MIDDLE", "LEFT_RING", "LEFT_LITTLE", "LEFT_THUMB", "RIGHT_INDEX", "RIGHT_MIDDLE", "RIGHT_RING", "RIGHT_LITTLE", "RIGHT_THUMB", "LEFT", "RIGHT"))) {
        stop("Invalid value for biometric_position2 when calling SecureAppApi$UpdateSecureApplication. Must be [UNKNOWN, LEFT_INDEX, LEFT_MIDDLE, LEFT_RING, LEFT_LITTLE, LEFT_THUMB, RIGHT_INDEX, RIGHT_MIDDLE, RIGHT_RING, RIGHT_LITTLE, RIGHT_THUMB, LEFT, RIGHT].")
      }
      query_params[["biometricPosition2"]] <- `biometric_position2`

      local_var_url_path <- "/secure/application/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
