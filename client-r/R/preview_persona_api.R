#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title PreviewPersona operations
#' @description PreviewPersonaApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreatePersona  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account ID of the user
#' var_title <- "title_example" # character | the title of the persona
#' var_preview_accounts <- "preview_accounts_example" # character | the accounts that are able to preview from this persona (Optional)
#' var_date <- 56 # integer | the sepcified date that this persona is viewing the app (Optional)
#' var_age <- 56 # integer | the specified age of this persona (Optional)
#' var_gender <- "gender_example" # character | the specified gender of this persona (Optional)
#' var_game_experience_level <- "game_experience_level_example" # character | the specified experience level of the persona (Optional)
#' var_latitude <- 3.4 # numeric | the specified latitude of the persona (Optional)
#' var_longitude <- 3.4 # numeric | the specified longitude of the persona (Optional)
#'
#' #Create Persona
#' api_instance <- PreviewPersonaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreatePersona(var_account_id, var_title, preview_accounts = var_preview_accounts, date = var_date, age = var_age, gender = var_gender, game_experience_level = var_game_experience_level, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$CreatePersona(var_account_id, var_title, preview_accounts = var_preview_accounts, date = var_date, age = var_age, gender = var_gender, game_experience_level = var_game_experience_level, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  DeletePersona  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account id of the user
#' var_persona_id <- 56 # integer | the id of the persona to delete
#'
#' #Delete Persona
#' api_instance <- PreviewPersonaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeletePersona(var_account_id, var_persona_iddata_file = "result.txt")
#' result <- api_instance$DeletePersona(var_account_id, var_persona_id)
#' dput(result)
#'
#'
#' ####################  GetPersonaList  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account ID of the user
#' var_persona_id <- 56 # integer | the persona ID of the persona
#'
#' #Get Persona
#' api_instance <- PreviewPersonaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPersonaList(var_account_id, var_persona_iddata_file = "result.txt")
#' result <- api_instance$GetPersonaList(var_account_id, var_persona_id)
#' dput(result)
#'
#'
#' ####################  SearchPersona  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account ID of the user
#' var_start <- 56 # integer | the start index for pagination
#' var_limit <- 56 # integer | the limit for pagination (There is a hard limit of 100)
#'
#' #Search Personas
#' api_instance <- PreviewPersonaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchPersona(var_account_id, var_start, var_limitdata_file = "result.txt")
#' result <- api_instance$SearchPersona(var_account_id, var_start, var_limit)
#' dput(result)
#'
#'
#' ####################  UpdatePersona  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account ID of the user
#' var_persona_id <- 56 # integer | the persona ID of the persona to update
#' var_title <- "title_example" # character | the title of the persona (Optional)
#' var_preview_accounts <- "preview_accounts_example" # character | the accounts that are able to preview from this persona (Optional)
#' var_active <- "active_example" # character | the status of the persona, there can only be one active persona per account (Optional)
#' var_date <- 56 # integer | the sepcified date that this persona is viewing the app (Optional)
#' var_age <- 56 # integer | the specified age of this persona (Optional)
#' var_gender <- "gender_example" # character | the specified gender of this persona (Optional)
#' var_game_experience_level <- "game_experience_level_example" # character | the specified experience level of the persona (Optional)
#' var_latitude <- 3.4 # numeric | the specified latitude of the persona (Optional)
#' var_longitude <- 3.4 # numeric | the specified longitude of the persona (Optional)
#'
#' #Update Persona
#' api_instance <- PreviewPersonaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdatePersona(var_account_id, var_persona_id, title = var_title, preview_accounts = var_preview_accounts, active = var_active, date = var_date, age = var_age, gender = var_gender, game_experience_level = var_game_experience_level, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$UpdatePersona(var_account_id, var_persona_id, title = var_title, preview_accounts = var_preview_accounts, active = var_active, date = var_date, age = var_age, gender = var_gender, game_experience_level = var_game_experience_level, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
PreviewPersonaApi <- R6::R6Class(
  "PreviewPersonaApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new PreviewPersonaApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Persona
    #'
    #' @param account_id the account ID of the user
    #' @param title the title of the persona
    #' @param preview_accounts (optional) the accounts that are able to preview from this persona
    #' @param date (optional) the sepcified date that this persona is viewing the app
    #' @param age (optional) the specified age of this persona
    #' @param gender (optional) the specified gender of this persona
    #' @param game_experience_level (optional) the specified experience level of the persona
    #' @param latitude (optional) the specified latitude of the persona
    #' @param longitude (optional) the specified longitude of the persona
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PreviewPersonaResponse
    CreatePersona = function(account_id, title, preview_accounts = NULL, date = NULL, age = NULL, gender = NULL, game_experience_level = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreatePersonaWithHttpInfo(account_id, title, preview_accounts, date, age, gender, game_experience_level, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Persona
    #'
    #' @param account_id the account ID of the user
    #' @param title the title of the persona
    #' @param preview_accounts (optional) the accounts that are able to preview from this persona
    #' @param date (optional) the sepcified date that this persona is viewing the app
    #' @param age (optional) the specified age of this persona
    #' @param gender (optional) the specified gender of this persona
    #' @param game_experience_level (optional) the specified experience level of the persona
    #' @param latitude (optional) the specified latitude of the persona
    #' @param longitude (optional) the specified longitude of the persona
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PreviewPersonaResponse) with additional information such as HTTP status code, headers
    CreatePersonaWithHttpInfo = function(account_id, title, preview_accounts = NULL, date = NULL, age = NULL, gender = NULL, game_experience_level = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`title`)) {
        stop("Missing required parameter `title`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PreviewPersonaApi$CreatePersona, `account_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling PreviewPersonaApi$CreatePersona, `title` is not nullable")
      }

      if (!missing(`preview_accounts`) && is.null(`preview_accounts`)) {
        stop("Invalid value for `preview_accounts` when calling PreviewPersonaApi$CreatePersona, `preview_accounts` is not nullable")
      }

      if (!missing(`date`) && is.null(`date`)) {
        stop("Invalid value for `date` when calling PreviewPersonaApi$CreatePersona, `date` is not nullable")
      }

      if (!missing(`age`) && is.null(`age`)) {
        stop("Invalid value for `age` when calling PreviewPersonaApi$CreatePersona, `age` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling PreviewPersonaApi$CreatePersona, `gender` is not nullable")
      }

      if (!missing(`game_experience_level`) && is.null(`game_experience_level`)) {
        stop("Invalid value for `game_experience_level` when calling PreviewPersonaApi$CreatePersona, `game_experience_level` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling PreviewPersonaApi$CreatePersona, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling PreviewPersonaApi$CreatePersona, `longitude` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["title"]] <- `title`

      query_params[["previewAccounts"]] <- `preview_accounts`

      query_params[["date"]] <- `date`

      query_params[["age"]] <- `age`

      query_params[["gender"]] <- `gender`

      query_params[["gameExperienceLevel"]] <- `game_experience_level`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/persona/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PreviewPersonaResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Persona
    #'
    #' @param account_id the account id of the user
    #' @param persona_id the id of the persona to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeletePersona = function(account_id, persona_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeletePersonaWithHttpInfo(account_id, persona_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Persona
    #'
    #' @param account_id the account id of the user
    #' @param persona_id the id of the persona to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeletePersonaWithHttpInfo = function(account_id, persona_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`persona_id`)) {
        stop("Missing required parameter `persona_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PreviewPersonaApi$DeletePersona, `account_id` is not nullable")
      }

      if (!missing(`persona_id`) && is.null(`persona_id`)) {
        stop("Invalid value for `persona_id` when calling PreviewPersonaApi$DeletePersona, `persona_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["personaId"]] <- `persona_id`

      local_var_url_path <- "/persona/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Persona
    #'
    #' @param account_id the account ID of the user
    #' @param persona_id the persona ID of the persona
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PreviewPersonaResponse
    GetPersonaList = function(account_id, persona_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetPersonaListWithHttpInfo(account_id, persona_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Persona
    #'
    #' @param account_id the account ID of the user
    #' @param persona_id the persona ID of the persona
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PreviewPersonaResponse) with additional information such as HTTP status code, headers
    GetPersonaListWithHttpInfo = function(account_id, persona_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`persona_id`)) {
        stop("Missing required parameter `persona_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PreviewPersonaApi$GetPersonaList, `account_id` is not nullable")
      }

      if (!missing(`persona_id`) && is.null(`persona_id`)) {
        stop("Invalid value for `persona_id` when calling PreviewPersonaApi$GetPersonaList, `persona_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["personaId"]] <- `persona_id`

      local_var_url_path <- "/persona/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PreviewPersonaResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Personas
    #'
    #' @param account_id the account ID of the user
    #' @param start the start index for pagination
    #' @param limit the limit for pagination (There is a hard limit of 100)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PreviewPersonaResponse
    SearchPersona = function(account_id, start, limit, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchPersonaWithHttpInfo(account_id, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Personas
    #'
    #' @param account_id the account ID of the user
    #' @param start the start index for pagination
    #' @param limit the limit for pagination (There is a hard limit of 100)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PreviewPersonaResponse) with additional information such as HTTP status code, headers
    SearchPersonaWithHttpInfo = function(account_id, start, limit, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PreviewPersonaApi$SearchPersona, `account_id` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling PreviewPersonaApi$SearchPersona, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling PreviewPersonaApi$SearchPersona, `limit` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/persona/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PreviewPersonaResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Persona
    #'
    #' @param account_id the account ID of the user
    #' @param persona_id the persona ID of the persona to update
    #' @param title (optional) the title of the persona
    #' @param preview_accounts (optional) the accounts that are able to preview from this persona
    #' @param active (optional) the status of the persona, there can only be one active persona per account
    #' @param date (optional) the sepcified date that this persona is viewing the app
    #' @param age (optional) the specified age of this persona
    #' @param gender (optional) the specified gender of this persona
    #' @param game_experience_level (optional) the specified experience level of the persona
    #' @param latitude (optional) the specified latitude of the persona
    #' @param longitude (optional) the specified longitude of the persona
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PreviewPersonaResponse
    UpdatePersona = function(account_id, persona_id, title = NULL, preview_accounts = NULL, active = NULL, date = NULL, age = NULL, gender = NULL, game_experience_level = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdatePersonaWithHttpInfo(account_id, persona_id, title, preview_accounts, active, date, age, gender, game_experience_level, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Persona
    #'
    #' @param account_id the account ID of the user
    #' @param persona_id the persona ID of the persona to update
    #' @param title (optional) the title of the persona
    #' @param preview_accounts (optional) the accounts that are able to preview from this persona
    #' @param active (optional) the status of the persona, there can only be one active persona per account
    #' @param date (optional) the sepcified date that this persona is viewing the app
    #' @param age (optional) the specified age of this persona
    #' @param gender (optional) the specified gender of this persona
    #' @param game_experience_level (optional) the specified experience level of the persona
    #' @param latitude (optional) the specified latitude of the persona
    #' @param longitude (optional) the specified longitude of the persona
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PreviewPersonaResponse) with additional information such as HTTP status code, headers
    UpdatePersonaWithHttpInfo = function(account_id, persona_id, title = NULL, preview_accounts = NULL, active = NULL, date = NULL, age = NULL, gender = NULL, game_experience_level = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`persona_id`)) {
        stop("Missing required parameter `persona_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PreviewPersonaApi$UpdatePersona, `account_id` is not nullable")
      }

      if (!missing(`persona_id`) && is.null(`persona_id`)) {
        stop("Invalid value for `persona_id` when calling PreviewPersonaApi$UpdatePersona, `persona_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling PreviewPersonaApi$UpdatePersona, `title` is not nullable")
      }

      if (!missing(`preview_accounts`) && is.null(`preview_accounts`)) {
        stop("Invalid value for `preview_accounts` when calling PreviewPersonaApi$UpdatePersona, `preview_accounts` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling PreviewPersonaApi$UpdatePersona, `active` is not nullable")
      }

      if (!missing(`date`) && is.null(`date`)) {
        stop("Invalid value for `date` when calling PreviewPersonaApi$UpdatePersona, `date` is not nullable")
      }

      if (!missing(`age`) && is.null(`age`)) {
        stop("Invalid value for `age` when calling PreviewPersonaApi$UpdatePersona, `age` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling PreviewPersonaApi$UpdatePersona, `gender` is not nullable")
      }

      if (!missing(`game_experience_level`) && is.null(`game_experience_level`)) {
        stop("Invalid value for `game_experience_level` when calling PreviewPersonaApi$UpdatePersona, `game_experience_level` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling PreviewPersonaApi$UpdatePersona, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling PreviewPersonaApi$UpdatePersona, `longitude` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["personaId"]] <- `persona_id`

      query_params[["title"]] <- `title`

      query_params[["previewAccounts"]] <- `preview_accounts`

      query_params[["active"]] <- `active`

      query_params[["date"]] <- `date`

      query_params[["age"]] <- `age`

      query_params[["gender"]] <- `gender`

      query_params[["gameExperienceLevel"]] <- `game_experience_level`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/persona/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PreviewPersonaResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
