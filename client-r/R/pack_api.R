#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Pack operations
#' @description PackApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreatePack  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_title <- "title_example" # character | The title of the pack
#' var_pack_order <- 56 # integer | The order of the pack
#' var_price <- 56 # integer | The price in tickets to purchase this pack
#' var_highest <- "highest_example" # character | The scoring is highest is best
#' var_allocate_tickets <- "allocate_tickets_example" # character | Flag to indicate owner should receive tickets for completed packs
#' var_ticket_count <- 56 # integer | The number of tickets to reward
#' var_description <- "description_example" # character | The description of the pack (Optional)
#' var_search_tags <- "search_tags_example" # character | The tags for searching the pack, comma separated (Optional)
#' var_active <- "active_example" # character | Activate/deactivate the pack (Optional)
#' var_game_type <- "game_type_example" # character | This parameter is deprecated. (Optional)
#' var_app_key <- "app_key_example" # character | The application key used to identify the application (Optional)
#' var_pack_type <- "pack_type_example" # character | The type of the pack (Optional)
#' var_sequence_type <- "sequence_type_example" # character | The type of game sequence of the pack (Optional)
#' var_background_id <- 56 # integer | The background asset id of the pack (Optional)
#' var_image_id <- 56 # integer | The image asset id of the pack (Optional)
#' var_start_date <- 56 # integer | The date/time to start the pack, send 0 to unset (Optional)
#' var_end_date <- 56 # integer | The date/time to end the pack, send 0 to unset (Optional)
#' var_author_override <- "author_override_example" # character |  (Optional)
#' var_price_type <- "price_type_example" # character | the type of ticket required to purchase this pack (Optional)
#' var_game_level_ids <- "game_level_ids_example" # character | the game level ids to include in the pack, comma separated (Optional)
#' var_in_game <- "in_game_example" # character |  (Optional)
#' var_ticket_type <- "ticket_type_example" # character | The type of ticket to reward, null means default type (Optional)
#' var_points <- 56 # integer | The number of points to award for completing a pack (Optional)
#'
#' #Create Pack
#' api_instance <- PackApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreatePack(var_account_id, var_title, var_pack_order, var_price, var_highest, var_allocate_tickets, var_ticket_count, description = var_description, search_tags = var_search_tags, active = var_active, game_type = var_game_type, app_key = var_app_key, pack_type = var_pack_type, sequence_type = var_sequence_type, background_id = var_background_id, image_id = var_image_id, start_date = var_start_date, end_date = var_end_date, author_override = var_author_override, price_type = var_price_type, game_level_ids = var_game_level_ids, in_game = var_in_game, ticket_type = var_ticket_type, points = var_pointsdata_file = "result.txt")
#' result <- api_instance$CreatePack(var_account_id, var_title, var_pack_order, var_price, var_highest, var_allocate_tickets, var_ticket_count, description = var_description, search_tags = var_search_tags, active = var_active, game_type = var_game_type, app_key = var_app_key, pack_type = var_pack_type, sequence_type = var_sequence_type, background_id = var_background_id, image_id = var_image_id, start_date = var_start_date, end_date = var_end_date, author_override = var_author_override, price_type = var_price_type, game_level_ids = var_game_level_ids, in_game = var_in_game, ticket_type = var_ticket_type, points = var_points)
#' dput(result)
#'
#'
#' ####################  DeletePack  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_pack_id <- 56 # integer | the id of the pack to delete
#'
#' #Delete Pack
#' api_instance <- PackApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeletePack(var_account_id, var_pack_iddata_file = "result.txt")
#' result <- api_instance$DeletePack(var_account_id, var_pack_id)
#' dput(result)
#'
#'
#' ####################  GetPack  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_pack_id <- 56 # integer | The id of the pack to return.
#' var_include_game_data <- "include_game_data_example" # character | If true include the game level data, otherwise don't. default is false.
#'
#' #Get Pack
#' api_instance <- PackApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPack(var_account_id, var_pack_id, var_include_game_datadata_file = "result.txt")
#' result <- api_instance$GetPack(var_account_id, var_pack_id, var_include_game_data)
#' dput(result)
#'
#'
#' ####################  SearchPacks  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_sort_field <- "sort_field_example" # character | The field to sort by. Possible values include: TITLE, DESCRIPTION, CREATED, UPDATED
#' var_descending <- "descending_example" # character | Determines whether the sorted list is in descending or ascending order
#' var_keyword <- "keyword_example" # character | Keyword search on the pack name (Optional)
#' var_pack_type <- "pack_type_example" # character | Filters results on pack type (Optional)
#' var_start <- 56 # integer | Start the result set at some index. (Optional)
#' var_limit <- 56 # integer | Limit the result to some number. (Optional)
#' var_include_game_data <- "include_game_data_example" # character | Determines whether to include game data in the results (Optional)
#' var_include_inactive <- "include_inactive_example" # character | Determines whether to include inactive results (Optional)
#' var_app_key <- "app_key_example" # character | The application to filter results on (Optional)
#'
#' #Search Packs
#' api_instance <- PackApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchPacks(var_account_id, var_sort_field, var_descending, keyword = var_keyword, pack_type = var_pack_type, start = var_start, limit = var_limit, include_game_data = var_include_game_data, include_inactive = var_include_inactive, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$SearchPacks(var_account_id, var_sort_field, var_descending, keyword = var_keyword, pack_type = var_pack_type, start = var_start, limit = var_limit, include_game_data = var_include_game_data, include_inactive = var_include_inactive, app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  UpdatePack  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_pack_id <- 56 # integer | The id of the pack to update.
#' var_allocate_tickets <- "allocate_tickets_example" # character | Flag to indicate owner should receive tickets for completed packs
#' var_ticket_count <- 56 # integer | The number of tickets to reward
#' var_title <- "title_example" # character | The title of the pack (Optional)
#' var_description <- "description_example" # character | The description of the pack (Optional)
#' var_search_tags <- "search_tags_example" # character | The tags for searching the pack, comma separated (Optional)
#' var_active <- "active_example" # character | Activate/deactivate the pack (Optional)
#' var_game_type <- "game_type_example" # character | This parameter is deprecated. (Optional)
#' var_app_key <- "app_key_example" # character | The application key used to identify the application (Optional)
#' var_pack_type <- "pack_type_example" # character | The type of the pack (Optional)
#' var_pack_order <- 56 # integer | The order of the pack (Optional)
#' var_sequence_type <- "sequence_type_example" # character | The type of game sequence of the pack (Optional)
#' var_background_id <- 56 # integer | The background asset id of the pack (Optional)
#' var_image_id <- 56 # integer | The image asset id of the pack (Optional)
#' var_start_date <- 56 # integer | The date/time to start the pack, send 0 to unset (Optional)
#' var_end_date <- 56 # integer | The date/time to end the pack, send 0 to unset (Optional)
#' var_author_override <- "author_override_example" # character |  (Optional)
#' var_price <- 56 # integer | The price in tickets to purchase this pack (Optional)
#' var_price_type <- "price_type_example" # character | the type of ticket required to purchase this pack (Optional)
#' var_game_level_ids <- "game_level_ids_example" # character | the game level ids to include in the pack, comma separated (Optional)
#' var_in_game <- "in_game_example" # character |  (Optional)
#' var_highest <- "highest_example" # character | The scoring is highest is best (Optional)
#' var_ticket_type <- "ticket_type_example" # character | The type of ticket to reward, null means default type (Optional)
#' var_points <- 56 # integer | The number of points to award for completing a pack (Optional)
#'
#' #Update Pack
#' api_instance <- PackApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdatePack(var_account_id, var_pack_id, var_allocate_tickets, var_ticket_count, title = var_title, description = var_description, search_tags = var_search_tags, active = var_active, game_type = var_game_type, app_key = var_app_key, pack_type = var_pack_type, pack_order = var_pack_order, sequence_type = var_sequence_type, background_id = var_background_id, image_id = var_image_id, start_date = var_start_date, end_date = var_end_date, author_override = var_author_override, price = var_price, price_type = var_price_type, game_level_ids = var_game_level_ids, in_game = var_in_game, highest = var_highest, ticket_type = var_ticket_type, points = var_pointsdata_file = "result.txt")
#' result <- api_instance$UpdatePack(var_account_id, var_pack_id, var_allocate_tickets, var_ticket_count, title = var_title, description = var_description, search_tags = var_search_tags, active = var_active, game_type = var_game_type, app_key = var_app_key, pack_type = var_pack_type, pack_order = var_pack_order, sequence_type = var_sequence_type, background_id = var_background_id, image_id = var_image_id, start_date = var_start_date, end_date = var_end_date, author_override = var_author_override, price = var_price, price_type = var_price_type, game_level_ids = var_game_level_ids, in_game = var_in_game, highest = var_highest, ticket_type = var_ticket_type, points = var_points)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
PackApi <- R6::R6Class(
  "PackApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new PackApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Pack
    #'
    #' @param account_id The logged in user.
    #' @param title The title of the pack
    #' @param pack_order The order of the pack
    #' @param price The price in tickets to purchase this pack
    #' @param highest The scoring is highest is best
    #' @param allocate_tickets Flag to indicate owner should receive tickets for completed packs
    #' @param ticket_count The number of tickets to reward
    #' @param description (optional) The description of the pack
    #' @param search_tags (optional) The tags for searching the pack, comma separated
    #' @param active (optional) Activate/deactivate the pack
    #' @param game_type (optional) This parameter is deprecated.
    #' @param app_key (optional) The application key used to identify the application
    #' @param pack_type (optional) The type of the pack
    #' @param sequence_type (optional) The type of game sequence of the pack
    #' @param background_id (optional) The background asset id of the pack
    #' @param image_id (optional) The image asset id of the pack
    #' @param start_date (optional) The date/time to start the pack, send 0 to unset
    #' @param end_date (optional) The date/time to end the pack, send 0 to unset
    #' @param author_override (optional) 
    #' @param price_type (optional) the type of ticket required to purchase this pack
    #' @param game_level_ids (optional) the game level ids to include in the pack, comma separated
    #' @param in_game (optional) 
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a pack
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PackResponse
    CreatePack = function(account_id, title, pack_order, price, highest, allocate_tickets, ticket_count, description = NULL, search_tags = NULL, active = NULL, game_type = NULL, app_key = NULL, pack_type = NULL, sequence_type = NULL, background_id = NULL, image_id = NULL, start_date = NULL, end_date = NULL, author_override = NULL, price_type = NULL, game_level_ids = NULL, in_game = NULL, ticket_type = NULL, points = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreatePackWithHttpInfo(account_id, title, pack_order, price, highest, allocate_tickets, ticket_count, description, search_tags, active, game_type, app_key, pack_type, sequence_type, background_id, image_id, start_date, end_date, author_override, price_type, game_level_ids, in_game, ticket_type, points, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Pack
    #'
    #' @param account_id The logged in user.
    #' @param title The title of the pack
    #' @param pack_order The order of the pack
    #' @param price The price in tickets to purchase this pack
    #' @param highest The scoring is highest is best
    #' @param allocate_tickets Flag to indicate owner should receive tickets for completed packs
    #' @param ticket_count The number of tickets to reward
    #' @param description (optional) The description of the pack
    #' @param search_tags (optional) The tags for searching the pack, comma separated
    #' @param active (optional) Activate/deactivate the pack
    #' @param game_type (optional) This parameter is deprecated.
    #' @param app_key (optional) The application key used to identify the application
    #' @param pack_type (optional) The type of the pack
    #' @param sequence_type (optional) The type of game sequence of the pack
    #' @param background_id (optional) The background asset id of the pack
    #' @param image_id (optional) The image asset id of the pack
    #' @param start_date (optional) The date/time to start the pack, send 0 to unset
    #' @param end_date (optional) The date/time to end the pack, send 0 to unset
    #' @param author_override (optional) 
    #' @param price_type (optional) the type of ticket required to purchase this pack
    #' @param game_level_ids (optional) the game level ids to include in the pack, comma separated
    #' @param in_game (optional) 
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a pack
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PackResponse) with additional information such as HTTP status code, headers
    CreatePackWithHttpInfo = function(account_id, title, pack_order, price, highest, allocate_tickets, ticket_count, description = NULL, search_tags = NULL, active = NULL, game_type = NULL, app_key = NULL, pack_type = NULL, sequence_type = NULL, background_id = NULL, image_id = NULL, start_date = NULL, end_date = NULL, author_override = NULL, price_type = NULL, game_level_ids = NULL, in_game = NULL, ticket_type = NULL, points = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`title`)) {
        stop("Missing required parameter `title`.")
      }

      if (missing(`pack_order`)) {
        stop("Missing required parameter `pack_order`.")
      }

      if (missing(`price`)) {
        stop("Missing required parameter `price`.")
      }

      if (missing(`highest`)) {
        stop("Missing required parameter `highest`.")
      }

      if (missing(`allocate_tickets`)) {
        stop("Missing required parameter `allocate_tickets`.")
      }

      if (missing(`ticket_count`)) {
        stop("Missing required parameter `ticket_count`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PackApi$CreatePack, `account_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling PackApi$CreatePack, `title` is not nullable")
      }

      if (!missing(`pack_order`) && is.null(`pack_order`)) {
        stop("Invalid value for `pack_order` when calling PackApi$CreatePack, `pack_order` is not nullable")
      }

      if (!missing(`price`) && is.null(`price`)) {
        stop("Invalid value for `price` when calling PackApi$CreatePack, `price` is not nullable")
      }

      if (!missing(`highest`) && is.null(`highest`)) {
        stop("Invalid value for `highest` when calling PackApi$CreatePack, `highest` is not nullable")
      }

      if (!missing(`allocate_tickets`) && is.null(`allocate_tickets`)) {
        stop("Invalid value for `allocate_tickets` when calling PackApi$CreatePack, `allocate_tickets` is not nullable")
      }

      if (!missing(`ticket_count`) && is.null(`ticket_count`)) {
        stop("Invalid value for `ticket_count` when calling PackApi$CreatePack, `ticket_count` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling PackApi$CreatePack, `description` is not nullable")
      }

      if (!missing(`search_tags`) && is.null(`search_tags`)) {
        stop("Invalid value for `search_tags` when calling PackApi$CreatePack, `search_tags` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling PackApi$CreatePack, `active` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling PackApi$CreatePack, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling PackApi$CreatePack, `app_key` is not nullable")
      }

      if (!missing(`pack_type`) && is.null(`pack_type`)) {
        stop("Invalid value for `pack_type` when calling PackApi$CreatePack, `pack_type` is not nullable")
      }

      if (!missing(`sequence_type`) && is.null(`sequence_type`)) {
        stop("Invalid value for `sequence_type` when calling PackApi$CreatePack, `sequence_type` is not nullable")
      }

      if (!missing(`background_id`) && is.null(`background_id`)) {
        stop("Invalid value for `background_id` when calling PackApi$CreatePack, `background_id` is not nullable")
      }

      if (!missing(`image_id`) && is.null(`image_id`)) {
        stop("Invalid value for `image_id` when calling PackApi$CreatePack, `image_id` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling PackApi$CreatePack, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling PackApi$CreatePack, `end_date` is not nullable")
      }

      if (!missing(`author_override`) && is.null(`author_override`)) {
        stop("Invalid value for `author_override` when calling PackApi$CreatePack, `author_override` is not nullable")
      }

      if (!missing(`price_type`) && is.null(`price_type`)) {
        stop("Invalid value for `price_type` when calling PackApi$CreatePack, `price_type` is not nullable")
      }

      if (!missing(`game_level_ids`) && is.null(`game_level_ids`)) {
        stop("Invalid value for `game_level_ids` when calling PackApi$CreatePack, `game_level_ids` is not nullable")
      }

      if (!missing(`in_game`) && is.null(`in_game`)) {
        stop("Invalid value for `in_game` when calling PackApi$CreatePack, `in_game` is not nullable")
      }

      if (!missing(`ticket_type`) && is.null(`ticket_type`)) {
        stop("Invalid value for `ticket_type` when calling PackApi$CreatePack, `ticket_type` is not nullable")
      }

      if (!missing(`points`) && is.null(`points`)) {
        stop("Invalid value for `points` when calling PackApi$CreatePack, `points` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["searchTags"]] <- `search_tags`

      query_params[["active"]] <- `active`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      if (!is.null(`pack_type`) && !(`pack_type` %in% c("TUTORIAL", "BUILTIN", "DOWNLOAD", "THRESHOLD", "THEME", "TOURNAMENT"))) {
        stop("Invalid value for pack_type when calling PackApi$CreatePack. Must be [TUTORIAL, BUILTIN, DOWNLOAD, THRESHOLD, THEME, TOURNAMENT].")
      }
      query_params[["packType"]] <- `pack_type`

      query_params[["packOrder"]] <- `pack_order`

      if (!is.null(`sequence_type`) && !(`sequence_type` %in% c("FIRST_AVAILABLE", "ALL_AVAILABLE"))) {
        stop("Invalid value for sequence_type when calling PackApi$CreatePack. Must be [FIRST_AVAILABLE, ALL_AVAILABLE].")
      }
      query_params[["sequenceType"]] <- `sequence_type`

      query_params[["backgroundId"]] <- `background_id`

      query_params[["imageId"]] <- `image_id`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["authorOverride"]] <- `author_override`

      query_params[["price"]] <- `price`

      query_params[["priceType"]] <- `price_type`

      query_params[["gameLevelIds"]] <- `game_level_ids`

      query_params[["inGame"]] <- `in_game`

      query_params[["highest"]] <- `highest`

      query_params[["allocateTickets"]] <- `allocate_tickets`

      query_params[["ticketCount"]] <- `ticket_count`

      query_params[["ticketType"]] <- `ticket_type`

      query_params[["points"]] <- `points`

      local_var_url_path <- "/pack/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PackResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Pack
    #'
    #' @param account_id the id of the logged in user
    #' @param pack_id the id of the pack to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeletePack = function(account_id, pack_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeletePackWithHttpInfo(account_id, pack_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Pack
    #'
    #' @param account_id the id of the logged in user
    #' @param pack_id the id of the pack to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeletePackWithHttpInfo = function(account_id, pack_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`pack_id`)) {
        stop("Missing required parameter `pack_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PackApi$DeletePack, `account_id` is not nullable")
      }

      if (!missing(`pack_id`) && is.null(`pack_id`)) {
        stop("Invalid value for `pack_id` when calling PackApi$DeletePack, `pack_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["packId"]] <- `pack_id`

      local_var_url_path <- "/pack/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Pack
    #'
    #' @param account_id The logged in user.
    #' @param pack_id The id of the pack to return.
    #' @param include_game_data If true include the game level data, otherwise don't. default is false.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PackResponse
    GetPack = function(account_id, pack_id, include_game_data, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetPackWithHttpInfo(account_id, pack_id, include_game_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Pack
    #'
    #' @param account_id The logged in user.
    #' @param pack_id The id of the pack to return.
    #' @param include_game_data If true include the game level data, otherwise don't. default is false.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PackResponse) with additional information such as HTTP status code, headers
    GetPackWithHttpInfo = function(account_id, pack_id, include_game_data, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`pack_id`)) {
        stop("Missing required parameter `pack_id`.")
      }

      if (missing(`include_game_data`)) {
        stop("Missing required parameter `include_game_data`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PackApi$GetPack, `account_id` is not nullable")
      }

      if (!missing(`pack_id`) && is.null(`pack_id`)) {
        stop("Invalid value for `pack_id` when calling PackApi$GetPack, `pack_id` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling PackApi$GetPack, `include_game_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["packId"]] <- `pack_id`

      query_params[["includeGameData"]] <- `include_game_data`

      local_var_url_path <- "/pack/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PackResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Packs
    #'
    #' @param account_id The logged in user.
    #' @param sort_field The field to sort by. Possible values include: TITLE, DESCRIPTION, CREATED, UPDATED
    #' @param descending Determines whether the sorted list is in descending or ascending order
    #' @param keyword (optional) Keyword search on the pack name
    #' @param pack_type (optional) Filters results on pack type
    #' @param start (optional) Start the result set at some index.
    #' @param limit (optional) Limit the result to some number.
    #' @param include_game_data (optional) Determines whether to include game data in the results
    #' @param include_inactive (optional) Determines whether to include inactive results
    #' @param app_key (optional) The application to filter results on
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[PackResponse]
    SearchPacks = function(account_id, sort_field, descending, keyword = NULL, pack_type = NULL, start = NULL, limit = NULL, include_game_data = NULL, include_inactive = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchPacksWithHttpInfo(account_id, sort_field, descending, keyword, pack_type, start, limit, include_game_data, include_inactive, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Packs
    #'
    #' @param account_id The logged in user.
    #' @param sort_field The field to sort by. Possible values include: TITLE, DESCRIPTION, CREATED, UPDATED
    #' @param descending Determines whether the sorted list is in descending or ascending order
    #' @param keyword (optional) Keyword search on the pack name
    #' @param pack_type (optional) Filters results on pack type
    #' @param start (optional) Start the result set at some index.
    #' @param limit (optional) Limit the result to some number.
    #' @param include_game_data (optional) Determines whether to include game data in the results
    #' @param include_inactive (optional) Determines whether to include inactive results
    #' @param app_key (optional) The application to filter results on
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[PackResponse]) with additional information such as HTTP status code, headers
    SearchPacksWithHttpInfo = function(account_id, sort_field, descending, keyword = NULL, pack_type = NULL, start = NULL, limit = NULL, include_game_data = NULL, include_inactive = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PackApi$SearchPacks, `account_id` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling PackApi$SearchPacks, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling PackApi$SearchPacks, `descending` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling PackApi$SearchPacks, `keyword` is not nullable")
      }

      if (!missing(`pack_type`) && is.null(`pack_type`)) {
        stop("Invalid value for `pack_type` when calling PackApi$SearchPacks, `pack_type` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling PackApi$SearchPacks, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling PackApi$SearchPacks, `limit` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling PackApi$SearchPacks, `include_game_data` is not nullable")
      }

      if (!missing(`include_inactive`) && is.null(`include_inactive`)) {
        stop("Invalid value for `include_inactive` when calling PackApi$SearchPacks, `include_inactive` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling PackApi$SearchPacks, `app_key` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      if (!is.null(`pack_type`) && !(`pack_type` %in% c("TUTORIAL", "BUILTIN", "DOWNLOAD", "THRESHOLD", "THEME", "TOURNAMENT"))) {
        stop("Invalid value for pack_type when calling PackApi$SearchPacks. Must be [TUTORIAL, BUILTIN, DOWNLOAD, THRESHOLD, THEME, TOURNAMENT].")
      }
      query_params[["packType"]] <- `pack_type`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("TITLE", "DESCRIPTION", "CREATED", "UPDATED"))) {
        stop("Invalid value for sort_field when calling PackApi$SearchPacks. Must be [TITLE, DESCRIPTION, CREATED, UPDATED].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["includeGameData"]] <- `include_game_data`

      query_params[["includeInactive"]] <- `include_inactive`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/pack/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[PackResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Pack
    #'
    #' @param account_id The logged in user.
    #' @param pack_id The id of the pack to update.
    #' @param allocate_tickets Flag to indicate owner should receive tickets for completed packs
    #' @param ticket_count The number of tickets to reward
    #' @param title (optional) The title of the pack
    #' @param description (optional) The description of the pack
    #' @param search_tags (optional) The tags for searching the pack, comma separated
    #' @param active (optional) Activate/deactivate the pack
    #' @param game_type (optional) This parameter is deprecated.
    #' @param app_key (optional) The application key used to identify the application
    #' @param pack_type (optional) The type of the pack
    #' @param pack_order (optional) The order of the pack
    #' @param sequence_type (optional) The type of game sequence of the pack
    #' @param background_id (optional) The background asset id of the pack
    #' @param image_id (optional) The image asset id of the pack
    #' @param start_date (optional) The date/time to start the pack, send 0 to unset
    #' @param end_date (optional) The date/time to end the pack, send 0 to unset
    #' @param author_override (optional) 
    #' @param price (optional) The price in tickets to purchase this pack
    #' @param price_type (optional) the type of ticket required to purchase this pack
    #' @param game_level_ids (optional) the game level ids to include in the pack, comma separated
    #' @param in_game (optional) 
    #' @param highest (optional) The scoring is highest is best
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a pack
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PackResponse
    UpdatePack = function(account_id, pack_id, allocate_tickets, ticket_count, title = NULL, description = NULL, search_tags = NULL, active = NULL, game_type = NULL, app_key = NULL, pack_type = NULL, pack_order = NULL, sequence_type = NULL, background_id = NULL, image_id = NULL, start_date = NULL, end_date = NULL, author_override = NULL, price = NULL, price_type = NULL, game_level_ids = NULL, in_game = NULL, highest = NULL, ticket_type = NULL, points = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdatePackWithHttpInfo(account_id, pack_id, allocate_tickets, ticket_count, title, description, search_tags, active, game_type, app_key, pack_type, pack_order, sequence_type, background_id, image_id, start_date, end_date, author_override, price, price_type, game_level_ids, in_game, highest, ticket_type, points, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Pack
    #'
    #' @param account_id The logged in user.
    #' @param pack_id The id of the pack to update.
    #' @param allocate_tickets Flag to indicate owner should receive tickets for completed packs
    #' @param ticket_count The number of tickets to reward
    #' @param title (optional) The title of the pack
    #' @param description (optional) The description of the pack
    #' @param search_tags (optional) The tags for searching the pack, comma separated
    #' @param active (optional) Activate/deactivate the pack
    #' @param game_type (optional) This parameter is deprecated.
    #' @param app_key (optional) The application key used to identify the application
    #' @param pack_type (optional) The type of the pack
    #' @param pack_order (optional) The order of the pack
    #' @param sequence_type (optional) The type of game sequence of the pack
    #' @param background_id (optional) The background asset id of the pack
    #' @param image_id (optional) The image asset id of the pack
    #' @param start_date (optional) The date/time to start the pack, send 0 to unset
    #' @param end_date (optional) The date/time to end the pack, send 0 to unset
    #' @param author_override (optional) 
    #' @param price (optional) The price in tickets to purchase this pack
    #' @param price_type (optional) the type of ticket required to purchase this pack
    #' @param game_level_ids (optional) the game level ids to include in the pack, comma separated
    #' @param in_game (optional) 
    #' @param highest (optional) The scoring is highest is best
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a pack
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PackResponse) with additional information such as HTTP status code, headers
    UpdatePackWithHttpInfo = function(account_id, pack_id, allocate_tickets, ticket_count, title = NULL, description = NULL, search_tags = NULL, active = NULL, game_type = NULL, app_key = NULL, pack_type = NULL, pack_order = NULL, sequence_type = NULL, background_id = NULL, image_id = NULL, start_date = NULL, end_date = NULL, author_override = NULL, price = NULL, price_type = NULL, game_level_ids = NULL, in_game = NULL, highest = NULL, ticket_type = NULL, points = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`pack_id`)) {
        stop("Missing required parameter `pack_id`.")
      }

      if (missing(`allocate_tickets`)) {
        stop("Missing required parameter `allocate_tickets`.")
      }

      if (missing(`ticket_count`)) {
        stop("Missing required parameter `ticket_count`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PackApi$UpdatePack, `account_id` is not nullable")
      }

      if (!missing(`pack_id`) && is.null(`pack_id`)) {
        stop("Invalid value for `pack_id` when calling PackApi$UpdatePack, `pack_id` is not nullable")
      }

      if (!missing(`allocate_tickets`) && is.null(`allocate_tickets`)) {
        stop("Invalid value for `allocate_tickets` when calling PackApi$UpdatePack, `allocate_tickets` is not nullable")
      }

      if (!missing(`ticket_count`) && is.null(`ticket_count`)) {
        stop("Invalid value for `ticket_count` when calling PackApi$UpdatePack, `ticket_count` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling PackApi$UpdatePack, `title` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling PackApi$UpdatePack, `description` is not nullable")
      }

      if (!missing(`search_tags`) && is.null(`search_tags`)) {
        stop("Invalid value for `search_tags` when calling PackApi$UpdatePack, `search_tags` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling PackApi$UpdatePack, `active` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling PackApi$UpdatePack, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling PackApi$UpdatePack, `app_key` is not nullable")
      }

      if (!missing(`pack_type`) && is.null(`pack_type`)) {
        stop("Invalid value for `pack_type` when calling PackApi$UpdatePack, `pack_type` is not nullable")
      }

      if (!missing(`pack_order`) && is.null(`pack_order`)) {
        stop("Invalid value for `pack_order` when calling PackApi$UpdatePack, `pack_order` is not nullable")
      }

      if (!missing(`sequence_type`) && is.null(`sequence_type`)) {
        stop("Invalid value for `sequence_type` when calling PackApi$UpdatePack, `sequence_type` is not nullable")
      }

      if (!missing(`background_id`) && is.null(`background_id`)) {
        stop("Invalid value for `background_id` when calling PackApi$UpdatePack, `background_id` is not nullable")
      }

      if (!missing(`image_id`) && is.null(`image_id`)) {
        stop("Invalid value for `image_id` when calling PackApi$UpdatePack, `image_id` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling PackApi$UpdatePack, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling PackApi$UpdatePack, `end_date` is not nullable")
      }

      if (!missing(`author_override`) && is.null(`author_override`)) {
        stop("Invalid value for `author_override` when calling PackApi$UpdatePack, `author_override` is not nullable")
      }

      if (!missing(`price`) && is.null(`price`)) {
        stop("Invalid value for `price` when calling PackApi$UpdatePack, `price` is not nullable")
      }

      if (!missing(`price_type`) && is.null(`price_type`)) {
        stop("Invalid value for `price_type` when calling PackApi$UpdatePack, `price_type` is not nullable")
      }

      if (!missing(`game_level_ids`) && is.null(`game_level_ids`)) {
        stop("Invalid value for `game_level_ids` when calling PackApi$UpdatePack, `game_level_ids` is not nullable")
      }

      if (!missing(`in_game`) && is.null(`in_game`)) {
        stop("Invalid value for `in_game` when calling PackApi$UpdatePack, `in_game` is not nullable")
      }

      if (!missing(`highest`) && is.null(`highest`)) {
        stop("Invalid value for `highest` when calling PackApi$UpdatePack, `highest` is not nullable")
      }

      if (!missing(`ticket_type`) && is.null(`ticket_type`)) {
        stop("Invalid value for `ticket_type` when calling PackApi$UpdatePack, `ticket_type` is not nullable")
      }

      if (!missing(`points`) && is.null(`points`)) {
        stop("Invalid value for `points` when calling PackApi$UpdatePack, `points` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["packId"]] <- `pack_id`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["searchTags"]] <- `search_tags`

      query_params[["active"]] <- `active`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      if (!is.null(`pack_type`) && !(`pack_type` %in% c("TUTORIAL", "BUILTIN", "DOWNLOAD", "THRESHOLD", "THEME", "TOURNAMENT"))) {
        stop("Invalid value for pack_type when calling PackApi$UpdatePack. Must be [TUTORIAL, BUILTIN, DOWNLOAD, THRESHOLD, THEME, TOURNAMENT].")
      }
      query_params[["packType"]] <- `pack_type`

      query_params[["packOrder"]] <- `pack_order`

      if (!is.null(`sequence_type`) && !(`sequence_type` %in% c("FIRST_AVAILABLE", "ALL_AVAILABLE"))) {
        stop("Invalid value for sequence_type when calling PackApi$UpdatePack. Must be [FIRST_AVAILABLE, ALL_AVAILABLE].")
      }
      query_params[["sequenceType"]] <- `sequence_type`

      query_params[["backgroundId"]] <- `background_id`

      query_params[["imageId"]] <- `image_id`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["authorOverride"]] <- `author_override`

      query_params[["price"]] <- `price`

      query_params[["priceType"]] <- `price_type`

      query_params[["gameLevelIds"]] <- `game_level_ids`

      query_params[["inGame"]] <- `in_game`

      query_params[["highest"]] <- `highest`

      query_params[["allocateTickets"]] <- `allocate_tickets`

      query_params[["ticketCount"]] <- `ticket_count`

      query_params[["ticketType"]] <- `ticket_type`

      query_params[["points"]] <- `points`

      local_var_url_path <- "/pack/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PackResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
