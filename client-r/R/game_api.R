#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Game operations
#' @description GameApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateGame  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user. (Optional)
#' var_app_key <- "app_key_example" # character | The game application key to save the level for. (Optional)
#' var_title <- "title_example" # character | Title of the game. (Optional)
#' var_description <- "description_example" # character | Description of the game. (Optional)
#' var_meta_data <- "meta_data_example" # character | metaData of the Game. (Optional)
#' var_pack_ids <- "pack_ids_example" # character | comma separated String of pack Ids that will associate with the game. (Optional)
#' var_include_game_data <- "include_game_data_example" # character | Show more details in response. (Optional)
#'
#' #Create a Game
#' api_instance <- GameApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateGame(account_id = var_account_id, app_key = var_app_key, title = var_title, description = var_description, meta_data = var_meta_data, pack_ids = var_pack_ids, include_game_data = var_include_game_datadata_file = "result.txt")
#' result <- api_instance$CreateGame(account_id = var_account_id, app_key = var_app_key, title = var_title, description = var_description, meta_data = var_meta_data, pack_ids = var_pack_ids, include_game_data = var_include_game_data)
#' dput(result)
#'
#'
#' ####################  DeleteGame  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_game_id <- 56 # integer | the updating game's id.
#'
#' #Delete a Game
#' api_instance <- GameApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteGame(var_account_id, var_game_iddata_file = "result.txt")
#' result <- api_instance$DeleteGame(var_account_id, var_game_id)
#' dput(result)
#'
#'
#' ####################  GetGame  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_game_id <- 56 # integer | the updating game's id.
#' var_include_game_data <- "include_game_data_example" # character | If true include the game level data, otherwise don't. default is false. (Optional)
#'
#' #Get a Game by id
#' api_instance <- GameApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetGame(var_account_id, var_game_id, include_game_data = var_include_game_datadata_file = "result.txt")
#' result <- api_instance$GetGame(var_account_id, var_game_id, include_game_data = var_include_game_data)
#' dput(result)
#'
#'
#' ####################  SearchGames  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_app_key <- "app_key_example" # character | the application key
#' var_start <- 56 # integer | Start the result set at some index.
#' var_limit <- 56 # integer | Limit the result to some number.
#' var_keyword <- "keyword_example" # character | Match the keyword to the owner name or level name. (Optional)
#' var_app_version <- "app_version_example" # character | The maximum version of the level to return. (Optional)
#' var_include_game_data <- "include_game_data_example" # character | more details in response (Optional)
#' var_include_inactive <- "include_inactive_example" # character | more details in response (Optional)
#'
#' #Search a Game
#' api_instance <- GameApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchGames(var_account_id, var_app_key, var_start, var_limit, keyword = var_keyword, app_version = var_app_version, include_game_data = var_include_game_data, include_inactive = var_include_inactivedata_file = "result.txt")
#' result <- api_instance$SearchGames(var_account_id, var_app_key, var_start, var_limit, keyword = var_keyword, app_version = var_app_version, include_game_data = var_include_game_data, include_inactive = var_include_inactive)
#' dput(result)
#'
#'
#' ####################  UpdateGame  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user. (Optional)
#' var_game_id <- 56 # integer | the updating game's id (Optional)
#' var_app_key <- "app_key_example" # character | The game application key to save the level for. (Optional)
#' var_title <- "title_example" # character | Title of the Game. (Optional)
#' var_description <- "description_example" # character | Description of the Game. (Optional)
#' var_meta_data <- "meta_data_example" # character | metaData of the Game. (Optional)
#' var_pack_ids <- "pack_ids_example" # character | comma separated String of pack Ids that will associate with the game. (Optional)
#' var_include_game_data <- "include_game_data_example" # character | show more details in response. (Optional)
#'
#' #Update a Game
#' api_instance <- GameApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateGame(account_id = var_account_id, game_id = var_game_id, app_key = var_app_key, title = var_title, description = var_description, meta_data = var_meta_data, pack_ids = var_pack_ids, include_game_data = var_include_game_datadata_file = "result.txt")
#' result <- api_instance$UpdateGame(account_id = var_account_id, game_id = var_game_id, app_key = var_app_key, title = var_title, description = var_description, meta_data = var_meta_data, pack_ids = var_pack_ids, include_game_data = var_include_game_data)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
GameApi <- R6::R6Class(
  "GameApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new GameApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create a Game
    #'
    #' @param account_id (optional) The logged in user.
    #' @param app_key (optional) The game application key to save the level for.
    #' @param title (optional) Title of the game.
    #' @param description (optional) Description of the game.
    #' @param meta_data (optional) metaData of the Game.
    #' @param pack_ids (optional) comma separated String of pack Ids that will associate with the game.
    #' @param include_game_data (optional) Show more details in response.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return GameResponse
    CreateGame = function(account_id = NULL, app_key = NULL, title = NULL, description = NULL, meta_data = NULL, pack_ids = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateGameWithHttpInfo(account_id, app_key, title, description, meta_data, pack_ids, include_game_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a Game
    #'
    #' @param account_id (optional) The logged in user.
    #' @param app_key (optional) The game application key to save the level for.
    #' @param title (optional) Title of the game.
    #' @param description (optional) Description of the game.
    #' @param meta_data (optional) metaData of the Game.
    #' @param pack_ids (optional) comma separated String of pack Ids that will associate with the game.
    #' @param include_game_data (optional) Show more details in response.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (GameResponse) with additional information such as HTTP status code, headers
    CreateGameWithHttpInfo = function(account_id = NULL, app_key = NULL, title = NULL, description = NULL, meta_data = NULL, pack_ids = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameApi$CreateGame, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling GameApi$CreateGame, `app_key` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling GameApi$CreateGame, `title` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling GameApi$CreateGame, `description` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling GameApi$CreateGame, `meta_data` is not nullable")
      }

      if (!missing(`pack_ids`) && is.null(`pack_ids`)) {
        stop("Invalid value for `pack_ids` when calling GameApi$CreateGame, `pack_ids` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling GameApi$CreateGame, `include_game_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["metaData"]] <- `meta_data`

      query_params[["packIds"]] <- `pack_ids`

      query_params[["includeGameData"]] <- `include_game_data`

      local_var_url_path <- "/game/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GameResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete a Game
    #'
    #' @param account_id The logged in user.
    #' @param game_id the updating game's id.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteGame = function(account_id, game_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteGameWithHttpInfo(account_id, game_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete a Game
    #'
    #' @param account_id The logged in user.
    #' @param game_id the updating game's id.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteGameWithHttpInfo = function(account_id, game_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`game_id`)) {
        stop("Missing required parameter `game_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameApi$DeleteGame, `account_id` is not nullable")
      }

      if (!missing(`game_id`) && is.null(`game_id`)) {
        stop("Invalid value for `game_id` when calling GameApi$DeleteGame, `game_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["gameId"]] <- `game_id`

      local_var_url_path <- "/game/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get a Game by id
    #'
    #' @param account_id The logged in user.
    #' @param game_id the updating game's id.
    #' @param include_game_data (optional) If true include the game level data, otherwise don't. default is false.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return GameResponse
    GetGame = function(account_id, game_id, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetGameWithHttpInfo(account_id, game_id, include_game_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get a Game by id
    #'
    #' @param account_id The logged in user.
    #' @param game_id the updating game's id.
    #' @param include_game_data (optional) If true include the game level data, otherwise don't. default is false.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (GameResponse) with additional information such as HTTP status code, headers
    GetGameWithHttpInfo = function(account_id, game_id, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`game_id`)) {
        stop("Missing required parameter `game_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameApi$GetGame, `account_id` is not nullable")
      }

      if (!missing(`game_id`) && is.null(`game_id`)) {
        stop("Invalid value for `game_id` when calling GameApi$GetGame, `game_id` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling GameApi$GetGame, `include_game_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["gameId"]] <- `game_id`

      query_params[["includeGameData"]] <- `include_game_data`

      local_var_url_path <- "/game/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GameResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search a Game
    #'
    #' @param account_id The logged in user.
    #' @param app_key the application key
    #' @param start Start the result set at some index.
    #' @param limit Limit the result to some number.
    #' @param keyword (optional) Match the keyword to the owner name or level name.
    #' @param app_version (optional) The maximum version of the level to return.
    #' @param include_game_data (optional) more details in response
    #' @param include_inactive (optional) more details in response
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return GameResponse
    SearchGames = function(account_id, app_key, start, limit, keyword = NULL, app_version = NULL, include_game_data = NULL, include_inactive = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchGamesWithHttpInfo(account_id, app_key, start, limit, keyword, app_version, include_game_data, include_inactive, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search a Game
    #'
    #' @param account_id The logged in user.
    #' @param app_key the application key
    #' @param start Start the result set at some index.
    #' @param limit Limit the result to some number.
    #' @param keyword (optional) Match the keyword to the owner name or level name.
    #' @param app_version (optional) The maximum version of the level to return.
    #' @param include_game_data (optional) more details in response
    #' @param include_inactive (optional) more details in response
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (GameResponse) with additional information such as HTTP status code, headers
    SearchGamesWithHttpInfo = function(account_id, app_key, start, limit, keyword = NULL, app_version = NULL, include_game_data = NULL, include_inactive = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameApi$SearchGames, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling GameApi$SearchGames, `app_key` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling GameApi$SearchGames, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling GameApi$SearchGames, `limit` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling GameApi$SearchGames, `keyword` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling GameApi$SearchGames, `app_version` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling GameApi$SearchGames, `include_game_data` is not nullable")
      }

      if (!missing(`include_inactive`) && is.null(`include_inactive`)) {
        stop("Invalid value for `include_inactive` when calling GameApi$SearchGames, `include_inactive` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["keyword"]] <- `keyword`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["appVersion"]] <- `app_version`

      query_params[["includeGameData"]] <- `include_game_data`

      query_params[["includeInactive"]] <- `include_inactive`

      local_var_url_path <- "/game/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GameResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update a Game
    #'
    #' @param account_id (optional) The logged in user.
    #' @param game_id (optional) the updating game's id
    #' @param app_key (optional) The game application key to save the level for.
    #' @param title (optional) Title of the Game.
    #' @param description (optional) Description of the Game.
    #' @param meta_data (optional) metaData of the Game.
    #' @param pack_ids (optional) comma separated String of pack Ids that will associate with the game.
    #' @param include_game_data (optional) show more details in response.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return GameResponse
    UpdateGame = function(account_id = NULL, game_id = NULL, app_key = NULL, title = NULL, description = NULL, meta_data = NULL, pack_ids = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateGameWithHttpInfo(account_id, game_id, app_key, title, description, meta_data, pack_ids, include_game_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update a Game
    #'
    #' @param account_id (optional) The logged in user.
    #' @param game_id (optional) the updating game's id
    #' @param app_key (optional) The game application key to save the level for.
    #' @param title (optional) Title of the Game.
    #' @param description (optional) Description of the Game.
    #' @param meta_data (optional) metaData of the Game.
    #' @param pack_ids (optional) comma separated String of pack Ids that will associate with the game.
    #' @param include_game_data (optional) show more details in response.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (GameResponse) with additional information such as HTTP status code, headers
    UpdateGameWithHttpInfo = function(account_id = NULL, game_id = NULL, app_key = NULL, title = NULL, description = NULL, meta_data = NULL, pack_ids = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameApi$UpdateGame, `account_id` is not nullable")
      }

      if (!missing(`game_id`) && is.null(`game_id`)) {
        stop("Invalid value for `game_id` when calling GameApi$UpdateGame, `game_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling GameApi$UpdateGame, `app_key` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling GameApi$UpdateGame, `title` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling GameApi$UpdateGame, `description` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling GameApi$UpdateGame, `meta_data` is not nullable")
      }

      if (!missing(`pack_ids`) && is.null(`pack_ids`)) {
        stop("Invalid value for `pack_ids` when calling GameApi$UpdateGame, `pack_ids` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling GameApi$UpdateGame, `include_game_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["gameId"]] <- `game_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["metaData"]] <- `meta_data`

      query_params[["packIds"]] <- `pack_ids`

      query_params[["includeGameData"]] <- `include_game_data`

      local_var_url_path <- "/game/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GameResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
