#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Listing operations
#' @description ListingApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateListing  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the user's account ID
#' var_name <- "name_example" # character | the name of the listing
#' var_filter_ids <- "filter_ids_example" # character | comma separated list of filter IDs (Optional)
#' var_description <- "description_example" # character | the description of the listing (Optional)
#' var_start <- 56 # integer | the start date of the listing (Optional)
#' var_end <- 56 # integer | the end date of the listing (Optional)
#' var_location_name <- "location_name_example" # character | the name of the location the listing will be held at (Optional)
#' var_location_description <- "location_description_example" # character | the description of the location the listing will be held at (Optional)
#' var_is_private <- FALSE # character | determines whether the listing is public or private (Optional)
#' var_external_id <- "external_id_example" # character | external identifier used by a third party (Optional)
#' var_external_id2 <- "external_id2_example" # character | secondary external identifier used by a third party (Optional)
#' var_external_group_id <- "external_group_id_example" # character | external group identifier used by a third party (Optional)
#' var_active <- "active_example" # character | Sets the active flag (Optional)
#' var_meta_data <- "meta_data_example" # character | external custom client defined data (Optional)
#'
#' #Create Listing
#' api_instance <- ListingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateListing(var_account_id, var_name, filter_ids = var_filter_ids, description = var_description, start = var_start, end = var_end, location_name = var_location_name, location_description = var_location_description, is_private = var_is_private, external_id = var_external_id, external_id2 = var_external_id2, external_group_id = var_external_group_id, active = var_active, meta_data = var_meta_datadata_file = "result.txt")
#' result <- api_instance$CreateListing(var_account_id, var_name, filter_ids = var_filter_ids, description = var_description, start = var_start, end = var_end, location_name = var_location_name, location_description = var_location_description, is_private = var_is_private, external_id = var_external_id, external_id2 = var_external_id2, external_group_id = var_external_group_id, active = var_active, meta_data = var_meta_data)
#' dput(result)
#'
#'
#' ####################  DeleteListing  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_listing_id <- 56 # integer | the id of the listing to delete
#'
#' #Delete Listing
#' api_instance <- ListingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteListing(var_account_id, var_listing_iddata_file = "result.txt")
#' result <- api_instance$DeleteListing(var_account_id, var_listing_id)
#' dput(result)
#'
#'
#' ####################  GetListing  ####################
#'
#' library(openapi)
#' var_listing_id <- 56 # integer | the id of the listing to get
#'
#' #Get Listing
#' api_instance <- ListingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetListing(var_listing_iddata_file = "result.txt")
#' result <- api_instance$GetListing(var_listing_id)
#' dput(result)
#'
#'
#' ####################  SearchListing  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account id of the user (Optional)
#' var_keyword <- "keyword_example" # character | search the event name and description for this keyword (Optional)
#' var_start <- 0 # integer | the record to begin the return set on (Optional)
#' var_limit <- 20 # integer | the number of records to return (Optional)
#' var_active_only <- FALSE # character | whether to search on active listings only (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#' var_start_date <- 56 # integer | the start date to search from (Optional)
#' var_end_date <- 56 # integer | the end date to search to (Optional)
#' var_category_ids <- "category_ids_example" # character | only return items of these categories (Optional)
#' var_filter_ids <- "filter_ids_example" # character |  (Optional)
#' var_use_listing_order_ids <- TRUE # character | determines whether to use configured listing order ids (Optional)
#' var_external_id <- "external_id_example" # character | external identifier used by a third party (Optional)
#' var_external_id2 <- "external_id2_example" # character | secondary external identifier used by a third party (Optional)
#' var_external_group_id <- "external_group_id_example" # character | external group identifier used by a third party (Optional)
#'
#' #Search Listings
#' api_instance <- ListingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchListing(account_id = var_account_id, keyword = var_keyword, start = var_start, limit = var_limit, active_only = var_active_only, latitude = var_latitude, longitude = var_longitude, start_date = var_start_date, end_date = var_end_date, category_ids = var_category_ids, filter_ids = var_filter_ids, use_listing_order_ids = var_use_listing_order_ids, external_id = var_external_id, external_id2 = var_external_id2, external_group_id = var_external_group_iddata_file = "result.txt")
#' result <- api_instance$SearchListing(account_id = var_account_id, keyword = var_keyword, start = var_start, limit = var_limit, active_only = var_active_only, latitude = var_latitude, longitude = var_longitude, start_date = var_start_date, end_date = var_end_date, category_ids = var_category_ids, filter_ids = var_filter_ids, use_listing_order_ids = var_use_listing_order_ids, external_id = var_external_id, external_id2 = var_external_id2, external_group_id = var_external_group_id)
#' dput(result)
#'
#'
#' ####################  SummaryListing  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account id of the user (Optional)
#' var_start_date <- 56 # integer | the start date to search from (Optional)
#' var_category_ids <- "category_ids_example" # character | the list of categories to search on (Optional)
#' var_days_to_include <- 15 # integer | how far out to search, in days (Optional)
#' var_use_listing_order_ids <- TRUE # character | determines whether to use configured listing order ids (Optional)
#'
#' #Summary Listing
#' api_instance <- ListingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SummaryListing(account_id = var_account_id, start_date = var_start_date, category_ids = var_category_ids, days_to_include = var_days_to_include, use_listing_order_ids = var_use_listing_order_idsdata_file = "result.txt")
#' result <- api_instance$SummaryListing(account_id = var_account_id, start_date = var_start_date, category_ids = var_category_ids, days_to_include = var_days_to_include, use_listing_order_ids = var_use_listing_order_ids)
#' dput(result)
#'
#'
#' ####################  UpdateListing  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the user's account ID
#' var_listing_id <- 56 # integer | the listing to update
#' var_filter_ids <- "filter_ids_example" # character | comma separated list of filter IDs (Optional)
#' var_name <- "name_example" # character | the name of the listing (Optional)
#' var_description <- "description_example" # character | the description of the listing (Optional)
#' var_start <- 56 # integer | the start date of the listing (Optional)
#' var_end <- 56 # integer | the end date of the listing (Optional)
#' var_location_name <- "location_name_example" # character | the name of the location the listing will be held at (Optional)
#' var_location_description <- "location_description_example" # character | the description of the location the listing will be held at (Optional)
#' var_is_private <- "is_private_example" # character | determines whether the listing is public or private (Optional)
#' var_external_id <- "external_id_example" # character | external identifier used by a third party (Optional)
#' var_external_id2 <- "external_id2_example" # character | secondary external identifier used by a third party (Optional)
#' var_external_group_id <- "external_group_id_example" # character | external group identifier used by a third party (Optional)
#' var_active <- "active_example" # character | Sets the active flag (Optional)
#' var_meta_data <- "meta_data_example" # character | external custom client defined data (Optional)
#'
#' #Update Listing
#' api_instance <- ListingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateListing(var_account_id, var_listing_id, filter_ids = var_filter_ids, name = var_name, description = var_description, start = var_start, end = var_end, location_name = var_location_name, location_description = var_location_description, is_private = var_is_private, external_id = var_external_id, external_id2 = var_external_id2, external_group_id = var_external_group_id, active = var_active, meta_data = var_meta_datadata_file = "result.txt")
#' result <- api_instance$UpdateListing(var_account_id, var_listing_id, filter_ids = var_filter_ids, name = var_name, description = var_description, start = var_start, end = var_end, location_name = var_location_name, location_description = var_location_description, is_private = var_is_private, external_id = var_external_id, external_id2 = var_external_id2, external_group_id = var_external_group_id, active = var_active, meta_data = var_meta_data)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ListingApi <- R6::R6Class(
  "ListingApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ListingApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Listing
    #'
    #' @param account_id the user's account ID
    #' @param name the name of the listing
    #' @param filter_ids (optional) comma separated list of filter IDs
    #' @param description (optional) the description of the listing
    #' @param start (optional) the start date of the listing
    #' @param end (optional) the end date of the listing
    #' @param location_name (optional) the name of the location the listing will be held at
    #' @param location_description (optional) the description of the location the listing will be held at
    #' @param is_private (optional) determines whether the listing is public or private (default value: FALSE)
    #' @param external_id (optional) external identifier used by a third party
    #' @param external_id2 (optional) secondary external identifier used by a third party
    #' @param external_group_id (optional) external group identifier used by a third party
    #' @param active (optional) Sets the active flag
    #' @param meta_data (optional) external custom client defined data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ListingFullResponse
    CreateListing = function(account_id, name, filter_ids = NULL, description = NULL, start = NULL, end = NULL, location_name = NULL, location_description = NULL, is_private = FALSE, external_id = NULL, external_id2 = NULL, external_group_id = NULL, active = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateListingWithHttpInfo(account_id, name, filter_ids, description, start, end, location_name, location_description, is_private, external_id, external_id2, external_group_id, active, meta_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Listing
    #'
    #' @param account_id the user's account ID
    #' @param name the name of the listing
    #' @param filter_ids (optional) comma separated list of filter IDs
    #' @param description (optional) the description of the listing
    #' @param start (optional) the start date of the listing
    #' @param end (optional) the end date of the listing
    #' @param location_name (optional) the name of the location the listing will be held at
    #' @param location_description (optional) the description of the location the listing will be held at
    #' @param is_private (optional) determines whether the listing is public or private (default value: FALSE)
    #' @param external_id (optional) external identifier used by a third party
    #' @param external_id2 (optional) secondary external identifier used by a third party
    #' @param external_group_id (optional) external group identifier used by a third party
    #' @param active (optional) Sets the active flag
    #' @param meta_data (optional) external custom client defined data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ListingFullResponse) with additional information such as HTTP status code, headers
    CreateListingWithHttpInfo = function(account_id, name, filter_ids = NULL, description = NULL, start = NULL, end = NULL, location_name = NULL, location_description = NULL, is_private = FALSE, external_id = NULL, external_id2 = NULL, external_group_id = NULL, active = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ListingApi$CreateListing, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling ListingApi$CreateListing, `name` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling ListingApi$CreateListing, `filter_ids` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling ListingApi$CreateListing, `description` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ListingApi$CreateListing, `start` is not nullable")
      }

      if (!missing(`end`) && is.null(`end`)) {
        stop("Invalid value for `end` when calling ListingApi$CreateListing, `end` is not nullable")
      }

      if (!missing(`location_name`) && is.null(`location_name`)) {
        stop("Invalid value for `location_name` when calling ListingApi$CreateListing, `location_name` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling ListingApi$CreateListing, `location_description` is not nullable")
      }

      if (!missing(`is_private`) && is.null(`is_private`)) {
        stop("Invalid value for `is_private` when calling ListingApi$CreateListing, `is_private` is not nullable")
      }

      if (!missing(`external_id`) && is.null(`external_id`)) {
        stop("Invalid value for `external_id` when calling ListingApi$CreateListing, `external_id` is not nullable")
      }

      if (!missing(`external_id2`) && is.null(`external_id2`)) {
        stop("Invalid value for `external_id2` when calling ListingApi$CreateListing, `external_id2` is not nullable")
      }

      if (!missing(`external_group_id`) && is.null(`external_group_id`)) {
        stop("Invalid value for `external_group_id` when calling ListingApi$CreateListing, `external_group_id` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling ListingApi$CreateListing, `active` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling ListingApi$CreateListing, `meta_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["name"]] <- `name`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["description"]] <- `description`

      query_params[["start"]] <- `start`

      query_params[["end"]] <- `end`

      query_params[["locationName"]] <- `location_name`

      query_params[["locationDescription"]] <- `location_description`

      query_params[["isPrivate"]] <- `is_private`

      query_params[["externalId"]] <- `external_id`

      query_params[["externalId2"]] <- `external_id2`

      query_params[["externalGroupId"]] <- `external_group_id`

      query_params[["active"]] <- `active`

      query_params[["metaData"]] <- `meta_data`

      local_var_url_path <- "/listing/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListingFullResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Listing
    #'
    #' @param account_id the id of the logged in user
    #' @param listing_id the id of the listing to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteListing = function(account_id, listing_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteListingWithHttpInfo(account_id, listing_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Listing
    #'
    #' @param account_id the id of the logged in user
    #' @param listing_id the id of the listing to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteListingWithHttpInfo = function(account_id, listing_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`listing_id`)) {
        stop("Missing required parameter `listing_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ListingApi$DeleteListing, `account_id` is not nullable")
      }

      if (!missing(`listing_id`) && is.null(`listing_id`)) {
        stop("Invalid value for `listing_id` when calling ListingApi$DeleteListing, `listing_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["listingId"]] <- `listing_id`

      local_var_url_path <- "/listing/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Listing
    #'
    #' @param listing_id the id of the listing to get
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ListingFullResponse
    GetListing = function(listing_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetListingWithHttpInfo(listing_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Listing
    #'
    #' @param listing_id the id of the listing to get
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ListingFullResponse) with additional information such as HTTP status code, headers
    GetListingWithHttpInfo = function(listing_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`listing_id`)) {
        stop("Missing required parameter `listing_id`.")
      }

      if (!missing(`listing_id`) && is.null(`listing_id`)) {
        stop("Invalid value for `listing_id` when calling ListingApi$GetListing, `listing_id` is not nullable")
      }

      query_params[["listingId"]] <- `listing_id`

      local_var_url_path <- "/listing/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListingFullResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Listings
    #'
    #' @param account_id (optional) the account id of the user
    #' @param keyword (optional) search the event name and description for this keyword
    #' @param start (optional) the record to begin the return set on (default value: 0)
    #' @param limit (optional) the number of records to return (default value: 20)
    #' @param active_only (optional) whether to search on active listings only (default value: FALSE)
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param start_date (optional) the start date to search from
    #' @param end_date (optional) the end date to search to
    #' @param category_ids (optional) only return items of these categories
    #' @param filter_ids (optional) 
    #' @param use_listing_order_ids (optional) determines whether to use configured listing order ids (default value: TRUE)
    #' @param external_id (optional) external identifier used by a third party
    #' @param external_id2 (optional) secondary external identifier used by a third party
    #' @param external_group_id (optional) external group identifier used by a third party
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ListingResponse]
    SearchListing = function(account_id = NULL, keyword = NULL, start = 0, limit = 20, active_only = FALSE, latitude = NULL, longitude = NULL, start_date = NULL, end_date = NULL, category_ids = NULL, filter_ids = NULL, use_listing_order_ids = TRUE, external_id = NULL, external_id2 = NULL, external_group_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchListingWithHttpInfo(account_id, keyword, start, limit, active_only, latitude, longitude, start_date, end_date, category_ids, filter_ids, use_listing_order_ids, external_id, external_id2, external_group_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Listings
    #'
    #' @param account_id (optional) the account id of the user
    #' @param keyword (optional) search the event name and description for this keyword
    #' @param start (optional) the record to begin the return set on (default value: 0)
    #' @param limit (optional) the number of records to return (default value: 20)
    #' @param active_only (optional) whether to search on active listings only (default value: FALSE)
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param start_date (optional) the start date to search from
    #' @param end_date (optional) the end date to search to
    #' @param category_ids (optional) only return items of these categories
    #' @param filter_ids (optional) 
    #' @param use_listing_order_ids (optional) determines whether to use configured listing order ids (default value: TRUE)
    #' @param external_id (optional) external identifier used by a third party
    #' @param external_id2 (optional) secondary external identifier used by a third party
    #' @param external_group_id (optional) external group identifier used by a third party
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ListingResponse]) with additional information such as HTTP status code, headers
    SearchListingWithHttpInfo = function(account_id = NULL, keyword = NULL, start = 0, limit = 20, active_only = FALSE, latitude = NULL, longitude = NULL, start_date = NULL, end_date = NULL, category_ids = NULL, filter_ids = NULL, use_listing_order_ids = TRUE, external_id = NULL, external_id2 = NULL, external_group_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ListingApi$SearchListing, `account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ListingApi$SearchListing, `keyword` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ListingApi$SearchListing, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ListingApi$SearchListing, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling ListingApi$SearchListing, `active_only` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ListingApi$SearchListing, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ListingApi$SearchListing, `longitude` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling ListingApi$SearchListing, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling ListingApi$SearchListing, `end_date` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling ListingApi$SearchListing, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling ListingApi$SearchListing, `filter_ids` is not nullable")
      }

      if (!missing(`use_listing_order_ids`) && is.null(`use_listing_order_ids`)) {
        stop("Invalid value for `use_listing_order_ids` when calling ListingApi$SearchListing, `use_listing_order_ids` is not nullable")
      }

      if (!missing(`external_id`) && is.null(`external_id`)) {
        stop("Invalid value for `external_id` when calling ListingApi$SearchListing, `external_id` is not nullable")
      }

      if (!missing(`external_id2`) && is.null(`external_id2`)) {
        stop("Invalid value for `external_id2` when calling ListingApi$SearchListing, `external_id2` is not nullable")
      }

      if (!missing(`external_group_id`) && is.null(`external_group_id`)) {
        stop("Invalid value for `external_group_id` when calling ListingApi$SearchListing, `external_group_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["useListingOrderIds"]] <- `use_listing_order_ids`

      query_params[["externalId"]] <- `external_id`

      query_params[["externalId2"]] <- `external_id2`

      query_params[["externalGroupId"]] <- `external_group_id`

      local_var_url_path <- "/listing/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ListingResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Summary Listing
    #'
    #' @param account_id (optional) the account id of the user
    #' @param start_date (optional) the start date to search from
    #' @param category_ids (optional) the list of categories to search on
    #' @param days_to_include (optional) how far out to search, in days (default value: 15)
    #' @param use_listing_order_ids (optional) determines whether to use configured listing order ids (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ListingGroupResponse]
    SummaryListing = function(account_id = NULL, start_date = NULL, category_ids = NULL, days_to_include = 15, use_listing_order_ids = TRUE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SummaryListingWithHttpInfo(account_id, start_date, category_ids, days_to_include, use_listing_order_ids, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Summary Listing
    #'
    #' @param account_id (optional) the account id of the user
    #' @param start_date (optional) the start date to search from
    #' @param category_ids (optional) the list of categories to search on
    #' @param days_to_include (optional) how far out to search, in days (default value: 15)
    #' @param use_listing_order_ids (optional) determines whether to use configured listing order ids (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ListingGroupResponse]) with additional information such as HTTP status code, headers
    SummaryListingWithHttpInfo = function(account_id = NULL, start_date = NULL, category_ids = NULL, days_to_include = 15, use_listing_order_ids = TRUE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ListingApi$SummaryListing, `account_id` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling ListingApi$SummaryListing, `start_date` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling ListingApi$SummaryListing, `category_ids` is not nullable")
      }

      if (!missing(`days_to_include`) && is.null(`days_to_include`)) {
        stop("Invalid value for `days_to_include` when calling ListingApi$SummaryListing, `days_to_include` is not nullable")
      }

      if (!missing(`use_listing_order_ids`) && is.null(`use_listing_order_ids`)) {
        stop("Invalid value for `use_listing_order_ids` when calling ListingApi$SummaryListing, `use_listing_order_ids` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["startDate"]] <- `start_date`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["daysToInclude"]] <- `days_to_include`

      query_params[["useListingOrderIds"]] <- `use_listing_order_ids`

      local_var_url_path <- "/listing/summary"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ListingGroupResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Listing
    #'
    #' @param account_id the user's account ID
    #' @param listing_id the listing to update
    #' @param filter_ids (optional) comma separated list of filter IDs
    #' @param name (optional) the name of the listing
    #' @param description (optional) the description of the listing
    #' @param start (optional) the start date of the listing
    #' @param end (optional) the end date of the listing
    #' @param location_name (optional) the name of the location the listing will be held at
    #' @param location_description (optional) the description of the location the listing will be held at
    #' @param is_private (optional) determines whether the listing is public or private
    #' @param external_id (optional) external identifier used by a third party
    #' @param external_id2 (optional) secondary external identifier used by a third party
    #' @param external_group_id (optional) external group identifier used by a third party
    #' @param active (optional) Sets the active flag
    #' @param meta_data (optional) external custom client defined data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ListingFullResponse
    UpdateListing = function(account_id, listing_id, filter_ids = NULL, name = NULL, description = NULL, start = NULL, end = NULL, location_name = NULL, location_description = NULL, is_private = NULL, external_id = NULL, external_id2 = NULL, external_group_id = NULL, active = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateListingWithHttpInfo(account_id, listing_id, filter_ids, name, description, start, end, location_name, location_description, is_private, external_id, external_id2, external_group_id, active, meta_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Listing
    #'
    #' @param account_id the user's account ID
    #' @param listing_id the listing to update
    #' @param filter_ids (optional) comma separated list of filter IDs
    #' @param name (optional) the name of the listing
    #' @param description (optional) the description of the listing
    #' @param start (optional) the start date of the listing
    #' @param end (optional) the end date of the listing
    #' @param location_name (optional) the name of the location the listing will be held at
    #' @param location_description (optional) the description of the location the listing will be held at
    #' @param is_private (optional) determines whether the listing is public or private
    #' @param external_id (optional) external identifier used by a third party
    #' @param external_id2 (optional) secondary external identifier used by a third party
    #' @param external_group_id (optional) external group identifier used by a third party
    #' @param active (optional) Sets the active flag
    #' @param meta_data (optional) external custom client defined data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ListingFullResponse) with additional information such as HTTP status code, headers
    UpdateListingWithHttpInfo = function(account_id, listing_id, filter_ids = NULL, name = NULL, description = NULL, start = NULL, end = NULL, location_name = NULL, location_description = NULL, is_private = NULL, external_id = NULL, external_id2 = NULL, external_group_id = NULL, active = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`listing_id`)) {
        stop("Missing required parameter `listing_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ListingApi$UpdateListing, `account_id` is not nullable")
      }

      if (!missing(`listing_id`) && is.null(`listing_id`)) {
        stop("Invalid value for `listing_id` when calling ListingApi$UpdateListing, `listing_id` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling ListingApi$UpdateListing, `filter_ids` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling ListingApi$UpdateListing, `name` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling ListingApi$UpdateListing, `description` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ListingApi$UpdateListing, `start` is not nullable")
      }

      if (!missing(`end`) && is.null(`end`)) {
        stop("Invalid value for `end` when calling ListingApi$UpdateListing, `end` is not nullable")
      }

      if (!missing(`location_name`) && is.null(`location_name`)) {
        stop("Invalid value for `location_name` when calling ListingApi$UpdateListing, `location_name` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling ListingApi$UpdateListing, `location_description` is not nullable")
      }

      if (!missing(`is_private`) && is.null(`is_private`)) {
        stop("Invalid value for `is_private` when calling ListingApi$UpdateListing, `is_private` is not nullable")
      }

      if (!missing(`external_id`) && is.null(`external_id`)) {
        stop("Invalid value for `external_id` when calling ListingApi$UpdateListing, `external_id` is not nullable")
      }

      if (!missing(`external_id2`) && is.null(`external_id2`)) {
        stop("Invalid value for `external_id2` when calling ListingApi$UpdateListing, `external_id2` is not nullable")
      }

      if (!missing(`external_group_id`) && is.null(`external_group_id`)) {
        stop("Invalid value for `external_group_id` when calling ListingApi$UpdateListing, `external_group_id` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling ListingApi$UpdateListing, `active` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling ListingApi$UpdateListing, `meta_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["listingId"]] <- `listing_id`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["start"]] <- `start`

      query_params[["end"]] <- `end`

      query_params[["locationName"]] <- `location_name`

      query_params[["locationDescription"]] <- `location_description`

      query_params[["isPrivate"]] <- `is_private`

      query_params[["externalId"]] <- `external_id`

      query_params[["externalId2"]] <- `external_id2`

      query_params[["externalGroupId"]] <- `external_group_id`

      query_params[["active"]] <- `active`

      query_params[["metaData"]] <- `meta_data`

      local_var_url_path <- "/listing/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListingFullResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
