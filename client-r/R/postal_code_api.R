#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title PostalCode operations
#' @description PostalCodeApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreatePostalCode  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_code <- "code_example" # character | the postal code
#' var_latitude <- 3.4 # numeric | the latitude of the postal code
#' var_longitude <- 3.4 # numeric | the longitude of the postal code
#' var_state_code <- "state_code_example" # character | the state code (e.g WA, WV, WI) that the postal code is located (Optional)
#' var_city <- "city_example" # character | the city that the postal code is located (Optional)
#' var_active <- "active_example" # character | whether the postal code created should be active or inactive (Optional)
#'
#' #Create Postal Code
#' api_instance <- PostalCodeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreatePostalCode(var_account_id, var_code, var_latitude, var_longitude, state_code = var_state_code, city = var_city, active = var_activedata_file = "result.txt")
#' result <- api_instance$CreatePostalCode(var_account_id, var_code, var_latitude, var_longitude, state_code = var_state_code, city = var_city, active = var_active)
#' dput(result)
#'
#'
#' ####################  DeletePostalCode  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_postal_code_id <- 56 # integer | the id of the postal code to delete
#'
#' #Delete Postal Code
#' api_instance <- PostalCodeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeletePostalCode(var_account_id, var_postal_code_iddata_file = "result.txt")
#' result <- api_instance$DeletePostalCode(var_account_id, var_postal_code_id)
#' dput(result)
#'
#'
#' ####################  GetPostalCode  ####################
#'
#' library(openapi)
#' var_postal_code_id <- 56 # integer | the id of the postal code to get
#'
#' #Get Postal Code
#' api_instance <- PostalCodeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPostalCode(var_postal_code_iddata_file = "result.txt")
#' result <- api_instance$GetPostalCode(var_postal_code_id)
#' dput(result)
#'
#'
#' ####################  GetPostalCodes  ####################
#'
#' library(openapi)
#' var_sort_field <- "sort_field_example" # character | the field to sort the results on
#' var_descending <- "descending_example" # character | whether to order results in ascending or descending order
#' var_latitude <- 3.4 # numeric | the latitude of the postal code to search on (Optional)
#' var_longitude <- 3.4 # numeric | the longitude of the postal code to search on (Optional)
#' var_keyword <- "keyword_example" # character | the keyword of the postal code to search on (Optional)
#' var_miles <- 3.4 # numeric | how far (in miles) to search on for the postal code (Optional)
#' var_start <- 56 # integer | the start of the index and/or pagination (Optional)
#' var_limit <- 56 # integer | the limit of the index and/or pagination (Optional)
#'
#' #Search Postal Codes
#' api_instance <- PostalCodeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPostalCodes(var_sort_field, var_descending, latitude = var_latitude, longitude = var_longitude, keyword = var_keyword, miles = var_miles, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GetPostalCodes(var_sort_field, var_descending, latitude = var_latitude, longitude = var_longitude, keyword = var_keyword, miles = var_miles, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  UpdatePostalCode  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_postal_code_id <- 56 # integer | the id of the postal code to update
#' var_code <- "code_example" # character | the postal code to update (Optional)
#' var_latitude <- 3.4 # numeric | the latitude of the postal code (Optional)
#' var_longitude <- 3.4 # numeric | the longitude of the postal code (Optional)
#' var_state_code <- "state_code_example" # character | the state code (e.g. WA, WI, WY) where the postal code is located (Optional)
#' var_city <- "city_example" # character | the city where the postal code is located (Optional)
#' var_active <- "active_example" # character | whether the postal code is active or inactive (Optional)
#'
#' #Update Postal Code
#' api_instance <- PostalCodeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdatePostalCode(var_account_id, var_postal_code_id, code = var_code, latitude = var_latitude, longitude = var_longitude, state_code = var_state_code, city = var_city, active = var_activedata_file = "result.txt")
#' result <- api_instance$UpdatePostalCode(var_account_id, var_postal_code_id, code = var_code, latitude = var_latitude, longitude = var_longitude, state_code = var_state_code, city = var_city, active = var_active)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
PostalCodeApi <- R6::R6Class(
  "PostalCodeApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new PostalCodeApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Postal Code
    #'
    #' @param account_id the id of the logged in user
    #' @param code the postal code
    #' @param latitude the latitude of the postal code
    #' @param longitude the longitude of the postal code
    #' @param state_code (optional) the state code (e.g WA, WV, WI) that the postal code is located
    #' @param city (optional) the city that the postal code is located
    #' @param active (optional) whether the postal code created should be active or inactive
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PostalCodeResponse
    CreatePostalCode = function(account_id, code, latitude, longitude, state_code = NULL, city = NULL, active = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreatePostalCodeWithHttpInfo(account_id, code, latitude, longitude, state_code, city, active, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Postal Code
    #'
    #' @param account_id the id of the logged in user
    #' @param code the postal code
    #' @param latitude the latitude of the postal code
    #' @param longitude the longitude of the postal code
    #' @param state_code (optional) the state code (e.g WA, WV, WI) that the postal code is located
    #' @param city (optional) the city that the postal code is located
    #' @param active (optional) whether the postal code created should be active or inactive
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PostalCodeResponse) with additional information such as HTTP status code, headers
    CreatePostalCodeWithHttpInfo = function(account_id, code, latitude, longitude, state_code = NULL, city = NULL, active = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`code`)) {
        stop("Missing required parameter `code`.")
      }

      if (missing(`latitude`)) {
        stop("Missing required parameter `latitude`.")
      }

      if (missing(`longitude`)) {
        stop("Missing required parameter `longitude`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PostalCodeApi$CreatePostalCode, `account_id` is not nullable")
      }

      if (!missing(`code`) && is.null(`code`)) {
        stop("Invalid value for `code` when calling PostalCodeApi$CreatePostalCode, `code` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling PostalCodeApi$CreatePostalCode, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling PostalCodeApi$CreatePostalCode, `longitude` is not nullable")
      }

      if (!missing(`state_code`) && is.null(`state_code`)) {
        stop("Invalid value for `state_code` when calling PostalCodeApi$CreatePostalCode, `state_code` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling PostalCodeApi$CreatePostalCode, `city` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling PostalCodeApi$CreatePostalCode, `active` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["code"]] <- `code`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["stateCode"]] <- `state_code`

      query_params[["city"]] <- `city`

      query_params[["active"]] <- `active`

      local_var_url_path <- "/postalCode/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PostalCodeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Postal Code
    #'
    #' @param account_id the id of the logged in user
    #' @param postal_code_id the id of the postal code to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeletePostalCode = function(account_id, postal_code_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeletePostalCodeWithHttpInfo(account_id, postal_code_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Postal Code
    #'
    #' @param account_id the id of the logged in user
    #' @param postal_code_id the id of the postal code to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeletePostalCodeWithHttpInfo = function(account_id, postal_code_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`postal_code_id`)) {
        stop("Missing required parameter `postal_code_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PostalCodeApi$DeletePostalCode, `account_id` is not nullable")
      }

      if (!missing(`postal_code_id`) && is.null(`postal_code_id`)) {
        stop("Invalid value for `postal_code_id` when calling PostalCodeApi$DeletePostalCode, `postal_code_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["postalCodeId"]] <- `postal_code_id`

      local_var_url_path <- "/postalCode/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Postal Code
    #'
    #' @param postal_code_id the id of the postal code to get
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PostalCodeResponse
    GetPostalCode = function(postal_code_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetPostalCodeWithHttpInfo(postal_code_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Postal Code
    #'
    #' @param postal_code_id the id of the postal code to get
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PostalCodeResponse) with additional information such as HTTP status code, headers
    GetPostalCodeWithHttpInfo = function(postal_code_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`postal_code_id`)) {
        stop("Missing required parameter `postal_code_id`.")
      }

      if (!missing(`postal_code_id`) && is.null(`postal_code_id`)) {
        stop("Invalid value for `postal_code_id` when calling PostalCodeApi$GetPostalCode, `postal_code_id` is not nullable")
      }

      query_params[["postalCodeId"]] <- `postal_code_id`

      local_var_url_path <- "/postalCode/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PostalCodeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Postal Codes
    #'
    #' @param sort_field the field to sort the results on
    #' @param descending whether to order results in ascending or descending order
    #' @param latitude (optional) the latitude of the postal code to search on
    #' @param longitude (optional) the longitude of the postal code to search on
    #' @param keyword (optional) the keyword of the postal code to search on
    #' @param miles (optional) how far (in miles) to search on for the postal code
    #' @param start (optional) the start of the index and/or pagination
    #' @param limit (optional) the limit of the index and/or pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[PostalCodeResponse]
    GetPostalCodes = function(sort_field, descending, latitude = NULL, longitude = NULL, keyword = NULL, miles = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetPostalCodesWithHttpInfo(sort_field, descending, latitude, longitude, keyword, miles, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Postal Codes
    #'
    #' @param sort_field the field to sort the results on
    #' @param descending whether to order results in ascending or descending order
    #' @param latitude (optional) the latitude of the postal code to search on
    #' @param longitude (optional) the longitude of the postal code to search on
    #' @param keyword (optional) the keyword of the postal code to search on
    #' @param miles (optional) how far (in miles) to search on for the postal code
    #' @param start (optional) the start of the index and/or pagination
    #' @param limit (optional) the limit of the index and/or pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[PostalCodeResponse]) with additional information such as HTTP status code, headers
    GetPostalCodesWithHttpInfo = function(sort_field, descending, latitude = NULL, longitude = NULL, keyword = NULL, miles = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling PostalCodeApi$GetPostalCodes, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling PostalCodeApi$GetPostalCodes, `descending` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling PostalCodeApi$GetPostalCodes, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling PostalCodeApi$GetPostalCodes, `longitude` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling PostalCodeApi$GetPostalCodes, `keyword` is not nullable")
      }

      if (!missing(`miles`) && is.null(`miles`)) {
        stop("Invalid value for `miles` when calling PostalCodeApi$GetPostalCodes, `miles` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling PostalCodeApi$GetPostalCodes, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling PostalCodeApi$GetPostalCodes, `limit` is not nullable")
      }

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["keyword"]] <- `keyword`

      query_params[["miles"]] <- `miles`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      local_var_url_path <- "/postalCode/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[PostalCodeResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Postal Code
    #'
    #' @param account_id the id of the logged in user
    #' @param postal_code_id the id of the postal code to update
    #' @param code (optional) the postal code to update
    #' @param latitude (optional) the latitude of the postal code
    #' @param longitude (optional) the longitude of the postal code
    #' @param state_code (optional) the state code (e.g. WA, WI, WY) where the postal code is located
    #' @param city (optional) the city where the postal code is located
    #' @param active (optional) whether the postal code is active or inactive
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PostalCodeResponse
    UpdatePostalCode = function(account_id, postal_code_id, code = NULL, latitude = NULL, longitude = NULL, state_code = NULL, city = NULL, active = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdatePostalCodeWithHttpInfo(account_id, postal_code_id, code, latitude, longitude, state_code, city, active, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Postal Code
    #'
    #' @param account_id the id of the logged in user
    #' @param postal_code_id the id of the postal code to update
    #' @param code (optional) the postal code to update
    #' @param latitude (optional) the latitude of the postal code
    #' @param longitude (optional) the longitude of the postal code
    #' @param state_code (optional) the state code (e.g. WA, WI, WY) where the postal code is located
    #' @param city (optional) the city where the postal code is located
    #' @param active (optional) whether the postal code is active or inactive
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PostalCodeResponse) with additional information such as HTTP status code, headers
    UpdatePostalCodeWithHttpInfo = function(account_id, postal_code_id, code = NULL, latitude = NULL, longitude = NULL, state_code = NULL, city = NULL, active = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`postal_code_id`)) {
        stop("Missing required parameter `postal_code_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PostalCodeApi$UpdatePostalCode, `account_id` is not nullable")
      }

      if (!missing(`postal_code_id`) && is.null(`postal_code_id`)) {
        stop("Invalid value for `postal_code_id` when calling PostalCodeApi$UpdatePostalCode, `postal_code_id` is not nullable")
      }

      if (!missing(`code`) && is.null(`code`)) {
        stop("Invalid value for `code` when calling PostalCodeApi$UpdatePostalCode, `code` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling PostalCodeApi$UpdatePostalCode, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling PostalCodeApi$UpdatePostalCode, `longitude` is not nullable")
      }

      if (!missing(`state_code`) && is.null(`state_code`)) {
        stop("Invalid value for `state_code` when calling PostalCodeApi$UpdatePostalCode, `state_code` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling PostalCodeApi$UpdatePostalCode, `city` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling PostalCodeApi$UpdatePostalCode, `active` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["postalCodeId"]] <- `postal_code_id`

      query_params[["code"]] <- `code`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["stateCode"]] <- `state_code`

      query_params[["city"]] <- `city`

      query_params[["active"]] <- `active`

      local_var_url_path <- "/postalCode/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PostalCodeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
