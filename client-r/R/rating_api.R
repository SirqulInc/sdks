#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Rating operations
#' @description RatingApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateRating  ####################
#'
#' library(openapi)
#' var_ratable_type <- "ratable_type_example" # character | The ratable object type {RETAILER_LOCATION}
#' var_ratable_id <- 56 # integer | The id of the ratable object
#' var_rating_value <- 56 # integer | The integer value of 0-100
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_category_id <- 56 # integer |  (Optional)
#' var_display <- "display_example" # character | A subject title for the user rating (limited to 255 characters) (Optional)
#' var_description <- "description_example" # character | The description of the rating (Optional)
#' var_location_description <- "location_description_example" # character | The description of the location (Optional)
#' var_latitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current location of the user (Optional)
#'
#' #Create Rating
#' api_instance <- RatingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateRating(var_ratable_type, var_ratable_id, var_rating_value, device_id = var_device_id, account_id = var_account_id, category_id = var_category_id, display = var_display, description = var_description, location_description = var_location_description, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$CreateRating(var_ratable_type, var_ratable_id, var_rating_value, device_id = var_device_id, account_id = var_account_id, category_id = var_category_id, display = var_display, description = var_description, location_description = var_location_description, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  DeleteRating  ####################
#'
#' library(openapi)
#' var_rating_id <- 56 # integer | The ID of the rating to delete
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#'
#' #Delete Rating
#' api_instance <- RatingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteRating(var_rating_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteRating(var_rating_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  SearchLocationRatingIndexes  ####################
#'
#' library(openapi)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category ids to filter the results by (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_location_type <- "location_type_example" # character | The type of location to filter the results by (Optional)
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, COUNT, SUMMATION, AVERAGE, VALUE} (Optional)
#' var_descending <- "descending_example" # character | The order to return the search results (Optional)
#' var_start <- 56 # integer | The record to begin the return set on (Optional)
#' var_limit <- 56 # integer | The number of records to return (Optional)
#' var_search_range <- 3.4 # numeric | The search radius in kilometers to filter results (Optional)
#' var_latitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_return_overall_rating <- "return_overall_rating_example" # character | Determines whether to return the overall rating record instead (Optional)
#' var_distance_unit <- "distance_unit_example" # character |  (Optional)
#' var_return_retailer <- "return_retailer_example" # character | whether to return the retailer or not (Optional)
#' var_return_assets <- "return_assets_example" # character | whether to return the assets or not (Optional)
#' var_return_offers <- "return_offers_example" # character | whether to return the offers or not (Optional)
#' var_return_categories <- "return_categories_example" # character | whether to return the categories or not (Optional)
#' var_return_filters <- "return_filters_example" # character | whether to return the filters or not (Optional)
#'
#' #Search Location Rating Indexes
#' api_instance <- RatingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchLocationRatingIndexes(category_ids = var_category_ids, keyword = var_keyword, location_type = var_location_type, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, search_range = var_search_range, latitude = var_latitude, longitude = var_longitude, return_overall_rating = var_return_overall_rating, distance_unit = var_distance_unit, return_retailer = var_return_retailer, return_assets = var_return_assets, return_offers = var_return_offers, return_categories = var_return_categories, return_filters = var_return_filtersdata_file = "result.txt")
#' result <- api_instance$SearchLocationRatingIndexes(category_ids = var_category_ids, keyword = var_keyword, location_type = var_location_type, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, search_range = var_search_range, latitude = var_latitude, longitude = var_longitude, return_overall_rating = var_return_overall_rating, distance_unit = var_distance_unit, return_retailer = var_return_retailer, return_assets = var_return_assets, return_offers = var_return_offers, return_categories = var_return_categories, return_filters = var_return_filters)
#' dput(result)
#'
#'
#' ####################  SearchRatingIndexes  ####################
#'
#' library(openapi)
#' var_ratable_type <- "ratable_type_example" # character | Filter results by a ratable type {RETAILER_LOCATION}
#' var_ratable_ids <- "ratable_ids_example" # character | Comma separated list of ratable ids to filter the resuts by (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category ids to filter the results by (Optional)
#' var_secondary_type <- "secondary_type_example" # character |  (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, COUNT, SUMMATION, AVERAGE, VALUE} (Optional)
#' var_descending <- "descending_example" # character | The order to return the search results (Optional)
#' var_start <- 56 # integer | The record to begin the return set on (Optional)
#' var_limit <- 56 # integer | The number of records to return (Optional)
#' var_latitude <- 3.4 # numeric |  (Optional)
#' var_longitude <- 3.4 # numeric |  (Optional)
#' var_return_ratable <- "return_ratable_example" # character | Determines whether to return the ratable object in the response (Optional)
#' var_return_overall_rating <- "return_overall_rating_example" # character | Determines whether to return the overall rating record instead (Optional)
#'
#' #Search Rating Indexes
#' api_instance <- RatingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchRatingIndexes(var_ratable_type, ratable_ids = var_ratable_ids, category_ids = var_category_ids, secondary_type = var_secondary_type, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, latitude = var_latitude, longitude = var_longitude, return_ratable = var_return_ratable, return_overall_rating = var_return_overall_ratingdata_file = "result.txt")
#' result <- api_instance$SearchRatingIndexes(var_ratable_type, ratable_ids = var_ratable_ids, category_ids = var_category_ids, secondary_type = var_secondary_type, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, latitude = var_latitude, longitude = var_longitude, return_ratable = var_return_ratable, return_overall_rating = var_return_overall_rating)
#' dput(result)
#'
#'
#' ####################  SearchRatings  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_filter_account_id <- 56 # integer | Filter results for a particular account (Optional)
#' var_ratable_type <- "ratable_type_example" # character | The ratable object type {RETAILER_LOCATION} (Optional)
#' var_ratable_id <- 56 # integer | The id of the ratable object (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category ids to filter the results by (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, OWNER_DISPLAY, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, VALUE} (Optional)
#' var_descending <- "descending_example" # character | The order to return the search results (Optional)
#' var_start <- 56 # integer | The record to begin the return set on (Optional)
#' var_limit <- 56 # integer | The number of records to return (Optional)
#'
#' #Search Ratings
#' api_instance <- RatingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchRatings(device_id = var_device_id, account_id = var_account_id, filter_account_id = var_filter_account_id, ratable_type = var_ratable_type, ratable_id = var_ratable_id, category_ids = var_category_ids, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchRatings(device_id = var_device_id, account_id = var_account_id, filter_account_id = var_filter_account_id, ratable_type = var_ratable_type, ratable_id = var_ratable_id, category_ids = var_category_ids, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  UpdateRating  ####################
#'
#' library(openapi)
#' var_rating_id <- 56 # integer | The id of the rating (Note: this is not the ratable object id)
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_rating_value <- 56 # integer | The rating value to update (Optional)
#' var_category_id <- 56 # integer |  (Optional)
#' var_display <- "display_example" # character | A subject title for the user rating (limited to 255 characters) (Optional)
#' var_description <- "description_example" # character | The description of the rating (Optional)
#' var_location_description <- "location_description_example" # character | The description of the location (Optional)
#' var_latitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current location of the user (Optional)
#'
#' #Update Rating
#' api_instance <- RatingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateRating(var_rating_id, device_id = var_device_id, account_id = var_account_id, rating_value = var_rating_value, category_id = var_category_id, display = var_display, description = var_description, location_description = var_location_description, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$UpdateRating(var_rating_id, device_id = var_device_id, account_id = var_account_id, rating_value = var_rating_value, category_id = var_category_id, display = var_display, description = var_description, location_description = var_location_description, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
RatingApi <- R6::R6Class(
  "RatingApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new RatingApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Rating
    #'
    #' @param ratable_type The ratable object type {RETAILER_LOCATION}
    #' @param ratable_id The id of the ratable object
    #' @param rating_value The integer value of 0-100
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param category_id (optional) 
    #' @param display (optional) A subject title for the user rating (limited to 255 characters)
    #' @param description (optional) The description of the rating
    #' @param location_description (optional) The description of the location
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RatingResponse
    CreateRating = function(ratable_type, ratable_id, rating_value, device_id = NULL, account_id = NULL, category_id = NULL, display = NULL, description = NULL, location_description = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateRatingWithHttpInfo(ratable_type, ratable_id, rating_value, device_id, account_id, category_id, display, description, location_description, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Rating
    #'
    #' @param ratable_type The ratable object type {RETAILER_LOCATION}
    #' @param ratable_id The id of the ratable object
    #' @param rating_value The integer value of 0-100
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param category_id (optional) 
    #' @param display (optional) A subject title for the user rating (limited to 255 characters)
    #' @param description (optional) The description of the rating
    #' @param location_description (optional) The description of the location
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RatingResponse) with additional information such as HTTP status code, headers
    CreateRatingWithHttpInfo = function(ratable_type, ratable_id, rating_value, device_id = NULL, account_id = NULL, category_id = NULL, display = NULL, description = NULL, location_description = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ratable_type`)) {
        stop("Missing required parameter `ratable_type`.")
      }

      if (missing(`ratable_id`)) {
        stop("Missing required parameter `ratable_id`.")
      }

      if (missing(`rating_value`)) {
        stop("Missing required parameter `rating_value`.")
      }

      if (!missing(`ratable_type`) && is.null(`ratable_type`)) {
        stop("Invalid value for `ratable_type` when calling RatingApi$CreateRating, `ratable_type` is not nullable")
      }

      if (!missing(`ratable_id`) && is.null(`ratable_id`)) {
        stop("Invalid value for `ratable_id` when calling RatingApi$CreateRating, `ratable_id` is not nullable")
      }

      if (!missing(`rating_value`) && is.null(`rating_value`)) {
        stop("Invalid value for `rating_value` when calling RatingApi$CreateRating, `rating_value` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RatingApi$CreateRating, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RatingApi$CreateRating, `account_id` is not nullable")
      }

      if (!missing(`category_id`) && is.null(`category_id`)) {
        stop("Invalid value for `category_id` when calling RatingApi$CreateRating, `category_id` is not nullable")
      }

      if (!missing(`display`) && is.null(`display`)) {
        stop("Invalid value for `display` when calling RatingApi$CreateRating, `display` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling RatingApi$CreateRating, `description` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling RatingApi$CreateRating, `location_description` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RatingApi$CreateRating, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RatingApi$CreateRating, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["ratableType"]] <- `ratable_type`

      query_params[["ratableId"]] <- `ratable_id`

      query_params[["ratingValue"]] <- `rating_value`

      query_params[["categoryId"]] <- `category_id`

      query_params[["display"]] <- `display`

      query_params[["description"]] <- `description`

      query_params[["locationDescription"]] <- `location_description`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/rating/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RatingResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Rating
    #'
    #' @param rating_id The ID of the rating to delete
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteRating = function(rating_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteRatingWithHttpInfo(rating_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Rating
    #'
    #' @param rating_id The ID of the rating to delete
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteRatingWithHttpInfo = function(rating_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`rating_id`)) {
        stop("Missing required parameter `rating_id`.")
      }

      if (!missing(`rating_id`) && is.null(`rating_id`)) {
        stop("Invalid value for `rating_id` when calling RatingApi$DeleteRating, `rating_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RatingApi$DeleteRating, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RatingApi$DeleteRating, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["ratingId"]] <- `rating_id`

      local_var_url_path <- "/rating/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Location Rating Indexes
    #'
    #' @param category_ids (optional) Comma separated list of category ids to filter the results by
    #' @param keyword (optional) The keyword used to search
    #' @param location_type (optional) The type of location to filter the results by
    #' @param sort_field (optional) The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, COUNT, SUMMATION, AVERAGE, VALUE}
    #' @param descending (optional) The order to return the search results
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param search_range (optional) The search radius in kilometers to filter results
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param return_overall_rating (optional) Determines whether to return the overall rating record instead
    #' @param distance_unit (optional) 
    #' @param return_retailer (optional) whether to return the retailer or not
    #' @param return_assets (optional) whether to return the assets or not
    #' @param return_offers (optional) whether to return the offers or not
    #' @param return_categories (optional) whether to return the categories or not
    #' @param return_filters (optional) whether to return the filters or not
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[RatingIndexResponse]
    SearchLocationRatingIndexes = function(category_ids = NULL, keyword = NULL, location_type = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, search_range = NULL, latitude = NULL, longitude = NULL, return_overall_rating = NULL, distance_unit = NULL, return_retailer = NULL, return_assets = NULL, return_offers = NULL, return_categories = NULL, return_filters = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchLocationRatingIndexesWithHttpInfo(category_ids, keyword, location_type, sort_field, descending, start, limit, search_range, latitude, longitude, return_overall_rating, distance_unit, return_retailer, return_assets, return_offers, return_categories, return_filters, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Location Rating Indexes
    #'
    #' @param category_ids (optional) Comma separated list of category ids to filter the results by
    #' @param keyword (optional) The keyword used to search
    #' @param location_type (optional) The type of location to filter the results by
    #' @param sort_field (optional) The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, COUNT, SUMMATION, AVERAGE, VALUE}
    #' @param descending (optional) The order to return the search results
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param search_range (optional) The search radius in kilometers to filter results
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param return_overall_rating (optional) Determines whether to return the overall rating record instead
    #' @param distance_unit (optional) 
    #' @param return_retailer (optional) whether to return the retailer or not
    #' @param return_assets (optional) whether to return the assets or not
    #' @param return_offers (optional) whether to return the offers or not
    #' @param return_categories (optional) whether to return the categories or not
    #' @param return_filters (optional) whether to return the filters or not
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[RatingIndexResponse]) with additional information such as HTTP status code, headers
    SearchLocationRatingIndexesWithHttpInfo = function(category_ids = NULL, keyword = NULL, location_type = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, search_range = NULL, latitude = NULL, longitude = NULL, return_overall_rating = NULL, distance_unit = NULL, return_retailer = NULL, return_assets = NULL, return_offers = NULL, return_categories = NULL, return_filters = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling RatingApi$SearchLocationRatingIndexes, `category_ids` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling RatingApi$SearchLocationRatingIndexes, `keyword` is not nullable")
      }

      if (!missing(`location_type`) && is.null(`location_type`)) {
        stop("Invalid value for `location_type` when calling RatingApi$SearchLocationRatingIndexes, `location_type` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling RatingApi$SearchLocationRatingIndexes, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling RatingApi$SearchLocationRatingIndexes, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling RatingApi$SearchLocationRatingIndexes, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling RatingApi$SearchLocationRatingIndexes, `limit` is not nullable")
      }

      if (!missing(`search_range`) && is.null(`search_range`)) {
        stop("Invalid value for `search_range` when calling RatingApi$SearchLocationRatingIndexes, `search_range` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RatingApi$SearchLocationRatingIndexes, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RatingApi$SearchLocationRatingIndexes, `longitude` is not nullable")
      }

      if (!missing(`return_overall_rating`) && is.null(`return_overall_rating`)) {
        stop("Invalid value for `return_overall_rating` when calling RatingApi$SearchLocationRatingIndexes, `return_overall_rating` is not nullable")
      }

      if (!missing(`distance_unit`) && is.null(`distance_unit`)) {
        stop("Invalid value for `distance_unit` when calling RatingApi$SearchLocationRatingIndexes, `distance_unit` is not nullable")
      }

      if (!missing(`return_retailer`) && is.null(`return_retailer`)) {
        stop("Invalid value for `return_retailer` when calling RatingApi$SearchLocationRatingIndexes, `return_retailer` is not nullable")
      }

      if (!missing(`return_assets`) && is.null(`return_assets`)) {
        stop("Invalid value for `return_assets` when calling RatingApi$SearchLocationRatingIndexes, `return_assets` is not nullable")
      }

      if (!missing(`return_offers`) && is.null(`return_offers`)) {
        stop("Invalid value for `return_offers` when calling RatingApi$SearchLocationRatingIndexes, `return_offers` is not nullable")
      }

      if (!missing(`return_categories`) && is.null(`return_categories`)) {
        stop("Invalid value for `return_categories` when calling RatingApi$SearchLocationRatingIndexes, `return_categories` is not nullable")
      }

      if (!missing(`return_filters`) && is.null(`return_filters`)) {
        stop("Invalid value for `return_filters` when calling RatingApi$SearchLocationRatingIndexes, `return_filters` is not nullable")
      }

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["keyword"]] <- `keyword`

      query_params[["locationType"]] <- `location_type`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "RATABLE_TYPE", "RATABLE_ID", "RATABLE_DISPLAY", "CATEGORY_ID", "CATEGORY_NAME", "CATEGORY_SHORT_NAME", "CATEGORY_DISPLAY", "COUNT", "SUMMATION", "AVERAGE", "VALUE"))) {
        stop("Invalid value for sort_field when calling RatingApi$SearchLocationRatingIndexes. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, COUNT, SUMMATION, AVERAGE, VALUE].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["searchRange"]] <- `search_range`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["returnOverallRating"]] <- `return_overall_rating`

      if (!is.null(`distance_unit`) && !(`distance_unit` %in% c("MILES", "KILOMETERS"))) {
        stop("Invalid value for distance_unit when calling RatingApi$SearchLocationRatingIndexes. Must be [MILES, KILOMETERS].")
      }
      query_params[["distanceUnit"]] <- `distance_unit`

      query_params[["returnRetailer"]] <- `return_retailer`

      query_params[["returnAssets"]] <- `return_assets`

      query_params[["returnOffers"]] <- `return_offers`

      query_params[["returnCategories"]] <- `return_categories`

      query_params[["returnFilters"]] <- `return_filters`

      local_var_url_path <- "/location/rating/index/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[RatingIndexResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Rating Indexes
    #'
    #' @param ratable_type Filter results by a ratable type {RETAILER_LOCATION}
    #' @param ratable_ids (optional) Comma separated list of ratable ids to filter the resuts by
    #' @param category_ids (optional) Comma separated list of category ids to filter the results by
    #' @param secondary_type (optional) 
    #' @param keyword (optional) The keyword used to search
    #' @param sort_field (optional) The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, COUNT, SUMMATION, AVERAGE, VALUE}
    #' @param descending (optional) The order to return the search results
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param latitude (optional) 
    #' @param longitude (optional) 
    #' @param return_ratable (optional) Determines whether to return the ratable object in the response
    #' @param return_overall_rating (optional) Determines whether to return the overall rating record instead
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[RatingIndexResponse]
    SearchRatingIndexes = function(ratable_type, ratable_ids = NULL, category_ids = NULL, secondary_type = NULL, keyword = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, latitude = NULL, longitude = NULL, return_ratable = NULL, return_overall_rating = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchRatingIndexesWithHttpInfo(ratable_type, ratable_ids, category_ids, secondary_type, keyword, sort_field, descending, start, limit, latitude, longitude, return_ratable, return_overall_rating, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Rating Indexes
    #'
    #' @param ratable_type Filter results by a ratable type {RETAILER_LOCATION}
    #' @param ratable_ids (optional) Comma separated list of ratable ids to filter the resuts by
    #' @param category_ids (optional) Comma separated list of category ids to filter the results by
    #' @param secondary_type (optional) 
    #' @param keyword (optional) The keyword used to search
    #' @param sort_field (optional) The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, COUNT, SUMMATION, AVERAGE, VALUE}
    #' @param descending (optional) The order to return the search results
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param latitude (optional) 
    #' @param longitude (optional) 
    #' @param return_ratable (optional) Determines whether to return the ratable object in the response
    #' @param return_overall_rating (optional) Determines whether to return the overall rating record instead
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[RatingIndexResponse]) with additional information such as HTTP status code, headers
    SearchRatingIndexesWithHttpInfo = function(ratable_type, ratable_ids = NULL, category_ids = NULL, secondary_type = NULL, keyword = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, latitude = NULL, longitude = NULL, return_ratable = NULL, return_overall_rating = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ratable_type`)) {
        stop("Missing required parameter `ratable_type`.")
      }

      if (!missing(`ratable_type`) && is.null(`ratable_type`)) {
        stop("Invalid value for `ratable_type` when calling RatingApi$SearchRatingIndexes, `ratable_type` is not nullable")
      }

      if (!missing(`ratable_ids`) && is.null(`ratable_ids`)) {
        stop("Invalid value for `ratable_ids` when calling RatingApi$SearchRatingIndexes, `ratable_ids` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling RatingApi$SearchRatingIndexes, `category_ids` is not nullable")
      }

      if (!missing(`secondary_type`) && is.null(`secondary_type`)) {
        stop("Invalid value for `secondary_type` when calling RatingApi$SearchRatingIndexes, `secondary_type` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling RatingApi$SearchRatingIndexes, `keyword` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling RatingApi$SearchRatingIndexes, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling RatingApi$SearchRatingIndexes, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling RatingApi$SearchRatingIndexes, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling RatingApi$SearchRatingIndexes, `limit` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RatingApi$SearchRatingIndexes, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RatingApi$SearchRatingIndexes, `longitude` is not nullable")
      }

      if (!missing(`return_ratable`) && is.null(`return_ratable`)) {
        stop("Invalid value for `return_ratable` when calling RatingApi$SearchRatingIndexes, `return_ratable` is not nullable")
      }

      if (!missing(`return_overall_rating`) && is.null(`return_overall_rating`)) {
        stop("Invalid value for `return_overall_rating` when calling RatingApi$SearchRatingIndexes, `return_overall_rating` is not nullable")
      }

      if (!is.null(`ratable_type`) && !(`ratable_type` %in% c("LOCATABLE", "RESERVABLE", "PERMISSIONABLE", "NOTABLE", "ASSETABLE", "LIKABLE", "FLAGABLE", "FAVORITABLE", "RATABLE", "ALBUM", "COLLECTION", "APPLICATION", "APPLICATION_SETTING", "APPLICATION_CERT", "APPLICATION_PLACEMENT", "ACCOUNT", "ACCOUNT_SETTING", "GAME_LEVEL", "PACK", "MISSION", "TOURNAMENT", "ASSET", "ALBUM_CONTEST", "THEME_DESCRIPTOR", "OFFER", "OFFER_LOCATION", "EVENT", "RETAILER", "RETAILER_LOCATION", "NOTE", "CREATIVE", "FAVORITE", "LIKE", "RATING", "ANALYTIC", "THIRD_PARTY_CREDENTIAL", "THIRD_PARTY_NETWORK", "REGION"))) {
        stop("Invalid value for ratable_type when calling RatingApi$SearchRatingIndexes. Must be [LOCATABLE, RESERVABLE, PERMISSIONABLE, NOTABLE, ASSETABLE, LIKABLE, FLAGABLE, FAVORITABLE, RATABLE, ALBUM, COLLECTION, APPLICATION, APPLICATION_SETTING, APPLICATION_CERT, APPLICATION_PLACEMENT, ACCOUNT, ACCOUNT_SETTING, GAME_LEVEL, PACK, MISSION, TOURNAMENT, ASSET, ALBUM_CONTEST, THEME_DESCRIPTOR, OFFER, OFFER_LOCATION, EVENT, RETAILER, RETAILER_LOCATION, NOTE, CREATIVE, FAVORITE, LIKE, RATING, ANALYTIC, THIRD_PARTY_CREDENTIAL, THIRD_PARTY_NETWORK, REGION].")
      }
      query_params[["ratableType"]] <- `ratable_type`

      query_params[["ratableIds"]] <- `ratable_ids`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["secondaryType"]] <- `secondary_type`

      query_params[["keyword"]] <- `keyword`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "RATABLE_TYPE", "RATABLE_ID", "RATABLE_DISPLAY", "CATEGORY_ID", "CATEGORY_NAME", "CATEGORY_SHORT_NAME", "CATEGORY_DISPLAY", "COUNT", "SUMMATION", "AVERAGE", "VALUE"))) {
        stop("Invalid value for sort_field when calling RatingApi$SearchRatingIndexes. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, COUNT, SUMMATION, AVERAGE, VALUE].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["returnRatable"]] <- `return_ratable`

      query_params[["returnOverallRating"]] <- `return_overall_rating`

      local_var_url_path <- "/rating/index/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[RatingIndexResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Ratings
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param filter_account_id (optional) Filter results for a particular account
    #' @param ratable_type (optional) The ratable object type {RETAILER_LOCATION}
    #' @param ratable_id (optional) The id of the ratable object
    #' @param category_ids (optional) Comma separated list of category ids to filter the results by
    #' @param keyword (optional) The keyword used to search
    #' @param sort_field (optional) The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, OWNER_DISPLAY, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, VALUE}
    #' @param descending (optional) The order to return the search results
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[RatingResponse]
    SearchRatings = function(device_id = NULL, account_id = NULL, filter_account_id = NULL, ratable_type = NULL, ratable_id = NULL, category_ids = NULL, keyword = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchRatingsWithHttpInfo(device_id, account_id, filter_account_id, ratable_type, ratable_id, category_ids, keyword, sort_field, descending, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Ratings
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param filter_account_id (optional) Filter results for a particular account
    #' @param ratable_type (optional) The ratable object type {RETAILER_LOCATION}
    #' @param ratable_id (optional) The id of the ratable object
    #' @param category_ids (optional) Comma separated list of category ids to filter the results by
    #' @param keyword (optional) The keyword used to search
    #' @param sort_field (optional) The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, OWNER_DISPLAY, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, VALUE}
    #' @param descending (optional) The order to return the search results
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[RatingResponse]) with additional information such as HTTP status code, headers
    SearchRatingsWithHttpInfo = function(device_id = NULL, account_id = NULL, filter_account_id = NULL, ratable_type = NULL, ratable_id = NULL, category_ids = NULL, keyword = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RatingApi$SearchRatings, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RatingApi$SearchRatings, `account_id` is not nullable")
      }

      if (!missing(`filter_account_id`) && is.null(`filter_account_id`)) {
        stop("Invalid value for `filter_account_id` when calling RatingApi$SearchRatings, `filter_account_id` is not nullable")
      }

      if (!missing(`ratable_type`) && is.null(`ratable_type`)) {
        stop("Invalid value for `ratable_type` when calling RatingApi$SearchRatings, `ratable_type` is not nullable")
      }

      if (!missing(`ratable_id`) && is.null(`ratable_id`)) {
        stop("Invalid value for `ratable_id` when calling RatingApi$SearchRatings, `ratable_id` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling RatingApi$SearchRatings, `category_ids` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling RatingApi$SearchRatings, `keyword` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling RatingApi$SearchRatings, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling RatingApi$SearchRatings, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling RatingApi$SearchRatings, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling RatingApi$SearchRatings, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["filterAccountId"]] <- `filter_account_id`

      query_params[["ratableType"]] <- `ratable_type`

      query_params[["ratableId"]] <- `ratable_id`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["keyword"]] <- `keyword`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "OWNER_DISPLAY", "RATABLE_TYPE", "RATABLE_ID", "RATABLE_DISPLAY", "CATEGORY_ID", "CATEGORY_NAME", "CATEGORY_SHORT_NAME", "CATEGORY_DISPLAY", "VALUE"))) {
        stop("Invalid value for sort_field when calling RatingApi$SearchRatings. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, OWNER_DISPLAY, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, VALUE].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/rating/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[RatingResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Rating
    #'
    #' @param rating_id The id of the rating (Note: this is not the ratable object id)
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param rating_value (optional) The rating value to update
    #' @param category_id (optional) 
    #' @param display (optional) A subject title for the user rating (limited to 255 characters)
    #' @param description (optional) The description of the rating
    #' @param location_description (optional) The description of the location
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RatingResponse
    UpdateRating = function(rating_id, device_id = NULL, account_id = NULL, rating_value = NULL, category_id = NULL, display = NULL, description = NULL, location_description = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateRatingWithHttpInfo(rating_id, device_id, account_id, rating_value, category_id, display, description, location_description, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Rating
    #'
    #' @param rating_id The id of the rating (Note: this is not the ratable object id)
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param rating_value (optional) The rating value to update
    #' @param category_id (optional) 
    #' @param display (optional) A subject title for the user rating (limited to 255 characters)
    #' @param description (optional) The description of the rating
    #' @param location_description (optional) The description of the location
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RatingResponse) with additional information such as HTTP status code, headers
    UpdateRatingWithHttpInfo = function(rating_id, device_id = NULL, account_id = NULL, rating_value = NULL, category_id = NULL, display = NULL, description = NULL, location_description = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`rating_id`)) {
        stop("Missing required parameter `rating_id`.")
      }

      if (!missing(`rating_id`) && is.null(`rating_id`)) {
        stop("Invalid value for `rating_id` when calling RatingApi$UpdateRating, `rating_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RatingApi$UpdateRating, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RatingApi$UpdateRating, `account_id` is not nullable")
      }

      if (!missing(`rating_value`) && is.null(`rating_value`)) {
        stop("Invalid value for `rating_value` when calling RatingApi$UpdateRating, `rating_value` is not nullable")
      }

      if (!missing(`category_id`) && is.null(`category_id`)) {
        stop("Invalid value for `category_id` when calling RatingApi$UpdateRating, `category_id` is not nullable")
      }

      if (!missing(`display`) && is.null(`display`)) {
        stop("Invalid value for `display` when calling RatingApi$UpdateRating, `display` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling RatingApi$UpdateRating, `description` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling RatingApi$UpdateRating, `location_description` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RatingApi$UpdateRating, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RatingApi$UpdateRating, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["ratingId"]] <- `rating_id`

      query_params[["ratingValue"]] <- `rating_value`

      query_params[["categoryId"]] <- `category_id`

      query_params[["display"]] <- `display`

      query_params[["description"]] <- `description`

      query_params[["locationDescription"]] <- `location_description`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/rating/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RatingResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
