#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Reservation operations
#' @description ReservationApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateReservation  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_start_date <- 56 # integer | The start date (Optional)
#' var_end_date <- 56 # integer | The end date (Optional)
#' var_offer_id <- 56 # integer | The id of the offer being added (offerId or offeLocationId required) (Optional)
#' var_offer_location_id <- 56 # integer | The id of the offer location being added (offerId or offeLocationId required) (Optional)
#' var_app_key <- "app_key_example" # character | The application requesting the reservation (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#'
#' #Create Reservation
#' api_instance <- ReservationApi$new()
#'
#' api_instance$CreateReservation(device_id = var_device_id, account_id = var_account_id, start_date = var_start_date, end_date = var_end_date, offer_id = var_offer_id, offer_location_id = var_offer_location_id, app_key = var_app_key, meta_data = var_meta_data)
#'
#'
#' ####################  DeleteReservation  ####################
#'
#' library(openapi)
#' var_reservation_id <- 56 # integer | The reservation id
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#'
#' #Delete Reservation
#' api_instance <- ReservationApi$new()
#'
#' api_instance$DeleteReservation(var_reservation_id, device_id = var_device_id, account_id = var_account_id)
#'
#'
#' ####################  ReservableAvailability  ####################
#'
#' library(openapi)
#' var_reservable_id <- 56 # integer | the id of the reservation
#' var_reservable_type <- "reservable_type_example" # character | the type of reservation
#' var_device_id <- "device_id_example" # character | the device id of the reservation (Optional)
#' var_account_id <- 56 # integer | the id of the logged in user (Optional)
#' var_availability <- "availability_example" # character | Availability (Optional)
#' var_availability_summary <- "availability_summary_example" # character | Availability Summary (Optional)
#'
#' #Update Availability
#' api_instance <- ReservationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ReservableAvailability(var_reservable_id, var_reservable_type, device_id = var_device_id, account_id = var_account_id, availability = var_availability, availability_summary = var_availability_summarydata_file = "result.txt")
#' result <- api_instance$ReservableAvailability(var_reservable_id, var_reservable_type, device_id = var_device_id, account_id = var_account_id, availability = var_availability, availability_summary = var_availability_summary)
#' dput(result)
#'
#'
#' ####################  SearchAvailability  ####################
#'
#' library(openapi)
#' var_reservable_id <- 56 # integer | the id of the reservation
#' var_reservable_type <- "reservable_type_example" # character | the reservable type
#' var_device_id <- "device_id_example" # character | the device ID that the reservation is on (Optional)
#' var_account_id <- 56 # integer | the id of the logged in user (Optional)
#' var_start_date <- 56 # integer | the start date of the reservation (Optional)
#' var_end_date <- 56 # integer | the end date of the reservation (Optional)
#' var_start <- 0 # integer | the start of the index and/or pagination (Optional)
#' var_limit <- 100 # integer | the limit of the index and/or pagination (Optional)
#'
#' #Search Availability
#' api_instance <- ReservationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchAvailability(var_reservable_id, var_reservable_type, device_id = var_device_id, account_id = var_account_id, start_date = var_start_date, end_date = var_end_date, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchAvailability(var_reservable_id, var_reservable_type, device_id = var_device_id, account_id = var_account_id, start_date = var_start_date, end_date = var_end_date, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  SearchReservations  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | Device Id (Optional)
#' var_app_key <- "app_key_example" # character | Appilcation Key (Optional)
#' var_account_id <- 56 # integer | the id of the logged in user (Optional)
#' var_filter_account_id <- 56 # integer | filter reservations by account ID (Optional)
#' var_reservable_id <- 56 # integer | the reservation ID (Optional)
#' var_reservable_type <- "reservable_type_example" # character | the type of reservation (Optional)
#' var_keyword <- "keyword_example" # character | the keyword to search the reservation on (Optional)
#' var_start_date <- 56 # integer | the start date of the reservation search (Optional)
#' var_end_date <- 56 # integer | the end date of the reservation search (Optional)
#' var_start <- 0 # integer | the start of the index and/or pagination (Optional)
#' var_limit <- 100 # integer | the limit of the index and/or pagination (Optional)
#'
#' #Search Reservations
#' api_instance <- ReservationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchReservations(device_id = var_device_id, app_key = var_app_key, account_id = var_account_id, filter_account_id = var_filter_account_id, reservable_id = var_reservable_id, reservable_type = var_reservable_type, keyword = var_keyword, start_date = var_start_date, end_date = var_end_date, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchReservations(device_id = var_device_id, app_key = var_app_key, account_id = var_account_id, filter_account_id = var_filter_account_id, reservable_id = var_reservable_id, reservable_type = var_reservable_type, keyword = var_keyword, start_date = var_start_date, end_date = var_end_date, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  SearchSchedule  ####################
#'
#' library(openapi)
#' var_reservable_id <- 56 # integer | the id of the reservation
#' var_reservable_type <- "reservable_type_example" # character | the reservation type
#' var_start_date <- 56 # integer | the start date of the reservation
#' var_end_date <- 56 # integer | the end date of the reservation
#' var_device_id <- "device_id_example" # character | the id of the device that the reservation is on (Optional)
#' var_account_id <- 56 # integer | the id of the logged in user (Optional)
#' var_time_bucket_mins <- 30 # integer | the length of time in minutes to search on for reservation (Optional)
#'
#' #Search Schedule
#' api_instance <- ReservationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchSchedule(var_reservable_id, var_reservable_type, var_start_date, var_end_date, device_id = var_device_id, account_id = var_account_id, time_bucket_mins = var_time_bucket_minsdata_file = "result.txt")
#' result <- api_instance$SearchSchedule(var_reservable_id, var_reservable_type, var_start_date, var_end_date, device_id = var_device_id, account_id = var_account_id, time_bucket_mins = var_time_bucket_mins)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ReservationApi <- R6::R6Class(
  "ReservationApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ReservationApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Reservation
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param start_date (optional) The start date
    #' @param end_date (optional) The end date
    #' @param offer_id (optional) The id of the offer being added (offerId or offeLocationId required)
    #' @param offer_location_id (optional) The id of the offer location being added (offerId or offeLocationId required)
    #' @param app_key (optional) The application requesting the reservation
    #' @param meta_data (optional) External custom client defined data
    #' @param ... Other optional arguments
    #'
    #' @return void
    CreateReservation = function(device_id = NULL, account_id = NULL, start_date = NULL, end_date = NULL, offer_id = NULL, offer_location_id = NULL, app_key = NULL, meta_data = NULL, ...) {
      local_var_response <- self$CreateReservationWithHttpInfo(device_id, account_id, start_date, end_date, offer_id, offer_location_id, app_key, meta_data, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Reservation
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param start_date (optional) The start date
    #' @param end_date (optional) The end date
    #' @param offer_id (optional) The id of the offer being added (offerId or offeLocationId required)
    #' @param offer_location_id (optional) The id of the offer location being added (offerId or offeLocationId required)
    #' @param app_key (optional) The application requesting the reservation
    #' @param meta_data (optional) External custom client defined data
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CreateReservationWithHttpInfo = function(device_id = NULL, account_id = NULL, start_date = NULL, end_date = NULL, offer_id = NULL, offer_location_id = NULL, app_key = NULL, meta_data = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ReservationApi$CreateReservation, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReservationApi$CreateReservation, `account_id` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling ReservationApi$CreateReservation, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling ReservationApi$CreateReservation, `end_date` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling ReservationApi$CreateReservation, `offer_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling ReservationApi$CreateReservation, `offer_location_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ReservationApi$CreateReservation, `app_key` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling ReservationApi$CreateReservation, `meta_data` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["offerId"]] <- `offer_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["metaData"]] <- `meta_data`

      local_var_url_path <- "/reservation/create"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Reservation
    #'
    #' @param reservation_id The reservation id
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param ... Other optional arguments
    #'
    #' @return void
    DeleteReservation = function(reservation_id, device_id = NULL, account_id = NULL, ...) {
      local_var_response <- self$DeleteReservationWithHttpInfo(reservation_id, device_id, account_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Reservation
    #'
    #' @param reservation_id The reservation id
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    DeleteReservationWithHttpInfo = function(reservation_id, device_id = NULL, account_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`reservation_id`)) {
        stop("Missing required parameter `reservation_id`.")
      }

      if (!missing(`reservation_id`) && is.null(`reservation_id`)) {
        stop("Invalid value for `reservation_id` when calling ReservationApi$DeleteReservation, `reservation_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ReservationApi$DeleteReservation, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReservationApi$DeleteReservation, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["reservationId"]] <- `reservation_id`

      local_var_url_path <- "/reservation/delete"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Availability
    #'
    #' @param reservable_id the id of the reservation
    #' @param reservable_type the type of reservation
    #' @param device_id (optional) the device id of the reservation
    #' @param account_id (optional) the id of the logged in user
    #' @param availability (optional) Availability
    #' @param availability_summary (optional) Availability Summary
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[AvailabilityResponse]
    ReservableAvailability = function(reservable_id, reservable_type, device_id = NULL, account_id = NULL, availability = NULL, availability_summary = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ReservableAvailabilityWithHttpInfo(reservable_id, reservable_type, device_id, account_id, availability, availability_summary, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Availability
    #'
    #' @param reservable_id the id of the reservation
    #' @param reservable_type the type of reservation
    #' @param device_id (optional) the device id of the reservation
    #' @param account_id (optional) the id of the logged in user
    #' @param availability (optional) Availability
    #' @param availability_summary (optional) Availability Summary
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[AvailabilityResponse]) with additional information such as HTTP status code, headers
    ReservableAvailabilityWithHttpInfo = function(reservable_id, reservable_type, device_id = NULL, account_id = NULL, availability = NULL, availability_summary = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`reservable_id`)) {
        stop("Missing required parameter `reservable_id`.")
      }

      if (missing(`reservable_type`)) {
        stop("Missing required parameter `reservable_type`.")
      }

      if (!missing(`reservable_id`) && is.null(`reservable_id`)) {
        stop("Invalid value for `reservable_id` when calling ReservationApi$ReservableAvailability, `reservable_id` is not nullable")
      }

      if (!missing(`reservable_type`) && is.null(`reservable_type`)) {
        stop("Invalid value for `reservable_type` when calling ReservationApi$ReservableAvailability, `reservable_type` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ReservationApi$ReservableAvailability, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReservationApi$ReservableAvailability, `account_id` is not nullable")
      }

      if (!missing(`availability`) && is.null(`availability`)) {
        stop("Invalid value for `availability` when calling ReservationApi$ReservableAvailability, `availability` is not nullable")
      }

      if (!missing(`availability_summary`) && is.null(`availability_summary`)) {
        stop("Invalid value for `availability_summary` when calling ReservationApi$ReservableAvailability, `availability_summary` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["reservableId"]] <- `reservable_id`

      if (!is.null(`reservable_type`) && !(`reservable_type` %in% c("LOCATABLE", "RESERVABLE", "PERMISSIONABLE", "NOTABLE", "ASSETABLE", "LIKABLE", "FLAGABLE", "FAVORITABLE", "RATABLE", "ALBUM", "COLLECTION", "APPLICATION", "APPLICATION_SETTING", "APPLICATION_CERT", "APPLICATION_PLACEMENT", "ACCOUNT", "ACCOUNT_SETTING", "GAME_LEVEL", "PACK", "MISSION", "TOURNAMENT", "ASSET", "ALBUM_CONTEST", "THEME_DESCRIPTOR", "OFFER", "OFFER_LOCATION", "EVENT", "RETAILER", "RETAILER_LOCATION", "NOTE", "CREATIVE", "FAVORITE", "LIKE", "RATING", "ANALYTIC", "THIRD_PARTY_CREDENTIAL", "THIRD_PARTY_NETWORK", "REGION"))) {
        stop("Invalid value for reservable_type when calling ReservationApi$ReservableAvailability. Must be [LOCATABLE, RESERVABLE, PERMISSIONABLE, NOTABLE, ASSETABLE, LIKABLE, FLAGABLE, FAVORITABLE, RATABLE, ALBUM, COLLECTION, APPLICATION, APPLICATION_SETTING, APPLICATION_CERT, APPLICATION_PLACEMENT, ACCOUNT, ACCOUNT_SETTING, GAME_LEVEL, PACK, MISSION, TOURNAMENT, ASSET, ALBUM_CONTEST, THEME_DESCRIPTOR, OFFER, OFFER_LOCATION, EVENT, RETAILER, RETAILER_LOCATION, NOTE, CREATIVE, FAVORITE, LIKE, RATING, ANALYTIC, THIRD_PARTY_CREDENTIAL, THIRD_PARTY_NETWORK, REGION].")
      }
      query_params[["reservableType"]] <- `reservable_type`

      query_params[["availability"]] <- `availability`

      query_params[["availabilitySummary"]] <- `availability_summary`

      local_var_url_path <- "/reservable/availability/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[AvailabilityResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Availability
    #'
    #' @param reservable_id the id of the reservation
    #' @param reservable_type the reservable type
    #' @param device_id (optional) the device ID that the reservation is on
    #' @param account_id (optional) the id of the logged in user
    #' @param start_date (optional) the start date of the reservation
    #' @param end_date (optional) the end date of the reservation
    #' @param start (optional) the start of the index and/or pagination (default value: 0)
    #' @param limit (optional) the limit of the index and/or pagination (default value: 100)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[AvailabilityResponse]
    SearchAvailability = function(reservable_id, reservable_type, device_id = NULL, account_id = NULL, start_date = NULL, end_date = NULL, start = 0, limit = 100, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchAvailabilityWithHttpInfo(reservable_id, reservable_type, device_id, account_id, start_date, end_date, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Availability
    #'
    #' @param reservable_id the id of the reservation
    #' @param reservable_type the reservable type
    #' @param device_id (optional) the device ID that the reservation is on
    #' @param account_id (optional) the id of the logged in user
    #' @param start_date (optional) the start date of the reservation
    #' @param end_date (optional) the end date of the reservation
    #' @param start (optional) the start of the index and/or pagination (default value: 0)
    #' @param limit (optional) the limit of the index and/or pagination (default value: 100)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[AvailabilityResponse]) with additional information such as HTTP status code, headers
    SearchAvailabilityWithHttpInfo = function(reservable_id, reservable_type, device_id = NULL, account_id = NULL, start_date = NULL, end_date = NULL, start = 0, limit = 100, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`reservable_id`)) {
        stop("Missing required parameter `reservable_id`.")
      }

      if (missing(`reservable_type`)) {
        stop("Missing required parameter `reservable_type`.")
      }

      if (!missing(`reservable_id`) && is.null(`reservable_id`)) {
        stop("Invalid value for `reservable_id` when calling ReservationApi$SearchAvailability, `reservable_id` is not nullable")
      }

      if (!missing(`reservable_type`) && is.null(`reservable_type`)) {
        stop("Invalid value for `reservable_type` when calling ReservationApi$SearchAvailability, `reservable_type` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ReservationApi$SearchAvailability, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReservationApi$SearchAvailability, `account_id` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling ReservationApi$SearchAvailability, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling ReservationApi$SearchAvailability, `end_date` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ReservationApi$SearchAvailability, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ReservationApi$SearchAvailability, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["reservableId"]] <- `reservable_id`

      if (!is.null(`reservable_type`) && !(`reservable_type` %in% c("LOCATABLE", "RESERVABLE", "PERMISSIONABLE", "NOTABLE", "ASSETABLE", "LIKABLE", "FLAGABLE", "FAVORITABLE", "RATABLE", "ALBUM", "COLLECTION", "APPLICATION", "APPLICATION_SETTING", "APPLICATION_CERT", "APPLICATION_PLACEMENT", "ACCOUNT", "ACCOUNT_SETTING", "GAME_LEVEL", "PACK", "MISSION", "TOURNAMENT", "ASSET", "ALBUM_CONTEST", "THEME_DESCRIPTOR", "OFFER", "OFFER_LOCATION", "EVENT", "RETAILER", "RETAILER_LOCATION", "NOTE", "CREATIVE", "FAVORITE", "LIKE", "RATING", "ANALYTIC", "THIRD_PARTY_CREDENTIAL", "THIRD_PARTY_NETWORK", "REGION"))) {
        stop("Invalid value for reservable_type when calling ReservationApi$SearchAvailability. Must be [LOCATABLE, RESERVABLE, PERMISSIONABLE, NOTABLE, ASSETABLE, LIKABLE, FLAGABLE, FAVORITABLE, RATABLE, ALBUM, COLLECTION, APPLICATION, APPLICATION_SETTING, APPLICATION_CERT, APPLICATION_PLACEMENT, ACCOUNT, ACCOUNT_SETTING, GAME_LEVEL, PACK, MISSION, TOURNAMENT, ASSET, ALBUM_CONTEST, THEME_DESCRIPTOR, OFFER, OFFER_LOCATION, EVENT, RETAILER, RETAILER_LOCATION, NOTE, CREATIVE, FAVORITE, LIKE, RATING, ANALYTIC, THIRD_PARTY_CREDENTIAL, THIRD_PARTY_NETWORK, REGION].")
      }
      query_params[["reservableType"]] <- `reservable_type`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/reservable/availability/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[AvailabilityResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Reservations
    #'
    #' @param device_id (optional) Device Id
    #' @param app_key (optional) Appilcation Key
    #' @param account_id (optional) the id of the logged in user
    #' @param filter_account_id (optional) filter reservations by account ID
    #' @param reservable_id (optional) the reservation ID
    #' @param reservable_type (optional) the type of reservation
    #' @param keyword (optional) the keyword to search the reservation on
    #' @param start_date (optional) the start date of the reservation search
    #' @param end_date (optional) the end date of the reservation search
    #' @param start (optional) the start of the index and/or pagination (default value: 0)
    #' @param limit (optional) the limit of the index and/or pagination (default value: 100)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ReservationResponse]
    SearchReservations = function(device_id = NULL, app_key = NULL, account_id = NULL, filter_account_id = NULL, reservable_id = NULL, reservable_type = NULL, keyword = NULL, start_date = NULL, end_date = NULL, start = 0, limit = 100, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchReservationsWithHttpInfo(device_id, app_key, account_id, filter_account_id, reservable_id, reservable_type, keyword, start_date, end_date, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Reservations
    #'
    #' @param device_id (optional) Device Id
    #' @param app_key (optional) Appilcation Key
    #' @param account_id (optional) the id of the logged in user
    #' @param filter_account_id (optional) filter reservations by account ID
    #' @param reservable_id (optional) the reservation ID
    #' @param reservable_type (optional) the type of reservation
    #' @param keyword (optional) the keyword to search the reservation on
    #' @param start_date (optional) the start date of the reservation search
    #' @param end_date (optional) the end date of the reservation search
    #' @param start (optional) the start of the index and/or pagination (default value: 0)
    #' @param limit (optional) the limit of the index and/or pagination (default value: 100)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ReservationResponse]) with additional information such as HTTP status code, headers
    SearchReservationsWithHttpInfo = function(device_id = NULL, app_key = NULL, account_id = NULL, filter_account_id = NULL, reservable_id = NULL, reservable_type = NULL, keyword = NULL, start_date = NULL, end_date = NULL, start = 0, limit = 100, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ReservationApi$SearchReservations, `device_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ReservationApi$SearchReservations, `app_key` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReservationApi$SearchReservations, `account_id` is not nullable")
      }

      if (!missing(`filter_account_id`) && is.null(`filter_account_id`)) {
        stop("Invalid value for `filter_account_id` when calling ReservationApi$SearchReservations, `filter_account_id` is not nullable")
      }

      if (!missing(`reservable_id`) && is.null(`reservable_id`)) {
        stop("Invalid value for `reservable_id` when calling ReservationApi$SearchReservations, `reservable_id` is not nullable")
      }

      if (!missing(`reservable_type`) && is.null(`reservable_type`)) {
        stop("Invalid value for `reservable_type` when calling ReservationApi$SearchReservations, `reservable_type` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ReservationApi$SearchReservations, `keyword` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling ReservationApi$SearchReservations, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling ReservationApi$SearchReservations, `end_date` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ReservationApi$SearchReservations, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ReservationApi$SearchReservations, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["accountId"]] <- `account_id`

      query_params[["filterAccountId"]] <- `filter_account_id`

      query_params[["reservableId"]] <- `reservable_id`

      if (!is.null(`reservable_type`) && !(`reservable_type` %in% c("LOCATABLE", "RESERVABLE", "PERMISSIONABLE", "NOTABLE", "ASSETABLE", "LIKABLE", "FLAGABLE", "FAVORITABLE", "RATABLE", "ALBUM", "COLLECTION", "APPLICATION", "APPLICATION_SETTING", "APPLICATION_CERT", "APPLICATION_PLACEMENT", "ACCOUNT", "ACCOUNT_SETTING", "GAME_LEVEL", "PACK", "MISSION", "TOURNAMENT", "ASSET", "ALBUM_CONTEST", "THEME_DESCRIPTOR", "OFFER", "OFFER_LOCATION", "EVENT", "RETAILER", "RETAILER_LOCATION", "NOTE", "CREATIVE", "FAVORITE", "LIKE", "RATING", "ANALYTIC", "THIRD_PARTY_CREDENTIAL", "THIRD_PARTY_NETWORK", "REGION"))) {
        stop("Invalid value for reservable_type when calling ReservationApi$SearchReservations. Must be [LOCATABLE, RESERVABLE, PERMISSIONABLE, NOTABLE, ASSETABLE, LIKABLE, FLAGABLE, FAVORITABLE, RATABLE, ALBUM, COLLECTION, APPLICATION, APPLICATION_SETTING, APPLICATION_CERT, APPLICATION_PLACEMENT, ACCOUNT, ACCOUNT_SETTING, GAME_LEVEL, PACK, MISSION, TOURNAMENT, ASSET, ALBUM_CONTEST, THEME_DESCRIPTOR, OFFER, OFFER_LOCATION, EVENT, RETAILER, RETAILER_LOCATION, NOTE, CREATIVE, FAVORITE, LIKE, RATING, ANALYTIC, THIRD_PARTY_CREDENTIAL, THIRD_PARTY_NETWORK, REGION].")
      }
      query_params[["reservableType"]] <- `reservable_type`

      query_params[["keyword"]] <- `keyword`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/reservation/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ReservationResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Schedule
    #'
    #' @param reservable_id the id of the reservation
    #' @param reservable_type the reservation type
    #' @param start_date the start date of the reservation
    #' @param end_date the end date of the reservation
    #' @param device_id (optional) the id of the device that the reservation is on
    #' @param account_id (optional) the id of the logged in user
    #' @param time_bucket_mins (optional) the length of time in minutes to search on for reservation (default value: 30)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[TimeSlotResponse]
    SearchSchedule = function(reservable_id, reservable_type, start_date, end_date, device_id = NULL, account_id = NULL, time_bucket_mins = 30, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchScheduleWithHttpInfo(reservable_id, reservable_type, start_date, end_date, device_id, account_id, time_bucket_mins, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Schedule
    #'
    #' @param reservable_id the id of the reservation
    #' @param reservable_type the reservation type
    #' @param start_date the start date of the reservation
    #' @param end_date the end date of the reservation
    #' @param device_id (optional) the id of the device that the reservation is on
    #' @param account_id (optional) the id of the logged in user
    #' @param time_bucket_mins (optional) the length of time in minutes to search on for reservation (default value: 30)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[TimeSlotResponse]) with additional information such as HTTP status code, headers
    SearchScheduleWithHttpInfo = function(reservable_id, reservable_type, start_date, end_date, device_id = NULL, account_id = NULL, time_bucket_mins = 30, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`reservable_id`)) {
        stop("Missing required parameter `reservable_id`.")
      }

      if (missing(`reservable_type`)) {
        stop("Missing required parameter `reservable_type`.")
      }

      if (missing(`start_date`)) {
        stop("Missing required parameter `start_date`.")
      }

      if (missing(`end_date`)) {
        stop("Missing required parameter `end_date`.")
      }

      if (!missing(`reservable_id`) && is.null(`reservable_id`)) {
        stop("Invalid value for `reservable_id` when calling ReservationApi$SearchSchedule, `reservable_id` is not nullable")
      }

      if (!missing(`reservable_type`) && is.null(`reservable_type`)) {
        stop("Invalid value for `reservable_type` when calling ReservationApi$SearchSchedule, `reservable_type` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling ReservationApi$SearchSchedule, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling ReservationApi$SearchSchedule, `end_date` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ReservationApi$SearchSchedule, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReservationApi$SearchSchedule, `account_id` is not nullable")
      }

      if (!missing(`time_bucket_mins`) && is.null(`time_bucket_mins`)) {
        stop("Invalid value for `time_bucket_mins` when calling ReservationApi$SearchSchedule, `time_bucket_mins` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["reservableId"]] <- `reservable_id`

      if (!is.null(`reservable_type`) && !(`reservable_type` %in% c("LOCATABLE", "RESERVABLE", "PERMISSIONABLE", "NOTABLE", "ASSETABLE", "LIKABLE", "FLAGABLE", "FAVORITABLE", "RATABLE", "ALBUM", "COLLECTION", "APPLICATION", "APPLICATION_SETTING", "APPLICATION_CERT", "APPLICATION_PLACEMENT", "ACCOUNT", "ACCOUNT_SETTING", "GAME_LEVEL", "PACK", "MISSION", "TOURNAMENT", "ASSET", "ALBUM_CONTEST", "THEME_DESCRIPTOR", "OFFER", "OFFER_LOCATION", "EVENT", "RETAILER", "RETAILER_LOCATION", "NOTE", "CREATIVE", "FAVORITE", "LIKE", "RATING", "ANALYTIC", "THIRD_PARTY_CREDENTIAL", "THIRD_PARTY_NETWORK", "REGION"))) {
        stop("Invalid value for reservable_type when calling ReservationApi$SearchSchedule. Must be [LOCATABLE, RESERVABLE, PERMISSIONABLE, NOTABLE, ASSETABLE, LIKABLE, FLAGABLE, FAVORITABLE, RATABLE, ALBUM, COLLECTION, APPLICATION, APPLICATION_SETTING, APPLICATION_CERT, APPLICATION_PLACEMENT, ACCOUNT, ACCOUNT_SETTING, GAME_LEVEL, PACK, MISSION, TOURNAMENT, ASSET, ALBUM_CONTEST, THEME_DESCRIPTOR, OFFER, OFFER_LOCATION, EVENT, RETAILER, RETAILER_LOCATION, NOTE, CREATIVE, FAVORITE, LIKE, RATING, ANALYTIC, THIRD_PARTY_CREDENTIAL, THIRD_PARTY_NETWORK, REGION].")
      }
      query_params[["reservableType"]] <- `reservable_type`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["timeBucketMins"]] <- `time_bucket_mins`

      local_var_url_path <- "/reservable/schedule/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[TimeSlotResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
