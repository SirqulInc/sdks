#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Reporting operations
#' @description ReportingApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateBatch  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user for passing account related params
#' var_status <- "status_example" # character | the status of the report
#' var_preview_limit <- 56 # integer | the limit on how much you can preview of the batch report
#' var_app_key <- "app_key_example" # character | The application key for passing application related params (Optional)
#' var_endpoint <- "endpoint_example" # character |  (Optional)
#' var_parameters <- "parameters_example" # character | a json structure list of the parameter values, example: ```json {   \"string\":\"value\",    \"number\":3.345,   \"date\":\"2014-05-01 00:00:00\" } ```  (Optional)
#' var_name <- "name_example" # character | name of the batch report (Optional)
#' var_start_date <- 56 # integer | the start date of the batch report (Optional)
#' var_end_date <- 56 # integer | the end date of the batch report (Optional)
#' var_description <- "description_example" # character | the description of the batch report (Optional)
#' var_page_url <- "page_url_example" # character |  (Optional)
#'
#' #Create Offline Report
#' api_instance <- ReportingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateBatch(var_account_id, var_status, var_preview_limit, app_key = var_app_key, endpoint = var_endpoint, parameters = var_parameters, name = var_name, start_date = var_start_date, end_date = var_end_date, description = var_description, page_url = var_page_urldata_file = "result.txt")
#' result <- api_instance$CreateBatch(var_account_id, var_status, var_preview_limit, app_key = var_app_key, endpoint = var_endpoint, parameters = var_parameters, name = var_name, start_date = var_start_date, end_date = var_end_date, description = var_description, page_url = var_page_url)
#' dput(result)
#'
#'
#' ####################  CreateRegionLegSummaryBatch  ####################
#'
#' library(openapi)
#' var_body <- c(RegionLegSummary$new(123, "active_example", "valid_example", 123, "floorId_example", 123, 123)) # array[RegionLegSummary] |  (Optional)
#'
#' #Create Offline Report
#' api_instance <- ReportingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateRegionLegSummaryBatch(body = var_bodydata_file = "result.txt")
#' result <- api_instance$CreateRegionLegSummaryBatch(body = var_body)
#' dput(result)
#'
#'
#' ####################  DeleteBatch  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the account
#' var_batch_id <- 56 # integer | the id of the batch to delete
#'
#' #Delete Offline Report
#' api_instance <- ReportingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteBatch(var_account_id, var_batch_iddata_file = "result.txt")
#' result <- api_instance$DeleteBatch(var_account_id, var_batch_id)
#' dput(result)
#'
#'
#' ####################  GetReportBatch  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_batch_id <- 56 # integer | returned by /report/batch/create
#' var_all_results <- "all_results_example" # character | whether to return all batch results or not
#'
#' #Get Offline Report
#' api_instance <- ReportingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetReportBatch(var_account_id, var_batch_id, var_all_resultsdata_file = "result.txt")
#' result <- api_instance$GetReportBatch(var_account_id, var_batch_id, var_all_results)
#' dput(result)
#'
#'
#' ####################  RunReport  ####################
#'
#' library(openapi)
#' var_desc <- "desc_example" # character | If true then descending order, false is ascending
#' var_account_id <- 56 # integer | The account id of the user for passing account related params (Optional)
#' var_query <- "query_example" # character | The named identifier of the query (Optional)
#' var_parameters <- "parameters_example" # character | Parameter values used in the query in JSON format, example: ```json {   \"string\":\"value\",    \"number\":3.345,   \"date\":\"2014-05-01 00:00:00\" } ```  (Optional)
#' var_order <- "order_example" # character | The order to use, must be a column name (see response results for list of column names) (Optional)
#' var_start <- 56 # integer | The start of the pagination (Optional)
#' var_limit <- 56 # integer | The limit of the pagination (Optional)
#' var_response_format <- "response_format_example" # character | Determines what response format to return. Options are: JSON or CSV (Optional)
#'
#' #Run Report
#' api_instance <- ReportingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RunReport(var_desc, account_id = var_account_id, query = var_query, parameters = var_parameters, order = var_order, start = var_start, limit = var_limit, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$RunReport(var_desc, account_id = var_account_id, query = var_query, parameters = var_parameters, order = var_order, start = var_start, limit = var_limit, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  SearchBatch  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the account logged in
#' var_start <- 56 # integer | the start of the index and/or pagination
#' var_limit <- 56 # integer | the limit of the index and/or pagination
#' var_names <- "names_example" # character | the names of the report batch to search on (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_status <- "status_example" # character | the report batch status (Optional)
#' var_global_app_search <- "global_app_search_example" # character | the global app to search on (Optional)
#' var_start_date <- 56 # integer | the start date of the report batch to search on (Optional)
#' var_end_date <- 56 # integer | the end date of the report batch to search on (Optional)
#'
#' #Search Offline Reports
#' api_instance <- ReportingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchBatch(var_account_id, var_start, var_limit, names = var_names, app_key = var_app_key, status = var_status, global_app_search = var_global_app_search, start_date = var_start_date, end_date = var_end_datedata_file = "result.txt")
#' result <- api_instance$SearchBatch(var_account_id, var_start, var_limit, names = var_names, app_key = var_app_key, status = var_status, global_app_search = var_global_app_search, start_date = var_start_date, end_date = var_end_date)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ReportingApi <- R6::R6Class(
  "ReportingApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ReportingApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Offline Report
    #'
    #' @param account_id The account id of the user for passing account related params
    #' @param status the status of the report
    #' @param preview_limit the limit on how much you can preview of the batch report
    #' @param app_key (optional) The application key for passing application related params
    #' @param endpoint (optional) 
    #' @param parameters (optional) a json structure list of the parameter values, example: ```json {   \"string\":\"value\",    \"number\":3.345,   \"date\":\"2014-05-01 00:00:00\" } ``` 
    #' @param name (optional) name of the batch report
    #' @param start_date (optional) the start date of the batch report
    #' @param end_date (optional) the end date of the batch report
    #' @param description (optional) the description of the batch report
    #' @param page_url (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ReportBatchResponse
    CreateBatch = function(account_id, status, preview_limit, app_key = NULL, endpoint = NULL, parameters = NULL, name = NULL, start_date = NULL, end_date = NULL, description = NULL, page_url = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateBatchWithHttpInfo(account_id, status, preview_limit, app_key, endpoint, parameters, name, start_date, end_date, description, page_url, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Offline Report
    #'
    #' @param account_id The account id of the user for passing account related params
    #' @param status the status of the report
    #' @param preview_limit the limit on how much you can preview of the batch report
    #' @param app_key (optional) The application key for passing application related params
    #' @param endpoint (optional) 
    #' @param parameters (optional) a json structure list of the parameter values, example: ```json {   \"string\":\"value\",    \"number\":3.345,   \"date\":\"2014-05-01 00:00:00\" } ``` 
    #' @param name (optional) name of the batch report
    #' @param start_date (optional) the start date of the batch report
    #' @param end_date (optional) the end date of the batch report
    #' @param description (optional) the description of the batch report
    #' @param page_url (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ReportBatchResponse) with additional information such as HTTP status code, headers
    CreateBatchWithHttpInfo = function(account_id, status, preview_limit, app_key = NULL, endpoint = NULL, parameters = NULL, name = NULL, start_date = NULL, end_date = NULL, description = NULL, page_url = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`status`)) {
        stop("Missing required parameter `status`.")
      }

      if (missing(`preview_limit`)) {
        stop("Missing required parameter `preview_limit`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReportingApi$CreateBatch, `account_id` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling ReportingApi$CreateBatch, `status` is not nullable")
      }

      if (!missing(`preview_limit`) && is.null(`preview_limit`)) {
        stop("Invalid value for `preview_limit` when calling ReportingApi$CreateBatch, `preview_limit` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ReportingApi$CreateBatch, `app_key` is not nullable")
      }

      if (!missing(`endpoint`) && is.null(`endpoint`)) {
        stop("Invalid value for `endpoint` when calling ReportingApi$CreateBatch, `endpoint` is not nullable")
      }

      if (!missing(`parameters`) && is.null(`parameters`)) {
        stop("Invalid value for `parameters` when calling ReportingApi$CreateBatch, `parameters` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling ReportingApi$CreateBatch, `name` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling ReportingApi$CreateBatch, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling ReportingApi$CreateBatch, `end_date` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling ReportingApi$CreateBatch, `description` is not nullable")
      }

      if (!missing(`page_url`) && is.null(`page_url`)) {
        stop("Invalid value for `page_url` when calling ReportingApi$CreateBatch, `page_url` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      if (!is.null(`status`) && !(`status` %in% c("NEW", "ERROR", "COMPLETE", "PARSE_ERROR", "PROCESSING", "DUPLICATE", "SAVEONLY"))) {
        stop("Invalid value for status when calling ReportingApi$CreateBatch. Must be [NEW, ERROR, COMPLETE, PARSE_ERROR, PROCESSING, DUPLICATE, SAVEONLY].")
      }
      query_params[["status"]] <- `status`

      query_params[["endpoint"]] <- `endpoint`

      query_params[["parameters"]] <- `parameters`

      query_params[["name"]] <- `name`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["description"]] <- `description`

      query_params[["previewLimit"]] <- `preview_limit`

      query_params[["pageUrl"]] <- `page_url`

      local_var_url_path <- "/report/batch/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ReportBatchResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Offline Report
    #'
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ReportRegionLegSummaryBatchResponse
    CreateRegionLegSummaryBatch = function(body = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateRegionLegSummaryBatchWithHttpInfo(body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Offline Report
    #'
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ReportRegionLegSummaryBatchResponse) with additional information such as HTTP status code, headers
    CreateRegionLegSummaryBatchWithHttpInfo = function(body = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`body`) && is.null(`body`)) {
        stop("Invalid value for `body` when calling ReportingApi$CreateRegionLegSummaryBatch, `body` is not nullable")
      }

      if (!is.null(`body`)) {
        body.items <- paste(unlist(lapply(`body`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/report/region/summary/batch"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ReportRegionLegSummaryBatchResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Offline Report
    #'
    #' @param account_id the id of the account
    #' @param batch_id the id of the batch to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteBatch = function(account_id, batch_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteBatchWithHttpInfo(account_id, batch_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Offline Report
    #'
    #' @param account_id the id of the account
    #' @param batch_id the id of the batch to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteBatchWithHttpInfo = function(account_id, batch_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`batch_id`)) {
        stop("Missing required parameter `batch_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReportingApi$DeleteBatch, `account_id` is not nullable")
      }

      if (!missing(`batch_id`) && is.null(`batch_id`)) {
        stop("Invalid value for `batch_id` when calling ReportingApi$DeleteBatch, `batch_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["batchId"]] <- `batch_id`

      local_var_url_path <- "/report/batch/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Offline Report
    #'
    #' @param account_id the id of the logged in user
    #' @param batch_id returned by /report/batch/create
    #' @param all_results whether to return all batch results or not
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ReportBatchResponse
    GetReportBatch = function(account_id, batch_id, all_results, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetReportBatchWithHttpInfo(account_id, batch_id, all_results, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Offline Report
    #'
    #' @param account_id the id of the logged in user
    #' @param batch_id returned by /report/batch/create
    #' @param all_results whether to return all batch results or not
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ReportBatchResponse) with additional information such as HTTP status code, headers
    GetReportBatchWithHttpInfo = function(account_id, batch_id, all_results, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`batch_id`)) {
        stop("Missing required parameter `batch_id`.")
      }

      if (missing(`all_results`)) {
        stop("Missing required parameter `all_results`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReportingApi$GetReportBatch, `account_id` is not nullable")
      }

      if (!missing(`batch_id`) && is.null(`batch_id`)) {
        stop("Invalid value for `batch_id` when calling ReportingApi$GetReportBatch, `batch_id` is not nullable")
      }

      if (!missing(`all_results`) && is.null(`all_results`)) {
        stop("Invalid value for `all_results` when calling ReportingApi$GetReportBatch, `all_results` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["batchId"]] <- `batch_id`

      query_params[["allResults"]] <- `all_results`

      local_var_url_path <- "/report/batch/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ReportBatchResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Run Report
    #'
    #' @param desc If true then descending order, false is ascending
    #' @param account_id (optional) The account id of the user for passing account related params
    #' @param query (optional) The named identifier of the query
    #' @param parameters (optional) Parameter values used in the query in JSON format, example: ```json {   \"string\":\"value\",    \"number\":3.345,   \"date\":\"2014-05-01 00:00:00\" } ``` 
    #' @param order (optional) The order to use, must be a column name (see response results for list of column names)
    #' @param start (optional) The start of the pagination
    #' @param limit (optional) The limit of the pagination
    #' @param response_format (optional) Determines what response format to return. Options are: JSON or CSV
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ReportResponse
    RunReport = function(desc, account_id = NULL, query = NULL, parameters = NULL, order = NULL, start = NULL, limit = NULL, response_format = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RunReportWithHttpInfo(desc, account_id, query, parameters, order, start, limit, response_format, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Run Report
    #'
    #' @param desc If true then descending order, false is ascending
    #' @param account_id (optional) The account id of the user for passing account related params
    #' @param query (optional) The named identifier of the query
    #' @param parameters (optional) Parameter values used in the query in JSON format, example: ```json {   \"string\":\"value\",    \"number\":3.345,   \"date\":\"2014-05-01 00:00:00\" } ``` 
    #' @param order (optional) The order to use, must be a column name (see response results for list of column names)
    #' @param start (optional) The start of the pagination
    #' @param limit (optional) The limit of the pagination
    #' @param response_format (optional) Determines what response format to return. Options are: JSON or CSV
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ReportResponse) with additional information such as HTTP status code, headers
    RunReportWithHttpInfo = function(desc, account_id = NULL, query = NULL, parameters = NULL, order = NULL, start = NULL, limit = NULL, response_format = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`desc`)) {
        stop("Missing required parameter `desc`.")
      }

      if (!missing(`desc`) && is.null(`desc`)) {
        stop("Invalid value for `desc` when calling ReportingApi$RunReport, `desc` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReportingApi$RunReport, `account_id` is not nullable")
      }

      if (!missing(`query`) && is.null(`query`)) {
        stop("Invalid value for `query` when calling ReportingApi$RunReport, `query` is not nullable")
      }

      if (!missing(`parameters`) && is.null(`parameters`)) {
        stop("Invalid value for `parameters` when calling ReportingApi$RunReport, `parameters` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling ReportingApi$RunReport, `order` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ReportingApi$RunReport, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ReportingApi$RunReport, `limit` is not nullable")
      }

      if (!missing(`response_format`) && is.null(`response_format`)) {
        stop("Invalid value for `response_format` when calling ReportingApi$RunReport, `response_format` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["query"]] <- `query`

      query_params[["parameters"]] <- `parameters`

      query_params[["order"]] <- `order`

      query_params[["desc"]] <- `desc`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      if (!is.null(`response_format`) && !(`response_format` %in% c("HTML", "XML", "JSON", "CSV"))) {
        stop("Invalid value for response_format when calling ReportingApi$RunReport. Must be [HTML, XML, JSON, CSV].")
      }
      query_params[["responseFormat"]] <- `response_format`

      local_var_url_path <- "/report/run"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ReportResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Offline Reports
    #'
    #' @param account_id the id of the account logged in
    #' @param start the start of the index and/or pagination
    #' @param limit the limit of the index and/or pagination
    #' @param names (optional) the names of the report batch to search on
    #' @param app_key (optional) the application key
    #' @param status (optional) the report batch status
    #' @param global_app_search (optional) the global app to search on
    #' @param start_date (optional) the start date of the report batch to search on
    #' @param end_date (optional) the end date of the report batch to search on
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ReportBatchResponse]
    SearchBatch = function(account_id, start, limit, names = NULL, app_key = NULL, status = NULL, global_app_search = NULL, start_date = NULL, end_date = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchBatchWithHttpInfo(account_id, start, limit, names, app_key, status, global_app_search, start_date, end_date, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Offline Reports
    #'
    #' @param account_id the id of the account logged in
    #' @param start the start of the index and/or pagination
    #' @param limit the limit of the index and/or pagination
    #' @param names (optional) the names of the report batch to search on
    #' @param app_key (optional) the application key
    #' @param status (optional) the report batch status
    #' @param global_app_search (optional) the global app to search on
    #' @param start_date (optional) the start date of the report batch to search on
    #' @param end_date (optional) the end date of the report batch to search on
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ReportBatchResponse]) with additional information such as HTTP status code, headers
    SearchBatchWithHttpInfo = function(account_id, start, limit, names = NULL, app_key = NULL, status = NULL, global_app_search = NULL, start_date = NULL, end_date = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ReportingApi$SearchBatch, `account_id` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ReportingApi$SearchBatch, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ReportingApi$SearchBatch, `limit` is not nullable")
      }

      if (!missing(`names`) && is.null(`names`)) {
        stop("Invalid value for `names` when calling ReportingApi$SearchBatch, `names` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ReportingApi$SearchBatch, `app_key` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling ReportingApi$SearchBatch, `status` is not nullable")
      }

      if (!missing(`global_app_search`) && is.null(`global_app_search`)) {
        stop("Invalid value for `global_app_search` when calling ReportingApi$SearchBatch, `global_app_search` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling ReportingApi$SearchBatch, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling ReportingApi$SearchBatch, `end_date` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["names"]] <- `names`

      query_params[["appKey"]] <- `app_key`

      if (!is.null(`status`) && !(`status` %in% c("NEW", "ERROR", "COMPLETE", "PARSE_ERROR", "PROCESSING", "DUPLICATE", "SAVEONLY"))) {
        stop("Invalid value for status when calling ReportingApi$SearchBatch. Must be [NEW, ERROR, COMPLETE, PARSE_ERROR, PROCESSING, DUPLICATE, SAVEONLY].")
      }
      query_params[["status"]] <- `status`

      query_params[["globalAppSearch"]] <- `global_app_search`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/report/batch/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ReportBatchResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
