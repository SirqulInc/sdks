#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Achievement operations
#' @description AchievementApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AchievementTierSearchPost  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_keyword <- "keyword_example" # character | the keyword used to filter resutls with (this returns results that have the keyword in the title or the description of the achievement tier) (Optional)
#' var_achievement_type <- 56 # integer | filter results by achievementType (these are exact case sensitive matches) (Optional)
#' var_rank_type <- "rank_type_example" # character | filter results by the rankType (these are the exact case sensitive matches) (Optional)
#' var_sort_field <- "sort_field_example" # character | the field to sort by. See {@link AchievementApiMap} (Optional)
#' var_descending <- "descending_example" # character | determines whether the sort list is in descending or ascending order (of the achievement) (Optional)
#' var_descending_goal <- "descending_goal_example" # character | determines whether the results are in descending or ascending order by the tier goal count (after the initial sort on the achievement) (Optional)
#' var_start <- 56 # integer | The start of the index for pagination (Optional)
#' var_limit <- 56 # integer | the limit for pagination (has a hard limit of 1000) (Optional)
#'
#' #Searches an Achievement Tier
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AchievementTierSearchPost(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, keyword = var_keyword, achievement_type = var_achievement_type, rank_type = var_rank_type, sort_field = var_sort_field, descending = var_descending, descending_goal = var_descending_goal, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$AchievementTierSearchPost(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, keyword = var_keyword, achievement_type = var_achievement_type, rank_type = var_rank_type, sort_field = var_sort_field, descending = var_descending, descending_goal = var_descending_goal, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  CreateAchievement  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | the application key the achievement is for
#' var_title <- "title_example" # character | the title of the achievement (255 character limit)
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_analytics_tag <- "analytics_tag_example" # character | the analytics tag that will trigger when a user's achievement count gets updated (Optional)
#' var_description <- "description_example" # character | the description of the achievement (Optional)
#' var_rank_type <- "rank_type_example" # character | the rank type for updating leader boards (Optional)
#' var_rank_increment <- 56 # integer | determines how much the rank count is incremented (Optional)
#' var_min_increment <- 56 # integer | restrict scores to be above or equal to this minimum value (Optional)
#' var_max_increment <- 56 # integer | restrict scores to be below or equal to this maximum value (Optional)
#' var_validate <- "validate_example" # character | determines whether the customId on analytics are used to validate a user's achievement progress. (Optional)
#' var_active <- "active_example" # character | achievement is active or inactive (Optional)
#' var_trigger_definition <- "trigger_definition_example" # character | if provided will define what triggers to run after a tier is completed (Optional)
#'
#' #Create Achievement
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateAchievement(var_app_key, var_title, device_id = var_device_id, account_id = var_account_id, analytics_tag = var_analytics_tag, description = var_description, rank_type = var_rank_type, rank_increment = var_rank_increment, min_increment = var_min_increment, max_increment = var_max_increment, validate = var_validate, active = var_active, trigger_definition = var_trigger_definitiondata_file = "result.txt")
#' result <- api_instance$CreateAchievement(var_app_key, var_title, device_id = var_device_id, account_id = var_account_id, analytics_tag = var_analytics_tag, description = var_description, rank_type = var_rank_type, rank_increment = var_rank_increment, min_increment = var_min_increment, max_increment = var_max_increment, validate = var_validate, active = var_active, trigger_definition = var_trigger_definition)
#' dput(result)
#'
#'
#' ####################  CreateAchievementTier  ####################
#'
#' library(openapi)
#' var_achievement_id <- 56 # integer | the achievement id for adding a new tier
#' var_score_all_instances <- "score_all_instances_example" # character | score all instances
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_icon <- File.new('/path/to/file') # data.frame | achievement tier icon image file (Optional)
#' var_icon_asset_id <- 56 # integer | the icon assetId, if icon is provided, icon will overrule (Optional)
#' var_title <- "title_example" # character | the title of the achievement tier (Optional)
#' var_description <- "description_example" # character | the description of the achievement tier (Optional)
#' var_goal_count <- 56 # integer | the count requirement for completing the achievement tier (Optional)
#' var_mission_id <- 56 # integer | The ID of the mission to associate with the achievement (Optional)
#' var_game_id <- 56 # integer | The ID of the game to associate with the achievement (Optional)
#' var_pack_id <- 56 # integer | The ID of the pack to associate with the achievement (Optional)
#' var_game_level_id <- 56 # integer | The ID of the game level to associate with the achievement (Optional)
#' var_game_object_id <- 56 # integer | The ID of the game object to associate with the achievement (Optional)
#'
#' #Create Achievement Tier
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateAchievementTier(var_achievement_id, var_score_all_instances, device_id = var_device_id, account_id = var_account_id, icon = var_icon, icon_asset_id = var_icon_asset_id, title = var_title, description = var_description, goal_count = var_goal_count, mission_id = var_mission_id, game_id = var_game_id, pack_id = var_pack_id, game_level_id = var_game_level_id, game_object_id = var_game_object_iddata_file = "result.txt")
#' result <- api_instance$CreateAchievementTier(var_achievement_id, var_score_all_instances, device_id = var_device_id, account_id = var_account_id, icon = var_icon, icon_asset_id = var_icon_asset_id, title = var_title, description = var_description, goal_count = var_goal_count, mission_id = var_mission_id, game_id = var_game_id, pack_id = var_pack_id, game_level_id = var_game_level_id, game_object_id = var_game_object_id)
#' dput(result)
#'
#'
#' ####################  DeleteAchievement  ####################
#'
#' library(openapi)
#' var_achievement_id <- 56 # integer | The ID of the achievement
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#'
#' #Delete Achievement
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteAchievement(var_achievement_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteAchievement(var_achievement_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  DeleteAchievementTier  ####################
#'
#' library(openapi)
#' var_achievement_tier_id <- 56 # integer | the achievement id for deletion
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required). (Optional)
#'
#' #Delete Achievement Tier
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteAchievementTier(var_achievement_tier_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteAchievementTier(var_achievement_tier_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  GetAchievement  ####################
#'
#' library(openapi)
#' var_achievement_id <- 56 # integer | The ID of the achievement
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_achievement_type <- "achievement_type_example" # character | achievementType (Optional)
#'
#' #Get Achievement
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAchievement(var_achievement_id, device_id = var_device_id, account_id = var_account_id, achievement_type = var_achievement_typedata_file = "result.txt")
#' result <- api_instance$GetAchievement(var_achievement_id, device_id = var_device_id, account_id = var_account_id, achievement_type = var_achievement_type)
#' dput(result)
#'
#'
#' ####################  GetAchievementTier  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required)
#' var_achievement_tier_id <- 56 # integer | the achievement tier id that is being retrieved
#'
#' #Gets an achievement tier
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAchievementTier(var_account_id, var_achievement_tier_iddata_file = "result.txt")
#' result <- api_instance$GetAchievementTier(var_account_id, var_achievement_tier_id)
#' dput(result)
#'
#'
#' ####################  GetUserAchievements  ####################
#'
#' library(openapi)
#' var_return_nulls <- TRUE # character | determines whether to return null fields in the response
#' var_app_key <- "app_key_example" # character | the application key for filtering results by application
#' var_include_undiscovered <- TRUE # character | determines whether to return achievements that the user has not discovered yet
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_connection_account_email <- "connection_account_email_example" # character | the email of the account to view achievements (Optional)
#' var_connection_account_id <- 56 # integer | the id of the account to view achievements (Optional)
#' var_rank_type <- "rank_type_example" # character | filter results by achievement rankType (Optional)
#' var_achievement_type <- "achievement_type_example" # character | filter results by achievement type (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#'
#' #Get Achievement Progress
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetUserAchievements(var_return_nulls, var_app_key, var_include_undiscovered, device_id = var_device_id, account_id = var_account_id, connection_account_email = var_connection_account_email, connection_account_id = var_connection_account_id, rank_type = var_rank_type, achievement_type = var_achievement_type, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$GetUserAchievements(var_return_nulls, var_app_key, var_include_undiscovered, device_id = var_device_id, account_id = var_account_id, connection_account_email = var_connection_account_email, connection_account_id = var_connection_account_id, rank_type = var_rank_type, achievement_type = var_achievement_type, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  ListAchievementTags  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | filter results by application key (Optional)
#'
#' #List Achievement Tags
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListAchievementTags(app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$ListAchievementTags(app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  ListAchievements  ####################
#'
#' library(openapi)
#' var_sort_field <- "sort_field_example" # character | the field to sort by. See AchievementApiMap
#' var_descending <- "descending_example" # character | determines whether the sorted list is in descending or ascending order
#' var_start <- 56 # integer | the start index for pagination
#' var_limit <- 56 # integer | the limit for pagination (has a hard limit of 1000)
#' var_active_only <- "active_only_example" # character | Filter results to only return active achievements
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_keyword <- "keyword_example" # character | the keyword used to filter results with (this returns results that have the keyword in the title or description of the achievement) (Optional)
#' var_achievement_type <- "achievement_type_example" # character | filter results by the achievementType (these are exact case sensitive matches) (Optional)
#' var_rank_type <- "rank_type_example" # character | filter results by the rankType (these are exact case sensitive matches) (Optional)
#'
#' #List Achievements
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListAchievements(var_sort_field, var_descending, var_start, var_limit, var_active_only, device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, keyword = var_keyword, achievement_type = var_achievement_type, rank_type = var_rank_typedata_file = "result.txt")
#' result <- api_instance$ListAchievements(var_sort_field, var_descending, var_start, var_limit, var_active_only, device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, keyword = var_keyword, achievement_type = var_achievement_type, rank_type = var_rank_type)
#' dput(result)
#'
#'
#' ####################  SearchAchievements  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | the application key
#' var_sort_field <- "TITLE" # character | the field to sort by. See AchievementApiMap
#' var_descending <- FALSE # character | determines whether the sorted list is in descending or ascending order
#' var_include_tiers <- FALSE # character | return tiers, only applicable for version >3.18
#' var_include_inactive_tiers <- FALSE # character | return inactive tiers, only applicable when includeTiers is true, only applicable for version >3.18
#' var_start <- 0 # integer | the start index for pagination
#' var_limit <- 100 # integer | the limit for pagination (has a hard limit of 1000)
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_keyword <- "keyword_example" # character | the keyword used to filter results with (this returns results that have the keyword in the title or description of the achievement) (Optional)
#' var_achievement_type <- "achievement_type_example" # character | filter results by the achievementType (these are exact case sensitive matches) (Optional)
#' var_rank_type <- "rank_type_example" # character | filter results by the rankType (these are exact case sensitive matches) (Optional)
#'
#' #Search Achievements
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchAchievements(var_app_key, var_sort_field, var_descending, var_include_tiers, var_include_inactive_tiers, var_start, var_limit, device_id = var_device_id, account_id = var_account_id, keyword = var_keyword, achievement_type = var_achievement_type, rank_type = var_rank_typedata_file = "result.txt")
#' result <- api_instance$SearchAchievements(var_app_key, var_sort_field, var_descending, var_include_tiers, var_include_inactive_tiers, var_start, var_limit, device_id = var_device_id, account_id = var_account_id, keyword = var_keyword, achievement_type = var_achievement_type, rank_type = var_rank_type)
#' dput(result)
#'
#'
#' ####################  UpdateAchievement  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_achievement_id <- 56 # integer | the achievement ID for updating an existing achievement (Optional)
#' var_analytics_tag <- "analytics_tag_example" # character | the analytics tag that will trigger when a user's achievement count gets updated (Optional)
#' var_title <- "title_example" # character | the title of the achievement (255 character limit) (Optional)
#' var_description <- "description_example" # character | the description of the achievement (Optional)
#' var_rank_type <- "rank_type_example" # character | the rank type for updating leader boards (Optional)
#' var_rank_increment <- 56 # integer | determines how much the rank count is incremented (Optional)
#' var_min_increment <- 56 # integer | restrict scores to be above or equal to this minimum value (Optional)
#' var_null_min_increment <- "null_min_increment_example" # character | enable to ignore usage of minIncrement (Optional)
#' var_max_increment <- 56 # integer | restrict scores to be below or equal to this maximum value (Optional)
#' var_null_max_increment <- "null_max_increment_example" # character | enable to ignore usage of maxIncrement (Optional)
#' var_validate <- "validate_example" # character | determines whether the customId on analytics are used to validate a user's achievement progress. (Optional)
#' var_active <- "active_example" # character | if it's active or inactive (Optional)
#' var_trigger_definition <- "trigger_definition_example" # character | if provided will define what triggers to run after a tier is completed (Optional)
#'
#' #Update Achievement
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateAchievement(device_id = var_device_id, account_id = var_account_id, achievement_id = var_achievement_id, analytics_tag = var_analytics_tag, title = var_title, description = var_description, rank_type = var_rank_type, rank_increment = var_rank_increment, min_increment = var_min_increment, null_min_increment = var_null_min_increment, max_increment = var_max_increment, null_max_increment = var_null_max_increment, validate = var_validate, active = var_active, trigger_definition = var_trigger_definitiondata_file = "result.txt")
#' result <- api_instance$UpdateAchievement(device_id = var_device_id, account_id = var_account_id, achievement_id = var_achievement_id, analytics_tag = var_analytics_tag, title = var_title, description = var_description, rank_type = var_rank_type, rank_increment = var_rank_increment, min_increment = var_min_increment, null_min_increment = var_null_min_increment, max_increment = var_max_increment, null_max_increment = var_null_max_increment, validate = var_validate, active = var_active, trigger_definition = var_trigger_definition)
#' dput(result)
#'
#'
#' ####################  UpdateAchievementTier  ####################
#'
#' library(openapi)
#' var_achievement_tier_id <- 56 # integer | the achievement tier id for updating
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_icon <- File.new('/path/to/file') # data.frame | achievement tier icon image file (Optional)
#' var_icon_asset_id <- 56 # integer | the icon assetId, if icon is provided, icon will overrule (Optional)
#' var_title <- "title_example" # character | the title of the achievement tier (Optional)
#' var_description <- "description_example" # character | the description of the achievement tier (Optional)
#' var_goal_count <- 56 # integer | the count requirement for completing the achievement tier (Optional)
#' var_mission_id <- 56 # integer | The ID of the mission to associate with the achievement (Optional)
#' var_game_id <- 56 # integer | The ID of the game to associate with the achievement (Optional)
#' var_pack_id <- 56 # integer | The ID of the pack to associate with the achievement (Optional)
#' var_game_level_id <- 56 # integer | The ID of the game level to associate with the achievement (Optional)
#' var_game_object_id <- 56 # integer | The ID of the game object to associate with the achievement (Optional)
#' var_score_all_instances <- "score_all_instances_example" # character | score all instances (Optional)
#'
#' #Update Achievement Tier
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateAchievementTier(var_achievement_tier_id, device_id = var_device_id, account_id = var_account_id, icon = var_icon, icon_asset_id = var_icon_asset_id, title = var_title, description = var_description, goal_count = var_goal_count, mission_id = var_mission_id, game_id = var_game_id, pack_id = var_pack_id, game_level_id = var_game_level_id, game_object_id = var_game_object_id, score_all_instances = var_score_all_instancesdata_file = "result.txt")
#' result <- api_instance$UpdateAchievementTier(var_achievement_tier_id, device_id = var_device_id, account_id = var_account_id, icon = var_icon, icon_asset_id = var_icon_asset_id, title = var_title, description = var_description, goal_count = var_goal_count, mission_id = var_mission_id, game_id = var_game_id, pack_id = var_pack_id, game_level_id = var_game_level_id, game_object_id = var_game_object_id, score_all_instances = var_score_all_instances)
#' dput(result)
#'
#'
#' ####################  UpdateUserAchievement  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account id of the user
#' var_achievement_id <- 56 # integer | the achievement id (achievementId or tag required) (Optional)
#' var_tag <- "tag_example" # character | the analytic tag to identify an achievement (achievementId or tag required) (Optional)
#' var_custom_id <- 56 # integer | a custom identifier used for validation (Optional)
#' var_increment <- 56 # integer | the amount to increment an achievement progress by (if no increment is sent in, the server defaults to using the achievement's rankIncrement value) (Optional)
#' var_start_date <- 56 # integer | a custom start date that the client can set (not yet used in server logic) (Optional)
#' var_end_date <- 56 # integer | a custom end date that the client can set (not yet used in server logic) (Optional)
#' var_return_progress <- "return_progress_example" # character | determines whether to return the achievement progress response (Optional)
#'
#' #Update Achievement Progress
#' api_instance <- AchievementApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateUserAchievement(var_account_id, achievement_id = var_achievement_id, tag = var_tag, custom_id = var_custom_id, increment = var_increment, start_date = var_start_date, end_date = var_end_date, return_progress = var_return_progressdata_file = "result.txt")
#' result <- api_instance$UpdateUserAchievement(var_account_id, achievement_id = var_achievement_id, tag = var_tag, custom_id = var_custom_id, increment = var_increment, start_date = var_start_date, end_date = var_end_date, return_progress = var_return_progress)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AchievementApi <- R6::R6Class(
  "AchievementApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AchievementApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Searches an Achievement Tier
    #'
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param app_key (optional) the application key
    #' @param keyword (optional) the keyword used to filter resutls with (this returns results that have the keyword in the title or the description of the achievement tier)
    #' @param achievement_type (optional) filter results by achievementType (these are exact case sensitive matches)
    #' @param rank_type (optional) filter results by the rankType (these are the exact case sensitive matches)
    #' @param sort_field (optional) the field to sort by. See {@link AchievementApiMap}
    #' @param descending (optional) determines whether the sort list is in descending or ascending order (of the achievement)
    #' @param descending_goal (optional) determines whether the results are in descending or ascending order by the tier goal count (after the initial sort on the achievement)
    #' @param start (optional) The start of the index for pagination
    #' @param limit (optional) the limit for pagination (has a hard limit of 1000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AchievementTierResponse
    AchievementTierSearchPost = function(device_id = NULL, account_id = NULL, app_key = NULL, keyword = NULL, achievement_type = NULL, rank_type = NULL, sort_field = NULL, descending = NULL, descending_goal = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AchievementTierSearchPostWithHttpInfo(device_id, account_id, app_key, keyword, achievement_type, rank_type, sort_field, descending, descending_goal, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Searches an Achievement Tier
    #'
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param app_key (optional) the application key
    #' @param keyword (optional) the keyword used to filter resutls with (this returns results that have the keyword in the title or the description of the achievement tier)
    #' @param achievement_type (optional) filter results by achievementType (these are exact case sensitive matches)
    #' @param rank_type (optional) filter results by the rankType (these are the exact case sensitive matches)
    #' @param sort_field (optional) the field to sort by. See {@link AchievementApiMap}
    #' @param descending (optional) determines whether the sort list is in descending or ascending order (of the achievement)
    #' @param descending_goal (optional) determines whether the results are in descending or ascending order by the tier goal count (after the initial sort on the achievement)
    #' @param start (optional) The start of the index for pagination
    #' @param limit (optional) the limit for pagination (has a hard limit of 1000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AchievementTierResponse) with additional information such as HTTP status code, headers
    AchievementTierSearchPostWithHttpInfo = function(device_id = NULL, account_id = NULL, app_key = NULL, keyword = NULL, achievement_type = NULL, rank_type = NULL, sort_field = NULL, descending = NULL, descending_goal = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AchievementApi$AchievementTierSearchPost, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$AchievementTierSearchPost, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AchievementApi$AchievementTierSearchPost, `app_key` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling AchievementApi$AchievementTierSearchPost, `keyword` is not nullable")
      }

      if (!missing(`achievement_type`) && is.null(`achievement_type`)) {
        stop("Invalid value for `achievement_type` when calling AchievementApi$AchievementTierSearchPost, `achievement_type` is not nullable")
      }

      if (!missing(`rank_type`) && is.null(`rank_type`)) {
        stop("Invalid value for `rank_type` when calling AchievementApi$AchievementTierSearchPost, `rank_type` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AchievementApi$AchievementTierSearchPost, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AchievementApi$AchievementTierSearchPost, `descending` is not nullable")
      }

      if (!missing(`descending_goal`) && is.null(`descending_goal`)) {
        stop("Invalid value for `descending_goal` when calling AchievementApi$AchievementTierSearchPost, `descending_goal` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AchievementApi$AchievementTierSearchPost, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AchievementApi$AchievementTierSearchPost, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["keyword"]] <- `keyword`

      query_params[["achievementType"]] <- `achievement_type`

      query_params[["rankType"]] <- `rank_type`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["descendingGoal"]] <- `descending_goal`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/achievement/tier/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AchievementTierResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Achievement
    #'
    #' @param app_key the application key the achievement is for
    #' @param title the title of the achievement (255 character limit)
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param analytics_tag (optional) the analytics tag that will trigger when a user's achievement count gets updated
    #' @param description (optional) the description of the achievement
    #' @param rank_type (optional) the rank type for updating leader boards
    #' @param rank_increment (optional) determines how much the rank count is incremented
    #' @param min_increment (optional) restrict scores to be above or equal to this minimum value
    #' @param max_increment (optional) restrict scores to be below or equal to this maximum value
    #' @param validate (optional) determines whether the customId on analytics are used to validate a user's achievement progress.
    #' @param active (optional) achievement is active or inactive
    #' @param trigger_definition (optional) if provided will define what triggers to run after a tier is completed
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AchievementResponse
    CreateAchievement = function(app_key, title, device_id = NULL, account_id = NULL, analytics_tag = NULL, description = NULL, rank_type = NULL, rank_increment = NULL, min_increment = NULL, max_increment = NULL, validate = NULL, active = NULL, trigger_definition = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateAchievementWithHttpInfo(app_key, title, device_id, account_id, analytics_tag, description, rank_type, rank_increment, min_increment, max_increment, validate, active, trigger_definition, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Achievement
    #'
    #' @param app_key the application key the achievement is for
    #' @param title the title of the achievement (255 character limit)
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param analytics_tag (optional) the analytics tag that will trigger when a user's achievement count gets updated
    #' @param description (optional) the description of the achievement
    #' @param rank_type (optional) the rank type for updating leader boards
    #' @param rank_increment (optional) determines how much the rank count is incremented
    #' @param min_increment (optional) restrict scores to be above or equal to this minimum value
    #' @param max_increment (optional) restrict scores to be below or equal to this maximum value
    #' @param validate (optional) determines whether the customId on analytics are used to validate a user's achievement progress.
    #' @param active (optional) achievement is active or inactive
    #' @param trigger_definition (optional) if provided will define what triggers to run after a tier is completed
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AchievementResponse) with additional information such as HTTP status code, headers
    CreateAchievementWithHttpInfo = function(app_key, title, device_id = NULL, account_id = NULL, analytics_tag = NULL, description = NULL, rank_type = NULL, rank_increment = NULL, min_increment = NULL, max_increment = NULL, validate = NULL, active = NULL, trigger_definition = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`title`)) {
        stop("Missing required parameter `title`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AchievementApi$CreateAchievement, `app_key` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling AchievementApi$CreateAchievement, `title` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AchievementApi$CreateAchievement, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$CreateAchievement, `account_id` is not nullable")
      }

      if (!missing(`analytics_tag`) && is.null(`analytics_tag`)) {
        stop("Invalid value for `analytics_tag` when calling AchievementApi$CreateAchievement, `analytics_tag` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling AchievementApi$CreateAchievement, `description` is not nullable")
      }

      if (!missing(`rank_type`) && is.null(`rank_type`)) {
        stop("Invalid value for `rank_type` when calling AchievementApi$CreateAchievement, `rank_type` is not nullable")
      }

      if (!missing(`rank_increment`) && is.null(`rank_increment`)) {
        stop("Invalid value for `rank_increment` when calling AchievementApi$CreateAchievement, `rank_increment` is not nullable")
      }

      if (!missing(`min_increment`) && is.null(`min_increment`)) {
        stop("Invalid value for `min_increment` when calling AchievementApi$CreateAchievement, `min_increment` is not nullable")
      }

      if (!missing(`max_increment`) && is.null(`max_increment`)) {
        stop("Invalid value for `max_increment` when calling AchievementApi$CreateAchievement, `max_increment` is not nullable")
      }

      if (!missing(`validate`) && is.null(`validate`)) {
        stop("Invalid value for `validate` when calling AchievementApi$CreateAchievement, `validate` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling AchievementApi$CreateAchievement, `active` is not nullable")
      }

      if (!missing(`trigger_definition`) && is.null(`trigger_definition`)) {
        stop("Invalid value for `trigger_definition` when calling AchievementApi$CreateAchievement, `trigger_definition` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["analyticsTag"]] <- `analytics_tag`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["rankType"]] <- `rank_type`

      query_params[["rankIncrement"]] <- `rank_increment`

      query_params[["minIncrement"]] <- `min_increment`

      query_params[["maxIncrement"]] <- `max_increment`

      query_params[["validate"]] <- `validate`

      query_params[["active"]] <- `active`

      query_params[["triggerDefinition"]] <- `trigger_definition`

      local_var_url_path <- "/achievement/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AchievementResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Achievement Tier
    #'
    #' @param achievement_id the achievement id for adding a new tier
    #' @param score_all_instances score all instances
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param icon (optional) achievement tier icon image file
    #' @param icon_asset_id (optional) the icon assetId, if icon is provided, icon will overrule
    #' @param title (optional) the title of the achievement tier
    #' @param description (optional) the description of the achievement tier
    #' @param goal_count (optional) the count requirement for completing the achievement tier
    #' @param mission_id (optional) The ID of the mission to associate with the achievement
    #' @param game_id (optional) The ID of the game to associate with the achievement
    #' @param pack_id (optional) The ID of the pack to associate with the achievement
    #' @param game_level_id (optional) The ID of the game level to associate with the achievement
    #' @param game_object_id (optional) The ID of the game object to associate with the achievement
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AchievementTierResponse
    CreateAchievementTier = function(achievement_id, score_all_instances, device_id = NULL, account_id = NULL, icon = NULL, icon_asset_id = NULL, title = NULL, description = NULL, goal_count = NULL, mission_id = NULL, game_id = NULL, pack_id = NULL, game_level_id = NULL, game_object_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateAchievementTierWithHttpInfo(achievement_id, score_all_instances, device_id, account_id, icon, icon_asset_id, title, description, goal_count, mission_id, game_id, pack_id, game_level_id, game_object_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Achievement Tier
    #'
    #' @param achievement_id the achievement id for adding a new tier
    #' @param score_all_instances score all instances
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param icon (optional) achievement tier icon image file
    #' @param icon_asset_id (optional) the icon assetId, if icon is provided, icon will overrule
    #' @param title (optional) the title of the achievement tier
    #' @param description (optional) the description of the achievement tier
    #' @param goal_count (optional) the count requirement for completing the achievement tier
    #' @param mission_id (optional) The ID of the mission to associate with the achievement
    #' @param game_id (optional) The ID of the game to associate with the achievement
    #' @param pack_id (optional) The ID of the pack to associate with the achievement
    #' @param game_level_id (optional) The ID of the game level to associate with the achievement
    #' @param game_object_id (optional) The ID of the game object to associate with the achievement
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AchievementTierResponse) with additional information such as HTTP status code, headers
    CreateAchievementTierWithHttpInfo = function(achievement_id, score_all_instances, device_id = NULL, account_id = NULL, icon = NULL, icon_asset_id = NULL, title = NULL, description = NULL, goal_count = NULL, mission_id = NULL, game_id = NULL, pack_id = NULL, game_level_id = NULL, game_object_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`achievement_id`)) {
        stop("Missing required parameter `achievement_id`.")
      }

      if (missing(`score_all_instances`)) {
        stop("Missing required parameter `score_all_instances`.")
      }

      if (!missing(`achievement_id`) && is.null(`achievement_id`)) {
        stop("Invalid value for `achievement_id` when calling AchievementApi$CreateAchievementTier, `achievement_id` is not nullable")
      }

      if (!missing(`score_all_instances`) && is.null(`score_all_instances`)) {
        stop("Invalid value for `score_all_instances` when calling AchievementApi$CreateAchievementTier, `score_all_instances` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AchievementApi$CreateAchievementTier, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$CreateAchievementTier, `account_id` is not nullable")
      }

      if (!missing(`icon`) && is.null(`icon`)) {
        stop("Invalid value for `icon` when calling AchievementApi$CreateAchievementTier, `icon` is not nullable")
      }

      if (!missing(`icon_asset_id`) && is.null(`icon_asset_id`)) {
        stop("Invalid value for `icon_asset_id` when calling AchievementApi$CreateAchievementTier, `icon_asset_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling AchievementApi$CreateAchievementTier, `title` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling AchievementApi$CreateAchievementTier, `description` is not nullable")
      }

      if (!missing(`goal_count`) && is.null(`goal_count`)) {
        stop("Invalid value for `goal_count` when calling AchievementApi$CreateAchievementTier, `goal_count` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling AchievementApi$CreateAchievementTier, `mission_id` is not nullable")
      }

      if (!missing(`game_id`) && is.null(`game_id`)) {
        stop("Invalid value for `game_id` when calling AchievementApi$CreateAchievementTier, `game_id` is not nullable")
      }

      if (!missing(`pack_id`) && is.null(`pack_id`)) {
        stop("Invalid value for `pack_id` when calling AchievementApi$CreateAchievementTier, `pack_id` is not nullable")
      }

      if (!missing(`game_level_id`) && is.null(`game_level_id`)) {
        stop("Invalid value for `game_level_id` when calling AchievementApi$CreateAchievementTier, `game_level_id` is not nullable")
      }

      if (!missing(`game_object_id`) && is.null(`game_object_id`)) {
        stop("Invalid value for `game_object_id` when calling AchievementApi$CreateAchievementTier, `game_object_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["achievementId"]] <- `achievement_id`

      query_params[["icon"]] <- `icon`

      query_params[["iconAssetId"]] <- `icon_asset_id`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["goalCount"]] <- `goal_count`

      query_params[["missionId"]] <- `mission_id`

      query_params[["gameId"]] <- `game_id`

      query_params[["packId"]] <- `pack_id`

      query_params[["gameLevelId"]] <- `game_level_id`

      query_params[["gameObjectId"]] <- `game_object_id`

      query_params[["scoreAllInstances"]] <- `score_all_instances`

      local_var_url_path <- "/achievement/tier/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AchievementTierResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Achievement
    #'
    #' @param achievement_id The ID of the achievement
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteAchievement = function(achievement_id, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteAchievementWithHttpInfo(achievement_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Achievement
    #'
    #' @param achievement_id The ID of the achievement
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteAchievementWithHttpInfo = function(achievement_id, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`achievement_id`)) {
        stop("Missing required parameter `achievement_id`.")
      }

      if (!missing(`achievement_id`) && is.null(`achievement_id`)) {
        stop("Invalid value for `achievement_id` when calling AchievementApi$DeleteAchievement, `achievement_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$DeleteAchievement, `account_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["achievementId"]] <- `achievement_id`

      local_var_url_path <- "/achievement/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Achievement Tier
    #'
    #' @param achievement_tier_id the achievement id for deletion
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteAchievementTier = function(achievement_tier_id, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteAchievementTierWithHttpInfo(achievement_tier_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Achievement Tier
    #'
    #' @param achievement_tier_id the achievement id for deletion
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteAchievementTierWithHttpInfo = function(achievement_tier_id, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`achievement_tier_id`)) {
        stop("Missing required parameter `achievement_tier_id`.")
      }

      if (!missing(`achievement_tier_id`) && is.null(`achievement_tier_id`)) {
        stop("Invalid value for `achievement_tier_id` when calling AchievementApi$DeleteAchievementTier, `achievement_tier_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$DeleteAchievementTier, `account_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["achievementTierId"]] <- `achievement_tier_id`

      local_var_url_path <- "/achievement/tier/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Achievement
    #'
    #' @param achievement_id The ID of the achievement
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param achievement_type (optional) achievementType
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AchievementTierResponse
    GetAchievement = function(achievement_id, device_id = NULL, account_id = NULL, achievement_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetAchievementWithHttpInfo(achievement_id, device_id, account_id, achievement_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Achievement
    #'
    #' @param achievement_id The ID of the achievement
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param achievement_type (optional) achievementType
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AchievementTierResponse) with additional information such as HTTP status code, headers
    GetAchievementWithHttpInfo = function(achievement_id, device_id = NULL, account_id = NULL, achievement_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`achievement_id`)) {
        stop("Missing required parameter `achievement_id`.")
      }

      if (!missing(`achievement_id`) && is.null(`achievement_id`)) {
        stop("Invalid value for `achievement_id` when calling AchievementApi$GetAchievement, `achievement_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AchievementApi$GetAchievement, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$GetAchievement, `account_id` is not nullable")
      }

      if (!missing(`achievement_type`) && is.null(`achievement_type`)) {
        stop("Invalid value for `achievement_type` when calling AchievementApi$GetAchievement, `achievement_type` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["achievementId"]] <- `achievement_id`

      query_params[["achievementType"]] <- `achievement_type`

      local_var_url_path <- "/achievement/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AchievementTierResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Gets an achievement tier
    #'
    #' @param account_id the account id of the user (deviceId or accountId required)
    #' @param achievement_tier_id the achievement tier id that is being retrieved
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AchievementTierResponse
    GetAchievementTier = function(account_id, achievement_tier_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetAchievementTierWithHttpInfo(account_id, achievement_tier_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Gets an achievement tier
    #'
    #' @param account_id the account id of the user (deviceId or accountId required)
    #' @param achievement_tier_id the achievement tier id that is being retrieved
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AchievementTierResponse) with additional information such as HTTP status code, headers
    GetAchievementTierWithHttpInfo = function(account_id, achievement_tier_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`achievement_tier_id`)) {
        stop("Missing required parameter `achievement_tier_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$GetAchievementTier, `account_id` is not nullable")
      }

      if (!missing(`achievement_tier_id`) && is.null(`achievement_tier_id`)) {
        stop("Invalid value for `achievement_tier_id` when calling AchievementApi$GetAchievementTier, `achievement_tier_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["achievementTierId"]] <- `achievement_tier_id`

      local_var_url_path <- "/achievement/tier/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AchievementTierResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Achievement Progress
    #'
    #' @param return_nulls determines whether to return null fields in the response
    #' @param app_key the application key for filtering results by application
    #' @param include_undiscovered determines whether to return achievements that the user has not discovered yet
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param connection_account_email (optional) the email of the account to view achievements
    #' @param connection_account_id (optional) the id of the account to view achievements
    #' @param rank_type (optional) filter results by achievement rankType
    #' @param achievement_type (optional) filter results by achievement type
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[AchievementProgressResponse]
    GetUserAchievements = function(return_nulls, app_key, include_undiscovered, device_id = NULL, account_id = NULL, connection_account_email = NULL, connection_account_id = NULL, rank_type = NULL, achievement_type = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetUserAchievementsWithHttpInfo(return_nulls, app_key, include_undiscovered, device_id, account_id, connection_account_email, connection_account_id, rank_type, achievement_type, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Achievement Progress
    #'
    #' @param return_nulls determines whether to return null fields in the response
    #' @param app_key the application key for filtering results by application
    #' @param include_undiscovered determines whether to return achievements that the user has not discovered yet
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param connection_account_email (optional) the email of the account to view achievements
    #' @param connection_account_id (optional) the id of the account to view achievements
    #' @param rank_type (optional) filter results by achievement rankType
    #' @param achievement_type (optional) filter results by achievement type
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[AchievementProgressResponse]) with additional information such as HTTP status code, headers
    GetUserAchievementsWithHttpInfo = function(return_nulls, app_key, include_undiscovered, device_id = NULL, account_id = NULL, connection_account_email = NULL, connection_account_id = NULL, rank_type = NULL, achievement_type = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`return_nulls`)) {
        stop("Missing required parameter `return_nulls`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`include_undiscovered`)) {
        stop("Missing required parameter `include_undiscovered`.")
      }

      if (!missing(`return_nulls`) && is.null(`return_nulls`)) {
        stop("Invalid value for `return_nulls` when calling AchievementApi$GetUserAchievements, `return_nulls` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AchievementApi$GetUserAchievements, `app_key` is not nullable")
      }

      if (!missing(`include_undiscovered`) && is.null(`include_undiscovered`)) {
        stop("Invalid value for `include_undiscovered` when calling AchievementApi$GetUserAchievements, `include_undiscovered` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AchievementApi$GetUserAchievements, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$GetUserAchievements, `account_id` is not nullable")
      }

      if (!missing(`connection_account_email`) && is.null(`connection_account_email`)) {
        stop("Invalid value for `connection_account_email` when calling AchievementApi$GetUserAchievements, `connection_account_email` is not nullable")
      }

      if (!missing(`connection_account_id`) && is.null(`connection_account_id`)) {
        stop("Invalid value for `connection_account_id` when calling AchievementApi$GetUserAchievements, `connection_account_id` is not nullable")
      }

      if (!missing(`rank_type`) && is.null(`rank_type`)) {
        stop("Invalid value for `rank_type` when calling AchievementApi$GetUserAchievements, `rank_type` is not nullable")
      }

      if (!missing(`achievement_type`) && is.null(`achievement_type`)) {
        stop("Invalid value for `achievement_type` when calling AchievementApi$GetUserAchievements, `achievement_type` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AchievementApi$GetUserAchievements, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AchievementApi$GetUserAchievements, `longitude` is not nullable")
      }

      query_params[["returnNulls"]] <- `return_nulls`

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["connectionAccountEmail"]] <- `connection_account_email`

      query_params[["connectionAccountId"]] <- `connection_account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["rankType"]] <- `rank_type`

      query_params[["achievementType"]] <- `achievement_type`

      query_params[["includeUndiscovered"]] <- `include_undiscovered`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/achievement/progress/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[AchievementProgressResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List Achievement Tags
    #'
    #' @param app_key (optional) filter results by application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    ListAchievementTags = function(app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ListAchievementTagsWithHttpInfo(app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List Achievement Tags
    #'
    #' @param app_key (optional) filter results by application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    ListAchievementTagsWithHttpInfo = function(app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AchievementApi$ListAchievementTags, `app_key` is not nullable")
      }

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/achievement/tag/list"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List Achievements
    #'
    #' @param sort_field the field to sort by. See AchievementApiMap
    #' @param descending determines whether the sorted list is in descending or ascending order
    #' @param start the start index for pagination
    #' @param limit the limit for pagination (has a hard limit of 1000)
    #' @param active_only Filter results to only return active achievements
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param app_key (optional) the application key
    #' @param keyword (optional) the keyword used to filter results with (this returns results that have the keyword in the title or description of the achievement)
    #' @param achievement_type (optional) filter results by the achievementType (these are exact case sensitive matches)
    #' @param rank_type (optional) filter results by the rankType (these are exact case sensitive matches)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[AchievementShortResponse]
    ListAchievements = function(sort_field, descending, start, limit, active_only, device_id = NULL, account_id = NULL, app_key = NULL, keyword = NULL, achievement_type = NULL, rank_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ListAchievementsWithHttpInfo(sort_field, descending, start, limit, active_only, device_id, account_id, app_key, keyword, achievement_type, rank_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List Achievements
    #'
    #' @param sort_field the field to sort by. See AchievementApiMap
    #' @param descending determines whether the sorted list is in descending or ascending order
    #' @param start the start index for pagination
    #' @param limit the limit for pagination (has a hard limit of 1000)
    #' @param active_only Filter results to only return active achievements
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param app_key (optional) the application key
    #' @param keyword (optional) the keyword used to filter results with (this returns results that have the keyword in the title or description of the achievement)
    #' @param achievement_type (optional) filter results by the achievementType (these are exact case sensitive matches)
    #' @param rank_type (optional) filter results by the rankType (these are exact case sensitive matches)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[AchievementShortResponse]) with additional information such as HTTP status code, headers
    ListAchievementsWithHttpInfo = function(sort_field, descending, start, limit, active_only, device_id = NULL, account_id = NULL, app_key = NULL, keyword = NULL, achievement_type = NULL, rank_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`active_only`)) {
        stop("Missing required parameter `active_only`.")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AchievementApi$ListAchievements, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AchievementApi$ListAchievements, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AchievementApi$ListAchievements, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AchievementApi$ListAchievements, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling AchievementApi$ListAchievements, `active_only` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AchievementApi$ListAchievements, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$ListAchievements, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AchievementApi$ListAchievements, `app_key` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling AchievementApi$ListAchievements, `keyword` is not nullable")
      }

      if (!missing(`achievement_type`) && is.null(`achievement_type`)) {
        stop("Invalid value for `achievement_type` when calling AchievementApi$ListAchievements, `achievement_type` is not nullable")
      }

      if (!missing(`rank_type`) && is.null(`rank_type`)) {
        stop("Invalid value for `rank_type` when calling AchievementApi$ListAchievements, `rank_type` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["keyword"]] <- `keyword`

      query_params[["achievementType"]] <- `achievement_type`

      query_params[["rankType"]] <- `rank_type`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "TITLE", "DESCRIPTION", "RANK_TYPE", "APPLICATION_ID", "APPLICATION_NAME", "BILLABLE_ID"))) {
        stop("Invalid value for sort_field when calling AchievementApi$ListAchievements. Must be [CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, TITLE, DESCRIPTION, RANK_TYPE, APPLICATION_ID, APPLICATION_NAME, BILLABLE_ID].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      local_var_url_path <- "/achievement/list"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[AchievementShortResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Achievements
    #'
    #' @param app_key the application key
    #' @param sort_field the field to sort by. See AchievementApiMap
    #' @param descending determines whether the sorted list is in descending or ascending order
    #' @param include_tiers return tiers, only applicable for version >3.18
    #' @param include_inactive_tiers return inactive tiers, only applicable when includeTiers is true, only applicable for version >3.18
    #' @param start the start index for pagination
    #' @param limit the limit for pagination (has a hard limit of 1000)
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param keyword (optional) the keyword used to filter results with (this returns results that have the keyword in the title or description of the achievement)
    #' @param achievement_type (optional) filter results by the achievementType (these are exact case sensitive matches)
    #' @param rank_type (optional) filter results by the rankType (these are exact case sensitive matches)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[AchievementShortResponse]
    SearchAchievements = function(app_key, sort_field, descending, include_tiers, include_inactive_tiers, start, limit, device_id = NULL, account_id = NULL, keyword = NULL, achievement_type = NULL, rank_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchAchievementsWithHttpInfo(app_key, sort_field, descending, include_tiers, include_inactive_tiers, start, limit, device_id, account_id, keyword, achievement_type, rank_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Achievements
    #'
    #' @param app_key the application key
    #' @param sort_field the field to sort by. See AchievementApiMap
    #' @param descending determines whether the sorted list is in descending or ascending order
    #' @param include_tiers return tiers, only applicable for version >3.18
    #' @param include_inactive_tiers return inactive tiers, only applicable when includeTiers is true, only applicable for version >3.18
    #' @param start the start index for pagination
    #' @param limit the limit for pagination (has a hard limit of 1000)
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param keyword (optional) the keyword used to filter results with (this returns results that have the keyword in the title or description of the achievement)
    #' @param achievement_type (optional) filter results by the achievementType (these are exact case sensitive matches)
    #' @param rank_type (optional) filter results by the rankType (these are exact case sensitive matches)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[AchievementShortResponse]) with additional information such as HTTP status code, headers
    SearchAchievementsWithHttpInfo = function(app_key, sort_field, descending, include_tiers, include_inactive_tiers, start, limit, device_id = NULL, account_id = NULL, keyword = NULL, achievement_type = NULL, rank_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`include_tiers`)) {
        stop("Missing required parameter `include_tiers`.")
      }

      if (missing(`include_inactive_tiers`)) {
        stop("Missing required parameter `include_inactive_tiers`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AchievementApi$SearchAchievements, `app_key` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AchievementApi$SearchAchievements, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AchievementApi$SearchAchievements, `descending` is not nullable")
      }

      if (!missing(`include_tiers`) && is.null(`include_tiers`)) {
        stop("Invalid value for `include_tiers` when calling AchievementApi$SearchAchievements, `include_tiers` is not nullable")
      }

      if (!missing(`include_inactive_tiers`) && is.null(`include_inactive_tiers`)) {
        stop("Invalid value for `include_inactive_tiers` when calling AchievementApi$SearchAchievements, `include_inactive_tiers` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AchievementApi$SearchAchievements, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AchievementApi$SearchAchievements, `limit` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AchievementApi$SearchAchievements, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$SearchAchievements, `account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling AchievementApi$SearchAchievements, `keyword` is not nullable")
      }

      if (!missing(`achievement_type`) && is.null(`achievement_type`)) {
        stop("Invalid value for `achievement_type` when calling AchievementApi$SearchAchievements, `achievement_type` is not nullable")
      }

      if (!missing(`rank_type`) && is.null(`rank_type`)) {
        stop("Invalid value for `rank_type` when calling AchievementApi$SearchAchievements, `rank_type` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["keyword"]] <- `keyword`

      query_params[["achievementType"]] <- `achievement_type`

      query_params[["rankType"]] <- `rank_type`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "TITLE", "DESCRIPTION", "RANK_TYPE", "APPLICATION_ID", "APPLICATION_NAME", "BILLABLE_ID"))) {
        stop("Invalid value for sort_field when calling AchievementApi$SearchAchievements. Must be [CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, TITLE, DESCRIPTION, RANK_TYPE, APPLICATION_ID, APPLICATION_NAME, BILLABLE_ID].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["includeTiers"]] <- `include_tiers`

      query_params[["includeInactiveTiers"]] <- `include_inactive_tiers`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/achievement/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[AchievementShortResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Achievement
    #'
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param achievement_id (optional) the achievement ID for updating an existing achievement
    #' @param analytics_tag (optional) the analytics tag that will trigger when a user's achievement count gets updated
    #' @param title (optional) the title of the achievement (255 character limit)
    #' @param description (optional) the description of the achievement
    #' @param rank_type (optional) the rank type for updating leader boards
    #' @param rank_increment (optional) determines how much the rank count is incremented
    #' @param min_increment (optional) restrict scores to be above or equal to this minimum value
    #' @param null_min_increment (optional) enable to ignore usage of minIncrement
    #' @param max_increment (optional) restrict scores to be below or equal to this maximum value
    #' @param null_max_increment (optional) enable to ignore usage of maxIncrement
    #' @param validate (optional) determines whether the customId on analytics are used to validate a user's achievement progress.
    #' @param active (optional) if it's active or inactive
    #' @param trigger_definition (optional) if provided will define what triggers to run after a tier is completed
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AchievementResponse
    UpdateAchievement = function(device_id = NULL, account_id = NULL, achievement_id = NULL, analytics_tag = NULL, title = NULL, description = NULL, rank_type = NULL, rank_increment = NULL, min_increment = NULL, null_min_increment = NULL, max_increment = NULL, null_max_increment = NULL, validate = NULL, active = NULL, trigger_definition = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateAchievementWithHttpInfo(device_id, account_id, achievement_id, analytics_tag, title, description, rank_type, rank_increment, min_increment, null_min_increment, max_increment, null_max_increment, validate, active, trigger_definition, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Achievement
    #'
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param achievement_id (optional) the achievement ID for updating an existing achievement
    #' @param analytics_tag (optional) the analytics tag that will trigger when a user's achievement count gets updated
    #' @param title (optional) the title of the achievement (255 character limit)
    #' @param description (optional) the description of the achievement
    #' @param rank_type (optional) the rank type for updating leader boards
    #' @param rank_increment (optional) determines how much the rank count is incremented
    #' @param min_increment (optional) restrict scores to be above or equal to this minimum value
    #' @param null_min_increment (optional) enable to ignore usage of minIncrement
    #' @param max_increment (optional) restrict scores to be below or equal to this maximum value
    #' @param null_max_increment (optional) enable to ignore usage of maxIncrement
    #' @param validate (optional) determines whether the customId on analytics are used to validate a user's achievement progress.
    #' @param active (optional) if it's active or inactive
    #' @param trigger_definition (optional) if provided will define what triggers to run after a tier is completed
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AchievementResponse) with additional information such as HTTP status code, headers
    UpdateAchievementWithHttpInfo = function(device_id = NULL, account_id = NULL, achievement_id = NULL, analytics_tag = NULL, title = NULL, description = NULL, rank_type = NULL, rank_increment = NULL, min_increment = NULL, null_min_increment = NULL, max_increment = NULL, null_max_increment = NULL, validate = NULL, active = NULL, trigger_definition = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AchievementApi$UpdateAchievement, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$UpdateAchievement, `account_id` is not nullable")
      }

      if (!missing(`achievement_id`) && is.null(`achievement_id`)) {
        stop("Invalid value for `achievement_id` when calling AchievementApi$UpdateAchievement, `achievement_id` is not nullable")
      }

      if (!missing(`analytics_tag`) && is.null(`analytics_tag`)) {
        stop("Invalid value for `analytics_tag` when calling AchievementApi$UpdateAchievement, `analytics_tag` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling AchievementApi$UpdateAchievement, `title` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling AchievementApi$UpdateAchievement, `description` is not nullable")
      }

      if (!missing(`rank_type`) && is.null(`rank_type`)) {
        stop("Invalid value for `rank_type` when calling AchievementApi$UpdateAchievement, `rank_type` is not nullable")
      }

      if (!missing(`rank_increment`) && is.null(`rank_increment`)) {
        stop("Invalid value for `rank_increment` when calling AchievementApi$UpdateAchievement, `rank_increment` is not nullable")
      }

      if (!missing(`min_increment`) && is.null(`min_increment`)) {
        stop("Invalid value for `min_increment` when calling AchievementApi$UpdateAchievement, `min_increment` is not nullable")
      }

      if (!missing(`null_min_increment`) && is.null(`null_min_increment`)) {
        stop("Invalid value for `null_min_increment` when calling AchievementApi$UpdateAchievement, `null_min_increment` is not nullable")
      }

      if (!missing(`max_increment`) && is.null(`max_increment`)) {
        stop("Invalid value for `max_increment` when calling AchievementApi$UpdateAchievement, `max_increment` is not nullable")
      }

      if (!missing(`null_max_increment`) && is.null(`null_max_increment`)) {
        stop("Invalid value for `null_max_increment` when calling AchievementApi$UpdateAchievement, `null_max_increment` is not nullable")
      }

      if (!missing(`validate`) && is.null(`validate`)) {
        stop("Invalid value for `validate` when calling AchievementApi$UpdateAchievement, `validate` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling AchievementApi$UpdateAchievement, `active` is not nullable")
      }

      if (!missing(`trigger_definition`) && is.null(`trigger_definition`)) {
        stop("Invalid value for `trigger_definition` when calling AchievementApi$UpdateAchievement, `trigger_definition` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["achievementId"]] <- `achievement_id`

      query_params[["analyticsTag"]] <- `analytics_tag`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["rankType"]] <- `rank_type`

      query_params[["rankIncrement"]] <- `rank_increment`

      query_params[["minIncrement"]] <- `min_increment`

      query_params[["nullMinIncrement"]] <- `null_min_increment`

      query_params[["maxIncrement"]] <- `max_increment`

      query_params[["nullMaxIncrement"]] <- `null_max_increment`

      query_params[["validate"]] <- `validate`

      query_params[["active"]] <- `active`

      query_params[["triggerDefinition"]] <- `trigger_definition`

      local_var_url_path <- "/achievement/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AchievementResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Achievement Tier
    #'
    #' @param achievement_tier_id the achievement tier id for updating
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param icon (optional) achievement tier icon image file
    #' @param icon_asset_id (optional) the icon assetId, if icon is provided, icon will overrule
    #' @param title (optional) the title of the achievement tier
    #' @param description (optional) the description of the achievement tier
    #' @param goal_count (optional) the count requirement for completing the achievement tier
    #' @param mission_id (optional) The ID of the mission to associate with the achievement
    #' @param game_id (optional) The ID of the game to associate with the achievement
    #' @param pack_id (optional) The ID of the pack to associate with the achievement
    #' @param game_level_id (optional) The ID of the game level to associate with the achievement
    #' @param game_object_id (optional) The ID of the game object to associate with the achievement
    #' @param score_all_instances (optional) score all instances
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AchievementTierResponse
    UpdateAchievementTier = function(achievement_tier_id, device_id = NULL, account_id = NULL, icon = NULL, icon_asset_id = NULL, title = NULL, description = NULL, goal_count = NULL, mission_id = NULL, game_id = NULL, pack_id = NULL, game_level_id = NULL, game_object_id = NULL, score_all_instances = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateAchievementTierWithHttpInfo(achievement_tier_id, device_id, account_id, icon, icon_asset_id, title, description, goal_count, mission_id, game_id, pack_id, game_level_id, game_object_id, score_all_instances, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Achievement Tier
    #'
    #' @param achievement_tier_id the achievement tier id for updating
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param icon (optional) achievement tier icon image file
    #' @param icon_asset_id (optional) the icon assetId, if icon is provided, icon will overrule
    #' @param title (optional) the title of the achievement tier
    #' @param description (optional) the description of the achievement tier
    #' @param goal_count (optional) the count requirement for completing the achievement tier
    #' @param mission_id (optional) The ID of the mission to associate with the achievement
    #' @param game_id (optional) The ID of the game to associate with the achievement
    #' @param pack_id (optional) The ID of the pack to associate with the achievement
    #' @param game_level_id (optional) The ID of the game level to associate with the achievement
    #' @param game_object_id (optional) The ID of the game object to associate with the achievement
    #' @param score_all_instances (optional) score all instances
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AchievementTierResponse) with additional information such as HTTP status code, headers
    UpdateAchievementTierWithHttpInfo = function(achievement_tier_id, device_id = NULL, account_id = NULL, icon = NULL, icon_asset_id = NULL, title = NULL, description = NULL, goal_count = NULL, mission_id = NULL, game_id = NULL, pack_id = NULL, game_level_id = NULL, game_object_id = NULL, score_all_instances = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`achievement_tier_id`)) {
        stop("Missing required parameter `achievement_tier_id`.")
      }

      if (!missing(`achievement_tier_id`) && is.null(`achievement_tier_id`)) {
        stop("Invalid value for `achievement_tier_id` when calling AchievementApi$UpdateAchievementTier, `achievement_tier_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AchievementApi$UpdateAchievementTier, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$UpdateAchievementTier, `account_id` is not nullable")
      }

      if (!missing(`icon`) && is.null(`icon`)) {
        stop("Invalid value for `icon` when calling AchievementApi$UpdateAchievementTier, `icon` is not nullable")
      }

      if (!missing(`icon_asset_id`) && is.null(`icon_asset_id`)) {
        stop("Invalid value for `icon_asset_id` when calling AchievementApi$UpdateAchievementTier, `icon_asset_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling AchievementApi$UpdateAchievementTier, `title` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling AchievementApi$UpdateAchievementTier, `description` is not nullable")
      }

      if (!missing(`goal_count`) && is.null(`goal_count`)) {
        stop("Invalid value for `goal_count` when calling AchievementApi$UpdateAchievementTier, `goal_count` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling AchievementApi$UpdateAchievementTier, `mission_id` is not nullable")
      }

      if (!missing(`game_id`) && is.null(`game_id`)) {
        stop("Invalid value for `game_id` when calling AchievementApi$UpdateAchievementTier, `game_id` is not nullable")
      }

      if (!missing(`pack_id`) && is.null(`pack_id`)) {
        stop("Invalid value for `pack_id` when calling AchievementApi$UpdateAchievementTier, `pack_id` is not nullable")
      }

      if (!missing(`game_level_id`) && is.null(`game_level_id`)) {
        stop("Invalid value for `game_level_id` when calling AchievementApi$UpdateAchievementTier, `game_level_id` is not nullable")
      }

      if (!missing(`game_object_id`) && is.null(`game_object_id`)) {
        stop("Invalid value for `game_object_id` when calling AchievementApi$UpdateAchievementTier, `game_object_id` is not nullable")
      }

      if (!missing(`score_all_instances`) && is.null(`score_all_instances`)) {
        stop("Invalid value for `score_all_instances` when calling AchievementApi$UpdateAchievementTier, `score_all_instances` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["achievementTierId"]] <- `achievement_tier_id`

      query_params[["icon"]] <- `icon`

      query_params[["iconAssetId"]] <- `icon_asset_id`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["goalCount"]] <- `goal_count`

      query_params[["missionId"]] <- `mission_id`

      query_params[["gameId"]] <- `game_id`

      query_params[["packId"]] <- `pack_id`

      query_params[["gameLevelId"]] <- `game_level_id`

      query_params[["gameObjectId"]] <- `game_object_id`

      query_params[["scoreAllInstances"]] <- `score_all_instances`

      local_var_url_path <- "/achievement/tier/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AchievementTierResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Achievement Progress
    #'
    #' @param account_id the account id of the user
    #' @param achievement_id (optional) the achievement id (achievementId or tag required)
    #' @param tag (optional) the analytic tag to identify an achievement (achievementId or tag required)
    #' @param custom_id (optional) a custom identifier used for validation
    #' @param increment (optional) the amount to increment an achievement progress by (if no increment is sent in, the server defaults to using the achievement's rankIncrement value)
    #' @param start_date (optional) a custom start date that the client can set (not yet used in server logic)
    #' @param end_date (optional) a custom end date that the client can set (not yet used in server logic)
    #' @param return_progress (optional) determines whether to return the achievement progress response
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UpdateUserAchievement = function(account_id, achievement_id = NULL, tag = NULL, custom_id = NULL, increment = NULL, start_date = NULL, end_date = NULL, return_progress = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateUserAchievementWithHttpInfo(account_id, achievement_id, tag, custom_id, increment, start_date, end_date, return_progress, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Achievement Progress
    #'
    #' @param account_id the account id of the user
    #' @param achievement_id (optional) the achievement id (achievementId or tag required)
    #' @param tag (optional) the analytic tag to identify an achievement (achievementId or tag required)
    #' @param custom_id (optional) a custom identifier used for validation
    #' @param increment (optional) the amount to increment an achievement progress by (if no increment is sent in, the server defaults to using the achievement's rankIncrement value)
    #' @param start_date (optional) a custom start date that the client can set (not yet used in server logic)
    #' @param end_date (optional) a custom end date that the client can set (not yet used in server logic)
    #' @param return_progress (optional) determines whether to return the achievement progress response
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UpdateUserAchievementWithHttpInfo = function(account_id, achievement_id = NULL, tag = NULL, custom_id = NULL, increment = NULL, start_date = NULL, end_date = NULL, return_progress = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AchievementApi$UpdateUserAchievement, `account_id` is not nullable")
      }

      if (!missing(`achievement_id`) && is.null(`achievement_id`)) {
        stop("Invalid value for `achievement_id` when calling AchievementApi$UpdateUserAchievement, `achievement_id` is not nullable")
      }

      if (!missing(`tag`) && is.null(`tag`)) {
        stop("Invalid value for `tag` when calling AchievementApi$UpdateUserAchievement, `tag` is not nullable")
      }

      if (!missing(`custom_id`) && is.null(`custom_id`)) {
        stop("Invalid value for `custom_id` when calling AchievementApi$UpdateUserAchievement, `custom_id` is not nullable")
      }

      if (!missing(`increment`) && is.null(`increment`)) {
        stop("Invalid value for `increment` when calling AchievementApi$UpdateUserAchievement, `increment` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling AchievementApi$UpdateUserAchievement, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling AchievementApi$UpdateUserAchievement, `end_date` is not nullable")
      }

      if (!missing(`return_progress`) && is.null(`return_progress`)) {
        stop("Invalid value for `return_progress` when calling AchievementApi$UpdateUserAchievement, `return_progress` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["achievementId"]] <- `achievement_id`

      query_params[["tag"]] <- `tag`

      query_params[["customId"]] <- `custom_id`

      query_params[["increment"]] <- `increment`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["returnProgress"]] <- `return_progress`

      local_var_url_path <- "/achievement/progress/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
