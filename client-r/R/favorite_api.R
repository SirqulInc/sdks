#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Favorite operations
#' @description FavoriteApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AddFavorite  ####################
#'
#' library(openapi)
#' var_favoritable_id <- 56 # integer | The ID of the object to favorite {offerId, offerLocationId, retailerLocationId, categoryId}
#' var_favoritable_type <- "favoritable_type_example" # character | The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY, ALBUM}
#' var_device_id <- "device_id_example" # character | The unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account ID of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#'
#' #Create Favorite
#' api_instance <- FavoriteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AddFavorite(var_favoritable_id, var_favoritable_type, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$AddFavorite(var_favoritable_id, var_favoritable_type, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  DeleteFavorite  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account ID of the user (deviceId or accountId required) (Optional)
#' var_favorite_id <- 56 # integer | The ID of the favorite reference record (only optional if favoritableId & favoritableType is pass in instead) (Optional)
#' var_favoritable_id <- 56 # integer | The ID of the object to un-favorite {offerId, offerLocationId, retailerLocationId, categoryId} (this is required if favoriteId is NOT passed in) (Optional)
#' var_favoritable_type <- "favoritable_type_example" # character | The type of the object to un-favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY} (this is required if favoriteId is NOT passed in) (Optional)
#'
#' #Delete Favorite
#' api_instance <- FavoriteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteFavorite(device_id = var_device_id, account_id = var_account_id, favorite_id = var_favorite_id, favoritable_id = var_favoritable_id, favoritable_type = var_favoritable_typedata_file = "result.txt")
#' result <- api_instance$DeleteFavorite(device_id = var_device_id, account_id = var_account_id, favorite_id = var_favorite_id, favoritable_id = var_favoritable_id, favoritable_type = var_favoritable_type)
#' dput(result)
#'
#'
#' ####################  GetFavorite  ####################
#'
#' library(openapi)
#' var_favorite_id <- 56 # integer | The ID of the favorite reference record
#' var_device_id <- "device_id_example" # character | The unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account ID of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#'
#' #Get Favorite
#' api_instance <- FavoriteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetFavorite(var_favorite_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$GetFavorite(var_favorite_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  SearchFavorites  ####################
#'
#' library(openapi)
#' var_favoritable_type <- "favoritable_type_example" # character | The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY}
#' var_sort_field <- "sort_field_example" # character | Determines what to sort the results by {CREATED, UPDATED, DISPLAY}
#' var_descending <- "descending_example" # character | Determines whether the results are in descending order
#' var_start <- 56 # integer | The start index for pagination
#' var_limit <- 56 # integer | The limit for pagination (there is a hard limit of 1000)
#' var_active_only <- "active_only_example" # character | Determines whether to only return active favorites
#' var_return_full_response <- "return_full_response_example" # character | Determines whether to return a detailed version of the response list
#' var_device_id <- "device_id_example" # character | The unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account ID of the user (deviceId or accountId required) (Optional)
#' var_connection_account_id <- 56 # integer | The ID of an account the user would like to view favorites for (Optional)
#' var_secondary_type <- "secondary_type_example" # character |  (Optional)
#' var_keyword <- "keyword_example" # character | The keyword to search for (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#'
#' #Search Favorites
#' api_instance <- FavoriteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchFavorites(var_favoritable_type, var_sort_field, var_descending, var_start, var_limit, var_active_only, var_return_full_response, device_id = var_device_id, account_id = var_account_id, connection_account_id = var_connection_account_id, secondary_type = var_secondary_type, keyword = var_keyword, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$SearchFavorites(var_favoritable_type, var_sort_field, var_descending, var_start, var_limit, var_active_only, var_return_full_response, device_id = var_device_id, account_id = var_account_id, connection_account_id = var_connection_account_id, secondary_type = var_secondary_type, keyword = var_keyword, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  WhoHasFavorited  ####################
#'
#' library(openapi)
#' var_favoritable_id <- 56 # integer | The ID of the favoritableType to search on
#' var_favoritable_type <- "favoritable_type_example" # character | The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY}
#' var_start <- 56 # integer | The start index for pagination
#' var_limit <- 56 # integer | The limit for pagination
#' var_device_id <- "device_id_example" # character | The unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account ID of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | The current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current longitude of the user (Optional)
#' var_keyword <- "keyword_example" # character | The keyword to limit that account list (Optional)
#'
#' #Who has Favorited
#' api_instance <- FavoriteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$WhoHasFavorited(var_favoritable_id, var_favoritable_type, var_start, var_limit, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, keyword = var_keyworddata_file = "result.txt")
#' result <- api_instance$WhoHasFavorited(var_favoritable_id, var_favoritable_type, var_start, var_limit, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, keyword = var_keyword)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
FavoriteApi <- R6::R6Class(
  "FavoriteApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new FavoriteApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Favorite
    #'
    #' @param favoritable_id The ID of the object to favorite {offerId, offerLocationId, retailerLocationId, categoryId}
    #' @param favoritable_type The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY, ALBUM}
    #' @param device_id (optional) The unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return WrappedResponse
    AddFavorite = function(favoritable_id, favoritable_type, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AddFavoriteWithHttpInfo(favoritable_id, favoritable_type, device_id, account_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Favorite
    #'
    #' @param favoritable_id The ID of the object to favorite {offerId, offerLocationId, retailerLocationId, categoryId}
    #' @param favoritable_type The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY, ALBUM}
    #' @param device_id (optional) The unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (WrappedResponse) with additional information such as HTTP status code, headers
    AddFavoriteWithHttpInfo = function(favoritable_id, favoritable_type, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`favoritable_id`)) {
        stop("Missing required parameter `favoritable_id`.")
      }

      if (missing(`favoritable_type`)) {
        stop("Missing required parameter `favoritable_type`.")
      }

      if (!missing(`favoritable_id`) && is.null(`favoritable_id`)) {
        stop("Invalid value for `favoritable_id` when calling FavoriteApi$AddFavorite, `favoritable_id` is not nullable")
      }

      if (!missing(`favoritable_type`) && is.null(`favoritable_type`)) {
        stop("Invalid value for `favoritable_type` when calling FavoriteApi$AddFavorite, `favoritable_type` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling FavoriteApi$AddFavorite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling FavoriteApi$AddFavorite, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling FavoriteApi$AddFavorite, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling FavoriteApi$AddFavorite, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["favoritableId"]] <- `favoritable_id`

      query_params[["favoritableType"]] <- `favoritable_type`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/favorite/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "WrappedResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Favorite
    #'
    #' @param device_id (optional) The unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param favorite_id (optional) The ID of the favorite reference record (only optional if favoritableId & favoritableType is pass in instead)
    #' @param favoritable_id (optional) The ID of the object to un-favorite {offerId, offerLocationId, retailerLocationId, categoryId} (this is required if favoriteId is NOT passed in)
    #' @param favoritable_type (optional) The type of the object to un-favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY} (this is required if favoriteId is NOT passed in)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteFavorite = function(device_id = NULL, account_id = NULL, favorite_id = NULL, favoritable_id = NULL, favoritable_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteFavoriteWithHttpInfo(device_id, account_id, favorite_id, favoritable_id, favoritable_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Favorite
    #'
    #' @param device_id (optional) The unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param favorite_id (optional) The ID of the favorite reference record (only optional if favoritableId & favoritableType is pass in instead)
    #' @param favoritable_id (optional) The ID of the object to un-favorite {offerId, offerLocationId, retailerLocationId, categoryId} (this is required if favoriteId is NOT passed in)
    #' @param favoritable_type (optional) The type of the object to un-favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY} (this is required if favoriteId is NOT passed in)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteFavoriteWithHttpInfo = function(device_id = NULL, account_id = NULL, favorite_id = NULL, favoritable_id = NULL, favoritable_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling FavoriteApi$DeleteFavorite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling FavoriteApi$DeleteFavorite, `account_id` is not nullable")
      }

      if (!missing(`favorite_id`) && is.null(`favorite_id`)) {
        stop("Invalid value for `favorite_id` when calling FavoriteApi$DeleteFavorite, `favorite_id` is not nullable")
      }

      if (!missing(`favoritable_id`) && is.null(`favoritable_id`)) {
        stop("Invalid value for `favoritable_id` when calling FavoriteApi$DeleteFavorite, `favoritable_id` is not nullable")
      }

      if (!missing(`favoritable_type`) && is.null(`favoritable_type`)) {
        stop("Invalid value for `favoritable_type` when calling FavoriteApi$DeleteFavorite, `favoritable_type` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["favoriteId"]] <- `favorite_id`

      query_params[["favoritableId"]] <- `favoritable_id`

      query_params[["favoritableType"]] <- `favoritable_type`

      local_var_url_path <- "/favorite/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Favorite
    #'
    #' @param favorite_id The ID of the favorite reference record
    #' @param device_id (optional) The unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return WrappedResponse
    GetFavorite = function(favorite_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetFavoriteWithHttpInfo(favorite_id, device_id, account_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Favorite
    #'
    #' @param favorite_id The ID of the favorite reference record
    #' @param device_id (optional) The unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (WrappedResponse) with additional information such as HTTP status code, headers
    GetFavoriteWithHttpInfo = function(favorite_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`favorite_id`)) {
        stop("Missing required parameter `favorite_id`.")
      }

      if (!missing(`favorite_id`) && is.null(`favorite_id`)) {
        stop("Invalid value for `favorite_id` when calling FavoriteApi$GetFavorite, `favorite_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling FavoriteApi$GetFavorite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling FavoriteApi$GetFavorite, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling FavoriteApi$GetFavorite, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling FavoriteApi$GetFavorite, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["favoriteId"]] <- `favorite_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/favorite/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "WrappedResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Favorites
    #'
    #' @param favoritable_type The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY}
    #' @param sort_field Determines what to sort the results by {CREATED, UPDATED, DISPLAY}
    #' @param descending Determines whether the results are in descending order
    #' @param start The start index for pagination
    #' @param limit The limit for pagination (there is a hard limit of 1000)
    #' @param active_only Determines whether to only return active favorites
    #' @param return_full_response Determines whether to return a detailed version of the response list
    #' @param device_id (optional) The unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param connection_account_id (optional) The ID of an account the user would like to view favorites for
    #' @param secondary_type (optional) 
    #' @param keyword (optional) The keyword to search for
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SearchResponse
    SearchFavorites = function(favoritable_type, sort_field, descending, start, limit, active_only, return_full_response, device_id = NULL, account_id = NULL, connection_account_id = NULL, secondary_type = NULL, keyword = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchFavoritesWithHttpInfo(favoritable_type, sort_field, descending, start, limit, active_only, return_full_response, device_id, account_id, connection_account_id, secondary_type, keyword, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Favorites
    #'
    #' @param favoritable_type The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY}
    #' @param sort_field Determines what to sort the results by {CREATED, UPDATED, DISPLAY}
    #' @param descending Determines whether the results are in descending order
    #' @param start The start index for pagination
    #' @param limit The limit for pagination (there is a hard limit of 1000)
    #' @param active_only Determines whether to only return active favorites
    #' @param return_full_response Determines whether to return a detailed version of the response list
    #' @param device_id (optional) The unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param connection_account_id (optional) The ID of an account the user would like to view favorites for
    #' @param secondary_type (optional) 
    #' @param keyword (optional) The keyword to search for
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SearchResponse) with additional information such as HTTP status code, headers
    SearchFavoritesWithHttpInfo = function(favoritable_type, sort_field, descending, start, limit, active_only, return_full_response, device_id = NULL, account_id = NULL, connection_account_id = NULL, secondary_type = NULL, keyword = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`favoritable_type`)) {
        stop("Missing required parameter `favoritable_type`.")
      }

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`active_only`)) {
        stop("Missing required parameter `active_only`.")
      }

      if (missing(`return_full_response`)) {
        stop("Missing required parameter `return_full_response`.")
      }

      if (!missing(`favoritable_type`) && is.null(`favoritable_type`)) {
        stop("Invalid value for `favoritable_type` when calling FavoriteApi$SearchFavorites, `favoritable_type` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling FavoriteApi$SearchFavorites, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling FavoriteApi$SearchFavorites, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling FavoriteApi$SearchFavorites, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling FavoriteApi$SearchFavorites, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling FavoriteApi$SearchFavorites, `active_only` is not nullable")
      }

      if (!missing(`return_full_response`) && is.null(`return_full_response`)) {
        stop("Invalid value for `return_full_response` when calling FavoriteApi$SearchFavorites, `return_full_response` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling FavoriteApi$SearchFavorites, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling FavoriteApi$SearchFavorites, `account_id` is not nullable")
      }

      if (!missing(`connection_account_id`) && is.null(`connection_account_id`)) {
        stop("Invalid value for `connection_account_id` when calling FavoriteApi$SearchFavorites, `connection_account_id` is not nullable")
      }

      if (!missing(`secondary_type`) && is.null(`secondary_type`)) {
        stop("Invalid value for `secondary_type` when calling FavoriteApi$SearchFavorites, `secondary_type` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling FavoriteApi$SearchFavorites, `keyword` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling FavoriteApi$SearchFavorites, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling FavoriteApi$SearchFavorites, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["connectionAccountId"]] <- `connection_account_id`

      query_params[["favoritableType"]] <- `favoritable_type`

      query_params[["secondaryType"]] <- `secondary_type`

      query_params[["keyword"]] <- `keyword`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "DISPLAY"))) {
        stop("Invalid value for sort_field when calling FavoriteApi$SearchFavorites. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, DISPLAY].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["returnFullResponse"]] <- `return_full_response`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/favorite/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SearchResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Who has Favorited
    #'
    #' @param favoritable_id The ID of the favoritableType to search on
    #' @param favoritable_type The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY}
    #' @param start The start index for pagination
    #' @param limit The limit for pagination
    #' @param device_id (optional) The unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param keyword (optional) The keyword to limit that account list
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[AccountResponse]
    WhoHasFavorited = function(favoritable_id, favoritable_type, start, limit, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, keyword = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$WhoHasFavoritedWithHttpInfo(favoritable_id, favoritable_type, start, limit, device_id, account_id, latitude, longitude, keyword, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Who has Favorited
    #'
    #' @param favoritable_id The ID of the favoritableType to search on
    #' @param favoritable_type The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY}
    #' @param start The start index for pagination
    #' @param limit The limit for pagination
    #' @param device_id (optional) The unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) The account ID of the user (deviceId or accountId required)
    #' @param latitude (optional) The current latitude of the user
    #' @param longitude (optional) The current longitude of the user
    #' @param keyword (optional) The keyword to limit that account list
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[AccountResponse]) with additional information such as HTTP status code, headers
    WhoHasFavoritedWithHttpInfo = function(favoritable_id, favoritable_type, start, limit, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, keyword = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`favoritable_id`)) {
        stop("Missing required parameter `favoritable_id`.")
      }

      if (missing(`favoritable_type`)) {
        stop("Missing required parameter `favoritable_type`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`favoritable_id`) && is.null(`favoritable_id`)) {
        stop("Invalid value for `favoritable_id` when calling FavoriteApi$WhoHasFavorited, `favoritable_id` is not nullable")
      }

      if (!missing(`favoritable_type`) && is.null(`favoritable_type`)) {
        stop("Invalid value for `favoritable_type` when calling FavoriteApi$WhoHasFavorited, `favoritable_type` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling FavoriteApi$WhoHasFavorited, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling FavoriteApi$WhoHasFavorited, `limit` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling FavoriteApi$WhoHasFavorited, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling FavoriteApi$WhoHasFavorited, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling FavoriteApi$WhoHasFavorited, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling FavoriteApi$WhoHasFavorited, `longitude` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling FavoriteApi$WhoHasFavorited, `keyword` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["favoritableId"]] <- `favoritable_id`

      query_params[["favoritableType"]] <- `favoritable_type`

      query_params[["keyword"]] <- `keyword`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/favorite/whois"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[AccountResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
