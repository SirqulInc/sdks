#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title RetailerLocation operations
#' @description RetailerLocationApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateRetailerLocationConsumer  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | the application key
#' var_name <- "name_example" # character | The name of the retailer location
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_street_address <- "street_address_example" # character | The street address of the retailer location (Optional)
#' var_street_address2 <- "street_address2_example" # character | Additional address information (such as a suite number, floor number, building name, or PO Box) (Optional)
#' var_city <- "city_example" # character | The city of the retailer location (Optional)
#' var_state <- "state_example" # character | The state of the retailer location (Optional)
#' var_postal_code <- "postal_code_example" # character | The postal code of the retailer location (Optional)
#' var_country <- "country_example" # character | the country of the retailer location (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone (Optional)
#' var_business_phone_ext <- "business_phone_ext_example" # character | The business phone extension (Optional)
#' var_website <- "website_example" # character | The website of the retailer location (Optional)
#' var_email <- "email_example" # character | The email of the retailer location (Optional)
#' var_details_header <- "details_header_example" # character | A brief description about the retailer location (255 character limit) (Optional)
#' var_details_body <- "details_body_example" # character | A detailed description about the retailer location (Optional)
#' var_hours <- "hours_example" # character | The hours of operation (Optional)
#' var_tags <- "tags_example" # character | Custom string field for doing full-text searches (Optional)
#' var_logo_asset_id <- 56 # integer | The retailer location logo asset id (Optional)
#' var_picture1_asset_id <- 56 # integer | An asset id (Optional)
#' var_picture2_asset_id <- 56 # integer | An asset id (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category IDs used to filter retailer locations by categories (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Comma separated list of filter IDs used to filter retailer locations (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_public_location <- "public_location_example" # character | Whether the location is public (Optional)
#' var_active <- "active_example" # character | whether the retailer location created should be active or not (Optional)
#' var_location_type <- "location_type_example" # character | External custom type identifier (Optional)
#' var_latitude <- 3.4 # numeric | The latitude to center the search on (Optional)
#' var_longitude <- 3.4 # numeric | The longitude to center the search on (Optional)
#'
#' #Create Retailer Location (Consumer)
#' api_instance <- RetailerLocationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateRetailerLocationConsumer(var_app_key, var_name, device_id = var_device_id, account_id = var_account_id, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, website = var_website, email = var_email, details_header = var_details_header, details_body = var_details_body, hours = var_hours, tags = var_tags, logo_asset_id = var_logo_asset_id, picture1_asset_id = var_picture1_asset_id, picture2_asset_id = var_picture2_asset_id, category_ids = var_category_ids, filter_ids = var_filter_ids, meta_data = var_meta_data, public_location = var_public_location, active = var_active, location_type = var_location_type, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$CreateRetailerLocationConsumer(var_app_key, var_name, device_id = var_device_id, account_id = var_account_id, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, website = var_website, email = var_email, details_header = var_details_header, details_body = var_details_body, hours = var_hours, tags = var_tags, logo_asset_id = var_logo_asset_id, picture1_asset_id = var_picture1_asset_id, picture2_asset_id = var_picture2_asset_id, category_ids = var_category_ids, filter_ids = var_filter_ids, meta_data = var_meta_data, public_location = var_public_location, active = var_active, location_type = var_location_type, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  CreateRetailerLocations  ####################
#'
#' library(openapi)
#' var_retailer_id <- 56 # integer | The ID of the retailer
#' var_name <- "name_example" # character | The name of the retailer location
#' var_street_address <- "street_address_example" # character | The street address of the retailer location
#' var_city <- "city_example" # character | The city of the retailer location
#' var_state <- "state_example" # character | The state of the retailer location
#' var_postal_code <- "postal_code_example" # character | The postal code of the retailer location
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_street_address2 <- "street_address2_example" # character | Additional address information (such as a suite number, floor number, building name, or PO Box) (Optional)
#' var_country <- "country_example" # character | the country of the retailer location (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone number of the retailer location (Optional)
#' var_business_phone_ext <- "business_phone_ext_example" # character | The business phone extension (Optional)
#' var_website <- "website_example" # character | The website of the retailer location (Optional)
#' var_email <- "email_example" # character | The email of the retailer location (Optional)
#' var_internal_id <- "internal_id_example" # character | An internal identifier used by the retailer (Optional)
#' var_details_header <- "details_header_example" # character | A brief description about the retailer location (255 character limit) (Optional)
#' var_details_body <- "details_body_example" # character | A detailed description about the retailer location (Optional)
#' var_hours <- "hours_example" # character | The hours of operation (Optional)
#' var_logo <- File.new('/path/to/file') # data.frame | The retailer location logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before) (Optional)
#' var_logo_asset_id <- 56 # integer | The retailer location logo asset id (Optional)
#' var_picture1 <- File.new('/path/to/file') # data.frame | Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before) (Optional)
#' var_picture1_asset_id <- 56 # integer | An asset id (Optional)
#' var_picture2 <- File.new('/path/to/file') # data.frame | Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before) (Optional)
#' var_picture2_asset_id <- 56 # integer | An asset id (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category IDs used to filter retailer locations by categories (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Comma separated list of filter IDs used to filter retailer locations (Optional)
#' var_latitude <- 3.4 # numeric | the latitude of the retailer location (Optional)
#' var_longitude <- 3.4 # numeric | the longitude of the retailer location (Optional)
#' var_building <- "building_example" # character |  (Optional)
#' var_google_place_id <- "google_place_id_example" # character | the ID of the Google Place that this retailer location is assigned to (Optional)
#' var_yelp_id <- "yelp_id_example" # character | the Yelp ID that this retailer location is assigned to (Optional)
#' var_active <- "active_example" # character | whether the retailer location should be active or inactive (Optional)
#' var_public_location <- "public_location_example" # character | Sets whether the location is public or not (Optional)
#' var_location_type <- "location_type_example" # character | External custom type identifier (Optional)
#' var_audience_ids <- "audience_ids_example" # character | Comma separated list of audience IDs used to assign audiences to the retailer location (Optional)
#' var_audience_ids_to_add <- "audience_ids_to_add_example" # character | Comma separated list of audience IDs to add to the retailer location (Optional)
#' var_audience_ids_to_remove <- "audience_ids_to_remove_example" # character | Comma separated list of audience IDs to remove from the retailer location (Optional)
#' var_response_format <- "response_format_example" # character | The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets} (Optional)
#' var_response_includes <- "response_includes_example" # character | Comma separated list of response includes (e.g. RETAILER,ASSETS,OFFERS,CATEGORIES,FILTERS,AUDIENCES,QRCODE) (Optional)
#'
#' #Create Retailer Location
#' api_instance <- RetailerLocationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateRetailerLocations(var_retailer_id, var_name, var_street_address, var_city, var_state, var_postal_code, device_id = var_device_id, account_id = var_account_id, street_address2 = var_street_address2, country = var_country, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, website = var_website, email = var_email, internal_id = var_internal_id, details_header = var_details_header, details_body = var_details_body, hours = var_hours, logo = var_logo, logo_asset_id = var_logo_asset_id, picture1 = var_picture1, picture1_asset_id = var_picture1_asset_id, picture2 = var_picture2, picture2_asset_id = var_picture2_asset_id, category_ids = var_category_ids, filter_ids = var_filter_ids, latitude = var_latitude, longitude = var_longitude, building = var_building, google_place_id = var_google_place_id, yelp_id = var_yelp_id, active = var_active, public_location = var_public_location, location_type = var_location_type, audience_ids = var_audience_ids, audience_ids_to_add = var_audience_ids_to_add, audience_ids_to_remove = var_audience_ids_to_remove, response_format = var_response_format, response_includes = var_response_includesdata_file = "result.txt")
#' result <- api_instance$CreateRetailerLocations(var_retailer_id, var_name, var_street_address, var_city, var_state, var_postal_code, device_id = var_device_id, account_id = var_account_id, street_address2 = var_street_address2, country = var_country, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, website = var_website, email = var_email, internal_id = var_internal_id, details_header = var_details_header, details_body = var_details_body, hours = var_hours, logo = var_logo, logo_asset_id = var_logo_asset_id, picture1 = var_picture1, picture1_asset_id = var_picture1_asset_id, picture2 = var_picture2, picture2_asset_id = var_picture2_asset_id, category_ids = var_category_ids, filter_ids = var_filter_ids, latitude = var_latitude, longitude = var_longitude, building = var_building, google_place_id = var_google_place_id, yelp_id = var_yelp_id, active = var_active, public_location = var_public_location, location_type = var_location_type, audience_ids = var_audience_ids, audience_ids_to_add = var_audience_ids_to_add, audience_ids_to_remove = var_audience_ids_to_remove, response_format = var_response_format, response_includes = var_response_includes)
#' dput(result)
#'
#'
#' ####################  DeleteRetailerLocation  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the device id (Optional)
#' var_account_id <- 56 # integer | the id of the logged in user (Optional)
#' var_retailer_location_id <- 56 # integer | the id of the retailer location to delete (Optional)
#'
#' #Delete Retailer Location
#' api_instance <- RetailerLocationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteRetailerLocation(device_id = var_device_id, account_id = var_account_id, retailer_location_id = var_retailer_location_iddata_file = "result.txt")
#' result <- api_instance$DeleteRetailerLocation(device_id = var_device_id, account_id = var_account_id, retailer_location_id = var_retailer_location_id)
#' dput(result)
#'
#'
#' ####################  GetRetailerLocation  ####################
#'
#' library(openapi)
#' var_retailer_location_id <- 56 # integer | The ID of the retailer location
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_retailer_location_token <- "retailer_location_token_example" # character | the unique token of the retailer location (Optional)
#'
#' #Get Retailer Location
#' api_instance <- RetailerLocationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRetailerLocation(var_retailer_location_id, device_id = var_device_id, account_id = var_account_id, retailer_location_token = var_retailer_location_tokendata_file = "result.txt")
#' result <- api_instance$GetRetailerLocation(var_retailer_location_id, device_id = var_device_id, account_id = var_account_id, retailer_location_token = var_retailer_location_token)
#' dput(result)
#'
#'
#' ####################  GetRetailerLocationConsumer  ####################
#'
#' library(openapi)
#' var_retailer_location_id <- 56 # integer | The retailer location id
#' var_device_id <- "device_id_example" # character | The device id for returning account information (i.e. favorites) (Optional)
#' var_account_id <- 56 # integer | The account id for returning account information (i.e. favorites) (Optional)
#'
#' #Get Retailer Location (Consumer)
#' api_instance <- RetailerLocationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRetailerLocationConsumer(var_retailer_location_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$GetRetailerLocationConsumer(var_retailer_location_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  IndexedRetailerLocationDistanceSearch  ####################
#'
#' library(openapi)
#' var_latitude <- 3.4 # numeric | The latitude to center the search on
#' var_longitude <- 3.4 # numeric | The longitude to center the search on
#' var_search_range <- 3.4 # numeric | The search range in the distanceUnit specified; default is MILES.
#' var_start <- 56 # integer | The start index for pagination
#' var_limit <- 56 # integer | The limit for pagination
#' var_account_id <- 56 # integer | The account id of the user (Optional)
#' var_address <- "address_example" # character | Used to return results using this address as the center (Optional)
#' var_has_offers <- "has_offers_example" # character | boolean to indicate whether to include retailer locations that have no offers (Optional)
#' var_categories <- "categories_example" # character | Comma separate list of category ids (Optional)
#' var_filters <- "filters_example" # character | Comma separated list of filter ids (Optional)
#' var_audiences <- "audiences_example" # character | Comma separated list of audience ids (Optional)
#' var_retailer_ids <- "retailer_ids_example" # character | Comma separated list of retailer ids (Optional)
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Comma separated list of retailer location ids (Optional)
#' var_tags <- "tags_example" # character | Does a full-text search on tags (Optional)
#' var_location_type <- "location_type_example" # character | Location type filter (Optional)
#' var_sort_field <- "sort_field_example" # character | The field to sort the result set on. Possible values include: DISTANCE, RETAILER_NAME, RETAILER_LOCATION_NAME (Optional)
#' var_descending <- "descending_example" # character | Determines whether the sorted list is in descending or ascending order (Optional)
#' var_q <- "q_example" # character | This parameter is deprecated. (Optional)
#' var_keyword <- "keyword_example" # character | Search by keyword (Optional)
#' var_keyword_operator <- "keyword_operator_example" # character | Determines the operator used when there are multiple words in the 'keyword' parameter (Optional)
#' var_search_expression <- "search_expression_example" # character | Search expression to further refine results (Optional)
#' var_distance_unit <- "distance_unit_example" # character | Unit of distance (Optional)
#' var_return_favorited <- "return_favorited_example" # character | (deprecated) return favorited flag (Optional)
#' var_return_retailer <- "return_retailer_example" # character | Return retailer info (Optional)
#' var_return_assets <- "return_assets_example" # character | Return assets (Optional)
#' var_return_offers <- "return_offers_example" # character | Return offers (Optional)
#' var_return_categories <- "return_categories_example" # character | Return categories (Optional)
#' var_return_filters <- "return_filters_example" # character | Return filters (Optional)
#' var_return_audiences <- "return_audiences_example" # character | Return audiences (Optional)
#' var_return_qr_code <- "return_qr_code_example" # character | Return QR code info (Optional)
#' var_return_external_category_data <- "return_external_category_data_example" # character | Return external category data (Optional)
#' var_include_favorite <- "include_favorite_example" # character | Include favorites in response (Optional)
#' var_include_liked <- "include_liked_example" # character | Include liked flag in response (Optional)
#' var_include_rating <- "include_rating_example" # character | Include rating info in response (Optional)
#'
#' #Distance Search Retailer Locations (Indexed)
#' api_instance <- RetailerLocationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexedRetailerLocationDistanceSearch(var_latitude, var_longitude, var_search_range, var_start, var_limit, account_id = var_account_id, address = var_address, has_offers = var_has_offers, categories = var_categories, filters = var_filters, audiences = var_audiences, retailer_ids = var_retailer_ids, retailer_location_ids = var_retailer_location_ids, tags = var_tags, location_type = var_location_type, sort_field = var_sort_field, descending = var_descending, q = var_q, keyword = var_keyword, keyword_operator = var_keyword_operator, search_expression = var_search_expression, distance_unit = var_distance_unit, return_favorited = var_return_favorited, return_retailer = var_return_retailer, return_assets = var_return_assets, return_offers = var_return_offers, return_categories = var_return_categories, return_filters = var_return_filters, return_audiences = var_return_audiences, return_qr_code = var_return_qr_code, return_external_category_data = var_return_external_category_data, include_favorite = var_include_favorite, include_liked = var_include_liked, include_rating = var_include_ratingdata_file = "result.txt")
#' result <- api_instance$IndexedRetailerLocationDistanceSearch(var_latitude, var_longitude, var_search_range, var_start, var_limit, account_id = var_account_id, address = var_address, has_offers = var_has_offers, categories = var_categories, filters = var_filters, audiences = var_audiences, retailer_ids = var_retailer_ids, retailer_location_ids = var_retailer_location_ids, tags = var_tags, location_type = var_location_type, sort_field = var_sort_field, descending = var_descending, q = var_q, keyword = var_keyword, keyword_operator = var_keyword_operator, search_expression = var_search_expression, distance_unit = var_distance_unit, return_favorited = var_return_favorited, return_retailer = var_return_retailer, return_assets = var_return_assets, return_offers = var_return_offers, return_categories = var_return_categories, return_filters = var_return_filters, return_audiences = var_return_audiences, return_qr_code = var_return_qr_code, return_external_category_data = var_return_external_category_data, include_favorite = var_include_favorite, include_liked = var_include_liked, include_rating = var_include_rating)
#' dput(result)
#'
#'
#' ####################  IndexedRetailerLocationSearch  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user (Optional)
#' var_start <- 56 # integer | The start index for pagination (Optional)
#' var_limit <- 56 # integer | The limit for pagination (Optional)
#' var_has_offers <- "has_offers_example" # character | boolean to indicate whether to include retailer locations that have no offers (Optional)
#' var_categories <- "categories_example" # character | Comma separate list of category ids (Optional)
#' var_filters <- "filters_example" # character | Comma separated list of filter ids (Optional)
#' var_audiences <- "audiences_example" # character | Comma separated list of audience ids (Optional)
#' var_retailer_ids <- "retailer_ids_example" # character | Comma separated list of retailer ids (Optional)
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Comma separated list of retailer location ids (Optional)
#' var_tags <- "tags_example" # character | Does a full-text search on tags (Optional)
#' var_location_type <- "location_type_example" # character | Location type filter (Optional)
#' var_sort_field <- "sort_field_example" # character | The field to sort the result set on. Possible values include: RETAILER_NAME, RETAILER_LOCATION_NAME (Optional)
#' var_descending <- "descending_example" # character | Determines whether the sorted list is in descending or ascending order (Optional)
#' var_q <- "q_example" # character | This parameter is deprecated. (Optional)
#' var_keyword <- "keyword_example" # character | Search by keyword (Optional)
#' var_keyword_operator <- "keyword_operator_example" # character | Determines the operator used when there are multiple words in the 'keyword' parameter (Optional)
#' var_search_expression <- "search_expression_example" # character | Search expression to further refine results (Optional)
#' var_return_retailer <- "return_retailer_example" # character | Return retailer info (Optional)
#' var_return_assets <- "return_assets_example" # character | Return assets (Optional)
#' var_return_offers <- "return_offers_example" # character | Return offers (Optional)
#' var_return_categories <- "return_categories_example" # character | Return categories (Optional)
#' var_return_filters <- "return_filters_example" # character | Return filters (Optional)
#' var_return_audiences <- "return_audiences_example" # character | Return audiences (Optional)
#' var_return_qr_code <- "return_qr_code_example" # character | Return QR code info (Optional)
#' var_return_external_category_data <- "return_external_category_data_example" # character | Return external category data (Optional)
#' var_include_favorite <- "include_favorite_example" # character | Include favorites in response (Optional)
#' var_include_liked <- "include_liked_example" # character | Include liked flag in response (Optional)
#' var_include_rating <- "include_rating_example" # character | Include rating info in response (Optional)
#'
#' #Keyword Search Retailer Locations (Indexed)
#' api_instance <- RetailerLocationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IndexedRetailerLocationSearch(account_id = var_account_id, start = var_start, limit = var_limit, has_offers = var_has_offers, categories = var_categories, filters = var_filters, audiences = var_audiences, retailer_ids = var_retailer_ids, retailer_location_ids = var_retailer_location_ids, tags = var_tags, location_type = var_location_type, sort_field = var_sort_field, descending = var_descending, q = var_q, keyword = var_keyword, keyword_operator = var_keyword_operator, search_expression = var_search_expression, return_retailer = var_return_retailer, return_assets = var_return_assets, return_offers = var_return_offers, return_categories = var_return_categories, return_filters = var_return_filters, return_audiences = var_return_audiences, return_qr_code = var_return_qr_code, return_external_category_data = var_return_external_category_data, include_favorite = var_include_favorite, include_liked = var_include_liked, include_rating = var_include_ratingdata_file = "result.txt")
#' result <- api_instance$IndexedRetailerLocationSearch(account_id = var_account_id, start = var_start, limit = var_limit, has_offers = var_has_offers, categories = var_categories, filters = var_filters, audiences = var_audiences, retailer_ids = var_retailer_ids, retailer_location_ids = var_retailer_location_ids, tags = var_tags, location_type = var_location_type, sort_field = var_sort_field, descending = var_descending, q = var_q, keyword = var_keyword, keyword_operator = var_keyword_operator, search_expression = var_search_expression, return_retailer = var_return_retailer, return_assets = var_return_assets, return_offers = var_return_offers, return_categories = var_return_categories, return_filters = var_return_filters, return_audiences = var_return_audiences, return_qr_code = var_return_qr_code, return_external_category_data = var_return_external_category_data, include_favorite = var_include_favorite, include_liked = var_include_liked, include_rating = var_include_rating)
#' dput(result)
#'
#'
#' ####################  SearchRetailerLocations  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_q <- "q_example" # character | This parameter is deprecated. (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_retailer_ids <- "retailer_ids_example" # character | Comma separated list of retailer IDs which when passed in will filter the results to only return these retailers. (Optional)
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Comma separated list of retailer location IDs (Optional)
#' var_location_type <- "location_type_example" # character | Location type filter (Optional)
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on (Optional)
#' var_descending <- "descending_example" # character | The order to return the search results (Optional)
#' var__i <- 56 # integer | This parameter is deprecated. (Optional)
#' var_start <- 56 # integer | The record to begin the return set on (Optional)
#' var__l <- 56 # integer | This parameter is deprecated. (Optional)
#' var_limit <- 56 # integer | The number of records to return (Optional)
#' var_show_public_locations <- "show_public_locations_example" # character | Whether to include public locations in the results (Optional)
#' var_active_only <- "active_only_example" # character | Return only active results (Optional)
#' var_return_retailer <- "return_retailer_example" # character | Return retailer info (Optional)
#' var_return_assets <- "return_assets_example" # character | Return assets (Optional)
#' var_return_offers <- "return_offers_example" # character | Return offers (Optional)
#' var_return_categories <- "return_categories_example" # character | Return categories (Optional)
#' var_return_filters <- "return_filters_example" # character | Return filters (Optional)
#' var_return_audiences <- "return_audiences_example" # character | Return audiences (Optional)
#' var_return_qr_code <- "return_qr_code_example" # character | Return QR code info (Optional)
#' var_include_favorite <- "include_favorite_example" # character | Include favorites in response (Optional)
#' var_include_liked <- "include_liked_example" # character | Include liked flag in response (Optional)
#' var_include_rating <- "include_rating_example" # character | Include rating info in response (Optional)
#'
#' #Search Retailer Locations (Owned)
#' api_instance <- RetailerLocationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchRetailerLocations(device_id = var_device_id, account_id = var_account_id, q = var_q, keyword = var_keyword, retailer_ids = var_retailer_ids, retailer_location_ids = var_retailer_location_ids, location_type = var_location_type, sort_field = var_sort_field, descending = var_descending, _i = var__i, start = var_start, _l = var__l, limit = var_limit, show_public_locations = var_show_public_locations, active_only = var_active_only, return_retailer = var_return_retailer, return_assets = var_return_assets, return_offers = var_return_offers, return_categories = var_return_categories, return_filters = var_return_filters, return_audiences = var_return_audiences, return_qr_code = var_return_qr_code, include_favorite = var_include_favorite, include_liked = var_include_liked, include_rating = var_include_ratingdata_file = "result.txt")
#' result <- api_instance$SearchRetailerLocations(device_id = var_device_id, account_id = var_account_id, q = var_q, keyword = var_keyword, retailer_ids = var_retailer_ids, retailer_location_ids = var_retailer_location_ids, location_type = var_location_type, sort_field = var_sort_field, descending = var_descending, _i = var__i, start = var_start, _l = var__l, limit = var_limit, show_public_locations = var_show_public_locations, active_only = var_active_only, return_retailer = var_return_retailer, return_assets = var_return_assets, return_offers = var_return_offers, return_categories = var_return_categories, return_filters = var_return_filters, return_audiences = var_return_audiences, return_qr_code = var_return_qr_code, include_favorite = var_include_favorite, include_liked = var_include_liked, include_rating = var_include_rating)
#' dput(result)
#'
#'
#' ####################  UpdateRetailerLocations  ####################
#'
#' library(openapi)
#' var_retailer_location_id <- 56 # integer | The ID of the retailer location
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_name <- "name_example" # character | The name of the retailer location (Optional)
#' var_street_address <- "street_address_example" # character | The street address of the retailer location (Optional)
#' var_street_address2 <- "street_address2_example" # character | Additional address information (such as a suite number, floor number, building name, or PO Box) (Optional)
#' var_city <- "city_example" # character | The city of the retailer location (Optional)
#' var_state <- "state_example" # character | The state of the retailer location (Optional)
#' var_postal_code <- "postal_code_example" # character | The postal code of the retailer location (Optional)
#' var_country <- "country_example" # character | the country of the retailer location (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone number of the retailer location (Optional)
#' var_business_phone_ext <- "business_phone_ext_example" # character | The business phone extension of the retailer location (Optional)
#' var_website <- "website_example" # character | The website of the retailer location (Optional)
#' var_email <- "email_example" # character | The email of the retailer location (Optional)
#' var_internal_id <- "internal_id_example" # character | An internal identifier used by the retailer (Optional)
#' var_details_header <- "details_header_example" # character | A brief description about the retailer location (255 character limit) (Optional)
#' var_details_body <- "details_body_example" # character | A detailed description about the retailer location (Optional)
#' var_hours <- "hours_example" # character | The hours of operation (Optional)
#' var_logo <- File.new('/path/to/file') # data.frame | The retailer location logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before) (Optional)
#' var_logo_asset_id <- 56 # integer | The retailer location logo asset id (Optional)
#' var_picture1 <- File.new('/path/to/file') # data.frame | Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before) (Optional)
#' var_picture1_asset_id <- 56 # integer | An asset id (Optional)
#' var_picture2 <- File.new('/path/to/file') # data.frame | Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before) (Optional)
#' var_picture2_asset_id <- 56 # integer | An asset id (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category IDs used to filter retailer locations by categories (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Comma separated list of filter IDs used to filter retailer locations (Optional)
#' var_latitude <- 3.4 # numeric | the latituede of the retailer location (Optional)
#' var_longitude <- 3.4 # numeric | the longitude of the retailer location (Optional)
#' var_building <- "building_example" # character |  (Optional)
#' var_google_place_id <- "google_place_id_example" # character | the Google Place ID that the retailer location is associated with (Optional)
#' var_yelp_id <- "yelp_id_example" # character | the Yelp ID that the retailer location is associated with (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_payment_provider <- "payment_provider_example" # character | Specifies which payment provider Sirqul will use when making payments (Optional)
#' var_active <- "active_example" # character | Sets whether the retailer is active or inactive (hidden from consumers) (Optional)
#' var_public_location <- "public_location_example" # character | Sets whether the location is public or not (Optional)
#' var_location_type <- "location_type_example" # character | External custom type identifier (Optional)
#' var_audience_ids <- "audience_ids_example" # character | Comma separated list of audience IDs used to assign audiences to the retailer location (Optional)
#' var_audience_ids_to_add <- "audience_ids_to_add_example" # character | Comma separated list of audience IDs to add to the retailer location (Optional)
#' var_audience_ids_to_remove <- "audience_ids_to_remove_example" # character | Comma separated list of audience IDs to remove from the retailer location (Optional)
#' var_response_format <- "response_format_example" # character | The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets} (Optional)
#' var_tags <- "tags_example" # character | Custom string field for doing full-text searches (Optional)
#'
#' #Update Retailer Location
#' api_instance <- RetailerLocationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateRetailerLocations(var_retailer_location_id, device_id = var_device_id, account_id = var_account_id, name = var_name, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, website = var_website, email = var_email, internal_id = var_internal_id, details_header = var_details_header, details_body = var_details_body, hours = var_hours, logo = var_logo, logo_asset_id = var_logo_asset_id, picture1 = var_picture1, picture1_asset_id = var_picture1_asset_id, picture2 = var_picture2, picture2_asset_id = var_picture2_asset_id, category_ids = var_category_ids, filter_ids = var_filter_ids, latitude = var_latitude, longitude = var_longitude, building = var_building, google_place_id = var_google_place_id, yelp_id = var_yelp_id, meta_data = var_meta_data, payment_provider = var_payment_provider, active = var_active, public_location = var_public_location, location_type = var_location_type, audience_ids = var_audience_ids, audience_ids_to_add = var_audience_ids_to_add, audience_ids_to_remove = var_audience_ids_to_remove, response_format = var_response_format, tags = var_tagsdata_file = "result.txt")
#' result <- api_instance$UpdateRetailerLocations(var_retailer_location_id, device_id = var_device_id, account_id = var_account_id, name = var_name, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, website = var_website, email = var_email, internal_id = var_internal_id, details_header = var_details_header, details_body = var_details_body, hours = var_hours, logo = var_logo, logo_asset_id = var_logo_asset_id, picture1 = var_picture1, picture1_asset_id = var_picture1_asset_id, picture2 = var_picture2, picture2_asset_id = var_picture2_asset_id, category_ids = var_category_ids, filter_ids = var_filter_ids, latitude = var_latitude, longitude = var_longitude, building = var_building, google_place_id = var_google_place_id, yelp_id = var_yelp_id, meta_data = var_meta_data, payment_provider = var_payment_provider, active = var_active, public_location = var_public_location, location_type = var_location_type, audience_ids = var_audience_ids, audience_ids_to_add = var_audience_ids_to_add, audience_ids_to_remove = var_audience_ids_to_remove, response_format = var_response_format, tags = var_tags)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
RetailerLocationApi <- R6::R6Class(
  "RetailerLocationApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new RetailerLocationApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Retailer Location (Consumer)
    #'
    #' @param app_key the application key
    #' @param name The name of the retailer location
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param street_address (optional) The street address of the retailer location
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the retailer location
    #' @param state (optional) The state of the retailer location
    #' @param postal_code (optional) The postal code of the retailer location
    #' @param country (optional) the country of the retailer location
    #' @param business_phone (optional) The business phone
    #' @param business_phone_ext (optional) The business phone extension
    #' @param website (optional) The website of the retailer location
    #' @param email (optional) The email of the retailer location
    #' @param details_header (optional) A brief description about the retailer location (255 character limit)
    #' @param details_body (optional) A detailed description about the retailer location
    #' @param hours (optional) The hours of operation
    #' @param tags (optional) Custom string field for doing full-text searches
    #' @param logo_asset_id (optional) The retailer location logo asset id
    #' @param picture1_asset_id (optional) An asset id
    #' @param picture2_asset_id (optional) An asset id
    #' @param category_ids (optional) Comma separated list of category IDs used to filter retailer locations by categories
    #' @param filter_ids (optional) Comma separated list of filter IDs used to filter retailer locations
    #' @param meta_data (optional) External custom client defined data
    #' @param public_location (optional) Whether the location is public
    #' @param active (optional) whether the retailer location created should be active or not
    #' @param location_type (optional) External custom type identifier
    #' @param latitude (optional) The latitude to center the search on
    #' @param longitude (optional) The longitude to center the search on
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerLocationResponse
    CreateRetailerLocationConsumer = function(app_key, name, device_id = NULL, account_id = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, business_phone = NULL, business_phone_ext = NULL, website = NULL, email = NULL, details_header = NULL, details_body = NULL, hours = NULL, tags = NULL, logo_asset_id = NULL, picture1_asset_id = NULL, picture2_asset_id = NULL, category_ids = NULL, filter_ids = NULL, meta_data = NULL, public_location = NULL, active = NULL, location_type = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateRetailerLocationConsumerWithHttpInfo(app_key, name, device_id, account_id, street_address, street_address2, city, state, postal_code, country, business_phone, business_phone_ext, website, email, details_header, details_body, hours, tags, logo_asset_id, picture1_asset_id, picture2_asset_id, category_ids, filter_ids, meta_data, public_location, active, location_type, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Retailer Location (Consumer)
    #'
    #' @param app_key the application key
    #' @param name The name of the retailer location
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param street_address (optional) The street address of the retailer location
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the retailer location
    #' @param state (optional) The state of the retailer location
    #' @param postal_code (optional) The postal code of the retailer location
    #' @param country (optional) the country of the retailer location
    #' @param business_phone (optional) The business phone
    #' @param business_phone_ext (optional) The business phone extension
    #' @param website (optional) The website of the retailer location
    #' @param email (optional) The email of the retailer location
    #' @param details_header (optional) A brief description about the retailer location (255 character limit)
    #' @param details_body (optional) A detailed description about the retailer location
    #' @param hours (optional) The hours of operation
    #' @param tags (optional) Custom string field for doing full-text searches
    #' @param logo_asset_id (optional) The retailer location logo asset id
    #' @param picture1_asset_id (optional) An asset id
    #' @param picture2_asset_id (optional) An asset id
    #' @param category_ids (optional) Comma separated list of category IDs used to filter retailer locations by categories
    #' @param filter_ids (optional) Comma separated list of filter IDs used to filter retailer locations
    #' @param meta_data (optional) External custom client defined data
    #' @param public_location (optional) Whether the location is public
    #' @param active (optional) whether the retailer location created should be active or not
    #' @param location_type (optional) External custom type identifier
    #' @param latitude (optional) The latitude to center the search on
    #' @param longitude (optional) The longitude to center the search on
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerLocationResponse) with additional information such as HTTP status code, headers
    CreateRetailerLocationConsumerWithHttpInfo = function(app_key, name, device_id = NULL, account_id = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, business_phone = NULL, business_phone_ext = NULL, website = NULL, email = NULL, details_header = NULL, details_body = NULL, hours = NULL, tags = NULL, logo_asset_id = NULL, picture1_asset_id = NULL, picture2_asset_id = NULL, category_ids = NULL, filter_ids = NULL, meta_data = NULL, public_location = NULL, active = NULL, location_type = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `app_key` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `name` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `account_id` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `street_address` is not nullable")
      }

      if (!missing(`street_address2`) && is.null(`street_address2`)) {
        stop("Invalid value for `street_address2` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `street_address2` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `state` is not nullable")
      }

      if (!missing(`postal_code`) && is.null(`postal_code`)) {
        stop("Invalid value for `postal_code` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `postal_code` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `country` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `business_phone` is not nullable")
      }

      if (!missing(`business_phone_ext`) && is.null(`business_phone_ext`)) {
        stop("Invalid value for `business_phone_ext` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `business_phone_ext` is not nullable")
      }

      if (!missing(`website`) && is.null(`website`)) {
        stop("Invalid value for `website` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `website` is not nullable")
      }

      if (!missing(`email`) && is.null(`email`)) {
        stop("Invalid value for `email` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `email` is not nullable")
      }

      if (!missing(`details_header`) && is.null(`details_header`)) {
        stop("Invalid value for `details_header` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `details_header` is not nullable")
      }

      if (!missing(`details_body`) && is.null(`details_body`)) {
        stop("Invalid value for `details_body` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `details_body` is not nullable")
      }

      if (!missing(`hours`) && is.null(`hours`)) {
        stop("Invalid value for `hours` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `hours` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `tags` is not nullable")
      }

      if (!missing(`logo_asset_id`) && is.null(`logo_asset_id`)) {
        stop("Invalid value for `logo_asset_id` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `logo_asset_id` is not nullable")
      }

      if (!missing(`picture1_asset_id`) && is.null(`picture1_asset_id`)) {
        stop("Invalid value for `picture1_asset_id` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `picture1_asset_id` is not nullable")
      }

      if (!missing(`picture2_asset_id`) && is.null(`picture2_asset_id`)) {
        stop("Invalid value for `picture2_asset_id` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `picture2_asset_id` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `filter_ids` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `meta_data` is not nullable")
      }

      if (!missing(`public_location`) && is.null(`public_location`)) {
        stop("Invalid value for `public_location` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `public_location` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `active` is not nullable")
      }

      if (!missing(`location_type`) && is.null(`location_type`)) {
        stop("Invalid value for `location_type` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `location_type` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RetailerLocationApi$CreateRetailerLocationConsumer, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["name"]] <- `name`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["streetAddress2"]] <- `street_address2`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["postalCode"]] <- `postal_code`

      query_params[["country"]] <- `country`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["businessPhoneExt"]] <- `business_phone_ext`

      query_params[["website"]] <- `website`

      query_params[["email"]] <- `email`

      query_params[["detailsHeader"]] <- `details_header`

      query_params[["detailsBody"]] <- `details_body`

      query_params[["hours"]] <- `hours`

      query_params[["tags"]] <- `tags`

      query_params[["logoAssetId"]] <- `logo_asset_id`

      query_params[["picture1AssetId"]] <- `picture1_asset_id`

      query_params[["picture2AssetId"]] <- `picture2_asset_id`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["metaData"]] <- `meta_data`

      query_params[["publicLocation"]] <- `public_location`

      query_params[["active"]] <- `active`

      query_params[["locationType"]] <- `location_type`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/location/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerLocationResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Retailer Location
    #'
    #' @param retailer_id The ID of the retailer
    #' @param name The name of the retailer location
    #' @param street_address The street address of the retailer location
    #' @param city The city of the retailer location
    #' @param state The state of the retailer location
    #' @param postal_code The postal code of the retailer location
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param country (optional) the country of the retailer location
    #' @param business_phone (optional) The business phone number of the retailer location
    #' @param business_phone_ext (optional) The business phone extension
    #' @param website (optional) The website of the retailer location
    #' @param email (optional) The email of the retailer location
    #' @param internal_id (optional) An internal identifier used by the retailer
    #' @param details_header (optional) A brief description about the retailer location (255 character limit)
    #' @param details_body (optional) A detailed description about the retailer location
    #' @param hours (optional) The hours of operation
    #' @param logo (optional) The retailer location logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before)
    #' @param logo_asset_id (optional) The retailer location logo asset id
    #' @param picture1 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before)
    #' @param picture1_asset_id (optional) An asset id
    #' @param picture2 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before)
    #' @param picture2_asset_id (optional) An asset id
    #' @param category_ids (optional) Comma separated list of category IDs used to filter retailer locations by categories
    #' @param filter_ids (optional) Comma separated list of filter IDs used to filter retailer locations
    #' @param latitude (optional) the latitude of the retailer location
    #' @param longitude (optional) the longitude of the retailer location
    #' @param building (optional) 
    #' @param google_place_id (optional) the ID of the Google Place that this retailer location is assigned to
    #' @param yelp_id (optional) the Yelp ID that this retailer location is assigned to
    #' @param active (optional) whether the retailer location should be active or inactive
    #' @param public_location (optional) Sets whether the location is public or not
    #' @param location_type (optional) External custom type identifier
    #' @param audience_ids (optional) Comma separated list of audience IDs used to assign audiences to the retailer location
    #' @param audience_ids_to_add (optional) Comma separated list of audience IDs to add to the retailer location
    #' @param audience_ids_to_remove (optional) Comma separated list of audience IDs to remove from the retailer location
    #' @param response_format (optional) The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets}
    #' @param response_includes (optional) Comma separated list of response includes (e.g. RETAILER,ASSETS,OFFERS,CATEGORIES,FILTERS,AUDIENCES,QRCODE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerLocationResponse
    CreateRetailerLocations = function(retailer_id, name, street_address, city, state, postal_code, device_id = NULL, account_id = NULL, street_address2 = NULL, country = NULL, business_phone = NULL, business_phone_ext = NULL, website = NULL, email = NULL, internal_id = NULL, details_header = NULL, details_body = NULL, hours = NULL, logo = NULL, logo_asset_id = NULL, picture1 = NULL, picture1_asset_id = NULL, picture2 = NULL, picture2_asset_id = NULL, category_ids = NULL, filter_ids = NULL, latitude = NULL, longitude = NULL, building = NULL, google_place_id = NULL, yelp_id = NULL, active = NULL, public_location = NULL, location_type = NULL, audience_ids = NULL, audience_ids_to_add = NULL, audience_ids_to_remove = NULL, response_format = NULL, response_includes = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateRetailerLocationsWithHttpInfo(retailer_id, name, street_address, city, state, postal_code, device_id, account_id, street_address2, country, business_phone, business_phone_ext, website, email, internal_id, details_header, details_body, hours, logo, logo_asset_id, picture1, picture1_asset_id, picture2, picture2_asset_id, category_ids, filter_ids, latitude, longitude, building, google_place_id, yelp_id, active, public_location, location_type, audience_ids, audience_ids_to_add, audience_ids_to_remove, response_format, response_includes, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Retailer Location
    #'
    #' @param retailer_id The ID of the retailer
    #' @param name The name of the retailer location
    #' @param street_address The street address of the retailer location
    #' @param city The city of the retailer location
    #' @param state The state of the retailer location
    #' @param postal_code The postal code of the retailer location
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param country (optional) the country of the retailer location
    #' @param business_phone (optional) The business phone number of the retailer location
    #' @param business_phone_ext (optional) The business phone extension
    #' @param website (optional) The website of the retailer location
    #' @param email (optional) The email of the retailer location
    #' @param internal_id (optional) An internal identifier used by the retailer
    #' @param details_header (optional) A brief description about the retailer location (255 character limit)
    #' @param details_body (optional) A detailed description about the retailer location
    #' @param hours (optional) The hours of operation
    #' @param logo (optional) The retailer location logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before)
    #' @param logo_asset_id (optional) The retailer location logo asset id
    #' @param picture1 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before)
    #' @param picture1_asset_id (optional) An asset id
    #' @param picture2 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before)
    #' @param picture2_asset_id (optional) An asset id
    #' @param category_ids (optional) Comma separated list of category IDs used to filter retailer locations by categories
    #' @param filter_ids (optional) Comma separated list of filter IDs used to filter retailer locations
    #' @param latitude (optional) the latitude of the retailer location
    #' @param longitude (optional) the longitude of the retailer location
    #' @param building (optional) 
    #' @param google_place_id (optional) the ID of the Google Place that this retailer location is assigned to
    #' @param yelp_id (optional) the Yelp ID that this retailer location is assigned to
    #' @param active (optional) whether the retailer location should be active or inactive
    #' @param public_location (optional) Sets whether the location is public or not
    #' @param location_type (optional) External custom type identifier
    #' @param audience_ids (optional) Comma separated list of audience IDs used to assign audiences to the retailer location
    #' @param audience_ids_to_add (optional) Comma separated list of audience IDs to add to the retailer location
    #' @param audience_ids_to_remove (optional) Comma separated list of audience IDs to remove from the retailer location
    #' @param response_format (optional) The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets}
    #' @param response_includes (optional) Comma separated list of response includes (e.g. RETAILER,ASSETS,OFFERS,CATEGORIES,FILTERS,AUDIENCES,QRCODE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerLocationResponse) with additional information such as HTTP status code, headers
    CreateRetailerLocationsWithHttpInfo = function(retailer_id, name, street_address, city, state, postal_code, device_id = NULL, account_id = NULL, street_address2 = NULL, country = NULL, business_phone = NULL, business_phone_ext = NULL, website = NULL, email = NULL, internal_id = NULL, details_header = NULL, details_body = NULL, hours = NULL, logo = NULL, logo_asset_id = NULL, picture1 = NULL, picture1_asset_id = NULL, picture2 = NULL, picture2_asset_id = NULL, category_ids = NULL, filter_ids = NULL, latitude = NULL, longitude = NULL, building = NULL, google_place_id = NULL, yelp_id = NULL, active = NULL, public_location = NULL, location_type = NULL, audience_ids = NULL, audience_ids_to_add = NULL, audience_ids_to_remove = NULL, response_format = NULL, response_includes = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`retailer_id`)) {
        stop("Missing required parameter `retailer_id`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (missing(`street_address`)) {
        stop("Missing required parameter `street_address`.")
      }

      if (missing(`city`)) {
        stop("Missing required parameter `city`.")
      }

      if (missing(`state`)) {
        stop("Missing required parameter `state`.")
      }

      if (missing(`postal_code`)) {
        stop("Missing required parameter `postal_code`.")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling RetailerLocationApi$CreateRetailerLocations, `retailer_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling RetailerLocationApi$CreateRetailerLocations, `name` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling RetailerLocationApi$CreateRetailerLocations, `street_address` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling RetailerLocationApi$CreateRetailerLocations, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling RetailerLocationApi$CreateRetailerLocations, `state` is not nullable")
      }

      if (!missing(`postal_code`) && is.null(`postal_code`)) {
        stop("Invalid value for `postal_code` when calling RetailerLocationApi$CreateRetailerLocations, `postal_code` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerLocationApi$CreateRetailerLocations, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerLocationApi$CreateRetailerLocations, `account_id` is not nullable")
      }

      if (!missing(`street_address2`) && is.null(`street_address2`)) {
        stop("Invalid value for `street_address2` when calling RetailerLocationApi$CreateRetailerLocations, `street_address2` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling RetailerLocationApi$CreateRetailerLocations, `country` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling RetailerLocationApi$CreateRetailerLocations, `business_phone` is not nullable")
      }

      if (!missing(`business_phone_ext`) && is.null(`business_phone_ext`)) {
        stop("Invalid value for `business_phone_ext` when calling RetailerLocationApi$CreateRetailerLocations, `business_phone_ext` is not nullable")
      }

      if (!missing(`website`) && is.null(`website`)) {
        stop("Invalid value for `website` when calling RetailerLocationApi$CreateRetailerLocations, `website` is not nullable")
      }

      if (!missing(`email`) && is.null(`email`)) {
        stop("Invalid value for `email` when calling RetailerLocationApi$CreateRetailerLocations, `email` is not nullable")
      }

      if (!missing(`internal_id`) && is.null(`internal_id`)) {
        stop("Invalid value for `internal_id` when calling RetailerLocationApi$CreateRetailerLocations, `internal_id` is not nullable")
      }

      if (!missing(`details_header`) && is.null(`details_header`)) {
        stop("Invalid value for `details_header` when calling RetailerLocationApi$CreateRetailerLocations, `details_header` is not nullable")
      }

      if (!missing(`details_body`) && is.null(`details_body`)) {
        stop("Invalid value for `details_body` when calling RetailerLocationApi$CreateRetailerLocations, `details_body` is not nullable")
      }

      if (!missing(`hours`) && is.null(`hours`)) {
        stop("Invalid value for `hours` when calling RetailerLocationApi$CreateRetailerLocations, `hours` is not nullable")
      }

      if (!missing(`logo`) && is.null(`logo`)) {
        stop("Invalid value for `logo` when calling RetailerLocationApi$CreateRetailerLocations, `logo` is not nullable")
      }

      if (!missing(`logo_asset_id`) && is.null(`logo_asset_id`)) {
        stop("Invalid value for `logo_asset_id` when calling RetailerLocationApi$CreateRetailerLocations, `logo_asset_id` is not nullable")
      }

      if (!missing(`picture1`) && is.null(`picture1`)) {
        stop("Invalid value for `picture1` when calling RetailerLocationApi$CreateRetailerLocations, `picture1` is not nullable")
      }

      if (!missing(`picture1_asset_id`) && is.null(`picture1_asset_id`)) {
        stop("Invalid value for `picture1_asset_id` when calling RetailerLocationApi$CreateRetailerLocations, `picture1_asset_id` is not nullable")
      }

      if (!missing(`picture2`) && is.null(`picture2`)) {
        stop("Invalid value for `picture2` when calling RetailerLocationApi$CreateRetailerLocations, `picture2` is not nullable")
      }

      if (!missing(`picture2_asset_id`) && is.null(`picture2_asset_id`)) {
        stop("Invalid value for `picture2_asset_id` when calling RetailerLocationApi$CreateRetailerLocations, `picture2_asset_id` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling RetailerLocationApi$CreateRetailerLocations, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling RetailerLocationApi$CreateRetailerLocations, `filter_ids` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RetailerLocationApi$CreateRetailerLocations, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RetailerLocationApi$CreateRetailerLocations, `longitude` is not nullable")
      }

      if (!missing(`building`) && is.null(`building`)) {
        stop("Invalid value for `building` when calling RetailerLocationApi$CreateRetailerLocations, `building` is not nullable")
      }

      if (!missing(`google_place_id`) && is.null(`google_place_id`)) {
        stop("Invalid value for `google_place_id` when calling RetailerLocationApi$CreateRetailerLocations, `google_place_id` is not nullable")
      }

      if (!missing(`yelp_id`) && is.null(`yelp_id`)) {
        stop("Invalid value for `yelp_id` when calling RetailerLocationApi$CreateRetailerLocations, `yelp_id` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling RetailerLocationApi$CreateRetailerLocations, `active` is not nullable")
      }

      if (!missing(`public_location`) && is.null(`public_location`)) {
        stop("Invalid value for `public_location` when calling RetailerLocationApi$CreateRetailerLocations, `public_location` is not nullable")
      }

      if (!missing(`location_type`) && is.null(`location_type`)) {
        stop("Invalid value for `location_type` when calling RetailerLocationApi$CreateRetailerLocations, `location_type` is not nullable")
      }

      if (!missing(`audience_ids`) && is.null(`audience_ids`)) {
        stop("Invalid value for `audience_ids` when calling RetailerLocationApi$CreateRetailerLocations, `audience_ids` is not nullable")
      }

      if (!missing(`audience_ids_to_add`) && is.null(`audience_ids_to_add`)) {
        stop("Invalid value for `audience_ids_to_add` when calling RetailerLocationApi$CreateRetailerLocations, `audience_ids_to_add` is not nullable")
      }

      if (!missing(`audience_ids_to_remove`) && is.null(`audience_ids_to_remove`)) {
        stop("Invalid value for `audience_ids_to_remove` when calling RetailerLocationApi$CreateRetailerLocations, `audience_ids_to_remove` is not nullable")
      }

      if (!missing(`response_format`) && is.null(`response_format`)) {
        stop("Invalid value for `response_format` when calling RetailerLocationApi$CreateRetailerLocations, `response_format` is not nullable")
      }

      if (!missing(`response_includes`) && is.null(`response_includes`)) {
        stop("Invalid value for `response_includes` when calling RetailerLocationApi$CreateRetailerLocations, `response_includes` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["name"]] <- `name`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["streetAddress2"]] <- `street_address2`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["postalCode"]] <- `postal_code`

      query_params[["country"]] <- `country`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["businessPhoneExt"]] <- `business_phone_ext`

      query_params[["website"]] <- `website`

      query_params[["email"]] <- `email`

      query_params[["internalId"]] <- `internal_id`

      query_params[["detailsHeader"]] <- `details_header`

      query_params[["detailsBody"]] <- `details_body`

      query_params[["hours"]] <- `hours`

      query_params[["logo"]] <- `logo`

      query_params[["logoAssetId"]] <- `logo_asset_id`

      query_params[["picture1"]] <- `picture1`

      query_params[["picture1AssetId"]] <- `picture1_asset_id`

      query_params[["picture2"]] <- `picture2`

      query_params[["picture2AssetId"]] <- `picture2_asset_id`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["building"]] <- `building`

      query_params[["googlePlaceId"]] <- `google_place_id`

      query_params[["yelpId"]] <- `yelp_id`

      query_params[["active"]] <- `active`

      query_params[["publicLocation"]] <- `public_location`

      query_params[["locationType"]] <- `location_type`

      query_params[["audienceIds"]] <- `audience_ids`

      query_params[["audienceIdsToAdd"]] <- `audience_ids_to_add`

      query_params[["audienceIdsToRemove"]] <- `audience_ids_to_remove`

      if (!is.null(`response_format`) && !(`response_format` %in% c("HTML", "XML", "JSON", "CSV"))) {
        stop("Invalid value for response_format when calling RetailerLocationApi$CreateRetailerLocations. Must be [HTML, XML, JSON, CSV].")
      }
      query_params[["responseFormat"]] <- `response_format`

      query_params[["responseIncludes"]] <- `response_includes`

      local_var_url_path <- "/retailer/location/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerLocationResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Retailer Location
    #'
    #' @param device_id (optional) the device id
    #' @param account_id (optional) the id of the logged in user
    #' @param retailer_location_id (optional) the id of the retailer location to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteRetailerLocation = function(device_id = NULL, account_id = NULL, retailer_location_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteRetailerLocationWithHttpInfo(device_id, account_id, retailer_location_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Retailer Location
    #'
    #' @param device_id (optional) the device id
    #' @param account_id (optional) the id of the logged in user
    #' @param retailer_location_id (optional) the id of the retailer location to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteRetailerLocationWithHttpInfo = function(device_id = NULL, account_id = NULL, retailer_location_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerLocationApi$DeleteRetailerLocation, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerLocationApi$DeleteRetailerLocation, `account_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling RetailerLocationApi$DeleteRetailerLocation, `retailer_location_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      local_var_url_path <- "/retailer/location/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Retailer Location
    #'
    #' @param retailer_location_id The ID of the retailer location
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param retailer_location_token (optional) the unique token of the retailer location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerLocationResponse
    GetRetailerLocation = function(retailer_location_id, device_id = NULL, account_id = NULL, retailer_location_token = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetRetailerLocationWithHttpInfo(retailer_location_id, device_id, account_id, retailer_location_token, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Retailer Location
    #'
    #' @param retailer_location_id The ID of the retailer location
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param retailer_location_token (optional) the unique token of the retailer location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerLocationResponse) with additional information such as HTTP status code, headers
    GetRetailerLocationWithHttpInfo = function(retailer_location_id, device_id = NULL, account_id = NULL, retailer_location_token = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`retailer_location_id`)) {
        stop("Missing required parameter `retailer_location_id`.")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling RetailerLocationApi$GetRetailerLocation, `retailer_location_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerLocationApi$GetRetailerLocation, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerLocationApi$GetRetailerLocation, `account_id` is not nullable")
      }

      if (!missing(`retailer_location_token`) && is.null(`retailer_location_token`)) {
        stop("Invalid value for `retailer_location_token` when calling RetailerLocationApi$GetRetailerLocation, `retailer_location_token` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["retailerLocationToken"]] <- `retailer_location_token`

      local_var_url_path <- "/retailer/location/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerLocationResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Retailer Location (Consumer)
    #'
    #' @param retailer_location_id The retailer location id
    #' @param device_id (optional) The device id for returning account information (i.e. favorites)
    #' @param account_id (optional) The account id for returning account information (i.e. favorites)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerLocationResponse
    GetRetailerLocationConsumer = function(retailer_location_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetRetailerLocationConsumerWithHttpInfo(retailer_location_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Retailer Location (Consumer)
    #'
    #' @param retailer_location_id The retailer location id
    #' @param device_id (optional) The device id for returning account information (i.e. favorites)
    #' @param account_id (optional) The account id for returning account information (i.e. favorites)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerLocationResponse) with additional information such as HTTP status code, headers
    GetRetailerLocationConsumerWithHttpInfo = function(retailer_location_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`retailer_location_id`)) {
        stop("Missing required parameter `retailer_location_id`.")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling RetailerLocationApi$GetRetailerLocationConsumer, `retailer_location_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerLocationApi$GetRetailerLocationConsumer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerLocationApi$GetRetailerLocationConsumer, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      local_var_url_path <- "/location/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerLocationResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Distance Search Retailer Locations (Indexed)
    #'
    #' @param latitude The latitude to center the search on
    #' @param longitude The longitude to center the search on
    #' @param search_range The search range in the distanceUnit specified; default is MILES.
    #' @param start The start index for pagination
    #' @param limit The limit for pagination
    #' @param account_id (optional) The account id of the user
    #' @param address (optional) Used to return results using this address as the center
    #' @param has_offers (optional) boolean to indicate whether to include retailer locations that have no offers
    #' @param categories (optional) Comma separate list of category ids
    #' @param filters (optional) Comma separated list of filter ids
    #' @param audiences (optional) Comma separated list of audience ids
    #' @param retailer_ids (optional) Comma separated list of retailer ids
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids
    #' @param tags (optional) Does a full-text search on tags
    #' @param location_type (optional) Location type filter
    #' @param sort_field (optional) The field to sort the result set on. Possible values include: DISTANCE, RETAILER_NAME, RETAILER_LOCATION_NAME
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) Search by keyword
    #' @param keyword_operator (optional) Determines the operator used when there are multiple words in the 'keyword' parameter
    #' @param search_expression (optional) Search expression to further refine results
    #' @param distance_unit (optional) Unit of distance
    #' @param return_favorited (optional) (deprecated) return favorited flag
    #' @param return_retailer (optional) Return retailer info
    #' @param return_assets (optional) Return assets
    #' @param return_offers (optional) Return offers
    #' @param return_categories (optional) Return categories
    #' @param return_filters (optional) Return filters
    #' @param return_audiences (optional) Return audiences
    #' @param return_qr_code (optional) Return QR code info
    #' @param return_external_category_data (optional) Return external category data
    #' @param include_favorite (optional) Include favorites in response
    #' @param include_liked (optional) Include liked flag in response
    #' @param include_rating (optional) Include rating info in response
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[RetailerLocationResponse]
    IndexedRetailerLocationDistanceSearch = function(latitude, longitude, search_range, start, limit, account_id = NULL, address = NULL, has_offers = NULL, categories = NULL, filters = NULL, audiences = NULL, retailer_ids = NULL, retailer_location_ids = NULL, tags = NULL, location_type = NULL, sort_field = NULL, descending = NULL, q = NULL, keyword = NULL, keyword_operator = NULL, search_expression = NULL, distance_unit = NULL, return_favorited = NULL, return_retailer = NULL, return_assets = NULL, return_offers = NULL, return_categories = NULL, return_filters = NULL, return_audiences = NULL, return_qr_code = NULL, return_external_category_data = NULL, include_favorite = NULL, include_liked = NULL, include_rating = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$IndexedRetailerLocationDistanceSearchWithHttpInfo(latitude, longitude, search_range, start, limit, account_id, address, has_offers, categories, filters, audiences, retailer_ids, retailer_location_ids, tags, location_type, sort_field, descending, q, keyword, keyword_operator, search_expression, distance_unit, return_favorited, return_retailer, return_assets, return_offers, return_categories, return_filters, return_audiences, return_qr_code, return_external_category_data, include_favorite, include_liked, include_rating, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Distance Search Retailer Locations (Indexed)
    #'
    #' @param latitude The latitude to center the search on
    #' @param longitude The longitude to center the search on
    #' @param search_range The search range in the distanceUnit specified; default is MILES.
    #' @param start The start index for pagination
    #' @param limit The limit for pagination
    #' @param account_id (optional) The account id of the user
    #' @param address (optional) Used to return results using this address as the center
    #' @param has_offers (optional) boolean to indicate whether to include retailer locations that have no offers
    #' @param categories (optional) Comma separate list of category ids
    #' @param filters (optional) Comma separated list of filter ids
    #' @param audiences (optional) Comma separated list of audience ids
    #' @param retailer_ids (optional) Comma separated list of retailer ids
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids
    #' @param tags (optional) Does a full-text search on tags
    #' @param location_type (optional) Location type filter
    #' @param sort_field (optional) The field to sort the result set on. Possible values include: DISTANCE, RETAILER_NAME, RETAILER_LOCATION_NAME
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) Search by keyword
    #' @param keyword_operator (optional) Determines the operator used when there are multiple words in the 'keyword' parameter
    #' @param search_expression (optional) Search expression to further refine results
    #' @param distance_unit (optional) Unit of distance
    #' @param return_favorited (optional) (deprecated) return favorited flag
    #' @param return_retailer (optional) Return retailer info
    #' @param return_assets (optional) Return assets
    #' @param return_offers (optional) Return offers
    #' @param return_categories (optional) Return categories
    #' @param return_filters (optional) Return filters
    #' @param return_audiences (optional) Return audiences
    #' @param return_qr_code (optional) Return QR code info
    #' @param return_external_category_data (optional) Return external category data
    #' @param include_favorite (optional) Include favorites in response
    #' @param include_liked (optional) Include liked flag in response
    #' @param include_rating (optional) Include rating info in response
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[RetailerLocationResponse]) with additional information such as HTTP status code, headers
    IndexedRetailerLocationDistanceSearchWithHttpInfo = function(latitude, longitude, search_range, start, limit, account_id = NULL, address = NULL, has_offers = NULL, categories = NULL, filters = NULL, audiences = NULL, retailer_ids = NULL, retailer_location_ids = NULL, tags = NULL, location_type = NULL, sort_field = NULL, descending = NULL, q = NULL, keyword = NULL, keyword_operator = NULL, search_expression = NULL, distance_unit = NULL, return_favorited = NULL, return_retailer = NULL, return_assets = NULL, return_offers = NULL, return_categories = NULL, return_filters = NULL, return_audiences = NULL, return_qr_code = NULL, return_external_category_data = NULL, include_favorite = NULL, include_liked = NULL, include_rating = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`latitude`)) {
        stop("Missing required parameter `latitude`.")
      }

      if (missing(`longitude`)) {
        stop("Missing required parameter `longitude`.")
      }

      if (missing(`search_range`)) {
        stop("Missing required parameter `search_range`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `longitude` is not nullable")
      }

      if (!missing(`search_range`) && is.null(`search_range`)) {
        stop("Invalid value for `search_range` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `search_range` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `limit` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `account_id` is not nullable")
      }

      if (!missing(`address`) && is.null(`address`)) {
        stop("Invalid value for `address` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `address` is not nullable")
      }

      if (!missing(`has_offers`) && is.null(`has_offers`)) {
        stop("Invalid value for `has_offers` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `has_offers` is not nullable")
      }

      if (!missing(`categories`) && is.null(`categories`)) {
        stop("Invalid value for `categories` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `categories` is not nullable")
      }

      if (!missing(`filters`) && is.null(`filters`)) {
        stop("Invalid value for `filters` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `filters` is not nullable")
      }

      if (!missing(`audiences`) && is.null(`audiences`)) {
        stop("Invalid value for `audiences` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `audiences` is not nullable")
      }

      if (!missing(`retailer_ids`) && is.null(`retailer_ids`)) {
        stop("Invalid value for `retailer_ids` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `retailer_ids` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `retailer_location_ids` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `tags` is not nullable")
      }

      if (!missing(`location_type`) && is.null(`location_type`)) {
        stop("Invalid value for `location_type` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `location_type` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `descending` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `keyword` is not nullable")
      }

      if (!missing(`keyword_operator`) && is.null(`keyword_operator`)) {
        stop("Invalid value for `keyword_operator` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `keyword_operator` is not nullable")
      }

      if (!missing(`search_expression`) && is.null(`search_expression`)) {
        stop("Invalid value for `search_expression` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `search_expression` is not nullable")
      }

      if (!missing(`distance_unit`) && is.null(`distance_unit`)) {
        stop("Invalid value for `distance_unit` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `distance_unit` is not nullable")
      }

      if (!missing(`return_favorited`) && is.null(`return_favorited`)) {
        stop("Invalid value for `return_favorited` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `return_favorited` is not nullable")
      }

      if (!missing(`return_retailer`) && is.null(`return_retailer`)) {
        stop("Invalid value for `return_retailer` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `return_retailer` is not nullable")
      }

      if (!missing(`return_assets`) && is.null(`return_assets`)) {
        stop("Invalid value for `return_assets` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `return_assets` is not nullable")
      }

      if (!missing(`return_offers`) && is.null(`return_offers`)) {
        stop("Invalid value for `return_offers` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `return_offers` is not nullable")
      }

      if (!missing(`return_categories`) && is.null(`return_categories`)) {
        stop("Invalid value for `return_categories` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `return_categories` is not nullable")
      }

      if (!missing(`return_filters`) && is.null(`return_filters`)) {
        stop("Invalid value for `return_filters` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `return_filters` is not nullable")
      }

      if (!missing(`return_audiences`) && is.null(`return_audiences`)) {
        stop("Invalid value for `return_audiences` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `return_audiences` is not nullable")
      }

      if (!missing(`return_qr_code`) && is.null(`return_qr_code`)) {
        stop("Invalid value for `return_qr_code` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `return_qr_code` is not nullable")
      }

      if (!missing(`return_external_category_data`) && is.null(`return_external_category_data`)) {
        stop("Invalid value for `return_external_category_data` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `return_external_category_data` is not nullable")
      }

      if (!missing(`include_favorite`) && is.null(`include_favorite`)) {
        stop("Invalid value for `include_favorite` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `include_favorite` is not nullable")
      }

      if (!missing(`include_liked`) && is.null(`include_liked`)) {
        stop("Invalid value for `include_liked` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `include_liked` is not nullable")
      }

      if (!missing(`include_rating`) && is.null(`include_rating`)) {
        stop("Invalid value for `include_rating` when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch, `include_rating` is not nullable")
      }

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["accountId"]] <- `account_id`

      query_params[["address"]] <- `address`

      query_params[["searchRange"]] <- `search_range`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["hasOffers"]] <- `has_offers`

      query_params[["categories"]] <- `categories`

      query_params[["filters"]] <- `filters`

      query_params[["audiences"]] <- `audiences`

      query_params[["retailerIds"]] <- `retailer_ids`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["tags"]] <- `tags`

      query_params[["locationType"]] <- `location_type`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["keywordOperator"]] <- `keyword_operator`

      query_params[["searchExpression"]] <- `search_expression`

      if (!is.null(`distance_unit`) && !(`distance_unit` %in% c("MILES", "KILOMETERS"))) {
        stop("Invalid value for distance_unit when calling RetailerLocationApi$IndexedRetailerLocationDistanceSearch. Must be [MILES, KILOMETERS].")
      }
      query_params[["distanceUnit"]] <- `distance_unit`

      query_params[["returnFavorited"]] <- `return_favorited`

      query_params[["returnRetailer"]] <- `return_retailer`

      query_params[["returnAssets"]] <- `return_assets`

      query_params[["returnOffers"]] <- `return_offers`

      query_params[["returnCategories"]] <- `return_categories`

      query_params[["returnFilters"]] <- `return_filters`

      query_params[["returnAudiences"]] <- `return_audiences`

      query_params[["returnQrCode"]] <- `return_qr_code`

      query_params[["returnExternalCategoryData"]] <- `return_external_category_data`

      query_params[["includeFavorite"]] <- `include_favorite`

      query_params[["includeLiked"]] <- `include_liked`

      query_params[["includeRating"]] <- `include_rating`

      local_var_url_path <- "/retailer/location/idistancesearch"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[RetailerLocationResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Keyword Search Retailer Locations (Indexed)
    #'
    #' @param account_id (optional) The account id of the user
    #' @param start (optional) The start index for pagination
    #' @param limit (optional) The limit for pagination
    #' @param has_offers (optional) boolean to indicate whether to include retailer locations that have no offers
    #' @param categories (optional) Comma separate list of category ids
    #' @param filters (optional) Comma separated list of filter ids
    #' @param audiences (optional) Comma separated list of audience ids
    #' @param retailer_ids (optional) Comma separated list of retailer ids
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids
    #' @param tags (optional) Does a full-text search on tags
    #' @param location_type (optional) Location type filter
    #' @param sort_field (optional) The field to sort the result set on. Possible values include: RETAILER_NAME, RETAILER_LOCATION_NAME
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) Search by keyword
    #' @param keyword_operator (optional) Determines the operator used when there are multiple words in the 'keyword' parameter
    #' @param search_expression (optional) Search expression to further refine results
    #' @param return_retailer (optional) Return retailer info
    #' @param return_assets (optional) Return assets
    #' @param return_offers (optional) Return offers
    #' @param return_categories (optional) Return categories
    #' @param return_filters (optional) Return filters
    #' @param return_audiences (optional) Return audiences
    #' @param return_qr_code (optional) Return QR code info
    #' @param return_external_category_data (optional) Return external category data
    #' @param include_favorite (optional) Include favorites in response
    #' @param include_liked (optional) Include liked flag in response
    #' @param include_rating (optional) Include rating info in response
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[RetailerLocationResponse]
    IndexedRetailerLocationSearch = function(account_id = NULL, start = NULL, limit = NULL, has_offers = NULL, categories = NULL, filters = NULL, audiences = NULL, retailer_ids = NULL, retailer_location_ids = NULL, tags = NULL, location_type = NULL, sort_field = NULL, descending = NULL, q = NULL, keyword = NULL, keyword_operator = NULL, search_expression = NULL, return_retailer = NULL, return_assets = NULL, return_offers = NULL, return_categories = NULL, return_filters = NULL, return_audiences = NULL, return_qr_code = NULL, return_external_category_data = NULL, include_favorite = NULL, include_liked = NULL, include_rating = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$IndexedRetailerLocationSearchWithHttpInfo(account_id, start, limit, has_offers, categories, filters, audiences, retailer_ids, retailer_location_ids, tags, location_type, sort_field, descending, q, keyword, keyword_operator, search_expression, return_retailer, return_assets, return_offers, return_categories, return_filters, return_audiences, return_qr_code, return_external_category_data, include_favorite, include_liked, include_rating, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Keyword Search Retailer Locations (Indexed)
    #'
    #' @param account_id (optional) The account id of the user
    #' @param start (optional) The start index for pagination
    #' @param limit (optional) The limit for pagination
    #' @param has_offers (optional) boolean to indicate whether to include retailer locations that have no offers
    #' @param categories (optional) Comma separate list of category ids
    #' @param filters (optional) Comma separated list of filter ids
    #' @param audiences (optional) Comma separated list of audience ids
    #' @param retailer_ids (optional) Comma separated list of retailer ids
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids
    #' @param tags (optional) Does a full-text search on tags
    #' @param location_type (optional) Location type filter
    #' @param sort_field (optional) The field to sort the result set on. Possible values include: RETAILER_NAME, RETAILER_LOCATION_NAME
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) Search by keyword
    #' @param keyword_operator (optional) Determines the operator used when there are multiple words in the 'keyword' parameter
    #' @param search_expression (optional) Search expression to further refine results
    #' @param return_retailer (optional) Return retailer info
    #' @param return_assets (optional) Return assets
    #' @param return_offers (optional) Return offers
    #' @param return_categories (optional) Return categories
    #' @param return_filters (optional) Return filters
    #' @param return_audiences (optional) Return audiences
    #' @param return_qr_code (optional) Return QR code info
    #' @param return_external_category_data (optional) Return external category data
    #' @param include_favorite (optional) Include favorites in response
    #' @param include_liked (optional) Include liked flag in response
    #' @param include_rating (optional) Include rating info in response
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[RetailerLocationResponse]) with additional information such as HTTP status code, headers
    IndexedRetailerLocationSearchWithHttpInfo = function(account_id = NULL, start = NULL, limit = NULL, has_offers = NULL, categories = NULL, filters = NULL, audiences = NULL, retailer_ids = NULL, retailer_location_ids = NULL, tags = NULL, location_type = NULL, sort_field = NULL, descending = NULL, q = NULL, keyword = NULL, keyword_operator = NULL, search_expression = NULL, return_retailer = NULL, return_assets = NULL, return_offers = NULL, return_categories = NULL, return_filters = NULL, return_audiences = NULL, return_qr_code = NULL, return_external_category_data = NULL, include_favorite = NULL, include_liked = NULL, include_rating = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `account_id` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `limit` is not nullable")
      }

      if (!missing(`has_offers`) && is.null(`has_offers`)) {
        stop("Invalid value for `has_offers` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `has_offers` is not nullable")
      }

      if (!missing(`categories`) && is.null(`categories`)) {
        stop("Invalid value for `categories` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `categories` is not nullable")
      }

      if (!missing(`filters`) && is.null(`filters`)) {
        stop("Invalid value for `filters` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `filters` is not nullable")
      }

      if (!missing(`audiences`) && is.null(`audiences`)) {
        stop("Invalid value for `audiences` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `audiences` is not nullable")
      }

      if (!missing(`retailer_ids`) && is.null(`retailer_ids`)) {
        stop("Invalid value for `retailer_ids` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `retailer_ids` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `retailer_location_ids` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `tags` is not nullable")
      }

      if (!missing(`location_type`) && is.null(`location_type`)) {
        stop("Invalid value for `location_type` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `location_type` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `descending` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `keyword` is not nullable")
      }

      if (!missing(`keyword_operator`) && is.null(`keyword_operator`)) {
        stop("Invalid value for `keyword_operator` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `keyword_operator` is not nullable")
      }

      if (!missing(`search_expression`) && is.null(`search_expression`)) {
        stop("Invalid value for `search_expression` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `search_expression` is not nullable")
      }

      if (!missing(`return_retailer`) && is.null(`return_retailer`)) {
        stop("Invalid value for `return_retailer` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `return_retailer` is not nullable")
      }

      if (!missing(`return_assets`) && is.null(`return_assets`)) {
        stop("Invalid value for `return_assets` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `return_assets` is not nullable")
      }

      if (!missing(`return_offers`) && is.null(`return_offers`)) {
        stop("Invalid value for `return_offers` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `return_offers` is not nullable")
      }

      if (!missing(`return_categories`) && is.null(`return_categories`)) {
        stop("Invalid value for `return_categories` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `return_categories` is not nullable")
      }

      if (!missing(`return_filters`) && is.null(`return_filters`)) {
        stop("Invalid value for `return_filters` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `return_filters` is not nullable")
      }

      if (!missing(`return_audiences`) && is.null(`return_audiences`)) {
        stop("Invalid value for `return_audiences` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `return_audiences` is not nullable")
      }

      if (!missing(`return_qr_code`) && is.null(`return_qr_code`)) {
        stop("Invalid value for `return_qr_code` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `return_qr_code` is not nullable")
      }

      if (!missing(`return_external_category_data`) && is.null(`return_external_category_data`)) {
        stop("Invalid value for `return_external_category_data` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `return_external_category_data` is not nullable")
      }

      if (!missing(`include_favorite`) && is.null(`include_favorite`)) {
        stop("Invalid value for `include_favorite` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `include_favorite` is not nullable")
      }

      if (!missing(`include_liked`) && is.null(`include_liked`)) {
        stop("Invalid value for `include_liked` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `include_liked` is not nullable")
      }

      if (!missing(`include_rating`) && is.null(`include_rating`)) {
        stop("Invalid value for `include_rating` when calling RetailerLocationApi$IndexedRetailerLocationSearch, `include_rating` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["hasOffers"]] <- `has_offers`

      query_params[["categories"]] <- `categories`

      query_params[["filters"]] <- `filters`

      query_params[["audiences"]] <- `audiences`

      query_params[["retailerIds"]] <- `retailer_ids`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["tags"]] <- `tags`

      query_params[["locationType"]] <- `location_type`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["keywordOperator"]] <- `keyword_operator`

      query_params[["searchExpression"]] <- `search_expression`

      query_params[["returnRetailer"]] <- `return_retailer`

      query_params[["returnAssets"]] <- `return_assets`

      query_params[["returnOffers"]] <- `return_offers`

      query_params[["returnCategories"]] <- `return_categories`

      query_params[["returnFilters"]] <- `return_filters`

      query_params[["returnAudiences"]] <- `return_audiences`

      query_params[["returnQrCode"]] <- `return_qr_code`

      query_params[["returnExternalCategoryData"]] <- `return_external_category_data`

      query_params[["includeFavorite"]] <- `include_favorite`

      query_params[["includeLiked"]] <- `include_liked`

      query_params[["includeRating"]] <- `include_rating`

      local_var_url_path <- "/retailer/location/isearch"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[RetailerLocationResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Retailer Locations (Owned)
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) The keyword used to search
    #' @param retailer_ids (optional) Comma separated list of retailer IDs which when passed in will filter the results to only return these retailers.
    #' @param retailer_location_ids (optional) Comma separated list of retailer location IDs
    #' @param location_type (optional) Location type filter
    #' @param sort_field (optional) The column to sort the search on
    #' @param descending (optional) The order to return the search results
    #' @param _i (optional) This parameter is deprecated.
    #' @param start (optional) The record to begin the return set on
    #' @param _l (optional) This parameter is deprecated.
    #' @param limit (optional) The number of records to return
    #' @param show_public_locations (optional) Whether to include public locations in the results
    #' @param active_only (optional) Return only active results
    #' @param return_retailer (optional) Return retailer info
    #' @param return_assets (optional) Return assets
    #' @param return_offers (optional) Return offers
    #' @param return_categories (optional) Return categories
    #' @param return_filters (optional) Return filters
    #' @param return_audiences (optional) Return audiences
    #' @param return_qr_code (optional) Return QR code info
    #' @param include_favorite (optional) Include favorites in response
    #' @param include_liked (optional) Include liked flag in response
    #' @param include_rating (optional) Include rating info in response
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[RetailerLocationResponse]
    SearchRetailerLocations = function(device_id = NULL, account_id = NULL, q = NULL, keyword = NULL, retailer_ids = NULL, retailer_location_ids = NULL, location_type = NULL, sort_field = NULL, descending = NULL, _i = NULL, start = NULL, _l = NULL, limit = NULL, show_public_locations = NULL, active_only = NULL, return_retailer = NULL, return_assets = NULL, return_offers = NULL, return_categories = NULL, return_filters = NULL, return_audiences = NULL, return_qr_code = NULL, include_favorite = NULL, include_liked = NULL, include_rating = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchRetailerLocationsWithHttpInfo(device_id, account_id, q, keyword, retailer_ids, retailer_location_ids, location_type, sort_field, descending, _i, start, _l, limit, show_public_locations, active_only, return_retailer, return_assets, return_offers, return_categories, return_filters, return_audiences, return_qr_code, include_favorite, include_liked, include_rating, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Retailer Locations (Owned)
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) The keyword used to search
    #' @param retailer_ids (optional) Comma separated list of retailer IDs which when passed in will filter the results to only return these retailers.
    #' @param retailer_location_ids (optional) Comma separated list of retailer location IDs
    #' @param location_type (optional) Location type filter
    #' @param sort_field (optional) The column to sort the search on
    #' @param descending (optional) The order to return the search results
    #' @param _i (optional) This parameter is deprecated.
    #' @param start (optional) The record to begin the return set on
    #' @param _l (optional) This parameter is deprecated.
    #' @param limit (optional) The number of records to return
    #' @param show_public_locations (optional) Whether to include public locations in the results
    #' @param active_only (optional) Return only active results
    #' @param return_retailer (optional) Return retailer info
    #' @param return_assets (optional) Return assets
    #' @param return_offers (optional) Return offers
    #' @param return_categories (optional) Return categories
    #' @param return_filters (optional) Return filters
    #' @param return_audiences (optional) Return audiences
    #' @param return_qr_code (optional) Return QR code info
    #' @param include_favorite (optional) Include favorites in response
    #' @param include_liked (optional) Include liked flag in response
    #' @param include_rating (optional) Include rating info in response
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[RetailerLocationResponse]) with additional information such as HTTP status code, headers
    SearchRetailerLocationsWithHttpInfo = function(device_id = NULL, account_id = NULL, q = NULL, keyword = NULL, retailer_ids = NULL, retailer_location_ids = NULL, location_type = NULL, sort_field = NULL, descending = NULL, _i = NULL, start = NULL, _l = NULL, limit = NULL, show_public_locations = NULL, active_only = NULL, return_retailer = NULL, return_assets = NULL, return_offers = NULL, return_categories = NULL, return_filters = NULL, return_audiences = NULL, return_qr_code = NULL, include_favorite = NULL, include_liked = NULL, include_rating = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerLocationApi$SearchRetailerLocations, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerLocationApi$SearchRetailerLocations, `account_id` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling RetailerLocationApi$SearchRetailerLocations, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling RetailerLocationApi$SearchRetailerLocations, `keyword` is not nullable")
      }

      if (!missing(`retailer_ids`) && is.null(`retailer_ids`)) {
        stop("Invalid value for `retailer_ids` when calling RetailerLocationApi$SearchRetailerLocations, `retailer_ids` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling RetailerLocationApi$SearchRetailerLocations, `retailer_location_ids` is not nullable")
      }

      if (!missing(`location_type`) && is.null(`location_type`)) {
        stop("Invalid value for `location_type` when calling RetailerLocationApi$SearchRetailerLocations, `location_type` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling RetailerLocationApi$SearchRetailerLocations, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling RetailerLocationApi$SearchRetailerLocations, `descending` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling RetailerLocationApi$SearchRetailerLocations, `_i` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling RetailerLocationApi$SearchRetailerLocations, `start` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling RetailerLocationApi$SearchRetailerLocations, `_l` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling RetailerLocationApi$SearchRetailerLocations, `limit` is not nullable")
      }

      if (!missing(`show_public_locations`) && is.null(`show_public_locations`)) {
        stop("Invalid value for `show_public_locations` when calling RetailerLocationApi$SearchRetailerLocations, `show_public_locations` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling RetailerLocationApi$SearchRetailerLocations, `active_only` is not nullable")
      }

      if (!missing(`return_retailer`) && is.null(`return_retailer`)) {
        stop("Invalid value for `return_retailer` when calling RetailerLocationApi$SearchRetailerLocations, `return_retailer` is not nullable")
      }

      if (!missing(`return_assets`) && is.null(`return_assets`)) {
        stop("Invalid value for `return_assets` when calling RetailerLocationApi$SearchRetailerLocations, `return_assets` is not nullable")
      }

      if (!missing(`return_offers`) && is.null(`return_offers`)) {
        stop("Invalid value for `return_offers` when calling RetailerLocationApi$SearchRetailerLocations, `return_offers` is not nullable")
      }

      if (!missing(`return_categories`) && is.null(`return_categories`)) {
        stop("Invalid value for `return_categories` when calling RetailerLocationApi$SearchRetailerLocations, `return_categories` is not nullable")
      }

      if (!missing(`return_filters`) && is.null(`return_filters`)) {
        stop("Invalid value for `return_filters` when calling RetailerLocationApi$SearchRetailerLocations, `return_filters` is not nullable")
      }

      if (!missing(`return_audiences`) && is.null(`return_audiences`)) {
        stop("Invalid value for `return_audiences` when calling RetailerLocationApi$SearchRetailerLocations, `return_audiences` is not nullable")
      }

      if (!missing(`return_qr_code`) && is.null(`return_qr_code`)) {
        stop("Invalid value for `return_qr_code` when calling RetailerLocationApi$SearchRetailerLocations, `return_qr_code` is not nullable")
      }

      if (!missing(`include_favorite`) && is.null(`include_favorite`)) {
        stop("Invalid value for `include_favorite` when calling RetailerLocationApi$SearchRetailerLocations, `include_favorite` is not nullable")
      }

      if (!missing(`include_liked`) && is.null(`include_liked`)) {
        stop("Invalid value for `include_liked` when calling RetailerLocationApi$SearchRetailerLocations, `include_liked` is not nullable")
      }

      if (!missing(`include_rating`) && is.null(`include_rating`)) {
        stop("Invalid value for `include_rating` when calling RetailerLocationApi$SearchRetailerLocations, `include_rating` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["retailerIds"]] <- `retailer_ids`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["locationType"]] <- `location_type`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "RETAILER_ID", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY", "ADDRESS_STREET", "ADDRESS_CITY", "ADDRESS_STATE", "ADDRESS_POSTAL_CODE", "ADDRESS_COUNTRY", "NAME", "CODE"))) {
        stop("Invalid value for sort_field when calling RetailerLocationApi$SearchRetailerLocations. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, RETAILER_ID, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY, ADDRESS_STREET, ADDRESS_CITY, ADDRESS_STATE, ADDRESS_POSTAL_CODE, ADDRESS_COUNTRY, NAME, CODE].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["showPublicLocations"]] <- `show_public_locations`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["returnRetailer"]] <- `return_retailer`

      query_params[["returnAssets"]] <- `return_assets`

      query_params[["returnOffers"]] <- `return_offers`

      query_params[["returnCategories"]] <- `return_categories`

      query_params[["returnFilters"]] <- `return_filters`

      query_params[["returnAudiences"]] <- `return_audiences`

      query_params[["returnQrCode"]] <- `return_qr_code`

      query_params[["includeFavorite"]] <- `include_favorite`

      query_params[["includeLiked"]] <- `include_liked`

      query_params[["includeRating"]] <- `include_rating`

      local_var_url_path <- "/retailer/location/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[RetailerLocationResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Retailer Location
    #'
    #' @param retailer_location_id The ID of the retailer location
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param name (optional) The name of the retailer location
    #' @param street_address (optional) The street address of the retailer location
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the retailer location
    #' @param state (optional) The state of the retailer location
    #' @param postal_code (optional) The postal code of the retailer location
    #' @param country (optional) the country of the retailer location
    #' @param business_phone (optional) The business phone number of the retailer location
    #' @param business_phone_ext (optional) The business phone extension of the retailer location
    #' @param website (optional) The website of the retailer location
    #' @param email (optional) The email of the retailer location
    #' @param internal_id (optional) An internal identifier used by the retailer
    #' @param details_header (optional) A brief description about the retailer location (255 character limit)
    #' @param details_body (optional) A detailed description about the retailer location
    #' @param hours (optional) The hours of operation
    #' @param logo (optional) The retailer location logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before)
    #' @param logo_asset_id (optional) The retailer location logo asset id
    #' @param picture1 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before)
    #' @param picture1_asset_id (optional) An asset id
    #' @param picture2 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before)
    #' @param picture2_asset_id (optional) An asset id
    #' @param category_ids (optional) Comma separated list of category IDs used to filter retailer locations by categories
    #' @param filter_ids (optional) Comma separated list of filter IDs used to filter retailer locations
    #' @param latitude (optional) the latituede of the retailer location
    #' @param longitude (optional) the longitude of the retailer location
    #' @param building (optional) 
    #' @param google_place_id (optional) the Google Place ID that the retailer location is associated with
    #' @param yelp_id (optional) the Yelp ID that the retailer location is associated with
    #' @param meta_data (optional) External custom client defined data
    #' @param payment_provider (optional) Specifies which payment provider Sirqul will use when making payments
    #' @param active (optional) Sets whether the retailer is active or inactive (hidden from consumers)
    #' @param public_location (optional) Sets whether the location is public or not
    #' @param location_type (optional) External custom type identifier
    #' @param audience_ids (optional) Comma separated list of audience IDs used to assign audiences to the retailer location
    #' @param audience_ids_to_add (optional) Comma separated list of audience IDs to add to the retailer location
    #' @param audience_ids_to_remove (optional) Comma separated list of audience IDs to remove from the retailer location
    #' @param response_format (optional) The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets}
    #' @param tags (optional) Custom string field for doing full-text searches
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerLocationResponse
    UpdateRetailerLocations = function(retailer_location_id, device_id = NULL, account_id = NULL, name = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, business_phone = NULL, business_phone_ext = NULL, website = NULL, email = NULL, internal_id = NULL, details_header = NULL, details_body = NULL, hours = NULL, logo = NULL, logo_asset_id = NULL, picture1 = NULL, picture1_asset_id = NULL, picture2 = NULL, picture2_asset_id = NULL, category_ids = NULL, filter_ids = NULL, latitude = NULL, longitude = NULL, building = NULL, google_place_id = NULL, yelp_id = NULL, meta_data = NULL, payment_provider = NULL, active = NULL, public_location = NULL, location_type = NULL, audience_ids = NULL, audience_ids_to_add = NULL, audience_ids_to_remove = NULL, response_format = NULL, tags = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateRetailerLocationsWithHttpInfo(retailer_location_id, device_id, account_id, name, street_address, street_address2, city, state, postal_code, country, business_phone, business_phone_ext, website, email, internal_id, details_header, details_body, hours, logo, logo_asset_id, picture1, picture1_asset_id, picture2, picture2_asset_id, category_ids, filter_ids, latitude, longitude, building, google_place_id, yelp_id, meta_data, payment_provider, active, public_location, location_type, audience_ids, audience_ids_to_add, audience_ids_to_remove, response_format, tags, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Retailer Location
    #'
    #' @param retailer_location_id The ID of the retailer location
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param name (optional) The name of the retailer location
    #' @param street_address (optional) The street address of the retailer location
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the retailer location
    #' @param state (optional) The state of the retailer location
    #' @param postal_code (optional) The postal code of the retailer location
    #' @param country (optional) the country of the retailer location
    #' @param business_phone (optional) The business phone number of the retailer location
    #' @param business_phone_ext (optional) The business phone extension of the retailer location
    #' @param website (optional) The website of the retailer location
    #' @param email (optional) The email of the retailer location
    #' @param internal_id (optional) An internal identifier used by the retailer
    #' @param details_header (optional) A brief description about the retailer location (255 character limit)
    #' @param details_body (optional) A detailed description about the retailer location
    #' @param hours (optional) The hours of operation
    #' @param logo (optional) The retailer location logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before)
    #' @param logo_asset_id (optional) The retailer location logo asset id
    #' @param picture1 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before)
    #' @param picture1_asset_id (optional) An asset id
    #' @param picture2 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before)
    #' @param picture2_asset_id (optional) An asset id
    #' @param category_ids (optional) Comma separated list of category IDs used to filter retailer locations by categories
    #' @param filter_ids (optional) Comma separated list of filter IDs used to filter retailer locations
    #' @param latitude (optional) the latituede of the retailer location
    #' @param longitude (optional) the longitude of the retailer location
    #' @param building (optional) 
    #' @param google_place_id (optional) the Google Place ID that the retailer location is associated with
    #' @param yelp_id (optional) the Yelp ID that the retailer location is associated with
    #' @param meta_data (optional) External custom client defined data
    #' @param payment_provider (optional) Specifies which payment provider Sirqul will use when making payments
    #' @param active (optional) Sets whether the retailer is active or inactive (hidden from consumers)
    #' @param public_location (optional) Sets whether the location is public or not
    #' @param location_type (optional) External custom type identifier
    #' @param audience_ids (optional) Comma separated list of audience IDs used to assign audiences to the retailer location
    #' @param audience_ids_to_add (optional) Comma separated list of audience IDs to add to the retailer location
    #' @param audience_ids_to_remove (optional) Comma separated list of audience IDs to remove from the retailer location
    #' @param response_format (optional) The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets}
    #' @param tags (optional) Custom string field for doing full-text searches
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerLocationResponse) with additional information such as HTTP status code, headers
    UpdateRetailerLocationsWithHttpInfo = function(retailer_location_id, device_id = NULL, account_id = NULL, name = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, business_phone = NULL, business_phone_ext = NULL, website = NULL, email = NULL, internal_id = NULL, details_header = NULL, details_body = NULL, hours = NULL, logo = NULL, logo_asset_id = NULL, picture1 = NULL, picture1_asset_id = NULL, picture2 = NULL, picture2_asset_id = NULL, category_ids = NULL, filter_ids = NULL, latitude = NULL, longitude = NULL, building = NULL, google_place_id = NULL, yelp_id = NULL, meta_data = NULL, payment_provider = NULL, active = NULL, public_location = NULL, location_type = NULL, audience_ids = NULL, audience_ids_to_add = NULL, audience_ids_to_remove = NULL, response_format = NULL, tags = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`retailer_location_id`)) {
        stop("Missing required parameter `retailer_location_id`.")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling RetailerLocationApi$UpdateRetailerLocations, `retailer_location_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerLocationApi$UpdateRetailerLocations, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerLocationApi$UpdateRetailerLocations, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling RetailerLocationApi$UpdateRetailerLocations, `name` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling RetailerLocationApi$UpdateRetailerLocations, `street_address` is not nullable")
      }

      if (!missing(`street_address2`) && is.null(`street_address2`)) {
        stop("Invalid value for `street_address2` when calling RetailerLocationApi$UpdateRetailerLocations, `street_address2` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling RetailerLocationApi$UpdateRetailerLocations, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling RetailerLocationApi$UpdateRetailerLocations, `state` is not nullable")
      }

      if (!missing(`postal_code`) && is.null(`postal_code`)) {
        stop("Invalid value for `postal_code` when calling RetailerLocationApi$UpdateRetailerLocations, `postal_code` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling RetailerLocationApi$UpdateRetailerLocations, `country` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling RetailerLocationApi$UpdateRetailerLocations, `business_phone` is not nullable")
      }

      if (!missing(`business_phone_ext`) && is.null(`business_phone_ext`)) {
        stop("Invalid value for `business_phone_ext` when calling RetailerLocationApi$UpdateRetailerLocations, `business_phone_ext` is not nullable")
      }

      if (!missing(`website`) && is.null(`website`)) {
        stop("Invalid value for `website` when calling RetailerLocationApi$UpdateRetailerLocations, `website` is not nullable")
      }

      if (!missing(`email`) && is.null(`email`)) {
        stop("Invalid value for `email` when calling RetailerLocationApi$UpdateRetailerLocations, `email` is not nullable")
      }

      if (!missing(`internal_id`) && is.null(`internal_id`)) {
        stop("Invalid value for `internal_id` when calling RetailerLocationApi$UpdateRetailerLocations, `internal_id` is not nullable")
      }

      if (!missing(`details_header`) && is.null(`details_header`)) {
        stop("Invalid value for `details_header` when calling RetailerLocationApi$UpdateRetailerLocations, `details_header` is not nullable")
      }

      if (!missing(`details_body`) && is.null(`details_body`)) {
        stop("Invalid value for `details_body` when calling RetailerLocationApi$UpdateRetailerLocations, `details_body` is not nullable")
      }

      if (!missing(`hours`) && is.null(`hours`)) {
        stop("Invalid value for `hours` when calling RetailerLocationApi$UpdateRetailerLocations, `hours` is not nullable")
      }

      if (!missing(`logo`) && is.null(`logo`)) {
        stop("Invalid value for `logo` when calling RetailerLocationApi$UpdateRetailerLocations, `logo` is not nullable")
      }

      if (!missing(`logo_asset_id`) && is.null(`logo_asset_id`)) {
        stop("Invalid value for `logo_asset_id` when calling RetailerLocationApi$UpdateRetailerLocations, `logo_asset_id` is not nullable")
      }

      if (!missing(`picture1`) && is.null(`picture1`)) {
        stop("Invalid value for `picture1` when calling RetailerLocationApi$UpdateRetailerLocations, `picture1` is not nullable")
      }

      if (!missing(`picture1_asset_id`) && is.null(`picture1_asset_id`)) {
        stop("Invalid value for `picture1_asset_id` when calling RetailerLocationApi$UpdateRetailerLocations, `picture1_asset_id` is not nullable")
      }

      if (!missing(`picture2`) && is.null(`picture2`)) {
        stop("Invalid value for `picture2` when calling RetailerLocationApi$UpdateRetailerLocations, `picture2` is not nullable")
      }

      if (!missing(`picture2_asset_id`) && is.null(`picture2_asset_id`)) {
        stop("Invalid value for `picture2_asset_id` when calling RetailerLocationApi$UpdateRetailerLocations, `picture2_asset_id` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling RetailerLocationApi$UpdateRetailerLocations, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling RetailerLocationApi$UpdateRetailerLocations, `filter_ids` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RetailerLocationApi$UpdateRetailerLocations, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RetailerLocationApi$UpdateRetailerLocations, `longitude` is not nullable")
      }

      if (!missing(`building`) && is.null(`building`)) {
        stop("Invalid value for `building` when calling RetailerLocationApi$UpdateRetailerLocations, `building` is not nullable")
      }

      if (!missing(`google_place_id`) && is.null(`google_place_id`)) {
        stop("Invalid value for `google_place_id` when calling RetailerLocationApi$UpdateRetailerLocations, `google_place_id` is not nullable")
      }

      if (!missing(`yelp_id`) && is.null(`yelp_id`)) {
        stop("Invalid value for `yelp_id` when calling RetailerLocationApi$UpdateRetailerLocations, `yelp_id` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling RetailerLocationApi$UpdateRetailerLocations, `meta_data` is not nullable")
      }

      if (!missing(`payment_provider`) && is.null(`payment_provider`)) {
        stop("Invalid value for `payment_provider` when calling RetailerLocationApi$UpdateRetailerLocations, `payment_provider` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling RetailerLocationApi$UpdateRetailerLocations, `active` is not nullable")
      }

      if (!missing(`public_location`) && is.null(`public_location`)) {
        stop("Invalid value for `public_location` when calling RetailerLocationApi$UpdateRetailerLocations, `public_location` is not nullable")
      }

      if (!missing(`location_type`) && is.null(`location_type`)) {
        stop("Invalid value for `location_type` when calling RetailerLocationApi$UpdateRetailerLocations, `location_type` is not nullable")
      }

      if (!missing(`audience_ids`) && is.null(`audience_ids`)) {
        stop("Invalid value for `audience_ids` when calling RetailerLocationApi$UpdateRetailerLocations, `audience_ids` is not nullable")
      }

      if (!missing(`audience_ids_to_add`) && is.null(`audience_ids_to_add`)) {
        stop("Invalid value for `audience_ids_to_add` when calling RetailerLocationApi$UpdateRetailerLocations, `audience_ids_to_add` is not nullable")
      }

      if (!missing(`audience_ids_to_remove`) && is.null(`audience_ids_to_remove`)) {
        stop("Invalid value for `audience_ids_to_remove` when calling RetailerLocationApi$UpdateRetailerLocations, `audience_ids_to_remove` is not nullable")
      }

      if (!missing(`response_format`) && is.null(`response_format`)) {
        stop("Invalid value for `response_format` when calling RetailerLocationApi$UpdateRetailerLocations, `response_format` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling RetailerLocationApi$UpdateRetailerLocations, `tags` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["name"]] <- `name`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["streetAddress2"]] <- `street_address2`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["postalCode"]] <- `postal_code`

      query_params[["country"]] <- `country`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["businessPhoneExt"]] <- `business_phone_ext`

      query_params[["website"]] <- `website`

      query_params[["email"]] <- `email`

      query_params[["internalId"]] <- `internal_id`

      query_params[["detailsHeader"]] <- `details_header`

      query_params[["detailsBody"]] <- `details_body`

      query_params[["hours"]] <- `hours`

      query_params[["logo"]] <- `logo`

      query_params[["logoAssetId"]] <- `logo_asset_id`

      query_params[["picture1"]] <- `picture1`

      query_params[["picture1AssetId"]] <- `picture1_asset_id`

      query_params[["picture2"]] <- `picture2`

      query_params[["picture2AssetId"]] <- `picture2_asset_id`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["building"]] <- `building`

      query_params[["googlePlaceId"]] <- `google_place_id`

      query_params[["yelpId"]] <- `yelp_id`

      query_params[["metaData"]] <- `meta_data`

      query_params[["paymentProvider"]] <- `payment_provider`

      query_params[["active"]] <- `active`

      query_params[["publicLocation"]] <- `public_location`

      query_params[["locationType"]] <- `location_type`

      query_params[["audienceIds"]] <- `audience_ids`

      query_params[["audienceIdsToAdd"]] <- `audience_ids_to_add`

      query_params[["audienceIdsToRemove"]] <- `audience_ids_to_remove`

      if (!is.null(`response_format`) && !(`response_format` %in% c("HTML", "XML", "JSON", "CSV"))) {
        stop("Invalid value for response_format when calling RetailerLocationApi$UpdateRetailerLocations. Must be [HTML, XML, JSON, CSV].")
      }
      query_params[["responseFormat"]] <- `response_format`

      query_params[["tags"]] <- `tags`

      local_var_url_path <- "/retailer/location/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerLocationResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
