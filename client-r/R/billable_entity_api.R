#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title BillableEntity operations
#' @description BillableEntityApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateBillableEntity  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_name <- "name_example" # character | The name of the entity responsible for billing  (Optional)
#' var_street_address <- "street_address_example" # character | The street address of the billable entity (Optional)
#' var_street_address2 <- "street_address2_example" # character | Additional address information (such as a suite number, floor number, building name, or PO Box)  (Optional)
#' var_city <- "city_example" # character | The city of the billable entity (Optional)
#' var_state <- "state_example" # character | The state of the billable entity (Optional)
#' var_postal_code <- "postal_code_example" # character | The postal code of the billable entity (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone of the billable entity (Optional)
#' var_business_phone_ext <- "business_phone_ext_example" # character | The business phone extension (Optional)
#' var_authorize_net_api_key <- "authorize_net_api_key_example" # character | Authorize Net Api Key (Optional)
#' var_authorize_net_transaction_key <- "authorize_net_transaction_key_example" # character | Authorize Net Transaction Key (Optional)
#'
#' #Create Billable
#' api_instance <- BillableEntityApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateBillableEntity(device_id = var_device_id, account_id = var_account_id, name = var_name, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, authorize_net_api_key = var_authorize_net_api_key, authorize_net_transaction_key = var_authorize_net_transaction_keydata_file = "result.txt")
#' result <- api_instance$CreateBillableEntity(device_id = var_device_id, account_id = var_account_id, name = var_name, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, authorize_net_api_key = var_authorize_net_api_key, authorize_net_transaction_key = var_authorize_net_transaction_key)
#' dput(result)
#'
#'
#' ####################  DeleteBillableEntity  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account used to perform the delete, must have rights to edit the billable entity. (Optional)
#'
#' #Delete Billable
#' api_instance <- BillableEntityApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteBillableEntity(device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteBillableEntity(device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  GetBillableEntity  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_include_counts <- FALSE # character | Determines whether to include the retailer dash board counts into the response (Optional)
#' var_include_payments <- TRUE # character | Whether to enable payments or not (Optional)
#'
#' #Get Billable
#' api_instance <- BillableEntityApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetBillableEntity(device_id = var_device_id, account_id = var_account_id, include_counts = var_include_counts, include_payments = var_include_paymentsdata_file = "result.txt")
#' result <- api_instance$GetBillableEntity(device_id = var_device_id, account_id = var_account_id, include_counts = var_include_counts, include_payments = var_include_payments)
#' dput(result)
#'
#'
#' ####################  UpdateBillableEntity  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used). The account must have rights to edit the billable entity. (Optional)
#' var_name <- "name_example" # character | The name of the entity responsible for billing  (Optional)
#' var_street_address <- "street_address_example" # character | The street address of the billable entity (Optional)
#' var_street_address2 <- "street_address2_example" # character | Additional address information (such as a suite number, floor number, building name, or PO Box)  (Optional)
#' var_city <- "city_example" # character | The city of the billable entity (Optional)
#' var_state <- "state_example" # character | The state of the billable entity (Optional)
#' var_postal_code <- "postal_code_example" # character | The postal code of the billable entity (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone of the billable entity (Optional)
#' var_business_phone_ext <- "business_phone_ext_example" # character | The business phone extension of the billable entity (Optional)
#' var_authorize_net_api_key <- "authorize_net_api_key_example" # character | Authorize Net Api Key of the billable entity (Optional)
#' var_authorize_net_transaction_key <- "authorize_net_transaction_key_example" # character | Authorize Net Transaction Key of the billable entity (Optional)
#'
#' #Update Billable
#' api_instance <- BillableEntityApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateBillableEntity(device_id = var_device_id, account_id = var_account_id, name = var_name, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, authorize_net_api_key = var_authorize_net_api_key, authorize_net_transaction_key = var_authorize_net_transaction_keydata_file = "result.txt")
#' result <- api_instance$UpdateBillableEntity(device_id = var_device_id, account_id = var_account_id, name = var_name, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, authorize_net_api_key = var_authorize_net_api_key, authorize_net_transaction_key = var_authorize_net_transaction_key)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
BillableEntityApi <- R6::R6Class(
  "BillableEntityApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new BillableEntityApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Billable
    #'
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param name (optional) The name of the entity responsible for billing 
    #' @param street_address (optional) The street address of the billable entity
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box) 
    #' @param city (optional) The city of the billable entity
    #' @param state (optional) The state of the billable entity
    #' @param postal_code (optional) The postal code of the billable entity
    #' @param business_phone (optional) The business phone of the billable entity
    #' @param business_phone_ext (optional) The business phone extension
    #' @param authorize_net_api_key (optional) Authorize Net Api Key
    #' @param authorize_net_transaction_key (optional) Authorize Net Transaction Key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BillableEntityResponse
    CreateBillableEntity = function(device_id = NULL, account_id = NULL, name = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, business_phone = NULL, business_phone_ext = NULL, authorize_net_api_key = NULL, authorize_net_transaction_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateBillableEntityWithHttpInfo(device_id, account_id, name, street_address, street_address2, city, state, postal_code, business_phone, business_phone_ext, authorize_net_api_key, authorize_net_transaction_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Billable
    #'
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param name (optional) The name of the entity responsible for billing 
    #' @param street_address (optional) The street address of the billable entity
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box) 
    #' @param city (optional) The city of the billable entity
    #' @param state (optional) The state of the billable entity
    #' @param postal_code (optional) The postal code of the billable entity
    #' @param business_phone (optional) The business phone of the billable entity
    #' @param business_phone_ext (optional) The business phone extension
    #' @param authorize_net_api_key (optional) Authorize Net Api Key
    #' @param authorize_net_transaction_key (optional) Authorize Net Transaction Key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BillableEntityResponse) with additional information such as HTTP status code, headers
    CreateBillableEntityWithHttpInfo = function(device_id = NULL, account_id = NULL, name = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, business_phone = NULL, business_phone_ext = NULL, authorize_net_api_key = NULL, authorize_net_transaction_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling BillableEntityApi$CreateBillableEntity, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BillableEntityApi$CreateBillableEntity, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling BillableEntityApi$CreateBillableEntity, `name` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling BillableEntityApi$CreateBillableEntity, `street_address` is not nullable")
      }

      if (!missing(`street_address2`) && is.null(`street_address2`)) {
        stop("Invalid value for `street_address2` when calling BillableEntityApi$CreateBillableEntity, `street_address2` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling BillableEntityApi$CreateBillableEntity, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling BillableEntityApi$CreateBillableEntity, `state` is not nullable")
      }

      if (!missing(`postal_code`) && is.null(`postal_code`)) {
        stop("Invalid value for `postal_code` when calling BillableEntityApi$CreateBillableEntity, `postal_code` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling BillableEntityApi$CreateBillableEntity, `business_phone` is not nullable")
      }

      if (!missing(`business_phone_ext`) && is.null(`business_phone_ext`)) {
        stop("Invalid value for `business_phone_ext` when calling BillableEntityApi$CreateBillableEntity, `business_phone_ext` is not nullable")
      }

      if (!missing(`authorize_net_api_key`) && is.null(`authorize_net_api_key`)) {
        stop("Invalid value for `authorize_net_api_key` when calling BillableEntityApi$CreateBillableEntity, `authorize_net_api_key` is not nullable")
      }

      if (!missing(`authorize_net_transaction_key`) && is.null(`authorize_net_transaction_key`)) {
        stop("Invalid value for `authorize_net_transaction_key` when calling BillableEntityApi$CreateBillableEntity, `authorize_net_transaction_key` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["name"]] <- `name`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["streetAddress2"]] <- `street_address2`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["postalCode"]] <- `postal_code`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["businessPhoneExt"]] <- `business_phone_ext`

      query_params[["authorizeNetApiKey"]] <- `authorize_net_api_key`

      query_params[["authorizeNetTransactionKey"]] <- `authorize_net_transaction_key`

      local_var_url_path <- "/billable/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BillableEntityResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Billable
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the billable entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteBillableEntity = function(device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteBillableEntityWithHttpInfo(device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Billable
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the billable entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteBillableEntityWithHttpInfo = function(device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling BillableEntityApi$DeleteBillableEntity, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BillableEntityApi$DeleteBillableEntity, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      local_var_url_path <- "/billable/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Billable
    #'
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param include_counts (optional) Determines whether to include the retailer dash board counts into the response (default value: FALSE)
    #' @param include_payments (optional) Whether to enable payments or not (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BillableEntityResponse
    GetBillableEntity = function(device_id = NULL, account_id = NULL, include_counts = FALSE, include_payments = TRUE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetBillableEntityWithHttpInfo(device_id, account_id, include_counts, include_payments, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Billable
    #'
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param include_counts (optional) Determines whether to include the retailer dash board counts into the response (default value: FALSE)
    #' @param include_payments (optional) Whether to enable payments or not (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BillableEntityResponse) with additional information such as HTTP status code, headers
    GetBillableEntityWithHttpInfo = function(device_id = NULL, account_id = NULL, include_counts = FALSE, include_payments = TRUE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling BillableEntityApi$GetBillableEntity, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BillableEntityApi$GetBillableEntity, `account_id` is not nullable")
      }

      if (!missing(`include_counts`) && is.null(`include_counts`)) {
        stop("Invalid value for `include_counts` when calling BillableEntityApi$GetBillableEntity, `include_counts` is not nullable")
      }

      if (!missing(`include_payments`) && is.null(`include_payments`)) {
        stop("Invalid value for `include_payments` when calling BillableEntityApi$GetBillableEntity, `include_payments` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["includeCounts"]] <- `include_counts`

      query_params[["includePayments"]] <- `include_payments`

      local_var_url_path <- "/billable/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BillableEntityResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Billable
    #'
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used). The account must have rights to edit the billable entity.
    #' @param name (optional) The name of the entity responsible for billing 
    #' @param street_address (optional) The street address of the billable entity
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box) 
    #' @param city (optional) The city of the billable entity
    #' @param state (optional) The state of the billable entity
    #' @param postal_code (optional) The postal code of the billable entity
    #' @param business_phone (optional) The business phone of the billable entity
    #' @param business_phone_ext (optional) The business phone extension of the billable entity
    #' @param authorize_net_api_key (optional) Authorize Net Api Key of the billable entity
    #' @param authorize_net_transaction_key (optional) Authorize Net Transaction Key of the billable entity
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BillableEntityResponse
    UpdateBillableEntity = function(device_id = NULL, account_id = NULL, name = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, business_phone = NULL, business_phone_ext = NULL, authorize_net_api_key = NULL, authorize_net_transaction_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateBillableEntityWithHttpInfo(device_id, account_id, name, street_address, street_address2, city, state, postal_code, business_phone, business_phone_ext, authorize_net_api_key, authorize_net_transaction_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Billable
    #'
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used). The account must have rights to edit the billable entity.
    #' @param name (optional) The name of the entity responsible for billing 
    #' @param street_address (optional) The street address of the billable entity
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box) 
    #' @param city (optional) The city of the billable entity
    #' @param state (optional) The state of the billable entity
    #' @param postal_code (optional) The postal code of the billable entity
    #' @param business_phone (optional) The business phone of the billable entity
    #' @param business_phone_ext (optional) The business phone extension of the billable entity
    #' @param authorize_net_api_key (optional) Authorize Net Api Key of the billable entity
    #' @param authorize_net_transaction_key (optional) Authorize Net Transaction Key of the billable entity
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BillableEntityResponse) with additional information such as HTTP status code, headers
    UpdateBillableEntityWithHttpInfo = function(device_id = NULL, account_id = NULL, name = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, business_phone = NULL, business_phone_ext = NULL, authorize_net_api_key = NULL, authorize_net_transaction_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling BillableEntityApi$UpdateBillableEntity, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BillableEntityApi$UpdateBillableEntity, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling BillableEntityApi$UpdateBillableEntity, `name` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling BillableEntityApi$UpdateBillableEntity, `street_address` is not nullable")
      }

      if (!missing(`street_address2`) && is.null(`street_address2`)) {
        stop("Invalid value for `street_address2` when calling BillableEntityApi$UpdateBillableEntity, `street_address2` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling BillableEntityApi$UpdateBillableEntity, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling BillableEntityApi$UpdateBillableEntity, `state` is not nullable")
      }

      if (!missing(`postal_code`) && is.null(`postal_code`)) {
        stop("Invalid value for `postal_code` when calling BillableEntityApi$UpdateBillableEntity, `postal_code` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling BillableEntityApi$UpdateBillableEntity, `business_phone` is not nullable")
      }

      if (!missing(`business_phone_ext`) && is.null(`business_phone_ext`)) {
        stop("Invalid value for `business_phone_ext` when calling BillableEntityApi$UpdateBillableEntity, `business_phone_ext` is not nullable")
      }

      if (!missing(`authorize_net_api_key`) && is.null(`authorize_net_api_key`)) {
        stop("Invalid value for `authorize_net_api_key` when calling BillableEntityApi$UpdateBillableEntity, `authorize_net_api_key` is not nullable")
      }

      if (!missing(`authorize_net_transaction_key`) && is.null(`authorize_net_transaction_key`)) {
        stop("Invalid value for `authorize_net_transaction_key` when calling BillableEntityApi$UpdateBillableEntity, `authorize_net_transaction_key` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["name"]] <- `name`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["streetAddress2"]] <- `street_address2`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["postalCode"]] <- `postal_code`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["businessPhoneExt"]] <- `business_phone_ext`

      query_params[["authorizeNetApiKey"]] <- `authorize_net_api_key`

      query_params[["authorizeNetTransactionKey"]] <- `authorize_net_transaction_key`

      local_var_url_path <- "/billable/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BillableEntityResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
