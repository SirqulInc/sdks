#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title BillingInfo operations
#' @description BillingInfoApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AddPaymentMethod  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account used to perform the the request
#' var_payment_method_id <- 56 # integer | Payment Method Id (Optional)
#' var_account_name <- "account_name_example" # character | the name of the account (Optional)
#' var_first_name <- "first_name_example" # character | First Name that the account is filed as (Optional)
#' var_last_name <- "last_name_example" # character | Last Name that the account is filed as (Optional)
#' var_address <- "address_example" # character | Address that the account is filed as (Optional)
#' var_city <- "city_example" # character | City that the account is filed as (Optional)
#' var_state <- "state_example" # character | State that the account is filed as (Optional)
#' var_postal_code <- "postal_code_example" # character | Postal Code that the account is filed as (Optional)
#' var_country <- "country_example" # character | Country that the account is filed as (Optional)
#' var_phone <- "phone_example" # character | Phone that the account is filed as (Optional)
#' var_credit_card_number <- "credit_card_number_example" # character | The full credit card number to store on file (Optional)
#' var_expiration_date <- "expiration_date_example" # character | The credit card expiration date YYYY-MM (Optional)
#' var_ccv <- "ccv_example" # character | The 3 digit confirmation code (Optional)
#' var_account_number <- "account_number_example" # character | The bank account number (Optional)
#' var_bank_name <- "bank_name_example" # character | The bank name (Optional)
#' var_routing_number <- "routing_number_example" # character | Routing Number (Optional)
#' var_default_payment_method <- "default_payment_method_example" # character | Default Payment Method (Optional)
#' var_payment_method_nickname <- "payment_method_nickname_example" # character | Payment Method Nickname (Optional)
#' var_tax_id <- "tax_id_example" # character | Tax Id (Optional)
#' var_provider_customer_profile_id <- "provider_customer_profile_id_example" # character | Provider customer profile Id (Optional)
#' var_provider_payment_profile_id <- "provider_payment_profile_id_example" # character | Provider customer payment profile Id (Optional)
#' var_meta_data <- "meta_data_example" # character | Meta Data (Optional)
#'
#' #Update Payment Method
#' api_instance <- BillingInfoApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AddPaymentMethod(var_account_id, payment_method_id = var_payment_method_id, account_name = var_account_name, first_name = var_first_name, last_name = var_last_name, address = var_address, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, phone = var_phone, credit_card_number = var_credit_card_number, expiration_date = var_expiration_date, ccv = var_ccv, account_number = var_account_number, bank_name = var_bank_name, routing_number = var_routing_number, default_payment_method = var_default_payment_method, payment_method_nickname = var_payment_method_nickname, tax_id = var_tax_id, provider_customer_profile_id = var_provider_customer_profile_id, provider_payment_profile_id = var_provider_payment_profile_id, meta_data = var_meta_datadata_file = "result.txt")
#' result <- api_instance$AddPaymentMethod(var_account_id, payment_method_id = var_payment_method_id, account_name = var_account_name, first_name = var_first_name, last_name = var_last_name, address = var_address, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, phone = var_phone, credit_card_number = var_credit_card_number, expiration_date = var_expiration_date, ccv = var_ccv, account_number = var_account_number, bank_name = var_bank_name, routing_number = var_routing_number, default_payment_method = var_default_payment_method, payment_method_nickname = var_payment_method_nickname, tax_id = var_tax_id, provider_customer_profile_id = var_provider_customer_profile_id, provider_payment_profile_id = var_provider_payment_profile_id, meta_data = var_meta_data)
#' dput(result)
#'
#'
#' ####################  CreatePaymentMethod  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account used to perform the the request
#' var_account_name <- "account_name_example" # character | Account Name of the credit card user (Optional)
#' var_first_name <- "first_name_example" # character | The first name on the credit card (Optional)
#' var_last_name <- "last_name_example" # character | The last name on the credit card (Optional)
#' var_address <- "address_example" # character | The billing address of the credit card (Optional)
#' var_city <- "city_example" # character | The billing city of the credit card (Optional)
#' var_state <- "state_example" # character | The billing state of the credit card (Optional)
#' var_postal_code <- "postal_code_example" # character | The billing zip code of the credit card (Optional)
#' var_country <- "country_example" # character | Country of the credit card (Optional)
#' var_phone <- "phone_example" # character | The billing phone of the credit card (Optional)
#' var_credit_card_number <- "credit_card_number_example" # character | The full credit card number to store on file (Optional)
#' var_expiration_date <- "expiration_date_example" # character | The credit card expiration date YYYY-MM (Optional)
#' var_ccv <- "ccv_example" # character | The 3 digit confirmation code (Optional)
#' var_account_number <- "account_number_example" # character | The bank account number (Optional)
#' var_bank_name <- "bank_name_example" # character | The bank name (Optional)
#' var_routing_number <- "routing_number_example" # character | The bank routing number (Optional)
#' var_payment_method_nickname <- "payment_method_nickname_example" # character | The nickname to give the payment method (Optional)
#' var_tax_id <- "tax_id_example" # character | Tax Id (Optional)
#' var_default_payment_method <- TRUE # character | Whether this should be the default payment method (Optional)
#' var_auth_token <- "auth_token_example" # character | An authorization token for providers that provide this (like Amazon Payments) (Optional)
#' var_provider <- "AUTHORIZE_NET" # character | The payment provider (see PaymentMethodProvider) (Optional)
#' var_provider_customer_profile_id <- "provider_customer_profile_id_example" # character | Provider customer profile Id (Optional)
#' var_provider_payment_profile_id <- "provider_payment_profile_id_example" # character | Provider customer payment profile Id (Optional)
#' var_meta_data <- "meta_data_example" # character | Meta Data (Optional)
#' var_app_key <- "app_key_example" # character | Application Key (Optional)
#'
#' #Create Payment Method
#' api_instance <- BillingInfoApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreatePaymentMethod(var_account_id, account_name = var_account_name, first_name = var_first_name, last_name = var_last_name, address = var_address, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, phone = var_phone, credit_card_number = var_credit_card_number, expiration_date = var_expiration_date, ccv = var_ccv, account_number = var_account_number, bank_name = var_bank_name, routing_number = var_routing_number, payment_method_nickname = var_payment_method_nickname, tax_id = var_tax_id, default_payment_method = var_default_payment_method, auth_token = var_auth_token, provider = var_provider, provider_customer_profile_id = var_provider_customer_profile_id, provider_payment_profile_id = var_provider_payment_profile_id, meta_data = var_meta_data, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$CreatePaymentMethod(var_account_id, account_name = var_account_name, first_name = var_first_name, last_name = var_last_name, address = var_address, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, phone = var_phone, credit_card_number = var_credit_card_number, expiration_date = var_expiration_date, ccv = var_ccv, account_number = var_account_number, bank_name = var_bank_name, routing_number = var_routing_number, payment_method_nickname = var_payment_method_nickname, tax_id = var_tax_id, default_payment_method = var_default_payment_method, auth_token = var_auth_token, provider = var_provider, provider_customer_profile_id = var_provider_customer_profile_id, provider_payment_profile_id = var_provider_payment_profile_id, meta_data = var_meta_data, app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  CreateSmartContract  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account used to perform the the request
#' var_token_name <- "token_name_example" # character | The token name
#' var_token_symbol <- "token_symbol_example" # character | The token symbol
#' var_payment_method_id <- 56 # integer | The payment method to return details on. If this is not set, then the user's default payment method will be returned. (Optional)
#'
#' #Create Smart Contract
#' api_instance <- BillingInfoApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateSmartContract(var_account_id, var_token_name, var_token_symbol, payment_method_id = var_payment_method_iddata_file = "result.txt")
#' result <- api_instance$CreateSmartContract(var_account_id, var_token_name, var_token_symbol, payment_method_id = var_payment_method_id)
#' dput(result)
#'
#'
#' ####################  GetCryptoBalance  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account used to perform the the request
#' var_owner_account_id <- 56 # integer | The account to retreive balances for (Optional)
#' var_payment_method_id <- 56 # integer | The payment method to return details on. If this is not set, then the user's default payment method will be returned. (Optional)
#'
#' #Get Crypto Balances
#' api_instance <- BillingInfoApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCryptoBalance(var_account_id, owner_account_id = var_owner_account_id, payment_method_id = var_payment_method_iddata_file = "result.txt")
#' result <- api_instance$GetCryptoBalance(var_account_id, owner_account_id = var_owner_account_id, payment_method_id = var_payment_method_id)
#' dput(result)
#'
#'
#' ####################  GetPaymentMethod  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account used to perform the the request
#' var_payment_method_id <- 56 # integer | The payment method to return details on. If this is not set, then the user's default payment method will be returned. (Optional)
#' var_get_current_balance <- "get_current_balance_example" # character | Determines whether to get the user's current balance for the requested payment method option (not all payment method options support this) (Optional)
#'
#' #Get Payment Method
#' api_instance <- BillingInfoApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPaymentMethod(var_account_id, payment_method_id = var_payment_method_id, get_current_balance = var_get_current_balancedata_file = "result.txt")
#' result <- api_instance$GetPaymentMethod(var_account_id, payment_method_id = var_payment_method_id, get_current_balance = var_get_current_balance)
#' dput(result)
#'
#'
#' ####################  SearchPaymentMethod  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | Account Id to search on
#' var_provider <- "AUTHORIZE_NET" # character | Provider to search on (Optional)
#' var_type <- "type_example" # character | the type to search on (Optional)
#' var_keyword <- "keyword_example" # character | the keyword to search on (Optional)
#' var_sort_field <- "UPDATED" # character | the sort field to use for the search (Optional)
#' var_descending <- TRUE # character | if the results should be in descending order (Optional)
#' var_start <- 0 # integer | the start of the search (Optional)
#' var_limit <- 5 # integer | the limit of the search (Optional)
#'
#' #Search Payment Methods
#' api_instance <- BillingInfoApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchPaymentMethod(var_account_id, provider = var_provider, type = var_type, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchPaymentMethod(var_account_id, provider = var_provider, type = var_type, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
BillingInfoApi <- R6::R6Class(
  "BillingInfoApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new BillingInfoApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Update Payment Method
    #'
    #' @param account_id The account used to perform the the request
    #' @param payment_method_id (optional) Payment Method Id
    #' @param account_name (optional) the name of the account
    #' @param first_name (optional) First Name that the account is filed as
    #' @param last_name (optional) Last Name that the account is filed as
    #' @param address (optional) Address that the account is filed as
    #' @param city (optional) City that the account is filed as
    #' @param state (optional) State that the account is filed as
    #' @param postal_code (optional) Postal Code that the account is filed as
    #' @param country (optional) Country that the account is filed as
    #' @param phone (optional) Phone that the account is filed as
    #' @param credit_card_number (optional) The full credit card number to store on file
    #' @param expiration_date (optional) The credit card expiration date YYYY-MM
    #' @param ccv (optional) The 3 digit confirmation code
    #' @param account_number (optional) The bank account number
    #' @param bank_name (optional) The bank name
    #' @param routing_number (optional) Routing Number
    #' @param default_payment_method (optional) Default Payment Method
    #' @param payment_method_nickname (optional) Payment Method Nickname
    #' @param tax_id (optional) Tax Id
    #' @param provider_customer_profile_id (optional) Provider customer profile Id
    #' @param provider_payment_profile_id (optional) Provider customer payment profile Id
    #' @param meta_data (optional) Meta Data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PaymentTypesResponse
    AddPaymentMethod = function(account_id, payment_method_id = NULL, account_name = NULL, first_name = NULL, last_name = NULL, address = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, phone = NULL, credit_card_number = NULL, expiration_date = NULL, ccv = NULL, account_number = NULL, bank_name = NULL, routing_number = NULL, default_payment_method = NULL, payment_method_nickname = NULL, tax_id = NULL, provider_customer_profile_id = NULL, provider_payment_profile_id = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AddPaymentMethodWithHttpInfo(account_id, payment_method_id, account_name, first_name, last_name, address, city, state, postal_code, country, phone, credit_card_number, expiration_date, ccv, account_number, bank_name, routing_number, default_payment_method, payment_method_nickname, tax_id, provider_customer_profile_id, provider_payment_profile_id, meta_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Payment Method
    #'
    #' @param account_id The account used to perform the the request
    #' @param payment_method_id (optional) Payment Method Id
    #' @param account_name (optional) the name of the account
    #' @param first_name (optional) First Name that the account is filed as
    #' @param last_name (optional) Last Name that the account is filed as
    #' @param address (optional) Address that the account is filed as
    #' @param city (optional) City that the account is filed as
    #' @param state (optional) State that the account is filed as
    #' @param postal_code (optional) Postal Code that the account is filed as
    #' @param country (optional) Country that the account is filed as
    #' @param phone (optional) Phone that the account is filed as
    #' @param credit_card_number (optional) The full credit card number to store on file
    #' @param expiration_date (optional) The credit card expiration date YYYY-MM
    #' @param ccv (optional) The 3 digit confirmation code
    #' @param account_number (optional) The bank account number
    #' @param bank_name (optional) The bank name
    #' @param routing_number (optional) Routing Number
    #' @param default_payment_method (optional) Default Payment Method
    #' @param payment_method_nickname (optional) Payment Method Nickname
    #' @param tax_id (optional) Tax Id
    #' @param provider_customer_profile_id (optional) Provider customer profile Id
    #' @param provider_payment_profile_id (optional) Provider customer payment profile Id
    #' @param meta_data (optional) Meta Data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PaymentTypesResponse) with additional information such as HTTP status code, headers
    AddPaymentMethodWithHttpInfo = function(account_id, payment_method_id = NULL, account_name = NULL, first_name = NULL, last_name = NULL, address = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, phone = NULL, credit_card_number = NULL, expiration_date = NULL, ccv = NULL, account_number = NULL, bank_name = NULL, routing_number = NULL, default_payment_method = NULL, payment_method_nickname = NULL, tax_id = NULL, provider_customer_profile_id = NULL, provider_payment_profile_id = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BillingInfoApi$AddPaymentMethod, `account_id` is not nullable")
      }

      if (!missing(`payment_method_id`) && is.null(`payment_method_id`)) {
        stop("Invalid value for `payment_method_id` when calling BillingInfoApi$AddPaymentMethod, `payment_method_id` is not nullable")
      }

      if (!missing(`account_name`) && is.null(`account_name`)) {
        stop("Invalid value for `account_name` when calling BillingInfoApi$AddPaymentMethod, `account_name` is not nullable")
      }

      if (!missing(`first_name`) && is.null(`first_name`)) {
        stop("Invalid value for `first_name` when calling BillingInfoApi$AddPaymentMethod, `first_name` is not nullable")
      }

      if (!missing(`last_name`) && is.null(`last_name`)) {
        stop("Invalid value for `last_name` when calling BillingInfoApi$AddPaymentMethod, `last_name` is not nullable")
      }

      if (!missing(`address`) && is.null(`address`)) {
        stop("Invalid value for `address` when calling BillingInfoApi$AddPaymentMethod, `address` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling BillingInfoApi$AddPaymentMethod, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling BillingInfoApi$AddPaymentMethod, `state` is not nullable")
      }

      if (!missing(`postal_code`) && is.null(`postal_code`)) {
        stop("Invalid value for `postal_code` when calling BillingInfoApi$AddPaymentMethod, `postal_code` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling BillingInfoApi$AddPaymentMethod, `country` is not nullable")
      }

      if (!missing(`phone`) && is.null(`phone`)) {
        stop("Invalid value for `phone` when calling BillingInfoApi$AddPaymentMethod, `phone` is not nullable")
      }

      if (!missing(`credit_card_number`) && is.null(`credit_card_number`)) {
        stop("Invalid value for `credit_card_number` when calling BillingInfoApi$AddPaymentMethod, `credit_card_number` is not nullable")
      }

      if (!missing(`expiration_date`) && is.null(`expiration_date`)) {
        stop("Invalid value for `expiration_date` when calling BillingInfoApi$AddPaymentMethod, `expiration_date` is not nullable")
      }

      if (!missing(`ccv`) && is.null(`ccv`)) {
        stop("Invalid value for `ccv` when calling BillingInfoApi$AddPaymentMethod, `ccv` is not nullable")
      }

      if (!missing(`account_number`) && is.null(`account_number`)) {
        stop("Invalid value for `account_number` when calling BillingInfoApi$AddPaymentMethod, `account_number` is not nullable")
      }

      if (!missing(`bank_name`) && is.null(`bank_name`)) {
        stop("Invalid value for `bank_name` when calling BillingInfoApi$AddPaymentMethod, `bank_name` is not nullable")
      }

      if (!missing(`routing_number`) && is.null(`routing_number`)) {
        stop("Invalid value for `routing_number` when calling BillingInfoApi$AddPaymentMethod, `routing_number` is not nullable")
      }

      if (!missing(`default_payment_method`) && is.null(`default_payment_method`)) {
        stop("Invalid value for `default_payment_method` when calling BillingInfoApi$AddPaymentMethod, `default_payment_method` is not nullable")
      }

      if (!missing(`payment_method_nickname`) && is.null(`payment_method_nickname`)) {
        stop("Invalid value for `payment_method_nickname` when calling BillingInfoApi$AddPaymentMethod, `payment_method_nickname` is not nullable")
      }

      if (!missing(`tax_id`) && is.null(`tax_id`)) {
        stop("Invalid value for `tax_id` when calling BillingInfoApi$AddPaymentMethod, `tax_id` is not nullable")
      }

      if (!missing(`provider_customer_profile_id`) && is.null(`provider_customer_profile_id`)) {
        stop("Invalid value for `provider_customer_profile_id` when calling BillingInfoApi$AddPaymentMethod, `provider_customer_profile_id` is not nullable")
      }

      if (!missing(`provider_payment_profile_id`) && is.null(`provider_payment_profile_id`)) {
        stop("Invalid value for `provider_payment_profile_id` when calling BillingInfoApi$AddPaymentMethod, `provider_payment_profile_id` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling BillingInfoApi$AddPaymentMethod, `meta_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["paymentMethodId"]] <- `payment_method_id`

      query_params[["accountName"]] <- `account_name`

      query_params[["firstName"]] <- `first_name`

      query_params[["lastName"]] <- `last_name`

      query_params[["address"]] <- `address`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["postalCode"]] <- `postal_code`

      query_params[["country"]] <- `country`

      query_params[["phone"]] <- `phone`

      query_params[["creditCardNumber"]] <- `credit_card_number`

      query_params[["expirationDate"]] <- `expiration_date`

      query_params[["ccv"]] <- `ccv`

      query_params[["accountNumber"]] <- `account_number`

      query_params[["bankName"]] <- `bank_name`

      query_params[["routingNumber"]] <- `routing_number`

      query_params[["defaultPaymentMethod"]] <- `default_payment_method`

      query_params[["paymentMethodNickname"]] <- `payment_method_nickname`

      query_params[["taxId"]] <- `tax_id`

      query_params[["providerCustomerProfileId"]] <- `provider_customer_profile_id`

      query_params[["providerPaymentProfileId"]] <- `provider_payment_profile_id`

      query_params[["metaData"]] <- `meta_data`

      local_var_url_path <- "/billing/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaymentTypesResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Payment Method
    #'
    #' @param account_id The account used to perform the the request
    #' @param account_name (optional) Account Name of the credit card user
    #' @param first_name (optional) The first name on the credit card
    #' @param last_name (optional) The last name on the credit card
    #' @param address (optional) The billing address of the credit card
    #' @param city (optional) The billing city of the credit card
    #' @param state (optional) The billing state of the credit card
    #' @param postal_code (optional) The billing zip code of the credit card
    #' @param country (optional) Country of the credit card
    #' @param phone (optional) The billing phone of the credit card
    #' @param credit_card_number (optional) The full credit card number to store on file
    #' @param expiration_date (optional) The credit card expiration date YYYY-MM
    #' @param ccv (optional) The 3 digit confirmation code
    #' @param account_number (optional) The bank account number
    #' @param bank_name (optional) The bank name
    #' @param routing_number (optional) The bank routing number
    #' @param payment_method_nickname (optional) The nickname to give the payment method
    #' @param tax_id (optional) Tax Id
    #' @param default_payment_method (optional) Whether this should be the default payment method (default value: TRUE)
    #' @param auth_token (optional) An authorization token for providers that provide this (like Amazon Payments)
    #' @param provider (optional) The payment provider (see PaymentMethodProvider) (default value: "AUTHORIZE_NET")
    #' @param provider_customer_profile_id (optional) Provider customer profile Id
    #' @param provider_payment_profile_id (optional) Provider customer payment profile Id
    #' @param meta_data (optional) Meta Data
    #' @param app_key (optional) Application Key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PaymentTypesResponse
    CreatePaymentMethod = function(account_id, account_name = NULL, first_name = NULL, last_name = NULL, address = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, phone = NULL, credit_card_number = NULL, expiration_date = NULL, ccv = NULL, account_number = NULL, bank_name = NULL, routing_number = NULL, payment_method_nickname = NULL, tax_id = NULL, default_payment_method = TRUE, auth_token = NULL, provider = "AUTHORIZE_NET", provider_customer_profile_id = NULL, provider_payment_profile_id = NULL, meta_data = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreatePaymentMethodWithHttpInfo(account_id, account_name, first_name, last_name, address, city, state, postal_code, country, phone, credit_card_number, expiration_date, ccv, account_number, bank_name, routing_number, payment_method_nickname, tax_id, default_payment_method, auth_token, provider, provider_customer_profile_id, provider_payment_profile_id, meta_data, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Payment Method
    #'
    #' @param account_id The account used to perform the the request
    #' @param account_name (optional) Account Name of the credit card user
    #' @param first_name (optional) The first name on the credit card
    #' @param last_name (optional) The last name on the credit card
    #' @param address (optional) The billing address of the credit card
    #' @param city (optional) The billing city of the credit card
    #' @param state (optional) The billing state of the credit card
    #' @param postal_code (optional) The billing zip code of the credit card
    #' @param country (optional) Country of the credit card
    #' @param phone (optional) The billing phone of the credit card
    #' @param credit_card_number (optional) The full credit card number to store on file
    #' @param expiration_date (optional) The credit card expiration date YYYY-MM
    #' @param ccv (optional) The 3 digit confirmation code
    #' @param account_number (optional) The bank account number
    #' @param bank_name (optional) The bank name
    #' @param routing_number (optional) The bank routing number
    #' @param payment_method_nickname (optional) The nickname to give the payment method
    #' @param tax_id (optional) Tax Id
    #' @param default_payment_method (optional) Whether this should be the default payment method (default value: TRUE)
    #' @param auth_token (optional) An authorization token for providers that provide this (like Amazon Payments)
    #' @param provider (optional) The payment provider (see PaymentMethodProvider) (default value: "AUTHORIZE_NET")
    #' @param provider_customer_profile_id (optional) Provider customer profile Id
    #' @param provider_payment_profile_id (optional) Provider customer payment profile Id
    #' @param meta_data (optional) Meta Data
    #' @param app_key (optional) Application Key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PaymentTypesResponse) with additional information such as HTTP status code, headers
    CreatePaymentMethodWithHttpInfo = function(account_id, account_name = NULL, first_name = NULL, last_name = NULL, address = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, phone = NULL, credit_card_number = NULL, expiration_date = NULL, ccv = NULL, account_number = NULL, bank_name = NULL, routing_number = NULL, payment_method_nickname = NULL, tax_id = NULL, default_payment_method = TRUE, auth_token = NULL, provider = "AUTHORIZE_NET", provider_customer_profile_id = NULL, provider_payment_profile_id = NULL, meta_data = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BillingInfoApi$CreatePaymentMethod, `account_id` is not nullable")
      }

      if (!missing(`account_name`) && is.null(`account_name`)) {
        stop("Invalid value for `account_name` when calling BillingInfoApi$CreatePaymentMethod, `account_name` is not nullable")
      }

      if (!missing(`first_name`) && is.null(`first_name`)) {
        stop("Invalid value for `first_name` when calling BillingInfoApi$CreatePaymentMethod, `first_name` is not nullable")
      }

      if (!missing(`last_name`) && is.null(`last_name`)) {
        stop("Invalid value for `last_name` when calling BillingInfoApi$CreatePaymentMethod, `last_name` is not nullable")
      }

      if (!missing(`address`) && is.null(`address`)) {
        stop("Invalid value for `address` when calling BillingInfoApi$CreatePaymentMethod, `address` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling BillingInfoApi$CreatePaymentMethod, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling BillingInfoApi$CreatePaymentMethod, `state` is not nullable")
      }

      if (!missing(`postal_code`) && is.null(`postal_code`)) {
        stop("Invalid value for `postal_code` when calling BillingInfoApi$CreatePaymentMethod, `postal_code` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling BillingInfoApi$CreatePaymentMethod, `country` is not nullable")
      }

      if (!missing(`phone`) && is.null(`phone`)) {
        stop("Invalid value for `phone` when calling BillingInfoApi$CreatePaymentMethod, `phone` is not nullable")
      }

      if (!missing(`credit_card_number`) && is.null(`credit_card_number`)) {
        stop("Invalid value for `credit_card_number` when calling BillingInfoApi$CreatePaymentMethod, `credit_card_number` is not nullable")
      }

      if (!missing(`expiration_date`) && is.null(`expiration_date`)) {
        stop("Invalid value for `expiration_date` when calling BillingInfoApi$CreatePaymentMethod, `expiration_date` is not nullable")
      }

      if (!missing(`ccv`) && is.null(`ccv`)) {
        stop("Invalid value for `ccv` when calling BillingInfoApi$CreatePaymentMethod, `ccv` is not nullable")
      }

      if (!missing(`account_number`) && is.null(`account_number`)) {
        stop("Invalid value for `account_number` when calling BillingInfoApi$CreatePaymentMethod, `account_number` is not nullable")
      }

      if (!missing(`bank_name`) && is.null(`bank_name`)) {
        stop("Invalid value for `bank_name` when calling BillingInfoApi$CreatePaymentMethod, `bank_name` is not nullable")
      }

      if (!missing(`routing_number`) && is.null(`routing_number`)) {
        stop("Invalid value for `routing_number` when calling BillingInfoApi$CreatePaymentMethod, `routing_number` is not nullable")
      }

      if (!missing(`payment_method_nickname`) && is.null(`payment_method_nickname`)) {
        stop("Invalid value for `payment_method_nickname` when calling BillingInfoApi$CreatePaymentMethod, `payment_method_nickname` is not nullable")
      }

      if (!missing(`tax_id`) && is.null(`tax_id`)) {
        stop("Invalid value for `tax_id` when calling BillingInfoApi$CreatePaymentMethod, `tax_id` is not nullable")
      }

      if (!missing(`default_payment_method`) && is.null(`default_payment_method`)) {
        stop("Invalid value for `default_payment_method` when calling BillingInfoApi$CreatePaymentMethod, `default_payment_method` is not nullable")
      }

      if (!missing(`auth_token`) && is.null(`auth_token`)) {
        stop("Invalid value for `auth_token` when calling BillingInfoApi$CreatePaymentMethod, `auth_token` is not nullable")
      }

      if (!missing(`provider`) && is.null(`provider`)) {
        stop("Invalid value for `provider` when calling BillingInfoApi$CreatePaymentMethod, `provider` is not nullable")
      }

      if (!missing(`provider_customer_profile_id`) && is.null(`provider_customer_profile_id`)) {
        stop("Invalid value for `provider_customer_profile_id` when calling BillingInfoApi$CreatePaymentMethod, `provider_customer_profile_id` is not nullable")
      }

      if (!missing(`provider_payment_profile_id`) && is.null(`provider_payment_profile_id`)) {
        stop("Invalid value for `provider_payment_profile_id` when calling BillingInfoApi$CreatePaymentMethod, `provider_payment_profile_id` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling BillingInfoApi$CreatePaymentMethod, `meta_data` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling BillingInfoApi$CreatePaymentMethod, `app_key` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["accountName"]] <- `account_name`

      query_params[["firstName"]] <- `first_name`

      query_params[["lastName"]] <- `last_name`

      query_params[["address"]] <- `address`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["postalCode"]] <- `postal_code`

      query_params[["country"]] <- `country`

      query_params[["phone"]] <- `phone`

      query_params[["creditCardNumber"]] <- `credit_card_number`

      query_params[["expirationDate"]] <- `expiration_date`

      query_params[["ccv"]] <- `ccv`

      query_params[["accountNumber"]] <- `account_number`

      query_params[["bankName"]] <- `bank_name`

      query_params[["routingNumber"]] <- `routing_number`

      query_params[["paymentMethodNickname"]] <- `payment_method_nickname`

      query_params[["taxId"]] <- `tax_id`

      query_params[["defaultPaymentMethod"]] <- `default_payment_method`

      query_params[["authToken"]] <- `auth_token`

      query_params[["provider"]] <- `provider`

      query_params[["providerCustomerProfileId"]] <- `provider_customer_profile_id`

      query_params[["providerPaymentProfileId"]] <- `provider_payment_profile_id`

      query_params[["metaData"]] <- `meta_data`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/billing/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaymentTypesResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Smart Contract
    #'
    #' @param account_id The account used to perform the the request
    #' @param token_name The token name
    #' @param token_symbol The token symbol
    #' @param payment_method_id (optional) The payment method to return details on. If this is not set, then the user's default payment method will be returned.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PaymentTypesResponse
    CreateSmartContract = function(account_id, token_name, token_symbol, payment_method_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateSmartContractWithHttpInfo(account_id, token_name, token_symbol, payment_method_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Smart Contract
    #'
    #' @param account_id The account used to perform the the request
    #' @param token_name The token name
    #' @param token_symbol The token symbol
    #' @param payment_method_id (optional) The payment method to return details on. If this is not set, then the user's default payment method will be returned.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PaymentTypesResponse) with additional information such as HTTP status code, headers
    CreateSmartContractWithHttpInfo = function(account_id, token_name, token_symbol, payment_method_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`token_name`)) {
        stop("Missing required parameter `token_name`.")
      }

      if (missing(`token_symbol`)) {
        stop("Missing required parameter `token_symbol`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BillingInfoApi$CreateSmartContract, `account_id` is not nullable")
      }

      if (!missing(`token_name`) && is.null(`token_name`)) {
        stop("Invalid value for `token_name` when calling BillingInfoApi$CreateSmartContract, `token_name` is not nullable")
      }

      if (!missing(`token_symbol`) && is.null(`token_symbol`)) {
        stop("Invalid value for `token_symbol` when calling BillingInfoApi$CreateSmartContract, `token_symbol` is not nullable")
      }

      if (!missing(`payment_method_id`) && is.null(`payment_method_id`)) {
        stop("Invalid value for `payment_method_id` when calling BillingInfoApi$CreateSmartContract, `payment_method_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["paymentMethodId"]] <- `payment_method_id`

      query_params[["tokenName"]] <- `token_name`

      query_params[["tokenSymbol"]] <- `token_symbol`

      local_var_url_path <- "/billing/crypto/transfer"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaymentTypesResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Crypto Balances
    #'
    #' @param account_id The account used to perform the the request
    #' @param owner_account_id (optional) The account to retreive balances for
    #' @param payment_method_id (optional) The payment method to return details on. If this is not set, then the user's default payment method will be returned.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PaymentTypesResponse
    GetCryptoBalance = function(account_id, owner_account_id = NULL, payment_method_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetCryptoBalanceWithHttpInfo(account_id, owner_account_id, payment_method_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Crypto Balances
    #'
    #' @param account_id The account used to perform the the request
    #' @param owner_account_id (optional) The account to retreive balances for
    #' @param payment_method_id (optional) The payment method to return details on. If this is not set, then the user's default payment method will be returned.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PaymentTypesResponse) with additional information such as HTTP status code, headers
    GetCryptoBalanceWithHttpInfo = function(account_id, owner_account_id = NULL, payment_method_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BillingInfoApi$GetCryptoBalance, `account_id` is not nullable")
      }

      if (!missing(`owner_account_id`) && is.null(`owner_account_id`)) {
        stop("Invalid value for `owner_account_id` when calling BillingInfoApi$GetCryptoBalance, `owner_account_id` is not nullable")
      }

      if (!missing(`payment_method_id`) && is.null(`payment_method_id`)) {
        stop("Invalid value for `payment_method_id` when calling BillingInfoApi$GetCryptoBalance, `payment_method_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["ownerAccountId"]] <- `owner_account_id`

      query_params[["paymentMethodId"]] <- `payment_method_id`

      local_var_url_path <- "/billing/crypto/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaymentTypesResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Payment Method
    #'
    #' @param account_id The account used to perform the the request
    #' @param payment_method_id (optional) The payment method to return details on. If this is not set, then the user's default payment method will be returned.
    #' @param get_current_balance (optional) Determines whether to get the user's current balance for the requested payment method option (not all payment method options support this)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PaymentTypesResponse
    GetPaymentMethod = function(account_id, payment_method_id = NULL, get_current_balance = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetPaymentMethodWithHttpInfo(account_id, payment_method_id, get_current_balance, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Payment Method
    #'
    #' @param account_id The account used to perform the the request
    #' @param payment_method_id (optional) The payment method to return details on. If this is not set, then the user's default payment method will be returned.
    #' @param get_current_balance (optional) Determines whether to get the user's current balance for the requested payment method option (not all payment method options support this)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PaymentTypesResponse) with additional information such as HTTP status code, headers
    GetPaymentMethodWithHttpInfo = function(account_id, payment_method_id = NULL, get_current_balance = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BillingInfoApi$GetPaymentMethod, `account_id` is not nullable")
      }

      if (!missing(`payment_method_id`) && is.null(`payment_method_id`)) {
        stop("Invalid value for `payment_method_id` when calling BillingInfoApi$GetPaymentMethod, `payment_method_id` is not nullable")
      }

      if (!missing(`get_current_balance`) && is.null(`get_current_balance`)) {
        stop("Invalid value for `get_current_balance` when calling BillingInfoApi$GetPaymentMethod, `get_current_balance` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["paymentMethodId"]] <- `payment_method_id`

      query_params[["getCurrentBalance"]] <- `get_current_balance`

      local_var_url_path <- "/billing/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaymentTypesResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Payment Methods
    #'
    #' @param account_id Account Id to search on
    #' @param provider (optional) Provider to search on (default value: "AUTHORIZE_NET")
    #' @param type (optional) the type to search on
    #' @param keyword (optional) the keyword to search on
    #' @param sort_field (optional) the sort field to use for the search (default value: "UPDATED")
    #' @param descending (optional) if the results should be in descending order (default value: TRUE)
    #' @param start (optional) the start of the search (default value: 0)
    #' @param limit (optional) the limit of the search (default value: 5)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PaymentTypesResponse
    SearchPaymentMethod = function(account_id, provider = "AUTHORIZE_NET", type = NULL, keyword = NULL, sort_field = "UPDATED", descending = TRUE, start = 0, limit = 5, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchPaymentMethodWithHttpInfo(account_id, provider, type, keyword, sort_field, descending, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Payment Methods
    #'
    #' @param account_id Account Id to search on
    #' @param provider (optional) Provider to search on (default value: "AUTHORIZE_NET")
    #' @param type (optional) the type to search on
    #' @param keyword (optional) the keyword to search on
    #' @param sort_field (optional) the sort field to use for the search (default value: "UPDATED")
    #' @param descending (optional) if the results should be in descending order (default value: TRUE)
    #' @param start (optional) the start of the search (default value: 0)
    #' @param limit (optional) the limit of the search (default value: 5)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PaymentTypesResponse) with additional information such as HTTP status code, headers
    SearchPaymentMethodWithHttpInfo = function(account_id, provider = "AUTHORIZE_NET", type = NULL, keyword = NULL, sort_field = "UPDATED", descending = TRUE, start = 0, limit = 5, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BillingInfoApi$SearchPaymentMethod, `account_id` is not nullable")
      }

      if (!missing(`provider`) && is.null(`provider`)) {
        stop("Invalid value for `provider` when calling BillingInfoApi$SearchPaymentMethod, `provider` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling BillingInfoApi$SearchPaymentMethod, `type` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling BillingInfoApi$SearchPaymentMethod, `keyword` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling BillingInfoApi$SearchPaymentMethod, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling BillingInfoApi$SearchPaymentMethod, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling BillingInfoApi$SearchPaymentMethod, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling BillingInfoApi$SearchPaymentMethod, `limit` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["provider"]] <- `provider`

      query_params[["type"]] <- `type`

      query_params[["keyword"]] <- `keyword`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/billing/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PaymentTypesResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
