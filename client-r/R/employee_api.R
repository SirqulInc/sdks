#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Employee operations
#' @description EmployeeApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AssignEmployee  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_manager_account_id <- 56 # integer | The account id of the manager to assign under
#' var_employee_account_id <- 56 # integer | The account id of the user to be assigned as employee
#' var_role <- "role_example" # character | The role to assign to the employee (e.g. RETAILER or RETAILER_LIMITED) (Optional)
#'
#' #Assign Employee
#' api_instance <- EmployeeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssignEmployee(var_account_id, var_manager_account_id, var_employee_account_id, role = var_roledata_file = "result.txt")
#' result <- api_instance$AssignEmployee(var_account_id, var_manager_account_id, var_employee_account_id, role = var_role)
#' dput(result)
#'
#'
#' ####################  AssignToLocationEmployee  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_retailer_location_id <- 56 # integer | The retailer location to apply the change to
#' var_employee_account_id <- 56 # integer | The account id of the user to apply the change to (Optional)
#' var_assign <- TRUE # character | If true (default) assign to the location, otherwise remove from the retailer (Optional)
#'
#' #Assign Employee to Location
#' api_instance <- EmployeeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssignToLocationEmployee(var_account_id, var_retailer_location_id, employee_account_id = var_employee_account_id, assign = var_assigndata_file = "result.txt")
#' result <- api_instance$AssignToLocationEmployee(var_account_id, var_retailer_location_id, employee_account_id = var_employee_account_id, assign = var_assign)
#' dput(result)
#'
#'
#' ####################  CreateEmployee  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_manager_account_id <- 56 # integer | The account id of the manager to assign under
#' var_username <- "username_example" # character | The username/email for the new user. This must be unique across the entire the system.
#' var_password <- "password_example" # character | The password for the new user
#' var_name <- "name_example" # character | a name field (Optional)
#' var_prefix_name <- "prefix_name_example" # character | The name prefix; Mr, Mrs, etc (Optional)
#' var_first_name <- "first_name_example" # character | The first name (Optional)
#' var_middle_name <- "middle_name_example" # character | The middle name (Optional)
#' var_last_name <- "last_name_example" # character | The last name (Optional)
#' var_suffix_name <- "suffix_name_example" # character | The name suffix; Jr, Sr, III, etc (Optional)
#' var_title <- "title_example" # character | The title of the user (Optional)
#' var_about_us <- "about_us_example" # character | Additional about/biography text (Optional)
#' var_asset_id <- 56 # integer | the asset id to set the user's profile image (Optional)
#' var_gender <- "gender_example" # character | The gender (Optional)
#' var_home_phone <- "home_phone_example" # character | The home phone number (Optional)
#' var_cell_phone <- "cell_phone_example" # character | The cellular phone number (Optional)
#' var_cell_phone_carrier <- "cell_phone_carrier_example" # character | The cellular service provider. This is required for sending SMS. Leave this empty if the provider is not on the list of supported carriers. (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone number (Optional)
#' var_email_address <- "email_address_example" # character | The user's contact email address (NOT the username) (Optional)
#' var_street_address <- "street_address_example" # character | The street address of the user's contact location (Optional)
#' var_street_address2 <- "street_address2_example" # character | Additional address information (such as a suite number, floor number, building name, or PO Box) (Optional)
#' var_city <- "city_example" # character | The city of the user's contact location (Optional)
#' var_state <- "state_example" # character | The state of the user's contact location (Optional)
#' var_zipcode <- "zipcode_example" # character | The zipcode of the user's contact location (Optional)
#' var_country <- "country_example" # character | The country of the user's contact location (Optional)
#' var_role <- "role_example" # character | The role; RETAILER or RETAILER_LIMITED, defaulted to RETAILER_LIMITED (Optional)
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | the retailer location IDs the employee is associated with (Optional)
#' var_settings_app_key <- "settings_app_key_example" # character | Determines whether to return the application settings for the employee for a particular application (Optional)
#' var_app_blob <- "app_blob_example" # character | external custom client defined data (per Application) (Optional)
#' var_assigned_device_id <- "assigned_device_id_example" # character | The device id to assign to the user (used for IPS beacon tracking) (Optional)
#'
#' #Create Employee
#' api_instance <- EmployeeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateEmployee(var_account_id, var_manager_account_id, var_username, var_password, name = var_name, prefix_name = var_prefix_name, first_name = var_first_name, middle_name = var_middle_name, last_name = var_last_name, suffix_name = var_suffix_name, title = var_title, about_us = var_about_us, asset_id = var_asset_id, gender = var_gender, home_phone = var_home_phone, cell_phone = var_cell_phone, cell_phone_carrier = var_cell_phone_carrier, business_phone = var_business_phone, email_address = var_email_address, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, zipcode = var_zipcode, country = var_country, role = var_role, retailer_location_ids = var_retailer_location_ids, settings_app_key = var_settings_app_key, app_blob = var_app_blob, assigned_device_id = var_assigned_device_iddata_file = "result.txt")
#' result <- api_instance$CreateEmployee(var_account_id, var_manager_account_id, var_username, var_password, name = var_name, prefix_name = var_prefix_name, first_name = var_first_name, middle_name = var_middle_name, last_name = var_last_name, suffix_name = var_suffix_name, title = var_title, about_us = var_about_us, asset_id = var_asset_id, gender = var_gender, home_phone = var_home_phone, cell_phone = var_cell_phone, cell_phone_carrier = var_cell_phone_carrier, business_phone = var_business_phone, email_address = var_email_address, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, zipcode = var_zipcode, country = var_country, role = var_role, retailer_location_ids = var_retailer_location_ids, settings_app_key = var_settings_app_key, app_blob = var_app_blob, assigned_device_id = var_assigned_device_id)
#' dput(result)
#'
#'
#' ####################  DeleteEmployee  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_employee_account_id <- 56 # integer | the id of the employee to delete
#'
#' #Delete Employee
#' api_instance <- EmployeeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteEmployee(var_account_id, var_employee_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteEmployee(var_account_id, var_employee_account_id)
#' dput(result)
#'
#'
#' ####################  GetEmployee  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of logged in user
#' var_employee_account_id <- 56 # integer | the id of the employee account to get
#' var_settings_app_key <- "settings_app_key_example" # character | Determines whether to return the application settings for the employee for a particular application (Optional)
#'
#' #Get Employee
#' api_instance <- EmployeeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetEmployee(var_account_id, var_employee_account_id, settings_app_key = var_settings_app_keydata_file = "result.txt")
#' result <- api_instance$GetEmployee(var_account_id, var_employee_account_id, settings_app_key = var_settings_app_key)
#' dput(result)
#'
#'
#' ####################  SearchEmployees  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_role <- "role_example" # character | The role to limit the search to: RETAILER or RETAILER_LIMITED. Leave empty to search on both roles. (Optional)
#' var_retailer_id <- 56 # integer | Filters employees by retailer (Optional)
#' var_retailer_location_id <- 56 # integer | Filter employees by retailer locations (Optional)
#' var_q <- "q_example" # character | Deprecated parameter (Optional)
#' var_keyword <- "keyword_example" # character | an optional keyword to search on; ignored if empty (Optional)
#' var_sort_field <- "sort_field_example" # character | The field to sort by. Possible values include: DISPLAY, CREATED, UPDATED, ACTIVE, DELETED, LAST_LOGGED_IN, CONTACT_EMAIL, RETAILER_LOCATION_NAME, RETAILER_NAME (Optional)
#' var_descending <- FALSE # character | Determines whether the sorted list is in descending or ascending order (Optional)
#' var__i <- 56 # integer | Deprecated parameter (Optional)
#' var_start <- 0 # integer | Start the result set at some index (Optional)
#' var__l <- 56 # integer | Deprecated parameter (Optional)
#' var_limit <- 20 # integer | Limit the result to some number (Optional)
#' var_active_only <- TRUE # character | Filter results to only return active employees (Optional)
#' var_managed_only <- "managed_only_example" # character | Filter results to only employees that you manage (Optional)
#' var_settings_app_key <- "settings_app_key_example" # character | Determines whether to return the application settings for the employee for a particular application (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category ids to filter results (Optional)
#' var_query <- "query_example" # character | Legacy/reporting query parameter used for formatting employee responses (Optional)
#'
#' #Search Employees
#' api_instance <- EmployeeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchEmployees(var_account_id, role = var_role, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, q = var_q, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, _i = var__i, start = var_start, _l = var__l, limit = var_limit, active_only = var_active_only, managed_only = var_managed_only, settings_app_key = var_settings_app_key, category_ids = var_category_ids, query = var_querydata_file = "result.txt")
#' result <- api_instance$SearchEmployees(var_account_id, role = var_role, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, q = var_q, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, _i = var__i, start = var_start, _l = var__l, limit = var_limit, active_only = var_active_only, managed_only = var_managed_only, settings_app_key = var_settings_app_key, category_ids = var_category_ids, query = var_query)
#' dput(result)
#'
#'
#' ####################  UnassignEmployee  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_employee_account_id <- 56 # integer | The account id of the user to be unassigned
#'
#' #Unassign Employee
#' api_instance <- EmployeeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UnassignEmployee(var_account_id, var_employee_account_iddata_file = "result.txt")
#' result <- api_instance$UnassignEmployee(var_account_id, var_employee_account_id)
#' dput(result)
#'
#'
#' ####################  UpdateEmployee  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_employee_account_id <- 56 # integer | the id of the employee account
#' var_manager_account_id <- 56 # integer | The account id of the manager to assign under (Optional)
#' var_name <- "name_example" # character | a name field (Optional)
#' var_prefix_name <- "prefix_name_example" # character | The name prefix; Mr, Mrs, etc (Optional)
#' var_first_name <- "first_name_example" # character | The first name (Optional)
#' var_middle_name <- "middle_name_example" # character | The middle name (Optional)
#' var_last_name <- "last_name_example" # character | The last name (Optional)
#' var_suffix_name <- "suffix_name_example" # character | The name suffix; Jr, Sr, III, etc (Optional)
#' var_title <- "title_example" # character | The title of the user (Optional)
#' var_asset_id <- 56 # integer | the asset id to set the user's profile image (Optional)
#' var_gender <- "gender_example" # character | The gender {MALE, FEMALE, ANY} (Optional)
#' var_home_phone <- "home_phone_example" # character | The home phone number (Optional)
#' var_cell_phone <- "cell_phone_example" # character | The cellular phone number (Optional)
#' var_cell_phone_carrier <- "cell_phone_carrier_example" # character | The cellular service provider. This is required for sending SMS. Leave this empty if the provider is not on the list of supported carriers. Supported Carriers: {ATT, QWEST, T_MOBILE, VERIZON, SPRINT, VIRIGIN_MOBILE, NEXTEL, ALLTEL, METRO_PCS, POWERTEL, BOOST_MOBILE, SUNCOM, TRACFONE, US_CELLULAR} (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone number (Optional)
#' var_email_address <- "email_address_example" # character | The user's contact email address (NOT the username) (Optional)
#' var_street_address <- "street_address_example" # character | The street address of the user's contact location (Optional)
#' var_street_address2 <- "street_address2_example" # character | Additional address information (such as a suite number, floor number, building name, or PO Box) (Optional)
#' var_city <- "city_example" # character | The city of the user's contact location (Optional)
#' var_state <- "state_example" # character | The state of the user's contact location (Optional)
#' var_zipcode <- "zipcode_example" # character | The zipcode of the user's contact location (Optional)
#' var_country <- "country_example" # character | The country of the user's contact location (Optional)
#' var_role <- "role_example" # character | The role; RETAILER or RETAILER_LIMITED, defaulted to RETAILER_LIMITED (Optional)
#' var_active <- "active_example" # character | Sets whether the employee is active or inactive (Optional)
#' var_password <- "password_example" # character | Sets the password for the employee (Optional)
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Sets which retailer locations the employee is assigned to (Optional)
#' var_settings_app_key <- "settings_app_key_example" # character | Determines whether to return the application settings for the employee for a particular application (Optional)
#' var_app_blob <- "app_blob_example" # character | external custom client defined data (per Application) (Optional)
#' var_assigned_device_id <- "assigned_device_id_example" # character | The device id to assign to the user (used for IPS beacon tracking) (Optional)
#'
#' #Update Employee
#' api_instance <- EmployeeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateEmployee(var_account_id, var_employee_account_id, manager_account_id = var_manager_account_id, name = var_name, prefix_name = var_prefix_name, first_name = var_first_name, middle_name = var_middle_name, last_name = var_last_name, suffix_name = var_suffix_name, title = var_title, asset_id = var_asset_id, gender = var_gender, home_phone = var_home_phone, cell_phone = var_cell_phone, cell_phone_carrier = var_cell_phone_carrier, business_phone = var_business_phone, email_address = var_email_address, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, zipcode = var_zipcode, country = var_country, role = var_role, active = var_active, password = var_password, retailer_location_ids = var_retailer_location_ids, settings_app_key = var_settings_app_key, app_blob = var_app_blob, assigned_device_id = var_assigned_device_iddata_file = "result.txt")
#' result <- api_instance$UpdateEmployee(var_account_id, var_employee_account_id, manager_account_id = var_manager_account_id, name = var_name, prefix_name = var_prefix_name, first_name = var_first_name, middle_name = var_middle_name, last_name = var_last_name, suffix_name = var_suffix_name, title = var_title, asset_id = var_asset_id, gender = var_gender, home_phone = var_home_phone, cell_phone = var_cell_phone, cell_phone_carrier = var_cell_phone_carrier, business_phone = var_business_phone, email_address = var_email_address, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, zipcode = var_zipcode, country = var_country, role = var_role, active = var_active, password = var_password, retailer_location_ids = var_retailer_location_ids, settings_app_key = var_settings_app_key, app_blob = var_app_blob, assigned_device_id = var_assigned_device_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
EmployeeApi <- R6::R6Class(
  "EmployeeApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new EmployeeApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Assign Employee
    #'
    #' @param account_id The account id of the logged in user
    #' @param manager_account_id The account id of the manager to assign under
    #' @param employee_account_id The account id of the user to be assigned as employee
    #' @param role (optional) The role to assign to the employee (e.g. RETAILER or RETAILER_LIMITED)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return EmployeeResponse
    AssignEmployee = function(account_id, manager_account_id, employee_account_id, role = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AssignEmployeeWithHttpInfo(account_id, manager_account_id, employee_account_id, role, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Assign Employee
    #'
    #' @param account_id The account id of the logged in user
    #' @param manager_account_id The account id of the manager to assign under
    #' @param employee_account_id The account id of the user to be assigned as employee
    #' @param role (optional) The role to assign to the employee (e.g. RETAILER or RETAILER_LIMITED)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (EmployeeResponse) with additional information such as HTTP status code, headers
    AssignEmployeeWithHttpInfo = function(account_id, manager_account_id, employee_account_id, role = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`manager_account_id`)) {
        stop("Missing required parameter `manager_account_id`.")
      }

      if (missing(`employee_account_id`)) {
        stop("Missing required parameter `employee_account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EmployeeApi$AssignEmployee, `account_id` is not nullable")
      }

      if (!missing(`manager_account_id`) && is.null(`manager_account_id`)) {
        stop("Invalid value for `manager_account_id` when calling EmployeeApi$AssignEmployee, `manager_account_id` is not nullable")
      }

      if (!missing(`employee_account_id`) && is.null(`employee_account_id`)) {
        stop("Invalid value for `employee_account_id` when calling EmployeeApi$AssignEmployee, `employee_account_id` is not nullable")
      }

      if (!missing(`role`) && is.null(`role`)) {
        stop("Invalid value for `role` when calling EmployeeApi$AssignEmployee, `role` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["managerAccountId"]] <- `manager_account_id`

      query_params[["employeeAccountId"]] <- `employee_account_id`

      query_params[["role"]] <- `role`

      local_var_url_path <- "/employee/assign"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "EmployeeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Assign Employee to Location
    #'
    #' @param account_id The account id of the logged in user
    #' @param retailer_location_id The retailer location to apply the change to
    #' @param employee_account_id (optional) The account id of the user to apply the change to
    #' @param assign (optional) If true (default) assign to the location, otherwise remove from the retailer (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    AssignToLocationEmployee = function(account_id, retailer_location_id, employee_account_id = NULL, assign = TRUE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AssignToLocationEmployeeWithHttpInfo(account_id, retailer_location_id, employee_account_id, assign, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Assign Employee to Location
    #'
    #' @param account_id The account id of the logged in user
    #' @param retailer_location_id The retailer location to apply the change to
    #' @param employee_account_id (optional) The account id of the user to apply the change to
    #' @param assign (optional) If true (default) assign to the location, otherwise remove from the retailer (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    AssignToLocationEmployeeWithHttpInfo = function(account_id, retailer_location_id, employee_account_id = NULL, assign = TRUE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`retailer_location_id`)) {
        stop("Missing required parameter `retailer_location_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EmployeeApi$AssignToLocationEmployee, `account_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling EmployeeApi$AssignToLocationEmployee, `retailer_location_id` is not nullable")
      }

      if (!missing(`employee_account_id`) && is.null(`employee_account_id`)) {
        stop("Invalid value for `employee_account_id` when calling EmployeeApi$AssignToLocationEmployee, `employee_account_id` is not nullable")
      }

      if (!missing(`assign`) && is.null(`assign`)) {
        stop("Invalid value for `assign` when calling EmployeeApi$AssignToLocationEmployee, `assign` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["employeeAccountId"]] <- `employee_account_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["assign"]] <- `assign`

      local_var_url_path <- "/employee/assignToLocation"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Employee
    #'
    #' @param account_id The account id of the logged in user
    #' @param manager_account_id The account id of the manager to assign under
    #' @param username The username/email for the new user. This must be unique across the entire the system.
    #' @param password The password for the new user
    #' @param name (optional) a name field
    #' @param prefix_name (optional) The name prefix; Mr, Mrs, etc
    #' @param first_name (optional) The first name
    #' @param middle_name (optional) The middle name
    #' @param last_name (optional) The last name
    #' @param suffix_name (optional) The name suffix; Jr, Sr, III, etc
    #' @param title (optional) The title of the user
    #' @param about_us (optional) Additional about/biography text
    #' @param asset_id (optional) the asset id to set the user's profile image
    #' @param gender (optional) The gender
    #' @param home_phone (optional) The home phone number
    #' @param cell_phone (optional) The cellular phone number
    #' @param cell_phone_carrier (optional) The cellular service provider. This is required for sending SMS. Leave this empty if the provider is not on the list of supported carriers.
    #' @param business_phone (optional) The business phone number
    #' @param email_address (optional) The user's contact email address (NOT the username)
    #' @param street_address (optional) The street address of the user's contact location
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the user's contact location
    #' @param state (optional) The state of the user's contact location
    #' @param zipcode (optional) The zipcode of the user's contact location
    #' @param country (optional) The country of the user's contact location
    #' @param role (optional) The role; RETAILER or RETAILER_LIMITED, defaulted to RETAILER_LIMITED
    #' @param retailer_location_ids (optional) the retailer location IDs the employee is associated with
    #' @param settings_app_key (optional) Determines whether to return the application settings for the employee for a particular application
    #' @param app_blob (optional) external custom client defined data (per Application)
    #' @param assigned_device_id (optional) The device id to assign to the user (used for IPS beacon tracking)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return EmployeeResponse
    CreateEmployee = function(account_id, manager_account_id, username, password, name = NULL, prefix_name = NULL, first_name = NULL, middle_name = NULL, last_name = NULL, suffix_name = NULL, title = NULL, about_us = NULL, asset_id = NULL, gender = NULL, home_phone = NULL, cell_phone = NULL, cell_phone_carrier = NULL, business_phone = NULL, email_address = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, zipcode = NULL, country = NULL, role = NULL, retailer_location_ids = NULL, settings_app_key = NULL, app_blob = NULL, assigned_device_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateEmployeeWithHttpInfo(account_id, manager_account_id, username, password, name, prefix_name, first_name, middle_name, last_name, suffix_name, title, about_us, asset_id, gender, home_phone, cell_phone, cell_phone_carrier, business_phone, email_address, street_address, street_address2, city, state, zipcode, country, role, retailer_location_ids, settings_app_key, app_blob, assigned_device_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Employee
    #'
    #' @param account_id The account id of the logged in user
    #' @param manager_account_id The account id of the manager to assign under
    #' @param username The username/email for the new user. This must be unique across the entire the system.
    #' @param password The password for the new user
    #' @param name (optional) a name field
    #' @param prefix_name (optional) The name prefix; Mr, Mrs, etc
    #' @param first_name (optional) The first name
    #' @param middle_name (optional) The middle name
    #' @param last_name (optional) The last name
    #' @param suffix_name (optional) The name suffix; Jr, Sr, III, etc
    #' @param title (optional) The title of the user
    #' @param about_us (optional) Additional about/biography text
    #' @param asset_id (optional) the asset id to set the user's profile image
    #' @param gender (optional) The gender
    #' @param home_phone (optional) The home phone number
    #' @param cell_phone (optional) The cellular phone number
    #' @param cell_phone_carrier (optional) The cellular service provider. This is required for sending SMS. Leave this empty if the provider is not on the list of supported carriers.
    #' @param business_phone (optional) The business phone number
    #' @param email_address (optional) The user's contact email address (NOT the username)
    #' @param street_address (optional) The street address of the user's contact location
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the user's contact location
    #' @param state (optional) The state of the user's contact location
    #' @param zipcode (optional) The zipcode of the user's contact location
    #' @param country (optional) The country of the user's contact location
    #' @param role (optional) The role; RETAILER or RETAILER_LIMITED, defaulted to RETAILER_LIMITED
    #' @param retailer_location_ids (optional) the retailer location IDs the employee is associated with
    #' @param settings_app_key (optional) Determines whether to return the application settings for the employee for a particular application
    #' @param app_blob (optional) external custom client defined data (per Application)
    #' @param assigned_device_id (optional) The device id to assign to the user (used for IPS beacon tracking)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (EmployeeResponse) with additional information such as HTTP status code, headers
    CreateEmployeeWithHttpInfo = function(account_id, manager_account_id, username, password, name = NULL, prefix_name = NULL, first_name = NULL, middle_name = NULL, last_name = NULL, suffix_name = NULL, title = NULL, about_us = NULL, asset_id = NULL, gender = NULL, home_phone = NULL, cell_phone = NULL, cell_phone_carrier = NULL, business_phone = NULL, email_address = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, zipcode = NULL, country = NULL, role = NULL, retailer_location_ids = NULL, settings_app_key = NULL, app_blob = NULL, assigned_device_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`manager_account_id`)) {
        stop("Missing required parameter `manager_account_id`.")
      }

      if (missing(`username`)) {
        stop("Missing required parameter `username`.")
      }

      if (missing(`password`)) {
        stop("Missing required parameter `password`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EmployeeApi$CreateEmployee, `account_id` is not nullable")
      }

      if (!missing(`manager_account_id`) && is.null(`manager_account_id`)) {
        stop("Invalid value for `manager_account_id` when calling EmployeeApi$CreateEmployee, `manager_account_id` is not nullable")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling EmployeeApi$CreateEmployee, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling EmployeeApi$CreateEmployee, `password` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling EmployeeApi$CreateEmployee, `name` is not nullable")
      }

      if (!missing(`prefix_name`) && is.null(`prefix_name`)) {
        stop("Invalid value for `prefix_name` when calling EmployeeApi$CreateEmployee, `prefix_name` is not nullable")
      }

      if (!missing(`first_name`) && is.null(`first_name`)) {
        stop("Invalid value for `first_name` when calling EmployeeApi$CreateEmployee, `first_name` is not nullable")
      }

      if (!missing(`middle_name`) && is.null(`middle_name`)) {
        stop("Invalid value for `middle_name` when calling EmployeeApi$CreateEmployee, `middle_name` is not nullable")
      }

      if (!missing(`last_name`) && is.null(`last_name`)) {
        stop("Invalid value for `last_name` when calling EmployeeApi$CreateEmployee, `last_name` is not nullable")
      }

      if (!missing(`suffix_name`) && is.null(`suffix_name`)) {
        stop("Invalid value for `suffix_name` when calling EmployeeApi$CreateEmployee, `suffix_name` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling EmployeeApi$CreateEmployee, `title` is not nullable")
      }

      if (!missing(`about_us`) && is.null(`about_us`)) {
        stop("Invalid value for `about_us` when calling EmployeeApi$CreateEmployee, `about_us` is not nullable")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling EmployeeApi$CreateEmployee, `asset_id` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling EmployeeApi$CreateEmployee, `gender` is not nullable")
      }

      if (!missing(`home_phone`) && is.null(`home_phone`)) {
        stop("Invalid value for `home_phone` when calling EmployeeApi$CreateEmployee, `home_phone` is not nullable")
      }

      if (!missing(`cell_phone`) && is.null(`cell_phone`)) {
        stop("Invalid value for `cell_phone` when calling EmployeeApi$CreateEmployee, `cell_phone` is not nullable")
      }

      if (!missing(`cell_phone_carrier`) && is.null(`cell_phone_carrier`)) {
        stop("Invalid value for `cell_phone_carrier` when calling EmployeeApi$CreateEmployee, `cell_phone_carrier` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling EmployeeApi$CreateEmployee, `business_phone` is not nullable")
      }

      if (!missing(`email_address`) && is.null(`email_address`)) {
        stop("Invalid value for `email_address` when calling EmployeeApi$CreateEmployee, `email_address` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling EmployeeApi$CreateEmployee, `street_address` is not nullable")
      }

      if (!missing(`street_address2`) && is.null(`street_address2`)) {
        stop("Invalid value for `street_address2` when calling EmployeeApi$CreateEmployee, `street_address2` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling EmployeeApi$CreateEmployee, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling EmployeeApi$CreateEmployee, `state` is not nullable")
      }

      if (!missing(`zipcode`) && is.null(`zipcode`)) {
        stop("Invalid value for `zipcode` when calling EmployeeApi$CreateEmployee, `zipcode` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling EmployeeApi$CreateEmployee, `country` is not nullable")
      }

      if (!missing(`role`) && is.null(`role`)) {
        stop("Invalid value for `role` when calling EmployeeApi$CreateEmployee, `role` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling EmployeeApi$CreateEmployee, `retailer_location_ids` is not nullable")
      }

      if (!missing(`settings_app_key`) && is.null(`settings_app_key`)) {
        stop("Invalid value for `settings_app_key` when calling EmployeeApi$CreateEmployee, `settings_app_key` is not nullable")
      }

      if (!missing(`app_blob`) && is.null(`app_blob`)) {
        stop("Invalid value for `app_blob` when calling EmployeeApi$CreateEmployee, `app_blob` is not nullable")
      }

      if (!missing(`assigned_device_id`) && is.null(`assigned_device_id`)) {
        stop("Invalid value for `assigned_device_id` when calling EmployeeApi$CreateEmployee, `assigned_device_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["managerAccountId"]] <- `manager_account_id`

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      query_params[["name"]] <- `name`

      query_params[["prefixName"]] <- `prefix_name`

      query_params[["firstName"]] <- `first_name`

      query_params[["middleName"]] <- `middle_name`

      query_params[["lastName"]] <- `last_name`

      query_params[["suffixName"]] <- `suffix_name`

      query_params[["title"]] <- `title`

      query_params[["aboutUs"]] <- `about_us`

      query_params[["assetId"]] <- `asset_id`

      if (!is.null(`gender`) && !(`gender` %in% c("MALE", "FEMALE", "ANY"))) {
        stop("Invalid value for gender when calling EmployeeApi$CreateEmployee. Must be [MALE, FEMALE, ANY].")
      }
      query_params[["gender"]] <- `gender`

      query_params[["homePhone"]] <- `home_phone`

      query_params[["cellPhone"]] <- `cell_phone`

      query_params[["cellPhoneCarrier"]] <- `cell_phone_carrier`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["emailAddress"]] <- `email_address`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["streetAddress2"]] <- `street_address2`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["zipcode"]] <- `zipcode`

      query_params[["country"]] <- `country`

      query_params[["role"]] <- `role`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["settingsAppKey"]] <- `settings_app_key`

      query_params[["appBlob"]] <- `app_blob`

      query_params[["assignedDeviceId"]] <- `assigned_device_id`

      local_var_url_path <- "/employee/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "EmployeeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Employee
    #'
    #' @param account_id the id of the logged in user
    #' @param employee_account_id the id of the employee to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteEmployee = function(account_id, employee_account_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteEmployeeWithHttpInfo(account_id, employee_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Employee
    #'
    #' @param account_id the id of the logged in user
    #' @param employee_account_id the id of the employee to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteEmployeeWithHttpInfo = function(account_id, employee_account_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`employee_account_id`)) {
        stop("Missing required parameter `employee_account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EmployeeApi$DeleteEmployee, `account_id` is not nullable")
      }

      if (!missing(`employee_account_id`) && is.null(`employee_account_id`)) {
        stop("Invalid value for `employee_account_id` when calling EmployeeApi$DeleteEmployee, `employee_account_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["employeeAccountId"]] <- `employee_account_id`

      local_var_url_path <- "/employee/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Employee
    #'
    #' @param account_id the id of logged in user
    #' @param employee_account_id the id of the employee account to get
    #' @param settings_app_key (optional) Determines whether to return the application settings for the employee for a particular application
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return EmployeeResponse
    GetEmployee = function(account_id, employee_account_id, settings_app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetEmployeeWithHttpInfo(account_id, employee_account_id, settings_app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Employee
    #'
    #' @param account_id the id of logged in user
    #' @param employee_account_id the id of the employee account to get
    #' @param settings_app_key (optional) Determines whether to return the application settings for the employee for a particular application
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (EmployeeResponse) with additional information such as HTTP status code, headers
    GetEmployeeWithHttpInfo = function(account_id, employee_account_id, settings_app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`employee_account_id`)) {
        stop("Missing required parameter `employee_account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EmployeeApi$GetEmployee, `account_id` is not nullable")
      }

      if (!missing(`employee_account_id`) && is.null(`employee_account_id`)) {
        stop("Invalid value for `employee_account_id` when calling EmployeeApi$GetEmployee, `employee_account_id` is not nullable")
      }

      if (!missing(`settings_app_key`) && is.null(`settings_app_key`)) {
        stop("Invalid value for `settings_app_key` when calling EmployeeApi$GetEmployee, `settings_app_key` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["employeeAccountId"]] <- `employee_account_id`

      query_params[["settingsAppKey"]] <- `settings_app_key`

      local_var_url_path <- "/employee/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "EmployeeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Employees
    #'
    #' @param account_id The account id of the logged in user
    #' @param role (optional) The role to limit the search to: RETAILER or RETAILER_LIMITED. Leave empty to search on both roles.
    #' @param retailer_id (optional) Filters employees by retailer
    #' @param retailer_location_id (optional) Filter employees by retailer locations
    #' @param q (optional) Deprecated parameter
    #' @param keyword (optional) an optional keyword to search on; ignored if empty
    #' @param sort_field (optional) The field to sort by. Possible values include: DISPLAY, CREATED, UPDATED, ACTIVE, DELETED, LAST_LOGGED_IN, CONTACT_EMAIL, RETAILER_LOCATION_NAME, RETAILER_NAME
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order (default value: FALSE)
    #' @param _i (optional) Deprecated parameter
    #' @param start (optional) Start the result set at some index (default value: 0)
    #' @param _l (optional) Deprecated parameter
    #' @param limit (optional) Limit the result to some number (default value: 20)
    #' @param active_only (optional) Filter results to only return active employees (default value: TRUE)
    #' @param managed_only (optional) Filter results to only employees that you manage
    #' @param settings_app_key (optional) Determines whether to return the application settings for the employee for a particular application
    #' @param category_ids (optional) Comma separated list of category ids to filter results
    #' @param query (optional) Legacy/reporting query parameter used for formatting employee responses
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[EmployeeResponse]
    SearchEmployees = function(account_id, role = NULL, retailer_id = NULL, retailer_location_id = NULL, q = NULL, keyword = NULL, sort_field = NULL, descending = FALSE, _i = NULL, start = 0, _l = NULL, limit = 20, active_only = TRUE, managed_only = NULL, settings_app_key = NULL, category_ids = NULL, query = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchEmployeesWithHttpInfo(account_id, role, retailer_id, retailer_location_id, q, keyword, sort_field, descending, _i, start, _l, limit, active_only, managed_only, settings_app_key, category_ids, query, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Employees
    #'
    #' @param account_id The account id of the logged in user
    #' @param role (optional) The role to limit the search to: RETAILER or RETAILER_LIMITED. Leave empty to search on both roles.
    #' @param retailer_id (optional) Filters employees by retailer
    #' @param retailer_location_id (optional) Filter employees by retailer locations
    #' @param q (optional) Deprecated parameter
    #' @param keyword (optional) an optional keyword to search on; ignored if empty
    #' @param sort_field (optional) The field to sort by. Possible values include: DISPLAY, CREATED, UPDATED, ACTIVE, DELETED, LAST_LOGGED_IN, CONTACT_EMAIL, RETAILER_LOCATION_NAME, RETAILER_NAME
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order (default value: FALSE)
    #' @param _i (optional) Deprecated parameter
    #' @param start (optional) Start the result set at some index (default value: 0)
    #' @param _l (optional) Deprecated parameter
    #' @param limit (optional) Limit the result to some number (default value: 20)
    #' @param active_only (optional) Filter results to only return active employees (default value: TRUE)
    #' @param managed_only (optional) Filter results to only employees that you manage
    #' @param settings_app_key (optional) Determines whether to return the application settings for the employee for a particular application
    #' @param category_ids (optional) Comma separated list of category ids to filter results
    #' @param query (optional) Legacy/reporting query parameter used for formatting employee responses
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[EmployeeResponse]) with additional information such as HTTP status code, headers
    SearchEmployeesWithHttpInfo = function(account_id, role = NULL, retailer_id = NULL, retailer_location_id = NULL, q = NULL, keyword = NULL, sort_field = NULL, descending = FALSE, _i = NULL, start = 0, _l = NULL, limit = 20, active_only = TRUE, managed_only = NULL, settings_app_key = NULL, category_ids = NULL, query = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EmployeeApi$SearchEmployees, `account_id` is not nullable")
      }

      if (!missing(`role`) && is.null(`role`)) {
        stop("Invalid value for `role` when calling EmployeeApi$SearchEmployees, `role` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling EmployeeApi$SearchEmployees, `retailer_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling EmployeeApi$SearchEmployees, `retailer_location_id` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling EmployeeApi$SearchEmployees, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling EmployeeApi$SearchEmployees, `keyword` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling EmployeeApi$SearchEmployees, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling EmployeeApi$SearchEmployees, `descending` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling EmployeeApi$SearchEmployees, `_i` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling EmployeeApi$SearchEmployees, `start` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling EmployeeApi$SearchEmployees, `_l` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling EmployeeApi$SearchEmployees, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling EmployeeApi$SearchEmployees, `active_only` is not nullable")
      }

      if (!missing(`managed_only`) && is.null(`managed_only`)) {
        stop("Invalid value for `managed_only` when calling EmployeeApi$SearchEmployees, `managed_only` is not nullable")
      }

      if (!missing(`settings_app_key`) && is.null(`settings_app_key`)) {
        stop("Invalid value for `settings_app_key` when calling EmployeeApi$SearchEmployees, `settings_app_key` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling EmployeeApi$SearchEmployees, `category_ids` is not nullable")
      }

      if (!missing(`query`) && is.null(`query`)) {
        stop("Invalid value for `query` when calling EmployeeApi$SearchEmployees, `query` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["role"]] <- `role`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "DISPLAY", "CREATED", "UPDATED", "ACTIVE", "DELETED", "LAST_LOGGED_IN", "CONTACT_EMAIL", "ACCOUNT_TYPE", "RETAILER_LOCATION_NAME", "RETAILER_NAME"))) {
        stop("Invalid value for sort_field when calling EmployeeApi$SearchEmployees. Must be [ID, DISPLAY, CREATED, UPDATED, ACTIVE, DELETED, LAST_LOGGED_IN, CONTACT_EMAIL, ACCOUNT_TYPE, RETAILER_LOCATION_NAME, RETAILER_NAME].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["managedOnly"]] <- `managed_only`

      query_params[["settingsAppKey"]] <- `settings_app_key`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["query"]] <- `query`

      local_var_url_path <- "/employee/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[EmployeeResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Unassign Employee
    #'
    #' @param account_id The account id of the logged in user
    #' @param employee_account_id The account id of the user to be unassigned
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return EmployeeResponse
    UnassignEmployee = function(account_id, employee_account_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UnassignEmployeeWithHttpInfo(account_id, employee_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Unassign Employee
    #'
    #' @param account_id The account id of the logged in user
    #' @param employee_account_id The account id of the user to be unassigned
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (EmployeeResponse) with additional information such as HTTP status code, headers
    UnassignEmployeeWithHttpInfo = function(account_id, employee_account_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`employee_account_id`)) {
        stop("Missing required parameter `employee_account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EmployeeApi$UnassignEmployee, `account_id` is not nullable")
      }

      if (!missing(`employee_account_id`) && is.null(`employee_account_id`)) {
        stop("Invalid value for `employee_account_id` when calling EmployeeApi$UnassignEmployee, `employee_account_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["employeeAccountId"]] <- `employee_account_id`

      local_var_url_path <- "/employee/unassign"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "EmployeeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Employee
    #'
    #' @param account_id The account id of the logged in user
    #' @param employee_account_id the id of the employee account
    #' @param manager_account_id (optional) The account id of the manager to assign under
    #' @param name (optional) a name field
    #' @param prefix_name (optional) The name prefix; Mr, Mrs, etc
    #' @param first_name (optional) The first name
    #' @param middle_name (optional) The middle name
    #' @param last_name (optional) The last name
    #' @param suffix_name (optional) The name suffix; Jr, Sr, III, etc
    #' @param title (optional) The title of the user
    #' @param asset_id (optional) the asset id to set the user's profile image
    #' @param gender (optional) The gender {MALE, FEMALE, ANY}
    #' @param home_phone (optional) The home phone number
    #' @param cell_phone (optional) The cellular phone number
    #' @param cell_phone_carrier (optional) The cellular service provider. This is required for sending SMS. Leave this empty if the provider is not on the list of supported carriers. Supported Carriers: {ATT, QWEST, T_MOBILE, VERIZON, SPRINT, VIRIGIN_MOBILE, NEXTEL, ALLTEL, METRO_PCS, POWERTEL, BOOST_MOBILE, SUNCOM, TRACFONE, US_CELLULAR}
    #' @param business_phone (optional) The business phone number
    #' @param email_address (optional) The user's contact email address (NOT the username)
    #' @param street_address (optional) The street address of the user's contact location
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the user's contact location
    #' @param state (optional) The state of the user's contact location
    #' @param zipcode (optional) The zipcode of the user's contact location
    #' @param country (optional) The country of the user's contact location
    #' @param role (optional) The role; RETAILER or RETAILER_LIMITED, defaulted to RETAILER_LIMITED
    #' @param active (optional) Sets whether the employee is active or inactive
    #' @param password (optional) Sets the password for the employee
    #' @param retailer_location_ids (optional) Sets which retailer locations the employee is assigned to
    #' @param settings_app_key (optional) Determines whether to return the application settings for the employee for a particular application
    #' @param app_blob (optional) external custom client defined data (per Application)
    #' @param assigned_device_id (optional) The device id to assign to the user (used for IPS beacon tracking)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return EmployeeResponse
    UpdateEmployee = function(account_id, employee_account_id, manager_account_id = NULL, name = NULL, prefix_name = NULL, first_name = NULL, middle_name = NULL, last_name = NULL, suffix_name = NULL, title = NULL, asset_id = NULL, gender = NULL, home_phone = NULL, cell_phone = NULL, cell_phone_carrier = NULL, business_phone = NULL, email_address = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, zipcode = NULL, country = NULL, role = NULL, active = NULL, password = NULL, retailer_location_ids = NULL, settings_app_key = NULL, app_blob = NULL, assigned_device_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateEmployeeWithHttpInfo(account_id, employee_account_id, manager_account_id, name, prefix_name, first_name, middle_name, last_name, suffix_name, title, asset_id, gender, home_phone, cell_phone, cell_phone_carrier, business_phone, email_address, street_address, street_address2, city, state, zipcode, country, role, active, password, retailer_location_ids, settings_app_key, app_blob, assigned_device_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Employee
    #'
    #' @param account_id The account id of the logged in user
    #' @param employee_account_id the id of the employee account
    #' @param manager_account_id (optional) The account id of the manager to assign under
    #' @param name (optional) a name field
    #' @param prefix_name (optional) The name prefix; Mr, Mrs, etc
    #' @param first_name (optional) The first name
    #' @param middle_name (optional) The middle name
    #' @param last_name (optional) The last name
    #' @param suffix_name (optional) The name suffix; Jr, Sr, III, etc
    #' @param title (optional) The title of the user
    #' @param asset_id (optional) the asset id to set the user's profile image
    #' @param gender (optional) The gender {MALE, FEMALE, ANY}
    #' @param home_phone (optional) The home phone number
    #' @param cell_phone (optional) The cellular phone number
    #' @param cell_phone_carrier (optional) The cellular service provider. This is required for sending SMS. Leave this empty if the provider is not on the list of supported carriers. Supported Carriers: {ATT, QWEST, T_MOBILE, VERIZON, SPRINT, VIRIGIN_MOBILE, NEXTEL, ALLTEL, METRO_PCS, POWERTEL, BOOST_MOBILE, SUNCOM, TRACFONE, US_CELLULAR}
    #' @param business_phone (optional) The business phone number
    #' @param email_address (optional) The user's contact email address (NOT the username)
    #' @param street_address (optional) The street address of the user's contact location
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the user's contact location
    #' @param state (optional) The state of the user's contact location
    #' @param zipcode (optional) The zipcode of the user's contact location
    #' @param country (optional) The country of the user's contact location
    #' @param role (optional) The role; RETAILER or RETAILER_LIMITED, defaulted to RETAILER_LIMITED
    #' @param active (optional) Sets whether the employee is active or inactive
    #' @param password (optional) Sets the password for the employee
    #' @param retailer_location_ids (optional) Sets which retailer locations the employee is assigned to
    #' @param settings_app_key (optional) Determines whether to return the application settings for the employee for a particular application
    #' @param app_blob (optional) external custom client defined data (per Application)
    #' @param assigned_device_id (optional) The device id to assign to the user (used for IPS beacon tracking)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (EmployeeResponse) with additional information such as HTTP status code, headers
    UpdateEmployeeWithHttpInfo = function(account_id, employee_account_id, manager_account_id = NULL, name = NULL, prefix_name = NULL, first_name = NULL, middle_name = NULL, last_name = NULL, suffix_name = NULL, title = NULL, asset_id = NULL, gender = NULL, home_phone = NULL, cell_phone = NULL, cell_phone_carrier = NULL, business_phone = NULL, email_address = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, zipcode = NULL, country = NULL, role = NULL, active = NULL, password = NULL, retailer_location_ids = NULL, settings_app_key = NULL, app_blob = NULL, assigned_device_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`employee_account_id`)) {
        stop("Missing required parameter `employee_account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EmployeeApi$UpdateEmployee, `account_id` is not nullable")
      }

      if (!missing(`employee_account_id`) && is.null(`employee_account_id`)) {
        stop("Invalid value for `employee_account_id` when calling EmployeeApi$UpdateEmployee, `employee_account_id` is not nullable")
      }

      if (!missing(`manager_account_id`) && is.null(`manager_account_id`)) {
        stop("Invalid value for `manager_account_id` when calling EmployeeApi$UpdateEmployee, `manager_account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling EmployeeApi$UpdateEmployee, `name` is not nullable")
      }

      if (!missing(`prefix_name`) && is.null(`prefix_name`)) {
        stop("Invalid value for `prefix_name` when calling EmployeeApi$UpdateEmployee, `prefix_name` is not nullable")
      }

      if (!missing(`first_name`) && is.null(`first_name`)) {
        stop("Invalid value for `first_name` when calling EmployeeApi$UpdateEmployee, `first_name` is not nullable")
      }

      if (!missing(`middle_name`) && is.null(`middle_name`)) {
        stop("Invalid value for `middle_name` when calling EmployeeApi$UpdateEmployee, `middle_name` is not nullable")
      }

      if (!missing(`last_name`) && is.null(`last_name`)) {
        stop("Invalid value for `last_name` when calling EmployeeApi$UpdateEmployee, `last_name` is not nullable")
      }

      if (!missing(`suffix_name`) && is.null(`suffix_name`)) {
        stop("Invalid value for `suffix_name` when calling EmployeeApi$UpdateEmployee, `suffix_name` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling EmployeeApi$UpdateEmployee, `title` is not nullable")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling EmployeeApi$UpdateEmployee, `asset_id` is not nullable")
      }

      if (!missing(`gender`) && is.null(`gender`)) {
        stop("Invalid value for `gender` when calling EmployeeApi$UpdateEmployee, `gender` is not nullable")
      }

      if (!missing(`home_phone`) && is.null(`home_phone`)) {
        stop("Invalid value for `home_phone` when calling EmployeeApi$UpdateEmployee, `home_phone` is not nullable")
      }

      if (!missing(`cell_phone`) && is.null(`cell_phone`)) {
        stop("Invalid value for `cell_phone` when calling EmployeeApi$UpdateEmployee, `cell_phone` is not nullable")
      }

      if (!missing(`cell_phone_carrier`) && is.null(`cell_phone_carrier`)) {
        stop("Invalid value for `cell_phone_carrier` when calling EmployeeApi$UpdateEmployee, `cell_phone_carrier` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling EmployeeApi$UpdateEmployee, `business_phone` is not nullable")
      }

      if (!missing(`email_address`) && is.null(`email_address`)) {
        stop("Invalid value for `email_address` when calling EmployeeApi$UpdateEmployee, `email_address` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling EmployeeApi$UpdateEmployee, `street_address` is not nullable")
      }

      if (!missing(`street_address2`) && is.null(`street_address2`)) {
        stop("Invalid value for `street_address2` when calling EmployeeApi$UpdateEmployee, `street_address2` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling EmployeeApi$UpdateEmployee, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling EmployeeApi$UpdateEmployee, `state` is not nullable")
      }

      if (!missing(`zipcode`) && is.null(`zipcode`)) {
        stop("Invalid value for `zipcode` when calling EmployeeApi$UpdateEmployee, `zipcode` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling EmployeeApi$UpdateEmployee, `country` is not nullable")
      }

      if (!missing(`role`) && is.null(`role`)) {
        stop("Invalid value for `role` when calling EmployeeApi$UpdateEmployee, `role` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling EmployeeApi$UpdateEmployee, `active` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling EmployeeApi$UpdateEmployee, `password` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling EmployeeApi$UpdateEmployee, `retailer_location_ids` is not nullable")
      }

      if (!missing(`settings_app_key`) && is.null(`settings_app_key`)) {
        stop("Invalid value for `settings_app_key` when calling EmployeeApi$UpdateEmployee, `settings_app_key` is not nullable")
      }

      if (!missing(`app_blob`) && is.null(`app_blob`)) {
        stop("Invalid value for `app_blob` when calling EmployeeApi$UpdateEmployee, `app_blob` is not nullable")
      }

      if (!missing(`assigned_device_id`) && is.null(`assigned_device_id`)) {
        stop("Invalid value for `assigned_device_id` when calling EmployeeApi$UpdateEmployee, `assigned_device_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["employeeAccountId"]] <- `employee_account_id`

      query_params[["managerAccountId"]] <- `manager_account_id`

      query_params[["name"]] <- `name`

      query_params[["prefixName"]] <- `prefix_name`

      query_params[["firstName"]] <- `first_name`

      query_params[["middleName"]] <- `middle_name`

      query_params[["lastName"]] <- `last_name`

      query_params[["suffixName"]] <- `suffix_name`

      query_params[["title"]] <- `title`

      query_params[["assetId"]] <- `asset_id`

      if (!is.null(`gender`) && !(`gender` %in% c("MALE", "FEMALE", "ANY"))) {
        stop("Invalid value for gender when calling EmployeeApi$UpdateEmployee. Must be [MALE, FEMALE, ANY].")
      }
      query_params[["gender"]] <- `gender`

      query_params[["homePhone"]] <- `home_phone`

      query_params[["cellPhone"]] <- `cell_phone`

      query_params[["cellPhoneCarrier"]] <- `cell_phone_carrier`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["emailAddress"]] <- `email_address`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["streetAddress2"]] <- `street_address2`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["zipcode"]] <- `zipcode`

      query_params[["country"]] <- `country`

      query_params[["role"]] <- `role`

      query_params[["active"]] <- `active`

      query_params[["password"]] <- `password`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["settingsAppKey"]] <- `settings_app_key`

      query_params[["appBlob"]] <- `app_blob`

      query_params[["assignedDeviceId"]] <- `assigned_device_id`

      local_var_url_path <- "/employee/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "EmployeeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
