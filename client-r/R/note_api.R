#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Note operations
#' @description NoteApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  BatchOperation  ####################
#'
#' library(openapi)
#' var_notable_id <- 56 # integer | The id of the notable object the batch operation will affect
#' var_notable_type <- "notable_type_example" # character | The notable object type (for example ALBUM, ASSET, OFFER, etc.)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_batch_operation <- "batch_operation_example" # character | The batch operation to perform (e.g., DELETE_ALL_NOTES_IN_NOTABLE). Optional. (Optional)
#'
#' #Batch Note Operation
#' api_instance <- NoteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BatchOperation(var_notable_id, var_notable_type, device_id = var_device_id, account_id = var_account_id, batch_operation = var_batch_operationdata_file = "result.txt")
#' result <- api_instance$BatchOperation(var_notable_id, var_notable_type, device_id = var_device_id, account_id = var_account_id, batch_operation = var_batch_operation)
#' dput(result)
#'
#'
#' ####################  CreateNote  ####################
#'
#' library(openapi)
#' var_comment <- "comment_example" # character | The message the user wishes to leave a comment on
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_notable_type <- "notable_type_example" # character | The notable object type {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, OFFER, OFFER_LOCATION, RETAILER, RETAILER_LOCATION, THEME_DESCRIPTOR} (Optional)
#' var_notable_id <- 56 # integer | The id of the notable object (Optional)
#' var_note_type <- "note_type_example" # character | The custom string defined by the client (used for differentiating various note types) (Optional)
#' var_asset_ids <- "asset_ids_example" # character | A comma separated list of asset IDs to add with the note (Optional)
#' var_tags <- "tags_example" # character | search tags (Optional)
#' var_permissionable_type <- "permissionable_type_example" # character | This is used for sending out group notifications. For example, when someone adds a note to an asset which is also a part of an album, everyone in the album will receive a notification. This is achieved by passing in the permissionable type (in this case \"album\"), and the permissionable id (the album id). Possible types: {ALBUM, ALBUM_CONTEST, GAME_LEVEL, THEME_DESCRIPTOR} (Optional)
#' var_permissionable_id <- 56 # integer | The id of the permissionable object (for sending group notifications) (Optional)
#' var_app_key <- "app_key_example" # character | The application key used to identify the application (Optional)
#' var_location_description <- "location_description_example" # character | The description of the location (Optional)
#' var_latitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_receiver_account_ids <- "receiver_account_ids_example" # character | Comma separated list of additional account IDs that will receive the note notifications (Optional)
#' var_return_full_response <- "return_full_response_example" # character | whether to return the full response or not (Optional)
#' var_initialize_asset <- "initialize_asset_example" # character | Check true if need to initialize an asset and assign to current note (Optional)
#' var_asset_return_nulls <- "asset_return_nulls_example" # character | Return null fields for asset response when creating an asset (Optional)
#' var_asset_album_id <- 56 # integer | the album the asset will be added to (optional) (Optional)
#' var_asset_collection_id <- 56 # integer | the collection ID that the asset is associated with (Optional)
#' var_asset_add_to_default_album <- "asset_add_to_default_album_example" # character | the default album to add the asset to (Optional)
#' var_asset_add_to_media_library <- "asset_add_to_media_library_example" # character | the media library to add the asset to (Optional)
#' var_asset_version_code <- 56 # integer | the version code of the asset (Optional)
#' var_asset_version_name <- "asset_version_name_example" # character | the version name of the asset (Optional)
#' var_asset_meta_data <- "asset_meta_data_example" # character | the meta data of the asset (Optional)
#' var_asset_caption <- "asset_caption_example" # character | the caption of the asset (Optional)
#' var_asset_media <- File.new('/path/to/file') # data.frame | the media of the asset (Optional)
#' var_asset_media_url <- "asset_media_url_example" # character | the media URL of the asset (Optional)
#' var_asset_media_string <- "asset_media_string_example" # character | the media string of the asset (Optional)
#' var_asset_media_string_file_name <- "asset_media_string_file_name_example" # character | the media string file name of the asset (Optional)
#' var_asset_media_string_content_type <- "asset_media_string_content_type_example" # character | the media string content type of the asset (Optional)
#' var_asset_attached_media <- File.new('/path/to/file') # data.frame | the attached media of the asset (Optional)
#' var_asset_attached_media_url <- "asset_attached_media_url_example" # character | the attached media URL of the asset (Optional)
#' var_asset_attached_media_string <- "asset_attached_media_string_example" # character | the attached media string of the asset (Optional)
#' var_asset_attached_media_string_file_name <- "asset_attached_media_string_file_name_example" # character | the attached media string file name of the asset (Optional)
#' var_asset_attached_media_string_content_type <- "asset_attached_media_string_content_type_example" # character | the attached media string content type of the asset (Optional)
#' var_asset_location_description <- "asset_location_description_example" # character | the location description for the asset (Optional)
#' var_asset_app <- "asset_app_example" # character | the application for the asset (Optional)
#' var_asset_search_tags <- "asset_search_tags_example" # character | the search tags used for the asset (Optional)
#' var_asset_latitude <- 3.4 # numeric | the latitude of the asset (Optional)
#' var_asset_longitude <- 3.4 # numeric | the longitude of the asset (Optional)
#'
#' #Create Note
#' api_instance <- NoteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateNote(var_comment, device_id = var_device_id, account_id = var_account_id, notable_type = var_notable_type, notable_id = var_notable_id, note_type = var_note_type, asset_ids = var_asset_ids, tags = var_tags, permissionable_type = var_permissionable_type, permissionable_id = var_permissionable_id, app_key = var_app_key, location_description = var_location_description, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, receiver_account_ids = var_receiver_account_ids, return_full_response = var_return_full_response, initialize_asset = var_initialize_asset, asset_return_nulls = var_asset_return_nulls, asset_album_id = var_asset_album_id, asset_collection_id = var_asset_collection_id, asset_add_to_default_album = var_asset_add_to_default_album, asset_add_to_media_library = var_asset_add_to_media_library, asset_version_code = var_asset_version_code, asset_version_name = var_asset_version_name, asset_meta_data = var_asset_meta_data, asset_caption = var_asset_caption, asset_media = var_asset_media, asset_media_url = var_asset_media_url, asset_media_string = var_asset_media_string, asset_media_string_file_name = var_asset_media_string_file_name, asset_media_string_content_type = var_asset_media_string_content_type, asset_attached_media = var_asset_attached_media, asset_attached_media_url = var_asset_attached_media_url, asset_attached_media_string = var_asset_attached_media_string, asset_attached_media_string_file_name = var_asset_attached_media_string_file_name, asset_attached_media_string_content_type = var_asset_attached_media_string_content_type, asset_location_description = var_asset_location_description, asset_app = var_asset_app, asset_search_tags = var_asset_search_tags, asset_latitude = var_asset_latitude, asset_longitude = var_asset_longitudedata_file = "result.txt")
#' result <- api_instance$CreateNote(var_comment, device_id = var_device_id, account_id = var_account_id, notable_type = var_notable_type, notable_id = var_notable_id, note_type = var_note_type, asset_ids = var_asset_ids, tags = var_tags, permissionable_type = var_permissionable_type, permissionable_id = var_permissionable_id, app_key = var_app_key, location_description = var_location_description, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, receiver_account_ids = var_receiver_account_ids, return_full_response = var_return_full_response, initialize_asset = var_initialize_asset, asset_return_nulls = var_asset_return_nulls, asset_album_id = var_asset_album_id, asset_collection_id = var_asset_collection_id, asset_add_to_default_album = var_asset_add_to_default_album, asset_add_to_media_library = var_asset_add_to_media_library, asset_version_code = var_asset_version_code, asset_version_name = var_asset_version_name, asset_meta_data = var_asset_meta_data, asset_caption = var_asset_caption, asset_media = var_asset_media, asset_media_url = var_asset_media_url, asset_media_string = var_asset_media_string, asset_media_string_file_name = var_asset_media_string_file_name, asset_media_string_content_type = var_asset_media_string_content_type, asset_attached_media = var_asset_attached_media, asset_attached_media_url = var_asset_attached_media_url, asset_attached_media_string = var_asset_attached_media_string, asset_attached_media_string_file_name = var_asset_attached_media_string_file_name, asset_attached_media_string_content_type = var_asset_attached_media_string_content_type, asset_location_description = var_asset_location_description, asset_app = var_asset_app, asset_search_tags = var_asset_search_tags, asset_latitude = var_asset_latitude, asset_longitude = var_asset_longitude)
#' dput(result)
#'
#'
#' ####################  DeleteNote  ####################
#'
#' library(openapi)
#' var_note_id <- 56 # integer | The ID of the note to delete
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_latitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_app_key <- "app_key_example" # character | The application key used to identify the application (Optional)
#'
#' #Delete Note
#' api_instance <- NoteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteNote(var_note_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$DeleteNote(var_note_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  GetNote  ####################
#'
#' library(openapi)
#' var_note_id <- 56 # integer | the id of the note to get
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_return_full_response <- "return_full_response_example" # character | Determines whether to return the NoteFullResponse for the item (Optional)
#'
#' #Get Note
#' api_instance <- NoteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetNote(var_note_id, device_id = var_device_id, account_id = var_account_id, return_full_response = var_return_full_responsedata_file = "result.txt")
#' result <- api_instance$GetNote(var_note_id, device_id = var_device_id, account_id = var_account_id, return_full_response = var_return_full_response)
#' dput(result)
#'
#'
#' ####################  SearchNotes  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_notable_type <- "notable_type_example" # character | The notable object type {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, OFFER, OFFER_LOCATION, RETAILER, RETAILER_LOCATION, THEME_DESCRIPTOR} (Optional)
#' var_notable_id <- 56 # integer | The id of the notable object (Optional)
#' var_note_types <- "note_types_example" # character | Comma separated list of noteType strings to filter results with (Optional)
#' var_app_key <- "app_key_example" # character | The application key used to identify the application (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_flag_count_minimum <- 56 # integer | return items that has flagCount >= flagCountMinimum if this is set, return all items, even ones with flagCount >= flagThreshold (Optional)
#' var_flags_exceed_threshold <- "flags_exceed_threshold_example" # character | return items that has flagCount >= flagThreshold, which are hidden by default (Optional)
#' var_include_inactive <- "include_inactive_example" # character | include inactive in the result (Optional)
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on (Optional)
#' var_descending <- "descending_example" # character | The order to return the search results (Optional)
#' var_return_full_response <- "return_full_response_example" # character | Determines whether to return the NoteFullResponse for each search item (Optional)
#' var_updated_since <- 56 # integer | return items that have been updated since this date (time-stamp in milliseconds) (Optional)
#' var_updated_before <- 56 # integer | return items that have been updated before this date (time-stamp in milliseconds) (Optional)
#' var_start <- 56 # integer | The record to begin the return set on (Optional)
#' var_limit <- 56 # integer | The number of records to return (Optional)
#'
#' #Search Notes
#' api_instance <- NoteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchNotes(device_id = var_device_id, account_id = var_account_id, notable_type = var_notable_type, notable_id = var_notable_id, note_types = var_note_types, app_key = var_app_key, keyword = var_keyword, flag_count_minimum = var_flag_count_minimum, flags_exceed_threshold = var_flags_exceed_threshold, include_inactive = var_include_inactive, sort_field = var_sort_field, descending = var_descending, return_full_response = var_return_full_response, updated_since = var_updated_since, updated_before = var_updated_before, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchNotes(device_id = var_device_id, account_id = var_account_id, notable_type = var_notable_type, notable_id = var_notable_id, note_types = var_note_types, app_key = var_app_key, keyword = var_keyword, flag_count_minimum = var_flag_count_minimum, flags_exceed_threshold = var_flags_exceed_threshold, include_inactive = var_include_inactive, sort_field = var_sort_field, descending = var_descending, return_full_response = var_return_full_response, updated_since = var_updated_since, updated_before = var_updated_before, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  UpdateNote  ####################
#'
#' library(openapi)
#' var_note_id <- 56 # integer | The id of the note, used when editing a comment
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_comment <- "comment_example" # character | The message the user wishes to leave a comment on (Optional)
#' var_note_type <- "note_type_example" # character | The custom string defined by the client (used for differentiating on various note types) (Optional)
#' var_asset_ids <- "asset_ids_example" # character | A comma separated list of asset IDs to add with the note (Optional)
#' var_tags <- "tags_example" # character | search tags (Optional)
#' var_permissionable_type <- "permissionable_type_example" # character | This is used for sending out group notifications. For example, when someone adds a note to an asset which is also a part of an album, everyone in the album will receive a notification. This is achieved by passing in the permissionable type (in this case \"album\"), and the permissionable id (the album id). Possible types: {ALBUM, ALBUM_CONTEST, GAME_LEVEL, THEME_DESCRIPTOR} (Optional)
#' var_permissionable_id <- 56 # integer | The id of the permissionable object (for sending group notifications) (Optional)
#' var_app_key <- "app_key_example" # character | The application key used to identify the application (Optional)
#' var_location_description <- "location_description_example" # character | The description of the location (Optional)
#' var_latitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_meta_data <- "meta_data_example" # character | meta data to update with the note (Optional)
#' var_return_full_response <- "return_full_response_example" # character | whether to return the full response or not (Optional)
#' var_active <- "active_example" # character | Sets the active flag for the note (Optional)
#' var_update_asset <- "update_asset_example" # character | main flag for updating asset in note, must set to true if you want to update the note's asset (Optional)
#' var_asset_return_nulls <- "asset_return_nulls_example" # character | Return null fields for asset response when updating an asset (Optional)
#' var_asset_album_id <- 56 # integer | the album the asset will be added to (optional) (Optional)
#' var_asset_collection_id <- 56 # integer | the collection ID that the asset is associated with (Optional)
#' var_asset_add_to_default_album <- "asset_add_to_default_album_example" # character | the default album to add the asset to (Optional)
#' var_asset_add_to_media_library <- "asset_add_to_media_library_example" # character | the media library to add the asset to (Optional)
#' var_asset_version_code <- 56 # integer | the version code of the asset (Optional)
#' var_asset_version_name <- "asset_version_name_example" # character | the version name of the asset (Optional)
#' var_asset_meta_data <- "asset_meta_data_example" # character | the meta data of the asset (Optional)
#' var_asset_caption <- "asset_caption_example" # character | the caption of the asset (Optional)
#' var_asset_media <- File.new('/path/to/file') # data.frame | the media of the asset (Optional)
#' var_asset_media_url <- "asset_media_url_example" # character | the media URL of the asset (Optional)
#' var_asset_media_string <- "asset_media_string_example" # character | the media string of the asset (Optional)
#' var_asset_media_string_file_name <- "asset_media_string_file_name_example" # character | the media string file name of the asset (Optional)
#' var_asset_media_string_content_type <- "asset_media_string_content_type_example" # character | the media string content type of the asset (Optional)
#' var_asset_attached_media <- File.new('/path/to/file') # data.frame | the attached media of the asset (Optional)
#' var_asset_attached_media_url <- "asset_attached_media_url_example" # character | the attached media URL of the asset (Optional)
#' var_asset_attached_media_string <- "asset_attached_media_string_example" # character | the attached media string of the asset (Optional)
#' var_asset_attached_media_string_file_name <- "asset_attached_media_string_file_name_example" # character | the attached media string file name of the asset (Optional)
#' var_asset_attached_media_string_content_type <- "asset_attached_media_string_content_type_example" # character | the attached media string content type of the asset (Optional)
#' var_asset_location_description <- "asset_location_description_example" # character | the location description for the asset (Optional)
#' var_asset_app <- "asset_app_example" # character | the application for the asset (Optional)
#' var_asset_search_tags <- "asset_search_tags_example" # character | the search tags used for the asset (Optional)
#' var_asset_latitude <- 3.4 # numeric | the latitude of the asset (Optional)
#' var_asset_longitude <- 3.4 # numeric | the longitude of the asset (Optional)
#'
#' #Update Note
#' api_instance <- NoteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateNote(var_note_id, device_id = var_device_id, account_id = var_account_id, comment = var_comment, note_type = var_note_type, asset_ids = var_asset_ids, tags = var_tags, permissionable_type = var_permissionable_type, permissionable_id = var_permissionable_id, app_key = var_app_key, location_description = var_location_description, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, return_full_response = var_return_full_response, active = var_active, update_asset = var_update_asset, asset_return_nulls = var_asset_return_nulls, asset_album_id = var_asset_album_id, asset_collection_id = var_asset_collection_id, asset_add_to_default_album = var_asset_add_to_default_album, asset_add_to_media_library = var_asset_add_to_media_library, asset_version_code = var_asset_version_code, asset_version_name = var_asset_version_name, asset_meta_data = var_asset_meta_data, asset_caption = var_asset_caption, asset_media = var_asset_media, asset_media_url = var_asset_media_url, asset_media_string = var_asset_media_string, asset_media_string_file_name = var_asset_media_string_file_name, asset_media_string_content_type = var_asset_media_string_content_type, asset_attached_media = var_asset_attached_media, asset_attached_media_url = var_asset_attached_media_url, asset_attached_media_string = var_asset_attached_media_string, asset_attached_media_string_file_name = var_asset_attached_media_string_file_name, asset_attached_media_string_content_type = var_asset_attached_media_string_content_type, asset_location_description = var_asset_location_description, asset_app = var_asset_app, asset_search_tags = var_asset_search_tags, asset_latitude = var_asset_latitude, asset_longitude = var_asset_longitudedata_file = "result.txt")
#' result <- api_instance$UpdateNote(var_note_id, device_id = var_device_id, account_id = var_account_id, comment = var_comment, note_type = var_note_type, asset_ids = var_asset_ids, tags = var_tags, permissionable_type = var_permissionable_type, permissionable_id = var_permissionable_id, app_key = var_app_key, location_description = var_location_description, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, return_full_response = var_return_full_response, active = var_active, update_asset = var_update_asset, asset_return_nulls = var_asset_return_nulls, asset_album_id = var_asset_album_id, asset_collection_id = var_asset_collection_id, asset_add_to_default_album = var_asset_add_to_default_album, asset_add_to_media_library = var_asset_add_to_media_library, asset_version_code = var_asset_version_code, asset_version_name = var_asset_version_name, asset_meta_data = var_asset_meta_data, asset_caption = var_asset_caption, asset_media = var_asset_media, asset_media_url = var_asset_media_url, asset_media_string = var_asset_media_string, asset_media_string_file_name = var_asset_media_string_file_name, asset_media_string_content_type = var_asset_media_string_content_type, asset_attached_media = var_asset_attached_media, asset_attached_media_url = var_asset_attached_media_url, asset_attached_media_string = var_asset_attached_media_string, asset_attached_media_string_file_name = var_asset_attached_media_string_file_name, asset_attached_media_string_content_type = var_asset_attached_media_string_content_type, asset_location_description = var_asset_location_description, asset_app = var_asset_app, asset_search_tags = var_asset_search_tags, asset_latitude = var_asset_latitude, asset_longitude = var_asset_longitude)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
NoteApi <- R6::R6Class(
  "NoteApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new NoteApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Batch Note Operation
    #'
    #' @param notable_id The id of the notable object the batch operation will affect
    #' @param notable_type The notable object type (for example ALBUM, ASSET, OFFER, etc.)
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param batch_operation (optional) The batch operation to perform (e.g., DELETE_ALL_NOTES_IN_NOTABLE). Optional.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    BatchOperation = function(notable_id, notable_type, device_id = NULL, account_id = NULL, batch_operation = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BatchOperationWithHttpInfo(notable_id, notable_type, device_id, account_id, batch_operation, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Batch Note Operation
    #'
    #' @param notable_id The id of the notable object the batch operation will affect
    #' @param notable_type The notable object type (for example ALBUM, ASSET, OFFER, etc.)
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param batch_operation (optional) The batch operation to perform (e.g., DELETE_ALL_NOTES_IN_NOTABLE). Optional.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    BatchOperationWithHttpInfo = function(notable_id, notable_type, device_id = NULL, account_id = NULL, batch_operation = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`notable_id`)) {
        stop("Missing required parameter `notable_id`.")
      }

      if (missing(`notable_type`)) {
        stop("Missing required parameter `notable_type`.")
      }

      if (!missing(`notable_id`) && is.null(`notable_id`)) {
        stop("Invalid value for `notable_id` when calling NoteApi$BatchOperation, `notable_id` is not nullable")
      }

      if (!missing(`notable_type`) && is.null(`notable_type`)) {
        stop("Invalid value for `notable_type` when calling NoteApi$BatchOperation, `notable_type` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NoteApi$BatchOperation, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NoteApi$BatchOperation, `account_id` is not nullable")
      }

      if (!missing(`batch_operation`) && is.null(`batch_operation`)) {
        stop("Invalid value for `batch_operation` when calling NoteApi$BatchOperation, `batch_operation` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["notableId"]] <- `notable_id`

      query_params[["notableType"]] <- `notable_type`

      query_params[["batchOperation"]] <- `batch_operation`

      local_var_url_path <- "/note/batch"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Note
    #'
    #' @param comment The message the user wishes to leave a comment on
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param notable_type (optional) The notable object type {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, OFFER, OFFER_LOCATION, RETAILER, RETAILER_LOCATION, THEME_DESCRIPTOR}
    #' @param notable_id (optional) The id of the notable object
    #' @param note_type (optional) The custom string defined by the client (used for differentiating various note types)
    #' @param asset_ids (optional) A comma separated list of asset IDs to add with the note
    #' @param tags (optional) search tags
    #' @param permissionable_type (optional) This is used for sending out group notifications. For example, when someone adds a note to an asset which is also a part of an album, everyone in the album will receive a notification. This is achieved by passing in the permissionable type (in this case \"album\"), and the permissionable id (the album id). Possible types: {ALBUM, ALBUM_CONTEST, GAME_LEVEL, THEME_DESCRIPTOR}
    #' @param permissionable_id (optional) The id of the permissionable object (for sending group notifications)
    #' @param app_key (optional) The application key used to identify the application
    #' @param location_description (optional) The description of the location
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param meta_data (optional) External custom client defined data
    #' @param receiver_account_ids (optional) Comma separated list of additional account IDs that will receive the note notifications
    #' @param return_full_response (optional) whether to return the full response or not
    #' @param initialize_asset (optional) Check true if need to initialize an asset and assign to current note
    #' @param asset_return_nulls (optional) Return null fields for asset response when creating an asset
    #' @param asset_album_id (optional) the album the asset will be added to (optional)
    #' @param asset_collection_id (optional) the collection ID that the asset is associated with
    #' @param asset_add_to_default_album (optional) the default album to add the asset to
    #' @param asset_add_to_media_library (optional) the media library to add the asset to
    #' @param asset_version_code (optional) the version code of the asset
    #' @param asset_version_name (optional) the version name of the asset
    #' @param asset_meta_data (optional) the meta data of the asset
    #' @param asset_caption (optional) the caption of the asset
    #' @param asset_media (optional) the media of the asset
    #' @param asset_media_url (optional) the media URL of the asset
    #' @param asset_media_string (optional) the media string of the asset
    #' @param asset_media_string_file_name (optional) the media string file name of the asset
    #' @param asset_media_string_content_type (optional) the media string content type of the asset
    #' @param asset_attached_media (optional) the attached media of the asset
    #' @param asset_attached_media_url (optional) the attached media URL of the asset
    #' @param asset_attached_media_string (optional) the attached media string of the asset
    #' @param asset_attached_media_string_file_name (optional) the attached media string file name of the asset
    #' @param asset_attached_media_string_content_type (optional) the attached media string content type of the asset
    #' @param asset_location_description (optional) the location description for the asset
    #' @param asset_app (optional) the application for the asset
    #' @param asset_search_tags (optional) the search tags used for the asset
    #' @param asset_latitude (optional) the latitude of the asset
    #' @param asset_longitude (optional) the longitude of the asset
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return NoteResponse
    CreateNote = function(comment, device_id = NULL, account_id = NULL, notable_type = NULL, notable_id = NULL, note_type = NULL, asset_ids = NULL, tags = NULL, permissionable_type = NULL, permissionable_id = NULL, app_key = NULL, location_description = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, receiver_account_ids = NULL, return_full_response = NULL, initialize_asset = NULL, asset_return_nulls = NULL, asset_album_id = NULL, asset_collection_id = NULL, asset_add_to_default_album = NULL, asset_add_to_media_library = NULL, asset_version_code = NULL, asset_version_name = NULL, asset_meta_data = NULL, asset_caption = NULL, asset_media = NULL, asset_media_url = NULL, asset_media_string = NULL, asset_media_string_file_name = NULL, asset_media_string_content_type = NULL, asset_attached_media = NULL, asset_attached_media_url = NULL, asset_attached_media_string = NULL, asset_attached_media_string_file_name = NULL, asset_attached_media_string_content_type = NULL, asset_location_description = NULL, asset_app = NULL, asset_search_tags = NULL, asset_latitude = NULL, asset_longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateNoteWithHttpInfo(comment, device_id, account_id, notable_type, notable_id, note_type, asset_ids, tags, permissionable_type, permissionable_id, app_key, location_description, latitude, longitude, meta_data, receiver_account_ids, return_full_response, initialize_asset, asset_return_nulls, asset_album_id, asset_collection_id, asset_add_to_default_album, asset_add_to_media_library, asset_version_code, asset_version_name, asset_meta_data, asset_caption, asset_media, asset_media_url, asset_media_string, asset_media_string_file_name, asset_media_string_content_type, asset_attached_media, asset_attached_media_url, asset_attached_media_string, asset_attached_media_string_file_name, asset_attached_media_string_content_type, asset_location_description, asset_app, asset_search_tags, asset_latitude, asset_longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Note
    #'
    #' @param comment The message the user wishes to leave a comment on
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param notable_type (optional) The notable object type {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, OFFER, OFFER_LOCATION, RETAILER, RETAILER_LOCATION, THEME_DESCRIPTOR}
    #' @param notable_id (optional) The id of the notable object
    #' @param note_type (optional) The custom string defined by the client (used for differentiating various note types)
    #' @param asset_ids (optional) A comma separated list of asset IDs to add with the note
    #' @param tags (optional) search tags
    #' @param permissionable_type (optional) This is used for sending out group notifications. For example, when someone adds a note to an asset which is also a part of an album, everyone in the album will receive a notification. This is achieved by passing in the permissionable type (in this case \"album\"), and the permissionable id (the album id). Possible types: {ALBUM, ALBUM_CONTEST, GAME_LEVEL, THEME_DESCRIPTOR}
    #' @param permissionable_id (optional) The id of the permissionable object (for sending group notifications)
    #' @param app_key (optional) The application key used to identify the application
    #' @param location_description (optional) The description of the location
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param meta_data (optional) External custom client defined data
    #' @param receiver_account_ids (optional) Comma separated list of additional account IDs that will receive the note notifications
    #' @param return_full_response (optional) whether to return the full response or not
    #' @param initialize_asset (optional) Check true if need to initialize an asset and assign to current note
    #' @param asset_return_nulls (optional) Return null fields for asset response when creating an asset
    #' @param asset_album_id (optional) the album the asset will be added to (optional)
    #' @param asset_collection_id (optional) the collection ID that the asset is associated with
    #' @param asset_add_to_default_album (optional) the default album to add the asset to
    #' @param asset_add_to_media_library (optional) the media library to add the asset to
    #' @param asset_version_code (optional) the version code of the asset
    #' @param asset_version_name (optional) the version name of the asset
    #' @param asset_meta_data (optional) the meta data of the asset
    #' @param asset_caption (optional) the caption of the asset
    #' @param asset_media (optional) the media of the asset
    #' @param asset_media_url (optional) the media URL of the asset
    #' @param asset_media_string (optional) the media string of the asset
    #' @param asset_media_string_file_name (optional) the media string file name of the asset
    #' @param asset_media_string_content_type (optional) the media string content type of the asset
    #' @param asset_attached_media (optional) the attached media of the asset
    #' @param asset_attached_media_url (optional) the attached media URL of the asset
    #' @param asset_attached_media_string (optional) the attached media string of the asset
    #' @param asset_attached_media_string_file_name (optional) the attached media string file name of the asset
    #' @param asset_attached_media_string_content_type (optional) the attached media string content type of the asset
    #' @param asset_location_description (optional) the location description for the asset
    #' @param asset_app (optional) the application for the asset
    #' @param asset_search_tags (optional) the search tags used for the asset
    #' @param asset_latitude (optional) the latitude of the asset
    #' @param asset_longitude (optional) the longitude of the asset
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (NoteResponse) with additional information such as HTTP status code, headers
    CreateNoteWithHttpInfo = function(comment, device_id = NULL, account_id = NULL, notable_type = NULL, notable_id = NULL, note_type = NULL, asset_ids = NULL, tags = NULL, permissionable_type = NULL, permissionable_id = NULL, app_key = NULL, location_description = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, receiver_account_ids = NULL, return_full_response = NULL, initialize_asset = NULL, asset_return_nulls = NULL, asset_album_id = NULL, asset_collection_id = NULL, asset_add_to_default_album = NULL, asset_add_to_media_library = NULL, asset_version_code = NULL, asset_version_name = NULL, asset_meta_data = NULL, asset_caption = NULL, asset_media = NULL, asset_media_url = NULL, asset_media_string = NULL, asset_media_string_file_name = NULL, asset_media_string_content_type = NULL, asset_attached_media = NULL, asset_attached_media_url = NULL, asset_attached_media_string = NULL, asset_attached_media_string_file_name = NULL, asset_attached_media_string_content_type = NULL, asset_location_description = NULL, asset_app = NULL, asset_search_tags = NULL, asset_latitude = NULL, asset_longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`comment`)) {
        stop("Missing required parameter `comment`.")
      }

      if (!missing(`comment`) && is.null(`comment`)) {
        stop("Invalid value for `comment` when calling NoteApi$CreateNote, `comment` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NoteApi$CreateNote, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NoteApi$CreateNote, `account_id` is not nullable")
      }

      if (!missing(`notable_type`) && is.null(`notable_type`)) {
        stop("Invalid value for `notable_type` when calling NoteApi$CreateNote, `notable_type` is not nullable")
      }

      if (!missing(`notable_id`) && is.null(`notable_id`)) {
        stop("Invalid value for `notable_id` when calling NoteApi$CreateNote, `notable_id` is not nullable")
      }

      if (!missing(`note_type`) && is.null(`note_type`)) {
        stop("Invalid value for `note_type` when calling NoteApi$CreateNote, `note_type` is not nullable")
      }

      if (!missing(`asset_ids`) && is.null(`asset_ids`)) {
        stop("Invalid value for `asset_ids` when calling NoteApi$CreateNote, `asset_ids` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling NoteApi$CreateNote, `tags` is not nullable")
      }

      if (!missing(`permissionable_type`) && is.null(`permissionable_type`)) {
        stop("Invalid value for `permissionable_type` when calling NoteApi$CreateNote, `permissionable_type` is not nullable")
      }

      if (!missing(`permissionable_id`) && is.null(`permissionable_id`)) {
        stop("Invalid value for `permissionable_id` when calling NoteApi$CreateNote, `permissionable_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NoteApi$CreateNote, `app_key` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling NoteApi$CreateNote, `location_description` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling NoteApi$CreateNote, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling NoteApi$CreateNote, `longitude` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling NoteApi$CreateNote, `meta_data` is not nullable")
      }

      if (!missing(`receiver_account_ids`) && is.null(`receiver_account_ids`)) {
        stop("Invalid value for `receiver_account_ids` when calling NoteApi$CreateNote, `receiver_account_ids` is not nullable")
      }

      if (!missing(`return_full_response`) && is.null(`return_full_response`)) {
        stop("Invalid value for `return_full_response` when calling NoteApi$CreateNote, `return_full_response` is not nullable")
      }

      if (!missing(`initialize_asset`) && is.null(`initialize_asset`)) {
        stop("Invalid value for `initialize_asset` when calling NoteApi$CreateNote, `initialize_asset` is not nullable")
      }

      if (!missing(`asset_return_nulls`) && is.null(`asset_return_nulls`)) {
        stop("Invalid value for `asset_return_nulls` when calling NoteApi$CreateNote, `asset_return_nulls` is not nullable")
      }

      if (!missing(`asset_album_id`) && is.null(`asset_album_id`)) {
        stop("Invalid value for `asset_album_id` when calling NoteApi$CreateNote, `asset_album_id` is not nullable")
      }

      if (!missing(`asset_collection_id`) && is.null(`asset_collection_id`)) {
        stop("Invalid value for `asset_collection_id` when calling NoteApi$CreateNote, `asset_collection_id` is not nullable")
      }

      if (!missing(`asset_add_to_default_album`) && is.null(`asset_add_to_default_album`)) {
        stop("Invalid value for `asset_add_to_default_album` when calling NoteApi$CreateNote, `asset_add_to_default_album` is not nullable")
      }

      if (!missing(`asset_add_to_media_library`) && is.null(`asset_add_to_media_library`)) {
        stop("Invalid value for `asset_add_to_media_library` when calling NoteApi$CreateNote, `asset_add_to_media_library` is not nullable")
      }

      if (!missing(`asset_version_code`) && is.null(`asset_version_code`)) {
        stop("Invalid value for `asset_version_code` when calling NoteApi$CreateNote, `asset_version_code` is not nullable")
      }

      if (!missing(`asset_version_name`) && is.null(`asset_version_name`)) {
        stop("Invalid value for `asset_version_name` when calling NoteApi$CreateNote, `asset_version_name` is not nullable")
      }

      if (!missing(`asset_meta_data`) && is.null(`asset_meta_data`)) {
        stop("Invalid value for `asset_meta_data` when calling NoteApi$CreateNote, `asset_meta_data` is not nullable")
      }

      if (!missing(`asset_caption`) && is.null(`asset_caption`)) {
        stop("Invalid value for `asset_caption` when calling NoteApi$CreateNote, `asset_caption` is not nullable")
      }

      if (!missing(`asset_media`) && is.null(`asset_media`)) {
        stop("Invalid value for `asset_media` when calling NoteApi$CreateNote, `asset_media` is not nullable")
      }

      if (!missing(`asset_media_url`) && is.null(`asset_media_url`)) {
        stop("Invalid value for `asset_media_url` when calling NoteApi$CreateNote, `asset_media_url` is not nullable")
      }

      if (!missing(`asset_media_string`) && is.null(`asset_media_string`)) {
        stop("Invalid value for `asset_media_string` when calling NoteApi$CreateNote, `asset_media_string` is not nullable")
      }

      if (!missing(`asset_media_string_file_name`) && is.null(`asset_media_string_file_name`)) {
        stop("Invalid value for `asset_media_string_file_name` when calling NoteApi$CreateNote, `asset_media_string_file_name` is not nullable")
      }

      if (!missing(`asset_media_string_content_type`) && is.null(`asset_media_string_content_type`)) {
        stop("Invalid value for `asset_media_string_content_type` when calling NoteApi$CreateNote, `asset_media_string_content_type` is not nullable")
      }

      if (!missing(`asset_attached_media`) && is.null(`asset_attached_media`)) {
        stop("Invalid value for `asset_attached_media` when calling NoteApi$CreateNote, `asset_attached_media` is not nullable")
      }

      if (!missing(`asset_attached_media_url`) && is.null(`asset_attached_media_url`)) {
        stop("Invalid value for `asset_attached_media_url` when calling NoteApi$CreateNote, `asset_attached_media_url` is not nullable")
      }

      if (!missing(`asset_attached_media_string`) && is.null(`asset_attached_media_string`)) {
        stop("Invalid value for `asset_attached_media_string` when calling NoteApi$CreateNote, `asset_attached_media_string` is not nullable")
      }

      if (!missing(`asset_attached_media_string_file_name`) && is.null(`asset_attached_media_string_file_name`)) {
        stop("Invalid value for `asset_attached_media_string_file_name` when calling NoteApi$CreateNote, `asset_attached_media_string_file_name` is not nullable")
      }

      if (!missing(`asset_attached_media_string_content_type`) && is.null(`asset_attached_media_string_content_type`)) {
        stop("Invalid value for `asset_attached_media_string_content_type` when calling NoteApi$CreateNote, `asset_attached_media_string_content_type` is not nullable")
      }

      if (!missing(`asset_location_description`) && is.null(`asset_location_description`)) {
        stop("Invalid value for `asset_location_description` when calling NoteApi$CreateNote, `asset_location_description` is not nullable")
      }

      if (!missing(`asset_app`) && is.null(`asset_app`)) {
        stop("Invalid value for `asset_app` when calling NoteApi$CreateNote, `asset_app` is not nullable")
      }

      if (!missing(`asset_search_tags`) && is.null(`asset_search_tags`)) {
        stop("Invalid value for `asset_search_tags` when calling NoteApi$CreateNote, `asset_search_tags` is not nullable")
      }

      if (!missing(`asset_latitude`) && is.null(`asset_latitude`)) {
        stop("Invalid value for `asset_latitude` when calling NoteApi$CreateNote, `asset_latitude` is not nullable")
      }

      if (!missing(`asset_longitude`) && is.null(`asset_longitude`)) {
        stop("Invalid value for `asset_longitude` when calling NoteApi$CreateNote, `asset_longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["notableType"]] <- `notable_type`

      query_params[["notableId"]] <- `notable_id`

      query_params[["comment"]] <- `comment`

      query_params[["noteType"]] <- `note_type`

      query_params[["assetIds"]] <- `asset_ids`

      query_params[["tags"]] <- `tags`

      query_params[["permissionableType"]] <- `permissionable_type`

      query_params[["permissionableId"]] <- `permissionable_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["locationDescription"]] <- `location_description`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["metaData"]] <- `meta_data`

      query_params[["receiverAccountIds"]] <- `receiver_account_ids`

      query_params[["returnFullResponse"]] <- `return_full_response`

      query_params[["initializeAsset"]] <- `initialize_asset`

      query_params[["assetReturnNulls"]] <- `asset_return_nulls`

      query_params[["assetAlbumId"]] <- `asset_album_id`

      query_params[["assetCollectionId"]] <- `asset_collection_id`

      query_params[["assetAddToDefaultAlbum"]] <- `asset_add_to_default_album`

      query_params[["assetAddToMediaLibrary"]] <- `asset_add_to_media_library`

      query_params[["assetVersionCode"]] <- `asset_version_code`

      query_params[["assetVersionName"]] <- `asset_version_name`

      query_params[["assetMetaData"]] <- `asset_meta_data`

      query_params[["assetCaption"]] <- `asset_caption`

      query_params[["assetMedia"]] <- `asset_media`

      query_params[["assetMediaUrl"]] <- `asset_media_url`

      query_params[["assetMediaString"]] <- `asset_media_string`

      query_params[["assetMediaStringFileName"]] <- `asset_media_string_file_name`

      query_params[["assetMediaStringContentType"]] <- `asset_media_string_content_type`

      query_params[["assetAttachedMedia"]] <- `asset_attached_media`

      query_params[["assetAttachedMediaUrl"]] <- `asset_attached_media_url`

      query_params[["assetAttachedMediaString"]] <- `asset_attached_media_string`

      query_params[["assetAttachedMediaStringFileName"]] <- `asset_attached_media_string_file_name`

      query_params[["assetAttachedMediaStringContentType"]] <- `asset_attached_media_string_content_type`

      query_params[["assetLocationDescription"]] <- `asset_location_description`

      query_params[["assetApp"]] <- `asset_app`

      query_params[["assetSearchTags"]] <- `asset_search_tags`

      query_params[["assetLatitude"]] <- `asset_latitude`

      query_params[["assetLongitude"]] <- `asset_longitude`

      local_var_url_path <- "/note/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "NoteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Note
    #'
    #' @param note_id The ID of the note to delete
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param app_key (optional) The application key used to identify the application
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteNote = function(note_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteNoteWithHttpInfo(note_id, device_id, account_id, latitude, longitude, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Note
    #'
    #' @param note_id The ID of the note to delete
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param app_key (optional) The application key used to identify the application
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteNoteWithHttpInfo = function(note_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`note_id`)) {
        stop("Missing required parameter `note_id`.")
      }

      if (!missing(`note_id`) && is.null(`note_id`)) {
        stop("Invalid value for `note_id` when calling NoteApi$DeleteNote, `note_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NoteApi$DeleteNote, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NoteApi$DeleteNote, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling NoteApi$DeleteNote, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling NoteApi$DeleteNote, `longitude` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NoteApi$DeleteNote, `app_key` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["noteId"]] <- `note_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/note/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Note
    #'
    #' @param note_id the id of the note to get
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param return_full_response (optional) Determines whether to return the NoteFullResponse for the item
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    GetNote = function(note_id, device_id = NULL, account_id = NULL, return_full_response = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetNoteWithHttpInfo(note_id, device_id, account_id, return_full_response, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Note
    #'
    #' @param note_id the id of the note to get
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param return_full_response (optional) Determines whether to return the NoteFullResponse for the item
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    GetNoteWithHttpInfo = function(note_id, device_id = NULL, account_id = NULL, return_full_response = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`note_id`)) {
        stop("Missing required parameter `note_id`.")
      }

      if (!missing(`note_id`) && is.null(`note_id`)) {
        stop("Invalid value for `note_id` when calling NoteApi$GetNote, `note_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NoteApi$GetNote, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NoteApi$GetNote, `account_id` is not nullable")
      }

      if (!missing(`return_full_response`) && is.null(`return_full_response`)) {
        stop("Invalid value for `return_full_response` when calling NoteApi$GetNote, `return_full_response` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["returnFullResponse"]] <- `return_full_response`

      query_params[["noteId"]] <- `note_id`

      local_var_url_path <- "/note/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Notes
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param notable_type (optional) The notable object type {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, OFFER, OFFER_LOCATION, RETAILER, RETAILER_LOCATION, THEME_DESCRIPTOR}
    #' @param notable_id (optional) The id of the notable object
    #' @param note_types (optional) Comma separated list of noteType strings to filter results with
    #' @param app_key (optional) The application key used to identify the application
    #' @param keyword (optional) The keyword used to search
    #' @param flag_count_minimum (optional) return items that has flagCount >= flagCountMinimum if this is set, return all items, even ones with flagCount >= flagThreshold
    #' @param flags_exceed_threshold (optional) return items that has flagCount >= flagThreshold, which are hidden by default
    #' @param include_inactive (optional) include inactive in the result
    #' @param sort_field (optional) The column to sort the search on
    #' @param descending (optional) The order to return the search results
    #' @param return_full_response (optional) Determines whether to return the NoteFullResponse for each search item
    #' @param updated_since (optional) return items that have been updated since this date (time-stamp in milliseconds)
    #' @param updated_before (optional) return items that have been updated before this date (time-stamp in milliseconds)
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[NoteResponse]
    SearchNotes = function(device_id = NULL, account_id = NULL, notable_type = NULL, notable_id = NULL, note_types = NULL, app_key = NULL, keyword = NULL, flag_count_minimum = NULL, flags_exceed_threshold = NULL, include_inactive = NULL, sort_field = NULL, descending = NULL, return_full_response = NULL, updated_since = NULL, updated_before = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchNotesWithHttpInfo(device_id, account_id, notable_type, notable_id, note_types, app_key, keyword, flag_count_minimum, flags_exceed_threshold, include_inactive, sort_field, descending, return_full_response, updated_since, updated_before, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Notes
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param notable_type (optional) The notable object type {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, OFFER, OFFER_LOCATION, RETAILER, RETAILER_LOCATION, THEME_DESCRIPTOR}
    #' @param notable_id (optional) The id of the notable object
    #' @param note_types (optional) Comma separated list of noteType strings to filter results with
    #' @param app_key (optional) The application key used to identify the application
    #' @param keyword (optional) The keyword used to search
    #' @param flag_count_minimum (optional) return items that has flagCount >= flagCountMinimum if this is set, return all items, even ones with flagCount >= flagThreshold
    #' @param flags_exceed_threshold (optional) return items that has flagCount >= flagThreshold, which are hidden by default
    #' @param include_inactive (optional) include inactive in the result
    #' @param sort_field (optional) The column to sort the search on
    #' @param descending (optional) The order to return the search results
    #' @param return_full_response (optional) Determines whether to return the NoteFullResponse for each search item
    #' @param updated_since (optional) return items that have been updated since this date (time-stamp in milliseconds)
    #' @param updated_before (optional) return items that have been updated before this date (time-stamp in milliseconds)
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[NoteResponse]) with additional information such as HTTP status code, headers
    SearchNotesWithHttpInfo = function(device_id = NULL, account_id = NULL, notable_type = NULL, notable_id = NULL, note_types = NULL, app_key = NULL, keyword = NULL, flag_count_minimum = NULL, flags_exceed_threshold = NULL, include_inactive = NULL, sort_field = NULL, descending = NULL, return_full_response = NULL, updated_since = NULL, updated_before = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NoteApi$SearchNotes, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NoteApi$SearchNotes, `account_id` is not nullable")
      }

      if (!missing(`notable_type`) && is.null(`notable_type`)) {
        stop("Invalid value for `notable_type` when calling NoteApi$SearchNotes, `notable_type` is not nullable")
      }

      if (!missing(`notable_id`) && is.null(`notable_id`)) {
        stop("Invalid value for `notable_id` when calling NoteApi$SearchNotes, `notable_id` is not nullable")
      }

      if (!missing(`note_types`) && is.null(`note_types`)) {
        stop("Invalid value for `note_types` when calling NoteApi$SearchNotes, `note_types` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NoteApi$SearchNotes, `app_key` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling NoteApi$SearchNotes, `keyword` is not nullable")
      }

      if (!missing(`flag_count_minimum`) && is.null(`flag_count_minimum`)) {
        stop("Invalid value for `flag_count_minimum` when calling NoteApi$SearchNotes, `flag_count_minimum` is not nullable")
      }

      if (!missing(`flags_exceed_threshold`) && is.null(`flags_exceed_threshold`)) {
        stop("Invalid value for `flags_exceed_threshold` when calling NoteApi$SearchNotes, `flags_exceed_threshold` is not nullable")
      }

      if (!missing(`include_inactive`) && is.null(`include_inactive`)) {
        stop("Invalid value for `include_inactive` when calling NoteApi$SearchNotes, `include_inactive` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling NoteApi$SearchNotes, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling NoteApi$SearchNotes, `descending` is not nullable")
      }

      if (!missing(`return_full_response`) && is.null(`return_full_response`)) {
        stop("Invalid value for `return_full_response` when calling NoteApi$SearchNotes, `return_full_response` is not nullable")
      }

      if (!missing(`updated_since`) && is.null(`updated_since`)) {
        stop("Invalid value for `updated_since` when calling NoteApi$SearchNotes, `updated_since` is not nullable")
      }

      if (!missing(`updated_before`) && is.null(`updated_before`)) {
        stop("Invalid value for `updated_before` when calling NoteApi$SearchNotes, `updated_before` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling NoteApi$SearchNotes, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling NoteApi$SearchNotes, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["notableType"]] <- `notable_type`

      query_params[["notableId"]] <- `notable_id`

      query_params[["noteTypes"]] <- `note_types`

      query_params[["appKey"]] <- `app_key`

      query_params[["keyword"]] <- `keyword`

      query_params[["flagCountMinimum"]] <- `flag_count_minimum`

      query_params[["flagsExceedThreshold"]] <- `flags_exceed_threshold`

      query_params[["includeInactive"]] <- `include_inactive`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "OWNER_DISPLAY", "NOTABLE_TYPE", "NOTE_TAG", "NOTE_COUNT", "LIKES", "DISLIKES"))) {
        stop("Invalid value for sort_field when calling NoteApi$SearchNotes. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, OWNER_DISPLAY, NOTABLE_TYPE, NOTE_TAG, NOTE_COUNT, LIKES, DISLIKES].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["returnFullResponse"]] <- `return_full_response`

      query_params[["updatedSince"]] <- `updated_since`

      query_params[["updatedBefore"]] <- `updated_before`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/note/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[NoteResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Note
    #'
    #' @param note_id The id of the note, used when editing a comment
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param comment (optional) The message the user wishes to leave a comment on
    #' @param note_type (optional) The custom string defined by the client (used for differentiating on various note types)
    #' @param asset_ids (optional) A comma separated list of asset IDs to add with the note
    #' @param tags (optional) search tags
    #' @param permissionable_type (optional) This is used for sending out group notifications. For example, when someone adds a note to an asset which is also a part of an album, everyone in the album will receive a notification. This is achieved by passing in the permissionable type (in this case \"album\"), and the permissionable id (the album id). Possible types: {ALBUM, ALBUM_CONTEST, GAME_LEVEL, THEME_DESCRIPTOR}
    #' @param permissionable_id (optional) The id of the permissionable object (for sending group notifications)
    #' @param app_key (optional) The application key used to identify the application
    #' @param location_description (optional) The description of the location
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param meta_data (optional) meta data to update with the note
    #' @param return_full_response (optional) whether to return the full response or not
    #' @param active (optional) Sets the active flag for the note
    #' @param update_asset (optional) main flag for updating asset in note, must set to true if you want to update the note's asset
    #' @param asset_return_nulls (optional) Return null fields for asset response when updating an asset
    #' @param asset_album_id (optional) the album the asset will be added to (optional)
    #' @param asset_collection_id (optional) the collection ID that the asset is associated with
    #' @param asset_add_to_default_album (optional) the default album to add the asset to
    #' @param asset_add_to_media_library (optional) the media library to add the asset to
    #' @param asset_version_code (optional) the version code of the asset
    #' @param asset_version_name (optional) the version name of the asset
    #' @param asset_meta_data (optional) the meta data of the asset
    #' @param asset_caption (optional) the caption of the asset
    #' @param asset_media (optional) the media of the asset
    #' @param asset_media_url (optional) the media URL of the asset
    #' @param asset_media_string (optional) the media string of the asset
    #' @param asset_media_string_file_name (optional) the media string file name of the asset
    #' @param asset_media_string_content_type (optional) the media string content type of the asset
    #' @param asset_attached_media (optional) the attached media of the asset
    #' @param asset_attached_media_url (optional) the attached media URL of the asset
    #' @param asset_attached_media_string (optional) the attached media string of the asset
    #' @param asset_attached_media_string_file_name (optional) the attached media string file name of the asset
    #' @param asset_attached_media_string_content_type (optional) the attached media string content type of the asset
    #' @param asset_location_description (optional) the location description for the asset
    #' @param asset_app (optional) the application for the asset
    #' @param asset_search_tags (optional) the search tags used for the asset
    #' @param asset_latitude (optional) the latitude of the asset
    #' @param asset_longitude (optional) the longitude of the asset
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return NoteResponse
    UpdateNote = function(note_id, device_id = NULL, account_id = NULL, comment = NULL, note_type = NULL, asset_ids = NULL, tags = NULL, permissionable_type = NULL, permissionable_id = NULL, app_key = NULL, location_description = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, return_full_response = NULL, active = NULL, update_asset = NULL, asset_return_nulls = NULL, asset_album_id = NULL, asset_collection_id = NULL, asset_add_to_default_album = NULL, asset_add_to_media_library = NULL, asset_version_code = NULL, asset_version_name = NULL, asset_meta_data = NULL, asset_caption = NULL, asset_media = NULL, asset_media_url = NULL, asset_media_string = NULL, asset_media_string_file_name = NULL, asset_media_string_content_type = NULL, asset_attached_media = NULL, asset_attached_media_url = NULL, asset_attached_media_string = NULL, asset_attached_media_string_file_name = NULL, asset_attached_media_string_content_type = NULL, asset_location_description = NULL, asset_app = NULL, asset_search_tags = NULL, asset_latitude = NULL, asset_longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateNoteWithHttpInfo(note_id, device_id, account_id, comment, note_type, asset_ids, tags, permissionable_type, permissionable_id, app_key, location_description, latitude, longitude, meta_data, return_full_response, active, update_asset, asset_return_nulls, asset_album_id, asset_collection_id, asset_add_to_default_album, asset_add_to_media_library, asset_version_code, asset_version_name, asset_meta_data, asset_caption, asset_media, asset_media_url, asset_media_string, asset_media_string_file_name, asset_media_string_content_type, asset_attached_media, asset_attached_media_url, asset_attached_media_string, asset_attached_media_string_file_name, asset_attached_media_string_content_type, asset_location_description, asset_app, asset_search_tags, asset_latitude, asset_longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Note
    #'
    #' @param note_id The id of the note, used when editing a comment
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param comment (optional) The message the user wishes to leave a comment on
    #' @param note_type (optional) The custom string defined by the client (used for differentiating on various note types)
    #' @param asset_ids (optional) A comma separated list of asset IDs to add with the note
    #' @param tags (optional) search tags
    #' @param permissionable_type (optional) This is used for sending out group notifications. For example, when someone adds a note to an asset which is also a part of an album, everyone in the album will receive a notification. This is achieved by passing in the permissionable type (in this case \"album\"), and the permissionable id (the album id). Possible types: {ALBUM, ALBUM_CONTEST, GAME_LEVEL, THEME_DESCRIPTOR}
    #' @param permissionable_id (optional) The id of the permissionable object (for sending group notifications)
    #' @param app_key (optional) The application key used to identify the application
    #' @param location_description (optional) The description of the location
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param meta_data (optional) meta data to update with the note
    #' @param return_full_response (optional) whether to return the full response or not
    #' @param active (optional) Sets the active flag for the note
    #' @param update_asset (optional) main flag for updating asset in note, must set to true if you want to update the note's asset
    #' @param asset_return_nulls (optional) Return null fields for asset response when updating an asset
    #' @param asset_album_id (optional) the album the asset will be added to (optional)
    #' @param asset_collection_id (optional) the collection ID that the asset is associated with
    #' @param asset_add_to_default_album (optional) the default album to add the asset to
    #' @param asset_add_to_media_library (optional) the media library to add the asset to
    #' @param asset_version_code (optional) the version code of the asset
    #' @param asset_version_name (optional) the version name of the asset
    #' @param asset_meta_data (optional) the meta data of the asset
    #' @param asset_caption (optional) the caption of the asset
    #' @param asset_media (optional) the media of the asset
    #' @param asset_media_url (optional) the media URL of the asset
    #' @param asset_media_string (optional) the media string of the asset
    #' @param asset_media_string_file_name (optional) the media string file name of the asset
    #' @param asset_media_string_content_type (optional) the media string content type of the asset
    #' @param asset_attached_media (optional) the attached media of the asset
    #' @param asset_attached_media_url (optional) the attached media URL of the asset
    #' @param asset_attached_media_string (optional) the attached media string of the asset
    #' @param asset_attached_media_string_file_name (optional) the attached media string file name of the asset
    #' @param asset_attached_media_string_content_type (optional) the attached media string content type of the asset
    #' @param asset_location_description (optional) the location description for the asset
    #' @param asset_app (optional) the application for the asset
    #' @param asset_search_tags (optional) the search tags used for the asset
    #' @param asset_latitude (optional) the latitude of the asset
    #' @param asset_longitude (optional) the longitude of the asset
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (NoteResponse) with additional information such as HTTP status code, headers
    UpdateNoteWithHttpInfo = function(note_id, device_id = NULL, account_id = NULL, comment = NULL, note_type = NULL, asset_ids = NULL, tags = NULL, permissionable_type = NULL, permissionable_id = NULL, app_key = NULL, location_description = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, return_full_response = NULL, active = NULL, update_asset = NULL, asset_return_nulls = NULL, asset_album_id = NULL, asset_collection_id = NULL, asset_add_to_default_album = NULL, asset_add_to_media_library = NULL, asset_version_code = NULL, asset_version_name = NULL, asset_meta_data = NULL, asset_caption = NULL, asset_media = NULL, asset_media_url = NULL, asset_media_string = NULL, asset_media_string_file_name = NULL, asset_media_string_content_type = NULL, asset_attached_media = NULL, asset_attached_media_url = NULL, asset_attached_media_string = NULL, asset_attached_media_string_file_name = NULL, asset_attached_media_string_content_type = NULL, asset_location_description = NULL, asset_app = NULL, asset_search_tags = NULL, asset_latitude = NULL, asset_longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`note_id`)) {
        stop("Missing required parameter `note_id`.")
      }

      if (!missing(`note_id`) && is.null(`note_id`)) {
        stop("Invalid value for `note_id` when calling NoteApi$UpdateNote, `note_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NoteApi$UpdateNote, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NoteApi$UpdateNote, `account_id` is not nullable")
      }

      if (!missing(`comment`) && is.null(`comment`)) {
        stop("Invalid value for `comment` when calling NoteApi$UpdateNote, `comment` is not nullable")
      }

      if (!missing(`note_type`) && is.null(`note_type`)) {
        stop("Invalid value for `note_type` when calling NoteApi$UpdateNote, `note_type` is not nullable")
      }

      if (!missing(`asset_ids`) && is.null(`asset_ids`)) {
        stop("Invalid value for `asset_ids` when calling NoteApi$UpdateNote, `asset_ids` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling NoteApi$UpdateNote, `tags` is not nullable")
      }

      if (!missing(`permissionable_type`) && is.null(`permissionable_type`)) {
        stop("Invalid value for `permissionable_type` when calling NoteApi$UpdateNote, `permissionable_type` is not nullable")
      }

      if (!missing(`permissionable_id`) && is.null(`permissionable_id`)) {
        stop("Invalid value for `permissionable_id` when calling NoteApi$UpdateNote, `permissionable_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NoteApi$UpdateNote, `app_key` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling NoteApi$UpdateNote, `location_description` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling NoteApi$UpdateNote, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling NoteApi$UpdateNote, `longitude` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling NoteApi$UpdateNote, `meta_data` is not nullable")
      }

      if (!missing(`return_full_response`) && is.null(`return_full_response`)) {
        stop("Invalid value for `return_full_response` when calling NoteApi$UpdateNote, `return_full_response` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling NoteApi$UpdateNote, `active` is not nullable")
      }

      if (!missing(`update_asset`) && is.null(`update_asset`)) {
        stop("Invalid value for `update_asset` when calling NoteApi$UpdateNote, `update_asset` is not nullable")
      }

      if (!missing(`asset_return_nulls`) && is.null(`asset_return_nulls`)) {
        stop("Invalid value for `asset_return_nulls` when calling NoteApi$UpdateNote, `asset_return_nulls` is not nullable")
      }

      if (!missing(`asset_album_id`) && is.null(`asset_album_id`)) {
        stop("Invalid value for `asset_album_id` when calling NoteApi$UpdateNote, `asset_album_id` is not nullable")
      }

      if (!missing(`asset_collection_id`) && is.null(`asset_collection_id`)) {
        stop("Invalid value for `asset_collection_id` when calling NoteApi$UpdateNote, `asset_collection_id` is not nullable")
      }

      if (!missing(`asset_add_to_default_album`) && is.null(`asset_add_to_default_album`)) {
        stop("Invalid value for `asset_add_to_default_album` when calling NoteApi$UpdateNote, `asset_add_to_default_album` is not nullable")
      }

      if (!missing(`asset_add_to_media_library`) && is.null(`asset_add_to_media_library`)) {
        stop("Invalid value for `asset_add_to_media_library` when calling NoteApi$UpdateNote, `asset_add_to_media_library` is not nullable")
      }

      if (!missing(`asset_version_code`) && is.null(`asset_version_code`)) {
        stop("Invalid value for `asset_version_code` when calling NoteApi$UpdateNote, `asset_version_code` is not nullable")
      }

      if (!missing(`asset_version_name`) && is.null(`asset_version_name`)) {
        stop("Invalid value for `asset_version_name` when calling NoteApi$UpdateNote, `asset_version_name` is not nullable")
      }

      if (!missing(`asset_meta_data`) && is.null(`asset_meta_data`)) {
        stop("Invalid value for `asset_meta_data` when calling NoteApi$UpdateNote, `asset_meta_data` is not nullable")
      }

      if (!missing(`asset_caption`) && is.null(`asset_caption`)) {
        stop("Invalid value for `asset_caption` when calling NoteApi$UpdateNote, `asset_caption` is not nullable")
      }

      if (!missing(`asset_media`) && is.null(`asset_media`)) {
        stop("Invalid value for `asset_media` when calling NoteApi$UpdateNote, `asset_media` is not nullable")
      }

      if (!missing(`asset_media_url`) && is.null(`asset_media_url`)) {
        stop("Invalid value for `asset_media_url` when calling NoteApi$UpdateNote, `asset_media_url` is not nullable")
      }

      if (!missing(`asset_media_string`) && is.null(`asset_media_string`)) {
        stop("Invalid value for `asset_media_string` when calling NoteApi$UpdateNote, `asset_media_string` is not nullable")
      }

      if (!missing(`asset_media_string_file_name`) && is.null(`asset_media_string_file_name`)) {
        stop("Invalid value for `asset_media_string_file_name` when calling NoteApi$UpdateNote, `asset_media_string_file_name` is not nullable")
      }

      if (!missing(`asset_media_string_content_type`) && is.null(`asset_media_string_content_type`)) {
        stop("Invalid value for `asset_media_string_content_type` when calling NoteApi$UpdateNote, `asset_media_string_content_type` is not nullable")
      }

      if (!missing(`asset_attached_media`) && is.null(`asset_attached_media`)) {
        stop("Invalid value for `asset_attached_media` when calling NoteApi$UpdateNote, `asset_attached_media` is not nullable")
      }

      if (!missing(`asset_attached_media_url`) && is.null(`asset_attached_media_url`)) {
        stop("Invalid value for `asset_attached_media_url` when calling NoteApi$UpdateNote, `asset_attached_media_url` is not nullable")
      }

      if (!missing(`asset_attached_media_string`) && is.null(`asset_attached_media_string`)) {
        stop("Invalid value for `asset_attached_media_string` when calling NoteApi$UpdateNote, `asset_attached_media_string` is not nullable")
      }

      if (!missing(`asset_attached_media_string_file_name`) && is.null(`asset_attached_media_string_file_name`)) {
        stop("Invalid value for `asset_attached_media_string_file_name` when calling NoteApi$UpdateNote, `asset_attached_media_string_file_name` is not nullable")
      }

      if (!missing(`asset_attached_media_string_content_type`) && is.null(`asset_attached_media_string_content_type`)) {
        stop("Invalid value for `asset_attached_media_string_content_type` when calling NoteApi$UpdateNote, `asset_attached_media_string_content_type` is not nullable")
      }

      if (!missing(`asset_location_description`) && is.null(`asset_location_description`)) {
        stop("Invalid value for `asset_location_description` when calling NoteApi$UpdateNote, `asset_location_description` is not nullable")
      }

      if (!missing(`asset_app`) && is.null(`asset_app`)) {
        stop("Invalid value for `asset_app` when calling NoteApi$UpdateNote, `asset_app` is not nullable")
      }

      if (!missing(`asset_search_tags`) && is.null(`asset_search_tags`)) {
        stop("Invalid value for `asset_search_tags` when calling NoteApi$UpdateNote, `asset_search_tags` is not nullable")
      }

      if (!missing(`asset_latitude`) && is.null(`asset_latitude`)) {
        stop("Invalid value for `asset_latitude` when calling NoteApi$UpdateNote, `asset_latitude` is not nullable")
      }

      if (!missing(`asset_longitude`) && is.null(`asset_longitude`)) {
        stop("Invalid value for `asset_longitude` when calling NoteApi$UpdateNote, `asset_longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["noteId"]] <- `note_id`

      query_params[["comment"]] <- `comment`

      query_params[["noteType"]] <- `note_type`

      query_params[["assetIds"]] <- `asset_ids`

      query_params[["tags"]] <- `tags`

      query_params[["permissionableType"]] <- `permissionable_type`

      query_params[["permissionableId"]] <- `permissionable_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["locationDescription"]] <- `location_description`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["metaData"]] <- `meta_data`

      query_params[["returnFullResponse"]] <- `return_full_response`

      query_params[["active"]] <- `active`

      query_params[["updateAsset"]] <- `update_asset`

      query_params[["assetReturnNulls"]] <- `asset_return_nulls`

      query_params[["assetAlbumId"]] <- `asset_album_id`

      query_params[["assetCollectionId"]] <- `asset_collection_id`

      query_params[["assetAddToDefaultAlbum"]] <- `asset_add_to_default_album`

      query_params[["assetAddToMediaLibrary"]] <- `asset_add_to_media_library`

      query_params[["assetVersionCode"]] <- `asset_version_code`

      query_params[["assetVersionName"]] <- `asset_version_name`

      query_params[["assetMetaData"]] <- `asset_meta_data`

      query_params[["assetCaption"]] <- `asset_caption`

      query_params[["assetMedia"]] <- `asset_media`

      query_params[["assetMediaUrl"]] <- `asset_media_url`

      query_params[["assetMediaString"]] <- `asset_media_string`

      query_params[["assetMediaStringFileName"]] <- `asset_media_string_file_name`

      query_params[["assetMediaStringContentType"]] <- `asset_media_string_content_type`

      query_params[["assetAttachedMedia"]] <- `asset_attached_media`

      query_params[["assetAttachedMediaUrl"]] <- `asset_attached_media_url`

      query_params[["assetAttachedMediaString"]] <- `asset_attached_media_string`

      query_params[["assetAttachedMediaStringFileName"]] <- `asset_attached_media_string_file_name`

      query_params[["assetAttachedMediaStringContentType"]] <- `asset_attached_media_string_content_type`

      query_params[["assetLocationDescription"]] <- `asset_location_description`

      query_params[["assetApp"]] <- `asset_app`

      query_params[["assetSearchTags"]] <- `asset_search_tags`

      query_params[["assetLatitude"]] <- `asset_latitude`

      query_params[["assetLongitude"]] <- `asset_longitude`

      local_var_url_path <- "/note/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "NoteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
