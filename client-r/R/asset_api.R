#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Asset operations
#' @description AssetApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AssetDownload  ####################
#'
#' library(openapi)
#' var_filename <- "filename_example" # character | the filename in the following formats: {assetId}-{suffix}.{extension} | {assetId}.{extension} | {assetId}
#'
#' #Download Asset
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetDownload(var_filenamedata_file = "result.txt")
#' result <- api_instance$AssetDownload(var_filename)
#' dput(result)
#'
#'
#' ####################  AssetMorph  ####################
#'
#' library(openapi)
#' var_offer_id <- 56 # integer | offer id used for inserting offer text/flavor
#' var_ad_size <- "ad_size_example" # character | the ad size used for selecting a format for the creative image
#' var_creative_id <- 56 # integer | used for inserting the newly created image into (Optional)
#' var_width <- 56 # integer | total width of the creative image (Optional)
#' var_height <- 56 # integer | total height of the creative image (Optional)
#' var_background_size <- "background_size_example" # character | the size of the background (Optional)
#' var_template <- "template_example" # character | the template to use (Optional)
#'
#' #Convert Offer to Creative
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetMorph(var_offer_id, var_ad_size, creative_id = var_creative_id, width = var_width, height = var_height, background_size = var_background_size, template = var_templatedata_file = "result.txt")
#' result <- api_instance$AssetMorph(var_offer_id, var_ad_size, creative_id = var_creative_id, width = var_width, height = var_height, background_size = var_background_size, template = var_template)
#' dput(result)
#'
#'
#' ####################  CreateAsset  ####################
#'
#' library(openapi)
#' var_return_nulls <- "return_nulls_example" # character | to return nulls (Optional)
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_album_id <- 56 # integer | the album the asset will be added to (optional) (Optional)
#' var_collection_id <- 56 # integer | Deprecated: use collections via the collection endpoints. (Optional)
#' var_add_to_default_album <- "add_to_default_album_example" # character | Whether to add to a default album (Optional)
#' var_add_to_media_library <- "add_to_media_library_example" # character | Whether to add to the media library (Optional)
#' var_version_code <- 56 # integer | the version code (Optional)
#' var_version_name <- "version_name_example" # character | the version name (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_caption <- "caption_example" # character | the caption (optional) (Optional)
#' var_asset_type <- "asset_type_example" # character | the asset type (Optional)
#' var_approval_status <- "approval_status_example" # character | approval status for the asset (Optional)
#' var_assigned_account_id <- 56 # integer | account id assigned to the asset (Optional)
#' var_media <- File.new('/path/to/file') # data.frame | a MultipartFile containing the mimetype, etc (Optional)
#' var_media_url <- "media_url_example" # character | this can be used if the \"media\" is a link (optional) (Optional)
#' var_media_string <- "media_string_example" # character | the media content as a string (optional) (Optional)
#' var_media_string_file_name <- "media_string_file_name_example" # character | file name for mediaString (optional) (Optional)
#' var_media_string_content_type <- "media_string_content_type_example" # character | content type for mediaString (optional) (Optional)
#' var_media_height <- 56 # integer | the media height (optional) (Optional)
#' var_media_width <- 56 # integer | the media width (optional) (Optional)
#' var_attached_media <- File.new('/path/to/file') # data.frame | a MultipartFile containing an asset that the \"media\" file references (optional). Example: to upload a video the \"media\" file should contain a screen capture and the \"attachedMedia\" should be the actual video. (Optional)
#' var_attached_media_url <- "attached_media_url_example" # character | this can be used if the \"attachedMedia\" is a link (i.e. a Youtube video, etc) (optional) (Optional)
#' var_attached_media_string <- "attached_media_string_example" # character | attached media content as a string (optional) (Optional)
#' var_attached_media_string_file_name <- "attached_media_string_file_name_example" # character | file name for attachedMediaString (optional) (Optional)
#' var_attached_media_string_content_type <- "attached_media_string_content_type_example" # character | content type for attachedMediaString (optional) (Optional)
#' var_attached_media_height <- 56 # integer | the attached media height (optional) (Optional)
#' var_attached_media_width <- 56 # integer | the attached media width (optional) (Optional)
#' var_location_description <- "location_description_example" # character | the location description (optional) (Optional)
#' var_app <- "app_example" # character | Deprecated: app parameter (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_search_tags <- "search_tags_example" # character | the search tags (Optional)
#' var_latitude <- 3.4 # numeric | the latitude (optional) (Optional)
#' var_longitude <- 3.4 # numeric | the longitude (optional) (Optional)
#'
#' #Create Asset
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateAsset(return_nulls = var_return_nulls, device_id = var_device_id, account_id = var_account_id, album_id = var_album_id, collection_id = var_collection_id, add_to_default_album = var_add_to_default_album, add_to_media_library = var_add_to_media_library, version_code = var_version_code, version_name = var_version_name, meta_data = var_meta_data, caption = var_caption, asset_type = var_asset_type, approval_status = var_approval_status, assigned_account_id = var_assigned_account_id, media = var_media, media_url = var_media_url, media_string = var_media_string, media_string_file_name = var_media_string_file_name, media_string_content_type = var_media_string_content_type, media_height = var_media_height, media_width = var_media_width, attached_media = var_attached_media, attached_media_url = var_attached_media_url, attached_media_string = var_attached_media_string, attached_media_string_file_name = var_attached_media_string_file_name, attached_media_string_content_type = var_attached_media_string_content_type, attached_media_height = var_attached_media_height, attached_media_width = var_attached_media_width, location_description = var_location_description, app = var_app, app_key = var_app_key, search_tags = var_search_tags, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$CreateAsset(return_nulls = var_return_nulls, device_id = var_device_id, account_id = var_account_id, album_id = var_album_id, collection_id = var_collection_id, add_to_default_album = var_add_to_default_album, add_to_media_library = var_add_to_media_library, version_code = var_version_code, version_name = var_version_name, meta_data = var_meta_data, caption = var_caption, asset_type = var_asset_type, approval_status = var_approval_status, assigned_account_id = var_assigned_account_id, media = var_media, media_url = var_media_url, media_string = var_media_string, media_string_file_name = var_media_string_file_name, media_string_content_type = var_media_string_content_type, media_height = var_media_height, media_width = var_media_width, attached_media = var_attached_media, attached_media_url = var_attached_media_url, attached_media_string = var_attached_media_string, attached_media_string_file_name = var_attached_media_string_file_name, attached_media_string_content_type = var_attached_media_string_content_type, attached_media_height = var_attached_media_height, attached_media_width = var_attached_media_width, location_description = var_location_description, app = var_app, app_key = var_app_key, search_tags = var_search_tags, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  DeleteAsset  ####################
#'
#' library(openapi)
#' var_asset_id <- "asset_id_example" # character | the id of the asset to delete
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Delete Asset
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteAsset(var_asset_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$DeleteAsset(var_asset_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  GetAsset  ####################
#'
#' library(openapi)
#' var_asset_id <- 56 # integer | the asset ID
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_note_descending <- FALSE # character | determines whether the notes on the asset are in descending order (Optional)
#'
#' #Get Asset
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAsset(var_asset_id, device_id = var_device_id, account_id = var_account_id, note_descending = var_note_descendingdata_file = "result.txt")
#' result <- api_instance$GetAsset(var_asset_id, device_id = var_device_id, account_id = var_account_id, note_descending = var_note_descending)
#' dput(result)
#'
#'
#' ####################  RemoveAsset  ####################
#'
#' library(openapi)
#' var_asset_id <- "asset_id_example" # character | the id of the asset to remove
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_album_id <- 56 # integer | the album id to remove the asset from (Optional)
#' var_collection_id <- 56 # integer | the collection id to remove the asset from (Optional)
#' var_remove_from_default_albums <- "remove_from_default_albums_example" # character | remove from the user's profile album (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Remove Asset from Collection
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RemoveAsset(var_asset_id, device_id = var_device_id, account_id = var_account_id, album_id = var_album_id, collection_id = var_collection_id, remove_from_default_albums = var_remove_from_default_albums, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$RemoveAsset(var_asset_id, device_id = var_device_id, account_id = var_account_id, album_id = var_album_id, collection_id = var_collection_id, remove_from_default_albums = var_remove_from_default_albums, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  SearchAssets  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_album_ids <- "album_ids_example" # character | comma separated list of album ids to search on (Optional)
#' var_asset_ids <- "asset_ids_example" # character | comma separated list of asset ids to search on (Optional)
#' var_app_key <- "app_key_example" # character | the application key to filter results by application. (Optional)
#' var_media_type <- "media_type_example" # character | filter results by media type. Possible values include: APPLICATION, IMAGE, AUDIO, VIDEO, MULTIPART, TEXT (Optional)
#' var_mime_type <- "mime_type_example" # character | filter results by mime type (Optional)
#' var_keyword <- "keyword_example" # character | keyword search string (Optional)
#' var_version_code <- 56 # integer | filter results by version code (Optional)
#' var_version_name <- "version_name_example" # character | filter results by version name (Optional)
#' var_updated_since <- 56 # integer | return items that have been updated since this date (time-stamp in milliseconds) (Optional)
#' var_updated_before <- 56 # integer | return items that have been updated before this date (time-stamp in milliseconds) (Optional)
#' var_sort_field <- "sort_field_example" # character | the field to sort by. See AssetApiMap (Optional)
#' var_descending <- "descending_example" # character | determines whether the sorted list is in descending or ascending order (Optional)
#' var_search_media_library <- "search_media_library_example" # character | determines whether to search on the media library album (Optional)
#' var_filter_by_billable <- "filter_by_billable_example" # character | filter results by billable entity (Optional)
#' var_active_only <- "active_only_example" # character | if only searching for active assets (Optional)
#' var_return_app <- "return_app_example" # character | determines whether to include the application response as part of the asset response (Optional)
#' var_start <- 56 # integer | the start index for pagination (Optional)
#' var_limit <- 56 # integer | the limit for pagination (there is a hard limit of 200) (Optional)
#' var_search_mode <- "search_mode_example" # character | search mode for index (SearchIndexMode) (Optional)
#' var_asset_type <- "asset_type_example" # character | filter by asset type (Optional)
#' var_approval_status <- "approval_status_example" # character | filter by approval status (Optional)
#' var_assigned_account_id <- 56 # integer | filter results by an assigned account id (Optional)
#'
#' #Search Assets
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchAssets(device_id = var_device_id, account_id = var_account_id, album_ids = var_album_ids, asset_ids = var_asset_ids, app_key = var_app_key, media_type = var_media_type, mime_type = var_mime_type, keyword = var_keyword, version_code = var_version_code, version_name = var_version_name, updated_since = var_updated_since, updated_before = var_updated_before, sort_field = var_sort_field, descending = var_descending, search_media_library = var_search_media_library, filter_by_billable = var_filter_by_billable, active_only = var_active_only, return_app = var_return_app, start = var_start, limit = var_limit, search_mode = var_search_mode, asset_type = var_asset_type, approval_status = var_approval_status, assigned_account_id = var_assigned_account_iddata_file = "result.txt")
#' result <- api_instance$SearchAssets(device_id = var_device_id, account_id = var_account_id, album_ids = var_album_ids, asset_ids = var_asset_ids, app_key = var_app_key, media_type = var_media_type, mime_type = var_mime_type, keyword = var_keyword, version_code = var_version_code, version_name = var_version_name, updated_since = var_updated_since, updated_before = var_updated_before, sort_field = var_sort_field, descending = var_descending, search_media_library = var_search_media_library, filter_by_billable = var_filter_by_billable, active_only = var_active_only, return_app = var_return_app, start = var_start, limit = var_limit, search_mode = var_search_mode, asset_type = var_asset_type, approval_status = var_approval_status, assigned_account_id = var_assigned_account_id)
#' dput(result)
#'
#'
#' ####################  UpdateAsset  ####################
#'
#' library(openapi)
#' var_asset_id <- 56 # integer | the ID of the asset to update
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_album_id <- 56 # integer | the ID of the album to check user permissions (Optional)
#' var_attached_asset_id <- 56 # integer | the ID of the attached asset (Optional)
#' var_version_code <- 56 # integer | the version code (Optional)
#' var_version_name <- "version_name_example" # character | the version name (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_caption <- "caption_example" # character | the caption (Optional)
#' var_asset_type <- "asset_type_example" # character | the asset type (Optional)
#' var_approval_status <- "approval_status_example" # character | approval status for the asset (Optional)
#' var_assigned_account_id <- 56 # integer | account id assigned to the asset (Optional)
#' var_media <- File.new('/path/to/file') # data.frame | a MultipartFile containing the mimetype, etc (Optional)
#' var_media_url <- "media_url_example" # character | this can be used if the \"media\" is a link (optional) (Optional)
#' var_media_string <- "media_string_example" # character | the media content as a string (optional) (Optional)
#' var_media_string_file_name <- "media_string_file_name_example" # character | file name for mediaString (optional) (Optional)
#' var_media_string_content_type <- "media_string_content_type_example" # character | content type for mediaString (optional) (Optional)
#' var_media_height <- 56 # integer | the media height (optional) (Optional)
#' var_media_width <- 56 # integer | the media width (optional) (Optional)
#' var_attached_media <- File.new('/path/to/file') # data.frame | a MultipartFile containing an asset that the \"media\" file references (optional). Example: to upload a video the \"media\" file should contain a screen capture and the \"attachedMedia\" should be the actual video. (Optional)
#' var_attached_media_url <- "attached_media_url_example" # character | this can be used if the \"attachedMedia\" is a link (i.e. a Youtube video, etc) (optional) (Optional)
#' var_attached_media_string <- "attached_media_string_example" # character | attached media content as a string (optional) (Optional)
#' var_attached_media_string_file_name <- "attached_media_string_file_name_example" # character | file name for attachedMediaString (optional) (Optional)
#' var_attached_media_string_content_type <- "attached_media_string_content_type_example" # character | content type for attachedMediaString (optional) (Optional)
#' var_attached_media_height <- 56 # integer | the attached media height (optional) (Optional)
#' var_attached_media_width <- 56 # integer | the attached media width (optional) (Optional)
#' var_location_description <- "location_description_example" # character | the location description (Optional)
#' var_search_tags <- "search_tags_example" # character | search tags (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the asset's location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the asset's location (Optional)
#'
#' #Update Asset
#' api_instance <- AssetApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateAsset(var_asset_id, device_id = var_device_id, account_id = var_account_id, album_id = var_album_id, attached_asset_id = var_attached_asset_id, version_code = var_version_code, version_name = var_version_name, meta_data = var_meta_data, caption = var_caption, asset_type = var_asset_type, approval_status = var_approval_status, assigned_account_id = var_assigned_account_id, media = var_media, media_url = var_media_url, media_string = var_media_string, media_string_file_name = var_media_string_file_name, media_string_content_type = var_media_string_content_type, media_height = var_media_height, media_width = var_media_width, attached_media = var_attached_media, attached_media_url = var_attached_media_url, attached_media_string = var_attached_media_string, attached_media_string_file_name = var_attached_media_string_file_name, attached_media_string_content_type = var_attached_media_string_content_type, attached_media_height = var_attached_media_height, attached_media_width = var_attached_media_width, location_description = var_location_description, search_tags = var_search_tags, app_key = var_app_key, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$UpdateAsset(var_asset_id, device_id = var_device_id, account_id = var_account_id, album_id = var_album_id, attached_asset_id = var_attached_asset_id, version_code = var_version_code, version_name = var_version_name, meta_data = var_meta_data, caption = var_caption, asset_type = var_asset_type, approval_status = var_approval_status, assigned_account_id = var_assigned_account_id, media = var_media, media_url = var_media_url, media_string = var_media_string, media_string_file_name = var_media_string_file_name, media_string_content_type = var_media_string_content_type, media_height = var_media_height, media_width = var_media_width, attached_media = var_attached_media, attached_media_url = var_attached_media_url, attached_media_string = var_attached_media_string, attached_media_string_file_name = var_attached_media_string_file_name, attached_media_string_content_type = var_attached_media_string_content_type, attached_media_height = var_attached_media_height, attached_media_width = var_attached_media_width, location_description = var_location_description, search_tags = var_search_tags, app_key = var_app_key, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AssetApi <- R6::R6Class(
  "AssetApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AssetApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Download Asset
    #'
    #' @param filename the filename in the following formats: {assetId}-{suffix}.{extension} | {assetId}.{extension} | {assetId}
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    AssetDownload = function(filename, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AssetDownloadWithHttpInfo(filename, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Asset
    #'
    #' @param filename the filename in the following formats: {assetId}-{suffix}.{extension} | {assetId}.{extension} | {assetId}
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    AssetDownloadWithHttpInfo = function(filename, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`filename`)) {
        stop("Missing required parameter `filename`.")
      }

      if (!missing(`filename`) && is.null(`filename`)) {
        stop("Invalid value for `filename` when calling AssetApi$AssetDownload, `filename` is not nullable")
      }
      if (!is.null(`filename`) && !stringr::str_detect(`filename`, ".+")) {
        stop("Invalid value for `filename` when calling AssetApi$AssetDownload, must conform to the pattern .+.")
      }

      local_var_url_path <- "/asset/download/{filename}"
      if (!missing(`filename`)) {
        local_var_url_path <- gsub("\\{filename\\}", URLencode(as.character(`filename`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Convert Offer to Creative
    #'
    #' @param offer_id offer id used for inserting offer text/flavor
    #' @param ad_size the ad size used for selecting a format for the creative image
    #' @param creative_id (optional) used for inserting the newly created image into
    #' @param width (optional) total width of the creative image
    #' @param height (optional) total height of the creative image
    #' @param background_size (optional) the size of the background
    #' @param template (optional) the template to use
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AssetShortResponse
    AssetMorph = function(offer_id, ad_size, creative_id = NULL, width = NULL, height = NULL, background_size = NULL, template = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AssetMorphWithHttpInfo(offer_id, ad_size, creative_id, width, height, background_size, template, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Convert Offer to Creative
    #'
    #' @param offer_id offer id used for inserting offer text/flavor
    #' @param ad_size the ad size used for selecting a format for the creative image
    #' @param creative_id (optional) used for inserting the newly created image into
    #' @param width (optional) total width of the creative image
    #' @param height (optional) total height of the creative image
    #' @param background_size (optional) the size of the background
    #' @param template (optional) the template to use
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AssetShortResponse) with additional information such as HTTP status code, headers
    AssetMorphWithHttpInfo = function(offer_id, ad_size, creative_id = NULL, width = NULL, height = NULL, background_size = NULL, template = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offer_id`)) {
        stop("Missing required parameter `offer_id`.")
      }

      if (missing(`ad_size`)) {
        stop("Missing required parameter `ad_size`.")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling AssetApi$AssetMorph, `offer_id` is not nullable")
      }

      if (!missing(`ad_size`) && is.null(`ad_size`)) {
        stop("Invalid value for `ad_size` when calling AssetApi$AssetMorph, `ad_size` is not nullable")
      }

      if (!missing(`creative_id`) && is.null(`creative_id`)) {
        stop("Invalid value for `creative_id` when calling AssetApi$AssetMorph, `creative_id` is not nullable")
      }

      if (!missing(`width`) && is.null(`width`)) {
        stop("Invalid value for `width` when calling AssetApi$AssetMorph, `width` is not nullable")
      }

      if (!missing(`height`) && is.null(`height`)) {
        stop("Invalid value for `height` when calling AssetApi$AssetMorph, `height` is not nullable")
      }

      if (!missing(`background_size`) && is.null(`background_size`)) {
        stop("Invalid value for `background_size` when calling AssetApi$AssetMorph, `background_size` is not nullable")
      }

      if (!missing(`template`) && is.null(`template`)) {
        stop("Invalid value for `template` when calling AssetApi$AssetMorph, `template` is not nullable")
      }

      query_params[["offerId"]] <- `offer_id`

      query_params[["creativeId"]] <- `creative_id`

      if (!is.null(`ad_size`) && !(`ad_size` %in% c("CONFIG", "BANNER", "LEADERBOARD", "SKYSCRAPER", "VIDEO", "ZIP", "INTERSTITIAL", "CUSTOM1", "CUSTOM2", "CUSTOM3", "CUSTOM4", "CUSTOM5", "CUSTOM6", "CUSTOM7", "CUSTOM8", "CUSTOM9", "CUSTOM10"))) {
        stop("Invalid value for ad_size when calling AssetApi$AssetMorph. Must be [CONFIG, BANNER, LEADERBOARD, SKYSCRAPER, VIDEO, ZIP, INTERSTITIAL, CUSTOM1, CUSTOM2, CUSTOM3, CUSTOM4, CUSTOM5, CUSTOM6, CUSTOM7, CUSTOM8, CUSTOM9, CUSTOM10].")
      }
      query_params[["adSize"]] <- `ad_size`

      query_params[["width"]] <- `width`

      query_params[["height"]] <- `height`

      query_params[["backgroundSize"]] <- `background_size`

      query_params[["template"]] <- `template`

      local_var_url_path <- "/asset/morph"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AssetShortResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Asset
    #'
    #' @param return_nulls (optional) to return nulls
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param album_id (optional) the album the asset will be added to (optional)
    #' @param collection_id (optional) Deprecated: use collections via the collection endpoints.
    #' @param add_to_default_album (optional) Whether to add to a default album
    #' @param add_to_media_library (optional) Whether to add to the media library
    #' @param version_code (optional) the version code
    #' @param version_name (optional) the version name
    #' @param meta_data (optional) External custom client defined data
    #' @param caption (optional) the caption (optional)
    #' @param asset_type (optional) the asset type
    #' @param approval_status (optional) approval status for the asset
    #' @param assigned_account_id (optional) account id assigned to the asset
    #' @param media (optional) a MultipartFile containing the mimetype, etc
    #' @param media_url (optional) this can be used if the \"media\" is a link (optional)
    #' @param media_string (optional) the media content as a string (optional)
    #' @param media_string_file_name (optional) file name for mediaString (optional)
    #' @param media_string_content_type (optional) content type for mediaString (optional)
    #' @param media_height (optional) the media height (optional)
    #' @param media_width (optional) the media width (optional)
    #' @param attached_media (optional) a MultipartFile containing an asset that the \"media\" file references (optional). Example: to upload a video the \"media\" file should contain a screen capture and the \"attachedMedia\" should be the actual video.
    #' @param attached_media_url (optional) this can be used if the \"attachedMedia\" is a link (i.e. a Youtube video, etc) (optional)
    #' @param attached_media_string (optional) attached media content as a string (optional)
    #' @param attached_media_string_file_name (optional) file name for attachedMediaString (optional)
    #' @param attached_media_string_content_type (optional) content type for attachedMediaString (optional)
    #' @param attached_media_height (optional) the attached media height (optional)
    #' @param attached_media_width (optional) the attached media width (optional)
    #' @param location_description (optional) the location description (optional)
    #' @param app (optional) Deprecated: app parameter
    #' @param app_key (optional) the application key
    #' @param search_tags (optional) the search tags
    #' @param latitude (optional) the latitude (optional)
    #' @param longitude (optional) the longitude (optional)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AssetResponse
    CreateAsset = function(return_nulls = NULL, device_id = NULL, account_id = NULL, album_id = NULL, collection_id = NULL, add_to_default_album = NULL, add_to_media_library = NULL, version_code = NULL, version_name = NULL, meta_data = NULL, caption = NULL, asset_type = NULL, approval_status = NULL, assigned_account_id = NULL, media = NULL, media_url = NULL, media_string = NULL, media_string_file_name = NULL, media_string_content_type = NULL, media_height = NULL, media_width = NULL, attached_media = NULL, attached_media_url = NULL, attached_media_string = NULL, attached_media_string_file_name = NULL, attached_media_string_content_type = NULL, attached_media_height = NULL, attached_media_width = NULL, location_description = NULL, app = NULL, app_key = NULL, search_tags = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateAssetWithHttpInfo(return_nulls, device_id, account_id, album_id, collection_id, add_to_default_album, add_to_media_library, version_code, version_name, meta_data, caption, asset_type, approval_status, assigned_account_id, media, media_url, media_string, media_string_file_name, media_string_content_type, media_height, media_width, attached_media, attached_media_url, attached_media_string, attached_media_string_file_name, attached_media_string_content_type, attached_media_height, attached_media_width, location_description, app, app_key, search_tags, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Asset
    #'
    #' @param return_nulls (optional) to return nulls
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param album_id (optional) the album the asset will be added to (optional)
    #' @param collection_id (optional) Deprecated: use collections via the collection endpoints.
    #' @param add_to_default_album (optional) Whether to add to a default album
    #' @param add_to_media_library (optional) Whether to add to the media library
    #' @param version_code (optional) the version code
    #' @param version_name (optional) the version name
    #' @param meta_data (optional) External custom client defined data
    #' @param caption (optional) the caption (optional)
    #' @param asset_type (optional) the asset type
    #' @param approval_status (optional) approval status for the asset
    #' @param assigned_account_id (optional) account id assigned to the asset
    #' @param media (optional) a MultipartFile containing the mimetype, etc
    #' @param media_url (optional) this can be used if the \"media\" is a link (optional)
    #' @param media_string (optional) the media content as a string (optional)
    #' @param media_string_file_name (optional) file name for mediaString (optional)
    #' @param media_string_content_type (optional) content type for mediaString (optional)
    #' @param media_height (optional) the media height (optional)
    #' @param media_width (optional) the media width (optional)
    #' @param attached_media (optional) a MultipartFile containing an asset that the \"media\" file references (optional). Example: to upload a video the \"media\" file should contain a screen capture and the \"attachedMedia\" should be the actual video.
    #' @param attached_media_url (optional) this can be used if the \"attachedMedia\" is a link (i.e. a Youtube video, etc) (optional)
    #' @param attached_media_string (optional) attached media content as a string (optional)
    #' @param attached_media_string_file_name (optional) file name for attachedMediaString (optional)
    #' @param attached_media_string_content_type (optional) content type for attachedMediaString (optional)
    #' @param attached_media_height (optional) the attached media height (optional)
    #' @param attached_media_width (optional) the attached media width (optional)
    #' @param location_description (optional) the location description (optional)
    #' @param app (optional) Deprecated: app parameter
    #' @param app_key (optional) the application key
    #' @param search_tags (optional) the search tags
    #' @param latitude (optional) the latitude (optional)
    #' @param longitude (optional) the longitude (optional)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AssetResponse) with additional information such as HTTP status code, headers
    CreateAssetWithHttpInfo = function(return_nulls = NULL, device_id = NULL, account_id = NULL, album_id = NULL, collection_id = NULL, add_to_default_album = NULL, add_to_media_library = NULL, version_code = NULL, version_name = NULL, meta_data = NULL, caption = NULL, asset_type = NULL, approval_status = NULL, assigned_account_id = NULL, media = NULL, media_url = NULL, media_string = NULL, media_string_file_name = NULL, media_string_content_type = NULL, media_height = NULL, media_width = NULL, attached_media = NULL, attached_media_url = NULL, attached_media_string = NULL, attached_media_string_file_name = NULL, attached_media_string_content_type = NULL, attached_media_height = NULL, attached_media_width = NULL, location_description = NULL, app = NULL, app_key = NULL, search_tags = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`return_nulls`) && is.null(`return_nulls`)) {
        stop("Invalid value for `return_nulls` when calling AssetApi$CreateAsset, `return_nulls` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AssetApi$CreateAsset, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AssetApi$CreateAsset, `account_id` is not nullable")
      }

      if (!missing(`album_id`) && is.null(`album_id`)) {
        stop("Invalid value for `album_id` when calling AssetApi$CreateAsset, `album_id` is not nullable")
      }

      if (!missing(`collection_id`) && is.null(`collection_id`)) {
        stop("Invalid value for `collection_id` when calling AssetApi$CreateAsset, `collection_id` is not nullable")
      }

      if (!missing(`add_to_default_album`) && is.null(`add_to_default_album`)) {
        stop("Invalid value for `add_to_default_album` when calling AssetApi$CreateAsset, `add_to_default_album` is not nullable")
      }

      if (!missing(`add_to_media_library`) && is.null(`add_to_media_library`)) {
        stop("Invalid value for `add_to_media_library` when calling AssetApi$CreateAsset, `add_to_media_library` is not nullable")
      }

      if (!missing(`version_code`) && is.null(`version_code`)) {
        stop("Invalid value for `version_code` when calling AssetApi$CreateAsset, `version_code` is not nullable")
      }

      if (!missing(`version_name`) && is.null(`version_name`)) {
        stop("Invalid value for `version_name` when calling AssetApi$CreateAsset, `version_name` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling AssetApi$CreateAsset, `meta_data` is not nullable")
      }

      if (!missing(`caption`) && is.null(`caption`)) {
        stop("Invalid value for `caption` when calling AssetApi$CreateAsset, `caption` is not nullable")
      }

      if (!missing(`asset_type`) && is.null(`asset_type`)) {
        stop("Invalid value for `asset_type` when calling AssetApi$CreateAsset, `asset_type` is not nullable")
      }

      if (!missing(`approval_status`) && is.null(`approval_status`)) {
        stop("Invalid value for `approval_status` when calling AssetApi$CreateAsset, `approval_status` is not nullable")
      }

      if (!missing(`assigned_account_id`) && is.null(`assigned_account_id`)) {
        stop("Invalid value for `assigned_account_id` when calling AssetApi$CreateAsset, `assigned_account_id` is not nullable")
      }

      if (!missing(`media`) && is.null(`media`)) {
        stop("Invalid value for `media` when calling AssetApi$CreateAsset, `media` is not nullable")
      }

      if (!missing(`media_url`) && is.null(`media_url`)) {
        stop("Invalid value for `media_url` when calling AssetApi$CreateAsset, `media_url` is not nullable")
      }

      if (!missing(`media_string`) && is.null(`media_string`)) {
        stop("Invalid value for `media_string` when calling AssetApi$CreateAsset, `media_string` is not nullable")
      }

      if (!missing(`media_string_file_name`) && is.null(`media_string_file_name`)) {
        stop("Invalid value for `media_string_file_name` when calling AssetApi$CreateAsset, `media_string_file_name` is not nullable")
      }

      if (!missing(`media_string_content_type`) && is.null(`media_string_content_type`)) {
        stop("Invalid value for `media_string_content_type` when calling AssetApi$CreateAsset, `media_string_content_type` is not nullable")
      }

      if (!missing(`media_height`) && is.null(`media_height`)) {
        stop("Invalid value for `media_height` when calling AssetApi$CreateAsset, `media_height` is not nullable")
      }

      if (!missing(`media_width`) && is.null(`media_width`)) {
        stop("Invalid value for `media_width` when calling AssetApi$CreateAsset, `media_width` is not nullable")
      }

      if (!missing(`attached_media`) && is.null(`attached_media`)) {
        stop("Invalid value for `attached_media` when calling AssetApi$CreateAsset, `attached_media` is not nullable")
      }

      if (!missing(`attached_media_url`) && is.null(`attached_media_url`)) {
        stop("Invalid value for `attached_media_url` when calling AssetApi$CreateAsset, `attached_media_url` is not nullable")
      }

      if (!missing(`attached_media_string`) && is.null(`attached_media_string`)) {
        stop("Invalid value for `attached_media_string` when calling AssetApi$CreateAsset, `attached_media_string` is not nullable")
      }

      if (!missing(`attached_media_string_file_name`) && is.null(`attached_media_string_file_name`)) {
        stop("Invalid value for `attached_media_string_file_name` when calling AssetApi$CreateAsset, `attached_media_string_file_name` is not nullable")
      }

      if (!missing(`attached_media_string_content_type`) && is.null(`attached_media_string_content_type`)) {
        stop("Invalid value for `attached_media_string_content_type` when calling AssetApi$CreateAsset, `attached_media_string_content_type` is not nullable")
      }

      if (!missing(`attached_media_height`) && is.null(`attached_media_height`)) {
        stop("Invalid value for `attached_media_height` when calling AssetApi$CreateAsset, `attached_media_height` is not nullable")
      }

      if (!missing(`attached_media_width`) && is.null(`attached_media_width`)) {
        stop("Invalid value for `attached_media_width` when calling AssetApi$CreateAsset, `attached_media_width` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling AssetApi$CreateAsset, `location_description` is not nullable")
      }

      if (!missing(`app`) && is.null(`app`)) {
        stop("Invalid value for `app` when calling AssetApi$CreateAsset, `app` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AssetApi$CreateAsset, `app_key` is not nullable")
      }

      if (!missing(`search_tags`) && is.null(`search_tags`)) {
        stop("Invalid value for `search_tags` when calling AssetApi$CreateAsset, `search_tags` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AssetApi$CreateAsset, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AssetApi$CreateAsset, `longitude` is not nullable")
      }

      query_params[["returnNulls"]] <- `return_nulls`

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["albumId"]] <- `album_id`

      query_params[["collectionId"]] <- `collection_id`

      query_params[["addToDefaultAlbum"]] <- `add_to_default_album`

      query_params[["addToMediaLibrary"]] <- `add_to_media_library`

      query_params[["versionCode"]] <- `version_code`

      query_params[["versionName"]] <- `version_name`

      query_params[["metaData"]] <- `meta_data`

      query_params[["caption"]] <- `caption`

      query_params[["assetType"]] <- `asset_type`

      query_params[["approvalStatus"]] <- `approval_status`

      query_params[["assignedAccountId"]] <- `assigned_account_id`

      query_params[["media"]] <- `media`

      query_params[["mediaUrl"]] <- `media_url`

      query_params[["mediaString"]] <- `media_string`

      query_params[["mediaStringFileName"]] <- `media_string_file_name`

      query_params[["mediaStringContentType"]] <- `media_string_content_type`

      query_params[["mediaHeight"]] <- `media_height`

      query_params[["mediaWidth"]] <- `media_width`

      query_params[["attachedMedia"]] <- `attached_media`

      query_params[["attachedMediaUrl"]] <- `attached_media_url`

      query_params[["attachedMediaString"]] <- `attached_media_string`

      query_params[["attachedMediaStringFileName"]] <- `attached_media_string_file_name`

      query_params[["attachedMediaStringContentType"]] <- `attached_media_string_content_type`

      query_params[["attachedMediaHeight"]] <- `attached_media_height`

      query_params[["attachedMediaWidth"]] <- `attached_media_width`

      query_params[["locationDescription"]] <- `location_description`

      query_params[["app"]] <- `app`

      query_params[["appKey"]] <- `app_key`

      query_params[["searchTags"]] <- `search_tags`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/asset/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AssetResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Asset
    #'
    #' @param asset_id the id of the asset to delete
    #' @param device_id (optional) the device id (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteAsset = function(asset_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteAssetWithHttpInfo(asset_id, device_id, account_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Asset
    #'
    #' @param asset_id the id of the asset to delete
    #' @param device_id (optional) the device id (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteAssetWithHttpInfo = function(asset_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_id`)) {
        stop("Missing required parameter `asset_id`.")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling AssetApi$DeleteAsset, `asset_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AssetApi$DeleteAsset, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AssetApi$DeleteAsset, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AssetApi$DeleteAsset, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AssetApi$DeleteAsset, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["assetId"]] <- `asset_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/asset/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Asset
    #'
    #' @param asset_id the asset ID
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param note_descending (optional) determines whether the notes on the asset are in descending order (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AssetFullResponse
    GetAsset = function(asset_id, device_id = NULL, account_id = NULL, note_descending = FALSE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetAssetWithHttpInfo(asset_id, device_id, account_id, note_descending, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Asset
    #'
    #' @param asset_id the asset ID
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param note_descending (optional) determines whether the notes on the asset are in descending order (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AssetFullResponse) with additional information such as HTTP status code, headers
    GetAssetWithHttpInfo = function(asset_id, device_id = NULL, account_id = NULL, note_descending = FALSE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_id`)) {
        stop("Missing required parameter `asset_id`.")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling AssetApi$GetAsset, `asset_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AssetApi$GetAsset, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AssetApi$GetAsset, `account_id` is not nullable")
      }

      if (!missing(`note_descending`) && is.null(`note_descending`)) {
        stop("Invalid value for `note_descending` when calling AssetApi$GetAsset, `note_descending` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["assetId"]] <- `asset_id`

      query_params[["noteDescending"]] <- `note_descending`

      local_var_url_path <- "/asset/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AssetFullResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Remove Asset from Collection
    #'
    #' @param asset_id the id of the asset to remove
    #' @param device_id (optional) the device id (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param album_id (optional) the album id to remove the asset from
    #' @param collection_id (optional) the collection id to remove the asset from
    #' @param remove_from_default_albums (optional) remove from the user's profile album
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    RemoveAsset = function(asset_id, device_id = NULL, account_id = NULL, album_id = NULL, collection_id = NULL, remove_from_default_albums = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RemoveAssetWithHttpInfo(asset_id, device_id, account_id, album_id, collection_id, remove_from_default_albums, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Remove Asset from Collection
    #'
    #' @param asset_id the id of the asset to remove
    #' @param device_id (optional) the device id (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param album_id (optional) the album id to remove the asset from
    #' @param collection_id (optional) the collection id to remove the asset from
    #' @param remove_from_default_albums (optional) remove from the user's profile album
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    RemoveAssetWithHttpInfo = function(asset_id, device_id = NULL, account_id = NULL, album_id = NULL, collection_id = NULL, remove_from_default_albums = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_id`)) {
        stop("Missing required parameter `asset_id`.")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling AssetApi$RemoveAsset, `asset_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AssetApi$RemoveAsset, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AssetApi$RemoveAsset, `account_id` is not nullable")
      }

      if (!missing(`album_id`) && is.null(`album_id`)) {
        stop("Invalid value for `album_id` when calling AssetApi$RemoveAsset, `album_id` is not nullable")
      }

      if (!missing(`collection_id`) && is.null(`collection_id`)) {
        stop("Invalid value for `collection_id` when calling AssetApi$RemoveAsset, `collection_id` is not nullable")
      }

      if (!missing(`remove_from_default_albums`) && is.null(`remove_from_default_albums`)) {
        stop("Invalid value for `remove_from_default_albums` when calling AssetApi$RemoveAsset, `remove_from_default_albums` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AssetApi$RemoveAsset, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AssetApi$RemoveAsset, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["albumId"]] <- `album_id`

      query_params[["collectionId"]] <- `collection_id`

      query_params[["assetId"]] <- `asset_id`

      query_params[["removeFromDefaultAlbums"]] <- `remove_from_default_albums`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/asset/remove"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Assets
    #'
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param album_ids (optional) comma separated list of album ids to search on
    #' @param asset_ids (optional) comma separated list of asset ids to search on
    #' @param app_key (optional) the application key to filter results by application.
    #' @param media_type (optional) filter results by media type. Possible values include: APPLICATION, IMAGE, AUDIO, VIDEO, MULTIPART, TEXT
    #' @param mime_type (optional) filter results by mime type
    #' @param keyword (optional) keyword search string
    #' @param version_code (optional) filter results by version code
    #' @param version_name (optional) filter results by version name
    #' @param updated_since (optional) return items that have been updated since this date (time-stamp in milliseconds)
    #' @param updated_before (optional) return items that have been updated before this date (time-stamp in milliseconds)
    #' @param sort_field (optional) the field to sort by. See AssetApiMap
    #' @param descending (optional) determines whether the sorted list is in descending or ascending order
    #' @param search_media_library (optional) determines whether to search on the media library album
    #' @param filter_by_billable (optional) filter results by billable entity
    #' @param active_only (optional) if only searching for active assets
    #' @param return_app (optional) determines whether to include the application response as part of the asset response
    #' @param start (optional) the start index for pagination
    #' @param limit (optional) the limit for pagination (there is a hard limit of 200)
    #' @param search_mode (optional) search mode for index (SearchIndexMode)
    #' @param asset_type (optional) filter by asset type
    #' @param approval_status (optional) filter by approval status
    #' @param assigned_account_id (optional) filter results by an assigned account id
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[AssetResponse]
    SearchAssets = function(device_id = NULL, account_id = NULL, album_ids = NULL, asset_ids = NULL, app_key = NULL, media_type = NULL, mime_type = NULL, keyword = NULL, version_code = NULL, version_name = NULL, updated_since = NULL, updated_before = NULL, sort_field = NULL, descending = NULL, search_media_library = NULL, filter_by_billable = NULL, active_only = NULL, return_app = NULL, start = NULL, limit = NULL, search_mode = NULL, asset_type = NULL, approval_status = NULL, assigned_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchAssetsWithHttpInfo(device_id, account_id, album_ids, asset_ids, app_key, media_type, mime_type, keyword, version_code, version_name, updated_since, updated_before, sort_field, descending, search_media_library, filter_by_billable, active_only, return_app, start, limit, search_mode, asset_type, approval_status, assigned_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Assets
    #'
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param album_ids (optional) comma separated list of album ids to search on
    #' @param asset_ids (optional) comma separated list of asset ids to search on
    #' @param app_key (optional) the application key to filter results by application.
    #' @param media_type (optional) filter results by media type. Possible values include: APPLICATION, IMAGE, AUDIO, VIDEO, MULTIPART, TEXT
    #' @param mime_type (optional) filter results by mime type
    #' @param keyword (optional) keyword search string
    #' @param version_code (optional) filter results by version code
    #' @param version_name (optional) filter results by version name
    #' @param updated_since (optional) return items that have been updated since this date (time-stamp in milliseconds)
    #' @param updated_before (optional) return items that have been updated before this date (time-stamp in milliseconds)
    #' @param sort_field (optional) the field to sort by. See AssetApiMap
    #' @param descending (optional) determines whether the sorted list is in descending or ascending order
    #' @param search_media_library (optional) determines whether to search on the media library album
    #' @param filter_by_billable (optional) filter results by billable entity
    #' @param active_only (optional) if only searching for active assets
    #' @param return_app (optional) determines whether to include the application response as part of the asset response
    #' @param start (optional) the start index for pagination
    #' @param limit (optional) the limit for pagination (there is a hard limit of 200)
    #' @param search_mode (optional) search mode for index (SearchIndexMode)
    #' @param asset_type (optional) filter by asset type
    #' @param approval_status (optional) filter by approval status
    #' @param assigned_account_id (optional) filter results by an assigned account id
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[AssetResponse]) with additional information such as HTTP status code, headers
    SearchAssetsWithHttpInfo = function(device_id = NULL, account_id = NULL, album_ids = NULL, asset_ids = NULL, app_key = NULL, media_type = NULL, mime_type = NULL, keyword = NULL, version_code = NULL, version_name = NULL, updated_since = NULL, updated_before = NULL, sort_field = NULL, descending = NULL, search_media_library = NULL, filter_by_billable = NULL, active_only = NULL, return_app = NULL, start = NULL, limit = NULL, search_mode = NULL, asset_type = NULL, approval_status = NULL, assigned_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AssetApi$SearchAssets, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AssetApi$SearchAssets, `account_id` is not nullable")
      }

      if (!missing(`album_ids`) && is.null(`album_ids`)) {
        stop("Invalid value for `album_ids` when calling AssetApi$SearchAssets, `album_ids` is not nullable")
      }

      if (!missing(`asset_ids`) && is.null(`asset_ids`)) {
        stop("Invalid value for `asset_ids` when calling AssetApi$SearchAssets, `asset_ids` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AssetApi$SearchAssets, `app_key` is not nullable")
      }

      if (!missing(`media_type`) && is.null(`media_type`)) {
        stop("Invalid value for `media_type` when calling AssetApi$SearchAssets, `media_type` is not nullable")
      }

      if (!missing(`mime_type`) && is.null(`mime_type`)) {
        stop("Invalid value for `mime_type` when calling AssetApi$SearchAssets, `mime_type` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling AssetApi$SearchAssets, `keyword` is not nullable")
      }

      if (!missing(`version_code`) && is.null(`version_code`)) {
        stop("Invalid value for `version_code` when calling AssetApi$SearchAssets, `version_code` is not nullable")
      }

      if (!missing(`version_name`) && is.null(`version_name`)) {
        stop("Invalid value for `version_name` when calling AssetApi$SearchAssets, `version_name` is not nullable")
      }

      if (!missing(`updated_since`) && is.null(`updated_since`)) {
        stop("Invalid value for `updated_since` when calling AssetApi$SearchAssets, `updated_since` is not nullable")
      }

      if (!missing(`updated_before`) && is.null(`updated_before`)) {
        stop("Invalid value for `updated_before` when calling AssetApi$SearchAssets, `updated_before` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AssetApi$SearchAssets, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AssetApi$SearchAssets, `descending` is not nullable")
      }

      if (!missing(`search_media_library`) && is.null(`search_media_library`)) {
        stop("Invalid value for `search_media_library` when calling AssetApi$SearchAssets, `search_media_library` is not nullable")
      }

      if (!missing(`filter_by_billable`) && is.null(`filter_by_billable`)) {
        stop("Invalid value for `filter_by_billable` when calling AssetApi$SearchAssets, `filter_by_billable` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling AssetApi$SearchAssets, `active_only` is not nullable")
      }

      if (!missing(`return_app`) && is.null(`return_app`)) {
        stop("Invalid value for `return_app` when calling AssetApi$SearchAssets, `return_app` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AssetApi$SearchAssets, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AssetApi$SearchAssets, `limit` is not nullable")
      }

      if (!missing(`search_mode`) && is.null(`search_mode`)) {
        stop("Invalid value for `search_mode` when calling AssetApi$SearchAssets, `search_mode` is not nullable")
      }

      if (!missing(`asset_type`) && is.null(`asset_type`)) {
        stop("Invalid value for `asset_type` when calling AssetApi$SearchAssets, `asset_type` is not nullable")
      }

      if (!missing(`approval_status`) && is.null(`approval_status`)) {
        stop("Invalid value for `approval_status` when calling AssetApi$SearchAssets, `approval_status` is not nullable")
      }

      if (!missing(`assigned_account_id`) && is.null(`assigned_account_id`)) {
        stop("Invalid value for `assigned_account_id` when calling AssetApi$SearchAssets, `assigned_account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["albumIds"]] <- `album_ids`

      query_params[["assetIds"]] <- `asset_ids`

      query_params[["appKey"]] <- `app_key`

      query_params[["mediaType"]] <- `media_type`

      query_params[["mimeType"]] <- `mime_type`

      query_params[["keyword"]] <- `keyword`

      query_params[["versionCode"]] <- `version_code`

      query_params[["versionName"]] <- `version_name`

      query_params[["updatedSince"]] <- `updated_since`

      query_params[["updatedBefore"]] <- `updated_before`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["searchMediaLibrary"]] <- `search_media_library`

      query_params[["filterByBillable"]] <- `filter_by_billable`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["returnApp"]] <- `return_app`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["searchMode"]] <- `search_mode`

      query_params[["assetType"]] <- `asset_type`

      query_params[["approvalStatus"]] <- `approval_status`

      query_params[["assignedAccountId"]] <- `assigned_account_id`

      local_var_url_path <- "/asset/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[AssetResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Asset
    #'
    #' @param asset_id the ID of the asset to update
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param album_id (optional) the ID of the album to check user permissions
    #' @param attached_asset_id (optional) the ID of the attached asset
    #' @param version_code (optional) the version code
    #' @param version_name (optional) the version name
    #' @param meta_data (optional) External custom client defined data
    #' @param caption (optional) the caption
    #' @param asset_type (optional) the asset type
    #' @param approval_status (optional) approval status for the asset
    #' @param assigned_account_id (optional) account id assigned to the asset
    #' @param media (optional) a MultipartFile containing the mimetype, etc
    #' @param media_url (optional) this can be used if the \"media\" is a link (optional)
    #' @param media_string (optional) the media content as a string (optional)
    #' @param media_string_file_name (optional) file name for mediaString (optional)
    #' @param media_string_content_type (optional) content type for mediaString (optional)
    #' @param media_height (optional) the media height (optional)
    #' @param media_width (optional) the media width (optional)
    #' @param attached_media (optional) a MultipartFile containing an asset that the \"media\" file references (optional). Example: to upload a video the \"media\" file should contain a screen capture and the \"attachedMedia\" should be the actual video.
    #' @param attached_media_url (optional) this can be used if the \"attachedMedia\" is a link (i.e. a Youtube video, etc) (optional)
    #' @param attached_media_string (optional) attached media content as a string (optional)
    #' @param attached_media_string_file_name (optional) file name for attachedMediaString (optional)
    #' @param attached_media_string_content_type (optional) content type for attachedMediaString (optional)
    #' @param attached_media_height (optional) the attached media height (optional)
    #' @param attached_media_width (optional) the attached media width (optional)
    #' @param location_description (optional) the location description
    #' @param search_tags (optional) search tags
    #' @param app_key (optional) the application key
    #' @param latitude (optional) latitude used to update the asset's location
    #' @param longitude (optional) longitude used to update the asset's location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UpdateAsset = function(asset_id, device_id = NULL, account_id = NULL, album_id = NULL, attached_asset_id = NULL, version_code = NULL, version_name = NULL, meta_data = NULL, caption = NULL, asset_type = NULL, approval_status = NULL, assigned_account_id = NULL, media = NULL, media_url = NULL, media_string = NULL, media_string_file_name = NULL, media_string_content_type = NULL, media_height = NULL, media_width = NULL, attached_media = NULL, attached_media_url = NULL, attached_media_string = NULL, attached_media_string_file_name = NULL, attached_media_string_content_type = NULL, attached_media_height = NULL, attached_media_width = NULL, location_description = NULL, search_tags = NULL, app_key = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateAssetWithHttpInfo(asset_id, device_id, account_id, album_id, attached_asset_id, version_code, version_name, meta_data, caption, asset_type, approval_status, assigned_account_id, media, media_url, media_string, media_string_file_name, media_string_content_type, media_height, media_width, attached_media, attached_media_url, attached_media_string, attached_media_string_file_name, attached_media_string_content_type, attached_media_height, attached_media_width, location_description, search_tags, app_key, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Asset
    #'
    #' @param asset_id the ID of the asset to update
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param album_id (optional) the ID of the album to check user permissions
    #' @param attached_asset_id (optional) the ID of the attached asset
    #' @param version_code (optional) the version code
    #' @param version_name (optional) the version name
    #' @param meta_data (optional) External custom client defined data
    #' @param caption (optional) the caption
    #' @param asset_type (optional) the asset type
    #' @param approval_status (optional) approval status for the asset
    #' @param assigned_account_id (optional) account id assigned to the asset
    #' @param media (optional) a MultipartFile containing the mimetype, etc
    #' @param media_url (optional) this can be used if the \"media\" is a link (optional)
    #' @param media_string (optional) the media content as a string (optional)
    #' @param media_string_file_name (optional) file name for mediaString (optional)
    #' @param media_string_content_type (optional) content type for mediaString (optional)
    #' @param media_height (optional) the media height (optional)
    #' @param media_width (optional) the media width (optional)
    #' @param attached_media (optional) a MultipartFile containing an asset that the \"media\" file references (optional). Example: to upload a video the \"media\" file should contain a screen capture and the \"attachedMedia\" should be the actual video.
    #' @param attached_media_url (optional) this can be used if the \"attachedMedia\" is a link (i.e. a Youtube video, etc) (optional)
    #' @param attached_media_string (optional) attached media content as a string (optional)
    #' @param attached_media_string_file_name (optional) file name for attachedMediaString (optional)
    #' @param attached_media_string_content_type (optional) content type for attachedMediaString (optional)
    #' @param attached_media_height (optional) the attached media height (optional)
    #' @param attached_media_width (optional) the attached media width (optional)
    #' @param location_description (optional) the location description
    #' @param search_tags (optional) search tags
    #' @param app_key (optional) the application key
    #' @param latitude (optional) latitude used to update the asset's location
    #' @param longitude (optional) longitude used to update the asset's location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UpdateAssetWithHttpInfo = function(asset_id, device_id = NULL, account_id = NULL, album_id = NULL, attached_asset_id = NULL, version_code = NULL, version_name = NULL, meta_data = NULL, caption = NULL, asset_type = NULL, approval_status = NULL, assigned_account_id = NULL, media = NULL, media_url = NULL, media_string = NULL, media_string_file_name = NULL, media_string_content_type = NULL, media_height = NULL, media_width = NULL, attached_media = NULL, attached_media_url = NULL, attached_media_string = NULL, attached_media_string_file_name = NULL, attached_media_string_content_type = NULL, attached_media_height = NULL, attached_media_width = NULL, location_description = NULL, search_tags = NULL, app_key = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`asset_id`)) {
        stop("Missing required parameter `asset_id`.")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling AssetApi$UpdateAsset, `asset_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AssetApi$UpdateAsset, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AssetApi$UpdateAsset, `account_id` is not nullable")
      }

      if (!missing(`album_id`) && is.null(`album_id`)) {
        stop("Invalid value for `album_id` when calling AssetApi$UpdateAsset, `album_id` is not nullable")
      }

      if (!missing(`attached_asset_id`) && is.null(`attached_asset_id`)) {
        stop("Invalid value for `attached_asset_id` when calling AssetApi$UpdateAsset, `attached_asset_id` is not nullable")
      }

      if (!missing(`version_code`) && is.null(`version_code`)) {
        stop("Invalid value for `version_code` when calling AssetApi$UpdateAsset, `version_code` is not nullable")
      }

      if (!missing(`version_name`) && is.null(`version_name`)) {
        stop("Invalid value for `version_name` when calling AssetApi$UpdateAsset, `version_name` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling AssetApi$UpdateAsset, `meta_data` is not nullable")
      }

      if (!missing(`caption`) && is.null(`caption`)) {
        stop("Invalid value for `caption` when calling AssetApi$UpdateAsset, `caption` is not nullable")
      }

      if (!missing(`asset_type`) && is.null(`asset_type`)) {
        stop("Invalid value for `asset_type` when calling AssetApi$UpdateAsset, `asset_type` is not nullable")
      }

      if (!missing(`approval_status`) && is.null(`approval_status`)) {
        stop("Invalid value for `approval_status` when calling AssetApi$UpdateAsset, `approval_status` is not nullable")
      }

      if (!missing(`assigned_account_id`) && is.null(`assigned_account_id`)) {
        stop("Invalid value for `assigned_account_id` when calling AssetApi$UpdateAsset, `assigned_account_id` is not nullable")
      }

      if (!missing(`media`) && is.null(`media`)) {
        stop("Invalid value for `media` when calling AssetApi$UpdateAsset, `media` is not nullable")
      }

      if (!missing(`media_url`) && is.null(`media_url`)) {
        stop("Invalid value for `media_url` when calling AssetApi$UpdateAsset, `media_url` is not nullable")
      }

      if (!missing(`media_string`) && is.null(`media_string`)) {
        stop("Invalid value for `media_string` when calling AssetApi$UpdateAsset, `media_string` is not nullable")
      }

      if (!missing(`media_string_file_name`) && is.null(`media_string_file_name`)) {
        stop("Invalid value for `media_string_file_name` when calling AssetApi$UpdateAsset, `media_string_file_name` is not nullable")
      }

      if (!missing(`media_string_content_type`) && is.null(`media_string_content_type`)) {
        stop("Invalid value for `media_string_content_type` when calling AssetApi$UpdateAsset, `media_string_content_type` is not nullable")
      }

      if (!missing(`media_height`) && is.null(`media_height`)) {
        stop("Invalid value for `media_height` when calling AssetApi$UpdateAsset, `media_height` is not nullable")
      }

      if (!missing(`media_width`) && is.null(`media_width`)) {
        stop("Invalid value for `media_width` when calling AssetApi$UpdateAsset, `media_width` is not nullable")
      }

      if (!missing(`attached_media`) && is.null(`attached_media`)) {
        stop("Invalid value for `attached_media` when calling AssetApi$UpdateAsset, `attached_media` is not nullable")
      }

      if (!missing(`attached_media_url`) && is.null(`attached_media_url`)) {
        stop("Invalid value for `attached_media_url` when calling AssetApi$UpdateAsset, `attached_media_url` is not nullable")
      }

      if (!missing(`attached_media_string`) && is.null(`attached_media_string`)) {
        stop("Invalid value for `attached_media_string` when calling AssetApi$UpdateAsset, `attached_media_string` is not nullable")
      }

      if (!missing(`attached_media_string_file_name`) && is.null(`attached_media_string_file_name`)) {
        stop("Invalid value for `attached_media_string_file_name` when calling AssetApi$UpdateAsset, `attached_media_string_file_name` is not nullable")
      }

      if (!missing(`attached_media_string_content_type`) && is.null(`attached_media_string_content_type`)) {
        stop("Invalid value for `attached_media_string_content_type` when calling AssetApi$UpdateAsset, `attached_media_string_content_type` is not nullable")
      }

      if (!missing(`attached_media_height`) && is.null(`attached_media_height`)) {
        stop("Invalid value for `attached_media_height` when calling AssetApi$UpdateAsset, `attached_media_height` is not nullable")
      }

      if (!missing(`attached_media_width`) && is.null(`attached_media_width`)) {
        stop("Invalid value for `attached_media_width` when calling AssetApi$UpdateAsset, `attached_media_width` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling AssetApi$UpdateAsset, `location_description` is not nullable")
      }

      if (!missing(`search_tags`) && is.null(`search_tags`)) {
        stop("Invalid value for `search_tags` when calling AssetApi$UpdateAsset, `search_tags` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AssetApi$UpdateAsset, `app_key` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling AssetApi$UpdateAsset, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling AssetApi$UpdateAsset, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["assetId"]] <- `asset_id`

      query_params[["albumId"]] <- `album_id`

      query_params[["attachedAssetId"]] <- `attached_asset_id`

      query_params[["versionCode"]] <- `version_code`

      query_params[["versionName"]] <- `version_name`

      query_params[["metaData"]] <- `meta_data`

      query_params[["caption"]] <- `caption`

      query_params[["assetType"]] <- `asset_type`

      query_params[["approvalStatus"]] <- `approval_status`

      query_params[["assignedAccountId"]] <- `assigned_account_id`

      query_params[["media"]] <- `media`

      query_params[["mediaUrl"]] <- `media_url`

      query_params[["mediaString"]] <- `media_string`

      query_params[["mediaStringFileName"]] <- `media_string_file_name`

      query_params[["mediaStringContentType"]] <- `media_string_content_type`

      query_params[["mediaHeight"]] <- `media_height`

      query_params[["mediaWidth"]] <- `media_width`

      query_params[["attachedMedia"]] <- `attached_media`

      query_params[["attachedMediaUrl"]] <- `attached_media_url`

      query_params[["attachedMediaString"]] <- `attached_media_string`

      query_params[["attachedMediaStringFileName"]] <- `attached_media_string_file_name`

      query_params[["attachedMediaStringContentType"]] <- `attached_media_string_content_type`

      query_params[["attachedMediaHeight"]] <- `attached_media_height`

      query_params[["attachedMediaWidth"]] <- `attached_media_width`

      query_params[["locationDescription"]] <- `location_description`

      query_params[["searchTags"]] <- `search_tags`

      query_params[["appKey"]] <- `app_key`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/asset/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
