#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ThemeDescriptor operations
#' @description ThemeDescriptorApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AddOrUpdateThemeDescriptor  ####################
#'
#' library(openapi)
#' var_public_read <- "public_read_example" # character | determines whether the theme's participants have read permissions
#' var_public_write <- "public_write_example" # character | determines whether the theme's participants have write permissions
#' var_public_delete <- "public_delete_example" # character | determines whether the theme's participants have delete permissions
#' var_public_add <- "public_add_example" # character | 
#' var_visibility <- "visibility_example" # character | the determines the theme's participants (PUBLIC - includes everyone in the system as a potential participant, PRIVATE - only considers people who have been invited as participants)
#' var_include_friend_group <- "include_friend_group_example" # character | flag to determine whether to share to the user's \"friends\" group
#' var_complete_with_default_values <- "complete_with_default_values_example" # character | determines whether to use default values to complete the theme
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_game_type <- "game_type_example" # character | the application key (Optional)
#' var_theme_descriptor_id <- 56 # integer | the theme descriptor id used to update an existing theme, leave empty for creating a new theme (Optional)
#' var_title <- "title_example" # character | the title of the theme (Optional)
#' var_description <- "description_example" # character | the description of the theme (Optional)
#' var_connection_ids_to_add <- "connection_ids_to_add_example" # character | a comma separated list of connection IDs to share to users (Optional)
#' var_connection_group_ids_to_add <- "connection_group_ids_to_add_example" # character | a comma separated list of connection group IDs to share to groups (Optional)
#' var_app_version <- "app_version_example" # character | the application version the theme was created for (Optional)
#' var_color_value_json <- "color_value_json_example" # character | a json array used to replace colors within the application. Example: ```json [   {     \"name\": \"sceneAtlas\",     \"valueString\": \"100,100,100,255\"   },   {     \"name\": \"bg\",     \"valueString\": \"100,100,100,255\"   } ] ```  (Optional)
#' var_string_replacer_json <- "string_replacer_json_example" # character | a json array used to replace strings within the application. Example: ```json [   {     \"name\": \"coins\",     \"valueString\": \"Gems\"   },   {     \"name\": \"lives\",     \"valueString\": \"lives\"   } ] ```  (Optional)
#' var_custom_json_objects <- "custom_json_objects_example" # character | a json object used by the scene atlas to position sprites. Example: ```json {   \"pShootingOffset\": [     {       \"name\": \"x\",       \"valueString\": \"2.2\"     },     {       \"name\": \"y\",       \"valueString\": \"-0.3\"     }   ],   \"e1ShootingOffset\": [     {       \"name\": \"x\",       \"valueString\": \"0.25\"     },     {       \"name\": \"y\",       \"valueString\": \"0.5\"     }   ] } ```  (Optional)
#' var_icon_image <- File.new('/path/to/file') # data.frame | a MultipartFile containing the image used as the theme icon (Optional)
#' var_scene_atlas_image <- File.new('/path/to/file') # data.frame | a MultipartFile containing the scene atlas (Optional)
#' var_bg_image <- File.new('/path/to/file') # data.frame | a MultipartFile containing the background image (Optional)
#' var_bg_sound <- File.new('/path/to/file') # data.frame | a MultipartFile containing the background sound file (preferably in MP3 format) (Optional)
#' var_music_selection <- "music_selection_example" # character | used to select a default sound file that already exists in the application (Optional)
#' var_location_description <- "location_description_example" # character | the description of the user's current location (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#'
#' #Create/Update Theme
#' api_instance <- ThemeDescriptorApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AddOrUpdateThemeDescriptor(var_public_read, var_public_write, var_public_delete, var_public_add, var_visibility, var_include_friend_group, var_complete_with_default_values, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, theme_descriptor_id = var_theme_descriptor_id, title = var_title, description = var_description, connection_ids_to_add = var_connection_ids_to_add, connection_group_ids_to_add = var_connection_group_ids_to_add, app_version = var_app_version, color_value_json = var_color_value_json, string_replacer_json = var_string_replacer_json, custom_json_objects = var_custom_json_objects, icon_image = var_icon_image, scene_atlas_image = var_scene_atlas_image, bg_image = var_bg_image, bg_sound = var_bg_sound, music_selection = var_music_selection, location_description = var_location_description, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$AddOrUpdateThemeDescriptor(var_public_read, var_public_write, var_public_delete, var_public_add, var_visibility, var_include_friend_group, var_complete_with_default_values, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, theme_descriptor_id = var_theme_descriptor_id, title = var_title, description = var_description, connection_ids_to_add = var_connection_ids_to_add, connection_group_ids_to_add = var_connection_group_ids_to_add, app_version = var_app_version, color_value_json = var_color_value_json, string_replacer_json = var_string_replacer_json, custom_json_objects = var_custom_json_objects, icon_image = var_icon_image, scene_atlas_image = var_scene_atlas_image, bg_image = var_bg_image, bg_sound = var_bg_sound, music_selection = var_music_selection, location_description = var_location_description, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  GetThemeDescriptor  ####################
#'
#' library(openapi)
#' var_theme_descriptor_id <- 56 # integer | the theme id
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_game_type <- "game_type_example" # character | the application key (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Get Theme
#' api_instance <- ThemeDescriptorApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetThemeDescriptor(var_theme_descriptor_id, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$GetThemeDescriptor(var_theme_descriptor_id, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  GetThemeDescriptors  ####################
#'
#' library(openapi)
#' var_filter <- "filter_example" # character | a comma separated list of Ownership
#' var_sort_field <- "sort_field_example" # character | the field to sort by. See ThemeDescriptorApiMap
#' var_descending <- "descending_example" # character | determines whether the sorted list is in descending or ascending order
#' var_start <- 56 # integer | the start parameter for pagination
#' var_limit <- 56 # integer | the limit parameter for pagination
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_game_type <- "game_type_example" # character | the unique title of an application given from the admin tool (Optional)
#' var_contest_type <- "contest_type_example" # character | contest type (Optional)
#' var_owner_id <- 56 # integer | search on contests that an account has access to (Optional)
#' var_q <- "q_example" # character | This parameter is deprecated. (Optional)
#' var_keyword <- "keyword_example" # character | a keyword to search on (Optional)
#' var__i <- 56 # integer | This parameter is deprecated. (Optional)
#' var__l <- 56 # integer | This parameter is deprecated. (Optional)
#' var_date_created <- 56 # integer | filter on items that have been created before this date (Optional)
#' var_app_version <- "app_version_example" # character | application version of the theme to filter by (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Search Themes
#' api_instance <- ThemeDescriptorApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetThemeDescriptors(var_filter, var_sort_field, var_descending, var_start, var_limit, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, contest_type = var_contest_type, owner_id = var_owner_id, q = var_q, keyword = var_keyword, _i = var__i, _l = var__l, date_created = var_date_created, app_version = var_app_version, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$GetThemeDescriptors(var_filter, var_sort_field, var_descending, var_start, var_limit, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, contest_type = var_contest_type, owner_id = var_owner_id, q = var_q, keyword = var_keyword, _i = var__i, _l = var__l, date_created = var_date_created, app_version = var_app_version, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  RemoveThemeDescriptor  ####################
#'
#' library(openapi)
#' var_theme_descriptor_id <- 56 # integer | the theme id to remove
#' var_device_id <- "device_id_example" # character | a unique id given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_game_type <- "game_type_example" # character | the unique title of an application given from the admin tool (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Delete Theme
#' api_instance <- ThemeDescriptorApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RemoveThemeDescriptor(var_theme_descriptor_id, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$RemoveThemeDescriptor(var_theme_descriptor_id, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ThemeDescriptorApi <- R6::R6Class(
  "ThemeDescriptorApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ThemeDescriptorApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create/Update Theme
    #'
    #' @param public_read determines whether the theme's participants have read permissions
    #' @param public_write determines whether the theme's participants have write permissions
    #' @param public_delete determines whether the theme's participants have delete permissions
    #' @param public_add 
    #' @param visibility the determines the theme's participants (PUBLIC - includes everyone in the system as a potential participant, PRIVATE - only considers people who have been invited as participants)
    #' @param include_friend_group flag to determine whether to share to the user's \"friends\" group
    #' @param complete_with_default_values determines whether to use default values to complete the theme
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param game_type (optional) the application key
    #' @param theme_descriptor_id (optional) the theme descriptor id used to update an existing theme, leave empty for creating a new theme
    #' @param title (optional) the title of the theme
    #' @param description (optional) the description of the theme
    #' @param connection_ids_to_add (optional) a comma separated list of connection IDs to share to users
    #' @param connection_group_ids_to_add (optional) a comma separated list of connection group IDs to share to groups
    #' @param app_version (optional) the application version the theme was created for
    #' @param color_value_json (optional) a json array used to replace colors within the application. Example: ```json [   {     \"name\": \"sceneAtlas\",     \"valueString\": \"100,100,100,255\"   },   {     \"name\": \"bg\",     \"valueString\": \"100,100,100,255\"   } ] ``` 
    #' @param string_replacer_json (optional) a json array used to replace strings within the application. Example: ```json [   {     \"name\": \"coins\",     \"valueString\": \"Gems\"   },   {     \"name\": \"lives\",     \"valueString\": \"lives\"   } ] ``` 
    #' @param custom_json_objects (optional) a json object used by the scene atlas to position sprites. Example: ```json {   \"pShootingOffset\": [     {       \"name\": \"x\",       \"valueString\": \"2.2\"     },     {       \"name\": \"y\",       \"valueString\": \"-0.3\"     }   ],   \"e1ShootingOffset\": [     {       \"name\": \"x\",       \"valueString\": \"0.25\"     },     {       \"name\": \"y\",       \"valueString\": \"0.5\"     }   ] } ``` 
    #' @param icon_image (optional) a MultipartFile containing the image used as the theme icon
    #' @param scene_atlas_image (optional) a MultipartFile containing the scene atlas
    #' @param bg_image (optional) a MultipartFile containing the background image
    #' @param bg_sound (optional) a MultipartFile containing the background sound file (preferably in MP3 format)
    #' @param music_selection (optional) used to select a default sound file that already exists in the application
    #' @param location_description (optional) the description of the user's current location
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ThemeDescriptorResponse
    AddOrUpdateThemeDescriptor = function(public_read, public_write, public_delete, public_add, visibility, include_friend_group, complete_with_default_values, device_id = NULL, account_id = NULL, game_type = NULL, theme_descriptor_id = NULL, title = NULL, description = NULL, connection_ids_to_add = NULL, connection_group_ids_to_add = NULL, app_version = NULL, color_value_json = NULL, string_replacer_json = NULL, custom_json_objects = NULL, icon_image = NULL, scene_atlas_image = NULL, bg_image = NULL, bg_sound = NULL, music_selection = NULL, location_description = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AddOrUpdateThemeDescriptorWithHttpInfo(public_read, public_write, public_delete, public_add, visibility, include_friend_group, complete_with_default_values, device_id, account_id, game_type, theme_descriptor_id, title, description, connection_ids_to_add, connection_group_ids_to_add, app_version, color_value_json, string_replacer_json, custom_json_objects, icon_image, scene_atlas_image, bg_image, bg_sound, music_selection, location_description, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create/Update Theme
    #'
    #' @param public_read determines whether the theme's participants have read permissions
    #' @param public_write determines whether the theme's participants have write permissions
    #' @param public_delete determines whether the theme's participants have delete permissions
    #' @param public_add 
    #' @param visibility the determines the theme's participants (PUBLIC - includes everyone in the system as a potential participant, PRIVATE - only considers people who have been invited as participants)
    #' @param include_friend_group flag to determine whether to share to the user's \"friends\" group
    #' @param complete_with_default_values determines whether to use default values to complete the theme
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param game_type (optional) the application key
    #' @param theme_descriptor_id (optional) the theme descriptor id used to update an existing theme, leave empty for creating a new theme
    #' @param title (optional) the title of the theme
    #' @param description (optional) the description of the theme
    #' @param connection_ids_to_add (optional) a comma separated list of connection IDs to share to users
    #' @param connection_group_ids_to_add (optional) a comma separated list of connection group IDs to share to groups
    #' @param app_version (optional) the application version the theme was created for
    #' @param color_value_json (optional) a json array used to replace colors within the application. Example: ```json [   {     \"name\": \"sceneAtlas\",     \"valueString\": \"100,100,100,255\"   },   {     \"name\": \"bg\",     \"valueString\": \"100,100,100,255\"   } ] ``` 
    #' @param string_replacer_json (optional) a json array used to replace strings within the application. Example: ```json [   {     \"name\": \"coins\",     \"valueString\": \"Gems\"   },   {     \"name\": \"lives\",     \"valueString\": \"lives\"   } ] ``` 
    #' @param custom_json_objects (optional) a json object used by the scene atlas to position sprites. Example: ```json {   \"pShootingOffset\": [     {       \"name\": \"x\",       \"valueString\": \"2.2\"     },     {       \"name\": \"y\",       \"valueString\": \"-0.3\"     }   ],   \"e1ShootingOffset\": [     {       \"name\": \"x\",       \"valueString\": \"0.25\"     },     {       \"name\": \"y\",       \"valueString\": \"0.5\"     }   ] } ``` 
    #' @param icon_image (optional) a MultipartFile containing the image used as the theme icon
    #' @param scene_atlas_image (optional) a MultipartFile containing the scene atlas
    #' @param bg_image (optional) a MultipartFile containing the background image
    #' @param bg_sound (optional) a MultipartFile containing the background sound file (preferably in MP3 format)
    #' @param music_selection (optional) used to select a default sound file that already exists in the application
    #' @param location_description (optional) the description of the user's current location
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ThemeDescriptorResponse) with additional information such as HTTP status code, headers
    AddOrUpdateThemeDescriptorWithHttpInfo = function(public_read, public_write, public_delete, public_add, visibility, include_friend_group, complete_with_default_values, device_id = NULL, account_id = NULL, game_type = NULL, theme_descriptor_id = NULL, title = NULL, description = NULL, connection_ids_to_add = NULL, connection_group_ids_to_add = NULL, app_version = NULL, color_value_json = NULL, string_replacer_json = NULL, custom_json_objects = NULL, icon_image = NULL, scene_atlas_image = NULL, bg_image = NULL, bg_sound = NULL, music_selection = NULL, location_description = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`public_read`)) {
        stop("Missing required parameter `public_read`.")
      }

      if (missing(`public_write`)) {
        stop("Missing required parameter `public_write`.")
      }

      if (missing(`public_delete`)) {
        stop("Missing required parameter `public_delete`.")
      }

      if (missing(`public_add`)) {
        stop("Missing required parameter `public_add`.")
      }

      if (missing(`visibility`)) {
        stop("Missing required parameter `visibility`.")
      }

      if (missing(`include_friend_group`)) {
        stop("Missing required parameter `include_friend_group`.")
      }

      if (missing(`complete_with_default_values`)) {
        stop("Missing required parameter `complete_with_default_values`.")
      }

      if (!missing(`public_read`) && is.null(`public_read`)) {
        stop("Invalid value for `public_read` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `public_read` is not nullable")
      }

      if (!missing(`public_write`) && is.null(`public_write`)) {
        stop("Invalid value for `public_write` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `public_write` is not nullable")
      }

      if (!missing(`public_delete`) && is.null(`public_delete`)) {
        stop("Invalid value for `public_delete` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `public_delete` is not nullable")
      }

      if (!missing(`public_add`) && is.null(`public_add`)) {
        stop("Invalid value for `public_add` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `public_add` is not nullable")
      }

      if (!missing(`visibility`) && is.null(`visibility`)) {
        stop("Invalid value for `visibility` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `visibility` is not nullable")
      }

      if (!missing(`include_friend_group`) && is.null(`include_friend_group`)) {
        stop("Invalid value for `include_friend_group` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `include_friend_group` is not nullable")
      }

      if (!missing(`complete_with_default_values`) && is.null(`complete_with_default_values`)) {
        stop("Invalid value for `complete_with_default_values` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `complete_with_default_values` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `account_id` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `game_type` is not nullable")
      }

      if (!missing(`theme_descriptor_id`) && is.null(`theme_descriptor_id`)) {
        stop("Invalid value for `theme_descriptor_id` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `theme_descriptor_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `title` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `description` is not nullable")
      }

      if (!missing(`connection_ids_to_add`) && is.null(`connection_ids_to_add`)) {
        stop("Invalid value for `connection_ids_to_add` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `connection_ids_to_add` is not nullable")
      }

      if (!missing(`connection_group_ids_to_add`) && is.null(`connection_group_ids_to_add`)) {
        stop("Invalid value for `connection_group_ids_to_add` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `connection_group_ids_to_add` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `app_version` is not nullable")
      }

      if (!missing(`color_value_json`) && is.null(`color_value_json`)) {
        stop("Invalid value for `color_value_json` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `color_value_json` is not nullable")
      }

      if (!missing(`string_replacer_json`) && is.null(`string_replacer_json`)) {
        stop("Invalid value for `string_replacer_json` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `string_replacer_json` is not nullable")
      }

      if (!missing(`custom_json_objects`) && is.null(`custom_json_objects`)) {
        stop("Invalid value for `custom_json_objects` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `custom_json_objects` is not nullable")
      }

      if (!missing(`icon_image`) && is.null(`icon_image`)) {
        stop("Invalid value for `icon_image` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `icon_image` is not nullable")
      }

      if (!missing(`scene_atlas_image`) && is.null(`scene_atlas_image`)) {
        stop("Invalid value for `scene_atlas_image` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `scene_atlas_image` is not nullable")
      }

      if (!missing(`bg_image`) && is.null(`bg_image`)) {
        stop("Invalid value for `bg_image` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `bg_image` is not nullable")
      }

      if (!missing(`bg_sound`) && is.null(`bg_sound`)) {
        stop("Invalid value for `bg_sound` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `bg_sound` is not nullable")
      }

      if (!missing(`music_selection`) && is.null(`music_selection`)) {
        stop("Invalid value for `music_selection` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `music_selection` is not nullable")
      }

      if (!missing(`location_description`) && is.null(`location_description`)) {
        stop("Invalid value for `location_description` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `location_description` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["themeDescriptorId"]] <- `theme_descriptor_id`

      query_params[["title"]] <- `title`

      query_params[["description"]] <- `description`

      query_params[["publicRead"]] <- `public_read`

      query_params[["publicWrite"]] <- `public_write`

      query_params[["publicDelete"]] <- `public_delete`

      query_params[["publicAdd"]] <- `public_add`

      if (!is.null(`visibility`) && !(`visibility` %in% c("PUBLIC", "PRIVATE", "FRIENDS"))) {
        stop("Invalid value for visibility when calling ThemeDescriptorApi$AddOrUpdateThemeDescriptor. Must be [PUBLIC, PRIVATE, FRIENDS].")
      }
      query_params[["visibility"]] <- `visibility`

      query_params[["connectionIdsToAdd"]] <- `connection_ids_to_add`

      query_params[["connectionGroupIdsToAdd"]] <- `connection_group_ids_to_add`

      query_params[["includeFriendGroup"]] <- `include_friend_group`

      query_params[["appVersion"]] <- `app_version`

      query_params[["colorValueJson"]] <- `color_value_json`

      query_params[["stringReplacerJson"]] <- `string_replacer_json`

      query_params[["customJsonObjects"]] <- `custom_json_objects`

      query_params[["iconImage"]] <- `icon_image`

      query_params[["sceneAtlasImage"]] <- `scene_atlas_image`

      query_params[["bgImage"]] <- `bg_image`

      query_params[["bgSound"]] <- `bg_sound`

      query_params[["musicSelection"]] <- `music_selection`

      query_params[["completeWithDefaultValues"]] <- `complete_with_default_values`

      query_params[["locationDescription"]] <- `location_description`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/consumer/theme"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ThemeDescriptorResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Theme
    #'
    #' @param theme_descriptor_id the theme id
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param game_type (optional) the application key
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PurchaseItemListResponse
    GetThemeDescriptor = function(theme_descriptor_id, device_id = NULL, account_id = NULL, game_type = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetThemeDescriptorWithHttpInfo(theme_descriptor_id, device_id, account_id, game_type, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Theme
    #'
    #' @param theme_descriptor_id the theme id
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param game_type (optional) the application key
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PurchaseItemListResponse) with additional information such as HTTP status code, headers
    GetThemeDescriptorWithHttpInfo = function(theme_descriptor_id, device_id = NULL, account_id = NULL, game_type = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`theme_descriptor_id`)) {
        stop("Missing required parameter `theme_descriptor_id`.")
      }

      if (!missing(`theme_descriptor_id`) && is.null(`theme_descriptor_id`)) {
        stop("Invalid value for `theme_descriptor_id` when calling ThemeDescriptorApi$GetThemeDescriptor, `theme_descriptor_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ThemeDescriptorApi$GetThemeDescriptor, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThemeDescriptorApi$GetThemeDescriptor, `account_id` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling ThemeDescriptorApi$GetThemeDescriptor, `game_type` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ThemeDescriptorApi$GetThemeDescriptor, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ThemeDescriptorApi$GetThemeDescriptor, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["themeDescriptorId"]] <- `theme_descriptor_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/consumer/theme/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PurchaseItemListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Themes
    #'
    #' @param filter a comma separated list of Ownership
    #' @param sort_field the field to sort by. See ThemeDescriptorApiMap
    #' @param descending determines whether the sorted list is in descending or ascending order
    #' @param start the start parameter for pagination
    #' @param limit the limit parameter for pagination
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param game_type (optional) the unique title of an application given from the admin tool
    #' @param contest_type (optional) contest type
    #' @param owner_id (optional) search on contests that an account has access to
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) a keyword to search on
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param date_created (optional) filter on items that have been created before this date
    #' @param app_version (optional) application version of the theme to filter by
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PurchaseItemListResponse
    GetThemeDescriptors = function(filter, sort_field, descending, start, limit, device_id = NULL, account_id = NULL, game_type = NULL, contest_type = NULL, owner_id = NULL, q = NULL, keyword = NULL, _i = NULL, _l = NULL, date_created = NULL, app_version = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetThemeDescriptorsWithHttpInfo(filter, sort_field, descending, start, limit, device_id, account_id, game_type, contest_type, owner_id, q, keyword, _i, _l, date_created, app_version, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Themes
    #'
    #' @param filter a comma separated list of Ownership
    #' @param sort_field the field to sort by. See ThemeDescriptorApiMap
    #' @param descending determines whether the sorted list is in descending or ascending order
    #' @param start the start parameter for pagination
    #' @param limit the limit parameter for pagination
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param game_type (optional) the unique title of an application given from the admin tool
    #' @param contest_type (optional) contest type
    #' @param owner_id (optional) search on contests that an account has access to
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) a keyword to search on
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param date_created (optional) filter on items that have been created before this date
    #' @param app_version (optional) application version of the theme to filter by
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PurchaseItemListResponse) with additional information such as HTTP status code, headers
    GetThemeDescriptorsWithHttpInfo = function(filter, sort_field, descending, start, limit, device_id = NULL, account_id = NULL, game_type = NULL, contest_type = NULL, owner_id = NULL, q = NULL, keyword = NULL, _i = NULL, _l = NULL, date_created = NULL, app_version = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`filter`)) {
        stop("Missing required parameter `filter`.")
      }

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`filter`) && is.null(`filter`)) {
        stop("Invalid value for `filter` when calling ThemeDescriptorApi$GetThemeDescriptors, `filter` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling ThemeDescriptorApi$GetThemeDescriptors, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling ThemeDescriptorApi$GetThemeDescriptors, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ThemeDescriptorApi$GetThemeDescriptors, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ThemeDescriptorApi$GetThemeDescriptors, `limit` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ThemeDescriptorApi$GetThemeDescriptors, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThemeDescriptorApi$GetThemeDescriptors, `account_id` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling ThemeDescriptorApi$GetThemeDescriptors, `game_type` is not nullable")
      }

      if (!missing(`contest_type`) && is.null(`contest_type`)) {
        stop("Invalid value for `contest_type` when calling ThemeDescriptorApi$GetThemeDescriptors, `contest_type` is not nullable")
      }

      if (!missing(`owner_id`) && is.null(`owner_id`)) {
        stop("Invalid value for `owner_id` when calling ThemeDescriptorApi$GetThemeDescriptors, `owner_id` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling ThemeDescriptorApi$GetThemeDescriptors, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ThemeDescriptorApi$GetThemeDescriptors, `keyword` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling ThemeDescriptorApi$GetThemeDescriptors, `_i` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling ThemeDescriptorApi$GetThemeDescriptors, `_l` is not nullable")
      }

      if (!missing(`date_created`) && is.null(`date_created`)) {
        stop("Invalid value for `date_created` when calling ThemeDescriptorApi$GetThemeDescriptors, `date_created` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling ThemeDescriptorApi$GetThemeDescriptors, `app_version` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ThemeDescriptorApi$GetThemeDescriptors, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ThemeDescriptorApi$GetThemeDescriptors, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["contestType"]] <- `contest_type`

      query_params[["ownerId"]] <- `owner_id`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["filter"]] <- `filter`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["dateCreated"]] <- `date_created`

      query_params[["appVersion"]] <- `app_version`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/consumer/theme/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PurchaseItemListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Theme
    #'
    #' @param theme_descriptor_id the theme id to remove
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param game_type (optional) the unique title of an application given from the admin tool
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    RemoveThemeDescriptor = function(theme_descriptor_id, device_id = NULL, account_id = NULL, game_type = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RemoveThemeDescriptorWithHttpInfo(theme_descriptor_id, device_id, account_id, game_type, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Theme
    #'
    #' @param theme_descriptor_id the theme id to remove
    #' @param device_id (optional) a unique id given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param game_type (optional) the unique title of an application given from the admin tool
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    RemoveThemeDescriptorWithHttpInfo = function(theme_descriptor_id, device_id = NULL, account_id = NULL, game_type = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`theme_descriptor_id`)) {
        stop("Missing required parameter `theme_descriptor_id`.")
      }

      if (!missing(`theme_descriptor_id`) && is.null(`theme_descriptor_id`)) {
        stop("Invalid value for `theme_descriptor_id` when calling ThemeDescriptorApi$RemoveThemeDescriptor, `theme_descriptor_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ThemeDescriptorApi$RemoveThemeDescriptor, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThemeDescriptorApi$RemoveThemeDescriptor, `account_id` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling ThemeDescriptorApi$RemoveThemeDescriptor, `game_type` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ThemeDescriptorApi$RemoveThemeDescriptor, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ThemeDescriptorApi$RemoveThemeDescriptor, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["themeDescriptorId"]] <- `theme_descriptor_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/consumer/theme/remove"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
