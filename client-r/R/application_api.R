#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Application operations
#' @description ApplicationApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateApplication  ####################
#'
#' library(openapi)
#' var_app_name <- "app_name_example" # character | The name of the application
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_about <- "about_example" # character | The description of the application (Optional)
#' var_bundle_id <- "bundle_id_example" # character | The application bundle identifier (format - com.company.appName) (Optional)
#' var_app_icon_asset_id <- 56 # integer | The application icon asset id (Optional)
#' var_app_logo_asset_id <- 56 # integer | The application logo asset id (Optional)
#' var_facebook_app_id <- "facebook_app_id_example" # character | The Facebook application id (Optional)
#' var_facebook_app_secret <- "facebook_app_secret_example" # character | The Facebook application secret (Optional)
#' var_google_api_key <- "google_api_key_example" # character | This is the either the 'server key' or 'browser key' generated from google to enable Google Cloud Messaging (Optional)
#' var_update_eula_date <- "update_eula_date_example" # character | Determines whether to update the EULA date (Optional)
#' var_eula_version <- "eula_version_example" # character | The EULA version (Optional)
#' var_landing_page_url <- "landing_page_url_example" # character | The landing page URL (Optional)
#' var_show_in_activities <- "show_in_activities_example" # character | Determines whether to show the application in the activity feed (Optional)
#' var_activity_description <- "activity_description_example" # character | The description of the application in the activity feed (Optional)
#' var_invite_welcome_text <- "invite_welcome_text_example" # character | The text to display on the invite page (Optional)
#' var_invite_page_url <- "invite_page_url_example" # character | The url to the application invite page (Optional)
#' var_url_scheme <- "url_scheme_example" # character | The protocal the app uses to load the app via a browser (Optional)
#' var_platforms <- "platforms_example" # character | A json object with a list of supported platforms.  ```json {   \"platforms\": [     {       \"deviceId\": 1,       \"minimum\": 230,       \"maximum\": 421,       \"downloadUrl\": \"http://app.store.com/download\",       \"description\": \"description and version info\"     }   ] } ```  (Optional)
#' var_download_urls <- "download_urls_example" # character | Json formatted downloadUrls.  ```json {   \"ios\": \"the-url-to-app-store\",    \"android\": \"the-url-to-google-play\" } ```   (Optional)
#' var_category_ids <- "category_ids_example" # character | List of categories to apply (Optional)
#' var_scoring_type <- "GAME_LEVEL" # character | The type of scoring this application will use {GAME_LEVEL, GAME_OBJECT (Optional)
#' var_hint_cost <- 11 # integer | The cost of hints (Optional)
#' var_max_score <- 125 # integer | The maximum score that will be possible (Optional)
#' var_tickets_per_point <- 0.037 # numeric | The point-to-ticket conversion ratio (Optional)
#' var_has_game_data <- TRUE # character | Determines whether the application uses services to save custom game objects (Optional)
#' var_public_notifications <- "public_notifications_example" # character | Public Notifications (Optional)
#' var_use_matching_algorithm <- "use_matching_algorithm_example" # character | Use Matching Algorithm (Optional)
#' var_global_tickets <- FALSE # character | Determines whether earned tickets are applied across all applications (Optional)
#' var_build_version <- 1 # numeric | The current build version of the application (Optional)
#' var_api_version <- 3.4 # numeric | The current API version the application uses (Optional)
#' var_placement_name <- "placement_name_example" # character | The name of the placement (Optional)
#' var_placement_description <- "placement_description_example" # character | The description of the placement (Optional)
#' var_placement_size <- "placement_size_example" # character | The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM (this is required if a placements is to be created) (Optional)
#' var_placement_height <- 56 # integer | The height of a custom ad size (Optional)
#' var_placement_width <- 56 # integer | The width of a custom ad size (Optional)
#' var_placement_refresh_interval <- 56 # integer | The refresh interval in seconds (Optional)
#' var_create_object_store <- TRUE # character | Generate a custom object store to use (Optional)
#' var_public_content_approval <- FALSE # character | Determine whether or not public content requires admin approval before it becomes public (Optional)
#' var_production_mode <- FALSE # character | Determines whether the application uses production or sandbox services (Optional)
#' var_minimum_session_length <- 56 # integer | Minimum Session Length (Optional)
#' var_session_gap_length <- 56 # integer | Session Gap Length (Optional)
#' var_local_ads_enabled <- FALSE # character | Local Ads Enabled (Optional)
#' var_sqoot_api_key <- "sqoot_api_key_example" # character | Sqoot Api Key (Optional)
#' var_trilat_processing_type <- "FINGERPRINT" # character | Determines how to process trilateration data. Possible values include: DEFAULT, FINGERPRINT (Optional)
#' var_max_sample_size <- 56 # integer | Determines what the maximum sample size during trilateration (Optional)
#' var_min_rssi <- 3.4 # numeric | Determines what the minimum acceptable RSSI value (Optional)
#' var_modules <- "ALL" # character | List modules allowed to be served by the server, possible values include: COMMON, MEDIA, OFFER, GAME, SOCIAL, CONSUMER_WEBSITE, ALL (Optional)
#' var_authorized_count <- 1 # integer | How many servers the license will support (Optional)
#' var_authorized_servers <- "authorized_servers_example" # character | The list of ip addresses of servers the license will support, leave null for any server (Optional)
#' var_default_timezone <- "default_timezone_example" # character | Sets the default timezone for the app (used for leaderboards and other time specific content) (Optional)
#' var_smtp_pass <- "smtp_pass_example" # character | SMTP Pass (Optional)
#' var_meta_data <- "meta_data_example" # character | The application meta data. Defined by the client (Optional)
#' var_placement_meta_data <- "placement_meta_data_example" # character | The ad placement meta data. Defined by the client (Optional)
#' var_ips_floor <- FALSE # character | Create floor tables for Ips (Optional)
#' var_enable_apns_badge <- TRUE # character | Enables setting the APNS badge value in the payload (Optional)
#' var_include_in_report <- TRUE # character | Enables using the application in session reports (Optional)
#' var_default_app_filter_id <- 56 # integer | Sets the default filter to use (if none is passed in) for certain search APIs including Album Search. (Optional)
#' var_enable_welcome_email <- "enable_welcome_email_example" # character | Enables whether the default welcome email will be sent for new app users (Optional)
#' var_apple_app_id <- "apple_app_id_example" # character | The Apple Application ID (Optional)
#' var_apple_team_id <- "apple_team_id_example" # character | The Apple Team ID (Optional)
#' var_apple_auth_key_id <- "apple_auth_key_id_example" # character | The Apple Auth Key ID (Optional)
#' var_apple_auth_key <- File.new('/path/to/file') # data.frame | The Apple Auth Signin Key (p8) File (Optional)
#' var_apple_issuer_id <- "apple_issuer_id_example" # character | The Apple Issuer ID (Optional)
#' var_app_store_key_id <- "app_store_key_id_example" # character | The Apple App Store Key ID (Optional)
#' var_app_store_key <- File.new('/path/to/file') # data.frame | The Apple App Store Key (p8) File (Optional)
#' var_google_private_key_file <- File.new('/path/to/file') # data.frame | This is the private key file for your Google service account. (Optional)
#' var_authorize_net_api_key <- "authorize_net_api_key_example" # character | Authorize Net Api Key (Optional)
#' var_authorize_net_transaction_key <- "authorize_net_transaction_key_example" # character | Authorize Net Transaction Key (Optional)
#' var_email_sender <- "email_sender_example" # character | Email Sender (Optional)
#' var_smtp_user <- "smtp_user_example" # character | SMTP User (Optional)
#' var_smtp_host <- "smtp_host_example" # character | SMTP Host (Optional)
#' var_vatom_business_id <- "vatom_business_id_example" # character | Vatom Business Id (Optional)
#' var_vatom_rest_client_id <- "vatom_rest_client_id_example" # character | Vatom REST Client Id (Optional)
#' var_vatom_rest_secret_key <- "vatom_rest_secret_key_example" # character | Vatom Secret Key (Optional)
#' var_twilio_account_sid <- "twilio_account_sid_example" # character | Twilio Account SID (Optional)
#' var_twilio_auth_token <- "twilio_auth_token_example" # character | Twilio Auth Token (Optional)
#' var_twilio_sender_phone_number <- "twilio_sender_phone_number_example" # character | Twilio Sender Phone Number (Optional)
#' var_open_ai_secret_key <- "open_ai_secret_key_example" # character | OpenAI Secret API Key (Optional)
#'
#' #Create Application
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateApplication(var_app_name, device_id = var_device_id, account_id = var_account_id, about = var_about, bundle_id = var_bundle_id, app_icon_asset_id = var_app_icon_asset_id, app_logo_asset_id = var_app_logo_asset_id, facebook_app_id = var_facebook_app_id, facebook_app_secret = var_facebook_app_secret, google_api_key = var_google_api_key, update_eula_date = var_update_eula_date, eula_version = var_eula_version, landing_page_url = var_landing_page_url, show_in_activities = var_show_in_activities, activity_description = var_activity_description, invite_welcome_text = var_invite_welcome_text, invite_page_url = var_invite_page_url, url_scheme = var_url_scheme, platforms = var_platforms, download_urls = var_download_urls, category_ids = var_category_ids, scoring_type = var_scoring_type, hint_cost = var_hint_cost, max_score = var_max_score, tickets_per_point = var_tickets_per_point, has_game_data = var_has_game_data, public_notifications = var_public_notifications, use_matching_algorithm = var_use_matching_algorithm, global_tickets = var_global_tickets, build_version = var_build_version, api_version = var_api_version, placement_name = var_placement_name, placement_description = var_placement_description, placement_size = var_placement_size, placement_height = var_placement_height, placement_width = var_placement_width, placement_refresh_interval = var_placement_refresh_interval, create_object_store = var_create_object_store, public_content_approval = var_public_content_approval, production_mode = var_production_mode, minimum_session_length = var_minimum_session_length, session_gap_length = var_session_gap_length, local_ads_enabled = var_local_ads_enabled, sqoot_api_key = var_sqoot_api_key, trilat_processing_type = var_trilat_processing_type, max_sample_size = var_max_sample_size, min_rssi = var_min_rssi, modules = var_modules, authorized_count = var_authorized_count, authorized_servers = var_authorized_servers, default_timezone = var_default_timezone, smtp_pass = var_smtp_pass, meta_data = var_meta_data, placement_meta_data = var_placement_meta_data, ips_floor = var_ips_floor, enable_apns_badge = var_enable_apns_badge, include_in_report = var_include_in_report, default_app_filter_id = var_default_app_filter_id, enable_welcome_email = var_enable_welcome_email, apple_app_id = var_apple_app_id, apple_team_id = var_apple_team_id, apple_auth_key_id = var_apple_auth_key_id, apple_auth_key = var_apple_auth_key, apple_issuer_id = var_apple_issuer_id, app_store_key_id = var_app_store_key_id, app_store_key = var_app_store_key, google_private_key_file = var_google_private_key_file, authorize_net_api_key = var_authorize_net_api_key, authorize_net_transaction_key = var_authorize_net_transaction_key, email_sender = var_email_sender, smtp_user = var_smtp_user, smtp_host = var_smtp_host, vatom_business_id = var_vatom_business_id, vatom_rest_client_id = var_vatom_rest_client_id, vatom_rest_secret_key = var_vatom_rest_secret_key, twilio_account_sid = var_twilio_account_sid, twilio_auth_token = var_twilio_auth_token, twilio_sender_phone_number = var_twilio_sender_phone_number, open_ai_secret_key = var_open_ai_secret_keydata_file = "result.txt")
#' result <- api_instance$CreateApplication(var_app_name, device_id = var_device_id, account_id = var_account_id, about = var_about, bundle_id = var_bundle_id, app_icon_asset_id = var_app_icon_asset_id, app_logo_asset_id = var_app_logo_asset_id, facebook_app_id = var_facebook_app_id, facebook_app_secret = var_facebook_app_secret, google_api_key = var_google_api_key, update_eula_date = var_update_eula_date, eula_version = var_eula_version, landing_page_url = var_landing_page_url, show_in_activities = var_show_in_activities, activity_description = var_activity_description, invite_welcome_text = var_invite_welcome_text, invite_page_url = var_invite_page_url, url_scheme = var_url_scheme, platforms = var_platforms, download_urls = var_download_urls, category_ids = var_category_ids, scoring_type = var_scoring_type, hint_cost = var_hint_cost, max_score = var_max_score, tickets_per_point = var_tickets_per_point, has_game_data = var_has_game_data, public_notifications = var_public_notifications, use_matching_algorithm = var_use_matching_algorithm, global_tickets = var_global_tickets, build_version = var_build_version, api_version = var_api_version, placement_name = var_placement_name, placement_description = var_placement_description, placement_size = var_placement_size, placement_height = var_placement_height, placement_width = var_placement_width, placement_refresh_interval = var_placement_refresh_interval, create_object_store = var_create_object_store, public_content_approval = var_public_content_approval, production_mode = var_production_mode, minimum_session_length = var_minimum_session_length, session_gap_length = var_session_gap_length, local_ads_enabled = var_local_ads_enabled, sqoot_api_key = var_sqoot_api_key, trilat_processing_type = var_trilat_processing_type, max_sample_size = var_max_sample_size, min_rssi = var_min_rssi, modules = var_modules, authorized_count = var_authorized_count, authorized_servers = var_authorized_servers, default_timezone = var_default_timezone, smtp_pass = var_smtp_pass, meta_data = var_meta_data, placement_meta_data = var_placement_meta_data, ips_floor = var_ips_floor, enable_apns_badge = var_enable_apns_badge, include_in_report = var_include_in_report, default_app_filter_id = var_default_app_filter_id, enable_welcome_email = var_enable_welcome_email, apple_app_id = var_apple_app_id, apple_team_id = var_apple_team_id, apple_auth_key_id = var_apple_auth_key_id, apple_auth_key = var_apple_auth_key, apple_issuer_id = var_apple_issuer_id, app_store_key_id = var_app_store_key_id, app_store_key = var_app_store_key, google_private_key_file = var_google_private_key_file, authorize_net_api_key = var_authorize_net_api_key, authorize_net_transaction_key = var_authorize_net_transaction_key, email_sender = var_email_sender, smtp_user = var_smtp_user, smtp_host = var_smtp_host, vatom_business_id = var_vatom_business_id, vatom_rest_client_id = var_vatom_rest_client_id, vatom_rest_secret_key = var_vatom_rest_secret_key, twilio_account_sid = var_twilio_account_sid, twilio_auth_token = var_twilio_auth_token, twilio_sender_phone_number = var_twilio_sender_phone_number, open_ai_secret_key = var_open_ai_secret_key)
#' dput(result)
#'
#'
#' ####################  CreateApplicationPlacement  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The appKey of the application the ad placement is for
#' var_size <- "size_example" # character | The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_name <- "name_example" # character | The name of the placement (Optional)
#' var_description <- "description_example" # character | The description of the placement (Optional)
#' var_height <- 56 # integer | The height of a custom ad size (Optional)
#' var_width <- 56 # integer | The width of a custom ad size (Optional)
#' var_refresh_interval <- 56 # integer | The refresh interval in seconds (Optional)
#' var_default_image_id <- 56 # integer | Default Image Id (Optional)
#' var_active <- "active_example" # character | Active (Optional)
#'
#' #Create Ad Placement
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateApplicationPlacement(var_app_key, var_size, device_id = var_device_id, account_id = var_account_id, name = var_name, description = var_description, height = var_height, width = var_width, refresh_interval = var_refresh_interval, default_image_id = var_default_image_id, active = var_activedata_file = "result.txt")
#' result <- api_instance$CreateApplicationPlacement(var_app_key, var_size, device_id = var_device_id, account_id = var_account_id, name = var_name, description = var_description, height = var_height, width = var_width, refresh_interval = var_refresh_interval, default_image_id = var_default_image_id, active = var_active)
#' dput(result)
#'
#'
#' ####################  DeleteApplication  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account used to perform the delete, must have rights to edit the application. (Optional)
#' var_app_key <- "app_key_example" # character | The key of the application to be deleted (Optional)
#'
#' #Delete Application
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteApplication(account_id = var_account_id, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$DeleteApplication(account_id = var_account_id, app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  DeleteApplicationPlacement  ####################
#'
#' library(openapi)
#' var_placement_id <- 56 # integer | The id of the placement to delete, the user must have rights to the application the ad placement is for
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#'
#' #Delete Ad Placement
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteApplicationPlacement(var_placement_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteApplicationPlacement(var_placement_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  GetApplication  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The key of the application (Optional)
#' var_application_id <- 56 # integer | Application Id (Optional)
#'
#' #Get Application
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetApplication(app_key = var_app_key, application_id = var_application_iddata_file = "result.txt")
#' result <- api_instance$GetApplication(app_key = var_app_key, application_id = var_application_id)
#' dput(result)
#'
#'
#' ####################  GetApplicationPlacement  ####################
#'
#' library(openapi)
#' var_placement_id <- 56 # integer | The id of the placement
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#'
#' #Get Ad Placement
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetApplicationPlacement(var_placement_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$GetApplicationPlacement(var_placement_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  GetApplicationVersions  ####################
#'
#' library(openapi)
#'
#' #Get API versions
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetApplicationVersions(data_file = "result.txt")
#' result <- api_instance$GetApplicationVersions()
#' dput(result)
#'
#'
#' ####################  GetUniqueUsersByApp  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key
#' var_q <- "q_example" # character | Q (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_since <- 56 # integer | Return accounts that have been active after this date (UNIX time-stamp in milliseconds) (Optional)
#' var__i <- 56 # integer | the start of the index (Optional)
#' var_start <- 0 # integer | The start of the pagination (Optional)
#' var__l <- 56 # integer | the limit of the index (Optional)
#' var_limit <- 20 # integer | The limit of the pagination (Optional)
#'
#' #Search Application Users
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetUniqueUsersByApp(var_app_key, q = var_q, keyword = var_keyword, since = var_since, _i = var__i, start = var_start, _l = var__l, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GetUniqueUsersByApp(var_app_key, q = var_q, keyword = var_keyword, since = var_since, _i = var__i, start = var_start, _l = var__l, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  ListApplications  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the application owner/manager (Optional)
#' var_q <- "q_example" # character | Q (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search for title, about, and description fields (Optional)
#' var_platforms <- "platforms_example" # character | Deprecated, use deviceIds and deviceVersions (Optional)
#' var_device_ids <- "device_ids_example" # character | The list of targeted device ids, comma separated; possible values are retreived via /api/{version/audience/devices com.sirqul.geoapps.common.service.AudienceApi#getDevices getDevices (pass in deviceId values) (Optional)
#' var_device_versions <- "device_versions_example" # character | The list of targeted device version ranges that are aligned  with the provided devices list (see devices param for possible values),  comma separated; examples are: 2.3-X, 0-5.1.4, 4.3.1-6.1.4 where X  is no maximum and 0 is no minimum.  (Optional)
#' var_category_ids <- "category_ids_example" # character | The list of category ids to filter the list by (Optional)
#' var_sort_field <- "UPDATED" # character | The column to sort the search on, possible values include: UPDATED (default), CREATED, TITLE (Optional)
#' var_has_ads <- "has_ads_example" # character | Filter results on whether the application supports ads or not. Ignore this parameter to return all results. (Optional)
#' var_public_notifications <- "public_notifications_example" # character | Filter results on whether the application is available for public trigger notifications (Optional)
#' var_filter_billable <- "filter_billable_example" # character | Determines whether to only return applications that the user has access to (Optional)
#' var_filter_content_admin <- "filter_content_admin_example" # character | Determines whether to only return applications that the user is a content admin of (Optional)
#' var_descending <- TRUE # character | The order to return the search results (Optional)
#' var__i <- 56 # integer | the start of the index (Optional)
#' var_start <- 0 # integer | The start of the pagination (Optional)
#' var__l <- 56 # integer | The limit of the index (Optional)
#' var_limit <- 20 # integer | The limit of the pagination (Optional)
#' var_application_ids <- "application_ids_example" # character | The list of application ids, comma separated. If provided will ignore all other params. (Optional)
#' var_has_object_store <- FALSE # character | Only include applications with a object store (default is false) (Optional)
#' var_active_only <- TRUE # character | Return only active results (Optional)
#'
#' #List Applications
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListApplications(account_id = var_account_id, q = var_q, keyword = var_keyword, platforms = var_platforms, device_ids = var_device_ids, device_versions = var_device_versions, category_ids = var_category_ids, sort_field = var_sort_field, has_ads = var_has_ads, public_notifications = var_public_notifications, filter_billable = var_filter_billable, filter_content_admin = var_filter_content_admin, descending = var_descending, _i = var__i, start = var_start, _l = var__l, limit = var_limit, application_ids = var_application_ids, has_object_store = var_has_object_store, active_only = var_active_onlydata_file = "result.txt")
#' result <- api_instance$ListApplications(account_id = var_account_id, q = var_q, keyword = var_keyword, platforms = var_platforms, device_ids = var_device_ids, device_versions = var_device_versions, category_ids = var_category_ids, sort_field = var_sort_field, has_ads = var_has_ads, public_notifications = var_public_notifications, filter_billable = var_filter_billable, filter_content_admin = var_filter_content_admin, descending = var_descending, _i = var__i, start = var_start, _l = var__l, limit = var_limit, application_ids = var_application_ids, has_object_store = var_has_object_store, active_only = var_active_only)
#' dput(result)
#'
#'
#' ####################  SearchApplicationPlacement  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The key of the application
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_start <- 0 # integer | The start of the pagination (Optional)
#' var_limit <- 100 # integer | The limit of the pagination (Optional)
#'
#' #Search for Ad Placements
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchApplicationPlacement(var_app_key, device_id = var_device_id, account_id = var_account_id, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchApplicationPlacement(var_app_key, device_id = var_device_id, account_id = var_account_id, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  SearchApplicationSettings  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_connection_account_id <- 56 # integer | The account id used to view another person's account (Optional)
#' var_keyword <- "keyword_example" # character | The string to search applications on (Optional)
#' var_sort_field <- "APPLICATION_TITLE" # character | The column to sort the search on. Possible values include: CREATED, UPDATED, APPLICATION_TITLE (Optional)
#' var_descending <- FALSE # character | The order to return the search results (Optional)
#' var_start <- 0 # integer | The start index for pagination (Optional)
#' var_limit <- 20 # integer | The limit per result set for pagination (Optional)
#'
#' #Search for Application Settings
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchApplicationSettings(device_id = var_device_id, account_id = var_account_id, connection_account_id = var_connection_account_id, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchApplicationSettings(device_id = var_device_id, account_id = var_account_id, connection_account_id = var_connection_account_id, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  SearchApplications  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_latitude <- 3.4 # numeric | The location of the device (Optional)
#' var_longitude <- 3.4 # numeric | The location of the device (Optional)
#' var_q <- "q_example" # character | Q (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_q_search_fields <- "NAME,ABOUT,BILLABLE_ENTITY_NAME" # character | The columns to applied the keyword search to (Optional)
#' var_sort_field <- "APPLICATION_ID" # character | The column to sort the search on (Optional)
#' var_descending <- FALSE # character | The order to return the search results (Optional)
#' var__i <- 56 # integer | the start of the index (Optional)
#' var_start <- 0 # integer | The start of the pagination (Optional)
#' var__l <- 56 # integer | the limit of the index (Optional)
#' var_limit <- 100 # integer | The limit of the pagination (Optional)
#' var_has_ads <- "has_ads_example" # character | Filter results on whether the application supports ads or not. Ignore this parameter to return all results. (Optional)
#' var_public_notifications <- "public_notifications_example" # character | Filter results on whether the application is available for public trigger notifications (Optional)
#' var_active_only <- FALSE # character | Return only active results (Optional)
#'
#' #Search Applications
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchApplications(device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, q = var_q, keyword = var_keyword, q_search_fields = var_q_search_fields, sort_field = var_sort_field, descending = var_descending, _i = var__i, start = var_start, _l = var__l, limit = var_limit, has_ads = var_has_ads, public_notifications = var_public_notifications, active_only = var_active_onlydata_file = "result.txt")
#' result <- api_instance$SearchApplications(device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude, q = var_q, keyword = var_keyword, q_search_fields = var_q_search_fields, sort_field = var_sort_field, descending = var_descending, _i = var__i, start = var_start, _l = var__l, limit = var_limit, has_ads = var_has_ads, public_notifications = var_public_notifications, active_only = var_active_only)
#' dput(result)
#'
#'
#' ####################  UpdateApplication  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key for updating an existing application
#' var_app_name <- "app_name_example" # character | The name of the application
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_about <- "about_example" # character | The description of the application (Optional)
#' var_bundle_id <- "bundle_id_example" # character | The application bundle identifier (format - com.company.appName) (Optional)
#' var_app_icon_asset_id <- 56 # integer | The application icon asset id (Optional)
#' var_app_logo_asset_id <- 56 # integer | The application logo asset id (Optional)
#' var_facebook_app_id <- "facebook_app_id_example" # character | The Facebook application id (Optional)
#' var_facebook_app_secret <- "facebook_app_secret_example" # character | The Facebook application secret (Optional)
#' var_google_api_key <- "google_api_key_example" # character | This is the either the 'server key' or 'browser key' generated from google to enable Google Cloud Messaging (Optional)
#' var_update_eula_date <- "update_eula_date_example" # character | Determines whether to update the EULA date (Optional)
#' var_eula_version <- "eula_version_example" # character | The EULA version (Optional)
#' var_landing_page_url <- "landing_page_url_example" # character | The landing page URL (Optional)
#' var_show_in_activities <- "show_in_activities_example" # character | Determines whether to show the application in the activity feed (Optional)
#' var_activity_description <- "activity_description_example" # character | The description of the application in the activity feed (Optional)
#' var_invite_welcome_text <- "invite_welcome_text_example" # character | The text to display on the invite page (Optional)
#' var_invite_page_url <- "invite_page_url_example" # character | The url to the application invite page (Optional)
#' var_url_scheme <- "url_scheme_example" # character | The protocal the app uses to load the app via a browser (Optional)
#' var_platforms <- "platforms_example" # character | A json object with a list of supported platforms.  ```json {   \"platforms\": [     {       \"deviceId\": 1,       \"minimum\": 230,       \"maximum\": 421,       \"downloadUrl\": \"http://app.store.com/download\",       \"description\": \"description and version info\"     }   ] } ```  (Optional)
#' var_download_urls <- "download_urls_example" # character | Json formatted downloadUrls.  ```json {   \"ios\": \"the-url-to-app-store\",    \"android\": \"the-url-to-google-play\" } ```   (Optional)
#' var_category_ids <- "category_ids_example" # character | List of categories to apply (Optional)
#' var_scoring_type <- "GAME_LEVEL" # character | The type of scoring this application will use {GAME_LEVEL, GAME_OBJECT (Optional)
#' var_hint_cost <- 11 # integer | The cost of hints (Optional)
#' var_max_score <- 125 # integer | The maximum score that will be possible (Optional)
#' var_tickets_per_point <- 0.037 # numeric | The point-to-ticket conversion ratio (Optional)
#' var_has_game_data <- TRUE # character | Determines whether the application uses services to save custom game objects (Optional)
#' var_public_notifications <- "public_notifications_example" # character | Public Notifications (Optional)
#' var_use_matching_algorithm <- "use_matching_algorithm_example" # character | Use Matching Algorithm (Optional)
#' var_global_tickets <- FALSE # character | Determines whether earned tickets are applied across all applications (Optional)
#' var_build_version <- 1 # numeric | The current build version of the application (Optional)
#' var_api_version <- 3.4 # numeric | The current API version the application uses (Optional)
#' var_placement_name <- "placement_name_example" # character | The name of the placement (Optional)
#' var_placement_description <- "placement_description_example" # character | The description of the placement (Optional)
#' var_placement_size <- "placement_size_example" # character | The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM (this is required if a placements is to be created) (Optional)
#' var_placement_height <- 56 # integer | The height of a custom ad size (Optional)
#' var_placement_width <- 56 # integer | The width of a custom ad size (Optional)
#' var_placement_refresh_interval <- 56 # integer | The refresh interval in seconds (Optional)
#' var_create_object_store <- TRUE # character | Generate a custom object store to use (Optional)
#' var_public_content_approval <- FALSE # character | Determine whether or not public content requires admin approval before it becomes public (Optional)
#' var_production_mode <- FALSE # character | Determines whether the application uses production or sandbox services (Optional)
#' var_minimum_session_length <- 56 # integer | Minimum Session Length (Optional)
#' var_session_gap_length <- 56 # integer | Session Gap Length (Optional)
#' var_local_ads_enabled <- FALSE # character | Local Ads Enabled (Optional)
#' var_sqoot_api_key <- "sqoot_api_key_example" # character | Sqoot Api Key (Optional)
#' var_trilat_processing_type <- "FINGERPRINT" # character | Determines how to process trilateration data. Possible values include: DEFAULT, FINGERPRINT (Optional)
#' var_max_sample_size <- 56 # integer | Determines what the maximum sample size during trilateration (Optional)
#' var_min_rssi <- 3.4 # numeric | Determines what the minimum acceptable RSSI value (Optional)
#' var_modules <- "ALL" # character | List modules allowed to be served by the server, possible values include: COMMON, MEDIA, OFFER, GAME, SOCIAL, CONSUMER_WEBSITE, ALL (Optional)
#' var_authorized_count <- 1 # integer | How many servers the license will support (Optional)
#' var_authorized_servers <- "authorized_servers_example" # character | The list of ip addresses of servers the license will support, leave null for any server (Optional)
#' var_default_timezone <- "default_timezone_example" # character | Sets the default timezone for the app (used for leaderboards and other time specific content) (Optional)
#' var_smtp_pass <- "smtp_pass_example" # character | SMTP Pass (Optional)
#' var_meta_data <- "meta_data_example" # character | The application meta data. Defined by the client (Optional)
#' var_placement_meta_data <- "placement_meta_data_example" # character | The ad placement meta data. Defined by the client (Optional)
#' var_ips_floor <- FALSE # character | Create floor tables for Ips (Optional)
#' var_enable_apns_badge <- TRUE # character | Enables setting the APNS badge value in the payload (Optional)
#' var_include_in_report <- TRUE # character | Enables using the application in session reports (Optional)
#' var_default_app_filter_id <- 56 # integer | Sets the default filter to use (if none is passed in) for certain search APIs including Album Search. (Optional)
#' var_enable_welcome_email <- "enable_welcome_email_example" # character | Enables whether the default welcome email will be sent for new app users (Optional)
#' var_apple_app_id <- "apple_app_id_example" # character | The Apple Application ID (Optional)
#' var_apple_team_id <- "apple_team_id_example" # character | The Apple Team ID (Optional)
#' var_apple_auth_key_id <- "apple_auth_key_id_example" # character | The Apple Auth Key ID (Optional)
#' var_apple_auth_key <- File.new('/path/to/file') # data.frame | The Apple Auth Signin Key (p8) File (Optional)
#' var_apple_issuer_id <- "apple_issuer_id_example" # character | The Apple Issuer ID (Optional)
#' var_app_store_key_id <- "app_store_key_id_example" # character | The Apple App Store Key ID (Optional)
#' var_app_store_key <- File.new('/path/to/file') # data.frame | The Apple App Store Key (p8) File (Optional)
#' var_google_private_key_file <- File.new('/path/to/file') # data.frame | This is the private key file for your Google service account. (Optional)
#' var_authorize_net_api_key <- "authorize_net_api_key_example" # character | Authorize Net Api Key (Optional)
#' var_authorize_net_transaction_key <- "authorize_net_transaction_key_example" # character | Authorize Net Transaction Key (Optional)
#' var_email_sender <- "email_sender_example" # character | Email Sender (Optional)
#' var_smtp_user <- "smtp_user_example" # character | SMTP User (Optional)
#' var_smtp_host <- "smtp_host_example" # character | SMTP Host (Optional)
#' var_vatom_business_id <- "vatom_business_id_example" # character | Vatom Business Id (Optional)
#' var_vatom_rest_client_id <- "vatom_rest_client_id_example" # character | Vatom REST Client Id (Optional)
#' var_vatom_rest_secret_key <- "vatom_rest_secret_key_example" # character | Vatom Secret Key (Optional)
#' var_twilio_account_sid <- "twilio_account_sid_example" # character | Twilio Account SID (Optional)
#' var_twilio_auth_token <- "twilio_auth_token_example" # character | Twilio Auth Token (Optional)
#' var_twilio_sender_phone_number <- "twilio_sender_phone_number_example" # character | Twilio Sender Phone Number (Optional)
#' var_open_ai_secret_key <- "open_ai_secret_key_example" # character | OpenAI Secret API Key (Optional)
#'
#' #Update Application
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateApplication(var_app_key, var_app_name, device_id = var_device_id, account_id = var_account_id, about = var_about, bundle_id = var_bundle_id, app_icon_asset_id = var_app_icon_asset_id, app_logo_asset_id = var_app_logo_asset_id, facebook_app_id = var_facebook_app_id, facebook_app_secret = var_facebook_app_secret, google_api_key = var_google_api_key, update_eula_date = var_update_eula_date, eula_version = var_eula_version, landing_page_url = var_landing_page_url, show_in_activities = var_show_in_activities, activity_description = var_activity_description, invite_welcome_text = var_invite_welcome_text, invite_page_url = var_invite_page_url, url_scheme = var_url_scheme, platforms = var_platforms, download_urls = var_download_urls, category_ids = var_category_ids, scoring_type = var_scoring_type, hint_cost = var_hint_cost, max_score = var_max_score, tickets_per_point = var_tickets_per_point, has_game_data = var_has_game_data, public_notifications = var_public_notifications, use_matching_algorithm = var_use_matching_algorithm, global_tickets = var_global_tickets, build_version = var_build_version, api_version = var_api_version, placement_name = var_placement_name, placement_description = var_placement_description, placement_size = var_placement_size, placement_height = var_placement_height, placement_width = var_placement_width, placement_refresh_interval = var_placement_refresh_interval, create_object_store = var_create_object_store, public_content_approval = var_public_content_approval, production_mode = var_production_mode, minimum_session_length = var_minimum_session_length, session_gap_length = var_session_gap_length, local_ads_enabled = var_local_ads_enabled, sqoot_api_key = var_sqoot_api_key, trilat_processing_type = var_trilat_processing_type, max_sample_size = var_max_sample_size, min_rssi = var_min_rssi, modules = var_modules, authorized_count = var_authorized_count, authorized_servers = var_authorized_servers, default_timezone = var_default_timezone, smtp_pass = var_smtp_pass, meta_data = var_meta_data, placement_meta_data = var_placement_meta_data, ips_floor = var_ips_floor, enable_apns_badge = var_enable_apns_badge, include_in_report = var_include_in_report, default_app_filter_id = var_default_app_filter_id, enable_welcome_email = var_enable_welcome_email, apple_app_id = var_apple_app_id, apple_team_id = var_apple_team_id, apple_auth_key_id = var_apple_auth_key_id, apple_auth_key = var_apple_auth_key, apple_issuer_id = var_apple_issuer_id, app_store_key_id = var_app_store_key_id, app_store_key = var_app_store_key, google_private_key_file = var_google_private_key_file, authorize_net_api_key = var_authorize_net_api_key, authorize_net_transaction_key = var_authorize_net_transaction_key, email_sender = var_email_sender, smtp_user = var_smtp_user, smtp_host = var_smtp_host, vatom_business_id = var_vatom_business_id, vatom_rest_client_id = var_vatom_rest_client_id, vatom_rest_secret_key = var_vatom_rest_secret_key, twilio_account_sid = var_twilio_account_sid, twilio_auth_token = var_twilio_auth_token, twilio_sender_phone_number = var_twilio_sender_phone_number, open_ai_secret_key = var_open_ai_secret_keydata_file = "result.txt")
#' result <- api_instance$UpdateApplication(var_app_key, var_app_name, device_id = var_device_id, account_id = var_account_id, about = var_about, bundle_id = var_bundle_id, app_icon_asset_id = var_app_icon_asset_id, app_logo_asset_id = var_app_logo_asset_id, facebook_app_id = var_facebook_app_id, facebook_app_secret = var_facebook_app_secret, google_api_key = var_google_api_key, update_eula_date = var_update_eula_date, eula_version = var_eula_version, landing_page_url = var_landing_page_url, show_in_activities = var_show_in_activities, activity_description = var_activity_description, invite_welcome_text = var_invite_welcome_text, invite_page_url = var_invite_page_url, url_scheme = var_url_scheme, platforms = var_platforms, download_urls = var_download_urls, category_ids = var_category_ids, scoring_type = var_scoring_type, hint_cost = var_hint_cost, max_score = var_max_score, tickets_per_point = var_tickets_per_point, has_game_data = var_has_game_data, public_notifications = var_public_notifications, use_matching_algorithm = var_use_matching_algorithm, global_tickets = var_global_tickets, build_version = var_build_version, api_version = var_api_version, placement_name = var_placement_name, placement_description = var_placement_description, placement_size = var_placement_size, placement_height = var_placement_height, placement_width = var_placement_width, placement_refresh_interval = var_placement_refresh_interval, create_object_store = var_create_object_store, public_content_approval = var_public_content_approval, production_mode = var_production_mode, minimum_session_length = var_minimum_session_length, session_gap_length = var_session_gap_length, local_ads_enabled = var_local_ads_enabled, sqoot_api_key = var_sqoot_api_key, trilat_processing_type = var_trilat_processing_type, max_sample_size = var_max_sample_size, min_rssi = var_min_rssi, modules = var_modules, authorized_count = var_authorized_count, authorized_servers = var_authorized_servers, default_timezone = var_default_timezone, smtp_pass = var_smtp_pass, meta_data = var_meta_data, placement_meta_data = var_placement_meta_data, ips_floor = var_ips_floor, enable_apns_badge = var_enable_apns_badge, include_in_report = var_include_in_report, default_app_filter_id = var_default_app_filter_id, enable_welcome_email = var_enable_welcome_email, apple_app_id = var_apple_app_id, apple_team_id = var_apple_team_id, apple_auth_key_id = var_apple_auth_key_id, apple_auth_key = var_apple_auth_key, apple_issuer_id = var_apple_issuer_id, app_store_key_id = var_app_store_key_id, app_store_key = var_app_store_key, google_private_key_file = var_google_private_key_file, authorize_net_api_key = var_authorize_net_api_key, authorize_net_transaction_key = var_authorize_net_transaction_key, email_sender = var_email_sender, smtp_user = var_smtp_user, smtp_host = var_smtp_host, vatom_business_id = var_vatom_business_id, vatom_rest_client_id = var_vatom_rest_client_id, vatom_rest_secret_key = var_vatom_rest_secret_key, twilio_account_sid = var_twilio_account_sid, twilio_auth_token = var_twilio_auth_token, twilio_sender_phone_number = var_twilio_sender_phone_number, open_ai_secret_key = var_open_ai_secret_key)
#' dput(result)
#'
#'
#' ####################  UpdateApplicationActive  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account used to perform the delete, must have rights to edit the application.
#' var_app_key <- "app_key_example" # character | The key of the application to be deleted
#' var_active <- "active_example" # character | If true then set to active, false otherwise
#'
#' #Change Appliation Status
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateApplicationActive(var_account_id, var_app_key, var_activedata_file = "result.txt")
#' result <- api_instance$UpdateApplicationActive(var_account_id, var_app_key, var_active)
#' dput(result)
#'
#'
#' ####################  UpdateApplicationPlacement  ####################
#'
#' library(openapi)
#' var_placement_id <- 56 # integer | The id of the placement to update, the user must have rights to the application the ad placement is for
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_name <- "name_example" # character | The name of the placement (Optional)
#' var_description <- "description_example" # character | The description of the placement (Optional)
#' var_size <- "size_example" # character | The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM (Optional)
#' var_height <- 56 # integer | The height of a custom ad size (Optional)
#' var_width <- 56 # integer | The width of a custom ad size (Optional)
#' var_refresh_interval <- 56 # integer | The refresh interval in seconds (Optional)
#' var_default_image_id <- 56 # integer | Default Image Id (Optional)
#' var_active <- "active_example" # character | Active (Optional)
#'
#' #Update Ad Placement
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateApplicationPlacement(var_placement_id, device_id = var_device_id, account_id = var_account_id, name = var_name, description = var_description, size = var_size, height = var_height, width = var_width, refresh_interval = var_refresh_interval, default_image_id = var_default_image_id, active = var_activedata_file = "result.txt")
#' result <- api_instance$UpdateApplicationPlacement(var_placement_id, device_id = var_device_id, account_id = var_account_id, name = var_name, description = var_description, size = var_size, height = var_height, width = var_width, refresh_interval = var_refresh_interval, default_image_id = var_default_image_id, active = var_active)
#' dput(result)
#'
#'
#' ####################  UploadApplicationCertificate  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The key of the application
#' var_device_id <- "device_id_example" # character | Device Id (Optional)
#' var_account_id <- 56 # integer | The account used to perform the delete, must have rights to edit the application. (Optional)
#' var_certificate <- File.new('/path/to/file') # data.frame | Certificate (Optional)
#'
#' #Create Application Certificate
#' api_instance <- ApplicationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UploadApplicationCertificate(var_app_key, device_id = var_device_id, account_id = var_account_id, certificate = var_certificatedata_file = "result.txt")
#' result <- api_instance$UploadApplicationCertificate(var_app_key, device_id = var_device_id, account_id = var_account_id, certificate = var_certificate)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ApplicationApi <- R6::R6Class(
  "ApplicationApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ApplicationApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Application
    #'
    #' @param app_name The name of the application
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param about (optional) The description of the application
    #' @param bundle_id (optional) The application bundle identifier (format - com.company.appName)
    #' @param app_icon_asset_id (optional) The application icon asset id
    #' @param app_logo_asset_id (optional) The application logo asset id
    #' @param facebook_app_id (optional) The Facebook application id
    #' @param facebook_app_secret (optional) The Facebook application secret
    #' @param google_api_key (optional) This is the either the 'server key' or 'browser key' generated from google to enable Google Cloud Messaging
    #' @param update_eula_date (optional) Determines whether to update the EULA date
    #' @param eula_version (optional) The EULA version
    #' @param landing_page_url (optional) The landing page URL
    #' @param show_in_activities (optional) Determines whether to show the application in the activity feed
    #' @param activity_description (optional) The description of the application in the activity feed
    #' @param invite_welcome_text (optional) The text to display on the invite page
    #' @param invite_page_url (optional) The url to the application invite page
    #' @param url_scheme (optional) The protocal the app uses to load the app via a browser
    #' @param platforms (optional) A json object with a list of supported platforms.  ```json {   \"platforms\": [     {       \"deviceId\": 1,       \"minimum\": 230,       \"maximum\": 421,       \"downloadUrl\": \"http://app.store.com/download\",       \"description\": \"description and version info\"     }   ] } ``` 
    #' @param download_urls (optional) Json formatted downloadUrls.  ```json {   \"ios\": \"the-url-to-app-store\",    \"android\": \"the-url-to-google-play\" } ```  
    #' @param category_ids (optional) List of categories to apply
    #' @param scoring_type (optional) The type of scoring this application will use {GAME_LEVEL, GAME_OBJECT (default value: "GAME_LEVEL")
    #' @param hint_cost (optional) The cost of hints (default value: 11)
    #' @param max_score (optional) The maximum score that will be possible (default value: 125)
    #' @param tickets_per_point (optional) The point-to-ticket conversion ratio (default value: 0.037)
    #' @param has_game_data (optional) Determines whether the application uses services to save custom game objects (default value: TRUE)
    #' @param public_notifications (optional) Public Notifications
    #' @param use_matching_algorithm (optional) Use Matching Algorithm
    #' @param global_tickets (optional) Determines whether earned tickets are applied across all applications (default value: FALSE)
    #' @param build_version (optional) The current build version of the application (default value: 1)
    #' @param api_version (optional) The current API version the application uses
    #' @param placement_name (optional) The name of the placement
    #' @param placement_description (optional) The description of the placement
    #' @param placement_size (optional) The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM (this is required if a placements is to be created)
    #' @param placement_height (optional) The height of a custom ad size
    #' @param placement_width (optional) The width of a custom ad size
    #' @param placement_refresh_interval (optional) The refresh interval in seconds
    #' @param create_object_store (optional) Generate a custom object store to use (default value: TRUE)
    #' @param public_content_approval (optional) Determine whether or not public content requires admin approval before it becomes public (default value: FALSE)
    #' @param production_mode (optional) Determines whether the application uses production or sandbox services (default value: FALSE)
    #' @param minimum_session_length (optional) Minimum Session Length
    #' @param session_gap_length (optional) Session Gap Length
    #' @param local_ads_enabled (optional) Local Ads Enabled (default value: FALSE)
    #' @param sqoot_api_key (optional) Sqoot Api Key
    #' @param trilat_processing_type (optional) Determines how to process trilateration data. Possible values include: DEFAULT, FINGERPRINT (default value: "FINGERPRINT")
    #' @param max_sample_size (optional) Determines what the maximum sample size during trilateration
    #' @param min_rssi (optional) Determines what the minimum acceptable RSSI value
    #' @param modules (optional) List modules allowed to be served by the server, possible values include: COMMON, MEDIA, OFFER, GAME, SOCIAL, CONSUMER_WEBSITE, ALL (default value: "ALL")
    #' @param authorized_count (optional) How many servers the license will support (default value: 1)
    #' @param authorized_servers (optional) The list of ip addresses of servers the license will support, leave null for any server
    #' @param default_timezone (optional) Sets the default timezone for the app (used for leaderboards and other time specific content)
    #' @param smtp_pass (optional) SMTP Pass
    #' @param meta_data (optional) The application meta data. Defined by the client
    #' @param placement_meta_data (optional) The ad placement meta data. Defined by the client
    #' @param ips_floor (optional) Create floor tables for Ips (default value: FALSE)
    #' @param enable_apns_badge (optional) Enables setting the APNS badge value in the payload (default value: TRUE)
    #' @param include_in_report (optional) Enables using the application in session reports (default value: TRUE)
    #' @param default_app_filter_id (optional) Sets the default filter to use (if none is passed in) for certain search APIs including Album Search.
    #' @param enable_welcome_email (optional) Enables whether the default welcome email will be sent for new app users
    #' @param apple_app_id (optional) The Apple Application ID
    #' @param apple_team_id (optional) The Apple Team ID
    #' @param apple_auth_key_id (optional) The Apple Auth Key ID
    #' @param apple_auth_key (optional) The Apple Auth Signin Key (p8) File
    #' @param apple_issuer_id (optional) The Apple Issuer ID
    #' @param app_store_key_id (optional) The Apple App Store Key ID
    #' @param app_store_key (optional) The Apple App Store Key (p8) File
    #' @param google_private_key_file (optional) This is the private key file for your Google service account.
    #' @param authorize_net_api_key (optional) Authorize Net Api Key
    #' @param authorize_net_transaction_key (optional) Authorize Net Transaction Key
    #' @param email_sender (optional) Email Sender
    #' @param smtp_user (optional) SMTP User
    #' @param smtp_host (optional) SMTP Host
    #' @param vatom_business_id (optional) Vatom Business Id
    #' @param vatom_rest_client_id (optional) Vatom REST Client Id
    #' @param vatom_rest_secret_key (optional) Vatom Secret Key
    #' @param twilio_account_sid (optional) Twilio Account SID
    #' @param twilio_auth_token (optional) Twilio Auth Token
    #' @param twilio_sender_phone_number (optional) Twilio Sender Phone Number
    #' @param open_ai_secret_key (optional) OpenAI Secret API Key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ApplicationResponse
    CreateApplication = function(app_name, device_id = NULL, account_id = NULL, about = NULL, bundle_id = NULL, app_icon_asset_id = NULL, app_logo_asset_id = NULL, facebook_app_id = NULL, facebook_app_secret = NULL, google_api_key = NULL, update_eula_date = NULL, eula_version = NULL, landing_page_url = NULL, show_in_activities = NULL, activity_description = NULL, invite_welcome_text = NULL, invite_page_url = NULL, url_scheme = NULL, platforms = NULL, download_urls = NULL, category_ids = NULL, scoring_type = "GAME_LEVEL", hint_cost = 11, max_score = 125, tickets_per_point = 0.037, has_game_data = TRUE, public_notifications = NULL, use_matching_algorithm = NULL, global_tickets = FALSE, build_version = 1, api_version = NULL, placement_name = NULL, placement_description = NULL, placement_size = NULL, placement_height = NULL, placement_width = NULL, placement_refresh_interval = NULL, create_object_store = TRUE, public_content_approval = FALSE, production_mode = FALSE, minimum_session_length = NULL, session_gap_length = NULL, local_ads_enabled = FALSE, sqoot_api_key = NULL, trilat_processing_type = "FINGERPRINT", max_sample_size = NULL, min_rssi = NULL, modules = "ALL", authorized_count = 1, authorized_servers = NULL, default_timezone = NULL, smtp_pass = NULL, meta_data = NULL, placement_meta_data = NULL, ips_floor = FALSE, enable_apns_badge = TRUE, include_in_report = TRUE, default_app_filter_id = NULL, enable_welcome_email = NULL, apple_app_id = NULL, apple_team_id = NULL, apple_auth_key_id = NULL, apple_auth_key = NULL, apple_issuer_id = NULL, app_store_key_id = NULL, app_store_key = NULL, google_private_key_file = NULL, authorize_net_api_key = NULL, authorize_net_transaction_key = NULL, email_sender = NULL, smtp_user = NULL, smtp_host = NULL, vatom_business_id = NULL, vatom_rest_client_id = NULL, vatom_rest_secret_key = NULL, twilio_account_sid = NULL, twilio_auth_token = NULL, twilio_sender_phone_number = NULL, open_ai_secret_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateApplicationWithHttpInfo(app_name, device_id, account_id, about, bundle_id, app_icon_asset_id, app_logo_asset_id, facebook_app_id, facebook_app_secret, google_api_key, update_eula_date, eula_version, landing_page_url, show_in_activities, activity_description, invite_welcome_text, invite_page_url, url_scheme, platforms, download_urls, category_ids, scoring_type, hint_cost, max_score, tickets_per_point, has_game_data, public_notifications, use_matching_algorithm, global_tickets, build_version, api_version, placement_name, placement_description, placement_size, placement_height, placement_width, placement_refresh_interval, create_object_store, public_content_approval, production_mode, minimum_session_length, session_gap_length, local_ads_enabled, sqoot_api_key, trilat_processing_type, max_sample_size, min_rssi, modules, authorized_count, authorized_servers, default_timezone, smtp_pass, meta_data, placement_meta_data, ips_floor, enable_apns_badge, include_in_report, default_app_filter_id, enable_welcome_email, apple_app_id, apple_team_id, apple_auth_key_id, apple_auth_key, apple_issuer_id, app_store_key_id, app_store_key, google_private_key_file, authorize_net_api_key, authorize_net_transaction_key, email_sender, smtp_user, smtp_host, vatom_business_id, vatom_rest_client_id, vatom_rest_secret_key, twilio_account_sid, twilio_auth_token, twilio_sender_phone_number, open_ai_secret_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Application
    #'
    #' @param app_name The name of the application
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param about (optional) The description of the application
    #' @param bundle_id (optional) The application bundle identifier (format - com.company.appName)
    #' @param app_icon_asset_id (optional) The application icon asset id
    #' @param app_logo_asset_id (optional) The application logo asset id
    #' @param facebook_app_id (optional) The Facebook application id
    #' @param facebook_app_secret (optional) The Facebook application secret
    #' @param google_api_key (optional) This is the either the 'server key' or 'browser key' generated from google to enable Google Cloud Messaging
    #' @param update_eula_date (optional) Determines whether to update the EULA date
    #' @param eula_version (optional) The EULA version
    #' @param landing_page_url (optional) The landing page URL
    #' @param show_in_activities (optional) Determines whether to show the application in the activity feed
    #' @param activity_description (optional) The description of the application in the activity feed
    #' @param invite_welcome_text (optional) The text to display on the invite page
    #' @param invite_page_url (optional) The url to the application invite page
    #' @param url_scheme (optional) The protocal the app uses to load the app via a browser
    #' @param platforms (optional) A json object with a list of supported platforms.  ```json {   \"platforms\": [     {       \"deviceId\": 1,       \"minimum\": 230,       \"maximum\": 421,       \"downloadUrl\": \"http://app.store.com/download\",       \"description\": \"description and version info\"     }   ] } ``` 
    #' @param download_urls (optional) Json formatted downloadUrls.  ```json {   \"ios\": \"the-url-to-app-store\",    \"android\": \"the-url-to-google-play\" } ```  
    #' @param category_ids (optional) List of categories to apply
    #' @param scoring_type (optional) The type of scoring this application will use {GAME_LEVEL, GAME_OBJECT (default value: "GAME_LEVEL")
    #' @param hint_cost (optional) The cost of hints (default value: 11)
    #' @param max_score (optional) The maximum score that will be possible (default value: 125)
    #' @param tickets_per_point (optional) The point-to-ticket conversion ratio (default value: 0.037)
    #' @param has_game_data (optional) Determines whether the application uses services to save custom game objects (default value: TRUE)
    #' @param public_notifications (optional) Public Notifications
    #' @param use_matching_algorithm (optional) Use Matching Algorithm
    #' @param global_tickets (optional) Determines whether earned tickets are applied across all applications (default value: FALSE)
    #' @param build_version (optional) The current build version of the application (default value: 1)
    #' @param api_version (optional) The current API version the application uses
    #' @param placement_name (optional) The name of the placement
    #' @param placement_description (optional) The description of the placement
    #' @param placement_size (optional) The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM (this is required if a placements is to be created)
    #' @param placement_height (optional) The height of a custom ad size
    #' @param placement_width (optional) The width of a custom ad size
    #' @param placement_refresh_interval (optional) The refresh interval in seconds
    #' @param create_object_store (optional) Generate a custom object store to use (default value: TRUE)
    #' @param public_content_approval (optional) Determine whether or not public content requires admin approval before it becomes public (default value: FALSE)
    #' @param production_mode (optional) Determines whether the application uses production or sandbox services (default value: FALSE)
    #' @param minimum_session_length (optional) Minimum Session Length
    #' @param session_gap_length (optional) Session Gap Length
    #' @param local_ads_enabled (optional) Local Ads Enabled (default value: FALSE)
    #' @param sqoot_api_key (optional) Sqoot Api Key
    #' @param trilat_processing_type (optional) Determines how to process trilateration data. Possible values include: DEFAULT, FINGERPRINT (default value: "FINGERPRINT")
    #' @param max_sample_size (optional) Determines what the maximum sample size during trilateration
    #' @param min_rssi (optional) Determines what the minimum acceptable RSSI value
    #' @param modules (optional) List modules allowed to be served by the server, possible values include: COMMON, MEDIA, OFFER, GAME, SOCIAL, CONSUMER_WEBSITE, ALL (default value: "ALL")
    #' @param authorized_count (optional) How many servers the license will support (default value: 1)
    #' @param authorized_servers (optional) The list of ip addresses of servers the license will support, leave null for any server
    #' @param default_timezone (optional) Sets the default timezone for the app (used for leaderboards and other time specific content)
    #' @param smtp_pass (optional) SMTP Pass
    #' @param meta_data (optional) The application meta data. Defined by the client
    #' @param placement_meta_data (optional) The ad placement meta data. Defined by the client
    #' @param ips_floor (optional) Create floor tables for Ips (default value: FALSE)
    #' @param enable_apns_badge (optional) Enables setting the APNS badge value in the payload (default value: TRUE)
    #' @param include_in_report (optional) Enables using the application in session reports (default value: TRUE)
    #' @param default_app_filter_id (optional) Sets the default filter to use (if none is passed in) for certain search APIs including Album Search.
    #' @param enable_welcome_email (optional) Enables whether the default welcome email will be sent for new app users
    #' @param apple_app_id (optional) The Apple Application ID
    #' @param apple_team_id (optional) The Apple Team ID
    #' @param apple_auth_key_id (optional) The Apple Auth Key ID
    #' @param apple_auth_key (optional) The Apple Auth Signin Key (p8) File
    #' @param apple_issuer_id (optional) The Apple Issuer ID
    #' @param app_store_key_id (optional) The Apple App Store Key ID
    #' @param app_store_key (optional) The Apple App Store Key (p8) File
    #' @param google_private_key_file (optional) This is the private key file for your Google service account.
    #' @param authorize_net_api_key (optional) Authorize Net Api Key
    #' @param authorize_net_transaction_key (optional) Authorize Net Transaction Key
    #' @param email_sender (optional) Email Sender
    #' @param smtp_user (optional) SMTP User
    #' @param smtp_host (optional) SMTP Host
    #' @param vatom_business_id (optional) Vatom Business Id
    #' @param vatom_rest_client_id (optional) Vatom REST Client Id
    #' @param vatom_rest_secret_key (optional) Vatom Secret Key
    #' @param twilio_account_sid (optional) Twilio Account SID
    #' @param twilio_auth_token (optional) Twilio Auth Token
    #' @param twilio_sender_phone_number (optional) Twilio Sender Phone Number
    #' @param open_ai_secret_key (optional) OpenAI Secret API Key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ApplicationResponse) with additional information such as HTTP status code, headers
    CreateApplicationWithHttpInfo = function(app_name, device_id = NULL, account_id = NULL, about = NULL, bundle_id = NULL, app_icon_asset_id = NULL, app_logo_asset_id = NULL, facebook_app_id = NULL, facebook_app_secret = NULL, google_api_key = NULL, update_eula_date = NULL, eula_version = NULL, landing_page_url = NULL, show_in_activities = NULL, activity_description = NULL, invite_welcome_text = NULL, invite_page_url = NULL, url_scheme = NULL, platforms = NULL, download_urls = NULL, category_ids = NULL, scoring_type = "GAME_LEVEL", hint_cost = 11, max_score = 125, tickets_per_point = 0.037, has_game_data = TRUE, public_notifications = NULL, use_matching_algorithm = NULL, global_tickets = FALSE, build_version = 1, api_version = NULL, placement_name = NULL, placement_description = NULL, placement_size = NULL, placement_height = NULL, placement_width = NULL, placement_refresh_interval = NULL, create_object_store = TRUE, public_content_approval = FALSE, production_mode = FALSE, minimum_session_length = NULL, session_gap_length = NULL, local_ads_enabled = FALSE, sqoot_api_key = NULL, trilat_processing_type = "FINGERPRINT", max_sample_size = NULL, min_rssi = NULL, modules = "ALL", authorized_count = 1, authorized_servers = NULL, default_timezone = NULL, smtp_pass = NULL, meta_data = NULL, placement_meta_data = NULL, ips_floor = FALSE, enable_apns_badge = TRUE, include_in_report = TRUE, default_app_filter_id = NULL, enable_welcome_email = NULL, apple_app_id = NULL, apple_team_id = NULL, apple_auth_key_id = NULL, apple_auth_key = NULL, apple_issuer_id = NULL, app_store_key_id = NULL, app_store_key = NULL, google_private_key_file = NULL, authorize_net_api_key = NULL, authorize_net_transaction_key = NULL, email_sender = NULL, smtp_user = NULL, smtp_host = NULL, vatom_business_id = NULL, vatom_rest_client_id = NULL, vatom_rest_secret_key = NULL, twilio_account_sid = NULL, twilio_auth_token = NULL, twilio_sender_phone_number = NULL, open_ai_secret_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_name`)) {
        stop("Missing required parameter `app_name`.")
      }

      if (!missing(`app_name`) && is.null(`app_name`)) {
        stop("Invalid value for `app_name` when calling ApplicationApi$CreateApplication, `app_name` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ApplicationApi$CreateApplication, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$CreateApplication, `account_id` is not nullable")
      }

      if (!missing(`about`) && is.null(`about`)) {
        stop("Invalid value for `about` when calling ApplicationApi$CreateApplication, `about` is not nullable")
      }

      if (!missing(`bundle_id`) && is.null(`bundle_id`)) {
        stop("Invalid value for `bundle_id` when calling ApplicationApi$CreateApplication, `bundle_id` is not nullable")
      }

      if (!missing(`app_icon_asset_id`) && is.null(`app_icon_asset_id`)) {
        stop("Invalid value for `app_icon_asset_id` when calling ApplicationApi$CreateApplication, `app_icon_asset_id` is not nullable")
      }

      if (!missing(`app_logo_asset_id`) && is.null(`app_logo_asset_id`)) {
        stop("Invalid value for `app_logo_asset_id` when calling ApplicationApi$CreateApplication, `app_logo_asset_id` is not nullable")
      }

      if (!missing(`facebook_app_id`) && is.null(`facebook_app_id`)) {
        stop("Invalid value for `facebook_app_id` when calling ApplicationApi$CreateApplication, `facebook_app_id` is not nullable")
      }

      if (!missing(`facebook_app_secret`) && is.null(`facebook_app_secret`)) {
        stop("Invalid value for `facebook_app_secret` when calling ApplicationApi$CreateApplication, `facebook_app_secret` is not nullable")
      }

      if (!missing(`google_api_key`) && is.null(`google_api_key`)) {
        stop("Invalid value for `google_api_key` when calling ApplicationApi$CreateApplication, `google_api_key` is not nullable")
      }

      if (!missing(`update_eula_date`) && is.null(`update_eula_date`)) {
        stop("Invalid value for `update_eula_date` when calling ApplicationApi$CreateApplication, `update_eula_date` is not nullable")
      }

      if (!missing(`eula_version`) && is.null(`eula_version`)) {
        stop("Invalid value for `eula_version` when calling ApplicationApi$CreateApplication, `eula_version` is not nullable")
      }

      if (!missing(`landing_page_url`) && is.null(`landing_page_url`)) {
        stop("Invalid value for `landing_page_url` when calling ApplicationApi$CreateApplication, `landing_page_url` is not nullable")
      }

      if (!missing(`show_in_activities`) && is.null(`show_in_activities`)) {
        stop("Invalid value for `show_in_activities` when calling ApplicationApi$CreateApplication, `show_in_activities` is not nullable")
      }

      if (!missing(`activity_description`) && is.null(`activity_description`)) {
        stop("Invalid value for `activity_description` when calling ApplicationApi$CreateApplication, `activity_description` is not nullable")
      }

      if (!missing(`invite_welcome_text`) && is.null(`invite_welcome_text`)) {
        stop("Invalid value for `invite_welcome_text` when calling ApplicationApi$CreateApplication, `invite_welcome_text` is not nullable")
      }

      if (!missing(`invite_page_url`) && is.null(`invite_page_url`)) {
        stop("Invalid value for `invite_page_url` when calling ApplicationApi$CreateApplication, `invite_page_url` is not nullable")
      }

      if (!missing(`url_scheme`) && is.null(`url_scheme`)) {
        stop("Invalid value for `url_scheme` when calling ApplicationApi$CreateApplication, `url_scheme` is not nullable")
      }

      if (!missing(`platforms`) && is.null(`platforms`)) {
        stop("Invalid value for `platforms` when calling ApplicationApi$CreateApplication, `platforms` is not nullable")
      }

      if (!missing(`download_urls`) && is.null(`download_urls`)) {
        stop("Invalid value for `download_urls` when calling ApplicationApi$CreateApplication, `download_urls` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling ApplicationApi$CreateApplication, `category_ids` is not nullable")
      }

      if (!missing(`scoring_type`) && is.null(`scoring_type`)) {
        stop("Invalid value for `scoring_type` when calling ApplicationApi$CreateApplication, `scoring_type` is not nullable")
      }

      if (!missing(`hint_cost`) && is.null(`hint_cost`)) {
        stop("Invalid value for `hint_cost` when calling ApplicationApi$CreateApplication, `hint_cost` is not nullable")
      }

      if (!missing(`max_score`) && is.null(`max_score`)) {
        stop("Invalid value for `max_score` when calling ApplicationApi$CreateApplication, `max_score` is not nullable")
      }

      if (!missing(`tickets_per_point`) && is.null(`tickets_per_point`)) {
        stop("Invalid value for `tickets_per_point` when calling ApplicationApi$CreateApplication, `tickets_per_point` is not nullable")
      }

      if (!missing(`has_game_data`) && is.null(`has_game_data`)) {
        stop("Invalid value for `has_game_data` when calling ApplicationApi$CreateApplication, `has_game_data` is not nullable")
      }

      if (!missing(`public_notifications`) && is.null(`public_notifications`)) {
        stop("Invalid value for `public_notifications` when calling ApplicationApi$CreateApplication, `public_notifications` is not nullable")
      }

      if (!missing(`use_matching_algorithm`) && is.null(`use_matching_algorithm`)) {
        stop("Invalid value for `use_matching_algorithm` when calling ApplicationApi$CreateApplication, `use_matching_algorithm` is not nullable")
      }

      if (!missing(`global_tickets`) && is.null(`global_tickets`)) {
        stop("Invalid value for `global_tickets` when calling ApplicationApi$CreateApplication, `global_tickets` is not nullable")
      }

      if (!missing(`build_version`) && is.null(`build_version`)) {
        stop("Invalid value for `build_version` when calling ApplicationApi$CreateApplication, `build_version` is not nullable")
      }

      if (!missing(`api_version`) && is.null(`api_version`)) {
        stop("Invalid value for `api_version` when calling ApplicationApi$CreateApplication, `api_version` is not nullable")
      }

      if (!missing(`placement_name`) && is.null(`placement_name`)) {
        stop("Invalid value for `placement_name` when calling ApplicationApi$CreateApplication, `placement_name` is not nullable")
      }

      if (!missing(`placement_description`) && is.null(`placement_description`)) {
        stop("Invalid value for `placement_description` when calling ApplicationApi$CreateApplication, `placement_description` is not nullable")
      }

      if (!missing(`placement_size`) && is.null(`placement_size`)) {
        stop("Invalid value for `placement_size` when calling ApplicationApi$CreateApplication, `placement_size` is not nullable")
      }

      if (!missing(`placement_height`) && is.null(`placement_height`)) {
        stop("Invalid value for `placement_height` when calling ApplicationApi$CreateApplication, `placement_height` is not nullable")
      }

      if (!missing(`placement_width`) && is.null(`placement_width`)) {
        stop("Invalid value for `placement_width` when calling ApplicationApi$CreateApplication, `placement_width` is not nullable")
      }

      if (!missing(`placement_refresh_interval`) && is.null(`placement_refresh_interval`)) {
        stop("Invalid value for `placement_refresh_interval` when calling ApplicationApi$CreateApplication, `placement_refresh_interval` is not nullable")
      }

      if (!missing(`create_object_store`) && is.null(`create_object_store`)) {
        stop("Invalid value for `create_object_store` when calling ApplicationApi$CreateApplication, `create_object_store` is not nullable")
      }

      if (!missing(`public_content_approval`) && is.null(`public_content_approval`)) {
        stop("Invalid value for `public_content_approval` when calling ApplicationApi$CreateApplication, `public_content_approval` is not nullable")
      }

      if (!missing(`production_mode`) && is.null(`production_mode`)) {
        stop("Invalid value for `production_mode` when calling ApplicationApi$CreateApplication, `production_mode` is not nullable")
      }

      if (!missing(`minimum_session_length`) && is.null(`minimum_session_length`)) {
        stop("Invalid value for `minimum_session_length` when calling ApplicationApi$CreateApplication, `minimum_session_length` is not nullable")
      }

      if (!missing(`session_gap_length`) && is.null(`session_gap_length`)) {
        stop("Invalid value for `session_gap_length` when calling ApplicationApi$CreateApplication, `session_gap_length` is not nullable")
      }

      if (!missing(`local_ads_enabled`) && is.null(`local_ads_enabled`)) {
        stop("Invalid value for `local_ads_enabled` when calling ApplicationApi$CreateApplication, `local_ads_enabled` is not nullable")
      }

      if (!missing(`sqoot_api_key`) && is.null(`sqoot_api_key`)) {
        stop("Invalid value for `sqoot_api_key` when calling ApplicationApi$CreateApplication, `sqoot_api_key` is not nullable")
      }

      if (!missing(`trilat_processing_type`) && is.null(`trilat_processing_type`)) {
        stop("Invalid value for `trilat_processing_type` when calling ApplicationApi$CreateApplication, `trilat_processing_type` is not nullable")
      }

      if (!missing(`max_sample_size`) && is.null(`max_sample_size`)) {
        stop("Invalid value for `max_sample_size` when calling ApplicationApi$CreateApplication, `max_sample_size` is not nullable")
      }

      if (!missing(`min_rssi`) && is.null(`min_rssi`)) {
        stop("Invalid value for `min_rssi` when calling ApplicationApi$CreateApplication, `min_rssi` is not nullable")
      }

      if (!missing(`modules`) && is.null(`modules`)) {
        stop("Invalid value for `modules` when calling ApplicationApi$CreateApplication, `modules` is not nullable")
      }

      if (!missing(`authorized_count`) && is.null(`authorized_count`)) {
        stop("Invalid value for `authorized_count` when calling ApplicationApi$CreateApplication, `authorized_count` is not nullable")
      }

      if (!missing(`authorized_servers`) && is.null(`authorized_servers`)) {
        stop("Invalid value for `authorized_servers` when calling ApplicationApi$CreateApplication, `authorized_servers` is not nullable")
      }

      if (!missing(`default_timezone`) && is.null(`default_timezone`)) {
        stop("Invalid value for `default_timezone` when calling ApplicationApi$CreateApplication, `default_timezone` is not nullable")
      }

      if (!missing(`smtp_pass`) && is.null(`smtp_pass`)) {
        stop("Invalid value for `smtp_pass` when calling ApplicationApi$CreateApplication, `smtp_pass` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling ApplicationApi$CreateApplication, `meta_data` is not nullable")
      }

      if (!missing(`placement_meta_data`) && is.null(`placement_meta_data`)) {
        stop("Invalid value for `placement_meta_data` when calling ApplicationApi$CreateApplication, `placement_meta_data` is not nullable")
      }

      if (!missing(`ips_floor`) && is.null(`ips_floor`)) {
        stop("Invalid value for `ips_floor` when calling ApplicationApi$CreateApplication, `ips_floor` is not nullable")
      }

      if (!missing(`enable_apns_badge`) && is.null(`enable_apns_badge`)) {
        stop("Invalid value for `enable_apns_badge` when calling ApplicationApi$CreateApplication, `enable_apns_badge` is not nullable")
      }

      if (!missing(`include_in_report`) && is.null(`include_in_report`)) {
        stop("Invalid value for `include_in_report` when calling ApplicationApi$CreateApplication, `include_in_report` is not nullable")
      }

      if (!missing(`default_app_filter_id`) && is.null(`default_app_filter_id`)) {
        stop("Invalid value for `default_app_filter_id` when calling ApplicationApi$CreateApplication, `default_app_filter_id` is not nullable")
      }

      if (!missing(`enable_welcome_email`) && is.null(`enable_welcome_email`)) {
        stop("Invalid value for `enable_welcome_email` when calling ApplicationApi$CreateApplication, `enable_welcome_email` is not nullable")
      }

      if (!missing(`apple_app_id`) && is.null(`apple_app_id`)) {
        stop("Invalid value for `apple_app_id` when calling ApplicationApi$CreateApplication, `apple_app_id` is not nullable")
      }

      if (!missing(`apple_team_id`) && is.null(`apple_team_id`)) {
        stop("Invalid value for `apple_team_id` when calling ApplicationApi$CreateApplication, `apple_team_id` is not nullable")
      }

      if (!missing(`apple_auth_key_id`) && is.null(`apple_auth_key_id`)) {
        stop("Invalid value for `apple_auth_key_id` when calling ApplicationApi$CreateApplication, `apple_auth_key_id` is not nullable")
      }

      if (!missing(`apple_auth_key`) && is.null(`apple_auth_key`)) {
        stop("Invalid value for `apple_auth_key` when calling ApplicationApi$CreateApplication, `apple_auth_key` is not nullable")
      }

      if (!missing(`apple_issuer_id`) && is.null(`apple_issuer_id`)) {
        stop("Invalid value for `apple_issuer_id` when calling ApplicationApi$CreateApplication, `apple_issuer_id` is not nullable")
      }

      if (!missing(`app_store_key_id`) && is.null(`app_store_key_id`)) {
        stop("Invalid value for `app_store_key_id` when calling ApplicationApi$CreateApplication, `app_store_key_id` is not nullable")
      }

      if (!missing(`app_store_key`) && is.null(`app_store_key`)) {
        stop("Invalid value for `app_store_key` when calling ApplicationApi$CreateApplication, `app_store_key` is not nullable")
      }

      if (!missing(`google_private_key_file`) && is.null(`google_private_key_file`)) {
        stop("Invalid value for `google_private_key_file` when calling ApplicationApi$CreateApplication, `google_private_key_file` is not nullable")
      }

      if (!missing(`authorize_net_api_key`) && is.null(`authorize_net_api_key`)) {
        stop("Invalid value for `authorize_net_api_key` when calling ApplicationApi$CreateApplication, `authorize_net_api_key` is not nullable")
      }

      if (!missing(`authorize_net_transaction_key`) && is.null(`authorize_net_transaction_key`)) {
        stop("Invalid value for `authorize_net_transaction_key` when calling ApplicationApi$CreateApplication, `authorize_net_transaction_key` is not nullable")
      }

      if (!missing(`email_sender`) && is.null(`email_sender`)) {
        stop("Invalid value for `email_sender` when calling ApplicationApi$CreateApplication, `email_sender` is not nullable")
      }

      if (!missing(`smtp_user`) && is.null(`smtp_user`)) {
        stop("Invalid value for `smtp_user` when calling ApplicationApi$CreateApplication, `smtp_user` is not nullable")
      }

      if (!missing(`smtp_host`) && is.null(`smtp_host`)) {
        stop("Invalid value for `smtp_host` when calling ApplicationApi$CreateApplication, `smtp_host` is not nullable")
      }

      if (!missing(`vatom_business_id`) && is.null(`vatom_business_id`)) {
        stop("Invalid value for `vatom_business_id` when calling ApplicationApi$CreateApplication, `vatom_business_id` is not nullable")
      }

      if (!missing(`vatom_rest_client_id`) && is.null(`vatom_rest_client_id`)) {
        stop("Invalid value for `vatom_rest_client_id` when calling ApplicationApi$CreateApplication, `vatom_rest_client_id` is not nullable")
      }

      if (!missing(`vatom_rest_secret_key`) && is.null(`vatom_rest_secret_key`)) {
        stop("Invalid value for `vatom_rest_secret_key` when calling ApplicationApi$CreateApplication, `vatom_rest_secret_key` is not nullable")
      }

      if (!missing(`twilio_account_sid`) && is.null(`twilio_account_sid`)) {
        stop("Invalid value for `twilio_account_sid` when calling ApplicationApi$CreateApplication, `twilio_account_sid` is not nullable")
      }

      if (!missing(`twilio_auth_token`) && is.null(`twilio_auth_token`)) {
        stop("Invalid value for `twilio_auth_token` when calling ApplicationApi$CreateApplication, `twilio_auth_token` is not nullable")
      }

      if (!missing(`twilio_sender_phone_number`) && is.null(`twilio_sender_phone_number`)) {
        stop("Invalid value for `twilio_sender_phone_number` when calling ApplicationApi$CreateApplication, `twilio_sender_phone_number` is not nullable")
      }

      if (!missing(`open_ai_secret_key`) && is.null(`open_ai_secret_key`)) {
        stop("Invalid value for `open_ai_secret_key` when calling ApplicationApi$CreateApplication, `open_ai_secret_key` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appName"]] <- `app_name`

      query_params[["about"]] <- `about`

      query_params[["bundleId"]] <- `bundle_id`

      query_params[["appIconAssetId"]] <- `app_icon_asset_id`

      query_params[["appLogoAssetId"]] <- `app_logo_asset_id`

      query_params[["facebookAppId"]] <- `facebook_app_id`

      query_params[["facebookAppSecret"]] <- `facebook_app_secret`

      query_params[["googleApiKey"]] <- `google_api_key`

      query_params[["updateEULADate"]] <- `update_eula_date`

      query_params[["eulaVersion"]] <- `eula_version`

      query_params[["landingPageUrl"]] <- `landing_page_url`

      query_params[["showInActivities"]] <- `show_in_activities`

      query_params[["activityDescription"]] <- `activity_description`

      query_params[["inviteWelcomeText"]] <- `invite_welcome_text`

      query_params[["invitePageUrl"]] <- `invite_page_url`

      query_params[["urlScheme"]] <- `url_scheme`

      query_params[["platforms"]] <- `platforms`

      query_params[["downloadUrls"]] <- `download_urls`

      query_params[["categoryIds"]] <- `category_ids`

      if (!is.null(`scoring_type`) && !(`scoring_type` %in% c("GAME_LEVEL", "GAME_OBJECT"))) {
        stop("Invalid value for scoring_type when calling ApplicationApi$CreateApplication. Must be [GAME_LEVEL, GAME_OBJECT].")
      }
      query_params[["scoringType"]] <- `scoring_type`

      query_params[["hintCost"]] <- `hint_cost`

      query_params[["maxScore"]] <- `max_score`

      query_params[["ticketsPerPoint"]] <- `tickets_per_point`

      query_params[["hasGameData"]] <- `has_game_data`

      query_params[["publicNotifications"]] <- `public_notifications`

      query_params[["useMatchingAlgorithm"]] <- `use_matching_algorithm`

      query_params[["globalTickets"]] <- `global_tickets`

      query_params[["buildVersion"]] <- `build_version`

      query_params[["apiVersion"]] <- `api_version`

      query_params[["placementName"]] <- `placement_name`

      query_params[["placementDescription"]] <- `placement_description`

      if (!is.null(`placement_size`) && !(`placement_size` %in% c("CONFIG", "BANNER", "LEADERBOARD", "SKYSCRAPER", "VIDEO", "ZIP", "INTERSTITIAL", "CUSTOM1", "CUSTOM2", "CUSTOM3", "CUSTOM4", "CUSTOM5", "CUSTOM6", "CUSTOM7", "CUSTOM8", "CUSTOM9", "CUSTOM10"))) {
        stop("Invalid value for placement_size when calling ApplicationApi$CreateApplication. Must be [CONFIG, BANNER, LEADERBOARD, SKYSCRAPER, VIDEO, ZIP, INTERSTITIAL, CUSTOM1, CUSTOM2, CUSTOM3, CUSTOM4, CUSTOM5, CUSTOM6, CUSTOM7, CUSTOM8, CUSTOM9, CUSTOM10].")
      }
      query_params[["placementSize"]] <- `placement_size`

      query_params[["placementHeight"]] <- `placement_height`

      query_params[["placementWidth"]] <- `placement_width`

      query_params[["placementRefreshInterval"]] <- `placement_refresh_interval`

      query_params[["createObjectStore"]] <- `create_object_store`

      query_params[["publicContentApproval"]] <- `public_content_approval`

      query_params[["productionMode"]] <- `production_mode`

      query_params[["minimumSessionLength"]] <- `minimum_session_length`

      query_params[["sessionGapLength"]] <- `session_gap_length`

      query_params[["localAdsEnabled"]] <- `local_ads_enabled`

      query_params[["sqootApiKey"]] <- `sqoot_api_key`

      if (!is.null(`trilat_processing_type`) && !(`trilat_processing_type` %in% c("DEFAULT", "FINGERPRINT", "FINGERPRINT_V2"))) {
        stop("Invalid value for trilat_processing_type when calling ApplicationApi$CreateApplication. Must be [DEFAULT, FINGERPRINT, FINGERPRINT_V2].")
      }
      query_params[["trilatProcessingType"]] <- `trilat_processing_type`

      query_params[["maxSampleSize"]] <- `max_sample_size`

      query_params[["minRSSI"]] <- `min_rssi`

      query_params[["modules"]] <- `modules`

      query_params[["authorizedCount"]] <- `authorized_count`

      query_params[["authorizedServers"]] <- `authorized_servers`

      query_params[["defaultTimezone"]] <- `default_timezone`

      query_params[["smtpPass"]] <- `smtp_pass`

      query_params[["metaData"]] <- `meta_data`

      query_params[["placementMetaData"]] <- `placement_meta_data`

      query_params[["ipsFloor"]] <- `ips_floor`

      query_params[["enableAPNSBadge"]] <- `enable_apns_badge`

      query_params[["includeInReport"]] <- `include_in_report`

      query_params[["defaultAppFilterId"]] <- `default_app_filter_id`

      query_params[["enableWelcomeEmail"]] <- `enable_welcome_email`

      query_params[["appleAppId"]] <- `apple_app_id`

      query_params[["appleTeamId"]] <- `apple_team_id`

      query_params[["appleAuthKeyId"]] <- `apple_auth_key_id`

      query_params[["appleAuthKey"]] <- `apple_auth_key`

      query_params[["appleIssuerId"]] <- `apple_issuer_id`

      query_params[["appStoreKeyId"]] <- `app_store_key_id`

      query_params[["appStoreKey"]] <- `app_store_key`

      query_params[["googlePrivateKeyFile"]] <- `google_private_key_file`

      query_params[["authorizeNetApiKey"]] <- `authorize_net_api_key`

      query_params[["authorizeNetTransactionKey"]] <- `authorize_net_transaction_key`

      query_params[["emailSender"]] <- `email_sender`

      query_params[["smtpUser"]] <- `smtp_user`

      query_params[["smtpHost"]] <- `smtp_host`

      query_params[["vatomBusinessId"]] <- `vatom_business_id`

      query_params[["vatomRestClientId"]] <- `vatom_rest_client_id`

      query_params[["vatomRestSecretKey"]] <- `vatom_rest_secret_key`

      query_params[["twilioAccountSID"]] <- `twilio_account_sid`

      query_params[["twilioAuthToken"]] <- `twilio_auth_token`

      query_params[["twilioSenderPhoneNumber"]] <- `twilio_sender_phone_number`

      query_params[["openAISecretKey"]] <- `open_ai_secret_key`

      local_var_url_path <- "/application/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ApplicationResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Ad Placement
    #'
    #' @param app_key The appKey of the application the ad placement is for
    #' @param size The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param name (optional) The name of the placement
    #' @param description (optional) The description of the placement
    #' @param height (optional) The height of a custom ad size
    #' @param width (optional) The width of a custom ad size
    #' @param refresh_interval (optional) The refresh interval in seconds
    #' @param default_image_id (optional) Default Image Id
    #' @param active (optional) Active
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PlacementResponse
    CreateApplicationPlacement = function(app_key, size, device_id = NULL, account_id = NULL, name = NULL, description = NULL, height = NULL, width = NULL, refresh_interval = NULL, default_image_id = NULL, active = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateApplicationPlacementWithHttpInfo(app_key, size, device_id, account_id, name, description, height, width, refresh_interval, default_image_id, active, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Ad Placement
    #'
    #' @param app_key The appKey of the application the ad placement is for
    #' @param size The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param name (optional) The name of the placement
    #' @param description (optional) The description of the placement
    #' @param height (optional) The height of a custom ad size
    #' @param width (optional) The width of a custom ad size
    #' @param refresh_interval (optional) The refresh interval in seconds
    #' @param default_image_id (optional) Default Image Id
    #' @param active (optional) Active
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PlacementResponse) with additional information such as HTTP status code, headers
    CreateApplicationPlacementWithHttpInfo = function(app_key, size, device_id = NULL, account_id = NULL, name = NULL, description = NULL, height = NULL, width = NULL, refresh_interval = NULL, default_image_id = NULL, active = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`size`)) {
        stop("Missing required parameter `size`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationApi$CreateApplicationPlacement, `app_key` is not nullable")
      }

      if (!missing(`size`) && is.null(`size`)) {
        stop("Invalid value for `size` when calling ApplicationApi$CreateApplicationPlacement, `size` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ApplicationApi$CreateApplicationPlacement, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$CreateApplicationPlacement, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling ApplicationApi$CreateApplicationPlacement, `name` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling ApplicationApi$CreateApplicationPlacement, `description` is not nullable")
      }

      if (!missing(`height`) && is.null(`height`)) {
        stop("Invalid value for `height` when calling ApplicationApi$CreateApplicationPlacement, `height` is not nullable")
      }

      if (!missing(`width`) && is.null(`width`)) {
        stop("Invalid value for `width` when calling ApplicationApi$CreateApplicationPlacement, `width` is not nullable")
      }

      if (!missing(`refresh_interval`) && is.null(`refresh_interval`)) {
        stop("Invalid value for `refresh_interval` when calling ApplicationApi$CreateApplicationPlacement, `refresh_interval` is not nullable")
      }

      if (!missing(`default_image_id`) && is.null(`default_image_id`)) {
        stop("Invalid value for `default_image_id` when calling ApplicationApi$CreateApplicationPlacement, `default_image_id` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling ApplicationApi$CreateApplicationPlacement, `active` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      if (!is.null(`size`) && !(`size` %in% c("CONFIG", "BANNER", "LEADERBOARD", "SKYSCRAPER", "VIDEO", "ZIP", "INTERSTITIAL", "CUSTOM1", "CUSTOM2", "CUSTOM3", "CUSTOM4", "CUSTOM5", "CUSTOM6", "CUSTOM7", "CUSTOM8", "CUSTOM9", "CUSTOM10"))) {
        stop("Invalid value for size when calling ApplicationApi$CreateApplicationPlacement. Must be [CONFIG, BANNER, LEADERBOARD, SKYSCRAPER, VIDEO, ZIP, INTERSTITIAL, CUSTOM1, CUSTOM2, CUSTOM3, CUSTOM4, CUSTOM5, CUSTOM6, CUSTOM7, CUSTOM8, CUSTOM9, CUSTOM10].")
      }
      query_params[["size"]] <- `size`

      query_params[["height"]] <- `height`

      query_params[["width"]] <- `width`

      query_params[["refreshInterval"]] <- `refresh_interval`

      query_params[["defaultImageId"]] <- `default_image_id`

      query_params[["active"]] <- `active`

      local_var_url_path <- "/application/placement/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PlacementResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Application
    #'
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the application.
    #' @param app_key (optional) The key of the application to be deleted
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteApplication = function(account_id = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteApplicationWithHttpInfo(account_id, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Application
    #'
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the application.
    #' @param app_key (optional) The key of the application to be deleted
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteApplicationWithHttpInfo = function(account_id = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$DeleteApplication, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationApi$DeleteApplication, `app_key` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/application/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Ad Placement
    #'
    #' @param placement_id The id of the placement to delete, the user must have rights to the application the ad placement is for
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PlacementResponse
    DeleteApplicationPlacement = function(placement_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteApplicationPlacementWithHttpInfo(placement_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Ad Placement
    #'
    #' @param placement_id The id of the placement to delete, the user must have rights to the application the ad placement is for
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PlacementResponse) with additional information such as HTTP status code, headers
    DeleteApplicationPlacementWithHttpInfo = function(placement_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`placement_id`)) {
        stop("Missing required parameter `placement_id`.")
      }

      if (!missing(`placement_id`) && is.null(`placement_id`)) {
        stop("Invalid value for `placement_id` when calling ApplicationApi$DeleteApplicationPlacement, `placement_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ApplicationApi$DeleteApplicationPlacement, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$DeleteApplicationPlacement, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["placementId"]] <- `placement_id`

      local_var_url_path <- "/application/placement/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PlacementResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Application
    #'
    #' @param app_key (optional) The key of the application
    #' @param application_id (optional) Application Id
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ApplicationResponse
    GetApplication = function(app_key = NULL, application_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetApplicationWithHttpInfo(app_key, application_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Application
    #'
    #' @param app_key (optional) The key of the application
    #' @param application_id (optional) Application Id
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ApplicationResponse) with additional information such as HTTP status code, headers
    GetApplicationWithHttpInfo = function(app_key = NULL, application_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationApi$GetApplication, `app_key` is not nullable")
      }

      if (!missing(`application_id`) && is.null(`application_id`)) {
        stop("Invalid value for `application_id` when calling ApplicationApi$GetApplication, `application_id` is not nullable")
      }

      query_params[["appKey"]] <- `app_key`

      query_params[["applicationId"]] <- `application_id`

      local_var_url_path <- "/application/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ApplicationResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Ad Placement
    #'
    #' @param placement_id The id of the placement
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PlacementResponse
    GetApplicationPlacement = function(placement_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetApplicationPlacementWithHttpInfo(placement_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Ad Placement
    #'
    #' @param placement_id The id of the placement
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PlacementResponse) with additional information such as HTTP status code, headers
    GetApplicationPlacementWithHttpInfo = function(placement_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`placement_id`)) {
        stop("Missing required parameter `placement_id`.")
      }

      if (!missing(`placement_id`) && is.null(`placement_id`)) {
        stop("Invalid value for `placement_id` when calling ApplicationApi$GetApplicationPlacement, `placement_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ApplicationApi$GetApplicationPlacement, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$GetApplicationPlacement, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["placementId"]] <- `placement_id`

      local_var_url_path <- "/application/placement/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PlacementResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get API versions
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    GetApplicationVersions = function(data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetApplicationVersionsWithHttpInfo(data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get API versions
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    GetApplicationVersionsWithHttpInfo = function(data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/application/versions"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Application Users
    #'
    #' @param app_key The application key
    #' @param q (optional) Q
    #' @param keyword (optional) The keyword used to search
    #' @param since (optional) Return accounts that have been active after this date (UNIX time-stamp in milliseconds)
    #' @param _i (optional) the start of the index
    #' @param start (optional) The start of the pagination (default value: 0)
    #' @param _l (optional) the limit of the index
    #' @param limit (optional) The limit of the pagination (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AccountListResponse
    GetUniqueUsersByApp = function(app_key, q = NULL, keyword = NULL, since = NULL, _i = NULL, start = 0, _l = NULL, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetUniqueUsersByAppWithHttpInfo(app_key, q, keyword, since, _i, start, _l, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Application Users
    #'
    #' @param app_key The application key
    #' @param q (optional) Q
    #' @param keyword (optional) The keyword used to search
    #' @param since (optional) Return accounts that have been active after this date (UNIX time-stamp in milliseconds)
    #' @param _i (optional) the start of the index
    #' @param start (optional) The start of the pagination (default value: 0)
    #' @param _l (optional) the limit of the index
    #' @param limit (optional) The limit of the pagination (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AccountListResponse) with additional information such as HTTP status code, headers
    GetUniqueUsersByAppWithHttpInfo = function(app_key, q = NULL, keyword = NULL, since = NULL, _i = NULL, start = 0, _l = NULL, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationApi$GetUniqueUsersByApp, `app_key` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling ApplicationApi$GetUniqueUsersByApp, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ApplicationApi$GetUniqueUsersByApp, `keyword` is not nullable")
      }

      if (!missing(`since`) && is.null(`since`)) {
        stop("Invalid value for `since` when calling ApplicationApi$GetUniqueUsersByApp, `since` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling ApplicationApi$GetUniqueUsersByApp, `_i` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ApplicationApi$GetUniqueUsersByApp, `start` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling ApplicationApi$GetUniqueUsersByApp, `_l` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ApplicationApi$GetUniqueUsersByApp, `limit` is not nullable")
      }

      query_params[["appKey"]] <- `app_key`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["since"]] <- `since`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/application/users"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AccountListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List Applications
    #'
    #' @param account_id (optional) The account id of the application owner/manager
    #' @param q (optional) Q
    #' @param keyword (optional) The keyword used to search for title, about, and description fields
    #' @param platforms (optional) Deprecated, use deviceIds and deviceVersions
    #' @param device_ids (optional) The list of targeted device ids, comma separated; possible values are retreived via /api/{version/audience/devices com.sirqul.geoapps.common.service.AudienceApi#getDevices getDevices (pass in deviceId values)
    #' @param device_versions (optional) The list of targeted device version ranges that are aligned  with the provided devices list (see devices param for possible values),  comma separated; examples are: 2.3-X, 0-5.1.4, 4.3.1-6.1.4 where X  is no maximum and 0 is no minimum. 
    #' @param category_ids (optional) The list of category ids to filter the list by
    #' @param sort_field (optional) The column to sort the search on, possible values include: UPDATED (default), CREATED, TITLE (default value: "UPDATED")
    #' @param has_ads (optional) Filter results on whether the application supports ads or not. Ignore this parameter to return all results.
    #' @param public_notifications (optional) Filter results on whether the application is available for public trigger notifications
    #' @param filter_billable (optional) Determines whether to only return applications that the user has access to
    #' @param filter_content_admin (optional) Determines whether to only return applications that the user is a content admin of
    #' @param descending (optional) The order to return the search results (default value: TRUE)
    #' @param _i (optional) the start of the index
    #' @param start (optional) The start of the pagination (default value: 0)
    #' @param _l (optional) The limit of the index
    #' @param limit (optional) The limit of the pagination (default value: 20)
    #' @param application_ids (optional) The list of application ids, comma separated. If provided will ignore all other params.
    #' @param has_object_store (optional) Only include applications with a object store (default is false) (default value: FALSE)
    #' @param active_only (optional) Return only active results (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ApplicationShortResponse]
    ListApplications = function(account_id = NULL, q = NULL, keyword = NULL, platforms = NULL, device_ids = NULL, device_versions = NULL, category_ids = NULL, sort_field = "UPDATED", has_ads = NULL, public_notifications = NULL, filter_billable = NULL, filter_content_admin = NULL, descending = TRUE, _i = NULL, start = 0, _l = NULL, limit = 20, application_ids = NULL, has_object_store = FALSE, active_only = TRUE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ListApplicationsWithHttpInfo(account_id, q, keyword, platforms, device_ids, device_versions, category_ids, sort_field, has_ads, public_notifications, filter_billable, filter_content_admin, descending, _i, start, _l, limit, application_ids, has_object_store, active_only, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List Applications
    #'
    #' @param account_id (optional) The account id of the application owner/manager
    #' @param q (optional) Q
    #' @param keyword (optional) The keyword used to search for title, about, and description fields
    #' @param platforms (optional) Deprecated, use deviceIds and deviceVersions
    #' @param device_ids (optional) The list of targeted device ids, comma separated; possible values are retreived via /api/{version/audience/devices com.sirqul.geoapps.common.service.AudienceApi#getDevices getDevices (pass in deviceId values)
    #' @param device_versions (optional) The list of targeted device version ranges that are aligned  with the provided devices list (see devices param for possible values),  comma separated; examples are: 2.3-X, 0-5.1.4, 4.3.1-6.1.4 where X  is no maximum and 0 is no minimum. 
    #' @param category_ids (optional) The list of category ids to filter the list by
    #' @param sort_field (optional) The column to sort the search on, possible values include: UPDATED (default), CREATED, TITLE (default value: "UPDATED")
    #' @param has_ads (optional) Filter results on whether the application supports ads or not. Ignore this parameter to return all results.
    #' @param public_notifications (optional) Filter results on whether the application is available for public trigger notifications
    #' @param filter_billable (optional) Determines whether to only return applications that the user has access to
    #' @param filter_content_admin (optional) Determines whether to only return applications that the user is a content admin of
    #' @param descending (optional) The order to return the search results (default value: TRUE)
    #' @param _i (optional) the start of the index
    #' @param start (optional) The start of the pagination (default value: 0)
    #' @param _l (optional) The limit of the index
    #' @param limit (optional) The limit of the pagination (default value: 20)
    #' @param application_ids (optional) The list of application ids, comma separated. If provided will ignore all other params.
    #' @param has_object_store (optional) Only include applications with a object store (default is false) (default value: FALSE)
    #' @param active_only (optional) Return only active results (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ApplicationShortResponse]) with additional information such as HTTP status code, headers
    ListApplicationsWithHttpInfo = function(account_id = NULL, q = NULL, keyword = NULL, platforms = NULL, device_ids = NULL, device_versions = NULL, category_ids = NULL, sort_field = "UPDATED", has_ads = NULL, public_notifications = NULL, filter_billable = NULL, filter_content_admin = NULL, descending = TRUE, _i = NULL, start = 0, _l = NULL, limit = 20, application_ids = NULL, has_object_store = FALSE, active_only = TRUE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$ListApplications, `account_id` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling ApplicationApi$ListApplications, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ApplicationApi$ListApplications, `keyword` is not nullable")
      }

      if (!missing(`platforms`) && is.null(`platforms`)) {
        stop("Invalid value for `platforms` when calling ApplicationApi$ListApplications, `platforms` is not nullable")
      }

      if (!missing(`device_ids`) && is.null(`device_ids`)) {
        stop("Invalid value for `device_ids` when calling ApplicationApi$ListApplications, `device_ids` is not nullable")
      }

      if (!missing(`device_versions`) && is.null(`device_versions`)) {
        stop("Invalid value for `device_versions` when calling ApplicationApi$ListApplications, `device_versions` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling ApplicationApi$ListApplications, `category_ids` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling ApplicationApi$ListApplications, `sort_field` is not nullable")
      }

      if (!missing(`has_ads`) && is.null(`has_ads`)) {
        stop("Invalid value for `has_ads` when calling ApplicationApi$ListApplications, `has_ads` is not nullable")
      }

      if (!missing(`public_notifications`) && is.null(`public_notifications`)) {
        stop("Invalid value for `public_notifications` when calling ApplicationApi$ListApplications, `public_notifications` is not nullable")
      }

      if (!missing(`filter_billable`) && is.null(`filter_billable`)) {
        stop("Invalid value for `filter_billable` when calling ApplicationApi$ListApplications, `filter_billable` is not nullable")
      }

      if (!missing(`filter_content_admin`) && is.null(`filter_content_admin`)) {
        stop("Invalid value for `filter_content_admin` when calling ApplicationApi$ListApplications, `filter_content_admin` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling ApplicationApi$ListApplications, `descending` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling ApplicationApi$ListApplications, `_i` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ApplicationApi$ListApplications, `start` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling ApplicationApi$ListApplications, `_l` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ApplicationApi$ListApplications, `limit` is not nullable")
      }

      if (!missing(`application_ids`) && is.null(`application_ids`)) {
        stop("Invalid value for `application_ids` when calling ApplicationApi$ListApplications, `application_ids` is not nullable")
      }

      if (!missing(`has_object_store`) && is.null(`has_object_store`)) {
        stop("Invalid value for `has_object_store` when calling ApplicationApi$ListApplications, `has_object_store` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling ApplicationApi$ListApplications, `active_only` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["platforms"]] <- `platforms`

      query_params[["deviceIds"]] <- `device_ids`

      query_params[["deviceVersions"]] <- `device_versions`

      query_params[["categoryIds"]] <- `category_ids`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "HAS_ADVERTISEMENTS", "PUBLIC_NOTIFICATIONS", "PLACEMENTS", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY", "TITLE", "NAME", "ABOUT", "DESCRIPTION", "APPLICATION_ID", "APP_TYPE", "GAME_TYPE", "BUNDLE_ID", "SCORING_TYPE", "LANDING_PAGE_URL", "EULA_VERSION", "BUILD_VERSION", "API_VERSION"))) {
        stop("Invalid value for sort_field when calling ApplicationApi$ListApplications. Must be [CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, HAS_ADVERTISEMENTS, PUBLIC_NOTIFICATIONS, PLACEMENTS, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY, TITLE, NAME, ABOUT, DESCRIPTION, APPLICATION_ID, APP_TYPE, GAME_TYPE, BUNDLE_ID, SCORING_TYPE, LANDING_PAGE_URL, EULA_VERSION, BUILD_VERSION, API_VERSION].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["hasAds"]] <- `has_ads`

      query_params[["publicNotifications"]] <- `public_notifications`

      query_params[["filterBillable"]] <- `filter_billable`

      query_params[["filterContentAdmin"]] <- `filter_content_admin`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["applicationIds"]] <- `application_ids`

      query_params[["hasObjectStore"]] <- `has_object_store`

      query_params[["activeOnly"]] <- `active_only`

      local_var_url_path <- "/application/list"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ApplicationShortResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search for Ad Placements
    #'
    #' @param app_key The key of the application
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param start (optional) The start of the pagination (default value: 0)
    #' @param limit (optional) The limit of the pagination (default value: 100)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[PlacementResponse]
    SearchApplicationPlacement = function(app_key, device_id = NULL, account_id = NULL, start = 0, limit = 100, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchApplicationPlacementWithHttpInfo(app_key, device_id, account_id, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search for Ad Placements
    #'
    #' @param app_key The key of the application
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param start (optional) The start of the pagination (default value: 0)
    #' @param limit (optional) The limit of the pagination (default value: 100)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[PlacementResponse]) with additional information such as HTTP status code, headers
    SearchApplicationPlacementWithHttpInfo = function(app_key, device_id = NULL, account_id = NULL, start = 0, limit = 100, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationApi$SearchApplicationPlacement, `app_key` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ApplicationApi$SearchApplicationPlacement, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$SearchApplicationPlacement, `account_id` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ApplicationApi$SearchApplicationPlacement, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ApplicationApi$SearchApplicationPlacement, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/application/placement/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[PlacementResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search for Application Settings
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param connection_account_id (optional) The account id used to view another person's account
    #' @param keyword (optional) The string to search applications on
    #' @param sort_field (optional) The column to sort the search on. Possible values include: CREATED, UPDATED, APPLICATION_TITLE (default value: "APPLICATION_TITLE")
    #' @param descending (optional) The order to return the search results (default value: FALSE)
    #' @param start (optional) The start index for pagination (default value: 0)
    #' @param limit (optional) The limit per result set for pagination (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ApplicationSettingsResponse
    SearchApplicationSettings = function(device_id = NULL, account_id = NULL, connection_account_id = NULL, keyword = NULL, sort_field = "APPLICATION_TITLE", descending = FALSE, start = 0, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchApplicationSettingsWithHttpInfo(device_id, account_id, connection_account_id, keyword, sort_field, descending, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search for Application Settings
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param connection_account_id (optional) The account id used to view another person's account
    #' @param keyword (optional) The string to search applications on
    #' @param sort_field (optional) The column to sort the search on. Possible values include: CREATED, UPDATED, APPLICATION_TITLE (default value: "APPLICATION_TITLE")
    #' @param descending (optional) The order to return the search results (default value: FALSE)
    #' @param start (optional) The start index for pagination (default value: 0)
    #' @param limit (optional) The limit per result set for pagination (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ApplicationSettingsResponse) with additional information such as HTTP status code, headers
    SearchApplicationSettingsWithHttpInfo = function(device_id = NULL, account_id = NULL, connection_account_id = NULL, keyword = NULL, sort_field = "APPLICATION_TITLE", descending = FALSE, start = 0, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ApplicationApi$SearchApplicationSettings, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$SearchApplicationSettings, `account_id` is not nullable")
      }

      if (!missing(`connection_account_id`) && is.null(`connection_account_id`)) {
        stop("Invalid value for `connection_account_id` when calling ApplicationApi$SearchApplicationSettings, `connection_account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ApplicationApi$SearchApplicationSettings, `keyword` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling ApplicationApi$SearchApplicationSettings, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling ApplicationApi$SearchApplicationSettings, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ApplicationApi$SearchApplicationSettings, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ApplicationApi$SearchApplicationSettings, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["connectionAccountId"]] <- `connection_account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/application/settings/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ApplicationSettingsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Applications
    #'
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) The location of the device
    #' @param longitude (optional) The location of the device
    #' @param q (optional) Q
    #' @param keyword (optional) The keyword used to search
    #' @param q_search_fields (optional) The columns to applied the keyword search to (default value: "NAME,ABOUT,BILLABLE_ENTITY_NAME")
    #' @param sort_field (optional) The column to sort the search on (default value: "APPLICATION_ID")
    #' @param descending (optional) The order to return the search results (default value: FALSE)
    #' @param _i (optional) the start of the index
    #' @param start (optional) The start of the pagination (default value: 0)
    #' @param _l (optional) the limit of the index
    #' @param limit (optional) The limit of the pagination (default value: 100)
    #' @param has_ads (optional) Filter results on whether the application supports ads or not. Ignore this parameter to return all results.
    #' @param public_notifications (optional) Filter results on whether the application is available for public trigger notifications
    #' @param active_only (optional) Return only active results (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ApplicationResponse]
    SearchApplications = function(device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, q = NULL, keyword = NULL, q_search_fields = "NAME,ABOUT,BILLABLE_ENTITY_NAME", sort_field = "APPLICATION_ID", descending = FALSE, _i = NULL, start = 0, _l = NULL, limit = 100, has_ads = NULL, public_notifications = NULL, active_only = FALSE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchApplicationsWithHttpInfo(device_id, account_id, latitude, longitude, q, keyword, q_search_fields, sort_field, descending, _i, start, _l, limit, has_ads, public_notifications, active_only, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Applications
    #'
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param latitude (optional) The location of the device
    #' @param longitude (optional) The location of the device
    #' @param q (optional) Q
    #' @param keyword (optional) The keyword used to search
    #' @param q_search_fields (optional) The columns to applied the keyword search to (default value: "NAME,ABOUT,BILLABLE_ENTITY_NAME")
    #' @param sort_field (optional) The column to sort the search on (default value: "APPLICATION_ID")
    #' @param descending (optional) The order to return the search results (default value: FALSE)
    #' @param _i (optional) the start of the index
    #' @param start (optional) The start of the pagination (default value: 0)
    #' @param _l (optional) the limit of the index
    #' @param limit (optional) The limit of the pagination (default value: 100)
    #' @param has_ads (optional) Filter results on whether the application supports ads or not. Ignore this parameter to return all results.
    #' @param public_notifications (optional) Filter results on whether the application is available for public trigger notifications
    #' @param active_only (optional) Return only active results (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ApplicationResponse]) with additional information such as HTTP status code, headers
    SearchApplicationsWithHttpInfo = function(device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, q = NULL, keyword = NULL, q_search_fields = "NAME,ABOUT,BILLABLE_ENTITY_NAME", sort_field = "APPLICATION_ID", descending = FALSE, _i = NULL, start = 0, _l = NULL, limit = 100, has_ads = NULL, public_notifications = NULL, active_only = FALSE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ApplicationApi$SearchApplications, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$SearchApplications, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ApplicationApi$SearchApplications, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ApplicationApi$SearchApplications, `longitude` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling ApplicationApi$SearchApplications, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ApplicationApi$SearchApplications, `keyword` is not nullable")
      }

      if (!missing(`q_search_fields`) && is.null(`q_search_fields`)) {
        stop("Invalid value for `q_search_fields` when calling ApplicationApi$SearchApplications, `q_search_fields` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling ApplicationApi$SearchApplications, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling ApplicationApi$SearchApplications, `descending` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling ApplicationApi$SearchApplications, `_i` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ApplicationApi$SearchApplications, `start` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling ApplicationApi$SearchApplications, `_l` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ApplicationApi$SearchApplications, `limit` is not nullable")
      }

      if (!missing(`has_ads`) && is.null(`has_ads`)) {
        stop("Invalid value for `has_ads` when calling ApplicationApi$SearchApplications, `has_ads` is not nullable")
      }

      if (!missing(`public_notifications`) && is.null(`public_notifications`)) {
        stop("Invalid value for `public_notifications` when calling ApplicationApi$SearchApplications, `public_notifications` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling ApplicationApi$SearchApplications, `active_only` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["qSearchFields"]] <- `q_search_fields`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "HAS_ADVERTISEMENTS", "PUBLIC_NOTIFICATIONS", "PLACEMENTS", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY", "TITLE", "NAME", "ABOUT", "DESCRIPTION", "APPLICATION_ID", "APP_TYPE", "GAME_TYPE", "BUNDLE_ID", "SCORING_TYPE", "LANDING_PAGE_URL", "EULA_VERSION", "BUILD_VERSION", "API_VERSION"))) {
        stop("Invalid value for sort_field when calling ApplicationApi$SearchApplications. Must be [CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, HAS_ADVERTISEMENTS, PUBLIC_NOTIFICATIONS, PLACEMENTS, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY, TITLE, NAME, ABOUT, DESCRIPTION, APPLICATION_ID, APP_TYPE, GAME_TYPE, BUNDLE_ID, SCORING_TYPE, LANDING_PAGE_URL, EULA_VERSION, BUILD_VERSION, API_VERSION].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["hasAds"]] <- `has_ads`

      query_params[["publicNotifications"]] <- `public_notifications`

      query_params[["activeOnly"]] <- `active_only`

      local_var_url_path <- "/application/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ApplicationResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Application
    #'
    #' @param app_key The application key for updating an existing application
    #' @param app_name The name of the application
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param about (optional) The description of the application
    #' @param bundle_id (optional) The application bundle identifier (format - com.company.appName)
    #' @param app_icon_asset_id (optional) The application icon asset id
    #' @param app_logo_asset_id (optional) The application logo asset id
    #' @param facebook_app_id (optional) The Facebook application id
    #' @param facebook_app_secret (optional) The Facebook application secret
    #' @param google_api_key (optional) This is the either the 'server key' or 'browser key' generated from google to enable Google Cloud Messaging
    #' @param update_eula_date (optional) Determines whether to update the EULA date
    #' @param eula_version (optional) The EULA version
    #' @param landing_page_url (optional) The landing page URL
    #' @param show_in_activities (optional) Determines whether to show the application in the activity feed
    #' @param activity_description (optional) The description of the application in the activity feed
    #' @param invite_welcome_text (optional) The text to display on the invite page
    #' @param invite_page_url (optional) The url to the application invite page
    #' @param url_scheme (optional) The protocal the app uses to load the app via a browser
    #' @param platforms (optional) A json object with a list of supported platforms.  ```json {   \"platforms\": [     {       \"deviceId\": 1,       \"minimum\": 230,       \"maximum\": 421,       \"downloadUrl\": \"http://app.store.com/download\",       \"description\": \"description and version info\"     }   ] } ``` 
    #' @param download_urls (optional) Json formatted downloadUrls.  ```json {   \"ios\": \"the-url-to-app-store\",    \"android\": \"the-url-to-google-play\" } ```  
    #' @param category_ids (optional) List of categories to apply
    #' @param scoring_type (optional) The type of scoring this application will use {GAME_LEVEL, GAME_OBJECT (default value: "GAME_LEVEL")
    #' @param hint_cost (optional) The cost of hints (default value: 11)
    #' @param max_score (optional) The maximum score that will be possible (default value: 125)
    #' @param tickets_per_point (optional) The point-to-ticket conversion ratio (default value: 0.037)
    #' @param has_game_data (optional) Determines whether the application uses services to save custom game objects (default value: TRUE)
    #' @param public_notifications (optional) Public Notifications
    #' @param use_matching_algorithm (optional) Use Matching Algorithm
    #' @param global_tickets (optional) Determines whether earned tickets are applied across all applications (default value: FALSE)
    #' @param build_version (optional) The current build version of the application (default value: 1)
    #' @param api_version (optional) The current API version the application uses
    #' @param placement_name (optional) The name of the placement
    #' @param placement_description (optional) The description of the placement
    #' @param placement_size (optional) The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM (this is required if a placements is to be created)
    #' @param placement_height (optional) The height of a custom ad size
    #' @param placement_width (optional) The width of a custom ad size
    #' @param placement_refresh_interval (optional) The refresh interval in seconds
    #' @param create_object_store (optional) Generate a custom object store to use (default value: TRUE)
    #' @param public_content_approval (optional) Determine whether or not public content requires admin approval before it becomes public (default value: FALSE)
    #' @param production_mode (optional) Determines whether the application uses production or sandbox services (default value: FALSE)
    #' @param minimum_session_length (optional) Minimum Session Length
    #' @param session_gap_length (optional) Session Gap Length
    #' @param local_ads_enabled (optional) Local Ads Enabled (default value: FALSE)
    #' @param sqoot_api_key (optional) Sqoot Api Key
    #' @param trilat_processing_type (optional) Determines how to process trilateration data. Possible values include: DEFAULT, FINGERPRINT (default value: "FINGERPRINT")
    #' @param max_sample_size (optional) Determines what the maximum sample size during trilateration
    #' @param min_rssi (optional) Determines what the minimum acceptable RSSI value
    #' @param modules (optional) List modules allowed to be served by the server, possible values include: COMMON, MEDIA, OFFER, GAME, SOCIAL, CONSUMER_WEBSITE, ALL (default value: "ALL")
    #' @param authorized_count (optional) How many servers the license will support (default value: 1)
    #' @param authorized_servers (optional) The list of ip addresses of servers the license will support, leave null for any server
    #' @param default_timezone (optional) Sets the default timezone for the app (used for leaderboards and other time specific content)
    #' @param smtp_pass (optional) SMTP Pass
    #' @param meta_data (optional) The application meta data. Defined by the client
    #' @param placement_meta_data (optional) The ad placement meta data. Defined by the client
    #' @param ips_floor (optional) Create floor tables for Ips (default value: FALSE)
    #' @param enable_apns_badge (optional) Enables setting the APNS badge value in the payload (default value: TRUE)
    #' @param include_in_report (optional) Enables using the application in session reports (default value: TRUE)
    #' @param default_app_filter_id (optional) Sets the default filter to use (if none is passed in) for certain search APIs including Album Search.
    #' @param enable_welcome_email (optional) Enables whether the default welcome email will be sent for new app users
    #' @param apple_app_id (optional) The Apple Application ID
    #' @param apple_team_id (optional) The Apple Team ID
    #' @param apple_auth_key_id (optional) The Apple Auth Key ID
    #' @param apple_auth_key (optional) The Apple Auth Signin Key (p8) File
    #' @param apple_issuer_id (optional) The Apple Issuer ID
    #' @param app_store_key_id (optional) The Apple App Store Key ID
    #' @param app_store_key (optional) The Apple App Store Key (p8) File
    #' @param google_private_key_file (optional) This is the private key file for your Google service account.
    #' @param authorize_net_api_key (optional) Authorize Net Api Key
    #' @param authorize_net_transaction_key (optional) Authorize Net Transaction Key
    #' @param email_sender (optional) Email Sender
    #' @param smtp_user (optional) SMTP User
    #' @param smtp_host (optional) SMTP Host
    #' @param vatom_business_id (optional) Vatom Business Id
    #' @param vatom_rest_client_id (optional) Vatom REST Client Id
    #' @param vatom_rest_secret_key (optional) Vatom Secret Key
    #' @param twilio_account_sid (optional) Twilio Account SID
    #' @param twilio_auth_token (optional) Twilio Auth Token
    #' @param twilio_sender_phone_number (optional) Twilio Sender Phone Number
    #' @param open_ai_secret_key (optional) OpenAI Secret API Key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ApplicationResponse
    UpdateApplication = function(app_key, app_name, device_id = NULL, account_id = NULL, about = NULL, bundle_id = NULL, app_icon_asset_id = NULL, app_logo_asset_id = NULL, facebook_app_id = NULL, facebook_app_secret = NULL, google_api_key = NULL, update_eula_date = NULL, eula_version = NULL, landing_page_url = NULL, show_in_activities = NULL, activity_description = NULL, invite_welcome_text = NULL, invite_page_url = NULL, url_scheme = NULL, platforms = NULL, download_urls = NULL, category_ids = NULL, scoring_type = "GAME_LEVEL", hint_cost = 11, max_score = 125, tickets_per_point = 0.037, has_game_data = TRUE, public_notifications = NULL, use_matching_algorithm = NULL, global_tickets = FALSE, build_version = 1, api_version = NULL, placement_name = NULL, placement_description = NULL, placement_size = NULL, placement_height = NULL, placement_width = NULL, placement_refresh_interval = NULL, create_object_store = TRUE, public_content_approval = FALSE, production_mode = FALSE, minimum_session_length = NULL, session_gap_length = NULL, local_ads_enabled = FALSE, sqoot_api_key = NULL, trilat_processing_type = "FINGERPRINT", max_sample_size = NULL, min_rssi = NULL, modules = "ALL", authorized_count = 1, authorized_servers = NULL, default_timezone = NULL, smtp_pass = NULL, meta_data = NULL, placement_meta_data = NULL, ips_floor = FALSE, enable_apns_badge = TRUE, include_in_report = TRUE, default_app_filter_id = NULL, enable_welcome_email = NULL, apple_app_id = NULL, apple_team_id = NULL, apple_auth_key_id = NULL, apple_auth_key = NULL, apple_issuer_id = NULL, app_store_key_id = NULL, app_store_key = NULL, google_private_key_file = NULL, authorize_net_api_key = NULL, authorize_net_transaction_key = NULL, email_sender = NULL, smtp_user = NULL, smtp_host = NULL, vatom_business_id = NULL, vatom_rest_client_id = NULL, vatom_rest_secret_key = NULL, twilio_account_sid = NULL, twilio_auth_token = NULL, twilio_sender_phone_number = NULL, open_ai_secret_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateApplicationWithHttpInfo(app_key, app_name, device_id, account_id, about, bundle_id, app_icon_asset_id, app_logo_asset_id, facebook_app_id, facebook_app_secret, google_api_key, update_eula_date, eula_version, landing_page_url, show_in_activities, activity_description, invite_welcome_text, invite_page_url, url_scheme, platforms, download_urls, category_ids, scoring_type, hint_cost, max_score, tickets_per_point, has_game_data, public_notifications, use_matching_algorithm, global_tickets, build_version, api_version, placement_name, placement_description, placement_size, placement_height, placement_width, placement_refresh_interval, create_object_store, public_content_approval, production_mode, minimum_session_length, session_gap_length, local_ads_enabled, sqoot_api_key, trilat_processing_type, max_sample_size, min_rssi, modules, authorized_count, authorized_servers, default_timezone, smtp_pass, meta_data, placement_meta_data, ips_floor, enable_apns_badge, include_in_report, default_app_filter_id, enable_welcome_email, apple_app_id, apple_team_id, apple_auth_key_id, apple_auth_key, apple_issuer_id, app_store_key_id, app_store_key, google_private_key_file, authorize_net_api_key, authorize_net_transaction_key, email_sender, smtp_user, smtp_host, vatom_business_id, vatom_rest_client_id, vatom_rest_secret_key, twilio_account_sid, twilio_auth_token, twilio_sender_phone_number, open_ai_secret_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Application
    #'
    #' @param app_key The application key for updating an existing application
    #' @param app_name The name of the application
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param about (optional) The description of the application
    #' @param bundle_id (optional) The application bundle identifier (format - com.company.appName)
    #' @param app_icon_asset_id (optional) The application icon asset id
    #' @param app_logo_asset_id (optional) The application logo asset id
    #' @param facebook_app_id (optional) The Facebook application id
    #' @param facebook_app_secret (optional) The Facebook application secret
    #' @param google_api_key (optional) This is the either the 'server key' or 'browser key' generated from google to enable Google Cloud Messaging
    #' @param update_eula_date (optional) Determines whether to update the EULA date
    #' @param eula_version (optional) The EULA version
    #' @param landing_page_url (optional) The landing page URL
    #' @param show_in_activities (optional) Determines whether to show the application in the activity feed
    #' @param activity_description (optional) The description of the application in the activity feed
    #' @param invite_welcome_text (optional) The text to display on the invite page
    #' @param invite_page_url (optional) The url to the application invite page
    #' @param url_scheme (optional) The protocal the app uses to load the app via a browser
    #' @param platforms (optional) A json object with a list of supported platforms.  ```json {   \"platforms\": [     {       \"deviceId\": 1,       \"minimum\": 230,       \"maximum\": 421,       \"downloadUrl\": \"http://app.store.com/download\",       \"description\": \"description and version info\"     }   ] } ``` 
    #' @param download_urls (optional) Json formatted downloadUrls.  ```json {   \"ios\": \"the-url-to-app-store\",    \"android\": \"the-url-to-google-play\" } ```  
    #' @param category_ids (optional) List of categories to apply
    #' @param scoring_type (optional) The type of scoring this application will use {GAME_LEVEL, GAME_OBJECT (default value: "GAME_LEVEL")
    #' @param hint_cost (optional) The cost of hints (default value: 11)
    #' @param max_score (optional) The maximum score that will be possible (default value: 125)
    #' @param tickets_per_point (optional) The point-to-ticket conversion ratio (default value: 0.037)
    #' @param has_game_data (optional) Determines whether the application uses services to save custom game objects (default value: TRUE)
    #' @param public_notifications (optional) Public Notifications
    #' @param use_matching_algorithm (optional) Use Matching Algorithm
    #' @param global_tickets (optional) Determines whether earned tickets are applied across all applications (default value: FALSE)
    #' @param build_version (optional) The current build version of the application (default value: 1)
    #' @param api_version (optional) The current API version the application uses
    #' @param placement_name (optional) The name of the placement
    #' @param placement_description (optional) The description of the placement
    #' @param placement_size (optional) The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM (this is required if a placements is to be created)
    #' @param placement_height (optional) The height of a custom ad size
    #' @param placement_width (optional) The width of a custom ad size
    #' @param placement_refresh_interval (optional) The refresh interval in seconds
    #' @param create_object_store (optional) Generate a custom object store to use (default value: TRUE)
    #' @param public_content_approval (optional) Determine whether or not public content requires admin approval before it becomes public (default value: FALSE)
    #' @param production_mode (optional) Determines whether the application uses production or sandbox services (default value: FALSE)
    #' @param minimum_session_length (optional) Minimum Session Length
    #' @param session_gap_length (optional) Session Gap Length
    #' @param local_ads_enabled (optional) Local Ads Enabled (default value: FALSE)
    #' @param sqoot_api_key (optional) Sqoot Api Key
    #' @param trilat_processing_type (optional) Determines how to process trilateration data. Possible values include: DEFAULT, FINGERPRINT (default value: "FINGERPRINT")
    #' @param max_sample_size (optional) Determines what the maximum sample size during trilateration
    #' @param min_rssi (optional) Determines what the minimum acceptable RSSI value
    #' @param modules (optional) List modules allowed to be served by the server, possible values include: COMMON, MEDIA, OFFER, GAME, SOCIAL, CONSUMER_WEBSITE, ALL (default value: "ALL")
    #' @param authorized_count (optional) How many servers the license will support (default value: 1)
    #' @param authorized_servers (optional) The list of ip addresses of servers the license will support, leave null for any server
    #' @param default_timezone (optional) Sets the default timezone for the app (used for leaderboards and other time specific content)
    #' @param smtp_pass (optional) SMTP Pass
    #' @param meta_data (optional) The application meta data. Defined by the client
    #' @param placement_meta_data (optional) The ad placement meta data. Defined by the client
    #' @param ips_floor (optional) Create floor tables for Ips (default value: FALSE)
    #' @param enable_apns_badge (optional) Enables setting the APNS badge value in the payload (default value: TRUE)
    #' @param include_in_report (optional) Enables using the application in session reports (default value: TRUE)
    #' @param default_app_filter_id (optional) Sets the default filter to use (if none is passed in) for certain search APIs including Album Search.
    #' @param enable_welcome_email (optional) Enables whether the default welcome email will be sent for new app users
    #' @param apple_app_id (optional) The Apple Application ID
    #' @param apple_team_id (optional) The Apple Team ID
    #' @param apple_auth_key_id (optional) The Apple Auth Key ID
    #' @param apple_auth_key (optional) The Apple Auth Signin Key (p8) File
    #' @param apple_issuer_id (optional) The Apple Issuer ID
    #' @param app_store_key_id (optional) The Apple App Store Key ID
    #' @param app_store_key (optional) The Apple App Store Key (p8) File
    #' @param google_private_key_file (optional) This is the private key file for your Google service account.
    #' @param authorize_net_api_key (optional) Authorize Net Api Key
    #' @param authorize_net_transaction_key (optional) Authorize Net Transaction Key
    #' @param email_sender (optional) Email Sender
    #' @param smtp_user (optional) SMTP User
    #' @param smtp_host (optional) SMTP Host
    #' @param vatom_business_id (optional) Vatom Business Id
    #' @param vatom_rest_client_id (optional) Vatom REST Client Id
    #' @param vatom_rest_secret_key (optional) Vatom Secret Key
    #' @param twilio_account_sid (optional) Twilio Account SID
    #' @param twilio_auth_token (optional) Twilio Auth Token
    #' @param twilio_sender_phone_number (optional) Twilio Sender Phone Number
    #' @param open_ai_secret_key (optional) OpenAI Secret API Key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ApplicationResponse) with additional information such as HTTP status code, headers
    UpdateApplicationWithHttpInfo = function(app_key, app_name, device_id = NULL, account_id = NULL, about = NULL, bundle_id = NULL, app_icon_asset_id = NULL, app_logo_asset_id = NULL, facebook_app_id = NULL, facebook_app_secret = NULL, google_api_key = NULL, update_eula_date = NULL, eula_version = NULL, landing_page_url = NULL, show_in_activities = NULL, activity_description = NULL, invite_welcome_text = NULL, invite_page_url = NULL, url_scheme = NULL, platforms = NULL, download_urls = NULL, category_ids = NULL, scoring_type = "GAME_LEVEL", hint_cost = 11, max_score = 125, tickets_per_point = 0.037, has_game_data = TRUE, public_notifications = NULL, use_matching_algorithm = NULL, global_tickets = FALSE, build_version = 1, api_version = NULL, placement_name = NULL, placement_description = NULL, placement_size = NULL, placement_height = NULL, placement_width = NULL, placement_refresh_interval = NULL, create_object_store = TRUE, public_content_approval = FALSE, production_mode = FALSE, minimum_session_length = NULL, session_gap_length = NULL, local_ads_enabled = FALSE, sqoot_api_key = NULL, trilat_processing_type = "FINGERPRINT", max_sample_size = NULL, min_rssi = NULL, modules = "ALL", authorized_count = 1, authorized_servers = NULL, default_timezone = NULL, smtp_pass = NULL, meta_data = NULL, placement_meta_data = NULL, ips_floor = FALSE, enable_apns_badge = TRUE, include_in_report = TRUE, default_app_filter_id = NULL, enable_welcome_email = NULL, apple_app_id = NULL, apple_team_id = NULL, apple_auth_key_id = NULL, apple_auth_key = NULL, apple_issuer_id = NULL, app_store_key_id = NULL, app_store_key = NULL, google_private_key_file = NULL, authorize_net_api_key = NULL, authorize_net_transaction_key = NULL, email_sender = NULL, smtp_user = NULL, smtp_host = NULL, vatom_business_id = NULL, vatom_rest_client_id = NULL, vatom_rest_secret_key = NULL, twilio_account_sid = NULL, twilio_auth_token = NULL, twilio_sender_phone_number = NULL, open_ai_secret_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`app_name`)) {
        stop("Missing required parameter `app_name`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationApi$UpdateApplication, `app_key` is not nullable")
      }

      if (!missing(`app_name`) && is.null(`app_name`)) {
        stop("Invalid value for `app_name` when calling ApplicationApi$UpdateApplication, `app_name` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ApplicationApi$UpdateApplication, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$UpdateApplication, `account_id` is not nullable")
      }

      if (!missing(`about`) && is.null(`about`)) {
        stop("Invalid value for `about` when calling ApplicationApi$UpdateApplication, `about` is not nullable")
      }

      if (!missing(`bundle_id`) && is.null(`bundle_id`)) {
        stop("Invalid value for `bundle_id` when calling ApplicationApi$UpdateApplication, `bundle_id` is not nullable")
      }

      if (!missing(`app_icon_asset_id`) && is.null(`app_icon_asset_id`)) {
        stop("Invalid value for `app_icon_asset_id` when calling ApplicationApi$UpdateApplication, `app_icon_asset_id` is not nullable")
      }

      if (!missing(`app_logo_asset_id`) && is.null(`app_logo_asset_id`)) {
        stop("Invalid value for `app_logo_asset_id` when calling ApplicationApi$UpdateApplication, `app_logo_asset_id` is not nullable")
      }

      if (!missing(`facebook_app_id`) && is.null(`facebook_app_id`)) {
        stop("Invalid value for `facebook_app_id` when calling ApplicationApi$UpdateApplication, `facebook_app_id` is not nullable")
      }

      if (!missing(`facebook_app_secret`) && is.null(`facebook_app_secret`)) {
        stop("Invalid value for `facebook_app_secret` when calling ApplicationApi$UpdateApplication, `facebook_app_secret` is not nullable")
      }

      if (!missing(`google_api_key`) && is.null(`google_api_key`)) {
        stop("Invalid value for `google_api_key` when calling ApplicationApi$UpdateApplication, `google_api_key` is not nullable")
      }

      if (!missing(`update_eula_date`) && is.null(`update_eula_date`)) {
        stop("Invalid value for `update_eula_date` when calling ApplicationApi$UpdateApplication, `update_eula_date` is not nullable")
      }

      if (!missing(`eula_version`) && is.null(`eula_version`)) {
        stop("Invalid value for `eula_version` when calling ApplicationApi$UpdateApplication, `eula_version` is not nullable")
      }

      if (!missing(`landing_page_url`) && is.null(`landing_page_url`)) {
        stop("Invalid value for `landing_page_url` when calling ApplicationApi$UpdateApplication, `landing_page_url` is not nullable")
      }

      if (!missing(`show_in_activities`) && is.null(`show_in_activities`)) {
        stop("Invalid value for `show_in_activities` when calling ApplicationApi$UpdateApplication, `show_in_activities` is not nullable")
      }

      if (!missing(`activity_description`) && is.null(`activity_description`)) {
        stop("Invalid value for `activity_description` when calling ApplicationApi$UpdateApplication, `activity_description` is not nullable")
      }

      if (!missing(`invite_welcome_text`) && is.null(`invite_welcome_text`)) {
        stop("Invalid value for `invite_welcome_text` when calling ApplicationApi$UpdateApplication, `invite_welcome_text` is not nullable")
      }

      if (!missing(`invite_page_url`) && is.null(`invite_page_url`)) {
        stop("Invalid value for `invite_page_url` when calling ApplicationApi$UpdateApplication, `invite_page_url` is not nullable")
      }

      if (!missing(`url_scheme`) && is.null(`url_scheme`)) {
        stop("Invalid value for `url_scheme` when calling ApplicationApi$UpdateApplication, `url_scheme` is not nullable")
      }

      if (!missing(`platforms`) && is.null(`platforms`)) {
        stop("Invalid value for `platforms` when calling ApplicationApi$UpdateApplication, `platforms` is not nullable")
      }

      if (!missing(`download_urls`) && is.null(`download_urls`)) {
        stop("Invalid value for `download_urls` when calling ApplicationApi$UpdateApplication, `download_urls` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling ApplicationApi$UpdateApplication, `category_ids` is not nullable")
      }

      if (!missing(`scoring_type`) && is.null(`scoring_type`)) {
        stop("Invalid value for `scoring_type` when calling ApplicationApi$UpdateApplication, `scoring_type` is not nullable")
      }

      if (!missing(`hint_cost`) && is.null(`hint_cost`)) {
        stop("Invalid value for `hint_cost` when calling ApplicationApi$UpdateApplication, `hint_cost` is not nullable")
      }

      if (!missing(`max_score`) && is.null(`max_score`)) {
        stop("Invalid value for `max_score` when calling ApplicationApi$UpdateApplication, `max_score` is not nullable")
      }

      if (!missing(`tickets_per_point`) && is.null(`tickets_per_point`)) {
        stop("Invalid value for `tickets_per_point` when calling ApplicationApi$UpdateApplication, `tickets_per_point` is not nullable")
      }

      if (!missing(`has_game_data`) && is.null(`has_game_data`)) {
        stop("Invalid value for `has_game_data` when calling ApplicationApi$UpdateApplication, `has_game_data` is not nullable")
      }

      if (!missing(`public_notifications`) && is.null(`public_notifications`)) {
        stop("Invalid value for `public_notifications` when calling ApplicationApi$UpdateApplication, `public_notifications` is not nullable")
      }

      if (!missing(`use_matching_algorithm`) && is.null(`use_matching_algorithm`)) {
        stop("Invalid value for `use_matching_algorithm` when calling ApplicationApi$UpdateApplication, `use_matching_algorithm` is not nullable")
      }

      if (!missing(`global_tickets`) && is.null(`global_tickets`)) {
        stop("Invalid value for `global_tickets` when calling ApplicationApi$UpdateApplication, `global_tickets` is not nullable")
      }

      if (!missing(`build_version`) && is.null(`build_version`)) {
        stop("Invalid value for `build_version` when calling ApplicationApi$UpdateApplication, `build_version` is not nullable")
      }

      if (!missing(`api_version`) && is.null(`api_version`)) {
        stop("Invalid value for `api_version` when calling ApplicationApi$UpdateApplication, `api_version` is not nullable")
      }

      if (!missing(`placement_name`) && is.null(`placement_name`)) {
        stop("Invalid value for `placement_name` when calling ApplicationApi$UpdateApplication, `placement_name` is not nullable")
      }

      if (!missing(`placement_description`) && is.null(`placement_description`)) {
        stop("Invalid value for `placement_description` when calling ApplicationApi$UpdateApplication, `placement_description` is not nullable")
      }

      if (!missing(`placement_size`) && is.null(`placement_size`)) {
        stop("Invalid value for `placement_size` when calling ApplicationApi$UpdateApplication, `placement_size` is not nullable")
      }

      if (!missing(`placement_height`) && is.null(`placement_height`)) {
        stop("Invalid value for `placement_height` when calling ApplicationApi$UpdateApplication, `placement_height` is not nullable")
      }

      if (!missing(`placement_width`) && is.null(`placement_width`)) {
        stop("Invalid value for `placement_width` when calling ApplicationApi$UpdateApplication, `placement_width` is not nullable")
      }

      if (!missing(`placement_refresh_interval`) && is.null(`placement_refresh_interval`)) {
        stop("Invalid value for `placement_refresh_interval` when calling ApplicationApi$UpdateApplication, `placement_refresh_interval` is not nullable")
      }

      if (!missing(`create_object_store`) && is.null(`create_object_store`)) {
        stop("Invalid value for `create_object_store` when calling ApplicationApi$UpdateApplication, `create_object_store` is not nullable")
      }

      if (!missing(`public_content_approval`) && is.null(`public_content_approval`)) {
        stop("Invalid value for `public_content_approval` when calling ApplicationApi$UpdateApplication, `public_content_approval` is not nullable")
      }

      if (!missing(`production_mode`) && is.null(`production_mode`)) {
        stop("Invalid value for `production_mode` when calling ApplicationApi$UpdateApplication, `production_mode` is not nullable")
      }

      if (!missing(`minimum_session_length`) && is.null(`minimum_session_length`)) {
        stop("Invalid value for `minimum_session_length` when calling ApplicationApi$UpdateApplication, `minimum_session_length` is not nullable")
      }

      if (!missing(`session_gap_length`) && is.null(`session_gap_length`)) {
        stop("Invalid value for `session_gap_length` when calling ApplicationApi$UpdateApplication, `session_gap_length` is not nullable")
      }

      if (!missing(`local_ads_enabled`) && is.null(`local_ads_enabled`)) {
        stop("Invalid value for `local_ads_enabled` when calling ApplicationApi$UpdateApplication, `local_ads_enabled` is not nullable")
      }

      if (!missing(`sqoot_api_key`) && is.null(`sqoot_api_key`)) {
        stop("Invalid value for `sqoot_api_key` when calling ApplicationApi$UpdateApplication, `sqoot_api_key` is not nullable")
      }

      if (!missing(`trilat_processing_type`) && is.null(`trilat_processing_type`)) {
        stop("Invalid value for `trilat_processing_type` when calling ApplicationApi$UpdateApplication, `trilat_processing_type` is not nullable")
      }

      if (!missing(`max_sample_size`) && is.null(`max_sample_size`)) {
        stop("Invalid value for `max_sample_size` when calling ApplicationApi$UpdateApplication, `max_sample_size` is not nullable")
      }

      if (!missing(`min_rssi`) && is.null(`min_rssi`)) {
        stop("Invalid value for `min_rssi` when calling ApplicationApi$UpdateApplication, `min_rssi` is not nullable")
      }

      if (!missing(`modules`) && is.null(`modules`)) {
        stop("Invalid value for `modules` when calling ApplicationApi$UpdateApplication, `modules` is not nullable")
      }

      if (!missing(`authorized_count`) && is.null(`authorized_count`)) {
        stop("Invalid value for `authorized_count` when calling ApplicationApi$UpdateApplication, `authorized_count` is not nullable")
      }

      if (!missing(`authorized_servers`) && is.null(`authorized_servers`)) {
        stop("Invalid value for `authorized_servers` when calling ApplicationApi$UpdateApplication, `authorized_servers` is not nullable")
      }

      if (!missing(`default_timezone`) && is.null(`default_timezone`)) {
        stop("Invalid value for `default_timezone` when calling ApplicationApi$UpdateApplication, `default_timezone` is not nullable")
      }

      if (!missing(`smtp_pass`) && is.null(`smtp_pass`)) {
        stop("Invalid value for `smtp_pass` when calling ApplicationApi$UpdateApplication, `smtp_pass` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling ApplicationApi$UpdateApplication, `meta_data` is not nullable")
      }

      if (!missing(`placement_meta_data`) && is.null(`placement_meta_data`)) {
        stop("Invalid value for `placement_meta_data` when calling ApplicationApi$UpdateApplication, `placement_meta_data` is not nullable")
      }

      if (!missing(`ips_floor`) && is.null(`ips_floor`)) {
        stop("Invalid value for `ips_floor` when calling ApplicationApi$UpdateApplication, `ips_floor` is not nullable")
      }

      if (!missing(`enable_apns_badge`) && is.null(`enable_apns_badge`)) {
        stop("Invalid value for `enable_apns_badge` when calling ApplicationApi$UpdateApplication, `enable_apns_badge` is not nullable")
      }

      if (!missing(`include_in_report`) && is.null(`include_in_report`)) {
        stop("Invalid value for `include_in_report` when calling ApplicationApi$UpdateApplication, `include_in_report` is not nullable")
      }

      if (!missing(`default_app_filter_id`) && is.null(`default_app_filter_id`)) {
        stop("Invalid value for `default_app_filter_id` when calling ApplicationApi$UpdateApplication, `default_app_filter_id` is not nullable")
      }

      if (!missing(`enable_welcome_email`) && is.null(`enable_welcome_email`)) {
        stop("Invalid value for `enable_welcome_email` when calling ApplicationApi$UpdateApplication, `enable_welcome_email` is not nullable")
      }

      if (!missing(`apple_app_id`) && is.null(`apple_app_id`)) {
        stop("Invalid value for `apple_app_id` when calling ApplicationApi$UpdateApplication, `apple_app_id` is not nullable")
      }

      if (!missing(`apple_team_id`) && is.null(`apple_team_id`)) {
        stop("Invalid value for `apple_team_id` when calling ApplicationApi$UpdateApplication, `apple_team_id` is not nullable")
      }

      if (!missing(`apple_auth_key_id`) && is.null(`apple_auth_key_id`)) {
        stop("Invalid value for `apple_auth_key_id` when calling ApplicationApi$UpdateApplication, `apple_auth_key_id` is not nullable")
      }

      if (!missing(`apple_auth_key`) && is.null(`apple_auth_key`)) {
        stop("Invalid value for `apple_auth_key` when calling ApplicationApi$UpdateApplication, `apple_auth_key` is not nullable")
      }

      if (!missing(`apple_issuer_id`) && is.null(`apple_issuer_id`)) {
        stop("Invalid value for `apple_issuer_id` when calling ApplicationApi$UpdateApplication, `apple_issuer_id` is not nullable")
      }

      if (!missing(`app_store_key_id`) && is.null(`app_store_key_id`)) {
        stop("Invalid value for `app_store_key_id` when calling ApplicationApi$UpdateApplication, `app_store_key_id` is not nullable")
      }

      if (!missing(`app_store_key`) && is.null(`app_store_key`)) {
        stop("Invalid value for `app_store_key` when calling ApplicationApi$UpdateApplication, `app_store_key` is not nullable")
      }

      if (!missing(`google_private_key_file`) && is.null(`google_private_key_file`)) {
        stop("Invalid value for `google_private_key_file` when calling ApplicationApi$UpdateApplication, `google_private_key_file` is not nullable")
      }

      if (!missing(`authorize_net_api_key`) && is.null(`authorize_net_api_key`)) {
        stop("Invalid value for `authorize_net_api_key` when calling ApplicationApi$UpdateApplication, `authorize_net_api_key` is not nullable")
      }

      if (!missing(`authorize_net_transaction_key`) && is.null(`authorize_net_transaction_key`)) {
        stop("Invalid value for `authorize_net_transaction_key` when calling ApplicationApi$UpdateApplication, `authorize_net_transaction_key` is not nullable")
      }

      if (!missing(`email_sender`) && is.null(`email_sender`)) {
        stop("Invalid value for `email_sender` when calling ApplicationApi$UpdateApplication, `email_sender` is not nullable")
      }

      if (!missing(`smtp_user`) && is.null(`smtp_user`)) {
        stop("Invalid value for `smtp_user` when calling ApplicationApi$UpdateApplication, `smtp_user` is not nullable")
      }

      if (!missing(`smtp_host`) && is.null(`smtp_host`)) {
        stop("Invalid value for `smtp_host` when calling ApplicationApi$UpdateApplication, `smtp_host` is not nullable")
      }

      if (!missing(`vatom_business_id`) && is.null(`vatom_business_id`)) {
        stop("Invalid value for `vatom_business_id` when calling ApplicationApi$UpdateApplication, `vatom_business_id` is not nullable")
      }

      if (!missing(`vatom_rest_client_id`) && is.null(`vatom_rest_client_id`)) {
        stop("Invalid value for `vatom_rest_client_id` when calling ApplicationApi$UpdateApplication, `vatom_rest_client_id` is not nullable")
      }

      if (!missing(`vatom_rest_secret_key`) && is.null(`vatom_rest_secret_key`)) {
        stop("Invalid value for `vatom_rest_secret_key` when calling ApplicationApi$UpdateApplication, `vatom_rest_secret_key` is not nullable")
      }

      if (!missing(`twilio_account_sid`) && is.null(`twilio_account_sid`)) {
        stop("Invalid value for `twilio_account_sid` when calling ApplicationApi$UpdateApplication, `twilio_account_sid` is not nullable")
      }

      if (!missing(`twilio_auth_token`) && is.null(`twilio_auth_token`)) {
        stop("Invalid value for `twilio_auth_token` when calling ApplicationApi$UpdateApplication, `twilio_auth_token` is not nullable")
      }

      if (!missing(`twilio_sender_phone_number`) && is.null(`twilio_sender_phone_number`)) {
        stop("Invalid value for `twilio_sender_phone_number` when calling ApplicationApi$UpdateApplication, `twilio_sender_phone_number` is not nullable")
      }

      if (!missing(`open_ai_secret_key`) && is.null(`open_ai_secret_key`)) {
        stop("Invalid value for `open_ai_secret_key` when calling ApplicationApi$UpdateApplication, `open_ai_secret_key` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["appName"]] <- `app_name`

      query_params[["about"]] <- `about`

      query_params[["bundleId"]] <- `bundle_id`

      query_params[["appIconAssetId"]] <- `app_icon_asset_id`

      query_params[["appLogoAssetId"]] <- `app_logo_asset_id`

      query_params[["facebookAppId"]] <- `facebook_app_id`

      query_params[["facebookAppSecret"]] <- `facebook_app_secret`

      query_params[["googleApiKey"]] <- `google_api_key`

      query_params[["updateEULADate"]] <- `update_eula_date`

      query_params[["eulaVersion"]] <- `eula_version`

      query_params[["landingPageUrl"]] <- `landing_page_url`

      query_params[["showInActivities"]] <- `show_in_activities`

      query_params[["activityDescription"]] <- `activity_description`

      query_params[["inviteWelcomeText"]] <- `invite_welcome_text`

      query_params[["invitePageUrl"]] <- `invite_page_url`

      query_params[["urlScheme"]] <- `url_scheme`

      query_params[["platforms"]] <- `platforms`

      query_params[["downloadUrls"]] <- `download_urls`

      query_params[["categoryIds"]] <- `category_ids`

      if (!is.null(`scoring_type`) && !(`scoring_type` %in% c("GAME_LEVEL", "GAME_OBJECT"))) {
        stop("Invalid value for scoring_type when calling ApplicationApi$UpdateApplication. Must be [GAME_LEVEL, GAME_OBJECT].")
      }
      query_params[["scoringType"]] <- `scoring_type`

      query_params[["hintCost"]] <- `hint_cost`

      query_params[["maxScore"]] <- `max_score`

      query_params[["ticketsPerPoint"]] <- `tickets_per_point`

      query_params[["hasGameData"]] <- `has_game_data`

      query_params[["publicNotifications"]] <- `public_notifications`

      query_params[["useMatchingAlgorithm"]] <- `use_matching_algorithm`

      query_params[["globalTickets"]] <- `global_tickets`

      query_params[["buildVersion"]] <- `build_version`

      query_params[["apiVersion"]] <- `api_version`

      query_params[["placementName"]] <- `placement_name`

      query_params[["placementDescription"]] <- `placement_description`

      if (!is.null(`placement_size`) && !(`placement_size` %in% c("CONFIG", "BANNER", "LEADERBOARD", "SKYSCRAPER", "VIDEO", "ZIP", "INTERSTITIAL", "CUSTOM1", "CUSTOM2", "CUSTOM3", "CUSTOM4", "CUSTOM5", "CUSTOM6", "CUSTOM7", "CUSTOM8", "CUSTOM9", "CUSTOM10"))) {
        stop("Invalid value for placement_size when calling ApplicationApi$UpdateApplication. Must be [CONFIG, BANNER, LEADERBOARD, SKYSCRAPER, VIDEO, ZIP, INTERSTITIAL, CUSTOM1, CUSTOM2, CUSTOM3, CUSTOM4, CUSTOM5, CUSTOM6, CUSTOM7, CUSTOM8, CUSTOM9, CUSTOM10].")
      }
      query_params[["placementSize"]] <- `placement_size`

      query_params[["placementHeight"]] <- `placement_height`

      query_params[["placementWidth"]] <- `placement_width`

      query_params[["placementRefreshInterval"]] <- `placement_refresh_interval`

      query_params[["createObjectStore"]] <- `create_object_store`

      query_params[["publicContentApproval"]] <- `public_content_approval`

      query_params[["productionMode"]] <- `production_mode`

      query_params[["minimumSessionLength"]] <- `minimum_session_length`

      query_params[["sessionGapLength"]] <- `session_gap_length`

      query_params[["localAdsEnabled"]] <- `local_ads_enabled`

      query_params[["sqootApiKey"]] <- `sqoot_api_key`

      if (!is.null(`trilat_processing_type`) && !(`trilat_processing_type` %in% c("DEFAULT", "FINGERPRINT", "FINGERPRINT_V2"))) {
        stop("Invalid value for trilat_processing_type when calling ApplicationApi$UpdateApplication. Must be [DEFAULT, FINGERPRINT, FINGERPRINT_V2].")
      }
      query_params[["trilatProcessingType"]] <- `trilat_processing_type`

      query_params[["maxSampleSize"]] <- `max_sample_size`

      query_params[["minRSSI"]] <- `min_rssi`

      query_params[["modules"]] <- `modules`

      query_params[["authorizedCount"]] <- `authorized_count`

      query_params[["authorizedServers"]] <- `authorized_servers`

      query_params[["defaultTimezone"]] <- `default_timezone`

      query_params[["smtpPass"]] <- `smtp_pass`

      query_params[["metaData"]] <- `meta_data`

      query_params[["placementMetaData"]] <- `placement_meta_data`

      query_params[["ipsFloor"]] <- `ips_floor`

      query_params[["enableAPNSBadge"]] <- `enable_apns_badge`

      query_params[["includeInReport"]] <- `include_in_report`

      query_params[["defaultAppFilterId"]] <- `default_app_filter_id`

      query_params[["enableWelcomeEmail"]] <- `enable_welcome_email`

      query_params[["appleAppId"]] <- `apple_app_id`

      query_params[["appleTeamId"]] <- `apple_team_id`

      query_params[["appleAuthKeyId"]] <- `apple_auth_key_id`

      query_params[["appleAuthKey"]] <- `apple_auth_key`

      query_params[["appleIssuerId"]] <- `apple_issuer_id`

      query_params[["appStoreKeyId"]] <- `app_store_key_id`

      query_params[["appStoreKey"]] <- `app_store_key`

      query_params[["googlePrivateKeyFile"]] <- `google_private_key_file`

      query_params[["authorizeNetApiKey"]] <- `authorize_net_api_key`

      query_params[["authorizeNetTransactionKey"]] <- `authorize_net_transaction_key`

      query_params[["emailSender"]] <- `email_sender`

      query_params[["smtpUser"]] <- `smtp_user`

      query_params[["smtpHost"]] <- `smtp_host`

      query_params[["vatomBusinessId"]] <- `vatom_business_id`

      query_params[["vatomRestClientId"]] <- `vatom_rest_client_id`

      query_params[["vatomRestSecretKey"]] <- `vatom_rest_secret_key`

      query_params[["twilioAccountSID"]] <- `twilio_account_sid`

      query_params[["twilioAuthToken"]] <- `twilio_auth_token`

      query_params[["twilioSenderPhoneNumber"]] <- `twilio_sender_phone_number`

      query_params[["openAISecretKey"]] <- `open_ai_secret_key`

      local_var_url_path <- "/application/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ApplicationResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Change Appliation Status
    #'
    #' @param account_id The account used to perform the delete, must have rights to edit the application.
    #' @param app_key The key of the application to be deleted
    #' @param active If true then set to active, false otherwise
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UpdateApplicationActive = function(account_id, app_key, active, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateApplicationActiveWithHttpInfo(account_id, app_key, active, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Change Appliation Status
    #'
    #' @param account_id The account used to perform the delete, must have rights to edit the application.
    #' @param app_key The key of the application to be deleted
    #' @param active If true then set to active, false otherwise
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UpdateApplicationActiveWithHttpInfo = function(account_id, app_key, active, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`active`)) {
        stop("Missing required parameter `active`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$UpdateApplicationActive, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationApi$UpdateApplicationActive, `app_key` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling ApplicationApi$UpdateApplicationActive, `active` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["active"]] <- `active`

      local_var_url_path <- "/application/active"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Ad Placement
    #'
    #' @param placement_id The id of the placement to update, the user must have rights to the application the ad placement is for
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param name (optional) The name of the placement
    #' @param description (optional) The description of the placement
    #' @param size (optional) The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM
    #' @param height (optional) The height of a custom ad size
    #' @param width (optional) The width of a custom ad size
    #' @param refresh_interval (optional) The refresh interval in seconds
    #' @param default_image_id (optional) Default Image Id
    #' @param active (optional) Active
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PlacementResponse
    UpdateApplicationPlacement = function(placement_id, device_id = NULL, account_id = NULL, name = NULL, description = NULL, size = NULL, height = NULL, width = NULL, refresh_interval = NULL, default_image_id = NULL, active = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateApplicationPlacementWithHttpInfo(placement_id, device_id, account_id, name, description, size, height, width, refresh_interval, default_image_id, active, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Ad Placement
    #'
    #' @param placement_id The id of the placement to update, the user must have rights to the application the ad placement is for
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param name (optional) The name of the placement
    #' @param description (optional) The description of the placement
    #' @param size (optional) The ad placement size {BANNER, LEADERBOARD, SKYSCRAPER, INTERSTITIAL, CUSTOM
    #' @param height (optional) The height of a custom ad size
    #' @param width (optional) The width of a custom ad size
    #' @param refresh_interval (optional) The refresh interval in seconds
    #' @param default_image_id (optional) Default Image Id
    #' @param active (optional) Active
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PlacementResponse) with additional information such as HTTP status code, headers
    UpdateApplicationPlacementWithHttpInfo = function(placement_id, device_id = NULL, account_id = NULL, name = NULL, description = NULL, size = NULL, height = NULL, width = NULL, refresh_interval = NULL, default_image_id = NULL, active = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`placement_id`)) {
        stop("Missing required parameter `placement_id`.")
      }

      if (!missing(`placement_id`) && is.null(`placement_id`)) {
        stop("Invalid value for `placement_id` when calling ApplicationApi$UpdateApplicationPlacement, `placement_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ApplicationApi$UpdateApplicationPlacement, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$UpdateApplicationPlacement, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling ApplicationApi$UpdateApplicationPlacement, `name` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling ApplicationApi$UpdateApplicationPlacement, `description` is not nullable")
      }

      if (!missing(`size`) && is.null(`size`)) {
        stop("Invalid value for `size` when calling ApplicationApi$UpdateApplicationPlacement, `size` is not nullable")
      }

      if (!missing(`height`) && is.null(`height`)) {
        stop("Invalid value for `height` when calling ApplicationApi$UpdateApplicationPlacement, `height` is not nullable")
      }

      if (!missing(`width`) && is.null(`width`)) {
        stop("Invalid value for `width` when calling ApplicationApi$UpdateApplicationPlacement, `width` is not nullable")
      }

      if (!missing(`refresh_interval`) && is.null(`refresh_interval`)) {
        stop("Invalid value for `refresh_interval` when calling ApplicationApi$UpdateApplicationPlacement, `refresh_interval` is not nullable")
      }

      if (!missing(`default_image_id`) && is.null(`default_image_id`)) {
        stop("Invalid value for `default_image_id` when calling ApplicationApi$UpdateApplicationPlacement, `default_image_id` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling ApplicationApi$UpdateApplicationPlacement, `active` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["placementId"]] <- `placement_id`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      if (!is.null(`size`) && !(`size` %in% c("CONFIG", "BANNER", "LEADERBOARD", "SKYSCRAPER", "VIDEO", "ZIP", "INTERSTITIAL", "CUSTOM1", "CUSTOM2", "CUSTOM3", "CUSTOM4", "CUSTOM5", "CUSTOM6", "CUSTOM7", "CUSTOM8", "CUSTOM9", "CUSTOM10"))) {
        stop("Invalid value for size when calling ApplicationApi$UpdateApplicationPlacement. Must be [CONFIG, BANNER, LEADERBOARD, SKYSCRAPER, VIDEO, ZIP, INTERSTITIAL, CUSTOM1, CUSTOM2, CUSTOM3, CUSTOM4, CUSTOM5, CUSTOM6, CUSTOM7, CUSTOM8, CUSTOM9, CUSTOM10].")
      }
      query_params[["size"]] <- `size`

      query_params[["height"]] <- `height`

      query_params[["width"]] <- `width`

      query_params[["refreshInterval"]] <- `refresh_interval`

      query_params[["defaultImageId"]] <- `default_image_id`

      query_params[["active"]] <- `active`

      local_var_url_path <- "/application/placement/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PlacementResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Application Certificate
    #'
    #' @param app_key The key of the application
    #' @param device_id (optional) Device Id
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the application.
    #' @param certificate (optional) Certificate
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UploadApplicationCertificate = function(app_key, device_id = NULL, account_id = NULL, certificate = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UploadApplicationCertificateWithHttpInfo(app_key, device_id, account_id, certificate, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Application Certificate
    #'
    #' @param app_key The key of the application
    #' @param device_id (optional) Device Id
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the application.
    #' @param certificate (optional) Certificate
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UploadApplicationCertificateWithHttpInfo = function(app_key, device_id = NULL, account_id = NULL, certificate = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ApplicationApi$UploadApplicationCertificate, `app_key` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ApplicationApi$UploadApplicationCertificate, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ApplicationApi$UploadApplicationCertificate, `account_id` is not nullable")
      }

      if (!missing(`certificate`) && is.null(`certificate`)) {
        stop("Invalid value for `certificate` when calling ApplicationApi$UploadApplicationCertificate, `certificate` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["certificate"]] <- `certificate`

      local_var_url_path <- "/application/certificate/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
