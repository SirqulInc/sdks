#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Offer operations
#' @description OfferApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  BatchUpdateOfferLocations  ####################
#'
#' library(openapi)
#' var_data <- "data_example" # character | JSON string in the following format: ```json [{   \"offerLocationId\": 1705,   \"latitude\": 54.0,   \"longitude\": -122.0,   \"altitude\": 1.0,   \"locationDetail\": \"floor 1\",   \"locationDescription\": \"behind the Coke sign\" }, {   \"offerLocationId\": 1704,   \"latitude\": 54.1,   \"longitude\": -122.1 }] ``` 
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#'
#' #Update Offer Locations
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BatchUpdateOfferLocations(var_data, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$BatchUpdateOfferLocations(var_data, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  CreateOffer  ####################
#'
#' library(openapi)
#' var_include_offer_locations <- "include_offer_locations_example" # character | If true return all the offer locations associated with the offer
#' var_title <- "title_example" # character | The title (255 char limit)
#' var_barcode_type <- "barcode_type_example" # character | The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA}
#' var_no_expiration <- "no_expiration_example" # character | Overrides the expiration date so that the offer does not expire
#' var_available_limit <- 56 # integer | The limit of how many times the offer can be used by consumers
#' var_available_limit_per_user <- 56 # integer | The limit of how many times a user can used the same offer
#' var_added_limit <- 56 # integer | The limit of how many times the offer can be added to consumer wallets
#' var_view_limit <- 56 # integer | The limit of how many times the offer can be viewed
#' var_max_prints <- 56 # integer | The maximum number of times the offer can be printed
#' var_ticket_price <- 56 # integer | The cost of the offer in tickets
#' var_full_price <- 3.4 # numeric | The retail/full price cost of the offer in real currency
#' var_discount_price <- 3.4 # numeric | The cost of the offer at a discounted price (what the consumer pays)
#' var_offer_type <- "offer_type_example" # character | The offer type {VOUCHER, COUPON, PRODUCT, EVENT, MEDIA, DEVICE}
#' var_special_offer_type <- "special_offer_type_example" # character | The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
#' var_offer_visibility <- "offer_visibility_example" # character | The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE}
#' var_active <- "active_example" # character | Sets the active flag
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id (deviceId or accountId required) (Optional)
#' var_tags <- "tags_example" # character | Custom string field for doing full-text searches (Optional)
#' var_parent_offer_id <- 56 # integer | the parent offer id (Optional)
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Comma separated list of retailer location ids. This will assign the offer to these retailer locations. (Optional)
#' var_offer_locations <- "offer_locations_example" # character | A list of json data that has offer location specific values. (Optional)
#' var_sub_title <- "sub_title_example" # character | The sub title (255 char limit) (Optional)
#' var_details <- "details_example" # character | The details (Optional)
#' var_sub_details <- "sub_details_example" # character | A string for custom details (255 char limit) (Optional)
#' var_fine_print <- "fine_print_example" # character | The fine print (Optional)
#' var_barcode_entry <- "barcode_entry_example" # character | The bar code entry string (Optional)
#' var_external_redeem_options <- "external_redeem_options_example" # character | An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer (Optional)
#' var_external_url <- "external_url_example" # character | The clickUrl of the offer (Optional)
#' var_external_id <- "external_id_example" # character | an external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer (Optional)
#' var_tickets_reward_type <- "tickets_reward_type_example" # character | The type of ticket to reward, null means default type (Optional)
#' var_tickets_reward <- 56 # integer | Determines how many tickets are awarded (Optional)
#' var_activated <- 56 # integer | The date of when the offer will be visible to consumers (Optional)
#' var_expires <- 56 # integer | The date of when the offer expires (Optional)
#' var_ticket_price_type <- "ticket_price_type_example" # character | the type of ticket needed to buy offer (Optional)
#' var_show_remaining <- "show_remaining_example" # character | show remaining offers available (Optional)
#' var_show_redeemed <- "show_redeemed_example" # character | show how many offers have been redeemed (Optional)
#' var_replaced <- "replaced_example" # character |  (Optional)
#' var_featured <- "featured_example" # character | flag if offer is featured or not (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category ids (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Comma separated list of filter ids (Optional)
#' var_barcode_asset_id <- 56 # integer | The id of the barcode asset (Optional)
#' var_image_asset_id <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id1 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id2 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id3 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id4 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id5 <- 56 # integer | The id of the an offer asset (Optional)
#' var_publisher <- "publisher_example" # character | The maker of the item. (Optional)
#' var_redeemable_start <- 56 # integer | The redeemable start date/time of the offer. (Optional)
#' var_redeemable_end <- 56 # integer | The redeemable start date/time of the offer. (Optional)
#' var_brand <- "brand_example" # character | The brand. OfferType PRODUCT only. (Optional)
#' var_product_type <- "product_type_example" # character | The product type. OfferType PRODUCT only. (Optional)
#' var_condition_type <- "condition_type_example" # character | The condition. OfferType PRODUCT only. (Optional)
#' var_isbn <- "isbn_example" # character | The ISBN id. OfferType PRODUCT only. (Optional)
#' var_asin <- "asin_example" # character | The ASIN id. OfferType PRODUCT only. (Optional)
#' var_catalog_numbers <- "catalog_numbers_example" # character | The list of catelog numbers, comma seperated. OfferType PRODUCT only. (Optional)
#' var_department <- "department_example" # character | The department name. The OfferType PRODUCT only. (Optional)
#' var_features <- "features_example" # character | The list of features, comma seperated. OfferType PRODUCT only. (Optional)
#' var_minimum_price <- 3.4 # numeric | The MAP price. OfferType PRODUCT only. (Optional)
#' var_width <- 3.4 # numeric | The width of the item. OfferType PRODUCT only. (Optional)
#' var_height <- 3.4 # numeric | The height of the item. OfferType PRODUCT only. (Optional)
#' var_depth <- 3.4 # numeric | The depth of the item. OfferType PRODUCT only. (Optional)
#' var_weight <- 3.4 # numeric | The weight of the item. OfferType PRODUCT only. (Optional)
#' var_unit <- "unit_example" # character | The unit of measurement. OfferType PRODUCT only. (Optional)
#' var_studio <- "studio_example" # character | The studio name. OfferType PRODUCT only. (Optional)
#' var_parental_rating <- "parental_rating_example" # character | The parental control rating. OfferType PRODUCT only. (Optional)
#' var_publish_date <- 56 # integer | The date published. OfferType PRODUCT only. (Optional)
#' var_availability_date <- 56 # integer | The date available. OfferType PRODUCT only. (Optional)
#' var_size_id <- 56 # integer |  (Optional)
#' var_listing_id <- 56 # integer | The ID of the event listing (Optional)
#' var_media_type <- "media_type_example" # character | the media type of the offer (Optional)
#' var_duration <- 56 # integer | The total playing time of the media item. OfferType MEDIA only. (Optional)
#' var_author <- "author_example" # character | The created/author of the media item. OfferType MEDIA only. (Optional)
#' var_release_date <- 56 # integer | The date/time of when the media item was originally released. OfferType MEDIA only. (Optional)
#' var_collection_ids <- "collection_ids_example" # character |  (Optional)
#' var_reboot_time_hour <- 56 # integer | The reboot hour time ranging from 0 to 23 (Optional)
#' var_reboot_time_minute <- 56 # integer | The reboot minute time ranging from 0 to 59 (Optional)
#' var_idle_timeout_in_second <- 56 # integer | If the device is idle for idleTimeoutInSecond then the device should timeout (Optional)
#' var_serial_number <- "serial_number_example" # character | The serial number on the device (Optional)
#' var_udid <- "udid_example" # character | The unique device id for the device (Optional)
#' var_device_type <- "device_type_example" # character | The type of the device, for example: \"Kiosk\", \"Beacon\". (Optional)
#' var_device_power <- 3.4 # numeric | Edysen device power level setting (Optional)
#' var_device_interference <- 3.4 # numeric | Edysen device inteference setting (Optional)
#' var_availability <- "availability_example" # character |  (Optional)
#' var_availability_summary <- "availability_summary_example" # character |  (Optional)
#'
#' #Create Offer
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateOffer(var_include_offer_locations, var_title, var_barcode_type, var_no_expiration, var_available_limit, var_available_limit_per_user, var_added_limit, var_view_limit, var_max_prints, var_ticket_price, var_full_price, var_discount_price, var_offer_type, var_special_offer_type, var_offer_visibility, var_active, device_id = var_device_id, account_id = var_account_id, tags = var_tags, parent_offer_id = var_parent_offer_id, retailer_location_ids = var_retailer_location_ids, offer_locations = var_offer_locations, sub_title = var_sub_title, details = var_details, sub_details = var_sub_details, fine_print = var_fine_print, barcode_entry = var_barcode_entry, external_redeem_options = var_external_redeem_options, external_url = var_external_url, external_id = var_external_id, tickets_reward_type = var_tickets_reward_type, tickets_reward = var_tickets_reward, activated = var_activated, expires = var_expires, ticket_price_type = var_ticket_price_type, show_remaining = var_show_remaining, show_redeemed = var_show_redeemed, replaced = var_replaced, featured = var_featured, category_ids = var_category_ids, filter_ids = var_filter_ids, barcode_asset_id = var_barcode_asset_id, image_asset_id = var_image_asset_id, image_asset_id1 = var_image_asset_id1, image_asset_id2 = var_image_asset_id2, image_asset_id3 = var_image_asset_id3, image_asset_id4 = var_image_asset_id4, image_asset_id5 = var_image_asset_id5, publisher = var_publisher, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end, brand = var_brand, product_type = var_product_type, condition_type = var_condition_type, isbn = var_isbn, asin = var_asin, catalog_numbers = var_catalog_numbers, department = var_department, features = var_features, minimum_price = var_minimum_price, width = var_width, height = var_height, depth = var_depth, weight = var_weight, unit = var_unit, studio = var_studio, parental_rating = var_parental_rating, publish_date = var_publish_date, availability_date = var_availability_date, size_id = var_size_id, listing_id = var_listing_id, media_type = var_media_type, duration = var_duration, author = var_author, release_date = var_release_date, collection_ids = var_collection_ids, reboot_time_hour = var_reboot_time_hour, reboot_time_minute = var_reboot_time_minute, idle_timeout_in_second = var_idle_timeout_in_second, serial_number = var_serial_number, udid = var_udid, device_type = var_device_type, device_power = var_device_power, device_interference = var_device_interference, availability = var_availability, availability_summary = var_availability_summarydata_file = "result.txt")
#' result <- api_instance$CreateOffer(var_include_offer_locations, var_title, var_barcode_type, var_no_expiration, var_available_limit, var_available_limit_per_user, var_added_limit, var_view_limit, var_max_prints, var_ticket_price, var_full_price, var_discount_price, var_offer_type, var_special_offer_type, var_offer_visibility, var_active, device_id = var_device_id, account_id = var_account_id, tags = var_tags, parent_offer_id = var_parent_offer_id, retailer_location_ids = var_retailer_location_ids, offer_locations = var_offer_locations, sub_title = var_sub_title, details = var_details, sub_details = var_sub_details, fine_print = var_fine_print, barcode_entry = var_barcode_entry, external_redeem_options = var_external_redeem_options, external_url = var_external_url, external_id = var_external_id, tickets_reward_type = var_tickets_reward_type, tickets_reward = var_tickets_reward, activated = var_activated, expires = var_expires, ticket_price_type = var_ticket_price_type, show_remaining = var_show_remaining, show_redeemed = var_show_redeemed, replaced = var_replaced, featured = var_featured, category_ids = var_category_ids, filter_ids = var_filter_ids, barcode_asset_id = var_barcode_asset_id, image_asset_id = var_image_asset_id, image_asset_id1 = var_image_asset_id1, image_asset_id2 = var_image_asset_id2, image_asset_id3 = var_image_asset_id3, image_asset_id4 = var_image_asset_id4, image_asset_id5 = var_image_asset_id5, publisher = var_publisher, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end, brand = var_brand, product_type = var_product_type, condition_type = var_condition_type, isbn = var_isbn, asin = var_asin, catalog_numbers = var_catalog_numbers, department = var_department, features = var_features, minimum_price = var_minimum_price, width = var_width, height = var_height, depth = var_depth, weight = var_weight, unit = var_unit, studio = var_studio, parental_rating = var_parental_rating, publish_date = var_publish_date, availability_date = var_availability_date, size_id = var_size_id, listing_id = var_listing_id, media_type = var_media_type, duration = var_duration, author = var_author, release_date = var_release_date, collection_ids = var_collection_ids, reboot_time_hour = var_reboot_time_hour, reboot_time_minute = var_reboot_time_minute, idle_timeout_in_second = var_idle_timeout_in_second, serial_number = var_serial_number, udid = var_udid, device_type = var_device_type, device_power = var_device_power, device_interference = var_device_interference, availability = var_availability, availability_summary = var_availability_summary)
#' dput(result)
#'
#'
#' ####################  DeleteOffer  ####################
#'
#' library(openapi)
#' var_offer_id <- 56 # integer | The ID of the offer to be deleted
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account used to perform the delete, must have rights to edit the offer. (Optional)
#'
#' #Delete Offer
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteOffer(var_offer_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteOffer(var_offer_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  DeleteOfferLocation  ####################
#'
#' library(openapi)
#' var_offer_location_id <- 56 # integer | The ID of the offer location to be deleted
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account used to perform the delete, must have rights to edit the offer location. (Optional)
#'
#' #Delete Offer Location
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteOfferLocation(var_offer_location_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteOfferLocation(var_offer_location_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  GetOffer  ####################
#'
#' library(openapi)
#' var_offer_id <- 56 # integer | The id of the offer
#' var_include_offer_locations <- "include_offer_locations_example" # character | 
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id (deviceId or accountId required) (Optional)
#'
#' #Get Offer
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOffer(var_offer_id, var_include_offer_locations, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$GetOffer(var_offer_id, var_include_offer_locations, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  GetOfferDetails  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id for returning account information (i.e. favorites) (Optional)
#' var_account_id <- 56 # integer | The account id for returning account information (i.e. favorites) (Optional)
#' var_offer_id <- 56 # integer | The offer id (either offeLocationId or offerId must be provided) (Optional)
#' var_offer_location_id <- 56 # integer | The offer location id (either offeLocationId or offerId must be provided) (Optional)
#' var_distance <- 3.4 # numeric | The distance of the offer from the user's current location (this is returned when the offer is searched) (Optional)
#' var_latitude <- 3.4 # numeric | The latitude to calculate distance from the offer (Optional)
#' var_longitude <- 3.4 # numeric | The longitude to calculate distance from the offer (Optional)
#' var_include_offer_locations <- FALSE # character | Determines whether to return offer locations for the offer (Optional)
#' var_include_retailer_locations <- FALSE # character | Determines whether to return the retailer location info for each offer location response (includeOfferLocations must also be true for this to work) (Optional)
#' var_include_child_offers <- FALSE # character | Determines whether to include child offers in the response (Optional)
#'
#' #Get Offer
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOfferDetails(device_id = var_device_id, account_id = var_account_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, distance = var_distance, latitude = var_latitude, longitude = var_longitude, include_offer_locations = var_include_offer_locations, include_retailer_locations = var_include_retailer_locations, include_child_offers = var_include_child_offersdata_file = "result.txt")
#' result <- api_instance$GetOfferDetails(device_id = var_device_id, account_id = var_account_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, distance = var_distance, latitude = var_latitude, longitude = var_longitude, include_offer_locations = var_include_offer_locations, include_retailer_locations = var_include_retailer_locations, include_child_offers = var_include_child_offers)
#' dput(result)
#'
#'
#' ####################  GetOfferListCounts  ####################
#'
#' library(openapi)
#' var_latitude <- 3.4 # numeric | The latitude of where the search will center at
#' var_longitude <- 3.4 # numeric | The longitude of where the search will center at
#' var_search_range <- 5 # numeric | The range of the search (Optional)
#' var_distance_unit <- "MILES" # character | The units to use for distance calculations (e.g. MILES, KILOMETERS) (Optional)
#'
#' #Get Offers (Counts)
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOfferListCounts(var_latitude, var_longitude, search_range = var_search_range, distance_unit = var_distance_unitdata_file = "result.txt")
#' result <- api_instance$GetOfferListCounts(var_latitude, var_longitude, search_range = var_search_range, distance_unit = var_distance_unit)
#' dput(result)
#'
#'
#' ####################  GetOfferLocation  ####################
#'
#' library(openapi)
#' var_offer_location_id <- 56 # integer | the id of the offer location to get (Optional)
#' var_udid <- "udid_example" # character | the UDID of the device (Optional)
#'
#' #Get Offer Location
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOfferLocation(offer_location_id = var_offer_location_id, udid = var_udiddata_file = "result.txt")
#' result <- api_instance$GetOfferLocation(offer_location_id = var_offer_location_id, udid = var_udid)
#' dput(result)
#'
#'
#' ####################  GetOfferLocationsForRetailers  ####################
#'
#' library(openapi)
#' var_sort_field <- "sort_field_example" # character | The column to sort the results on. Default is \"TITLE\", which will sort the results by the offer title. Possible input values: {CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, RETAILER_ID,RETAILER_LOCATION_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY}
#' var_descending <- "descending_example" # character | The order to return the results. Default is false, which will return the results in ascending order.
#' var_start <- 56 # integer | The index into the record set to start with. Default is 0.
#' var_limit <- 56 # integer | The total number of records to return. Default is 20.
#' var_active_only <- "active_only_example" # character | Determines whether to return only active results. Default is false.
#' var_include_retailer_location <- "include_retailer_location_example" # character | 
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_retailer_id <- 56 # integer | Filter results for a specific retailer (Optional)
#' var_retailer_location_id <- 56 # integer | Filter results for a specific retailer location (Optional)
#' var_offer_type <- "offer_type_example" # character | Filter results to only return COUPON, VOUCHER, PRODUCT, MEDIA, or EVENT offers (Optional)
#' var_special_offer_type <- "special_offer_type_example" # character | Filter results to only return REGULAR_OFFER, ACT_NOW, GET_THERE_NOW offer specials (Optional)
#' var_barcode_type <- "barcode_type_example" # character |  (Optional)
#' var_barcode_entry <- "barcode_entry_example" # character |  (Optional)
#' var_isbn <- "isbn_example" # character |  (Optional)
#' var_asin <- "asin_example" # character |  (Optional)
#' var_device_status <- "device_status_example" # character | Edysen device status, running, warning, or down (Optional)
#' var_needs_notification_sent <- "needs_notification_sent_example" # character |  (Optional)
#' var_last_notification_sent <- 56 # integer |  (Optional)
#'
#' #Search Offer Locations
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOfferLocationsForRetailers(var_sort_field, var_descending, var_start, var_limit, var_active_only, var_include_retailer_location, device_id = var_device_id, account_id = var_account_id, keyword = var_keyword, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, offer_type = var_offer_type, special_offer_type = var_special_offer_type, barcode_type = var_barcode_type, barcode_entry = var_barcode_entry, isbn = var_isbn, asin = var_asin, device_status = var_device_status, needs_notification_sent = var_needs_notification_sent, last_notification_sent = var_last_notification_sentdata_file = "result.txt")
#' result <- api_instance$GetOfferLocationsForRetailers(var_sort_field, var_descending, var_start, var_limit, var_active_only, var_include_retailer_location, device_id = var_device_id, account_id = var_account_id, keyword = var_keyword, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, offer_type = var_offer_type, special_offer_type = var_special_offer_type, barcode_type = var_barcode_type, barcode_entry = var_barcode_entry, isbn = var_isbn, asin = var_asin, device_status = var_device_status, needs_notification_sent = var_needs_notification_sent, last_notification_sent = var_last_notification_sent)
#' dput(result)
#'
#'
#' ####################  GetOffersForRetailers  ####################
#'
#' library(openapi)
#' var_offer_visibility <- "offer_visibility_example" # character | 
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY
#' var_descending <- "descending_example" # character | The order to return the search results
#' var_start <- 56 # integer | The record to begin the return set on
#' var_limit <- 56 # integer | The number of records to return
#' var_available_only <- "available_only_example" # character | Return only results that are currently being promoted (is activated and not expired)
#' var_active_only <- "active_only_example" # character | Return only active results
#' var_include_categories <- "include_categories_example" # character | 
#' var_include_filters <- "include_filters_example" # character | 
#' var_include_offer_locations <- "include_offer_locations_example" # character | 
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_category_ids <- "category_ids_example" # character |  (Optional)
#' var_filter_ids <- "filter_ids_example" # character |  (Optional)
#' var_q <- "q_example" # character | This parameter is deprecated. (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_retailer_id <- 56 # integer | the id of the retailer (Optional)
#' var_retailer_location_id <- 56 # integer | the id of the retailer location (Optional)
#' var_coupon_type <- "coupon_type_example" # character | This parameter is deprecated. (Optional)
#' var_offer_type <- "offer_type_example" # character | This parameter is deprecated. Use offer types. Filter results to only return COUPON, VOUCHER, PRODUCT, MEDIA, or EVENT offers (Optional)
#' var_offer_types <- "offer_types_example" # character | Comma separated list of offer types {COUPON, VOUCHER, PRODUCT, MEDIA, EVENT, DEVICE} (Optional)
#' var_special_offer_type <- "special_offer_type_example" # character | Filter results to only return REGULAR_OFFER, ACT_NOW, GET_THERE_NOW offer specials (Optional)
#' var__i <- 56 # integer | This parameter is deprecated. (Optional)
#' var__l <- 56 # integer | This parameter is deprecated. (Optional)
#' var_barcode_type <- "barcode_type_example" # character |  (Optional)
#' var_barcode_entry <- "barcode_entry_example" # character |  (Optional)
#' var_isbn <- "isbn_example" # character |  (Optional)
#' var_asin <- "asin_example" # character |  (Optional)
#' var_device_status <- "device_status_example" # character | Edysen device status (Optional)
#' var_needs_notification_sent <- "needs_notification_sent_example" # character |  (Optional)
#' var_last_notification_sent <- 56 # integer |  (Optional)
#'
#' #Search Offers
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOffersForRetailers(var_offer_visibility, var_sort_field, var_descending, var_start, var_limit, var_available_only, var_active_only, var_include_categories, var_include_filters, var_include_offer_locations, device_id = var_device_id, account_id = var_account_id, category_ids = var_category_ids, filter_ids = var_filter_ids, q = var_q, keyword = var_keyword, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, coupon_type = var_coupon_type, offer_type = var_offer_type, offer_types = var_offer_types, special_offer_type = var_special_offer_type, _i = var__i, _l = var__l, barcode_type = var_barcode_type, barcode_entry = var_barcode_entry, isbn = var_isbn, asin = var_asin, device_status = var_device_status, needs_notification_sent = var_needs_notification_sent, last_notification_sent = var_last_notification_sentdata_file = "result.txt")
#' result <- api_instance$GetOffersForRetailers(var_offer_visibility, var_sort_field, var_descending, var_start, var_limit, var_available_only, var_active_only, var_include_categories, var_include_filters, var_include_offer_locations, device_id = var_device_id, account_id = var_account_id, category_ids = var_category_ids, filter_ids = var_filter_ids, q = var_q, keyword = var_keyword, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, coupon_type = var_coupon_type, offer_type = var_offer_type, offer_types = var_offer_types, special_offer_type = var_special_offer_type, _i = var__i, _l = var__l, barcode_type = var_barcode_type, barcode_entry = var_barcode_entry, isbn = var_isbn, asin = var_asin, device_status = var_device_status, needs_notification_sent = var_needs_notification_sent, last_notification_sent = var_last_notification_sent)
#' dput(result)
#'
#'
#' ####################  RedeemOfferTransaction  ####################
#'
#' library(openapi)
#' var_offer_transaction_id <- 56 # integer | the OfferTransaction ID of the transaction being redeemed
#' var_status <- 56 # integer | the status to set the offer transaction to - 1 sets it to redeemable and 2 sets it to redeemed
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_offer_location_id <- 56 # integer | the OfferLocation ID where the offer is being redeemed (Optional)
#'
#' #Update Offer Transaction
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RedeemOfferTransaction(var_offer_transaction_id, var_status, device_id = var_device_id, account_id = var_account_id, offer_location_id = var_offer_location_iddata_file = "result.txt")
#' result <- api_instance$RedeemOfferTransaction(var_offer_transaction_id, var_status, device_id = var_device_id, account_id = var_account_id, offer_location_id = var_offer_location_id)
#' dput(result)
#'
#'
#' ####################  SearchOfferTransactionsForRetailers  ####################
#'
#' library(openapi)
#' var_sort_field <- "sort_field_example" # character | Determines what to sort the results by {CREATED, UPDATED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, CUSTOMER_ID, CUSTOMER_DISPLAY, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY}
#' var_descending <- "descending_example" # character | Determines whether the results are in descending order
#' var_start <- 56 # integer | The start index for pagination
#' var_limit <- 56 # integer | The limit for pagination
#' var_active_only <- "active_only_example" # character | Determines whether to only return active offer transactions
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_q <- "q_example" # character | This parameter is deprecated. (Optional)
#' var_keyword <- "keyword_example" # character | The keyword to search for (Optional)
#' var_retailer_id <- 56 # integer | Filter results for this retailer (Optional)
#' var_retailer_location_id <- 56 # integer | Filter results for this retailer location (Optional)
#' var_offer_id <- 56 # integer | Filter results for this offer (Optional)
#' var_offer_location_id <- 56 # integer | Filter results for this offer location (Optional)
#' var_redeemed <- "redeemed_example" # character | if true return redeemed transactions (status 2), if false return active transactions (status 0 or 1) (Optional)
#' var_reservations_only <- "reservations_only_example" # character | returns only reservation transactions if true (Optional)
#' var_coupon_type <- "coupon_type_example" # character | This parameter is deprecated. (Optional)
#' var_offer_type <- "offer_type_example" # character | Filter results to only return COUPON, VOUCHER, PRODUCT, MEDIA, or EVENT offers (Optional)
#' var_special_offer_type <- "special_offer_type_example" # character | Filter results to only return REGULAR_OFFER, ACT_NOW, GET_THERE_NOW offer specials (Optional)
#' var_customer_account_ids <- "customer_account_ids_example" # character | Filter results by accounts (Optional)
#' var_category_ids <- "category_ids_example" # character |  (Optional)
#' var_redeemable_start_date <- 56 # integer |  (Optional)
#' var_redeemable_end_date <- 56 # integer |  (Optional)
#' var__i <- 56 # integer | This parameter is deprecated. (Optional)
#' var__l <- 56 # integer | This parameter is deprecated. (Optional)
#'
#' #Search Offer Transactions
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchOfferTransactionsForRetailers(var_sort_field, var_descending, var_start, var_limit, var_active_only, device_id = var_device_id, account_id = var_account_id, q = var_q, keyword = var_keyword, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, redeemed = var_redeemed, reservations_only = var_reservations_only, coupon_type = var_coupon_type, offer_type = var_offer_type, special_offer_type = var_special_offer_type, customer_account_ids = var_customer_account_ids, category_ids = var_category_ids, redeemable_start_date = var_redeemable_start_date, redeemable_end_date = var_redeemable_end_date, _i = var__i, _l = var__ldata_file = "result.txt")
#' result <- api_instance$SearchOfferTransactionsForRetailers(var_sort_field, var_descending, var_start, var_limit, var_active_only, device_id = var_device_id, account_id = var_account_id, q = var_q, keyword = var_keyword, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, redeemed = var_redeemed, reservations_only = var_reservations_only, coupon_type = var_coupon_type, offer_type = var_offer_type, special_offer_type = var_special_offer_type, customer_account_ids = var_customer_account_ids, category_ids = var_category_ids, redeemable_start_date = var_redeemable_start_date, redeemable_end_date = var_redeemable_end_date, _i = var__i, _l = var__l)
#' dput(result)
#'
#'
#' ####################  SearchOffersForConsumer  ####################
#'
#' library(openapi)
#' var_latitude <- 3.4 # numeric | The latitude of where the search will center at
#' var_longitude <- 3.4 # numeric | The longitude of where the search will center at
#' var_recommendation_type <- "recommendation_type_example" # character | The method to use to gather recommendations: WALLET base relevance on items in users wallets CLICKS base relevance on items users have clicked on BLENDED blend using all methods available
#' var_location_id <- 56 # integer | This parameter is deprecated. The location id
#' var_start <- 56 # integer | The start index for pagination
#' var_limit <- 56 # integer | The limit for pagination
#' var_max_recommendations <- 56 # integer | 
#' var_distance_unit <- "distance_unit_example" # character | 
#' var_app_key <- "app_key_example" # character | The application key used to identify the application (Optional)
#' var_device_id <- "device_id_example" # character | The device id for returning account information (i.e. offer transactions made by the user) (Optional)
#' var_account_id <- 56 # integer | The account id for returning account information (i.e. offer transactions made by the user) (Optional)
#' var_search_range <- 5 # numeric | The range of the search (Optional)
#' var_tags <- "tags_example" # character | Does a full-text search on tags (Optional)
#' var_supported_postal_codes <- "supported_postal_codes_example" # character | supported postal codes (Optional)
#' var_keyword <- "keyword_example" # character | The keyword to filter results by (Optional)
#' var_categories <- "categories_example" # character | Comma separate list of category ids (Optional)
#' var_filters <- "filters_example" # character | Comma separated list of filter ids (Optional)
#' var_offer_types <- "COUPON, VOUCHER" # character | Comma separated list of offer types {COUPON, VOUCHER, PRODUCT, MEDIA, EVENT, DEVICE} (Optional)
#' var_type <- "type_example" # character | The special offer type {ALL, RESERVABLE, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW} (Optional)
#' var_sort_field <- "sort_field_example" # character | The field to sort the result set on:DISTANCE Sort the offers nearest to farthest BUSINESS_NAME Sort the offers alphabetically by location name SHUFFLE Sort the nearest offers randomly USERPREFS accountId must accompany this parameter. Gives the first X recommendations where X equals the parameter maxRecommendations (Optional)
#' var_recommend_offer_ids <- "recommend_offer_ids_example" # character | Return recommendations based on these offers IDs (Optional)
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Only return offer locations for the specific retailer locations. (Optional)
#' var_offer_id <- 56 # integer | Only return offer locations for the specific offer. (Optional)
#' var_include_mission <- "include_mission_example" # character | If true include the mission response as part of the offer. Default is false. (Optional)
#' var_include_categories <- "include_categories_example" # character | If true include the category list response as part of the offer. Default is false. (Optional)
#' var_include_filters <- "include_filters_example" # character | If true include the filter list response as part of the offer. Default is false. (Optional)
#' var_include_expired <- "include_expired_example" # character | If true then ignore the expired dates. Default is false. (Optional)
#' var_include_favorite <- "include_favorite_example" # character | If true then ignore the favorite. Default is false. (Optional)
#' var_closest_offer_only <- "closest_offer_only_example" # character | This parameter is deprecated. see groupBy. If true then it only returns the offer location for an offer closest to the given lat/lon (Optional)
#' var_search_expression <- "search_expression_example" # character |  (Optional)
#' var_group_by <- "group_by_example" # character | groups the results by a certain field. For example, if you want to return the closest offer location of an offer, then pass in groupBy=OFFER_ID and sortField=DISTANCE (to sort by distance). (Optional)
#'
#' #Search Offers
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchOffersForConsumer(var_latitude, var_longitude, var_recommendation_type, var_location_id, var_start, var_limit, var_max_recommendations, var_distance_unit, app_key = var_app_key, device_id = var_device_id, account_id = var_account_id, search_range = var_search_range, tags = var_tags, supported_postal_codes = var_supported_postal_codes, keyword = var_keyword, categories = var_categories, filters = var_filters, offer_types = var_offer_types, type = var_type, sort_field = var_sort_field, recommend_offer_ids = var_recommend_offer_ids, retailer_location_ids = var_retailer_location_ids, offer_id = var_offer_id, include_mission = var_include_mission, include_categories = var_include_categories, include_filters = var_include_filters, include_expired = var_include_expired, include_favorite = var_include_favorite, closest_offer_only = var_closest_offer_only, search_expression = var_search_expression, group_by = var_group_bydata_file = "result.txt")
#' result <- api_instance$SearchOffersForConsumer(var_latitude, var_longitude, var_recommendation_type, var_location_id, var_start, var_limit, var_max_recommendations, var_distance_unit, app_key = var_app_key, device_id = var_device_id, account_id = var_account_id, search_range = var_search_range, tags = var_tags, supported_postal_codes = var_supported_postal_codes, keyword = var_keyword, categories = var_categories, filters = var_filters, offer_types = var_offer_types, type = var_type, sort_field = var_sort_field, recommend_offer_ids = var_recommend_offer_ids, retailer_location_ids = var_retailer_location_ids, offer_id = var_offer_id, include_mission = var_include_mission, include_categories = var_include_categories, include_filters = var_include_filters, include_expired = var_include_expired, include_favorite = var_include_favorite, closest_offer_only = var_closest_offer_only, search_expression = var_search_expression, group_by = var_group_by)
#' dput(result)
#'
#'
#' ####################  TopOfferTransactions  ####################
#'
#' library(openapi)
#' var_start <- 0 # integer | The index into the record set to start with. Default is 0. (Optional)
#' var_limit <- 20 # integer | The total number of record to return. Default id 20. (Optional)
#'
#' #Get Offers (Top)
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$TopOfferTransactions(start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$TopOfferTransactions(start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  UpdateOffer  ####################
#'
#' library(openapi)
#' var_offer_id <- 56 # integer | The offer to update
#' var_include_offer_locations <- "include_offer_locations_example" # character | If true return all the offer locations associated with the offer
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account used to perform the update, must have rights to edit the offer (deviceId or accountId required) (Optional)
#' var_parent_offer_id <- 56 # integer |  (Optional)
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Comma separated list of retailer location ids. This will assign the offer to these retailer locations. (Optional)
#' var_offer_locations <- "offer_locations_example" # character | A list of json data that has offer location specific values. (Optional)
#' var_tags <- "tags_example" # character | Custom string field for doing full-text searches (Optional)
#' var_title <- "title_example" # character | The title (255 char limit) (Optional)
#' var_sub_title <- "sub_title_example" # character | The sub title (255 char limit) (Optional)
#' var_details <- "details_example" # character | The details (Optional)
#' var_sub_details <- "sub_details_example" # character | A string for custom details (255 char limit) (Optional)
#' var_fine_print <- "fine_print_example" # character | The fine print (Optional)
#' var_barcode_type <- "barcode_type_example" # character | The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA} (Optional)
#' var_barcode_entry <- "barcode_entry_example" # character | The bar code entry string (Optional)
#' var_external_redeem_options <- "external_redeem_options_example" # character | An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer. (Optional)
#' var_external_url <- "external_url_example" # character | The clickUrl of the offer (Optional)
#' var_external_id <- "external_id_example" # character |  (Optional)
#' var_tickets_reward_type <- "tickets_reward_type_example" # character | The type of ticket to reward, null means default type (Optional)
#' var_tickets_reward <- 56 # integer | Determines how many tickets are awarded (Optional)
#' var_activated <- 56 # integer | The date of when the offer will be visible to consumers (Optional)
#' var_expires <- 56 # integer | The date of when the offer expires (Optional)
#' var_no_expiration <- "no_expiration_example" # character | Overrides the expiration date so that the offer does not expire (Optional)
#' var_available_limit <- 56 # integer | The limit of how many times the offer can be used by consumers (Optional)
#' var_available_limit_per_user <- 56 # integer | The limit of how many times a user can used the same offer (Optional)
#' var_added_limit <- 56 # integer | The limit of how many times the offer can be added to consumer wallets (Optional)
#' var_view_limit <- 56 # integer |  (Optional)
#' var_max_prints <- 56 # integer | The maximum number of times the offer can be printed (Optional)
#' var_ticket_price_type <- "ticket_price_type_example" # character | the type of ticket needed to buy the offer (Optional)
#' var_ticket_price <- 56 # integer | The cost of the offer in tickets (Optional)
#' var_full_price <- 3.4 # numeric | The retail/full price cost of the offer in real currency (Optional)
#' var_discount_price <- 3.4 # numeric | The cost of the offer at a discounted price (what the consumer pays) (Optional)
#' var_show_remaining <- "show_remaining_example" # character | show remaining offers available (Optional)
#' var_show_redeemed <- "show_redeemed_example" # character | show how many offers have been redeemed (Optional)
#' var_replaced <- "replaced_example" # character |  (Optional)
#' var_featured <- "featured_example" # character | whether the offer is featured or not (Optional)
#' var_offer_type <- "offer_type_example" # character | The offer type {VOUCHER, COUPON, PRODUCT, MEDIA, EVENT} (Optional)
#' var_special_offer_type <- "special_offer_type_example" # character | The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW} (Optional)
#' var_offer_visibility <- "offer_visibility_example" # character | The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE} (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category ids (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Comma separated list of filter ids (Optional)
#' var_active <- "active_example" # character | Sets the active flag (Optional)
#' var_barcode_asset_id <- 56 # integer | The id of the barcode asset (Optional)
#' var_image_asset_id <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id1 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id2 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id3 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id4 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id5 <- 56 # integer | The id of the an offer asset (Optional)
#' var_publisher <- "publisher_example" # character | The maker of the item. (Optional)
#' var_redeemable_start <- 56 # integer | The redeemable start date/time of the offer. (Optional)
#' var_redeemable_end <- 56 # integer | The redeemable start date/time of the offer. (Optional)
#' var_brand <- "brand_example" # character | The brand. OfferType PRODUCT only. (Optional)
#' var_product_type <- "product_type_example" # character | The product type. OfferType PRODUCT only. (Optional)
#' var_condition_type <- "condition_type_example" # character | The condition. OfferType PRODUCT only. (Optional)
#' var_isbn <- "isbn_example" # character | The ISBN id. OfferType PRODUCT only. (Optional)
#' var_asin <- "asin_example" # character | The ASIN id. OfferType PRODUCT only. (Optional)
#' var_catalog_numbers <- "catalog_numbers_example" # character | The list of catelog numbers, comma seperated. OfferType PRODUCT only. (Optional)
#' var_department <- "department_example" # character | The department name. The OfferType PRODUCT only. (Optional)
#' var_features <- "features_example" # character | The list of features, comma seperated. OfferType PRODUCT only. (Optional)
#' var_minimum_price <- 3.4 # numeric | The MAP price. OfferType PRODUCT only. (Optional)
#' var_width <- 3.4 # numeric | The width of the item. OfferType PRODUCT only. (Optional)
#' var_height <- 3.4 # numeric | The height of the item. OfferType PRODUCT only. (Optional)
#' var_depth <- 3.4 # numeric | The depth of the item. OfferType PRODUCT only. (Optional)
#' var_weight <- 3.4 # numeric | The weight of the item. OfferType PRODUCT only. (Optional)
#' var_unit <- "unit_example" # character | The unit of measurement. OfferType PRODUCT only. (Optional)
#' var_studio <- "studio_example" # character | The studio name. OfferType PRODUCT only. (Optional)
#' var_parental_rating <- "parental_rating_example" # character | The parental control rating. OfferType PRODUCT only. (Optional)
#' var_publish_date <- 56 # integer | The date published. OfferType PRODUCT only. (Optional)
#' var_availability_date <- 56 # integer | The date available. OfferType PRODUCT only. (Optional)
#' var_size_id <- 56 # integer | The size Id. OfferType PRODUCT only. (Optional)
#' var_listing_id <- 56 # integer | The ID of the event listing (Optional)
#' var_media_type <- "media_type_example" # character |  (Optional)
#' var_duration <- 56 # integer | The total playing time of the media item. OfferType MEDIA only. (Optional)
#' var_author <- "author_example" # character | The created/author of the media item. OfferType MEDIA only. (Optional)
#' var_release_date <- 56 # integer | The date/time of when the media item was originally released. OfferType MEDIA only. (Optional)
#' var_collection_ids <- "collection_ids_example" # character |  (Optional)
#' var_reboot_time_hour <- 56 # integer | The reboot hour time ranging from 0 to 23 (Optional)
#' var_reboot_time_minute <- 56 # integer | The reboot minute time ranging from 0 to 59 (Optional)
#' var_idle_timeout_in_second <- 56 # integer | If the device is idle for idleTimeoutInSecond then the device should timeout (Optional)
#' var_serial_number <- "serial_number_example" # character | The serial number on the device (Optional)
#' var_udid <- "udid_example" # character | The unique device id for the device (Optional)
#' var_device_type <- "device_type_example" # character | The type of the device, for example: \"Kiosk\", \"Beacon\". (Optional)
#' var_device_power <- 3.4 # numeric | RSSI reading at 1 meter from device (-10 to -140) (Optional)
#' var_device_interference <- 3.4 # numeric |  (Optional)
#' var_availability <- "availability_example" # character |  (Optional)
#' var_availability_summary <- "availability_summary_example" # character |  (Optional)
#'
#' #Update Offer
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateOffer(var_offer_id, var_include_offer_locations, device_id = var_device_id, account_id = var_account_id, parent_offer_id = var_parent_offer_id, retailer_location_ids = var_retailer_location_ids, offer_locations = var_offer_locations, tags = var_tags, title = var_title, sub_title = var_sub_title, details = var_details, sub_details = var_sub_details, fine_print = var_fine_print, barcode_type = var_barcode_type, barcode_entry = var_barcode_entry, external_redeem_options = var_external_redeem_options, external_url = var_external_url, external_id = var_external_id, tickets_reward_type = var_tickets_reward_type, tickets_reward = var_tickets_reward, activated = var_activated, expires = var_expires, no_expiration = var_no_expiration, available_limit = var_available_limit, available_limit_per_user = var_available_limit_per_user, added_limit = var_added_limit, view_limit = var_view_limit, max_prints = var_max_prints, ticket_price_type = var_ticket_price_type, ticket_price = var_ticket_price, full_price = var_full_price, discount_price = var_discount_price, show_remaining = var_show_remaining, show_redeemed = var_show_redeemed, replaced = var_replaced, featured = var_featured, offer_type = var_offer_type, special_offer_type = var_special_offer_type, offer_visibility = var_offer_visibility, category_ids = var_category_ids, filter_ids = var_filter_ids, active = var_active, barcode_asset_id = var_barcode_asset_id, image_asset_id = var_image_asset_id, image_asset_id1 = var_image_asset_id1, image_asset_id2 = var_image_asset_id2, image_asset_id3 = var_image_asset_id3, image_asset_id4 = var_image_asset_id4, image_asset_id5 = var_image_asset_id5, publisher = var_publisher, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end, brand = var_brand, product_type = var_product_type, condition_type = var_condition_type, isbn = var_isbn, asin = var_asin, catalog_numbers = var_catalog_numbers, department = var_department, features = var_features, minimum_price = var_minimum_price, width = var_width, height = var_height, depth = var_depth, weight = var_weight, unit = var_unit, studio = var_studio, parental_rating = var_parental_rating, publish_date = var_publish_date, availability_date = var_availability_date, size_id = var_size_id, listing_id = var_listing_id, media_type = var_media_type, duration = var_duration, author = var_author, release_date = var_release_date, collection_ids = var_collection_ids, reboot_time_hour = var_reboot_time_hour, reboot_time_minute = var_reboot_time_minute, idle_timeout_in_second = var_idle_timeout_in_second, serial_number = var_serial_number, udid = var_udid, device_type = var_device_type, device_power = var_device_power, device_interference = var_device_interference, availability = var_availability, availability_summary = var_availability_summarydata_file = "result.txt")
#' result <- api_instance$UpdateOffer(var_offer_id, var_include_offer_locations, device_id = var_device_id, account_id = var_account_id, parent_offer_id = var_parent_offer_id, retailer_location_ids = var_retailer_location_ids, offer_locations = var_offer_locations, tags = var_tags, title = var_title, sub_title = var_sub_title, details = var_details, sub_details = var_sub_details, fine_print = var_fine_print, barcode_type = var_barcode_type, barcode_entry = var_barcode_entry, external_redeem_options = var_external_redeem_options, external_url = var_external_url, external_id = var_external_id, tickets_reward_type = var_tickets_reward_type, tickets_reward = var_tickets_reward, activated = var_activated, expires = var_expires, no_expiration = var_no_expiration, available_limit = var_available_limit, available_limit_per_user = var_available_limit_per_user, added_limit = var_added_limit, view_limit = var_view_limit, max_prints = var_max_prints, ticket_price_type = var_ticket_price_type, ticket_price = var_ticket_price, full_price = var_full_price, discount_price = var_discount_price, show_remaining = var_show_remaining, show_redeemed = var_show_redeemed, replaced = var_replaced, featured = var_featured, offer_type = var_offer_type, special_offer_type = var_special_offer_type, offer_visibility = var_offer_visibility, category_ids = var_category_ids, filter_ids = var_filter_ids, active = var_active, barcode_asset_id = var_barcode_asset_id, image_asset_id = var_image_asset_id, image_asset_id1 = var_image_asset_id1, image_asset_id2 = var_image_asset_id2, image_asset_id3 = var_image_asset_id3, image_asset_id4 = var_image_asset_id4, image_asset_id5 = var_image_asset_id5, publisher = var_publisher, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end, brand = var_brand, product_type = var_product_type, condition_type = var_condition_type, isbn = var_isbn, asin = var_asin, catalog_numbers = var_catalog_numbers, department = var_department, features = var_features, minimum_price = var_minimum_price, width = var_width, height = var_height, depth = var_depth, weight = var_weight, unit = var_unit, studio = var_studio, parental_rating = var_parental_rating, publish_date = var_publish_date, availability_date = var_availability_date, size_id = var_size_id, listing_id = var_listing_id, media_type = var_media_type, duration = var_duration, author = var_author, release_date = var_release_date, collection_ids = var_collection_ids, reboot_time_hour = var_reboot_time_hour, reboot_time_minute = var_reboot_time_minute, idle_timeout_in_second = var_idle_timeout_in_second, serial_number = var_serial_number, udid = var_udid, device_type = var_device_type, device_power = var_device_power, device_interference = var_device_interference, availability = var_availability, availability_summary = var_availability_summary)
#' dput(result)
#'
#'
#' ####################  UpdateOfferStatus  ####################
#'
#' library(openapi)
#' var_offer_ids <- "offer_ids_example" # character | Comma separated list of offer ids
#' var_active <- "active_example" # character | Determines whether to make the offer active as well
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account used to perform the activation, must have rights to edit the offer. (Optional)
#'
#' #Activate Offer
#' api_instance <- OfferApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateOfferStatus(var_offer_ids, var_active, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$UpdateOfferStatus(var_offer_ids, var_active, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OfferApi <- R6::R6Class(
  "OfferApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new OfferApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Update Offer Locations
    #'
    #' @param data JSON string in the following format: ```json [{   \"offerLocationId\": 1705,   \"latitude\": 54.0,   \"longitude\": -122.0,   \"altitude\": 1.0,   \"locationDetail\": \"floor 1\",   \"locationDescription\": \"behind the Coke sign\" }, {   \"offerLocationId\": 1704,   \"latitude\": 54.1,   \"longitude\": -122.1 }] ``` 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    BatchUpdateOfferLocations = function(data, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BatchUpdateOfferLocationsWithHttpInfo(data, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Offer Locations
    #'
    #' @param data JSON string in the following format: ```json [{   \"offerLocationId\": 1705,   \"latitude\": 54.0,   \"longitude\": -122.0,   \"altitude\": 1.0,   \"locationDetail\": \"floor 1\",   \"locationDescription\": \"behind the Coke sign\" }, {   \"offerLocationId\": 1704,   \"latitude\": 54.1,   \"longitude\": -122.1 }] ``` 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    BatchUpdateOfferLocationsWithHttpInfo = function(data, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`data`)) {
        stop("Missing required parameter `data`.")
      }

      if (!missing(`data`) && is.null(`data`)) {
        stop("Invalid value for `data` when calling OfferApi$BatchUpdateOfferLocations, `data` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$BatchUpdateOfferLocations, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$BatchUpdateOfferLocations, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["data"]] <- `data`

      local_var_url_path <- "/retailer/offer/location/batchUpdate"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Offer
    #'
    #' @param include_offer_locations If true return all the offer locations associated with the offer
    #' @param title The title (255 char limit)
    #' @param barcode_type The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA}
    #' @param no_expiration Overrides the expiration date so that the offer does not expire
    #' @param available_limit The limit of how many times the offer can be used by consumers
    #' @param available_limit_per_user The limit of how many times a user can used the same offer
    #' @param added_limit The limit of how many times the offer can be added to consumer wallets
    #' @param view_limit The limit of how many times the offer can be viewed
    #' @param max_prints The maximum number of times the offer can be printed
    #' @param ticket_price The cost of the offer in tickets
    #' @param full_price The retail/full price cost of the offer in real currency
    #' @param discount_price The cost of the offer at a discounted price (what the consumer pays)
    #' @param offer_type The offer type {VOUCHER, COUPON, PRODUCT, EVENT, MEDIA, DEVICE}
    #' @param special_offer_type The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
    #' @param offer_visibility The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE}
    #' @param active Sets the active flag
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id (deviceId or accountId required)
    #' @param tags (optional) Custom string field for doing full-text searches
    #' @param parent_offer_id (optional) the parent offer id
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids. This will assign the offer to these retailer locations.
    #' @param offer_locations (optional) A list of json data that has offer location specific values.
    #' @param sub_title (optional) The sub title (255 char limit)
    #' @param details (optional) The details
    #' @param sub_details (optional) A string for custom details (255 char limit)
    #' @param fine_print (optional) The fine print
    #' @param barcode_entry (optional) The bar code entry string
    #' @param external_redeem_options (optional) An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer
    #' @param external_url (optional) The clickUrl of the offer
    #' @param external_id (optional) an external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer
    #' @param tickets_reward_type (optional) The type of ticket to reward, null means default type
    #' @param tickets_reward (optional) Determines how many tickets are awarded
    #' @param activated (optional) The date of when the offer will be visible to consumers
    #' @param expires (optional) The date of when the offer expires
    #' @param ticket_price_type (optional) the type of ticket needed to buy offer
    #' @param show_remaining (optional) show remaining offers available
    #' @param show_redeemed (optional) show how many offers have been redeemed
    #' @param replaced (optional) 
    #' @param featured (optional) flag if offer is featured or not
    #' @param category_ids (optional) Comma separated list of category ids
    #' @param filter_ids (optional) Comma separated list of filter ids
    #' @param barcode_asset_id (optional) The id of the barcode asset
    #' @param image_asset_id (optional) The id of the an offer asset
    #' @param image_asset_id1 (optional) The id of the an offer asset
    #' @param image_asset_id2 (optional) The id of the an offer asset
    #' @param image_asset_id3 (optional) The id of the an offer asset
    #' @param image_asset_id4 (optional) The id of the an offer asset
    #' @param image_asset_id5 (optional) The id of the an offer asset
    #' @param publisher (optional) The maker of the item.
    #' @param redeemable_start (optional) The redeemable start date/time of the offer.
    #' @param redeemable_end (optional) The redeemable start date/time of the offer.
    #' @param brand (optional) The brand. OfferType PRODUCT only.
    #' @param product_type (optional) The product type. OfferType PRODUCT only.
    #' @param condition_type (optional) The condition. OfferType PRODUCT only.
    #' @param isbn (optional) The ISBN id. OfferType PRODUCT only.
    #' @param asin (optional) The ASIN id. OfferType PRODUCT only.
    #' @param catalog_numbers (optional) The list of catelog numbers, comma seperated. OfferType PRODUCT only.
    #' @param department (optional) The department name. The OfferType PRODUCT only.
    #' @param features (optional) The list of features, comma seperated. OfferType PRODUCT only.
    #' @param minimum_price (optional) The MAP price. OfferType PRODUCT only.
    #' @param width (optional) The width of the item. OfferType PRODUCT only.
    #' @param height (optional) The height of the item. OfferType PRODUCT only.
    #' @param depth (optional) The depth of the item. OfferType PRODUCT only.
    #' @param weight (optional) The weight of the item. OfferType PRODUCT only.
    #' @param unit (optional) The unit of measurement. OfferType PRODUCT only.
    #' @param studio (optional) The studio name. OfferType PRODUCT only.
    #' @param parental_rating (optional) The parental control rating. OfferType PRODUCT only.
    #' @param publish_date (optional) The date published. OfferType PRODUCT only.
    #' @param availability_date (optional) The date available. OfferType PRODUCT only.
    #' @param size_id (optional) 
    #' @param listing_id (optional) The ID of the event listing
    #' @param media_type (optional) the media type of the offer
    #' @param duration (optional) The total playing time of the media item. OfferType MEDIA only.
    #' @param author (optional) The created/author of the media item. OfferType MEDIA only.
    #' @param release_date (optional) The date/time of when the media item was originally released. OfferType MEDIA only.
    #' @param collection_ids (optional) 
    #' @param reboot_time_hour (optional) The reboot hour time ranging from 0 to 23
    #' @param reboot_time_minute (optional) The reboot minute time ranging from 0 to 59
    #' @param idle_timeout_in_second (optional) If the device is idle for idleTimeoutInSecond then the device should timeout
    #' @param serial_number (optional) The serial number on the device
    #' @param udid (optional) The unique device id for the device
    #' @param device_type (optional) The type of the device, for example: \"Kiosk\", \"Beacon\".
    #' @param device_power (optional) Edysen device power level setting
    #' @param device_interference (optional) Edysen device inteference setting
    #' @param availability (optional) 
    #' @param availability_summary (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerOfferResponse
    CreateOffer = function(include_offer_locations, title, barcode_type, no_expiration, available_limit, available_limit_per_user, added_limit, view_limit, max_prints, ticket_price, full_price, discount_price, offer_type, special_offer_type, offer_visibility, active, device_id = NULL, account_id = NULL, tags = NULL, parent_offer_id = NULL, retailer_location_ids = NULL, offer_locations = NULL, sub_title = NULL, details = NULL, sub_details = NULL, fine_print = NULL, barcode_entry = NULL, external_redeem_options = NULL, external_url = NULL, external_id = NULL, tickets_reward_type = NULL, tickets_reward = NULL, activated = NULL, expires = NULL, ticket_price_type = NULL, show_remaining = NULL, show_redeemed = NULL, replaced = NULL, featured = NULL, category_ids = NULL, filter_ids = NULL, barcode_asset_id = NULL, image_asset_id = NULL, image_asset_id1 = NULL, image_asset_id2 = NULL, image_asset_id3 = NULL, image_asset_id4 = NULL, image_asset_id5 = NULL, publisher = NULL, redeemable_start = NULL, redeemable_end = NULL, brand = NULL, product_type = NULL, condition_type = NULL, isbn = NULL, asin = NULL, catalog_numbers = NULL, department = NULL, features = NULL, minimum_price = NULL, width = NULL, height = NULL, depth = NULL, weight = NULL, unit = NULL, studio = NULL, parental_rating = NULL, publish_date = NULL, availability_date = NULL, size_id = NULL, listing_id = NULL, media_type = NULL, duration = NULL, author = NULL, release_date = NULL, collection_ids = NULL, reboot_time_hour = NULL, reboot_time_minute = NULL, idle_timeout_in_second = NULL, serial_number = NULL, udid = NULL, device_type = NULL, device_power = NULL, device_interference = NULL, availability = NULL, availability_summary = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateOfferWithHttpInfo(include_offer_locations, title, barcode_type, no_expiration, available_limit, available_limit_per_user, added_limit, view_limit, max_prints, ticket_price, full_price, discount_price, offer_type, special_offer_type, offer_visibility, active, device_id, account_id, tags, parent_offer_id, retailer_location_ids, offer_locations, sub_title, details, sub_details, fine_print, barcode_entry, external_redeem_options, external_url, external_id, tickets_reward_type, tickets_reward, activated, expires, ticket_price_type, show_remaining, show_redeemed, replaced, featured, category_ids, filter_ids, barcode_asset_id, image_asset_id, image_asset_id1, image_asset_id2, image_asset_id3, image_asset_id4, image_asset_id5, publisher, redeemable_start, redeemable_end, brand, product_type, condition_type, isbn, asin, catalog_numbers, department, features, minimum_price, width, height, depth, weight, unit, studio, parental_rating, publish_date, availability_date, size_id, listing_id, media_type, duration, author, release_date, collection_ids, reboot_time_hour, reboot_time_minute, idle_timeout_in_second, serial_number, udid, device_type, device_power, device_interference, availability, availability_summary, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Offer
    #'
    #' @param include_offer_locations If true return all the offer locations associated with the offer
    #' @param title The title (255 char limit)
    #' @param barcode_type The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA}
    #' @param no_expiration Overrides the expiration date so that the offer does not expire
    #' @param available_limit The limit of how many times the offer can be used by consumers
    #' @param available_limit_per_user The limit of how many times a user can used the same offer
    #' @param added_limit The limit of how many times the offer can be added to consumer wallets
    #' @param view_limit The limit of how many times the offer can be viewed
    #' @param max_prints The maximum number of times the offer can be printed
    #' @param ticket_price The cost of the offer in tickets
    #' @param full_price The retail/full price cost of the offer in real currency
    #' @param discount_price The cost of the offer at a discounted price (what the consumer pays)
    #' @param offer_type The offer type {VOUCHER, COUPON, PRODUCT, EVENT, MEDIA, DEVICE}
    #' @param special_offer_type The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
    #' @param offer_visibility The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE}
    #' @param active Sets the active flag
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id (deviceId or accountId required)
    #' @param tags (optional) Custom string field for doing full-text searches
    #' @param parent_offer_id (optional) the parent offer id
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids. This will assign the offer to these retailer locations.
    #' @param offer_locations (optional) A list of json data that has offer location specific values.
    #' @param sub_title (optional) The sub title (255 char limit)
    #' @param details (optional) The details
    #' @param sub_details (optional) A string for custom details (255 char limit)
    #' @param fine_print (optional) The fine print
    #' @param barcode_entry (optional) The bar code entry string
    #' @param external_redeem_options (optional) An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer
    #' @param external_url (optional) The clickUrl of the offer
    #' @param external_id (optional) an external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer
    #' @param tickets_reward_type (optional) The type of ticket to reward, null means default type
    #' @param tickets_reward (optional) Determines how many tickets are awarded
    #' @param activated (optional) The date of when the offer will be visible to consumers
    #' @param expires (optional) The date of when the offer expires
    #' @param ticket_price_type (optional) the type of ticket needed to buy offer
    #' @param show_remaining (optional) show remaining offers available
    #' @param show_redeemed (optional) show how many offers have been redeemed
    #' @param replaced (optional) 
    #' @param featured (optional) flag if offer is featured or not
    #' @param category_ids (optional) Comma separated list of category ids
    #' @param filter_ids (optional) Comma separated list of filter ids
    #' @param barcode_asset_id (optional) The id of the barcode asset
    #' @param image_asset_id (optional) The id of the an offer asset
    #' @param image_asset_id1 (optional) The id of the an offer asset
    #' @param image_asset_id2 (optional) The id of the an offer asset
    #' @param image_asset_id3 (optional) The id of the an offer asset
    #' @param image_asset_id4 (optional) The id of the an offer asset
    #' @param image_asset_id5 (optional) The id of the an offer asset
    #' @param publisher (optional) The maker of the item.
    #' @param redeemable_start (optional) The redeemable start date/time of the offer.
    #' @param redeemable_end (optional) The redeemable start date/time of the offer.
    #' @param brand (optional) The brand. OfferType PRODUCT only.
    #' @param product_type (optional) The product type. OfferType PRODUCT only.
    #' @param condition_type (optional) The condition. OfferType PRODUCT only.
    #' @param isbn (optional) The ISBN id. OfferType PRODUCT only.
    #' @param asin (optional) The ASIN id. OfferType PRODUCT only.
    #' @param catalog_numbers (optional) The list of catelog numbers, comma seperated. OfferType PRODUCT only.
    #' @param department (optional) The department name. The OfferType PRODUCT only.
    #' @param features (optional) The list of features, comma seperated. OfferType PRODUCT only.
    #' @param minimum_price (optional) The MAP price. OfferType PRODUCT only.
    #' @param width (optional) The width of the item. OfferType PRODUCT only.
    #' @param height (optional) The height of the item. OfferType PRODUCT only.
    #' @param depth (optional) The depth of the item. OfferType PRODUCT only.
    #' @param weight (optional) The weight of the item. OfferType PRODUCT only.
    #' @param unit (optional) The unit of measurement. OfferType PRODUCT only.
    #' @param studio (optional) The studio name. OfferType PRODUCT only.
    #' @param parental_rating (optional) The parental control rating. OfferType PRODUCT only.
    #' @param publish_date (optional) The date published. OfferType PRODUCT only.
    #' @param availability_date (optional) The date available. OfferType PRODUCT only.
    #' @param size_id (optional) 
    #' @param listing_id (optional) The ID of the event listing
    #' @param media_type (optional) the media type of the offer
    #' @param duration (optional) The total playing time of the media item. OfferType MEDIA only.
    #' @param author (optional) The created/author of the media item. OfferType MEDIA only.
    #' @param release_date (optional) The date/time of when the media item was originally released. OfferType MEDIA only.
    #' @param collection_ids (optional) 
    #' @param reboot_time_hour (optional) The reboot hour time ranging from 0 to 23
    #' @param reboot_time_minute (optional) The reboot minute time ranging from 0 to 59
    #' @param idle_timeout_in_second (optional) If the device is idle for idleTimeoutInSecond then the device should timeout
    #' @param serial_number (optional) The serial number on the device
    #' @param udid (optional) The unique device id for the device
    #' @param device_type (optional) The type of the device, for example: \"Kiosk\", \"Beacon\".
    #' @param device_power (optional) Edysen device power level setting
    #' @param device_interference (optional) Edysen device inteference setting
    #' @param availability (optional) 
    #' @param availability_summary (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerOfferResponse) with additional information such as HTTP status code, headers
    CreateOfferWithHttpInfo = function(include_offer_locations, title, barcode_type, no_expiration, available_limit, available_limit_per_user, added_limit, view_limit, max_prints, ticket_price, full_price, discount_price, offer_type, special_offer_type, offer_visibility, active, device_id = NULL, account_id = NULL, tags = NULL, parent_offer_id = NULL, retailer_location_ids = NULL, offer_locations = NULL, sub_title = NULL, details = NULL, sub_details = NULL, fine_print = NULL, barcode_entry = NULL, external_redeem_options = NULL, external_url = NULL, external_id = NULL, tickets_reward_type = NULL, tickets_reward = NULL, activated = NULL, expires = NULL, ticket_price_type = NULL, show_remaining = NULL, show_redeemed = NULL, replaced = NULL, featured = NULL, category_ids = NULL, filter_ids = NULL, barcode_asset_id = NULL, image_asset_id = NULL, image_asset_id1 = NULL, image_asset_id2 = NULL, image_asset_id3 = NULL, image_asset_id4 = NULL, image_asset_id5 = NULL, publisher = NULL, redeemable_start = NULL, redeemable_end = NULL, brand = NULL, product_type = NULL, condition_type = NULL, isbn = NULL, asin = NULL, catalog_numbers = NULL, department = NULL, features = NULL, minimum_price = NULL, width = NULL, height = NULL, depth = NULL, weight = NULL, unit = NULL, studio = NULL, parental_rating = NULL, publish_date = NULL, availability_date = NULL, size_id = NULL, listing_id = NULL, media_type = NULL, duration = NULL, author = NULL, release_date = NULL, collection_ids = NULL, reboot_time_hour = NULL, reboot_time_minute = NULL, idle_timeout_in_second = NULL, serial_number = NULL, udid = NULL, device_type = NULL, device_power = NULL, device_interference = NULL, availability = NULL, availability_summary = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`include_offer_locations`)) {
        stop("Missing required parameter `include_offer_locations`.")
      }

      if (missing(`title`)) {
        stop("Missing required parameter `title`.")
      }

      if (missing(`barcode_type`)) {
        stop("Missing required parameter `barcode_type`.")
      }

      if (missing(`no_expiration`)) {
        stop("Missing required parameter `no_expiration`.")
      }

      if (missing(`available_limit`)) {
        stop("Missing required parameter `available_limit`.")
      }

      if (missing(`available_limit_per_user`)) {
        stop("Missing required parameter `available_limit_per_user`.")
      }

      if (missing(`added_limit`)) {
        stop("Missing required parameter `added_limit`.")
      }

      if (missing(`view_limit`)) {
        stop("Missing required parameter `view_limit`.")
      }

      if (missing(`max_prints`)) {
        stop("Missing required parameter `max_prints`.")
      }

      if (missing(`ticket_price`)) {
        stop("Missing required parameter `ticket_price`.")
      }

      if (missing(`full_price`)) {
        stop("Missing required parameter `full_price`.")
      }

      if (missing(`discount_price`)) {
        stop("Missing required parameter `discount_price`.")
      }

      if (missing(`offer_type`)) {
        stop("Missing required parameter `offer_type`.")
      }

      if (missing(`special_offer_type`)) {
        stop("Missing required parameter `special_offer_type`.")
      }

      if (missing(`offer_visibility`)) {
        stop("Missing required parameter `offer_visibility`.")
      }

      if (missing(`active`)) {
        stop("Missing required parameter `active`.")
      }

      if (!missing(`include_offer_locations`) && is.null(`include_offer_locations`)) {
        stop("Invalid value for `include_offer_locations` when calling OfferApi$CreateOffer, `include_offer_locations` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling OfferApi$CreateOffer, `title` is not nullable")
      }

      if (!missing(`barcode_type`) && is.null(`barcode_type`)) {
        stop("Invalid value for `barcode_type` when calling OfferApi$CreateOffer, `barcode_type` is not nullable")
      }

      if (!missing(`no_expiration`) && is.null(`no_expiration`)) {
        stop("Invalid value for `no_expiration` when calling OfferApi$CreateOffer, `no_expiration` is not nullable")
      }

      if (!missing(`available_limit`) && is.null(`available_limit`)) {
        stop("Invalid value for `available_limit` when calling OfferApi$CreateOffer, `available_limit` is not nullable")
      }

      if (!missing(`available_limit_per_user`) && is.null(`available_limit_per_user`)) {
        stop("Invalid value for `available_limit_per_user` when calling OfferApi$CreateOffer, `available_limit_per_user` is not nullable")
      }

      if (!missing(`added_limit`) && is.null(`added_limit`)) {
        stop("Invalid value for `added_limit` when calling OfferApi$CreateOffer, `added_limit` is not nullable")
      }

      if (!missing(`view_limit`) && is.null(`view_limit`)) {
        stop("Invalid value for `view_limit` when calling OfferApi$CreateOffer, `view_limit` is not nullable")
      }

      if (!missing(`max_prints`) && is.null(`max_prints`)) {
        stop("Invalid value for `max_prints` when calling OfferApi$CreateOffer, `max_prints` is not nullable")
      }

      if (!missing(`ticket_price`) && is.null(`ticket_price`)) {
        stop("Invalid value for `ticket_price` when calling OfferApi$CreateOffer, `ticket_price` is not nullable")
      }

      if (!missing(`full_price`) && is.null(`full_price`)) {
        stop("Invalid value for `full_price` when calling OfferApi$CreateOffer, `full_price` is not nullable")
      }

      if (!missing(`discount_price`) && is.null(`discount_price`)) {
        stop("Invalid value for `discount_price` when calling OfferApi$CreateOffer, `discount_price` is not nullable")
      }

      if (!missing(`offer_type`) && is.null(`offer_type`)) {
        stop("Invalid value for `offer_type` when calling OfferApi$CreateOffer, `offer_type` is not nullable")
      }

      if (!missing(`special_offer_type`) && is.null(`special_offer_type`)) {
        stop("Invalid value for `special_offer_type` when calling OfferApi$CreateOffer, `special_offer_type` is not nullable")
      }

      if (!missing(`offer_visibility`) && is.null(`offer_visibility`)) {
        stop("Invalid value for `offer_visibility` when calling OfferApi$CreateOffer, `offer_visibility` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling OfferApi$CreateOffer, `active` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$CreateOffer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$CreateOffer, `account_id` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling OfferApi$CreateOffer, `tags` is not nullable")
      }

      if (!missing(`parent_offer_id`) && is.null(`parent_offer_id`)) {
        stop("Invalid value for `parent_offer_id` when calling OfferApi$CreateOffer, `parent_offer_id` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling OfferApi$CreateOffer, `retailer_location_ids` is not nullable")
      }

      if (!missing(`offer_locations`) && is.null(`offer_locations`)) {
        stop("Invalid value for `offer_locations` when calling OfferApi$CreateOffer, `offer_locations` is not nullable")
      }

      if (!missing(`sub_title`) && is.null(`sub_title`)) {
        stop("Invalid value for `sub_title` when calling OfferApi$CreateOffer, `sub_title` is not nullable")
      }

      if (!missing(`details`) && is.null(`details`)) {
        stop("Invalid value for `details` when calling OfferApi$CreateOffer, `details` is not nullable")
      }

      if (!missing(`sub_details`) && is.null(`sub_details`)) {
        stop("Invalid value for `sub_details` when calling OfferApi$CreateOffer, `sub_details` is not nullable")
      }

      if (!missing(`fine_print`) && is.null(`fine_print`)) {
        stop("Invalid value for `fine_print` when calling OfferApi$CreateOffer, `fine_print` is not nullable")
      }

      if (!missing(`barcode_entry`) && is.null(`barcode_entry`)) {
        stop("Invalid value for `barcode_entry` when calling OfferApi$CreateOffer, `barcode_entry` is not nullable")
      }

      if (!missing(`external_redeem_options`) && is.null(`external_redeem_options`)) {
        stop("Invalid value for `external_redeem_options` when calling OfferApi$CreateOffer, `external_redeem_options` is not nullable")
      }

      if (!missing(`external_url`) && is.null(`external_url`)) {
        stop("Invalid value for `external_url` when calling OfferApi$CreateOffer, `external_url` is not nullable")
      }

      if (!missing(`external_id`) && is.null(`external_id`)) {
        stop("Invalid value for `external_id` when calling OfferApi$CreateOffer, `external_id` is not nullable")
      }

      if (!missing(`tickets_reward_type`) && is.null(`tickets_reward_type`)) {
        stop("Invalid value for `tickets_reward_type` when calling OfferApi$CreateOffer, `tickets_reward_type` is not nullable")
      }

      if (!missing(`tickets_reward`) && is.null(`tickets_reward`)) {
        stop("Invalid value for `tickets_reward` when calling OfferApi$CreateOffer, `tickets_reward` is not nullable")
      }

      if (!missing(`activated`) && is.null(`activated`)) {
        stop("Invalid value for `activated` when calling OfferApi$CreateOffer, `activated` is not nullable")
      }

      if (!missing(`expires`) && is.null(`expires`)) {
        stop("Invalid value for `expires` when calling OfferApi$CreateOffer, `expires` is not nullable")
      }

      if (!missing(`ticket_price_type`) && is.null(`ticket_price_type`)) {
        stop("Invalid value for `ticket_price_type` when calling OfferApi$CreateOffer, `ticket_price_type` is not nullable")
      }

      if (!missing(`show_remaining`) && is.null(`show_remaining`)) {
        stop("Invalid value for `show_remaining` when calling OfferApi$CreateOffer, `show_remaining` is not nullable")
      }

      if (!missing(`show_redeemed`) && is.null(`show_redeemed`)) {
        stop("Invalid value for `show_redeemed` when calling OfferApi$CreateOffer, `show_redeemed` is not nullable")
      }

      if (!missing(`replaced`) && is.null(`replaced`)) {
        stop("Invalid value for `replaced` when calling OfferApi$CreateOffer, `replaced` is not nullable")
      }

      if (!missing(`featured`) && is.null(`featured`)) {
        stop("Invalid value for `featured` when calling OfferApi$CreateOffer, `featured` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling OfferApi$CreateOffer, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling OfferApi$CreateOffer, `filter_ids` is not nullable")
      }

      if (!missing(`barcode_asset_id`) && is.null(`barcode_asset_id`)) {
        stop("Invalid value for `barcode_asset_id` when calling OfferApi$CreateOffer, `barcode_asset_id` is not nullable")
      }

      if (!missing(`image_asset_id`) && is.null(`image_asset_id`)) {
        stop("Invalid value for `image_asset_id` when calling OfferApi$CreateOffer, `image_asset_id` is not nullable")
      }

      if (!missing(`image_asset_id1`) && is.null(`image_asset_id1`)) {
        stop("Invalid value for `image_asset_id1` when calling OfferApi$CreateOffer, `image_asset_id1` is not nullable")
      }

      if (!missing(`image_asset_id2`) && is.null(`image_asset_id2`)) {
        stop("Invalid value for `image_asset_id2` when calling OfferApi$CreateOffer, `image_asset_id2` is not nullable")
      }

      if (!missing(`image_asset_id3`) && is.null(`image_asset_id3`)) {
        stop("Invalid value for `image_asset_id3` when calling OfferApi$CreateOffer, `image_asset_id3` is not nullable")
      }

      if (!missing(`image_asset_id4`) && is.null(`image_asset_id4`)) {
        stop("Invalid value for `image_asset_id4` when calling OfferApi$CreateOffer, `image_asset_id4` is not nullable")
      }

      if (!missing(`image_asset_id5`) && is.null(`image_asset_id5`)) {
        stop("Invalid value for `image_asset_id5` when calling OfferApi$CreateOffer, `image_asset_id5` is not nullable")
      }

      if (!missing(`publisher`) && is.null(`publisher`)) {
        stop("Invalid value for `publisher` when calling OfferApi$CreateOffer, `publisher` is not nullable")
      }

      if (!missing(`redeemable_start`) && is.null(`redeemable_start`)) {
        stop("Invalid value for `redeemable_start` when calling OfferApi$CreateOffer, `redeemable_start` is not nullable")
      }

      if (!missing(`redeemable_end`) && is.null(`redeemable_end`)) {
        stop("Invalid value for `redeemable_end` when calling OfferApi$CreateOffer, `redeemable_end` is not nullable")
      }

      if (!missing(`brand`) && is.null(`brand`)) {
        stop("Invalid value for `brand` when calling OfferApi$CreateOffer, `brand` is not nullable")
      }

      if (!missing(`product_type`) && is.null(`product_type`)) {
        stop("Invalid value for `product_type` when calling OfferApi$CreateOffer, `product_type` is not nullable")
      }

      if (!missing(`condition_type`) && is.null(`condition_type`)) {
        stop("Invalid value for `condition_type` when calling OfferApi$CreateOffer, `condition_type` is not nullable")
      }

      if (!missing(`isbn`) && is.null(`isbn`)) {
        stop("Invalid value for `isbn` when calling OfferApi$CreateOffer, `isbn` is not nullable")
      }

      if (!missing(`asin`) && is.null(`asin`)) {
        stop("Invalid value for `asin` when calling OfferApi$CreateOffer, `asin` is not nullable")
      }

      if (!missing(`catalog_numbers`) && is.null(`catalog_numbers`)) {
        stop("Invalid value for `catalog_numbers` when calling OfferApi$CreateOffer, `catalog_numbers` is not nullable")
      }

      if (!missing(`department`) && is.null(`department`)) {
        stop("Invalid value for `department` when calling OfferApi$CreateOffer, `department` is not nullable")
      }

      if (!missing(`features`) && is.null(`features`)) {
        stop("Invalid value for `features` when calling OfferApi$CreateOffer, `features` is not nullable")
      }

      if (!missing(`minimum_price`) && is.null(`minimum_price`)) {
        stop("Invalid value for `minimum_price` when calling OfferApi$CreateOffer, `minimum_price` is not nullable")
      }

      if (!missing(`width`) && is.null(`width`)) {
        stop("Invalid value for `width` when calling OfferApi$CreateOffer, `width` is not nullable")
      }

      if (!missing(`height`) && is.null(`height`)) {
        stop("Invalid value for `height` when calling OfferApi$CreateOffer, `height` is not nullable")
      }

      if (!missing(`depth`) && is.null(`depth`)) {
        stop("Invalid value for `depth` when calling OfferApi$CreateOffer, `depth` is not nullable")
      }

      if (!missing(`weight`) && is.null(`weight`)) {
        stop("Invalid value for `weight` when calling OfferApi$CreateOffer, `weight` is not nullable")
      }

      if (!missing(`unit`) && is.null(`unit`)) {
        stop("Invalid value for `unit` when calling OfferApi$CreateOffer, `unit` is not nullable")
      }

      if (!missing(`studio`) && is.null(`studio`)) {
        stop("Invalid value for `studio` when calling OfferApi$CreateOffer, `studio` is not nullable")
      }

      if (!missing(`parental_rating`) && is.null(`parental_rating`)) {
        stop("Invalid value for `parental_rating` when calling OfferApi$CreateOffer, `parental_rating` is not nullable")
      }

      if (!missing(`publish_date`) && is.null(`publish_date`)) {
        stop("Invalid value for `publish_date` when calling OfferApi$CreateOffer, `publish_date` is not nullable")
      }

      if (!missing(`availability_date`) && is.null(`availability_date`)) {
        stop("Invalid value for `availability_date` when calling OfferApi$CreateOffer, `availability_date` is not nullable")
      }

      if (!missing(`size_id`) && is.null(`size_id`)) {
        stop("Invalid value for `size_id` when calling OfferApi$CreateOffer, `size_id` is not nullable")
      }

      if (!missing(`listing_id`) && is.null(`listing_id`)) {
        stop("Invalid value for `listing_id` when calling OfferApi$CreateOffer, `listing_id` is not nullable")
      }

      if (!missing(`media_type`) && is.null(`media_type`)) {
        stop("Invalid value for `media_type` when calling OfferApi$CreateOffer, `media_type` is not nullable")
      }

      if (!missing(`duration`) && is.null(`duration`)) {
        stop("Invalid value for `duration` when calling OfferApi$CreateOffer, `duration` is not nullable")
      }

      if (!missing(`author`) && is.null(`author`)) {
        stop("Invalid value for `author` when calling OfferApi$CreateOffer, `author` is not nullable")
      }

      if (!missing(`release_date`) && is.null(`release_date`)) {
        stop("Invalid value for `release_date` when calling OfferApi$CreateOffer, `release_date` is not nullable")
      }

      if (!missing(`collection_ids`) && is.null(`collection_ids`)) {
        stop("Invalid value for `collection_ids` when calling OfferApi$CreateOffer, `collection_ids` is not nullable")
      }

      if (!missing(`reboot_time_hour`) && is.null(`reboot_time_hour`)) {
        stop("Invalid value for `reboot_time_hour` when calling OfferApi$CreateOffer, `reboot_time_hour` is not nullable")
      }

      if (!missing(`reboot_time_minute`) && is.null(`reboot_time_minute`)) {
        stop("Invalid value for `reboot_time_minute` when calling OfferApi$CreateOffer, `reboot_time_minute` is not nullable")
      }

      if (!missing(`idle_timeout_in_second`) && is.null(`idle_timeout_in_second`)) {
        stop("Invalid value for `idle_timeout_in_second` when calling OfferApi$CreateOffer, `idle_timeout_in_second` is not nullable")
      }

      if (!missing(`serial_number`) && is.null(`serial_number`)) {
        stop("Invalid value for `serial_number` when calling OfferApi$CreateOffer, `serial_number` is not nullable")
      }

      if (!missing(`udid`) && is.null(`udid`)) {
        stop("Invalid value for `udid` when calling OfferApi$CreateOffer, `udid` is not nullable")
      }

      if (!missing(`device_type`) && is.null(`device_type`)) {
        stop("Invalid value for `device_type` when calling OfferApi$CreateOffer, `device_type` is not nullable")
      }

      if (!missing(`device_power`) && is.null(`device_power`)) {
        stop("Invalid value for `device_power` when calling OfferApi$CreateOffer, `device_power` is not nullable")
      }

      if (!missing(`device_interference`) && is.null(`device_interference`)) {
        stop("Invalid value for `device_interference` when calling OfferApi$CreateOffer, `device_interference` is not nullable")
      }

      if (!missing(`availability`) && is.null(`availability`)) {
        stop("Invalid value for `availability` when calling OfferApi$CreateOffer, `availability` is not nullable")
      }

      if (!missing(`availability_summary`) && is.null(`availability_summary`)) {
        stop("Invalid value for `availability_summary` when calling OfferApi$CreateOffer, `availability_summary` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["tags"]] <- `tags`

      query_params[["parentOfferId"]] <- `parent_offer_id`

      query_params[["includeOfferLocations"]] <- `include_offer_locations`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["offerLocations"]] <- `offer_locations`

      query_params[["title"]] <- `title`

      query_params[["subTitle"]] <- `sub_title`

      query_params[["details"]] <- `details`

      query_params[["subDetails"]] <- `sub_details`

      query_params[["finePrint"]] <- `fine_print`

      if (!is.null(`barcode_type`) && !(`barcode_type` %in% c("NONE", "UPC", "CODE_128", "QR", "CUSTOM_MEDIA"))) {
        stop("Invalid value for barcode_type when calling OfferApi$CreateOffer. Must be [NONE, UPC, CODE_128, QR, CUSTOM_MEDIA].")
      }
      query_params[["barcodeType"]] <- `barcode_type`

      query_params[["barcodeEntry"]] <- `barcode_entry`

      query_params[["externalRedeemOptions"]] <- `external_redeem_options`

      query_params[["externalUrl"]] <- `external_url`

      query_params[["externalId"]] <- `external_id`

      query_params[["ticketsRewardType"]] <- `tickets_reward_type`

      query_params[["ticketsReward"]] <- `tickets_reward`

      query_params[["activated"]] <- `activated`

      query_params[["expires"]] <- `expires`

      query_params[["noExpiration"]] <- `no_expiration`

      query_params[["availableLimit"]] <- `available_limit`

      query_params[["availableLimitPerUser"]] <- `available_limit_per_user`

      query_params[["addedLimit"]] <- `added_limit`

      query_params[["viewLimit"]] <- `view_limit`

      query_params[["maxPrints"]] <- `max_prints`

      query_params[["ticketPriceType"]] <- `ticket_price_type`

      query_params[["ticketPrice"]] <- `ticket_price`

      query_params[["fullPrice"]] <- `full_price`

      query_params[["discountPrice"]] <- `discount_price`

      query_params[["showRemaining"]] <- `show_remaining`

      query_params[["showRedeemed"]] <- `show_redeemed`

      query_params[["replaced"]] <- `replaced`

      query_params[["featured"]] <- `featured`

      if (!is.null(`offer_type`) && !(`offer_type` %in% c("VOUCHER", "COUPON", "PRODUCT", "MEDIA", "EVENT", "DEVICE"))) {
        stop("Invalid value for offer_type when calling OfferApi$CreateOffer. Must be [VOUCHER, COUPON, PRODUCT, MEDIA, EVENT, DEVICE].")
      }
      query_params[["offerType"]] <- `offer_type`

      if (!is.null(`special_offer_type`) && !(`special_offer_type` %in% c("ALL", "RESERVABLE", "REGULAR_OFFER", "ACT_NOW", "GET_THERE_NOW", "SQOOT", "TICKETS", "YIPIT"))) {
        stop("Invalid value for special_offer_type when calling OfferApi$CreateOffer. Must be [ALL, RESERVABLE, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW, SQOOT, TICKETS, YIPIT].")
      }
      query_params[["specialOfferType"]] <- `special_offer_type`

      if (!is.null(`offer_visibility`) && !(`offer_visibility` %in% c("PUBLIC", "LISTABLE", "REWARDABLE", "TRIGGERABLE", "PRIVATE"))) {
        stop("Invalid value for offer_visibility when calling OfferApi$CreateOffer. Must be [PUBLIC, LISTABLE, REWARDABLE, TRIGGERABLE, PRIVATE].")
      }
      query_params[["offerVisibility"]] <- `offer_visibility`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["active"]] <- `active`

      query_params[["barcodeAssetId"]] <- `barcode_asset_id`

      query_params[["imageAssetId"]] <- `image_asset_id`

      query_params[["imageAssetId1"]] <- `image_asset_id1`

      query_params[["imageAssetId2"]] <- `image_asset_id2`

      query_params[["imageAssetId3"]] <- `image_asset_id3`

      query_params[["imageAssetId4"]] <- `image_asset_id4`

      query_params[["imageAssetId5"]] <- `image_asset_id5`

      query_params[["publisher"]] <- `publisher`

      query_params[["redeemableStart"]] <- `redeemable_start`

      query_params[["redeemableEnd"]] <- `redeemable_end`

      query_params[["brand"]] <- `brand`

      if (!is.null(`product_type`) && !(`product_type` %in% c("APPAREL", "APPLIANCES", "APPS", "ARTS_CRAFTS", "AUTOMOTIVE", "BABY", "BEAUTY", "BOOKS", "CLASSICAL", "COLLECTIBLES", "DVD", "ELECTRONICS", "GIFT_CARDS", "GOURMET_FOOD", "GROCERY", "HEALTH_PERSONAL_CARE", "HOME_GARDEN", "INDUSTRIAL", "JEWELRY", "KITCHEN", "LAWN_GARDEN", "MAGAZINES", "MISCELLANEOUS", "MUSICAL_INSTRUMENTS", "OFFICE_PRODUCTS", "OTHER", "OUTDOOR_LIVING", "PC_HARDWARE", "PET_SUPPLIES", "SHOES", "SOFTWARE", "SPORTING_GOODS", "TOOLS", "TOYS", "VIDEO_GAMES", "WATCHES", "WIRELESS", "WIRELESS_ACCESSORIES"))) {
        stop("Invalid value for product_type when calling OfferApi$CreateOffer. Must be [APPAREL, APPLIANCES, APPS, ARTS_CRAFTS, AUTOMOTIVE, BABY, BEAUTY, BOOKS, CLASSICAL, COLLECTIBLES, DVD, ELECTRONICS, GIFT_CARDS, GOURMET_FOOD, GROCERY, HEALTH_PERSONAL_CARE, HOME_GARDEN, INDUSTRIAL, JEWELRY, KITCHEN, LAWN_GARDEN, MAGAZINES, MISCELLANEOUS, MUSICAL_INSTRUMENTS, OFFICE_PRODUCTS, OTHER, OUTDOOR_LIVING, PC_HARDWARE, PET_SUPPLIES, SHOES, SOFTWARE, SPORTING_GOODS, TOOLS, TOYS, VIDEO_GAMES, WATCHES, WIRELESS, WIRELESS_ACCESSORIES].")
      }
      query_params[["productType"]] <- `product_type`

      if (!is.null(`condition_type`) && !(`condition_type` %in% c("NEW", "USED", "REFURBISHED", "OTHER"))) {
        stop("Invalid value for condition_type when calling OfferApi$CreateOffer. Must be [NEW, USED, REFURBISHED, OTHER].")
      }
      query_params[["conditionType"]] <- `condition_type`

      query_params[["isbn"]] <- `isbn`

      query_params[["asin"]] <- `asin`

      query_params[["catalogNumbers"]] <- `catalog_numbers`

      query_params[["department"]] <- `department`

      query_params[["features"]] <- `features`

      query_params[["minimumPrice"]] <- `minimum_price`

      query_params[["width"]] <- `width`

      query_params[["height"]] <- `height`

      query_params[["depth"]] <- `depth`

      query_params[["weight"]] <- `weight`

      if (!is.null(`unit`) && !(`unit` %in% c("MILIMETER", "CENTIMETER", "METER", "INCH", "FOOT", "YARD"))) {
        stop("Invalid value for unit when calling OfferApi$CreateOffer. Must be [MILIMETER, CENTIMETER, METER, INCH, FOOT, YARD].")
      }
      query_params[["unit"]] <- `unit`

      query_params[["studio"]] <- `studio`

      query_params[["parentalRating"]] <- `parental_rating`

      query_params[["publishDate"]] <- `publish_date`

      query_params[["availabilityDate"]] <- `availability_date`

      query_params[["sizeId"]] <- `size_id`

      query_params[["listingId"]] <- `listing_id`

      if (!is.null(`media_type`) && !(`media_type` %in% c("APK", "APPLICATION", "IMAGE", "AUDIO", "VIDEO", "MULTIPART", "BAR_CODE", "TEXT", "OTHER"))) {
        stop("Invalid value for media_type when calling OfferApi$CreateOffer. Must be [APK, APPLICATION, IMAGE, AUDIO, VIDEO, MULTIPART, BAR_CODE, TEXT, OTHER].")
      }
      query_params[["mediaType"]] <- `media_type`

      query_params[["duration"]] <- `duration`

      query_params[["author"]] <- `author`

      query_params[["releaseDate"]] <- `release_date`

      query_params[["collectionIds"]] <- `collection_ids`

      query_params[["rebootTimeHour"]] <- `reboot_time_hour`

      query_params[["rebootTimeMinute"]] <- `reboot_time_minute`

      query_params[["idleTimeoutInSecond"]] <- `idle_timeout_in_second`

      query_params[["serialNumber"]] <- `serial_number`

      query_params[["udid"]] <- `udid`

      query_params[["deviceType"]] <- `device_type`

      query_params[["devicePower"]] <- `device_power`

      query_params[["deviceInterference"]] <- `device_interference`

      query_params[["availability"]] <- `availability`

      query_params[["availabilitySummary"]] <- `availability_summary`

      local_var_url_path <- "/retailer/offer/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerOfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Offer
    #'
    #' @param offer_id The ID of the offer to be deleted
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the offer.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteOffer = function(offer_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteOfferWithHttpInfo(offer_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Offer
    #'
    #' @param offer_id The ID of the offer to be deleted
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the offer.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteOfferWithHttpInfo = function(offer_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offer_id`)) {
        stop("Missing required parameter `offer_id`.")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling OfferApi$DeleteOffer, `offer_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$DeleteOffer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$DeleteOffer, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["offerId"]] <- `offer_id`

      local_var_url_path <- "/retailer/offer/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Offer Location
    #'
    #' @param offer_location_id The ID of the offer location to be deleted
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the offer location.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteOfferLocation = function(offer_location_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteOfferLocationWithHttpInfo(offer_location_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Offer Location
    #'
    #' @param offer_location_id The ID of the offer location to be deleted
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the offer location.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteOfferLocationWithHttpInfo = function(offer_location_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offer_location_id`)) {
        stop("Missing required parameter `offer_location_id`.")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling OfferApi$DeleteOfferLocation, `offer_location_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$DeleteOfferLocation, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$DeleteOfferLocation, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      local_var_url_path <- "/retailer/offer/location/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Offer
    #'
    #' @param offer_id The id of the offer
    #' @param include_offer_locations 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerOfferResponse
    GetOffer = function(offer_id, include_offer_locations, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetOfferWithHttpInfo(offer_id, include_offer_locations, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Offer
    #'
    #' @param offer_id The id of the offer
    #' @param include_offer_locations 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerOfferResponse) with additional information such as HTTP status code, headers
    GetOfferWithHttpInfo = function(offer_id, include_offer_locations, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offer_id`)) {
        stop("Missing required parameter `offer_id`.")
      }

      if (missing(`include_offer_locations`)) {
        stop("Missing required parameter `include_offer_locations`.")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling OfferApi$GetOffer, `offer_id` is not nullable")
      }

      if (!missing(`include_offer_locations`) && is.null(`include_offer_locations`)) {
        stop("Invalid value for `include_offer_locations` when calling OfferApi$GetOffer, `include_offer_locations` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$GetOffer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$GetOffer, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["includeOfferLocations"]] <- `include_offer_locations`

      local_var_url_path <- "/retailer/offer/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerOfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Offer
    #'
    #' @param device_id (optional) The device id for returning account information (i.e. favorites)
    #' @param account_id (optional) The account id for returning account information (i.e. favorites)
    #' @param offer_id (optional) The offer id (either offeLocationId or offerId must be provided)
    #' @param offer_location_id (optional) The offer location id (either offeLocationId or offerId must be provided)
    #' @param distance (optional) The distance of the offer from the user's current location (this is returned when the offer is searched)
    #' @param latitude (optional) The latitude to calculate distance from the offer
    #' @param longitude (optional) The longitude to calculate distance from the offer
    #' @param include_offer_locations (optional) Determines whether to return offer locations for the offer (default value: FALSE)
    #' @param include_retailer_locations (optional) Determines whether to return the retailer location info for each offer location response (includeOfferLocations must also be true for this to work) (default value: FALSE)
    #' @param include_child_offers (optional) Determines whether to include child offers in the response (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferResponse
    GetOfferDetails = function(device_id = NULL, account_id = NULL, offer_id = NULL, offer_location_id = NULL, distance = NULL, latitude = NULL, longitude = NULL, include_offer_locations = FALSE, include_retailer_locations = FALSE, include_child_offers = FALSE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetOfferDetailsWithHttpInfo(device_id, account_id, offer_id, offer_location_id, distance, latitude, longitude, include_offer_locations, include_retailer_locations, include_child_offers, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Offer
    #'
    #' @param device_id (optional) The device id for returning account information (i.e. favorites)
    #' @param account_id (optional) The account id for returning account information (i.e. favorites)
    #' @param offer_id (optional) The offer id (either offeLocationId or offerId must be provided)
    #' @param offer_location_id (optional) The offer location id (either offeLocationId or offerId must be provided)
    #' @param distance (optional) The distance of the offer from the user's current location (this is returned when the offer is searched)
    #' @param latitude (optional) The latitude to calculate distance from the offer
    #' @param longitude (optional) The longitude to calculate distance from the offer
    #' @param include_offer_locations (optional) Determines whether to return offer locations for the offer (default value: FALSE)
    #' @param include_retailer_locations (optional) Determines whether to return the retailer location info for each offer location response (includeOfferLocations must also be true for this to work) (default value: FALSE)
    #' @param include_child_offers (optional) Determines whether to include child offers in the response (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferResponse) with additional information such as HTTP status code, headers
    GetOfferDetailsWithHttpInfo = function(device_id = NULL, account_id = NULL, offer_id = NULL, offer_location_id = NULL, distance = NULL, latitude = NULL, longitude = NULL, include_offer_locations = FALSE, include_retailer_locations = FALSE, include_child_offers = FALSE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$GetOfferDetails, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$GetOfferDetails, `account_id` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling OfferApi$GetOfferDetails, `offer_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling OfferApi$GetOfferDetails, `offer_location_id` is not nullable")
      }

      if (!missing(`distance`) && is.null(`distance`)) {
        stop("Invalid value for `distance` when calling OfferApi$GetOfferDetails, `distance` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling OfferApi$GetOfferDetails, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling OfferApi$GetOfferDetails, `longitude` is not nullable")
      }

      if (!missing(`include_offer_locations`) && is.null(`include_offer_locations`)) {
        stop("Invalid value for `include_offer_locations` when calling OfferApi$GetOfferDetails, `include_offer_locations` is not nullable")
      }

      if (!missing(`include_retailer_locations`) && is.null(`include_retailer_locations`)) {
        stop("Invalid value for `include_retailer_locations` when calling OfferApi$GetOfferDetails, `include_retailer_locations` is not nullable")
      }

      if (!missing(`include_child_offers`) && is.null(`include_child_offers`)) {
        stop("Invalid value for `include_child_offers` when calling OfferApi$GetOfferDetails, `include_child_offers` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["distance"]] <- `distance`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["includeOfferLocations"]] <- `include_offer_locations`

      query_params[["includeRetailerLocations"]] <- `include_retailer_locations`

      query_params[["includeChildOffers"]] <- `include_child_offers`

      local_var_url_path <- "/offer/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Offers (Counts)
    #'
    #' @param latitude The latitude of where the search will center at
    #' @param longitude The longitude of where the search will center at
    #' @param search_range (optional) The range of the search (default value: 5)
    #' @param distance_unit (optional) The units to use for distance calculations (e.g. MILES, KILOMETERS) (default value: "MILES")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ListCountResponse
    GetOfferListCounts = function(latitude, longitude, search_range = 5, distance_unit = "MILES", data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetOfferListCountsWithHttpInfo(latitude, longitude, search_range, distance_unit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Offers (Counts)
    #'
    #' @param latitude The latitude of where the search will center at
    #' @param longitude The longitude of where the search will center at
    #' @param search_range (optional) The range of the search (default value: 5)
    #' @param distance_unit (optional) The units to use for distance calculations (e.g. MILES, KILOMETERS) (default value: "MILES")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ListCountResponse) with additional information such as HTTP status code, headers
    GetOfferListCountsWithHttpInfo = function(latitude, longitude, search_range = 5, distance_unit = "MILES", data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`latitude`)) {
        stop("Missing required parameter `latitude`.")
      }

      if (missing(`longitude`)) {
        stop("Missing required parameter `longitude`.")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling OfferApi$GetOfferListCounts, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling OfferApi$GetOfferListCounts, `longitude` is not nullable")
      }

      if (!missing(`search_range`) && is.null(`search_range`)) {
        stop("Invalid value for `search_range` when calling OfferApi$GetOfferListCounts, `search_range` is not nullable")
      }

      if (!missing(`distance_unit`) && is.null(`distance_unit`)) {
        stop("Invalid value for `distance_unit` when calling OfferApi$GetOfferListCounts, `distance_unit` is not nullable")
      }

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["searchRange"]] <- `search_range`

      if (!is.null(`distance_unit`) && !(`distance_unit` %in% c("MILES", "KILOMETERS"))) {
        stop("Invalid value for distance_unit when calling OfferApi$GetOfferListCounts. Must be [MILES, KILOMETERS].")
      }
      query_params[["distanceUnit"]] <- `distance_unit`

      local_var_url_path <- "/offer/lists/count"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ListCountResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Offer Location
    #'
    #' @param offer_location_id (optional) the id of the offer location to get
    #' @param udid (optional) the UDID of the device
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferShortResponse
    GetOfferLocation = function(offer_location_id = NULL, udid = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetOfferLocationWithHttpInfo(offer_location_id, udid, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Offer Location
    #'
    #' @param offer_location_id (optional) the id of the offer location to get
    #' @param udid (optional) the UDID of the device
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferShortResponse) with additional information such as HTTP status code, headers
    GetOfferLocationWithHttpInfo = function(offer_location_id = NULL, udid = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling OfferApi$GetOfferLocation, `offer_location_id` is not nullable")
      }

      if (!missing(`udid`) && is.null(`udid`)) {
        stop("Invalid value for `udid` when calling OfferApi$GetOfferLocation, `udid` is not nullable")
      }

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["udid"]] <- `udid`

      local_var_url_path <- "/offer/location/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferShortResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Offer Locations
    #'
    #' @param sort_field The column to sort the results on. Default is \"TITLE\", which will sort the results by the offer title. Possible input values: {CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, RETAILER_ID,RETAILER_LOCATION_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY}
    #' @param descending The order to return the results. Default is false, which will return the results in ascending order.
    #' @param start The index into the record set to start with. Default is 0.
    #' @param limit The total number of records to return. Default is 20.
    #' @param active_only Determines whether to return only active results. Default is false.
    #' @param include_retailer_location 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param keyword (optional) The keyword used to search
    #' @param retailer_id (optional) Filter results for a specific retailer
    #' @param retailer_location_id (optional) Filter results for a specific retailer location
    #' @param offer_type (optional) Filter results to only return COUPON, VOUCHER, PRODUCT, MEDIA, or EVENT offers
    #' @param special_offer_type (optional) Filter results to only return REGULAR_OFFER, ACT_NOW, GET_THERE_NOW offer specials
    #' @param barcode_type (optional) 
    #' @param barcode_entry (optional) 
    #' @param isbn (optional) 
    #' @param asin (optional) 
    #' @param device_status (optional) Edysen device status, running, warning, or down
    #' @param needs_notification_sent (optional) 
    #' @param last_notification_sent (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[OfferShortResponse]
    GetOfferLocationsForRetailers = function(sort_field, descending, start, limit, active_only, include_retailer_location, device_id = NULL, account_id = NULL, keyword = NULL, retailer_id = NULL, retailer_location_id = NULL, offer_type = NULL, special_offer_type = NULL, barcode_type = NULL, barcode_entry = NULL, isbn = NULL, asin = NULL, device_status = NULL, needs_notification_sent = NULL, last_notification_sent = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetOfferLocationsForRetailersWithHttpInfo(sort_field, descending, start, limit, active_only, include_retailer_location, device_id, account_id, keyword, retailer_id, retailer_location_id, offer_type, special_offer_type, barcode_type, barcode_entry, isbn, asin, device_status, needs_notification_sent, last_notification_sent, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Offer Locations
    #'
    #' @param sort_field The column to sort the results on. Default is \"TITLE\", which will sort the results by the offer title. Possible input values: {CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, RETAILER_ID,RETAILER_LOCATION_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY}
    #' @param descending The order to return the results. Default is false, which will return the results in ascending order.
    #' @param start The index into the record set to start with. Default is 0.
    #' @param limit The total number of records to return. Default is 20.
    #' @param active_only Determines whether to return only active results. Default is false.
    #' @param include_retailer_location 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param keyword (optional) The keyword used to search
    #' @param retailer_id (optional) Filter results for a specific retailer
    #' @param retailer_location_id (optional) Filter results for a specific retailer location
    #' @param offer_type (optional) Filter results to only return COUPON, VOUCHER, PRODUCT, MEDIA, or EVENT offers
    #' @param special_offer_type (optional) Filter results to only return REGULAR_OFFER, ACT_NOW, GET_THERE_NOW offer specials
    #' @param barcode_type (optional) 
    #' @param barcode_entry (optional) 
    #' @param isbn (optional) 
    #' @param asin (optional) 
    #' @param device_status (optional) Edysen device status, running, warning, or down
    #' @param needs_notification_sent (optional) 
    #' @param last_notification_sent (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[OfferShortResponse]) with additional information such as HTTP status code, headers
    GetOfferLocationsForRetailersWithHttpInfo = function(sort_field, descending, start, limit, active_only, include_retailer_location, device_id = NULL, account_id = NULL, keyword = NULL, retailer_id = NULL, retailer_location_id = NULL, offer_type = NULL, special_offer_type = NULL, barcode_type = NULL, barcode_entry = NULL, isbn = NULL, asin = NULL, device_status = NULL, needs_notification_sent = NULL, last_notification_sent = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`active_only`)) {
        stop("Missing required parameter `active_only`.")
      }

      if (missing(`include_retailer_location`)) {
        stop("Missing required parameter `include_retailer_location`.")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling OfferApi$GetOfferLocationsForRetailers, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling OfferApi$GetOfferLocationsForRetailers, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling OfferApi$GetOfferLocationsForRetailers, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling OfferApi$GetOfferLocationsForRetailers, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling OfferApi$GetOfferLocationsForRetailers, `active_only` is not nullable")
      }

      if (!missing(`include_retailer_location`) && is.null(`include_retailer_location`)) {
        stop("Invalid value for `include_retailer_location` when calling OfferApi$GetOfferLocationsForRetailers, `include_retailer_location` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$GetOfferLocationsForRetailers, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$GetOfferLocationsForRetailers, `account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling OfferApi$GetOfferLocationsForRetailers, `keyword` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling OfferApi$GetOfferLocationsForRetailers, `retailer_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling OfferApi$GetOfferLocationsForRetailers, `retailer_location_id` is not nullable")
      }

      if (!missing(`offer_type`) && is.null(`offer_type`)) {
        stop("Invalid value for `offer_type` when calling OfferApi$GetOfferLocationsForRetailers, `offer_type` is not nullable")
      }

      if (!missing(`special_offer_type`) && is.null(`special_offer_type`)) {
        stop("Invalid value for `special_offer_type` when calling OfferApi$GetOfferLocationsForRetailers, `special_offer_type` is not nullable")
      }

      if (!missing(`barcode_type`) && is.null(`barcode_type`)) {
        stop("Invalid value for `barcode_type` when calling OfferApi$GetOfferLocationsForRetailers, `barcode_type` is not nullable")
      }

      if (!missing(`barcode_entry`) && is.null(`barcode_entry`)) {
        stop("Invalid value for `barcode_entry` when calling OfferApi$GetOfferLocationsForRetailers, `barcode_entry` is not nullable")
      }

      if (!missing(`isbn`) && is.null(`isbn`)) {
        stop("Invalid value for `isbn` when calling OfferApi$GetOfferLocationsForRetailers, `isbn` is not nullable")
      }

      if (!missing(`asin`) && is.null(`asin`)) {
        stop("Invalid value for `asin` when calling OfferApi$GetOfferLocationsForRetailers, `asin` is not nullable")
      }

      if (!missing(`device_status`) && is.null(`device_status`)) {
        stop("Invalid value for `device_status` when calling OfferApi$GetOfferLocationsForRetailers, `device_status` is not nullable")
      }

      if (!missing(`needs_notification_sent`) && is.null(`needs_notification_sent`)) {
        stop("Invalid value for `needs_notification_sent` when calling OfferApi$GetOfferLocationsForRetailers, `needs_notification_sent` is not nullable")
      }

      if (!missing(`last_notification_sent`) && is.null(`last_notification_sent`)) {
        stop("Invalid value for `last_notification_sent` when calling OfferApi$GetOfferLocationsForRetailers, `last_notification_sent` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      if (!is.null(`offer_type`) && !(`offer_type` %in% c("VOUCHER", "COUPON", "PRODUCT", "MEDIA", "EVENT", "DEVICE"))) {
        stop("Invalid value for offer_type when calling OfferApi$GetOfferLocationsForRetailers. Must be [VOUCHER, COUPON, PRODUCT, MEDIA, EVENT, DEVICE].")
      }
      query_params[["offerType"]] <- `offer_type`

      if (!is.null(`special_offer_type`) && !(`special_offer_type` %in% c("ALL", "RESERVABLE", "REGULAR_OFFER", "ACT_NOW", "GET_THERE_NOW", "SQOOT", "TICKETS", "YIPIT"))) {
        stop("Invalid value for special_offer_type when calling OfferApi$GetOfferLocationsForRetailers. Must be [ALL, RESERVABLE, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW, SQOOT, TICKETS, YIPIT].")
      }
      query_params[["specialOfferType"]] <- `special_offer_type`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "OFFER_ID", "ACTIVATED", "EXPIRES", "TITLE", "SUBTITLE", "DETAILS", "OFFER_TYPE", "SPECIAL_OFFER_TYPE", "OFFER_VISIBILITY", "RETAILER_ID", "RETAILER_LOCATION_ID", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY"))) {
        stop("Invalid value for sort_field when calling OfferApi$GetOfferLocationsForRetailers. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, OFFER_ID, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, RETAILER_ID, RETAILER_LOCATION_ID, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["includeRetailerLocation"]] <- `include_retailer_location`

      query_params[["barcodeType"]] <- `barcode_type`

      query_params[["barcodeEntry"]] <- `barcode_entry`

      query_params[["isbn"]] <- `isbn`

      query_params[["asin"]] <- `asin`

      if (!is.null(`device_status`) && !(`device_status` %in% c("RUNNING", "WARNING", "DOWN"))) {
        stop("Invalid value for device_status when calling OfferApi$GetOfferLocationsForRetailers. Must be [RUNNING, WARNING, DOWN].")
      }
      query_params[["deviceStatus"]] <- `device_status`

      query_params[["needsNotificationSent"]] <- `needs_notification_sent`

      query_params[["lastNotificationSent"]] <- `last_notification_sent`

      local_var_url_path <- "/retailer/offer/location/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[OfferShortResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Offers
    #'
    #' @param offer_visibility 
    #' @param sort_field The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY
    #' @param descending The order to return the search results
    #' @param start The record to begin the return set on
    #' @param limit The number of records to return
    #' @param available_only Return only results that are currently being promoted (is activated and not expired)
    #' @param active_only Return only active results
    #' @param include_categories 
    #' @param include_filters 
    #' @param include_offer_locations 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param category_ids (optional) 
    #' @param filter_ids (optional) 
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) The keyword used to search
    #' @param retailer_id (optional) the id of the retailer
    #' @param retailer_location_id (optional) the id of the retailer location
    #' @param coupon_type (optional) This parameter is deprecated.
    #' @param offer_type (optional) This parameter is deprecated. Use offer types. Filter results to only return COUPON, VOUCHER, PRODUCT, MEDIA, or EVENT offers
    #' @param offer_types (optional) Comma separated list of offer types {COUPON, VOUCHER, PRODUCT, MEDIA, EVENT, DEVICE}
    #' @param special_offer_type (optional) Filter results to only return REGULAR_OFFER, ACT_NOW, GET_THERE_NOW offer specials
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param barcode_type (optional) 
    #' @param barcode_entry (optional) 
    #' @param isbn (optional) 
    #' @param asin (optional) 
    #' @param device_status (optional) Edysen device status
    #' @param needs_notification_sent (optional) 
    #' @param last_notification_sent (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[OfferResponse]
    GetOffersForRetailers = function(offer_visibility, sort_field, descending, start, limit, available_only, active_only, include_categories, include_filters, include_offer_locations, device_id = NULL, account_id = NULL, category_ids = NULL, filter_ids = NULL, q = NULL, keyword = NULL, retailer_id = NULL, retailer_location_id = NULL, coupon_type = NULL, offer_type = NULL, offer_types = NULL, special_offer_type = NULL, _i = NULL, _l = NULL, barcode_type = NULL, barcode_entry = NULL, isbn = NULL, asin = NULL, device_status = NULL, needs_notification_sent = NULL, last_notification_sent = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetOffersForRetailersWithHttpInfo(offer_visibility, sort_field, descending, start, limit, available_only, active_only, include_categories, include_filters, include_offer_locations, device_id, account_id, category_ids, filter_ids, q, keyword, retailer_id, retailer_location_id, coupon_type, offer_type, offer_types, special_offer_type, _i, _l, barcode_type, barcode_entry, isbn, asin, device_status, needs_notification_sent, last_notification_sent, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Offers
    #'
    #' @param offer_visibility 
    #' @param sort_field The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY
    #' @param descending The order to return the search results
    #' @param start The record to begin the return set on
    #' @param limit The number of records to return
    #' @param available_only Return only results that are currently being promoted (is activated and not expired)
    #' @param active_only Return only active results
    #' @param include_categories 
    #' @param include_filters 
    #' @param include_offer_locations 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param category_ids (optional) 
    #' @param filter_ids (optional) 
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) The keyword used to search
    #' @param retailer_id (optional) the id of the retailer
    #' @param retailer_location_id (optional) the id of the retailer location
    #' @param coupon_type (optional) This parameter is deprecated.
    #' @param offer_type (optional) This parameter is deprecated. Use offer types. Filter results to only return COUPON, VOUCHER, PRODUCT, MEDIA, or EVENT offers
    #' @param offer_types (optional) Comma separated list of offer types {COUPON, VOUCHER, PRODUCT, MEDIA, EVENT, DEVICE}
    #' @param special_offer_type (optional) Filter results to only return REGULAR_OFFER, ACT_NOW, GET_THERE_NOW offer specials
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param barcode_type (optional) 
    #' @param barcode_entry (optional) 
    #' @param isbn (optional) 
    #' @param asin (optional) 
    #' @param device_status (optional) Edysen device status
    #' @param needs_notification_sent (optional) 
    #' @param last_notification_sent (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[OfferResponse]) with additional information such as HTTP status code, headers
    GetOffersForRetailersWithHttpInfo = function(offer_visibility, sort_field, descending, start, limit, available_only, active_only, include_categories, include_filters, include_offer_locations, device_id = NULL, account_id = NULL, category_ids = NULL, filter_ids = NULL, q = NULL, keyword = NULL, retailer_id = NULL, retailer_location_id = NULL, coupon_type = NULL, offer_type = NULL, offer_types = NULL, special_offer_type = NULL, _i = NULL, _l = NULL, barcode_type = NULL, barcode_entry = NULL, isbn = NULL, asin = NULL, device_status = NULL, needs_notification_sent = NULL, last_notification_sent = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offer_visibility`)) {
        stop("Missing required parameter `offer_visibility`.")
      }

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`available_only`)) {
        stop("Missing required parameter `available_only`.")
      }

      if (missing(`active_only`)) {
        stop("Missing required parameter `active_only`.")
      }

      if (missing(`include_categories`)) {
        stop("Missing required parameter `include_categories`.")
      }

      if (missing(`include_filters`)) {
        stop("Missing required parameter `include_filters`.")
      }

      if (missing(`include_offer_locations`)) {
        stop("Missing required parameter `include_offer_locations`.")
      }

      if (!missing(`offer_visibility`) && is.null(`offer_visibility`)) {
        stop("Invalid value for `offer_visibility` when calling OfferApi$GetOffersForRetailers, `offer_visibility` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling OfferApi$GetOffersForRetailers, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling OfferApi$GetOffersForRetailers, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling OfferApi$GetOffersForRetailers, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling OfferApi$GetOffersForRetailers, `limit` is not nullable")
      }

      if (!missing(`available_only`) && is.null(`available_only`)) {
        stop("Invalid value for `available_only` when calling OfferApi$GetOffersForRetailers, `available_only` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling OfferApi$GetOffersForRetailers, `active_only` is not nullable")
      }

      if (!missing(`include_categories`) && is.null(`include_categories`)) {
        stop("Invalid value for `include_categories` when calling OfferApi$GetOffersForRetailers, `include_categories` is not nullable")
      }

      if (!missing(`include_filters`) && is.null(`include_filters`)) {
        stop("Invalid value for `include_filters` when calling OfferApi$GetOffersForRetailers, `include_filters` is not nullable")
      }

      if (!missing(`include_offer_locations`) && is.null(`include_offer_locations`)) {
        stop("Invalid value for `include_offer_locations` when calling OfferApi$GetOffersForRetailers, `include_offer_locations` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$GetOffersForRetailers, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$GetOffersForRetailers, `account_id` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling OfferApi$GetOffersForRetailers, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling OfferApi$GetOffersForRetailers, `filter_ids` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling OfferApi$GetOffersForRetailers, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling OfferApi$GetOffersForRetailers, `keyword` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling OfferApi$GetOffersForRetailers, `retailer_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling OfferApi$GetOffersForRetailers, `retailer_location_id` is not nullable")
      }

      if (!missing(`coupon_type`) && is.null(`coupon_type`)) {
        stop("Invalid value for `coupon_type` when calling OfferApi$GetOffersForRetailers, `coupon_type` is not nullable")
      }

      if (!missing(`offer_type`) && is.null(`offer_type`)) {
        stop("Invalid value for `offer_type` when calling OfferApi$GetOffersForRetailers, `offer_type` is not nullable")
      }

      if (!missing(`offer_types`) && is.null(`offer_types`)) {
        stop("Invalid value for `offer_types` when calling OfferApi$GetOffersForRetailers, `offer_types` is not nullable")
      }

      if (!missing(`special_offer_type`) && is.null(`special_offer_type`)) {
        stop("Invalid value for `special_offer_type` when calling OfferApi$GetOffersForRetailers, `special_offer_type` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling OfferApi$GetOffersForRetailers, `_i` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling OfferApi$GetOffersForRetailers, `_l` is not nullable")
      }

      if (!missing(`barcode_type`) && is.null(`barcode_type`)) {
        stop("Invalid value for `barcode_type` when calling OfferApi$GetOffersForRetailers, `barcode_type` is not nullable")
      }

      if (!missing(`barcode_entry`) && is.null(`barcode_entry`)) {
        stop("Invalid value for `barcode_entry` when calling OfferApi$GetOffersForRetailers, `barcode_entry` is not nullable")
      }

      if (!missing(`isbn`) && is.null(`isbn`)) {
        stop("Invalid value for `isbn` when calling OfferApi$GetOffersForRetailers, `isbn` is not nullable")
      }

      if (!missing(`asin`) && is.null(`asin`)) {
        stop("Invalid value for `asin` when calling OfferApi$GetOffersForRetailers, `asin` is not nullable")
      }

      if (!missing(`device_status`) && is.null(`device_status`)) {
        stop("Invalid value for `device_status` when calling OfferApi$GetOffersForRetailers, `device_status` is not nullable")
      }

      if (!missing(`needs_notification_sent`) && is.null(`needs_notification_sent`)) {
        stop("Invalid value for `needs_notification_sent` when calling OfferApi$GetOffersForRetailers, `needs_notification_sent` is not nullable")
      }

      if (!missing(`last_notification_sent`) && is.null(`last_notification_sent`)) {
        stop("Invalid value for `last_notification_sent` when calling OfferApi$GetOffersForRetailers, `last_notification_sent` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      if (!is.null(`coupon_type`) && !(`coupon_type` %in% c("VOUCHER", "COUPON", "PRODUCT", "MEDIA", "EVENT", "DEVICE"))) {
        stop("Invalid value for coupon_type when calling OfferApi$GetOffersForRetailers. Must be [VOUCHER, COUPON, PRODUCT, MEDIA, EVENT, DEVICE].")
      }
      query_params[["couponType"]] <- `coupon_type`

      if (!is.null(`offer_type`) && !(`offer_type` %in% c("VOUCHER", "COUPON", "PRODUCT", "MEDIA", "EVENT", "DEVICE"))) {
        stop("Invalid value for offer_type when calling OfferApi$GetOffersForRetailers. Must be [VOUCHER, COUPON, PRODUCT, MEDIA, EVENT, DEVICE].")
      }
      query_params[["offerType"]] <- `offer_type`

      query_params[["offerTypes"]] <- `offer_types`

      if (!is.null(`special_offer_type`) && !(`special_offer_type` %in% c("ALL", "RESERVABLE", "REGULAR_OFFER", "ACT_NOW", "GET_THERE_NOW", "SQOOT", "TICKETS", "YIPIT"))) {
        stop("Invalid value for special_offer_type when calling OfferApi$GetOffersForRetailers. Must be [ALL, RESERVABLE, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW, SQOOT, TICKETS, YIPIT].")
      }
      query_params[["specialOfferType"]] <- `special_offer_type`

      if (!is.null(`offer_visibility`) && !(`offer_visibility` %in% c("PUBLIC", "LISTABLE", "REWARDABLE", "TRIGGERABLE", "PRIVATE"))) {
        stop("Invalid value for offer_visibility when calling OfferApi$GetOffersForRetailers. Must be [PUBLIC, LISTABLE, REWARDABLE, TRIGGERABLE, PRIVATE].")
      }
      query_params[["offerVisibility"]] <- `offer_visibility`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "ACTIVATED", "EXPIRES", "REDEEMABLE_START", "REDEEMABLE_END", "TITLE", "SUBTITLE", "DETAILS", "OFFER_TYPE", "SPECIAL_OFFER_TYPE", "OFFER_VISIBILITY", "ESTIMATED_VALUE", "VOUCHER_PRICE", "VOUCHER_DISCOUNT_PRICE", "FULL_PRICE", "DICOUNT_PRICE", "TICKETS_REWARD", "AVAILABILITY_DATE", "RELEASE_DATE", "RETAILER_ID", "RETAILER_NAME", "RETAILER_LOCATION_ID", "RETAILER_LOCATION_NAME", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY"))) {
        stop("Invalid value for sort_field when calling OfferApi$GetOffersForRetailers. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, REDEEMABLE_START, REDEEMABLE_END, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, VOUCHER_DISCOUNT_PRICE, FULL_PRICE, DICOUNT_PRICE, TICKETS_REWARD, AVAILABILITY_DATE, RELEASE_DATE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["availableOnly"]] <- `available_only`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["includeCategories"]] <- `include_categories`

      query_params[["includeFilters"]] <- `include_filters`

      query_params[["includeOfferLocations"]] <- `include_offer_locations`

      query_params[["barcodeType"]] <- `barcode_type`

      query_params[["barcodeEntry"]] <- `barcode_entry`

      query_params[["isbn"]] <- `isbn`

      query_params[["asin"]] <- `asin`

      if (!is.null(`device_status`) && !(`device_status` %in% c("RUNNING", "WARNING", "DOWN"))) {
        stop("Invalid value for device_status when calling OfferApi$GetOffersForRetailers. Must be [RUNNING, WARNING, DOWN].")
      }
      query_params[["deviceStatus"]] <- `device_status`

      query_params[["needsNotificationSent"]] <- `needs_notification_sent`

      query_params[["lastNotificationSent"]] <- `last_notification_sent`

      local_var_url_path <- "/retailer/offer/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[OfferResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Offer Transaction
    #'
    #' @param offer_transaction_id the OfferTransaction ID of the transaction being redeemed
    #' @param status the status to set the offer transaction to - 1 sets it to redeemable and 2 sets it to redeemed
    #' @param device_id (optional) the device id (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param offer_location_id (optional) the OfferLocation ID where the offer is being redeemed
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    RedeemOfferTransaction = function(offer_transaction_id, status, device_id = NULL, account_id = NULL, offer_location_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RedeemOfferTransactionWithHttpInfo(offer_transaction_id, status, device_id, account_id, offer_location_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Offer Transaction
    #'
    #' @param offer_transaction_id the OfferTransaction ID of the transaction being redeemed
    #' @param status the status to set the offer transaction to - 1 sets it to redeemable and 2 sets it to redeemed
    #' @param device_id (optional) the device id (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param offer_location_id (optional) the OfferLocation ID where the offer is being redeemed
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    RedeemOfferTransactionWithHttpInfo = function(offer_transaction_id, status, device_id = NULL, account_id = NULL, offer_location_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offer_transaction_id`)) {
        stop("Missing required parameter `offer_transaction_id`.")
      }

      if (missing(`status`)) {
        stop("Missing required parameter `status`.")
      }

      if (!missing(`offer_transaction_id`) && is.null(`offer_transaction_id`)) {
        stop("Invalid value for `offer_transaction_id` when calling OfferApi$RedeemOfferTransaction, `offer_transaction_id` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling OfferApi$RedeemOfferTransaction, `status` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$RedeemOfferTransaction, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$RedeemOfferTransaction, `account_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling OfferApi$RedeemOfferTransaction, `offer_location_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["offerTransactionId"]] <- `offer_transaction_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["status"]] <- `status`

      local_var_url_path <- "/retailer/offer/transaction/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Offer Transactions
    #'
    #' @param sort_field Determines what to sort the results by {CREATED, UPDATED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, CUSTOMER_ID, CUSTOMER_DISPLAY, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY}
    #' @param descending Determines whether the results are in descending order
    #' @param start The start index for pagination
    #' @param limit The limit for pagination
    #' @param active_only Determines whether to only return active offer transactions
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) The keyword to search for
    #' @param retailer_id (optional) Filter results for this retailer
    #' @param retailer_location_id (optional) Filter results for this retailer location
    #' @param offer_id (optional) Filter results for this offer
    #' @param offer_location_id (optional) Filter results for this offer location
    #' @param redeemed (optional) if true return redeemed transactions (status 2), if false return active transactions (status 0 or 1)
    #' @param reservations_only (optional) returns only reservation transactions if true
    #' @param coupon_type (optional) This parameter is deprecated.
    #' @param offer_type (optional) Filter results to only return COUPON, VOUCHER, PRODUCT, MEDIA, or EVENT offers
    #' @param special_offer_type (optional) Filter results to only return REGULAR_OFFER, ACT_NOW, GET_THERE_NOW offer specials
    #' @param customer_account_ids (optional) Filter results by accounts
    #' @param category_ids (optional) 
    #' @param redeemable_start_date (optional) 
    #' @param redeemable_end_date (optional) 
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[OfferTransactionResponse]
    SearchOfferTransactionsForRetailers = function(sort_field, descending, start, limit, active_only, device_id = NULL, account_id = NULL, q = NULL, keyword = NULL, retailer_id = NULL, retailer_location_id = NULL, offer_id = NULL, offer_location_id = NULL, redeemed = NULL, reservations_only = NULL, coupon_type = NULL, offer_type = NULL, special_offer_type = NULL, customer_account_ids = NULL, category_ids = NULL, redeemable_start_date = NULL, redeemable_end_date = NULL, _i = NULL, _l = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchOfferTransactionsForRetailersWithHttpInfo(sort_field, descending, start, limit, active_only, device_id, account_id, q, keyword, retailer_id, retailer_location_id, offer_id, offer_location_id, redeemed, reservations_only, coupon_type, offer_type, special_offer_type, customer_account_ids, category_ids, redeemable_start_date, redeemable_end_date, _i, _l, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Offer Transactions
    #'
    #' @param sort_field Determines what to sort the results by {CREATED, UPDATED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, CUSTOMER_ID, CUSTOMER_DISPLAY, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY}
    #' @param descending Determines whether the results are in descending order
    #' @param start The start index for pagination
    #' @param limit The limit for pagination
    #' @param active_only Determines whether to only return active offer transactions
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) The keyword to search for
    #' @param retailer_id (optional) Filter results for this retailer
    #' @param retailer_location_id (optional) Filter results for this retailer location
    #' @param offer_id (optional) Filter results for this offer
    #' @param offer_location_id (optional) Filter results for this offer location
    #' @param redeemed (optional) if true return redeemed transactions (status 2), if false return active transactions (status 0 or 1)
    #' @param reservations_only (optional) returns only reservation transactions if true
    #' @param coupon_type (optional) This parameter is deprecated.
    #' @param offer_type (optional) Filter results to only return COUPON, VOUCHER, PRODUCT, MEDIA, or EVENT offers
    #' @param special_offer_type (optional) Filter results to only return REGULAR_OFFER, ACT_NOW, GET_THERE_NOW offer specials
    #' @param customer_account_ids (optional) Filter results by accounts
    #' @param category_ids (optional) 
    #' @param redeemable_start_date (optional) 
    #' @param redeemable_end_date (optional) 
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[OfferTransactionResponse]) with additional information such as HTTP status code, headers
    SearchOfferTransactionsForRetailersWithHttpInfo = function(sort_field, descending, start, limit, active_only, device_id = NULL, account_id = NULL, q = NULL, keyword = NULL, retailer_id = NULL, retailer_location_id = NULL, offer_id = NULL, offer_location_id = NULL, redeemed = NULL, reservations_only = NULL, coupon_type = NULL, offer_type = NULL, special_offer_type = NULL, customer_account_ids = NULL, category_ids = NULL, redeemable_start_date = NULL, redeemable_end_date = NULL, _i = NULL, _l = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`active_only`)) {
        stop("Missing required parameter `active_only`.")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling OfferApi$SearchOfferTransactionsForRetailers, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling OfferApi$SearchOfferTransactionsForRetailers, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling OfferApi$SearchOfferTransactionsForRetailers, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling OfferApi$SearchOfferTransactionsForRetailers, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling OfferApi$SearchOfferTransactionsForRetailers, `active_only` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$SearchOfferTransactionsForRetailers, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$SearchOfferTransactionsForRetailers, `account_id` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling OfferApi$SearchOfferTransactionsForRetailers, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling OfferApi$SearchOfferTransactionsForRetailers, `keyword` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling OfferApi$SearchOfferTransactionsForRetailers, `retailer_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling OfferApi$SearchOfferTransactionsForRetailers, `retailer_location_id` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling OfferApi$SearchOfferTransactionsForRetailers, `offer_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling OfferApi$SearchOfferTransactionsForRetailers, `offer_location_id` is not nullable")
      }

      if (!missing(`redeemed`) && is.null(`redeemed`)) {
        stop("Invalid value for `redeemed` when calling OfferApi$SearchOfferTransactionsForRetailers, `redeemed` is not nullable")
      }

      if (!missing(`reservations_only`) && is.null(`reservations_only`)) {
        stop("Invalid value for `reservations_only` when calling OfferApi$SearchOfferTransactionsForRetailers, `reservations_only` is not nullable")
      }

      if (!missing(`coupon_type`) && is.null(`coupon_type`)) {
        stop("Invalid value for `coupon_type` when calling OfferApi$SearchOfferTransactionsForRetailers, `coupon_type` is not nullable")
      }

      if (!missing(`offer_type`) && is.null(`offer_type`)) {
        stop("Invalid value for `offer_type` when calling OfferApi$SearchOfferTransactionsForRetailers, `offer_type` is not nullable")
      }

      if (!missing(`special_offer_type`) && is.null(`special_offer_type`)) {
        stop("Invalid value for `special_offer_type` when calling OfferApi$SearchOfferTransactionsForRetailers, `special_offer_type` is not nullable")
      }

      if (!missing(`customer_account_ids`) && is.null(`customer_account_ids`)) {
        stop("Invalid value for `customer_account_ids` when calling OfferApi$SearchOfferTransactionsForRetailers, `customer_account_ids` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling OfferApi$SearchOfferTransactionsForRetailers, `category_ids` is not nullable")
      }

      if (!missing(`redeemable_start_date`) && is.null(`redeemable_start_date`)) {
        stop("Invalid value for `redeemable_start_date` when calling OfferApi$SearchOfferTransactionsForRetailers, `redeemable_start_date` is not nullable")
      }

      if (!missing(`redeemable_end_date`) && is.null(`redeemable_end_date`)) {
        stop("Invalid value for `redeemable_end_date` when calling OfferApi$SearchOfferTransactionsForRetailers, `redeemable_end_date` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling OfferApi$SearchOfferTransactionsForRetailers, `_i` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling OfferApi$SearchOfferTransactionsForRetailers, `_l` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["redeemed"]] <- `redeemed`

      query_params[["reservationsOnly"]] <- `reservations_only`

      if (!is.null(`coupon_type`) && !(`coupon_type` %in% c("VOUCHER", "COUPON", "PRODUCT", "MEDIA", "EVENT", "DEVICE"))) {
        stop("Invalid value for coupon_type when calling OfferApi$SearchOfferTransactionsForRetailers. Must be [VOUCHER, COUPON, PRODUCT, MEDIA, EVENT, DEVICE].")
      }
      query_params[["couponType"]] <- `coupon_type`

      if (!is.null(`offer_type`) && !(`offer_type` %in% c("VOUCHER", "COUPON", "PRODUCT", "MEDIA", "EVENT", "DEVICE"))) {
        stop("Invalid value for offer_type when calling OfferApi$SearchOfferTransactionsForRetailers. Must be [VOUCHER, COUPON, PRODUCT, MEDIA, EVENT, DEVICE].")
      }
      query_params[["offerType"]] <- `offer_type`

      if (!is.null(`special_offer_type`) && !(`special_offer_type` %in% c("ALL", "RESERVABLE", "REGULAR_OFFER", "ACT_NOW", "GET_THERE_NOW", "SQOOT", "TICKETS", "YIPIT"))) {
        stop("Invalid value for special_offer_type when calling OfferApi$SearchOfferTransactionsForRetailers. Must be [ALL, RESERVABLE, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW, SQOOT, TICKETS, YIPIT].")
      }
      query_params[["specialOfferType"]] <- `special_offer_type`

      query_params[["customerAccountIds"]] <- `customer_account_ids`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["redeemableStartDate"]] <- `redeemable_start_date`

      query_params[["redeemableEndDate"]] <- `redeemable_end_date`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "ACTIVATED", "EXPIRES", "TITLE", "SUBTITLE", "DETAILS", "OFFER_TYPE", "SPECIAL_OFFER_TYPE", "OFFER_VISIBILITY", "REDEEMABLE_START", "REDEEMABLE_END", "CUSTOMER_ID", "CUSTOMER_DISPLAY", "RETAILER_ID", "RETAILER_NAME", "RETAILER_LOCATION_ID", "RETAILER_LOCATION_NAME", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY"))) {
        stop("Invalid value for sort_field when calling OfferApi$SearchOfferTransactionsForRetailers. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, REDEEMABLE_START, REDEEMABLE_END, CUSTOMER_ID, CUSTOMER_DISPLAY, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      local_var_url_path <- "/retailer/offer/transaction/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[OfferTransactionResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Offers
    #'
    #' @param latitude The latitude of where the search will center at
    #' @param longitude The longitude of where the search will center at
    #' @param recommendation_type The method to use to gather recommendations: WALLET base relevance on items in users wallets CLICKS base relevance on items users have clicked on BLENDED blend using all methods available
    #' @param location_id This parameter is deprecated. The location id
    #' @param start The start index for pagination
    #' @param limit The limit for pagination
    #' @param max_recommendations 
    #' @param distance_unit 
    #' @param app_key (optional) The application key used to identify the application
    #' @param device_id (optional) The device id for returning account information (i.e. offer transactions made by the user)
    #' @param account_id (optional) The account id for returning account information (i.e. offer transactions made by the user)
    #' @param search_range (optional) The range of the search (default value: 5)
    #' @param tags (optional) Does a full-text search on tags
    #' @param supported_postal_codes (optional) supported postal codes
    #' @param keyword (optional) The keyword to filter results by
    #' @param categories (optional) Comma separate list of category ids
    #' @param filters (optional) Comma separated list of filter ids
    #' @param offer_types (optional) Comma separated list of offer types {COUPON, VOUCHER, PRODUCT, MEDIA, EVENT, DEVICE} (default value: "COUPON, VOUCHER")
    #' @param type (optional) The special offer type {ALL, RESERVABLE, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
    #' @param sort_field (optional) The field to sort the result set on:DISTANCE Sort the offers nearest to farthest BUSINESS_NAME Sort the offers alphabetically by location name SHUFFLE Sort the nearest offers randomly USERPREFS accountId must accompany this parameter. Gives the first X recommendations where X equals the parameter maxRecommendations
    #' @param recommend_offer_ids (optional) Return recommendations based on these offers IDs
    #' @param retailer_location_ids (optional) Only return offer locations for the specific retailer locations.
    #' @param offer_id (optional) Only return offer locations for the specific offer.
    #' @param include_mission (optional) If true include the mission response as part of the offer. Default is false.
    #' @param include_categories (optional) If true include the category list response as part of the offer. Default is false.
    #' @param include_filters (optional) If true include the filter list response as part of the offer. Default is false.
    #' @param include_expired (optional) If true then ignore the expired dates. Default is false.
    #' @param include_favorite (optional) If true then ignore the favorite. Default is false.
    #' @param closest_offer_only (optional) This parameter is deprecated. see groupBy. If true then it only returns the offer location for an offer closest to the given lat/lon
    #' @param search_expression (optional) 
    #' @param group_by (optional) groups the results by a certain field. For example, if you want to return the closest offer location of an offer, then pass in groupBy=OFFER_ID and sortField=DISTANCE (to sort by distance).
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferListResponse
    SearchOffersForConsumer = function(latitude, longitude, recommendation_type, location_id, start, limit, max_recommendations, distance_unit, app_key = NULL, device_id = NULL, account_id = NULL, search_range = 5, tags = NULL, supported_postal_codes = NULL, keyword = NULL, categories = NULL, filters = NULL, offer_types = "COUPON, VOUCHER", type = NULL, sort_field = NULL, recommend_offer_ids = NULL, retailer_location_ids = NULL, offer_id = NULL, include_mission = NULL, include_categories = NULL, include_filters = NULL, include_expired = NULL, include_favorite = NULL, closest_offer_only = NULL, search_expression = NULL, group_by = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchOffersForConsumerWithHttpInfo(latitude, longitude, recommendation_type, location_id, start, limit, max_recommendations, distance_unit, app_key, device_id, account_id, search_range, tags, supported_postal_codes, keyword, categories, filters, offer_types, type, sort_field, recommend_offer_ids, retailer_location_ids, offer_id, include_mission, include_categories, include_filters, include_expired, include_favorite, closest_offer_only, search_expression, group_by, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Offers
    #'
    #' @param latitude The latitude of where the search will center at
    #' @param longitude The longitude of where the search will center at
    #' @param recommendation_type The method to use to gather recommendations: WALLET base relevance on items in users wallets CLICKS base relevance on items users have clicked on BLENDED blend using all methods available
    #' @param location_id This parameter is deprecated. The location id
    #' @param start The start index for pagination
    #' @param limit The limit for pagination
    #' @param max_recommendations 
    #' @param distance_unit 
    #' @param app_key (optional) The application key used to identify the application
    #' @param device_id (optional) The device id for returning account information (i.e. offer transactions made by the user)
    #' @param account_id (optional) The account id for returning account information (i.e. offer transactions made by the user)
    #' @param search_range (optional) The range of the search (default value: 5)
    #' @param tags (optional) Does a full-text search on tags
    #' @param supported_postal_codes (optional) supported postal codes
    #' @param keyword (optional) The keyword to filter results by
    #' @param categories (optional) Comma separate list of category ids
    #' @param filters (optional) Comma separated list of filter ids
    #' @param offer_types (optional) Comma separated list of offer types {COUPON, VOUCHER, PRODUCT, MEDIA, EVENT, DEVICE} (default value: "COUPON, VOUCHER")
    #' @param type (optional) The special offer type {ALL, RESERVABLE, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
    #' @param sort_field (optional) The field to sort the result set on:DISTANCE Sort the offers nearest to farthest BUSINESS_NAME Sort the offers alphabetically by location name SHUFFLE Sort the nearest offers randomly USERPREFS accountId must accompany this parameter. Gives the first X recommendations where X equals the parameter maxRecommendations
    #' @param recommend_offer_ids (optional) Return recommendations based on these offers IDs
    #' @param retailer_location_ids (optional) Only return offer locations for the specific retailer locations.
    #' @param offer_id (optional) Only return offer locations for the specific offer.
    #' @param include_mission (optional) If true include the mission response as part of the offer. Default is false.
    #' @param include_categories (optional) If true include the category list response as part of the offer. Default is false.
    #' @param include_filters (optional) If true include the filter list response as part of the offer. Default is false.
    #' @param include_expired (optional) If true then ignore the expired dates. Default is false.
    #' @param include_favorite (optional) If true then ignore the favorite. Default is false.
    #' @param closest_offer_only (optional) This parameter is deprecated. see groupBy. If true then it only returns the offer location for an offer closest to the given lat/lon
    #' @param search_expression (optional) 
    #' @param group_by (optional) groups the results by a certain field. For example, if you want to return the closest offer location of an offer, then pass in groupBy=OFFER_ID and sortField=DISTANCE (to sort by distance).
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferListResponse) with additional information such as HTTP status code, headers
    SearchOffersForConsumerWithHttpInfo = function(latitude, longitude, recommendation_type, location_id, start, limit, max_recommendations, distance_unit, app_key = NULL, device_id = NULL, account_id = NULL, search_range = 5, tags = NULL, supported_postal_codes = NULL, keyword = NULL, categories = NULL, filters = NULL, offer_types = "COUPON, VOUCHER", type = NULL, sort_field = NULL, recommend_offer_ids = NULL, retailer_location_ids = NULL, offer_id = NULL, include_mission = NULL, include_categories = NULL, include_filters = NULL, include_expired = NULL, include_favorite = NULL, closest_offer_only = NULL, search_expression = NULL, group_by = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`latitude`)) {
        stop("Missing required parameter `latitude`.")
      }

      if (missing(`longitude`)) {
        stop("Missing required parameter `longitude`.")
      }

      if (missing(`recommendation_type`)) {
        stop("Missing required parameter `recommendation_type`.")
      }

      if (missing(`location_id`)) {
        stop("Missing required parameter `location_id`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`max_recommendations`)) {
        stop("Missing required parameter `max_recommendations`.")
      }

      if (missing(`distance_unit`)) {
        stop("Missing required parameter `distance_unit`.")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling OfferApi$SearchOffersForConsumer, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling OfferApi$SearchOffersForConsumer, `longitude` is not nullable")
      }

      if (!missing(`recommendation_type`) && is.null(`recommendation_type`)) {
        stop("Invalid value for `recommendation_type` when calling OfferApi$SearchOffersForConsumer, `recommendation_type` is not nullable")
      }

      if (!missing(`location_id`) && is.null(`location_id`)) {
        stop("Invalid value for `location_id` when calling OfferApi$SearchOffersForConsumer, `location_id` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling OfferApi$SearchOffersForConsumer, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling OfferApi$SearchOffersForConsumer, `limit` is not nullable")
      }

      if (!missing(`max_recommendations`) && is.null(`max_recommendations`)) {
        stop("Invalid value for `max_recommendations` when calling OfferApi$SearchOffersForConsumer, `max_recommendations` is not nullable")
      }

      if (!missing(`distance_unit`) && is.null(`distance_unit`)) {
        stop("Invalid value for `distance_unit` when calling OfferApi$SearchOffersForConsumer, `distance_unit` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling OfferApi$SearchOffersForConsumer, `app_key` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$SearchOffersForConsumer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$SearchOffersForConsumer, `account_id` is not nullable")
      }

      if (!missing(`search_range`) && is.null(`search_range`)) {
        stop("Invalid value for `search_range` when calling OfferApi$SearchOffersForConsumer, `search_range` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling OfferApi$SearchOffersForConsumer, `tags` is not nullable")
      }

      if (!missing(`supported_postal_codes`) && is.null(`supported_postal_codes`)) {
        stop("Invalid value for `supported_postal_codes` when calling OfferApi$SearchOffersForConsumer, `supported_postal_codes` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling OfferApi$SearchOffersForConsumer, `keyword` is not nullable")
      }

      if (!missing(`categories`) && is.null(`categories`)) {
        stop("Invalid value for `categories` when calling OfferApi$SearchOffersForConsumer, `categories` is not nullable")
      }

      if (!missing(`filters`) && is.null(`filters`)) {
        stop("Invalid value for `filters` when calling OfferApi$SearchOffersForConsumer, `filters` is not nullable")
      }

      if (!missing(`offer_types`) && is.null(`offer_types`)) {
        stop("Invalid value for `offer_types` when calling OfferApi$SearchOffersForConsumer, `offer_types` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling OfferApi$SearchOffersForConsumer, `type` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling OfferApi$SearchOffersForConsumer, `sort_field` is not nullable")
      }

      if (!missing(`recommend_offer_ids`) && is.null(`recommend_offer_ids`)) {
        stop("Invalid value for `recommend_offer_ids` when calling OfferApi$SearchOffersForConsumer, `recommend_offer_ids` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling OfferApi$SearchOffersForConsumer, `retailer_location_ids` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling OfferApi$SearchOffersForConsumer, `offer_id` is not nullable")
      }

      if (!missing(`include_mission`) && is.null(`include_mission`)) {
        stop("Invalid value for `include_mission` when calling OfferApi$SearchOffersForConsumer, `include_mission` is not nullable")
      }

      if (!missing(`include_categories`) && is.null(`include_categories`)) {
        stop("Invalid value for `include_categories` when calling OfferApi$SearchOffersForConsumer, `include_categories` is not nullable")
      }

      if (!missing(`include_filters`) && is.null(`include_filters`)) {
        stop("Invalid value for `include_filters` when calling OfferApi$SearchOffersForConsumer, `include_filters` is not nullable")
      }

      if (!missing(`include_expired`) && is.null(`include_expired`)) {
        stop("Invalid value for `include_expired` when calling OfferApi$SearchOffersForConsumer, `include_expired` is not nullable")
      }

      if (!missing(`include_favorite`) && is.null(`include_favorite`)) {
        stop("Invalid value for `include_favorite` when calling OfferApi$SearchOffersForConsumer, `include_favorite` is not nullable")
      }

      if (!missing(`closest_offer_only`) && is.null(`closest_offer_only`)) {
        stop("Invalid value for `closest_offer_only` when calling OfferApi$SearchOffersForConsumer, `closest_offer_only` is not nullable")
      }

      if (!missing(`search_expression`) && is.null(`search_expression`)) {
        stop("Invalid value for `search_expression` when calling OfferApi$SearchOffersForConsumer, `search_expression` is not nullable")
      }

      if (!missing(`group_by`) && is.null(`group_by`)) {
        stop("Invalid value for `group_by` when calling OfferApi$SearchOffersForConsumer, `group_by` is not nullable")
      }

      query_params[["appKey"]] <- `app_key`

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["searchRange"]] <- `search_range`

      query_params[["tags"]] <- `tags`

      query_params[["supportedPostalCodes"]] <- `supported_postal_codes`

      query_params[["keyword"]] <- `keyword`

      query_params[["categories"]] <- `categories`

      query_params[["filters"]] <- `filters`

      query_params[["offerTypes"]] <- `offer_types`

      query_params[["type"]] <- `type`

      query_params[["sortField"]] <- `sort_field`

      query_params[["recommendOfferIds"]] <- `recommend_offer_ids`

      if (!is.null(`recommendation_type`) && !(`recommendation_type` %in% c("WALLET", "CLICKS", "BLENDED"))) {
        stop("Invalid value for recommendation_type when calling OfferApi$SearchOffersForConsumer. Must be [WALLET, CLICKS, BLENDED].")
      }
      query_params[["recommendationType"]] <- `recommendation_type`

      query_params[["locationId"]] <- `location_id`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["offerId"]] <- `offer_id`

      query_params[["includeMission"]] <- `include_mission`

      query_params[["includeCategories"]] <- `include_categories`

      query_params[["includeFilters"]] <- `include_filters`

      query_params[["includeExpired"]] <- `include_expired`

      query_params[["includeFavorite"]] <- `include_favorite`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["maxRecommendations"]] <- `max_recommendations`

      if (!is.null(`distance_unit`) && !(`distance_unit` %in% c("MILES", "KILOMETERS"))) {
        stop("Invalid value for distance_unit when calling OfferApi$SearchOffersForConsumer. Must be [MILES, KILOMETERS].")
      }
      query_params[["distanceUnit"]] <- `distance_unit`

      query_params[["closestOfferOnly"]] <- `closest_offer_only`

      query_params[["searchExpression"]] <- `search_expression`

      if (!is.null(`group_by`) && !(`group_by` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "OFFER_ID", "ACTIVATED", "EXPIRES", "TITLE", "SUBTITLE", "DETAILS", "OFFER_TYPE", "SPECIAL_OFFER_TYPE", "OFFER_VISIBILITY", "RETAILER_ID", "RETAILER_LOCATION_ID", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY"))) {
        stop("Invalid value for group_by when calling OfferApi$SearchOffersForConsumer. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, OFFER_ID, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, RETAILER_ID, RETAILER_LOCATION_ID, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY].")
      }
      query_params[["groupBy"]] <- `group_by`

      local_var_url_path <- "/offer/lists"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Offers (Top)
    #'
    #' @param start (optional) The index into the record set to start with. Default is 0. (default value: 0)
    #' @param limit (optional) The total number of record to return. Default id 20. (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferListResponse
    TopOfferTransactions = function(start = 0, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$TopOfferTransactionsWithHttpInfo(start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Offers (Top)
    #'
    #' @param start (optional) The index into the record set to start with. Default is 0. (default value: 0)
    #' @param limit (optional) The total number of record to return. Default id 20. (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferListResponse) with additional information such as HTTP status code, headers
    TopOfferTransactionsWithHttpInfo = function(start = 0, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling OfferApi$TopOfferTransactions, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling OfferApi$TopOfferTransactions, `limit` is not nullable")
      }

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/offer/top"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Offer
    #'
    #' @param offer_id The offer to update
    #' @param include_offer_locations If true return all the offer locations associated with the offer
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the update, must have rights to edit the offer (deviceId or accountId required)
    #' @param parent_offer_id (optional) 
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids. This will assign the offer to these retailer locations.
    #' @param offer_locations (optional) A list of json data that has offer location specific values.
    #' @param tags (optional) Custom string field for doing full-text searches
    #' @param title (optional) The title (255 char limit)
    #' @param sub_title (optional) The sub title (255 char limit)
    #' @param details (optional) The details
    #' @param sub_details (optional) A string for custom details (255 char limit)
    #' @param fine_print (optional) The fine print
    #' @param barcode_type (optional) The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA}
    #' @param barcode_entry (optional) The bar code entry string
    #' @param external_redeem_options (optional) An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer.
    #' @param external_url (optional) The clickUrl of the offer
    #' @param external_id (optional) 
    #' @param tickets_reward_type (optional) The type of ticket to reward, null means default type
    #' @param tickets_reward (optional) Determines how many tickets are awarded
    #' @param activated (optional) The date of when the offer will be visible to consumers
    #' @param expires (optional) The date of when the offer expires
    #' @param no_expiration (optional) Overrides the expiration date so that the offer does not expire
    #' @param available_limit (optional) The limit of how many times the offer can be used by consumers
    #' @param available_limit_per_user (optional) The limit of how many times a user can used the same offer
    #' @param added_limit (optional) The limit of how many times the offer can be added to consumer wallets
    #' @param view_limit (optional) 
    #' @param max_prints (optional) The maximum number of times the offer can be printed
    #' @param ticket_price_type (optional) the type of ticket needed to buy the offer
    #' @param ticket_price (optional) The cost of the offer in tickets
    #' @param full_price (optional) The retail/full price cost of the offer in real currency
    #' @param discount_price (optional) The cost of the offer at a discounted price (what the consumer pays)
    #' @param show_remaining (optional) show remaining offers available
    #' @param show_redeemed (optional) show how many offers have been redeemed
    #' @param replaced (optional) 
    #' @param featured (optional) whether the offer is featured or not
    #' @param offer_type (optional) The offer type {VOUCHER, COUPON, PRODUCT, MEDIA, EVENT}
    #' @param special_offer_type (optional) The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
    #' @param offer_visibility (optional) The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE}
    #' @param category_ids (optional) Comma separated list of category ids
    #' @param filter_ids (optional) Comma separated list of filter ids
    #' @param active (optional) Sets the active flag
    #' @param barcode_asset_id (optional) The id of the barcode asset
    #' @param image_asset_id (optional) The id of the an offer asset
    #' @param image_asset_id1 (optional) The id of the an offer asset
    #' @param image_asset_id2 (optional) The id of the an offer asset
    #' @param image_asset_id3 (optional) The id of the an offer asset
    #' @param image_asset_id4 (optional) The id of the an offer asset
    #' @param image_asset_id5 (optional) The id of the an offer asset
    #' @param publisher (optional) The maker of the item.
    #' @param redeemable_start (optional) The redeemable start date/time of the offer.
    #' @param redeemable_end (optional) The redeemable start date/time of the offer.
    #' @param brand (optional) The brand. OfferType PRODUCT only.
    #' @param product_type (optional) The product type. OfferType PRODUCT only.
    #' @param condition_type (optional) The condition. OfferType PRODUCT only.
    #' @param isbn (optional) The ISBN id. OfferType PRODUCT only.
    #' @param asin (optional) The ASIN id. OfferType PRODUCT only.
    #' @param catalog_numbers (optional) The list of catelog numbers, comma seperated. OfferType PRODUCT only.
    #' @param department (optional) The department name. The OfferType PRODUCT only.
    #' @param features (optional) The list of features, comma seperated. OfferType PRODUCT only.
    #' @param minimum_price (optional) The MAP price. OfferType PRODUCT only.
    #' @param width (optional) The width of the item. OfferType PRODUCT only.
    #' @param height (optional) The height of the item. OfferType PRODUCT only.
    #' @param depth (optional) The depth of the item. OfferType PRODUCT only.
    #' @param weight (optional) The weight of the item. OfferType PRODUCT only.
    #' @param unit (optional) The unit of measurement. OfferType PRODUCT only.
    #' @param studio (optional) The studio name. OfferType PRODUCT only.
    #' @param parental_rating (optional) The parental control rating. OfferType PRODUCT only.
    #' @param publish_date (optional) The date published. OfferType PRODUCT only.
    #' @param availability_date (optional) The date available. OfferType PRODUCT only.
    #' @param size_id (optional) The size Id. OfferType PRODUCT only.
    #' @param listing_id (optional) The ID of the event listing
    #' @param media_type (optional) 
    #' @param duration (optional) The total playing time of the media item. OfferType MEDIA only.
    #' @param author (optional) The created/author of the media item. OfferType MEDIA only.
    #' @param release_date (optional) The date/time of when the media item was originally released. OfferType MEDIA only.
    #' @param collection_ids (optional) 
    #' @param reboot_time_hour (optional) The reboot hour time ranging from 0 to 23
    #' @param reboot_time_minute (optional) The reboot minute time ranging from 0 to 59
    #' @param idle_timeout_in_second (optional) If the device is idle for idleTimeoutInSecond then the device should timeout
    #' @param serial_number (optional) The serial number on the device
    #' @param udid (optional) The unique device id for the device
    #' @param device_type (optional) The type of the device, for example: \"Kiosk\", \"Beacon\".
    #' @param device_power (optional) RSSI reading at 1 meter from device (-10 to -140)
    #' @param device_interference (optional) 
    #' @param availability (optional) 
    #' @param availability_summary (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerOfferResponse
    UpdateOffer = function(offer_id, include_offer_locations, device_id = NULL, account_id = NULL, parent_offer_id = NULL, retailer_location_ids = NULL, offer_locations = NULL, tags = NULL, title = NULL, sub_title = NULL, details = NULL, sub_details = NULL, fine_print = NULL, barcode_type = NULL, barcode_entry = NULL, external_redeem_options = NULL, external_url = NULL, external_id = NULL, tickets_reward_type = NULL, tickets_reward = NULL, activated = NULL, expires = NULL, no_expiration = NULL, available_limit = NULL, available_limit_per_user = NULL, added_limit = NULL, view_limit = NULL, max_prints = NULL, ticket_price_type = NULL, ticket_price = NULL, full_price = NULL, discount_price = NULL, show_remaining = NULL, show_redeemed = NULL, replaced = NULL, featured = NULL, offer_type = NULL, special_offer_type = NULL, offer_visibility = NULL, category_ids = NULL, filter_ids = NULL, active = NULL, barcode_asset_id = NULL, image_asset_id = NULL, image_asset_id1 = NULL, image_asset_id2 = NULL, image_asset_id3 = NULL, image_asset_id4 = NULL, image_asset_id5 = NULL, publisher = NULL, redeemable_start = NULL, redeemable_end = NULL, brand = NULL, product_type = NULL, condition_type = NULL, isbn = NULL, asin = NULL, catalog_numbers = NULL, department = NULL, features = NULL, minimum_price = NULL, width = NULL, height = NULL, depth = NULL, weight = NULL, unit = NULL, studio = NULL, parental_rating = NULL, publish_date = NULL, availability_date = NULL, size_id = NULL, listing_id = NULL, media_type = NULL, duration = NULL, author = NULL, release_date = NULL, collection_ids = NULL, reboot_time_hour = NULL, reboot_time_minute = NULL, idle_timeout_in_second = NULL, serial_number = NULL, udid = NULL, device_type = NULL, device_power = NULL, device_interference = NULL, availability = NULL, availability_summary = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateOfferWithHttpInfo(offer_id, include_offer_locations, device_id, account_id, parent_offer_id, retailer_location_ids, offer_locations, tags, title, sub_title, details, sub_details, fine_print, barcode_type, barcode_entry, external_redeem_options, external_url, external_id, tickets_reward_type, tickets_reward, activated, expires, no_expiration, available_limit, available_limit_per_user, added_limit, view_limit, max_prints, ticket_price_type, ticket_price, full_price, discount_price, show_remaining, show_redeemed, replaced, featured, offer_type, special_offer_type, offer_visibility, category_ids, filter_ids, active, barcode_asset_id, image_asset_id, image_asset_id1, image_asset_id2, image_asset_id3, image_asset_id4, image_asset_id5, publisher, redeemable_start, redeemable_end, brand, product_type, condition_type, isbn, asin, catalog_numbers, department, features, minimum_price, width, height, depth, weight, unit, studio, parental_rating, publish_date, availability_date, size_id, listing_id, media_type, duration, author, release_date, collection_ids, reboot_time_hour, reboot_time_minute, idle_timeout_in_second, serial_number, udid, device_type, device_power, device_interference, availability, availability_summary, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Offer
    #'
    #' @param offer_id The offer to update
    #' @param include_offer_locations If true return all the offer locations associated with the offer
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the update, must have rights to edit the offer (deviceId or accountId required)
    #' @param parent_offer_id (optional) 
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids. This will assign the offer to these retailer locations.
    #' @param offer_locations (optional) A list of json data that has offer location specific values.
    #' @param tags (optional) Custom string field for doing full-text searches
    #' @param title (optional) The title (255 char limit)
    #' @param sub_title (optional) The sub title (255 char limit)
    #' @param details (optional) The details
    #' @param sub_details (optional) A string for custom details (255 char limit)
    #' @param fine_print (optional) The fine print
    #' @param barcode_type (optional) The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA}
    #' @param barcode_entry (optional) The bar code entry string
    #' @param external_redeem_options (optional) An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer.
    #' @param external_url (optional) The clickUrl of the offer
    #' @param external_id (optional) 
    #' @param tickets_reward_type (optional) The type of ticket to reward, null means default type
    #' @param tickets_reward (optional) Determines how many tickets are awarded
    #' @param activated (optional) The date of when the offer will be visible to consumers
    #' @param expires (optional) The date of when the offer expires
    #' @param no_expiration (optional) Overrides the expiration date so that the offer does not expire
    #' @param available_limit (optional) The limit of how many times the offer can be used by consumers
    #' @param available_limit_per_user (optional) The limit of how many times a user can used the same offer
    #' @param added_limit (optional) The limit of how many times the offer can be added to consumer wallets
    #' @param view_limit (optional) 
    #' @param max_prints (optional) The maximum number of times the offer can be printed
    #' @param ticket_price_type (optional) the type of ticket needed to buy the offer
    #' @param ticket_price (optional) The cost of the offer in tickets
    #' @param full_price (optional) The retail/full price cost of the offer in real currency
    #' @param discount_price (optional) The cost of the offer at a discounted price (what the consumer pays)
    #' @param show_remaining (optional) show remaining offers available
    #' @param show_redeemed (optional) show how many offers have been redeemed
    #' @param replaced (optional) 
    #' @param featured (optional) whether the offer is featured or not
    #' @param offer_type (optional) The offer type {VOUCHER, COUPON, PRODUCT, MEDIA, EVENT}
    #' @param special_offer_type (optional) The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
    #' @param offer_visibility (optional) The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE}
    #' @param category_ids (optional) Comma separated list of category ids
    #' @param filter_ids (optional) Comma separated list of filter ids
    #' @param active (optional) Sets the active flag
    #' @param barcode_asset_id (optional) The id of the barcode asset
    #' @param image_asset_id (optional) The id of the an offer asset
    #' @param image_asset_id1 (optional) The id of the an offer asset
    #' @param image_asset_id2 (optional) The id of the an offer asset
    #' @param image_asset_id3 (optional) The id of the an offer asset
    #' @param image_asset_id4 (optional) The id of the an offer asset
    #' @param image_asset_id5 (optional) The id of the an offer asset
    #' @param publisher (optional) The maker of the item.
    #' @param redeemable_start (optional) The redeemable start date/time of the offer.
    #' @param redeemable_end (optional) The redeemable start date/time of the offer.
    #' @param brand (optional) The brand. OfferType PRODUCT only.
    #' @param product_type (optional) The product type. OfferType PRODUCT only.
    #' @param condition_type (optional) The condition. OfferType PRODUCT only.
    #' @param isbn (optional) The ISBN id. OfferType PRODUCT only.
    #' @param asin (optional) The ASIN id. OfferType PRODUCT only.
    #' @param catalog_numbers (optional) The list of catelog numbers, comma seperated. OfferType PRODUCT only.
    #' @param department (optional) The department name. The OfferType PRODUCT only.
    #' @param features (optional) The list of features, comma seperated. OfferType PRODUCT only.
    #' @param minimum_price (optional) The MAP price. OfferType PRODUCT only.
    #' @param width (optional) The width of the item. OfferType PRODUCT only.
    #' @param height (optional) The height of the item. OfferType PRODUCT only.
    #' @param depth (optional) The depth of the item. OfferType PRODUCT only.
    #' @param weight (optional) The weight of the item. OfferType PRODUCT only.
    #' @param unit (optional) The unit of measurement. OfferType PRODUCT only.
    #' @param studio (optional) The studio name. OfferType PRODUCT only.
    #' @param parental_rating (optional) The parental control rating. OfferType PRODUCT only.
    #' @param publish_date (optional) The date published. OfferType PRODUCT only.
    #' @param availability_date (optional) The date available. OfferType PRODUCT only.
    #' @param size_id (optional) The size Id. OfferType PRODUCT only.
    #' @param listing_id (optional) The ID of the event listing
    #' @param media_type (optional) 
    #' @param duration (optional) The total playing time of the media item. OfferType MEDIA only.
    #' @param author (optional) The created/author of the media item. OfferType MEDIA only.
    #' @param release_date (optional) The date/time of when the media item was originally released. OfferType MEDIA only.
    #' @param collection_ids (optional) 
    #' @param reboot_time_hour (optional) The reboot hour time ranging from 0 to 23
    #' @param reboot_time_minute (optional) The reboot minute time ranging from 0 to 59
    #' @param idle_timeout_in_second (optional) If the device is idle for idleTimeoutInSecond then the device should timeout
    #' @param serial_number (optional) The serial number on the device
    #' @param udid (optional) The unique device id for the device
    #' @param device_type (optional) The type of the device, for example: \"Kiosk\", \"Beacon\".
    #' @param device_power (optional) RSSI reading at 1 meter from device (-10 to -140)
    #' @param device_interference (optional) 
    #' @param availability (optional) 
    #' @param availability_summary (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerOfferResponse) with additional information such as HTTP status code, headers
    UpdateOfferWithHttpInfo = function(offer_id, include_offer_locations, device_id = NULL, account_id = NULL, parent_offer_id = NULL, retailer_location_ids = NULL, offer_locations = NULL, tags = NULL, title = NULL, sub_title = NULL, details = NULL, sub_details = NULL, fine_print = NULL, barcode_type = NULL, barcode_entry = NULL, external_redeem_options = NULL, external_url = NULL, external_id = NULL, tickets_reward_type = NULL, tickets_reward = NULL, activated = NULL, expires = NULL, no_expiration = NULL, available_limit = NULL, available_limit_per_user = NULL, added_limit = NULL, view_limit = NULL, max_prints = NULL, ticket_price_type = NULL, ticket_price = NULL, full_price = NULL, discount_price = NULL, show_remaining = NULL, show_redeemed = NULL, replaced = NULL, featured = NULL, offer_type = NULL, special_offer_type = NULL, offer_visibility = NULL, category_ids = NULL, filter_ids = NULL, active = NULL, barcode_asset_id = NULL, image_asset_id = NULL, image_asset_id1 = NULL, image_asset_id2 = NULL, image_asset_id3 = NULL, image_asset_id4 = NULL, image_asset_id5 = NULL, publisher = NULL, redeemable_start = NULL, redeemable_end = NULL, brand = NULL, product_type = NULL, condition_type = NULL, isbn = NULL, asin = NULL, catalog_numbers = NULL, department = NULL, features = NULL, minimum_price = NULL, width = NULL, height = NULL, depth = NULL, weight = NULL, unit = NULL, studio = NULL, parental_rating = NULL, publish_date = NULL, availability_date = NULL, size_id = NULL, listing_id = NULL, media_type = NULL, duration = NULL, author = NULL, release_date = NULL, collection_ids = NULL, reboot_time_hour = NULL, reboot_time_minute = NULL, idle_timeout_in_second = NULL, serial_number = NULL, udid = NULL, device_type = NULL, device_power = NULL, device_interference = NULL, availability = NULL, availability_summary = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offer_id`)) {
        stop("Missing required parameter `offer_id`.")
      }

      if (missing(`include_offer_locations`)) {
        stop("Missing required parameter `include_offer_locations`.")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling OfferApi$UpdateOffer, `offer_id` is not nullable")
      }

      if (!missing(`include_offer_locations`) && is.null(`include_offer_locations`)) {
        stop("Invalid value for `include_offer_locations` when calling OfferApi$UpdateOffer, `include_offer_locations` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$UpdateOffer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$UpdateOffer, `account_id` is not nullable")
      }

      if (!missing(`parent_offer_id`) && is.null(`parent_offer_id`)) {
        stop("Invalid value for `parent_offer_id` when calling OfferApi$UpdateOffer, `parent_offer_id` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling OfferApi$UpdateOffer, `retailer_location_ids` is not nullable")
      }

      if (!missing(`offer_locations`) && is.null(`offer_locations`)) {
        stop("Invalid value for `offer_locations` when calling OfferApi$UpdateOffer, `offer_locations` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling OfferApi$UpdateOffer, `tags` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling OfferApi$UpdateOffer, `title` is not nullable")
      }

      if (!missing(`sub_title`) && is.null(`sub_title`)) {
        stop("Invalid value for `sub_title` when calling OfferApi$UpdateOffer, `sub_title` is not nullable")
      }

      if (!missing(`details`) && is.null(`details`)) {
        stop("Invalid value for `details` when calling OfferApi$UpdateOffer, `details` is not nullable")
      }

      if (!missing(`sub_details`) && is.null(`sub_details`)) {
        stop("Invalid value for `sub_details` when calling OfferApi$UpdateOffer, `sub_details` is not nullable")
      }

      if (!missing(`fine_print`) && is.null(`fine_print`)) {
        stop("Invalid value for `fine_print` when calling OfferApi$UpdateOffer, `fine_print` is not nullable")
      }

      if (!missing(`barcode_type`) && is.null(`barcode_type`)) {
        stop("Invalid value for `barcode_type` when calling OfferApi$UpdateOffer, `barcode_type` is not nullable")
      }

      if (!missing(`barcode_entry`) && is.null(`barcode_entry`)) {
        stop("Invalid value for `barcode_entry` when calling OfferApi$UpdateOffer, `barcode_entry` is not nullable")
      }

      if (!missing(`external_redeem_options`) && is.null(`external_redeem_options`)) {
        stop("Invalid value for `external_redeem_options` when calling OfferApi$UpdateOffer, `external_redeem_options` is not nullable")
      }

      if (!missing(`external_url`) && is.null(`external_url`)) {
        stop("Invalid value for `external_url` when calling OfferApi$UpdateOffer, `external_url` is not nullable")
      }

      if (!missing(`external_id`) && is.null(`external_id`)) {
        stop("Invalid value for `external_id` when calling OfferApi$UpdateOffer, `external_id` is not nullable")
      }

      if (!missing(`tickets_reward_type`) && is.null(`tickets_reward_type`)) {
        stop("Invalid value for `tickets_reward_type` when calling OfferApi$UpdateOffer, `tickets_reward_type` is not nullable")
      }

      if (!missing(`tickets_reward`) && is.null(`tickets_reward`)) {
        stop("Invalid value for `tickets_reward` when calling OfferApi$UpdateOffer, `tickets_reward` is not nullable")
      }

      if (!missing(`activated`) && is.null(`activated`)) {
        stop("Invalid value for `activated` when calling OfferApi$UpdateOffer, `activated` is not nullable")
      }

      if (!missing(`expires`) && is.null(`expires`)) {
        stop("Invalid value for `expires` when calling OfferApi$UpdateOffer, `expires` is not nullable")
      }

      if (!missing(`no_expiration`) && is.null(`no_expiration`)) {
        stop("Invalid value for `no_expiration` when calling OfferApi$UpdateOffer, `no_expiration` is not nullable")
      }

      if (!missing(`available_limit`) && is.null(`available_limit`)) {
        stop("Invalid value for `available_limit` when calling OfferApi$UpdateOffer, `available_limit` is not nullable")
      }

      if (!missing(`available_limit_per_user`) && is.null(`available_limit_per_user`)) {
        stop("Invalid value for `available_limit_per_user` when calling OfferApi$UpdateOffer, `available_limit_per_user` is not nullable")
      }

      if (!missing(`added_limit`) && is.null(`added_limit`)) {
        stop("Invalid value for `added_limit` when calling OfferApi$UpdateOffer, `added_limit` is not nullable")
      }

      if (!missing(`view_limit`) && is.null(`view_limit`)) {
        stop("Invalid value for `view_limit` when calling OfferApi$UpdateOffer, `view_limit` is not nullable")
      }

      if (!missing(`max_prints`) && is.null(`max_prints`)) {
        stop("Invalid value for `max_prints` when calling OfferApi$UpdateOffer, `max_prints` is not nullable")
      }

      if (!missing(`ticket_price_type`) && is.null(`ticket_price_type`)) {
        stop("Invalid value for `ticket_price_type` when calling OfferApi$UpdateOffer, `ticket_price_type` is not nullable")
      }

      if (!missing(`ticket_price`) && is.null(`ticket_price`)) {
        stop("Invalid value for `ticket_price` when calling OfferApi$UpdateOffer, `ticket_price` is not nullable")
      }

      if (!missing(`full_price`) && is.null(`full_price`)) {
        stop("Invalid value for `full_price` when calling OfferApi$UpdateOffer, `full_price` is not nullable")
      }

      if (!missing(`discount_price`) && is.null(`discount_price`)) {
        stop("Invalid value for `discount_price` when calling OfferApi$UpdateOffer, `discount_price` is not nullable")
      }

      if (!missing(`show_remaining`) && is.null(`show_remaining`)) {
        stop("Invalid value for `show_remaining` when calling OfferApi$UpdateOffer, `show_remaining` is not nullable")
      }

      if (!missing(`show_redeemed`) && is.null(`show_redeemed`)) {
        stop("Invalid value for `show_redeemed` when calling OfferApi$UpdateOffer, `show_redeemed` is not nullable")
      }

      if (!missing(`replaced`) && is.null(`replaced`)) {
        stop("Invalid value for `replaced` when calling OfferApi$UpdateOffer, `replaced` is not nullable")
      }

      if (!missing(`featured`) && is.null(`featured`)) {
        stop("Invalid value for `featured` when calling OfferApi$UpdateOffer, `featured` is not nullable")
      }

      if (!missing(`offer_type`) && is.null(`offer_type`)) {
        stop("Invalid value for `offer_type` when calling OfferApi$UpdateOffer, `offer_type` is not nullable")
      }

      if (!missing(`special_offer_type`) && is.null(`special_offer_type`)) {
        stop("Invalid value for `special_offer_type` when calling OfferApi$UpdateOffer, `special_offer_type` is not nullable")
      }

      if (!missing(`offer_visibility`) && is.null(`offer_visibility`)) {
        stop("Invalid value for `offer_visibility` when calling OfferApi$UpdateOffer, `offer_visibility` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling OfferApi$UpdateOffer, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling OfferApi$UpdateOffer, `filter_ids` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling OfferApi$UpdateOffer, `active` is not nullable")
      }

      if (!missing(`barcode_asset_id`) && is.null(`barcode_asset_id`)) {
        stop("Invalid value for `barcode_asset_id` when calling OfferApi$UpdateOffer, `barcode_asset_id` is not nullable")
      }

      if (!missing(`image_asset_id`) && is.null(`image_asset_id`)) {
        stop("Invalid value for `image_asset_id` when calling OfferApi$UpdateOffer, `image_asset_id` is not nullable")
      }

      if (!missing(`image_asset_id1`) && is.null(`image_asset_id1`)) {
        stop("Invalid value for `image_asset_id1` when calling OfferApi$UpdateOffer, `image_asset_id1` is not nullable")
      }

      if (!missing(`image_asset_id2`) && is.null(`image_asset_id2`)) {
        stop("Invalid value for `image_asset_id2` when calling OfferApi$UpdateOffer, `image_asset_id2` is not nullable")
      }

      if (!missing(`image_asset_id3`) && is.null(`image_asset_id3`)) {
        stop("Invalid value for `image_asset_id3` when calling OfferApi$UpdateOffer, `image_asset_id3` is not nullable")
      }

      if (!missing(`image_asset_id4`) && is.null(`image_asset_id4`)) {
        stop("Invalid value for `image_asset_id4` when calling OfferApi$UpdateOffer, `image_asset_id4` is not nullable")
      }

      if (!missing(`image_asset_id5`) && is.null(`image_asset_id5`)) {
        stop("Invalid value for `image_asset_id5` when calling OfferApi$UpdateOffer, `image_asset_id5` is not nullable")
      }

      if (!missing(`publisher`) && is.null(`publisher`)) {
        stop("Invalid value for `publisher` when calling OfferApi$UpdateOffer, `publisher` is not nullable")
      }

      if (!missing(`redeemable_start`) && is.null(`redeemable_start`)) {
        stop("Invalid value for `redeemable_start` when calling OfferApi$UpdateOffer, `redeemable_start` is not nullable")
      }

      if (!missing(`redeemable_end`) && is.null(`redeemable_end`)) {
        stop("Invalid value for `redeemable_end` when calling OfferApi$UpdateOffer, `redeemable_end` is not nullable")
      }

      if (!missing(`brand`) && is.null(`brand`)) {
        stop("Invalid value for `brand` when calling OfferApi$UpdateOffer, `brand` is not nullable")
      }

      if (!missing(`product_type`) && is.null(`product_type`)) {
        stop("Invalid value for `product_type` when calling OfferApi$UpdateOffer, `product_type` is not nullable")
      }

      if (!missing(`condition_type`) && is.null(`condition_type`)) {
        stop("Invalid value for `condition_type` when calling OfferApi$UpdateOffer, `condition_type` is not nullable")
      }

      if (!missing(`isbn`) && is.null(`isbn`)) {
        stop("Invalid value for `isbn` when calling OfferApi$UpdateOffer, `isbn` is not nullable")
      }

      if (!missing(`asin`) && is.null(`asin`)) {
        stop("Invalid value for `asin` when calling OfferApi$UpdateOffer, `asin` is not nullable")
      }

      if (!missing(`catalog_numbers`) && is.null(`catalog_numbers`)) {
        stop("Invalid value for `catalog_numbers` when calling OfferApi$UpdateOffer, `catalog_numbers` is not nullable")
      }

      if (!missing(`department`) && is.null(`department`)) {
        stop("Invalid value for `department` when calling OfferApi$UpdateOffer, `department` is not nullable")
      }

      if (!missing(`features`) && is.null(`features`)) {
        stop("Invalid value for `features` when calling OfferApi$UpdateOffer, `features` is not nullable")
      }

      if (!missing(`minimum_price`) && is.null(`minimum_price`)) {
        stop("Invalid value for `minimum_price` when calling OfferApi$UpdateOffer, `minimum_price` is not nullable")
      }

      if (!missing(`width`) && is.null(`width`)) {
        stop("Invalid value for `width` when calling OfferApi$UpdateOffer, `width` is not nullable")
      }

      if (!missing(`height`) && is.null(`height`)) {
        stop("Invalid value for `height` when calling OfferApi$UpdateOffer, `height` is not nullable")
      }

      if (!missing(`depth`) && is.null(`depth`)) {
        stop("Invalid value for `depth` when calling OfferApi$UpdateOffer, `depth` is not nullable")
      }

      if (!missing(`weight`) && is.null(`weight`)) {
        stop("Invalid value for `weight` when calling OfferApi$UpdateOffer, `weight` is not nullable")
      }

      if (!missing(`unit`) && is.null(`unit`)) {
        stop("Invalid value for `unit` when calling OfferApi$UpdateOffer, `unit` is not nullable")
      }

      if (!missing(`studio`) && is.null(`studio`)) {
        stop("Invalid value for `studio` when calling OfferApi$UpdateOffer, `studio` is not nullable")
      }

      if (!missing(`parental_rating`) && is.null(`parental_rating`)) {
        stop("Invalid value for `parental_rating` when calling OfferApi$UpdateOffer, `parental_rating` is not nullable")
      }

      if (!missing(`publish_date`) && is.null(`publish_date`)) {
        stop("Invalid value for `publish_date` when calling OfferApi$UpdateOffer, `publish_date` is not nullable")
      }

      if (!missing(`availability_date`) && is.null(`availability_date`)) {
        stop("Invalid value for `availability_date` when calling OfferApi$UpdateOffer, `availability_date` is not nullable")
      }

      if (!missing(`size_id`) && is.null(`size_id`)) {
        stop("Invalid value for `size_id` when calling OfferApi$UpdateOffer, `size_id` is not nullable")
      }

      if (!missing(`listing_id`) && is.null(`listing_id`)) {
        stop("Invalid value for `listing_id` when calling OfferApi$UpdateOffer, `listing_id` is not nullable")
      }

      if (!missing(`media_type`) && is.null(`media_type`)) {
        stop("Invalid value for `media_type` when calling OfferApi$UpdateOffer, `media_type` is not nullable")
      }

      if (!missing(`duration`) && is.null(`duration`)) {
        stop("Invalid value for `duration` when calling OfferApi$UpdateOffer, `duration` is not nullable")
      }

      if (!missing(`author`) && is.null(`author`)) {
        stop("Invalid value for `author` when calling OfferApi$UpdateOffer, `author` is not nullable")
      }

      if (!missing(`release_date`) && is.null(`release_date`)) {
        stop("Invalid value for `release_date` when calling OfferApi$UpdateOffer, `release_date` is not nullable")
      }

      if (!missing(`collection_ids`) && is.null(`collection_ids`)) {
        stop("Invalid value for `collection_ids` when calling OfferApi$UpdateOffer, `collection_ids` is not nullable")
      }

      if (!missing(`reboot_time_hour`) && is.null(`reboot_time_hour`)) {
        stop("Invalid value for `reboot_time_hour` when calling OfferApi$UpdateOffer, `reboot_time_hour` is not nullable")
      }

      if (!missing(`reboot_time_minute`) && is.null(`reboot_time_minute`)) {
        stop("Invalid value for `reboot_time_minute` when calling OfferApi$UpdateOffer, `reboot_time_minute` is not nullable")
      }

      if (!missing(`idle_timeout_in_second`) && is.null(`idle_timeout_in_second`)) {
        stop("Invalid value for `idle_timeout_in_second` when calling OfferApi$UpdateOffer, `idle_timeout_in_second` is not nullable")
      }

      if (!missing(`serial_number`) && is.null(`serial_number`)) {
        stop("Invalid value for `serial_number` when calling OfferApi$UpdateOffer, `serial_number` is not nullable")
      }

      if (!missing(`udid`) && is.null(`udid`)) {
        stop("Invalid value for `udid` when calling OfferApi$UpdateOffer, `udid` is not nullable")
      }

      if (!missing(`device_type`) && is.null(`device_type`)) {
        stop("Invalid value for `device_type` when calling OfferApi$UpdateOffer, `device_type` is not nullable")
      }

      if (!missing(`device_power`) && is.null(`device_power`)) {
        stop("Invalid value for `device_power` when calling OfferApi$UpdateOffer, `device_power` is not nullable")
      }

      if (!missing(`device_interference`) && is.null(`device_interference`)) {
        stop("Invalid value for `device_interference` when calling OfferApi$UpdateOffer, `device_interference` is not nullable")
      }

      if (!missing(`availability`) && is.null(`availability`)) {
        stop("Invalid value for `availability` when calling OfferApi$UpdateOffer, `availability` is not nullable")
      }

      if (!missing(`availability_summary`) && is.null(`availability_summary`)) {
        stop("Invalid value for `availability_summary` when calling OfferApi$UpdateOffer, `availability_summary` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["parentOfferId"]] <- `parent_offer_id`

      query_params[["includeOfferLocations"]] <- `include_offer_locations`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["offerLocations"]] <- `offer_locations`

      query_params[["tags"]] <- `tags`

      query_params[["title"]] <- `title`

      query_params[["subTitle"]] <- `sub_title`

      query_params[["details"]] <- `details`

      query_params[["subDetails"]] <- `sub_details`

      query_params[["finePrint"]] <- `fine_print`

      if (!is.null(`barcode_type`) && !(`barcode_type` %in% c("NONE", "UPC", "CODE_128", "QR", "CUSTOM_MEDIA"))) {
        stop("Invalid value for barcode_type when calling OfferApi$UpdateOffer. Must be [NONE, UPC, CODE_128, QR, CUSTOM_MEDIA].")
      }
      query_params[["barcodeType"]] <- `barcode_type`

      query_params[["barcodeEntry"]] <- `barcode_entry`

      query_params[["externalRedeemOptions"]] <- `external_redeem_options`

      query_params[["externalUrl"]] <- `external_url`

      query_params[["externalId"]] <- `external_id`

      query_params[["ticketsRewardType"]] <- `tickets_reward_type`

      query_params[["ticketsReward"]] <- `tickets_reward`

      query_params[["activated"]] <- `activated`

      query_params[["expires"]] <- `expires`

      query_params[["noExpiration"]] <- `no_expiration`

      query_params[["availableLimit"]] <- `available_limit`

      query_params[["availableLimitPerUser"]] <- `available_limit_per_user`

      query_params[["addedLimit"]] <- `added_limit`

      query_params[["viewLimit"]] <- `view_limit`

      query_params[["maxPrints"]] <- `max_prints`

      query_params[["ticketPriceType"]] <- `ticket_price_type`

      query_params[["ticketPrice"]] <- `ticket_price`

      query_params[["fullPrice"]] <- `full_price`

      query_params[["discountPrice"]] <- `discount_price`

      query_params[["showRemaining"]] <- `show_remaining`

      query_params[["showRedeemed"]] <- `show_redeemed`

      query_params[["replaced"]] <- `replaced`

      query_params[["featured"]] <- `featured`

      if (!is.null(`offer_type`) && !(`offer_type` %in% c("VOUCHER", "COUPON", "PRODUCT", "MEDIA", "EVENT", "DEVICE"))) {
        stop("Invalid value for offer_type when calling OfferApi$UpdateOffer. Must be [VOUCHER, COUPON, PRODUCT, MEDIA, EVENT, DEVICE].")
      }
      query_params[["offerType"]] <- `offer_type`

      if (!is.null(`special_offer_type`) && !(`special_offer_type` %in% c("ALL", "RESERVABLE", "REGULAR_OFFER", "ACT_NOW", "GET_THERE_NOW", "SQOOT", "TICKETS", "YIPIT"))) {
        stop("Invalid value for special_offer_type when calling OfferApi$UpdateOffer. Must be [ALL, RESERVABLE, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW, SQOOT, TICKETS, YIPIT].")
      }
      query_params[["specialOfferType"]] <- `special_offer_type`

      if (!is.null(`offer_visibility`) && !(`offer_visibility` %in% c("PUBLIC", "LISTABLE", "REWARDABLE", "TRIGGERABLE", "PRIVATE"))) {
        stop("Invalid value for offer_visibility when calling OfferApi$UpdateOffer. Must be [PUBLIC, LISTABLE, REWARDABLE, TRIGGERABLE, PRIVATE].")
      }
      query_params[["offerVisibility"]] <- `offer_visibility`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["active"]] <- `active`

      query_params[["barcodeAssetId"]] <- `barcode_asset_id`

      query_params[["imageAssetId"]] <- `image_asset_id`

      query_params[["imageAssetId1"]] <- `image_asset_id1`

      query_params[["imageAssetId2"]] <- `image_asset_id2`

      query_params[["imageAssetId3"]] <- `image_asset_id3`

      query_params[["imageAssetId4"]] <- `image_asset_id4`

      query_params[["imageAssetId5"]] <- `image_asset_id5`

      query_params[["publisher"]] <- `publisher`

      query_params[["redeemableStart"]] <- `redeemable_start`

      query_params[["redeemableEnd"]] <- `redeemable_end`

      query_params[["brand"]] <- `brand`

      if (!is.null(`product_type`) && !(`product_type` %in% c("APPAREL", "APPLIANCES", "APPS", "ARTS_CRAFTS", "AUTOMOTIVE", "BABY", "BEAUTY", "BOOKS", "CLASSICAL", "COLLECTIBLES", "DVD", "ELECTRONICS", "GIFT_CARDS", "GOURMET_FOOD", "GROCERY", "HEALTH_PERSONAL_CARE", "HOME_GARDEN", "INDUSTRIAL", "JEWELRY", "KITCHEN", "LAWN_GARDEN", "MAGAZINES", "MISCELLANEOUS", "MUSICAL_INSTRUMENTS", "OFFICE_PRODUCTS", "OTHER", "OUTDOOR_LIVING", "PC_HARDWARE", "PET_SUPPLIES", "SHOES", "SOFTWARE", "SPORTING_GOODS", "TOOLS", "TOYS", "VIDEO_GAMES", "WATCHES", "WIRELESS", "WIRELESS_ACCESSORIES"))) {
        stop("Invalid value for product_type when calling OfferApi$UpdateOffer. Must be [APPAREL, APPLIANCES, APPS, ARTS_CRAFTS, AUTOMOTIVE, BABY, BEAUTY, BOOKS, CLASSICAL, COLLECTIBLES, DVD, ELECTRONICS, GIFT_CARDS, GOURMET_FOOD, GROCERY, HEALTH_PERSONAL_CARE, HOME_GARDEN, INDUSTRIAL, JEWELRY, KITCHEN, LAWN_GARDEN, MAGAZINES, MISCELLANEOUS, MUSICAL_INSTRUMENTS, OFFICE_PRODUCTS, OTHER, OUTDOOR_LIVING, PC_HARDWARE, PET_SUPPLIES, SHOES, SOFTWARE, SPORTING_GOODS, TOOLS, TOYS, VIDEO_GAMES, WATCHES, WIRELESS, WIRELESS_ACCESSORIES].")
      }
      query_params[["productType"]] <- `product_type`

      if (!is.null(`condition_type`) && !(`condition_type` %in% c("NEW", "USED", "REFURBISHED", "OTHER"))) {
        stop("Invalid value for condition_type when calling OfferApi$UpdateOffer. Must be [NEW, USED, REFURBISHED, OTHER].")
      }
      query_params[["conditionType"]] <- `condition_type`

      query_params[["isbn"]] <- `isbn`

      query_params[["asin"]] <- `asin`

      query_params[["catalogNumbers"]] <- `catalog_numbers`

      query_params[["department"]] <- `department`

      query_params[["features"]] <- `features`

      query_params[["minimumPrice"]] <- `minimum_price`

      query_params[["width"]] <- `width`

      query_params[["height"]] <- `height`

      query_params[["depth"]] <- `depth`

      query_params[["weight"]] <- `weight`

      if (!is.null(`unit`) && !(`unit` %in% c("MILIMETER", "CENTIMETER", "METER", "INCH", "FOOT", "YARD"))) {
        stop("Invalid value for unit when calling OfferApi$UpdateOffer. Must be [MILIMETER, CENTIMETER, METER, INCH, FOOT, YARD].")
      }
      query_params[["unit"]] <- `unit`

      query_params[["studio"]] <- `studio`

      query_params[["parentalRating"]] <- `parental_rating`

      query_params[["publishDate"]] <- `publish_date`

      query_params[["availabilityDate"]] <- `availability_date`

      query_params[["sizeId"]] <- `size_id`

      query_params[["listingId"]] <- `listing_id`

      if (!is.null(`media_type`) && !(`media_type` %in% c("APK", "APPLICATION", "IMAGE", "AUDIO", "VIDEO", "MULTIPART", "BAR_CODE", "TEXT", "OTHER"))) {
        stop("Invalid value for media_type when calling OfferApi$UpdateOffer. Must be [APK, APPLICATION, IMAGE, AUDIO, VIDEO, MULTIPART, BAR_CODE, TEXT, OTHER].")
      }
      query_params[["mediaType"]] <- `media_type`

      query_params[["duration"]] <- `duration`

      query_params[["author"]] <- `author`

      query_params[["releaseDate"]] <- `release_date`

      query_params[["collectionIds"]] <- `collection_ids`

      query_params[["rebootTimeHour"]] <- `reboot_time_hour`

      query_params[["rebootTimeMinute"]] <- `reboot_time_minute`

      query_params[["idleTimeoutInSecond"]] <- `idle_timeout_in_second`

      query_params[["serialNumber"]] <- `serial_number`

      query_params[["udid"]] <- `udid`

      query_params[["deviceType"]] <- `device_type`

      query_params[["devicePower"]] <- `device_power`

      query_params[["deviceInterference"]] <- `device_interference`

      query_params[["availability"]] <- `availability`

      query_params[["availabilitySummary"]] <- `availability_summary`

      local_var_url_path <- "/retailer/offer/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerOfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Activate Offer
    #'
    #' @param offer_ids Comma separated list of offer ids
    #' @param active Determines whether to make the offer active as well
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the activation, must have rights to edit the offer.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UpdateOfferStatus = function(offer_ids, active, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateOfferStatusWithHttpInfo(offer_ids, active, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Activate Offer
    #'
    #' @param offer_ids Comma separated list of offer ids
    #' @param active Determines whether to make the offer active as well
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the activation, must have rights to edit the offer.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UpdateOfferStatusWithHttpInfo = function(offer_ids, active, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offer_ids`)) {
        stop("Missing required parameter `offer_ids`.")
      }

      if (missing(`active`)) {
        stop("Missing required parameter `active`.")
      }

      if (!missing(`offer_ids`) && is.null(`offer_ids`)) {
        stop("Invalid value for `offer_ids` when calling OfferApi$UpdateOfferStatus, `offer_ids` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling OfferApi$UpdateOfferStatus, `active` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling OfferApi$UpdateOfferStatus, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling OfferApi$UpdateOfferStatus, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["offerIds"]] <- `offer_ids`

      query_params[["active"]] <- `active`

      local_var_url_path <- "/retailer/offer/status"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
