#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title GameLevel operations
#' @description GameLevelApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateGameLevel  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_name <- "name_example" # character | The name of the level.
#' var_game_data <- "game_data_example" # character | The game level data: xml, json, or other text based format.
#' var_game_data_suffix <- "game_data_suffix_example" # character | The game level data format type.
#' var_app_key <- "app_key_example" # character | The game application key to save the level for. (Optional)
#' var_description <- "description_example" # character | The description of the level. (Optional)
#' var_difficulty <- "difficulty_example" # character | The difficulty, possible values are: VERY_EASY, EASY, MEDIUM, HARD, VERY_HARD. (Optional)
#' var_app_version <- "app_version_example" # character | The version number of the application required to correctly load/play the level. (Optional)
#' var_asset_image_id <- 56 # integer | The asset Id of the level image. (Optional)
#' var_asset_icon_id <- 56 # integer | The asset Id of the level icon. (Optional)
#' var_visibility <- "visibility_example" # character | Is the level visible to others, possible values are: PUBLIC, PRIVATE. (Optional)
#' var_friend_group <- "friend_group_example" # character | Make the level be readable by all friends. (Optional)
#' var_connection_ids <- "connection_ids_example" # character | Make the level be readable by connections in this list. (Optional)
#' var_connection_group_ids <- "connection_group_ids_example" # character | Make the level be readable by connection groups in this list. (Optional)
#' var_balance <- 3.4 # numeric | Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited. (Optional)
#' var_active <- "active_example" # character | If true set the game level as active. Default is false. (Optional)
#' var_allocate_tickets <- "allocate_tickets_example" # character | If true then scoring will give tickets. Default is false. (Optional)
#' var_ticket_count <- 56 # integer | The number of tickets to reward (Optional)
#' var_ticket_type <- "ticket_type_example" # character | The type of ticket to reward, null means default type (Optional)
#' var_points <- 56 # integer | The number of points to award for completing a level (Optional)
#' var_tutorial_title <- "tutorial_title_example" # character | Title of the tutorial. (Optional)
#' var_tutorial_message <- "tutorial_message_example" # character | Message of the tutotrial. (Optional)
#' var_tutorial_alignment <- "tutorial_alignment_example" # character | Alignment of the tutorial image. Default to NONE. Possible values are: NONE, IMAGE_ABOVE, IMAGE_BELOW, IMAGE_LEFT, IMAGE_RIGHT, IMAGE_ONLY, TEXT_ONLY (Optional)
#' var_tutorial_image_asset_id <- 56 # integer | Asset id of the tutorial image. (Optional)
#' var_offer_id <- 56 # integer | id of the offer (Optional)
#' var_meta_data <- "meta_data_example" # character | external custom client defined data (Optional)
#'
#' #Create Game Level
#' api_instance <- GameLevelApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateGameLevel(var_account_id, var_name, var_game_data, var_game_data_suffix, app_key = var_app_key, description = var_description, difficulty = var_difficulty, app_version = var_app_version, asset_image_id = var_asset_image_id, asset_icon_id = var_asset_icon_id, visibility = var_visibility, friend_group = var_friend_group, connection_ids = var_connection_ids, connection_group_ids = var_connection_group_ids, balance = var_balance, active = var_active, allocate_tickets = var_allocate_tickets, ticket_count = var_ticket_count, ticket_type = var_ticket_type, points = var_points, tutorial_title = var_tutorial_title, tutorial_message = var_tutorial_message, tutorial_alignment = var_tutorial_alignment, tutorial_image_asset_id = var_tutorial_image_asset_id, offer_id = var_offer_id, meta_data = var_meta_datadata_file = "result.txt")
#' result <- api_instance$CreateGameLevel(var_account_id, var_name, var_game_data, var_game_data_suffix, app_key = var_app_key, description = var_description, difficulty = var_difficulty, app_version = var_app_version, asset_image_id = var_asset_image_id, asset_icon_id = var_asset_icon_id, visibility = var_visibility, friend_group = var_friend_group, connection_ids = var_connection_ids, connection_group_ids = var_connection_group_ids, balance = var_balance, active = var_active, allocate_tickets = var_allocate_tickets, ticket_count = var_ticket_count, ticket_type = var_ticket_type, points = var_points, tutorial_title = var_tutorial_title, tutorial_message = var_tutorial_message, tutorial_alignment = var_tutorial_alignment, tutorial_image_asset_id = var_tutorial_image_asset_id, offer_id = var_offer_id, meta_data = var_meta_data)
#' dput(result)
#'
#'
#' ####################  DeleteGameLevel  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_level_id <- 56 # integer | The id of the level to return.
#'
#' #Delete Game Level
#' api_instance <- GameLevelApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteGameLevel(var_account_id, var_level_iddata_file = "result.txt")
#' result <- api_instance$DeleteGameLevel(var_account_id, var_level_id)
#' dput(result)
#'
#'
#' ####################  GetGameLevel  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_level_id <- 56 # integer | The id of the level to return.
#' var_include_game_data <- "include_game_data_example" # character | If true include the game level data, otherwise don't. default is false. (Optional)
#'
#' #Get Game Level
#' api_instance <- GameLevelApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetGameLevel(var_account_id, var_level_id, include_game_data = var_include_game_datadata_file = "result.txt")
#' result <- api_instance$GetGameLevel(var_account_id, var_level_id, include_game_data = var_include_game_data)
#' dput(result)
#'
#'
#' ####################  GetGameLevelsByApplication  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_app_key <- "app_key_example" # character | the application key
#' var_keyword <- "keyword_example" # character | Match the keyword to the owner name or level name. (Optional)
#' var_sort_field <- "sort_field_example" # character | what field to sort on (Optional)
#' var_descending <- "descending_example" # character | whether to return levels in ascending or descending order (Optional)
#' var_start <- 56 # integer | Start the result set at some index. (Optional)
#' var_limit <- 56 # integer | Limit the result to some number. (Optional)
#' var_app_version <- "app_version_example" # character | The maximum version of the level to return. (Optional)
#' var_include_game_data <- "include_game_data_example" # character | If true include the game level data, otherwise don't. default is false. (Optional)
#' var_filters <- "filters_example" # character |  (Optional)
#'
#' #Search Game Levels
#' api_instance <- GameLevelApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetGameLevelsByApplication(var_account_id, var_app_key, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, app_version = var_app_version, include_game_data = var_include_game_data, filters = var_filtersdata_file = "result.txt")
#' result <- api_instance$GetGameLevelsByApplication(var_account_id, var_app_key, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, app_version = var_app_version, include_game_data = var_include_game_data, filters = var_filters)
#' dput(result)
#'
#'
#' ####################  GetGameLevelsByBillableEntity  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on (Optional)
#' var_descending <- "descending_example" # character | The order to return the search results (Optional)
#' var_active_only <- "active_only_example" # character | Return only active results (Optional)
#' var_start <- 56 # integer | The record to begin the return set on (Optional)
#' var_limit <- 56 # integer | The number of records to return (Optional)
#'
#' #Search Game Level by Billable Entity
#' api_instance <- GameLevelApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetGameLevelsByBillableEntity(var_account_id, app_key = var_app_key, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, active_only = var_active_only, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GetGameLevelsByBillableEntity(var_account_id, app_key = var_app_key, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, active_only = var_active_only, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  GetQuestionsInLevel  ####################
#'
#' library(openapi)
#' var_level_id <- 56 # integer | the id of the level to get questions from
#' var_account_id <- 56 # integer | the id of the logged in user
#'
#' #Get Level Questions
#' api_instance <- GameLevelApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetQuestionsInLevel(var_level_id, var_account_iddata_file = "result.txt")
#' result <- api_instance$GetQuestionsInLevel(var_level_id, var_account_id)
#' dput(result)
#'
#'
#' ####################  GetWordsInLevel  ####################
#'
#' library(openapi)
#' var_level_id <- 56 # integer | the id of the level to get words for
#' var_account_id <- 56 # integer | the id of the logged in user
#'
#' #Get Level Words
#' api_instance <- GameLevelApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetWordsInLevel(var_level_id, var_account_iddata_file = "result.txt")
#' result <- api_instance$GetWordsInLevel(var_level_id, var_account_id)
#' dput(result)
#'
#'
#' ####################  UpdateGameLevel  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_level_id <- 56 # integer | If update then include the level Id.
#' var_app_key <- "app_key_example" # character | The game application key to save the level for. (Optional)
#' var_name <- "name_example" # character | The name of the level. (Optional)
#' var_description <- "description_example" # character | The description of the level. (Optional)
#' var_difficulty <- "difficulty_example" # character | The difficulty, possible values are: VERY_EASY, EASY, MEDIUM, HARD, VERY_HARD. (Optional)
#' var_app_version <- "app_version_example" # character | The version number of the applicatuion required to correctly load/play the level. (Optional)
#' var_asset_image_id <- 56 # integer | The asset Id of the level image. (Optional)
#' var_asset_icon_id <- 56 # integer | The asset Id of the level icon. (Optional)
#' var_game_data <- "game_data_example" # character | The game level data: xml, json, or other texted based format. (Optional)
#' var_game_data_suffix <- "game_data_suffix_example" # character | The game level data format type. (Optional)
#' var_visibility <- "visibility_example" # character | Is the level visible to others, possible values are: PUBLIC, PRIVATE. (Optional)
#' var_friend_group <- "friend_group_example" # character | Make the level be readable by all friends. (Optional)
#' var_connection_ids <- "connection_ids_example" # character | Make the level be readable by connections in this list. (Optional)
#' var_connection_group_ids <- "connection_group_ids_example" # character | Make the level be readable by connection groups in this list. (Optional)
#' var_balance <- 3.4 # numeric | Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited. (Optional)
#' var_active <- "active_example" # character | If true set the game level as active. (Optional)
#' var_allocate_tickets <- "allocate_tickets_example" # character | If true then scoring will give tickets. (Optional)
#' var_ticket_count <- 56 # integer | The number of tickets to reward (Optional)
#' var_ticket_type <- "ticket_type_example" # character | The type of ticket to reward, null means default type (Optional)
#' var_points <- 56 # integer | The number of points to award for completing a level (Optional)
#' var_tutorial_title <- "tutorial_title_example" # character | Title of the tutorial. (Optional)
#' var_tutorial_message <- "tutorial_message_example" # character | Message of the tutorial. (Optional)
#' var_tutorial_alignment <- "tutorial_alignment_example" # character | Alignment of the tutorial image, possible values are: NONE, IMAGE_ABOVE, IMAGE_BELOW, IMAGE_LEFT, IMAGE_RIGHT, IMAGE_ONLY, TEXT_ONLY (Optional)
#' var_tutorial_image_asset_id <- 56 # integer | Asset id of the tutorial image. (Optional)
#' var_offer_id <- 56 # integer |  (Optional)
#' var_meta_data <- "meta_data_example" # character | external custom client defined data (Optional)
#'
#' #Update Game Level
#' api_instance <- GameLevelApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateGameLevel(var_account_id, var_level_id, app_key = var_app_key, name = var_name, description = var_description, difficulty = var_difficulty, app_version = var_app_version, asset_image_id = var_asset_image_id, asset_icon_id = var_asset_icon_id, game_data = var_game_data, game_data_suffix = var_game_data_suffix, visibility = var_visibility, friend_group = var_friend_group, connection_ids = var_connection_ids, connection_group_ids = var_connection_group_ids, balance = var_balance, active = var_active, allocate_tickets = var_allocate_tickets, ticket_count = var_ticket_count, ticket_type = var_ticket_type, points = var_points, tutorial_title = var_tutorial_title, tutorial_message = var_tutorial_message, tutorial_alignment = var_tutorial_alignment, tutorial_image_asset_id = var_tutorial_image_asset_id, offer_id = var_offer_id, meta_data = var_meta_datadata_file = "result.txt")
#' result <- api_instance$UpdateGameLevel(var_account_id, var_level_id, app_key = var_app_key, name = var_name, description = var_description, difficulty = var_difficulty, app_version = var_app_version, asset_image_id = var_asset_image_id, asset_icon_id = var_asset_icon_id, game_data = var_game_data, game_data_suffix = var_game_data_suffix, visibility = var_visibility, friend_group = var_friend_group, connection_ids = var_connection_ids, connection_group_ids = var_connection_group_ids, balance = var_balance, active = var_active, allocate_tickets = var_allocate_tickets, ticket_count = var_ticket_count, ticket_type = var_ticket_type, points = var_points, tutorial_title = var_tutorial_title, tutorial_message = var_tutorial_message, tutorial_alignment = var_tutorial_alignment, tutorial_image_asset_id = var_tutorial_image_asset_id, offer_id = var_offer_id, meta_data = var_meta_data)
#' dput(result)
#'
#'
#' ####################  UpdateQuestionsInLevel  ####################
#'
#' library(openapi)
#' var_level_id <- 56 # integer | the id of the level to update questions on
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_question_ids <- "question_ids_example" # character | the IDs of the questions to update
#'
#' #Update Level Questions
#' api_instance <- GameLevelApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateQuestionsInLevel(var_level_id, var_account_id, var_question_idsdata_file = "result.txt")
#' result <- api_instance$UpdateQuestionsInLevel(var_level_id, var_account_id, var_question_ids)
#' dput(result)
#'
#'
#' ####################  UpdateWordsInLevel  ####################
#'
#' library(openapi)
#' var_level_id <- 56 # integer | the id of the level to update words for
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_word_ids <- "word_ids_example" # character | the ids of the words to update for the level
#'
#' #Update Level Words
#' api_instance <- GameLevelApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateWordsInLevel(var_level_id, var_account_id, var_word_idsdata_file = "result.txt")
#' result <- api_instance$UpdateWordsInLevel(var_level_id, var_account_id, var_word_ids)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
GameLevelApi <- R6::R6Class(
  "GameLevelApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new GameLevelApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Game Level
    #'
    #' @param account_id The logged in user.
    #' @param name The name of the level.
    #' @param game_data The game level data: xml, json, or other text based format.
    #' @param game_data_suffix The game level data format type.
    #' @param app_key (optional) The game application key to save the level for.
    #' @param description (optional) The description of the level.
    #' @param difficulty (optional) The difficulty, possible values are: VERY_EASY, EASY, MEDIUM, HARD, VERY_HARD.
    #' @param app_version (optional) The version number of the application required to correctly load/play the level.
    #' @param asset_image_id (optional) The asset Id of the level image.
    #' @param asset_icon_id (optional) The asset Id of the level icon.
    #' @param visibility (optional) Is the level visible to others, possible values are: PUBLIC, PRIVATE.
    #' @param friend_group (optional) Make the level be readable by all friends.
    #' @param connection_ids (optional) Make the level be readable by connections in this list.
    #' @param connection_group_ids (optional) Make the level be readable by connection groups in this list.
    #' @param balance (optional) Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited.
    #' @param active (optional) If true set the game level as active. Default is false.
    #' @param allocate_tickets (optional) If true then scoring will give tickets. Default is false.
    #' @param ticket_count (optional) The number of tickets to reward
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a level
    #' @param tutorial_title (optional) Title of the tutorial.
    #' @param tutorial_message (optional) Message of the tutotrial.
    #' @param tutorial_alignment (optional) Alignment of the tutorial image. Default to NONE. Possible values are: NONE, IMAGE_ABOVE, IMAGE_BELOW, IMAGE_LEFT, IMAGE_RIGHT, IMAGE_ONLY, TEXT_ONLY
    #' @param tutorial_image_asset_id (optional) Asset id of the tutorial image.
    #' @param offer_id (optional) id of the offer
    #' @param meta_data (optional) external custom client defined data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return GameLevelResponse
    CreateGameLevel = function(account_id, name, game_data, game_data_suffix, app_key = NULL, description = NULL, difficulty = NULL, app_version = NULL, asset_image_id = NULL, asset_icon_id = NULL, visibility = NULL, friend_group = NULL, connection_ids = NULL, connection_group_ids = NULL, balance = NULL, active = NULL, allocate_tickets = NULL, ticket_count = NULL, ticket_type = NULL, points = NULL, tutorial_title = NULL, tutorial_message = NULL, tutorial_alignment = NULL, tutorial_image_asset_id = NULL, offer_id = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateGameLevelWithHttpInfo(account_id, name, game_data, game_data_suffix, app_key, description, difficulty, app_version, asset_image_id, asset_icon_id, visibility, friend_group, connection_ids, connection_group_ids, balance, active, allocate_tickets, ticket_count, ticket_type, points, tutorial_title, tutorial_message, tutorial_alignment, tutorial_image_asset_id, offer_id, meta_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Game Level
    #'
    #' @param account_id The logged in user.
    #' @param name The name of the level.
    #' @param game_data The game level data: xml, json, or other text based format.
    #' @param game_data_suffix The game level data format type.
    #' @param app_key (optional) The game application key to save the level for.
    #' @param description (optional) The description of the level.
    #' @param difficulty (optional) The difficulty, possible values are: VERY_EASY, EASY, MEDIUM, HARD, VERY_HARD.
    #' @param app_version (optional) The version number of the application required to correctly load/play the level.
    #' @param asset_image_id (optional) The asset Id of the level image.
    #' @param asset_icon_id (optional) The asset Id of the level icon.
    #' @param visibility (optional) Is the level visible to others, possible values are: PUBLIC, PRIVATE.
    #' @param friend_group (optional) Make the level be readable by all friends.
    #' @param connection_ids (optional) Make the level be readable by connections in this list.
    #' @param connection_group_ids (optional) Make the level be readable by connection groups in this list.
    #' @param balance (optional) Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited.
    #' @param active (optional) If true set the game level as active. Default is false.
    #' @param allocate_tickets (optional) If true then scoring will give tickets. Default is false.
    #' @param ticket_count (optional) The number of tickets to reward
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a level
    #' @param tutorial_title (optional) Title of the tutorial.
    #' @param tutorial_message (optional) Message of the tutotrial.
    #' @param tutorial_alignment (optional) Alignment of the tutorial image. Default to NONE. Possible values are: NONE, IMAGE_ABOVE, IMAGE_BELOW, IMAGE_LEFT, IMAGE_RIGHT, IMAGE_ONLY, TEXT_ONLY
    #' @param tutorial_image_asset_id (optional) Asset id of the tutorial image.
    #' @param offer_id (optional) id of the offer
    #' @param meta_data (optional) external custom client defined data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (GameLevelResponse) with additional information such as HTTP status code, headers
    CreateGameLevelWithHttpInfo = function(account_id, name, game_data, game_data_suffix, app_key = NULL, description = NULL, difficulty = NULL, app_version = NULL, asset_image_id = NULL, asset_icon_id = NULL, visibility = NULL, friend_group = NULL, connection_ids = NULL, connection_group_ids = NULL, balance = NULL, active = NULL, allocate_tickets = NULL, ticket_count = NULL, ticket_type = NULL, points = NULL, tutorial_title = NULL, tutorial_message = NULL, tutorial_alignment = NULL, tutorial_image_asset_id = NULL, offer_id = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (missing(`game_data`)) {
        stop("Missing required parameter `game_data`.")
      }

      if (missing(`game_data_suffix`)) {
        stop("Missing required parameter `game_data_suffix`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameLevelApi$CreateGameLevel, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling GameLevelApi$CreateGameLevel, `name` is not nullable")
      }

      if (!missing(`game_data`) && is.null(`game_data`)) {
        stop("Invalid value for `game_data` when calling GameLevelApi$CreateGameLevel, `game_data` is not nullable")
      }

      if (!missing(`game_data_suffix`) && is.null(`game_data_suffix`)) {
        stop("Invalid value for `game_data_suffix` when calling GameLevelApi$CreateGameLevel, `game_data_suffix` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling GameLevelApi$CreateGameLevel, `app_key` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling GameLevelApi$CreateGameLevel, `description` is not nullable")
      }

      if (!missing(`difficulty`) && is.null(`difficulty`)) {
        stop("Invalid value for `difficulty` when calling GameLevelApi$CreateGameLevel, `difficulty` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling GameLevelApi$CreateGameLevel, `app_version` is not nullable")
      }

      if (!missing(`asset_image_id`) && is.null(`asset_image_id`)) {
        stop("Invalid value for `asset_image_id` when calling GameLevelApi$CreateGameLevel, `asset_image_id` is not nullable")
      }

      if (!missing(`asset_icon_id`) && is.null(`asset_icon_id`)) {
        stop("Invalid value for `asset_icon_id` when calling GameLevelApi$CreateGameLevel, `asset_icon_id` is not nullable")
      }

      if (!missing(`visibility`) && is.null(`visibility`)) {
        stop("Invalid value for `visibility` when calling GameLevelApi$CreateGameLevel, `visibility` is not nullable")
      }

      if (!missing(`friend_group`) && is.null(`friend_group`)) {
        stop("Invalid value for `friend_group` when calling GameLevelApi$CreateGameLevel, `friend_group` is not nullable")
      }

      if (!missing(`connection_ids`) && is.null(`connection_ids`)) {
        stop("Invalid value for `connection_ids` when calling GameLevelApi$CreateGameLevel, `connection_ids` is not nullable")
      }

      if (!missing(`connection_group_ids`) && is.null(`connection_group_ids`)) {
        stop("Invalid value for `connection_group_ids` when calling GameLevelApi$CreateGameLevel, `connection_group_ids` is not nullable")
      }

      if (!missing(`balance`) && is.null(`balance`)) {
        stop("Invalid value for `balance` when calling GameLevelApi$CreateGameLevel, `balance` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling GameLevelApi$CreateGameLevel, `active` is not nullable")
      }

      if (!missing(`allocate_tickets`) && is.null(`allocate_tickets`)) {
        stop("Invalid value for `allocate_tickets` when calling GameLevelApi$CreateGameLevel, `allocate_tickets` is not nullable")
      }

      if (!missing(`ticket_count`) && is.null(`ticket_count`)) {
        stop("Invalid value for `ticket_count` when calling GameLevelApi$CreateGameLevel, `ticket_count` is not nullable")
      }

      if (!missing(`ticket_type`) && is.null(`ticket_type`)) {
        stop("Invalid value for `ticket_type` when calling GameLevelApi$CreateGameLevel, `ticket_type` is not nullable")
      }

      if (!missing(`points`) && is.null(`points`)) {
        stop("Invalid value for `points` when calling GameLevelApi$CreateGameLevel, `points` is not nullable")
      }

      if (!missing(`tutorial_title`) && is.null(`tutorial_title`)) {
        stop("Invalid value for `tutorial_title` when calling GameLevelApi$CreateGameLevel, `tutorial_title` is not nullable")
      }

      if (!missing(`tutorial_message`) && is.null(`tutorial_message`)) {
        stop("Invalid value for `tutorial_message` when calling GameLevelApi$CreateGameLevel, `tutorial_message` is not nullable")
      }

      if (!missing(`tutorial_alignment`) && is.null(`tutorial_alignment`)) {
        stop("Invalid value for `tutorial_alignment` when calling GameLevelApi$CreateGameLevel, `tutorial_alignment` is not nullable")
      }

      if (!missing(`tutorial_image_asset_id`) && is.null(`tutorial_image_asset_id`)) {
        stop("Invalid value for `tutorial_image_asset_id` when calling GameLevelApi$CreateGameLevel, `tutorial_image_asset_id` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling GameLevelApi$CreateGameLevel, `offer_id` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling GameLevelApi$CreateGameLevel, `meta_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["difficulty"]] <- `difficulty`

      query_params[["appVersion"]] <- `app_version`

      query_params[["assetImageId"]] <- `asset_image_id`

      query_params[["assetIconId"]] <- `asset_icon_id`

      query_params[["gameData"]] <- `game_data`

      query_params[["gameDataSuffix"]] <- `game_data_suffix`

      query_params[["visibility"]] <- `visibility`

      query_params[["friendGroup"]] <- `friend_group`

      query_params[["connectionIds"]] <- `connection_ids`

      query_params[["connectionGroupIds"]] <- `connection_group_ids`

      query_params[["balance"]] <- `balance`

      query_params[["active"]] <- `active`

      query_params[["allocateTickets"]] <- `allocate_tickets`

      query_params[["ticketCount"]] <- `ticket_count`

      query_params[["ticketType"]] <- `ticket_type`

      query_params[["points"]] <- `points`

      query_params[["tutorialTitle"]] <- `tutorial_title`

      query_params[["tutorialMessage"]] <- `tutorial_message`

      query_params[["tutorialAlignment"]] <- `tutorial_alignment`

      query_params[["tutorialImageAssetId"]] <- `tutorial_image_asset_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["metaData"]] <- `meta_data`

      local_var_url_path <- "/level/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GameLevelResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Game Level
    #'
    #' @param account_id The logged in user.
    #' @param level_id The id of the level to return.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteGameLevel = function(account_id, level_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteGameLevelWithHttpInfo(account_id, level_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Game Level
    #'
    #' @param account_id The logged in user.
    #' @param level_id The id of the level to return.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteGameLevelWithHttpInfo = function(account_id, level_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`level_id`)) {
        stop("Missing required parameter `level_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameLevelApi$DeleteGameLevel, `account_id` is not nullable")
      }

      if (!missing(`level_id`) && is.null(`level_id`)) {
        stop("Invalid value for `level_id` when calling GameLevelApi$DeleteGameLevel, `level_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["levelId"]] <- `level_id`

      local_var_url_path <- "/level/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Game Level
    #'
    #' @param account_id The logged in user.
    #' @param level_id The id of the level to return.
    #' @param include_game_data (optional) If true include the game level data, otherwise don't. default is false.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return GameLevelResponse
    GetGameLevel = function(account_id, level_id, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetGameLevelWithHttpInfo(account_id, level_id, include_game_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Game Level
    #'
    #' @param account_id The logged in user.
    #' @param level_id The id of the level to return.
    #' @param include_game_data (optional) If true include the game level data, otherwise don't. default is false.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (GameLevelResponse) with additional information such as HTTP status code, headers
    GetGameLevelWithHttpInfo = function(account_id, level_id, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`level_id`)) {
        stop("Missing required parameter `level_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameLevelApi$GetGameLevel, `account_id` is not nullable")
      }

      if (!missing(`level_id`) && is.null(`level_id`)) {
        stop("Invalid value for `level_id` when calling GameLevelApi$GetGameLevel, `level_id` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling GameLevelApi$GetGameLevel, `include_game_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["levelId"]] <- `level_id`

      query_params[["includeGameData"]] <- `include_game_data`

      local_var_url_path <- "/level/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GameLevelResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Game Levels
    #'
    #' @param account_id The logged in user.
    #' @param app_key the application key
    #' @param keyword (optional) Match the keyword to the owner name or level name.
    #' @param sort_field (optional) what field to sort on
    #' @param descending (optional) whether to return levels in ascending or descending order
    #' @param start (optional) Start the result set at some index.
    #' @param limit (optional) Limit the result to some number.
    #' @param app_version (optional) The maximum version of the level to return.
    #' @param include_game_data (optional) If true include the game level data, otherwise don't. default is false.
    #' @param filters (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return GameLevelListResponse
    GetGameLevelsByApplication = function(account_id, app_key, keyword = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, app_version = NULL, include_game_data = NULL, filters = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetGameLevelsByApplicationWithHttpInfo(account_id, app_key, keyword, sort_field, descending, start, limit, app_version, include_game_data, filters, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Game Levels
    #'
    #' @param account_id The logged in user.
    #' @param app_key the application key
    #' @param keyword (optional) Match the keyword to the owner name or level name.
    #' @param sort_field (optional) what field to sort on
    #' @param descending (optional) whether to return levels in ascending or descending order
    #' @param start (optional) Start the result set at some index.
    #' @param limit (optional) Limit the result to some number.
    #' @param app_version (optional) The maximum version of the level to return.
    #' @param include_game_data (optional) If true include the game level data, otherwise don't. default is false.
    #' @param filters (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (GameLevelListResponse) with additional information such as HTTP status code, headers
    GetGameLevelsByApplicationWithHttpInfo = function(account_id, app_key, keyword = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, app_version = NULL, include_game_data = NULL, filters = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameLevelApi$GetGameLevelsByApplication, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling GameLevelApi$GetGameLevelsByApplication, `app_key` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling GameLevelApi$GetGameLevelsByApplication, `keyword` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling GameLevelApi$GetGameLevelsByApplication, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling GameLevelApi$GetGameLevelsByApplication, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling GameLevelApi$GetGameLevelsByApplication, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling GameLevelApi$GetGameLevelsByApplication, `limit` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling GameLevelApi$GetGameLevelsByApplication, `app_version` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling GameLevelApi$GetGameLevelsByApplication, `include_game_data` is not nullable")
      }

      if (!missing(`filters`) && is.null(`filters`)) {
        stop("Invalid value for `filters` when calling GameLevelApi$GetGameLevelsByApplication, `filters` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["keyword"]] <- `keyword`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["appVersion"]] <- `app_version`

      query_params[["includeGameData"]] <- `include_game_data`

      query_params[["filters"]] <- `filters`

      local_var_url_path <- "/level/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GameLevelListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Game Level by Billable Entity
    #'
    #' @param account_id The account id of the user
    #' @param app_key (optional) the application key
    #' @param keyword (optional) The keyword used to search
    #' @param sort_field (optional) The column to sort the search on
    #' @param descending (optional) The order to return the search results
    #' @param active_only (optional) Return only active results
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return GameLevelResponse
    GetGameLevelsByBillableEntity = function(account_id, app_key = NULL, keyword = NULL, sort_field = NULL, descending = NULL, active_only = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetGameLevelsByBillableEntityWithHttpInfo(account_id, app_key, keyword, sort_field, descending, active_only, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Game Level by Billable Entity
    #'
    #' @param account_id The account id of the user
    #' @param app_key (optional) the application key
    #' @param keyword (optional) The keyword used to search
    #' @param sort_field (optional) The column to sort the search on
    #' @param descending (optional) The order to return the search results
    #' @param active_only (optional) Return only active results
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (GameLevelResponse) with additional information such as HTTP status code, headers
    GetGameLevelsByBillableEntityWithHttpInfo = function(account_id, app_key = NULL, keyword = NULL, sort_field = NULL, descending = NULL, active_only = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameLevelApi$GetGameLevelsByBillableEntity, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling GameLevelApi$GetGameLevelsByBillableEntity, `app_key` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling GameLevelApi$GetGameLevelsByBillableEntity, `keyword` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling GameLevelApi$GetGameLevelsByBillableEntity, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling GameLevelApi$GetGameLevelsByBillableEntity, `descending` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling GameLevelApi$GetGameLevelsByBillableEntity, `active_only` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling GameLevelApi$GetGameLevelsByBillableEntity, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling GameLevelApi$GetGameLevelsByBillableEntity, `limit` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["keyword"]] <- `keyword`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("LEVEL_ACTIVE", "LEVEL_NAME", "LEVEL_DESCRIPTION", "LEVEL_CREATED", "LEVEL_UPDATED", "LEVEL_LIKES", "LEVEL_DISLIKES", "LEVEL_NOTES", "LEVEL_PLAYS", "LEVEL_DOWNLOADS", "LEVEL_QUITS", "LEVEL_COMPLETES", "LEVEL_VERSION", "LEVEL_MISSION_TYPE", "LEVEL_OWNER_DISPLAY", "GAME_OWNER_DISPLAY", "GAME_TITLE", "GAME_DESCRIPTION", "GAME_LIKES", "GAME_DISLIKES", "APP_NAME", "APP_SCORING_TYPE"))) {
        stop("Invalid value for sort_field when calling GameLevelApi$GetGameLevelsByBillableEntity. Must be [LEVEL_ACTIVE, LEVEL_NAME, LEVEL_DESCRIPTION, LEVEL_CREATED, LEVEL_UPDATED, LEVEL_LIKES, LEVEL_DISLIKES, LEVEL_NOTES, LEVEL_PLAYS, LEVEL_DOWNLOADS, LEVEL_QUITS, LEVEL_COMPLETES, LEVEL_VERSION, LEVEL_MISSION_TYPE, LEVEL_OWNER_DISPLAY, GAME_OWNER_DISPLAY, GAME_TITLE, GAME_DESCRIPTION, GAME_LIKES, GAME_DISLIKES, APP_NAME, APP_SCORING_TYPE].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/level/searchByBillableEntity"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GameLevelResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Level Questions
    #'
    #' @param level_id the id of the level to get questions from
    #' @param account_id the id of the logged in user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return QuestionResponse
    GetQuestionsInLevel = function(level_id, account_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetQuestionsInLevelWithHttpInfo(level_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Level Questions
    #'
    #' @param level_id the id of the level to get questions from
    #' @param account_id the id of the logged in user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (QuestionResponse) with additional information such as HTTP status code, headers
    GetQuestionsInLevelWithHttpInfo = function(level_id, account_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`level_id`)) {
        stop("Missing required parameter `level_id`.")
      }

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`level_id`) && is.null(`level_id`)) {
        stop("Invalid value for `level_id` when calling GameLevelApi$GetQuestionsInLevel, `level_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameLevelApi$GetQuestionsInLevel, `account_id` is not nullable")
      }

      query_params[["levelId"]] <- `level_id`

      query_params[["accountId"]] <- `account_id`

      local_var_url_path <- "/level/questions/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QuestionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Level Words
    #'
    #' @param level_id the id of the level to get words for
    #' @param account_id the id of the logged in user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return WordzWordResponse
    GetWordsInLevel = function(level_id, account_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetWordsInLevelWithHttpInfo(level_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Level Words
    #'
    #' @param level_id the id of the level to get words for
    #' @param account_id the id of the logged in user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (WordzWordResponse) with additional information such as HTTP status code, headers
    GetWordsInLevelWithHttpInfo = function(level_id, account_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`level_id`)) {
        stop("Missing required parameter `level_id`.")
      }

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`level_id`) && is.null(`level_id`)) {
        stop("Invalid value for `level_id` when calling GameLevelApi$GetWordsInLevel, `level_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameLevelApi$GetWordsInLevel, `account_id` is not nullable")
      }

      query_params[["levelId"]] <- `level_id`

      query_params[["accountId"]] <- `account_id`

      local_var_url_path <- "/level/words/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "WordzWordResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Game Level
    #'
    #' @param account_id The logged in user.
    #' @param level_id If update then include the level Id.
    #' @param app_key (optional) The game application key to save the level for.
    #' @param name (optional) The name of the level.
    #' @param description (optional) The description of the level.
    #' @param difficulty (optional) The difficulty, possible values are: VERY_EASY, EASY, MEDIUM, HARD, VERY_HARD.
    #' @param app_version (optional) The version number of the applicatuion required to correctly load/play the level.
    #' @param asset_image_id (optional) The asset Id of the level image.
    #' @param asset_icon_id (optional) The asset Id of the level icon.
    #' @param game_data (optional) The game level data: xml, json, or other texted based format.
    #' @param game_data_suffix (optional) The game level data format type.
    #' @param visibility (optional) Is the level visible to others, possible values are: PUBLIC, PRIVATE.
    #' @param friend_group (optional) Make the level be readable by all friends.
    #' @param connection_ids (optional) Make the level be readable by connections in this list.
    #' @param connection_group_ids (optional) Make the level be readable by connection groups in this list.
    #' @param balance (optional) Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited.
    #' @param active (optional) If true set the game level as active.
    #' @param allocate_tickets (optional) If true then scoring will give tickets.
    #' @param ticket_count (optional) The number of tickets to reward
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a level
    #' @param tutorial_title (optional) Title of the tutorial.
    #' @param tutorial_message (optional) Message of the tutorial.
    #' @param tutorial_alignment (optional) Alignment of the tutorial image, possible values are: NONE, IMAGE_ABOVE, IMAGE_BELOW, IMAGE_LEFT, IMAGE_RIGHT, IMAGE_ONLY, TEXT_ONLY
    #' @param tutorial_image_asset_id (optional) Asset id of the tutorial image.
    #' @param offer_id (optional) 
    #' @param meta_data (optional) external custom client defined data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return GameLevelResponse
    UpdateGameLevel = function(account_id, level_id, app_key = NULL, name = NULL, description = NULL, difficulty = NULL, app_version = NULL, asset_image_id = NULL, asset_icon_id = NULL, game_data = NULL, game_data_suffix = NULL, visibility = NULL, friend_group = NULL, connection_ids = NULL, connection_group_ids = NULL, balance = NULL, active = NULL, allocate_tickets = NULL, ticket_count = NULL, ticket_type = NULL, points = NULL, tutorial_title = NULL, tutorial_message = NULL, tutorial_alignment = NULL, tutorial_image_asset_id = NULL, offer_id = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateGameLevelWithHttpInfo(account_id, level_id, app_key, name, description, difficulty, app_version, asset_image_id, asset_icon_id, game_data, game_data_suffix, visibility, friend_group, connection_ids, connection_group_ids, balance, active, allocate_tickets, ticket_count, ticket_type, points, tutorial_title, tutorial_message, tutorial_alignment, tutorial_image_asset_id, offer_id, meta_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Game Level
    #'
    #' @param account_id The logged in user.
    #' @param level_id If update then include the level Id.
    #' @param app_key (optional) The game application key to save the level for.
    #' @param name (optional) The name of the level.
    #' @param description (optional) The description of the level.
    #' @param difficulty (optional) The difficulty, possible values are: VERY_EASY, EASY, MEDIUM, HARD, VERY_HARD.
    #' @param app_version (optional) The version number of the applicatuion required to correctly load/play the level.
    #' @param asset_image_id (optional) The asset Id of the level image.
    #' @param asset_icon_id (optional) The asset Id of the level icon.
    #' @param game_data (optional) The game level data: xml, json, or other texted based format.
    #' @param game_data_suffix (optional) The game level data format type.
    #' @param visibility (optional) Is the level visible to others, possible values are: PUBLIC, PRIVATE.
    #' @param friend_group (optional) Make the level be readable by all friends.
    #' @param connection_ids (optional) Make the level be readable by connections in this list.
    #' @param connection_group_ids (optional) Make the level be readable by connection groups in this list.
    #' @param balance (optional) Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited.
    #' @param active (optional) If true set the game level as active.
    #' @param allocate_tickets (optional) If true then scoring will give tickets.
    #' @param ticket_count (optional) The number of tickets to reward
    #' @param ticket_type (optional) The type of ticket to reward, null means default type
    #' @param points (optional) The number of points to award for completing a level
    #' @param tutorial_title (optional) Title of the tutorial.
    #' @param tutorial_message (optional) Message of the tutorial.
    #' @param tutorial_alignment (optional) Alignment of the tutorial image, possible values are: NONE, IMAGE_ABOVE, IMAGE_BELOW, IMAGE_LEFT, IMAGE_RIGHT, IMAGE_ONLY, TEXT_ONLY
    #' @param tutorial_image_asset_id (optional) Asset id of the tutorial image.
    #' @param offer_id (optional) 
    #' @param meta_data (optional) external custom client defined data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (GameLevelResponse) with additional information such as HTTP status code, headers
    UpdateGameLevelWithHttpInfo = function(account_id, level_id, app_key = NULL, name = NULL, description = NULL, difficulty = NULL, app_version = NULL, asset_image_id = NULL, asset_icon_id = NULL, game_data = NULL, game_data_suffix = NULL, visibility = NULL, friend_group = NULL, connection_ids = NULL, connection_group_ids = NULL, balance = NULL, active = NULL, allocate_tickets = NULL, ticket_count = NULL, ticket_type = NULL, points = NULL, tutorial_title = NULL, tutorial_message = NULL, tutorial_alignment = NULL, tutorial_image_asset_id = NULL, offer_id = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`level_id`)) {
        stop("Missing required parameter `level_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameLevelApi$UpdateGameLevel, `account_id` is not nullable")
      }

      if (!missing(`level_id`) && is.null(`level_id`)) {
        stop("Invalid value for `level_id` when calling GameLevelApi$UpdateGameLevel, `level_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling GameLevelApi$UpdateGameLevel, `app_key` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling GameLevelApi$UpdateGameLevel, `name` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling GameLevelApi$UpdateGameLevel, `description` is not nullable")
      }

      if (!missing(`difficulty`) && is.null(`difficulty`)) {
        stop("Invalid value for `difficulty` when calling GameLevelApi$UpdateGameLevel, `difficulty` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling GameLevelApi$UpdateGameLevel, `app_version` is not nullable")
      }

      if (!missing(`asset_image_id`) && is.null(`asset_image_id`)) {
        stop("Invalid value for `asset_image_id` when calling GameLevelApi$UpdateGameLevel, `asset_image_id` is not nullable")
      }

      if (!missing(`asset_icon_id`) && is.null(`asset_icon_id`)) {
        stop("Invalid value for `asset_icon_id` when calling GameLevelApi$UpdateGameLevel, `asset_icon_id` is not nullable")
      }

      if (!missing(`game_data`) && is.null(`game_data`)) {
        stop("Invalid value for `game_data` when calling GameLevelApi$UpdateGameLevel, `game_data` is not nullable")
      }

      if (!missing(`game_data_suffix`) && is.null(`game_data_suffix`)) {
        stop("Invalid value for `game_data_suffix` when calling GameLevelApi$UpdateGameLevel, `game_data_suffix` is not nullable")
      }

      if (!missing(`visibility`) && is.null(`visibility`)) {
        stop("Invalid value for `visibility` when calling GameLevelApi$UpdateGameLevel, `visibility` is not nullable")
      }

      if (!missing(`friend_group`) && is.null(`friend_group`)) {
        stop("Invalid value for `friend_group` when calling GameLevelApi$UpdateGameLevel, `friend_group` is not nullable")
      }

      if (!missing(`connection_ids`) && is.null(`connection_ids`)) {
        stop("Invalid value for `connection_ids` when calling GameLevelApi$UpdateGameLevel, `connection_ids` is not nullable")
      }

      if (!missing(`connection_group_ids`) && is.null(`connection_group_ids`)) {
        stop("Invalid value for `connection_group_ids` when calling GameLevelApi$UpdateGameLevel, `connection_group_ids` is not nullable")
      }

      if (!missing(`balance`) && is.null(`balance`)) {
        stop("Invalid value for `balance` when calling GameLevelApi$UpdateGameLevel, `balance` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling GameLevelApi$UpdateGameLevel, `active` is not nullable")
      }

      if (!missing(`allocate_tickets`) && is.null(`allocate_tickets`)) {
        stop("Invalid value for `allocate_tickets` when calling GameLevelApi$UpdateGameLevel, `allocate_tickets` is not nullable")
      }

      if (!missing(`ticket_count`) && is.null(`ticket_count`)) {
        stop("Invalid value for `ticket_count` when calling GameLevelApi$UpdateGameLevel, `ticket_count` is not nullable")
      }

      if (!missing(`ticket_type`) && is.null(`ticket_type`)) {
        stop("Invalid value for `ticket_type` when calling GameLevelApi$UpdateGameLevel, `ticket_type` is not nullable")
      }

      if (!missing(`points`) && is.null(`points`)) {
        stop("Invalid value for `points` when calling GameLevelApi$UpdateGameLevel, `points` is not nullable")
      }

      if (!missing(`tutorial_title`) && is.null(`tutorial_title`)) {
        stop("Invalid value for `tutorial_title` when calling GameLevelApi$UpdateGameLevel, `tutorial_title` is not nullable")
      }

      if (!missing(`tutorial_message`) && is.null(`tutorial_message`)) {
        stop("Invalid value for `tutorial_message` when calling GameLevelApi$UpdateGameLevel, `tutorial_message` is not nullable")
      }

      if (!missing(`tutorial_alignment`) && is.null(`tutorial_alignment`)) {
        stop("Invalid value for `tutorial_alignment` when calling GameLevelApi$UpdateGameLevel, `tutorial_alignment` is not nullable")
      }

      if (!missing(`tutorial_image_asset_id`) && is.null(`tutorial_image_asset_id`)) {
        stop("Invalid value for `tutorial_image_asset_id` when calling GameLevelApi$UpdateGameLevel, `tutorial_image_asset_id` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling GameLevelApi$UpdateGameLevel, `offer_id` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling GameLevelApi$UpdateGameLevel, `meta_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["levelId"]] <- `level_id`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["difficulty"]] <- `difficulty`

      query_params[["appVersion"]] <- `app_version`

      query_params[["assetImageId"]] <- `asset_image_id`

      query_params[["assetIconId"]] <- `asset_icon_id`

      query_params[["gameData"]] <- `game_data`

      query_params[["gameDataSuffix"]] <- `game_data_suffix`

      query_params[["visibility"]] <- `visibility`

      query_params[["friendGroup"]] <- `friend_group`

      query_params[["connectionIds"]] <- `connection_ids`

      query_params[["connectionGroupIds"]] <- `connection_group_ids`

      query_params[["balance"]] <- `balance`

      query_params[["active"]] <- `active`

      query_params[["allocateTickets"]] <- `allocate_tickets`

      query_params[["ticketCount"]] <- `ticket_count`

      query_params[["ticketType"]] <- `ticket_type`

      query_params[["points"]] <- `points`

      query_params[["tutorialTitle"]] <- `tutorial_title`

      query_params[["tutorialMessage"]] <- `tutorial_message`

      query_params[["tutorialAlignment"]] <- `tutorial_alignment`

      query_params[["tutorialImageAssetId"]] <- `tutorial_image_asset_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["metaData"]] <- `meta_data`

      local_var_url_path <- "/level/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "GameLevelResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Level Questions
    #'
    #' @param level_id the id of the level to update questions on
    #' @param account_id the id of the logged in user
    #' @param question_ids the IDs of the questions to update
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UpdateQuestionsInLevel = function(level_id, account_id, question_ids, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateQuestionsInLevelWithHttpInfo(level_id, account_id, question_ids, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Level Questions
    #'
    #' @param level_id the id of the level to update questions on
    #' @param account_id the id of the logged in user
    #' @param question_ids the IDs of the questions to update
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UpdateQuestionsInLevelWithHttpInfo = function(level_id, account_id, question_ids, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`level_id`)) {
        stop("Missing required parameter `level_id`.")
      }

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`question_ids`)) {
        stop("Missing required parameter `question_ids`.")
      }

      if (!missing(`level_id`) && is.null(`level_id`)) {
        stop("Invalid value for `level_id` when calling GameLevelApi$UpdateQuestionsInLevel, `level_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameLevelApi$UpdateQuestionsInLevel, `account_id` is not nullable")
      }

      if (!missing(`question_ids`) && is.null(`question_ids`)) {
        stop("Invalid value for `question_ids` when calling GameLevelApi$UpdateQuestionsInLevel, `question_ids` is not nullable")
      }

      query_params[["levelId"]] <- `level_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["questionIds"]] <- `question_ids`

      local_var_url_path <- "/level/questions/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Level Words
    #'
    #' @param level_id the id of the level to update words for
    #' @param account_id the id of the logged in user
    #' @param word_ids the ids of the words to update for the level
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    UpdateWordsInLevel = function(level_id, account_id, word_ids, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateWordsInLevelWithHttpInfo(level_id, account_id, word_ids, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Level Words
    #'
    #' @param level_id the id of the level to update words for
    #' @param account_id the id of the logged in user
    #' @param word_ids the ids of the words to update for the level
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    UpdateWordsInLevelWithHttpInfo = function(level_id, account_id, word_ids, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`level_id`)) {
        stop("Missing required parameter `level_id`.")
      }

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`word_ids`)) {
        stop("Missing required parameter `word_ids`.")
      }

      if (!missing(`level_id`) && is.null(`level_id`)) {
        stop("Invalid value for `level_id` when calling GameLevelApi$UpdateWordsInLevel, `level_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling GameLevelApi$UpdateWordsInLevel, `account_id` is not nullable")
      }

      if (!missing(`word_ids`) && is.null(`word_ids`)) {
        stop("Invalid value for `word_ids` when calling GameLevelApi$UpdateWordsInLevel, `word_ids` is not nullable")
      }

      query_params[["levelId"]] <- `level_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["wordIds"]] <- `word_ids`

      local_var_url_path <- "/level/words/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
