#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Like operations
#' @description LikeApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  RegisterLike  ####################
#'
#' library(openapi)
#' var_likable_type <- "likable_type_example" # character | The type of likable object {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, NOTE, THEME_DESCRIPTOR}
#' var_likable_id <- 56 # integer | The id of the likable object
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_permissionable_type <- "permissionable_type_example" # character | This is used for sending out group notifications. For example, when someone likes an asset which is also a part of an album, everyone in the album will receive a notification. This is achieved by passing in the permissionable type (in this case \"album\"), and the permissionable id (the album id). Possible types: {ALBUM, ALBUM_CONTEST, GAME_LEVEL, THEME_DESCRIPTOR} (Optional)
#' var_permissionable_id <- 56 # integer | The id of the permissionable object (for sending group notifications) (Optional)
#' var_like <- "like_example" # character | determines whether the user likes or dislikes the object (Optional)
#' var_app <- "app_example" # character | This parameter is deprecated. This is deprecated, use \"appKey\" instead. (Optional)
#' var_game_type <- "game_type_example" # character | This parameter is deprecated. This is deprecated, use \"appKey\" instead. (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_latitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current location of the user (Optional)
#'
#' #Create Like
#' api_instance <- LikeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RegisterLike(var_likable_type, var_likable_id, device_id = var_device_id, account_id = var_account_id, permissionable_type = var_permissionable_type, permissionable_id = var_permissionable_id, like = var_like, app = var_app, game_type = var_game_type, app_key = var_app_key, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$RegisterLike(var_likable_type, var_likable_id, device_id = var_device_id, account_id = var_account_id, permissionable_type = var_permissionable_type, permissionable_id = var_permissionable_id, like = var_like, app = var_app, game_type = var_game_type, app_key = var_app_key, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  RemoveLike  ####################
#'
#' library(openapi)
#' var_likable_type <- "likable_type_example" # character | The type of the likable object {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, NOTE, THEME_DESCRIPTOR}
#' var_likable_id <- 56 # integer | The id of the likable object
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_latitude <- 3.4 # numeric | The current location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The current location of the user (Optional)
#'
#' #Delete Like
#' api_instance <- LikeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RemoveLike(var_likable_type, var_likable_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$RemoveLike(var_likable_type, var_likable_id, device_id = var_device_id, account_id = var_account_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  SearchLikes  ####################
#'
#' library(openapi)
#' var_likable_type <- "likable_type_example" # character | The type of the likable object {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, NOTE, THEME_DESCRIPTOR}
#' var_likable_id <- 56 # integer | The id of the likable object
#' var_device_id <- "device_id_example" # character | The unique device identifier that made the request (either deviceId or accountId must be used) (Optional)
#' var_account_id <- 56 # integer | The unique accountId that made the request (either deviceId or accountId must be used) (Optional)
#' var_connection_account_ids <- "connection_account_ids_example" # character | Comma separated list of account IDs for filtering on users (Optional)
#' var_sort_field <- "ID" # character | The field to sort by. Possible values include: ID (Optional)
#' var_descending <- TRUE # character | Determines whether the sorted list is in descending or ascending order (Optional)
#' var_updated_since <- 56 # integer | return items that have been updated since this date (time-stamp in milliseconds) (Optional)
#' var_updated_before <- 56 # integer | return items that have been updated before this date (time-stamp in milliseconds) (Optional)
#' var_start <- 0 # integer | the start index for pagination (Optional)
#' var_limit <- 20 # integer | the limit for pagination (Optional)
#'
#' #Search Likes
#' api_instance <- LikeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchLikes(var_likable_type, var_likable_id, device_id = var_device_id, account_id = var_account_id, connection_account_ids = var_connection_account_ids, sort_field = var_sort_field, descending = var_descending, updated_since = var_updated_since, updated_before = var_updated_before, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchLikes(var_likable_type, var_likable_id, device_id = var_device_id, account_id = var_account_id, connection_account_ids = var_connection_account_ids, sort_field = var_sort_field, descending = var_descending, updated_since = var_updated_since, updated_before = var_updated_before, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
LikeApi <- R6::R6Class(
  "LikeApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new LikeApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Like
    #'
    #' @param likable_type The type of likable object {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, NOTE, THEME_DESCRIPTOR}
    #' @param likable_id The id of the likable object
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param permissionable_type (optional) This is used for sending out group notifications. For example, when someone likes an asset which is also a part of an album, everyone in the album will receive a notification. This is achieved by passing in the permissionable type (in this case \"album\"), and the permissionable id (the album id). Possible types: {ALBUM, ALBUM_CONTEST, GAME_LEVEL, THEME_DESCRIPTOR}
    #' @param permissionable_id (optional) The id of the permissionable object (for sending group notifications)
    #' @param like (optional) determines whether the user likes or dislikes the object
    #' @param app (optional) This parameter is deprecated. This is deprecated, use \"appKey\" instead.
    #' @param game_type (optional) This parameter is deprecated. This is deprecated, use \"appKey\" instead.
    #' @param app_key (optional) the application key
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return LikableResponse
    RegisterLike = function(likable_type, likable_id, device_id = NULL, account_id = NULL, permissionable_type = NULL, permissionable_id = NULL, like = NULL, app = NULL, game_type = NULL, app_key = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RegisterLikeWithHttpInfo(likable_type, likable_id, device_id, account_id, permissionable_type, permissionable_id, like, app, game_type, app_key, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Like
    #'
    #' @param likable_type The type of likable object {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, NOTE, THEME_DESCRIPTOR}
    #' @param likable_id The id of the likable object
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param permissionable_type (optional) This is used for sending out group notifications. For example, when someone likes an asset which is also a part of an album, everyone in the album will receive a notification. This is achieved by passing in the permissionable type (in this case \"album\"), and the permissionable id (the album id). Possible types: {ALBUM, ALBUM_CONTEST, GAME_LEVEL, THEME_DESCRIPTOR}
    #' @param permissionable_id (optional) The id of the permissionable object (for sending group notifications)
    #' @param like (optional) determines whether the user likes or dislikes the object
    #' @param app (optional) This parameter is deprecated. This is deprecated, use \"appKey\" instead.
    #' @param game_type (optional) This parameter is deprecated. This is deprecated, use \"appKey\" instead.
    #' @param app_key (optional) the application key
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (LikableResponse) with additional information such as HTTP status code, headers
    RegisterLikeWithHttpInfo = function(likable_type, likable_id, device_id = NULL, account_id = NULL, permissionable_type = NULL, permissionable_id = NULL, like = NULL, app = NULL, game_type = NULL, app_key = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`likable_type`)) {
        stop("Missing required parameter `likable_type`.")
      }

      if (missing(`likable_id`)) {
        stop("Missing required parameter `likable_id`.")
      }

      if (!missing(`likable_type`) && is.null(`likable_type`)) {
        stop("Invalid value for `likable_type` when calling LikeApi$RegisterLike, `likable_type` is not nullable")
      }

      if (!missing(`likable_id`) && is.null(`likable_id`)) {
        stop("Invalid value for `likable_id` when calling LikeApi$RegisterLike, `likable_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling LikeApi$RegisterLike, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling LikeApi$RegisterLike, `account_id` is not nullable")
      }

      if (!missing(`permissionable_type`) && is.null(`permissionable_type`)) {
        stop("Invalid value for `permissionable_type` when calling LikeApi$RegisterLike, `permissionable_type` is not nullable")
      }

      if (!missing(`permissionable_id`) && is.null(`permissionable_id`)) {
        stop("Invalid value for `permissionable_id` when calling LikeApi$RegisterLike, `permissionable_id` is not nullable")
      }

      if (!missing(`like`) && is.null(`like`)) {
        stop("Invalid value for `like` when calling LikeApi$RegisterLike, `like` is not nullable")
      }

      if (!missing(`app`) && is.null(`app`)) {
        stop("Invalid value for `app` when calling LikeApi$RegisterLike, `app` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling LikeApi$RegisterLike, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling LikeApi$RegisterLike, `app_key` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling LikeApi$RegisterLike, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling LikeApi$RegisterLike, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["likableType"]] <- `likable_type`

      query_params[["likableId"]] <- `likable_id`

      query_params[["permissionableType"]] <- `permissionable_type`

      query_params[["permissionableId"]] <- `permissionable_id`

      query_params[["like"]] <- `like`

      query_params[["app"]] <- `app`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/like"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "LikableResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Like
    #'
    #' @param likable_type The type of the likable object {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, NOTE, THEME_DESCRIPTOR}
    #' @param likable_id The id of the likable object
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return LikableResponse
    RemoveLike = function(likable_type, likable_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RemoveLikeWithHttpInfo(likable_type, likable_id, device_id, account_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Like
    #'
    #' @param likable_type The type of the likable object {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, NOTE, THEME_DESCRIPTOR}
    #' @param likable_id The id of the likable object
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param latitude (optional) The current location of the user
    #' @param longitude (optional) The current location of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (LikableResponse) with additional information such as HTTP status code, headers
    RemoveLikeWithHttpInfo = function(likable_type, likable_id, device_id = NULL, account_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`likable_type`)) {
        stop("Missing required parameter `likable_type`.")
      }

      if (missing(`likable_id`)) {
        stop("Missing required parameter `likable_id`.")
      }

      if (!missing(`likable_type`) && is.null(`likable_type`)) {
        stop("Invalid value for `likable_type` when calling LikeApi$RemoveLike, `likable_type` is not nullable")
      }

      if (!missing(`likable_id`) && is.null(`likable_id`)) {
        stop("Invalid value for `likable_id` when calling LikeApi$RemoveLike, `likable_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling LikeApi$RemoveLike, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling LikeApi$RemoveLike, `account_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling LikeApi$RemoveLike, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling LikeApi$RemoveLike, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["likableType"]] <- `likable_type`

      query_params[["likableId"]] <- `likable_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/like/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "LikableResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Likes
    #'
    #' @param likable_type The type of the likable object {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, NOTE, THEME_DESCRIPTOR}
    #' @param likable_id The id of the likable object
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param connection_account_ids (optional) Comma separated list of account IDs for filtering on users
    #' @param sort_field (optional) The field to sort by. Possible values include: ID (default value: "ID")
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order (default value: TRUE)
    #' @param updated_since (optional) return items that have been updated since this date (time-stamp in milliseconds)
    #' @param updated_before (optional) return items that have been updated before this date (time-stamp in milliseconds)
    #' @param start (optional) the start index for pagination (default value: 0)
    #' @param limit (optional) the limit for pagination (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SearchResponse
    SearchLikes = function(likable_type, likable_id, device_id = NULL, account_id = NULL, connection_account_ids = NULL, sort_field = "ID", descending = TRUE, updated_since = NULL, updated_before = NULL, start = 0, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchLikesWithHttpInfo(likable_type, likable_id, device_id, account_id, connection_account_ids, sort_field, descending, updated_since, updated_before, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Likes
    #'
    #' @param likable_type The type of the likable object {ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, NOTE, THEME_DESCRIPTOR}
    #' @param likable_id The id of the likable object
    #' @param device_id (optional) The unique device identifier that made the request (either deviceId or accountId must be used)
    #' @param account_id (optional) The unique accountId that made the request (either deviceId or accountId must be used)
    #' @param connection_account_ids (optional) Comma separated list of account IDs for filtering on users
    #' @param sort_field (optional) The field to sort by. Possible values include: ID (default value: "ID")
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order (default value: TRUE)
    #' @param updated_since (optional) return items that have been updated since this date (time-stamp in milliseconds)
    #' @param updated_before (optional) return items that have been updated before this date (time-stamp in milliseconds)
    #' @param start (optional) the start index for pagination (default value: 0)
    #' @param limit (optional) the limit for pagination (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SearchResponse) with additional information such as HTTP status code, headers
    SearchLikesWithHttpInfo = function(likable_type, likable_id, device_id = NULL, account_id = NULL, connection_account_ids = NULL, sort_field = "ID", descending = TRUE, updated_since = NULL, updated_before = NULL, start = 0, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`likable_type`)) {
        stop("Missing required parameter `likable_type`.")
      }

      if (missing(`likable_id`)) {
        stop("Missing required parameter `likable_id`.")
      }

      if (!missing(`likable_type`) && is.null(`likable_type`)) {
        stop("Invalid value for `likable_type` when calling LikeApi$SearchLikes, `likable_type` is not nullable")
      }

      if (!missing(`likable_id`) && is.null(`likable_id`)) {
        stop("Invalid value for `likable_id` when calling LikeApi$SearchLikes, `likable_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling LikeApi$SearchLikes, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling LikeApi$SearchLikes, `account_id` is not nullable")
      }

      if (!missing(`connection_account_ids`) && is.null(`connection_account_ids`)) {
        stop("Invalid value for `connection_account_ids` when calling LikeApi$SearchLikes, `connection_account_ids` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling LikeApi$SearchLikes, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling LikeApi$SearchLikes, `descending` is not nullable")
      }

      if (!missing(`updated_since`) && is.null(`updated_since`)) {
        stop("Invalid value for `updated_since` when calling LikeApi$SearchLikes, `updated_since` is not nullable")
      }

      if (!missing(`updated_before`) && is.null(`updated_before`)) {
        stop("Invalid value for `updated_before` when calling LikeApi$SearchLikes, `updated_before` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling LikeApi$SearchLikes, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling LikeApi$SearchLikes, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["connectionAccountIds"]] <- `connection_account_ids`

      query_params[["likableType"]] <- `likable_type`

      query_params[["likableId"]] <- `likable_id`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["updatedSince"]] <- `updated_since`

      query_params[["updatedBefore"]] <- `updated_before`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/like/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SearchResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
