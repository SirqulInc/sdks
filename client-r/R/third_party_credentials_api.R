#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ThirdPartyCredentials operations
#' @description ThirdPartyCredentialsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateCredential  ####################
#'
#' library(openapi)
#' var_third_party_id <- "third_party_id_example" # character | the third party user account id
#' var_third_party_token <- "third_party_token_example" # character | the access token to authenticate with (ex: username or fb token or phone number)
#' var_network_uid <- "network_uid_example" # character | the access provider to authenticate against
#' var_app_key <- "app_key_example" # character | the application key
#' var_account_id <- 56 # integer | the unique id of the account that needs authenticating (optional for PHONE_V2) (Optional)
#' var_device_id <- "device_id_example" # character | the unique id of the device making the request (Optional)
#' var_session_id <- "session_id_example" # character | the session id for the request (Optional)
#' var_third_party_name <- "third_party_name_example" # character | the third party user's display name (Optional)
#' var_email_address <- "email_address_example" # character | optional email address associated with the third party account (Optional)
#' var_signin_only_mode <- FALSE # character | when true will error out if can't find any accounts matching (signin only) (Optional)
#' var_response_filters <- "response_filters_example" # character | this determines how much of the profile should be returned, see ProfileFilters (Optional)
#' var_latitude <- 3.4 # numeric | the latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the longitude of the user (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_third_party_refresh_token <- "third_party_refresh_token_example" # character | optional refresh token for the third party (Optional)
#' var_audience_ids_to_add <- "audience_ids_to_add_example" # character | audience ids to add to the account (Optional)
#' var_audience_ids_to_remove <- "audience_ids_to_remove_example" # character | audience ids to remove from the account (Optional)
#'
#' #Create Credential
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateCredential(var_third_party_id, var_third_party_token, var_network_uid, var_app_key, account_id = var_account_id, device_id = var_device_id, session_id = var_session_id, third_party_name = var_third_party_name, email_address = var_email_address, signin_only_mode = var_signin_only_mode, response_filters = var_response_filters, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, third_party_refresh_token = var_third_party_refresh_token, audience_ids_to_add = var_audience_ids_to_add, audience_ids_to_remove = var_audience_ids_to_removedata_file = "result.txt")
#' result <- api_instance$CreateCredential(var_third_party_id, var_third_party_token, var_network_uid, var_app_key, account_id = var_account_id, device_id = var_device_id, session_id = var_session_id, third_party_name = var_third_party_name, email_address = var_email_address, signin_only_mode = var_signin_only_mode, response_filters = var_response_filters, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, third_party_refresh_token = var_third_party_refresh_token, audience_ids_to_add = var_audience_ids_to_add, audience_ids_to_remove = var_audience_ids_to_remove)
#' dput(result)
#'
#'
#' ####################  CreateNetwork  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id making the request
#' var_name <- "name_example" # character | The name of the network
#' var_enable_introspection <- "enable_introspection_example" # character | Whether the network uses introspection calls
#' var_description <- "description_example" # character | The description of the network (Optional)
#' var_introspection_method <- "introspection_method_example" # character | HTTP method to use for introspection calls (e.g., GET, POST) (Optional)
#' var_introspection_url <- "introspection_url_example" # character | The HTTP URL of the introspection call (Optional)
#' var_introspection_params <- "introspection_params_example" # character | The parameters of the introspection call (Optional)
#' var_required_root_field <- "required_root_field_example" # character | Required response params (Optional)
#' var_enable_mfa <- "enable_mfa_example" # character | Whether this network uses MFA (Optional)
#' var_size_mfa <- 56 # integer | Size of the MFA token (Optional)
#' var_shelf_life_mfa <- 56 # integer | Shelf life (seconds) of the MFA token (Optional)
#' var_oauth_token_url <- "oauth_token_url_example" # character | OAuth token endpoint URL (Optional)
#' var_oauth_private_key <- File.new('/path/to/file') # data.frame | OAuth private key file (multipart) (Optional)
#' var_oauth_public_key <- File.new('/path/to/file') # data.frame | OAuth public key file (multipart) (Optional)
#' var_oauth_client_id <- "oauth_client_id_example" # character | OAuth client id (Optional)
#' var_oauth_secret_key <- "oauth_secret_key_example" # character | OAuth secret key (Optional)
#' var_body <- "body_example" # character |  (Optional)
#'
#' #Create Network
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateNetwork(var_account_id, var_name, var_enable_introspection, description = var_description, introspection_method = var_introspection_method, introspection_url = var_introspection_url, introspection_params = var_introspection_params, required_root_field = var_required_root_field, enable_mfa = var_enable_mfa, size_mfa = var_size_mfa, shelf_life_mfa = var_shelf_life_mfa, oauth_token_url = var_oauth_token_url, oauth_private_key = var_oauth_private_key, oauth_public_key = var_oauth_public_key, oauth_client_id = var_oauth_client_id, oauth_secret_key = var_oauth_secret_key, body = var_bodydata_file = "result.txt")
#' result <- api_instance$CreateNetwork(var_account_id, var_name, var_enable_introspection, description = var_description, introspection_method = var_introspection_method, introspection_url = var_introspection_url, introspection_params = var_introspection_params, required_root_field = var_required_root_field, enable_mfa = var_enable_mfa, size_mfa = var_size_mfa, shelf_life_mfa = var_shelf_life_mfa, oauth_token_url = var_oauth_token_url, oauth_private_key = var_oauth_private_key, oauth_public_key = var_oauth_public_key, oauth_client_id = var_oauth_client_id, oauth_secret_key = var_oauth_secret_key, body = var_body)
#' dput(result)
#'
#'
#' ####################  DeleteCredential  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user
#' var_network_uid <- "network_uid_example" # character | The third party network identifier
#' var_third_party_id <- "third_party_id_example" # character | The third party user id
#' var_app_key <- "app_key_example" # character | the application key
#'
#' #Delete Credential
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteCredential(var_account_id, var_network_uid, var_third_party_id, var_app_keydata_file = "result.txt")
#' result <- api_instance$DeleteCredential(var_account_id, var_network_uid, var_third_party_id, var_app_key)
#' dput(result)
#'
#'
#' ####################  DeleteNetwork  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_network_uid <- "network_uid_example" # character | The unique identifier for the third party network defined by Sirqul
#'
#' #Delete Network
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteNetwork(var_account_id, var_network_uiddata_file = "result.txt")
#' result <- api_instance$DeleteNetwork(var_account_id, var_network_uid)
#' dput(result)
#'
#'
#' ####################  GetCredential  ####################
#'
#' library(openapi)
#' var_network_uid <- "network_uid_example" # character | the access provider to authenticate against
#' var_app_key <- "app_key_example" # character | the application key
#' var_account_id <- 56 # integer | the unique account id of a specific account that will be bound to the third-party credentials (Optional)
#' var_device_id <- "device_id_example" # character | the unique id of the device making the request (Optional)
#' var_session_id <- "session_id_example" # character | the session id for the request (Optional)
#' var_third_party_credential_id <- 56 # integer | the third-party credentials id from the response of the credential/create step (Optional)
#' var_third_party_token <- "third_party_token_example" # character | the access token to authenticate with (Optional)
#' var_third_party_secret <- "third_party_secret_example" # character | the secret code to authenticate with (used for MFA) (Optional)
#' var_create_new_account <- FALSE # character | flag to force creation of a new account when no accountId is passed and user chooses not to use listed accounts (Optional)
#' var_response_filters <- "response_filters_example" # character | this determines how much of the profile should be returned, see ProfileFilters (Optional)
#' var_latitude <- 3.4 # numeric | the latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the longitude of the user (Optional)
#' var_audience_ids_to_add <- "audience_ids_to_add_example" # character | audience ids to add to the account (Optional)
#' var_audience_ids_to_remove <- "audience_ids_to_remove_example" # character | audience ids to remove from the account (Optional)
#' var_referral_account_id <- 56 # integer | account id of the referrer (inviter-invitee relationship) (Optional)
#'
#' #Get Credential
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCredential(var_network_uid, var_app_key, account_id = var_account_id, device_id = var_device_id, session_id = var_session_id, third_party_credential_id = var_third_party_credential_id, third_party_token = var_third_party_token, third_party_secret = var_third_party_secret, create_new_account = var_create_new_account, response_filters = var_response_filters, latitude = var_latitude, longitude = var_longitude, audience_ids_to_add = var_audience_ids_to_add, audience_ids_to_remove = var_audience_ids_to_remove, referral_account_id = var_referral_account_iddata_file = "result.txt")
#' result <- api_instance$GetCredential(var_network_uid, var_app_key, account_id = var_account_id, device_id = var_device_id, session_id = var_session_id, third_party_credential_id = var_third_party_credential_id, third_party_token = var_third_party_token, third_party_secret = var_third_party_secret, create_new_account = var_create_new_account, response_filters = var_response_filters, latitude = var_latitude, longitude = var_longitude, audience_ids_to_add = var_audience_ids_to_add, audience_ids_to_remove = var_audience_ids_to_remove, referral_account_id = var_referral_account_id)
#' dput(result)
#'
#'
#' ####################  GetNetwork  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id making the request
#' var_network_uid <- "network_uid_example" # character | The unique identifier for the third party network defined by Sirqul
#'
#' #Get Network
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetNetwork(var_account_id, var_network_uiddata_file = "result.txt")
#' result <- api_instance$GetNetwork(var_account_id, var_network_uid)
#' dput(result)
#'
#'
#' ####################  SearchCredentials  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the user
#' var_keyword <- "keyword_example" # character | The keyword used to search on the third party name and network string (Optional)
#' var_network_uid <- "network_uid_example" # character | The network UID to filter results with (Optional)
#' var_descending <- "descending_example" # character | The order to return the search results (Optional)
#' var_start <- 0 # integer | The start of the pagination (Optional)
#' var_limit <- 20 # integer | The limit of the pagination (Optional)
#'
#' #Search Credentials
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchCredentials(var_account_id, keyword = var_keyword, network_uid = var_network_uid, descending = var_descending, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchCredentials(var_account_id, keyword = var_keyword, network_uid = var_network_uid, descending = var_descending, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  SearchNetworks  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id making the request
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on, possible values include: UPDATED (default), CREATED, NAME
#' var_descending <- "descending_example" # character | The order to return the search results
#' var_start <- 56 # integer | The start of the pagination
#' var_limit <- 56 # integer | The limit of the pagination
#' var_active_only <- "active_only_example" # character | Return only active results
#' var_keyword <- "keyword_example" # character | The keyword used to search on the network name and description fields (Optional)
#' var_filter_billable <- "filter_billable_example" # character | Determines whether to only return applications that the user has access to (Optional)
#'
#' #Search Networks
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchNetworks(var_account_id, var_sort_field, var_descending, var_start, var_limit, var_active_only, keyword = var_keyword, filter_billable = var_filter_billabledata_file = "result.txt")
#' result <- api_instance$SearchNetworks(var_account_id, var_sort_field, var_descending, var_start, var_limit, var_active_only, keyword = var_keyword, filter_billable = var_filter_billable)
#' dput(result)
#'
#'
#' ####################  SendMFAChallenge  ####################
#'
#' library(openapi)
#' var_network_uid <- "network_uid_example" # character | the third party network provider that has MFA enabled
#' var_app_key <- "app_key_example" # character | the application key
#' var_third_party_token <- "third_party_token_example" # character | the access token to authenticate with (Optional)
#' var_third_party_credential_id <- 56 # integer | optional id of the existing third party credential (Optional)
#' var_device_id <- "device_id_example" # character | the unique id of the device making the request (Optional)
#'
#' #Send MFA Challenge
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SendMFAChallenge(var_network_uid, var_app_key, third_party_token = var_third_party_token, third_party_credential_id = var_third_party_credential_id, device_id = var_device_iddata_file = "result.txt")
#' result <- api_instance$SendMFAChallenge(var_network_uid, var_app_key, third_party_token = var_third_party_token, third_party_credential_id = var_third_party_credential_id, device_id = var_device_id)
#' dput(result)
#'
#'
#' ####################  UpdateCredential  ####################
#'
#' library(openapi)
#' var_network_uid <- "network_uid_example" # character | the access provider to authenticate against
#' var_third_party_id <- "third_party_id_example" # character | the third party user account id
#' var_app_key <- "app_key_example" # character | the application key
#' var_device_id <- "device_id_example" # character | the unique id of the device making the request (Optional)
#' var_third_party_name <- "third_party_name_example" # character | the third party user name (Optional)
#' var_third_party_token <- "third_party_token_example" # character | the access token to authenticate with (ex: username or fb token) (Optional)
#' var_response_filters <- "response_filters_example" # character | this determines how much of the profile should be returned, see ProfileFilters (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_third_party_refresh_token <- "third_party_refresh_token_example" # character | optional refresh token for the third party (Optional)
#'
#' #Update Credential
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateCredential(var_network_uid, var_third_party_id, var_app_key, device_id = var_device_id, third_party_name = var_third_party_name, third_party_token = var_third_party_token, response_filters = var_response_filters, meta_data = var_meta_data, third_party_refresh_token = var_third_party_refresh_tokendata_file = "result.txt")
#' result <- api_instance$UpdateCredential(var_network_uid, var_third_party_id, var_app_key, device_id = var_device_id, third_party_name = var_third_party_name, third_party_token = var_third_party_token, response_filters = var_response_filters, meta_data = var_meta_data, third_party_refresh_token = var_third_party_refresh_token)
#' dput(result)
#'
#'
#' ####################  UpdateNetwork  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id making the request
#' var_network_uid <- "network_uid_example" # character | The unique identifier for the third party network defined by Sirqul
#' var_name <- "name_example" # character | The name of the network (Optional)
#' var_description <- "description_example" # character | The description of the network (Optional)
#' var_enable_introspection <- "enable_introspection_example" # character | Whether the network uses introspection calls (Optional)
#' var_introspection_method <- "introspection_method_example" # character | HTTP method to use for introspection calls (e.g., GET, POST) (Optional)
#' var_introspection_url <- "introspection_url_example" # character | The HTTP URL of the introspection call (Optional)
#' var_introspection_params <- "introspection_params_example" # character | The parameters of the introspection call (Optional)
#' var_required_root_field <- "required_root_field_example" # character | Required response params (Optional)
#' var_enable_mfa <- "enable_mfa_example" # character | Whether this network uses MFA (Optional)
#' var_size_mfa <- 56 # integer | Size of the MFA token (Optional)
#' var_shelf_life_mfa <- 56 # integer | Shelf life (seconds) of the MFA token (Optional)
#' var_oauth_token_url <- "oauth_token_url_example" # character | OAuth token endpoint URL (Optional)
#' var_oauth_private_key <- File.new('/path/to/file') # data.frame | OAuth private key file (multipart) (Optional)
#' var_oauth_public_key <- File.new('/path/to/file') # data.frame | OAuth public key file (multipart) (Optional)
#' var_oauth_client_id <- "oauth_client_id_example" # character | OAuth client id (Optional)
#' var_oauth_secret_key <- "oauth_secret_key_example" # character | OAuth secret key (Optional)
#' var_body <- "body_example" # character |  (Optional)
#'
#' #Update Network
#' api_instance <- ThirdPartyCredentialsApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateNetwork(var_account_id, var_network_uid, name = var_name, description = var_description, enable_introspection = var_enable_introspection, introspection_method = var_introspection_method, introspection_url = var_introspection_url, introspection_params = var_introspection_params, required_root_field = var_required_root_field, enable_mfa = var_enable_mfa, size_mfa = var_size_mfa, shelf_life_mfa = var_shelf_life_mfa, oauth_token_url = var_oauth_token_url, oauth_private_key = var_oauth_private_key, oauth_public_key = var_oauth_public_key, oauth_client_id = var_oauth_client_id, oauth_secret_key = var_oauth_secret_key, body = var_bodydata_file = "result.txt")
#' result <- api_instance$UpdateNetwork(var_account_id, var_network_uid, name = var_name, description = var_description, enable_introspection = var_enable_introspection, introspection_method = var_introspection_method, introspection_url = var_introspection_url, introspection_params = var_introspection_params, required_root_field = var_required_root_field, enable_mfa = var_enable_mfa, size_mfa = var_size_mfa, shelf_life_mfa = var_shelf_life_mfa, oauth_token_url = var_oauth_token_url, oauth_private_key = var_oauth_private_key, oauth_public_key = var_oauth_public_key, oauth_client_id = var_oauth_client_id, oauth_secret_key = var_oauth_secret_key, body = var_body)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ThirdPartyCredentialsApi <- R6::R6Class(
  "ThirdPartyCredentialsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ThirdPartyCredentialsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Credential
    #'
    #' @param third_party_id the third party user account id
    #' @param third_party_token the access token to authenticate with (ex: username or fb token or phone number)
    #' @param network_uid the access provider to authenticate against
    #' @param app_key the application key
    #' @param account_id (optional) the unique id of the account that needs authenticating (optional for PHONE_V2)
    #' @param device_id (optional) the unique id of the device making the request
    #' @param session_id (optional) the session id for the request
    #' @param third_party_name (optional) the third party user's display name
    #' @param email_address (optional) optional email address associated with the third party account
    #' @param signin_only_mode (optional) when true will error out if can't find any accounts matching (signin only) (default value: FALSE)
    #' @param response_filters (optional) this determines how much of the profile should be returned, see ProfileFilters
    #' @param latitude (optional) the latitude of the user
    #' @param longitude (optional) the longitude of the user
    #' @param meta_data (optional) External custom client defined data
    #' @param third_party_refresh_token (optional) optional refresh token for the third party
    #' @param audience_ids_to_add (optional) audience ids to add to the account
    #' @param audience_ids_to_remove (optional) audience ids to remove from the account
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    CreateCredential = function(third_party_id, third_party_token, network_uid, app_key, account_id = NULL, device_id = NULL, session_id = NULL, third_party_name = NULL, email_address = NULL, signin_only_mode = FALSE, response_filters = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, third_party_refresh_token = NULL, audience_ids_to_add = NULL, audience_ids_to_remove = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateCredentialWithHttpInfo(third_party_id, third_party_token, network_uid, app_key, account_id, device_id, session_id, third_party_name, email_address, signin_only_mode, response_filters, latitude, longitude, meta_data, third_party_refresh_token, audience_ids_to_add, audience_ids_to_remove, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Credential
    #'
    #' @param third_party_id the third party user account id
    #' @param third_party_token the access token to authenticate with (ex: username or fb token or phone number)
    #' @param network_uid the access provider to authenticate against
    #' @param app_key the application key
    #' @param account_id (optional) the unique id of the account that needs authenticating (optional for PHONE_V2)
    #' @param device_id (optional) the unique id of the device making the request
    #' @param session_id (optional) the session id for the request
    #' @param third_party_name (optional) the third party user's display name
    #' @param email_address (optional) optional email address associated with the third party account
    #' @param signin_only_mode (optional) when true will error out if can't find any accounts matching (signin only) (default value: FALSE)
    #' @param response_filters (optional) this determines how much of the profile should be returned, see ProfileFilters
    #' @param latitude (optional) the latitude of the user
    #' @param longitude (optional) the longitude of the user
    #' @param meta_data (optional) External custom client defined data
    #' @param third_party_refresh_token (optional) optional refresh token for the third party
    #' @param audience_ids_to_add (optional) audience ids to add to the account
    #' @param audience_ids_to_remove (optional) audience ids to remove from the account
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    CreateCredentialWithHttpInfo = function(third_party_id, third_party_token, network_uid, app_key, account_id = NULL, device_id = NULL, session_id = NULL, third_party_name = NULL, email_address = NULL, signin_only_mode = FALSE, response_filters = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, third_party_refresh_token = NULL, audience_ids_to_add = NULL, audience_ids_to_remove = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`third_party_id`)) {
        stop("Missing required parameter `third_party_id`.")
      }

      if (missing(`third_party_token`)) {
        stop("Missing required parameter `third_party_token`.")
      }

      if (missing(`network_uid`)) {
        stop("Missing required parameter `network_uid`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`third_party_id`) && is.null(`third_party_id`)) {
        stop("Invalid value for `third_party_id` when calling ThirdPartyCredentialsApi$CreateCredential, `third_party_id` is not nullable")
      }

      if (!missing(`third_party_token`) && is.null(`third_party_token`)) {
        stop("Invalid value for `third_party_token` when calling ThirdPartyCredentialsApi$CreateCredential, `third_party_token` is not nullable")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling ThirdPartyCredentialsApi$CreateCredential, `network_uid` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ThirdPartyCredentialsApi$CreateCredential, `app_key` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThirdPartyCredentialsApi$CreateCredential, `account_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ThirdPartyCredentialsApi$CreateCredential, `device_id` is not nullable")
      }

      if (!missing(`session_id`) && is.null(`session_id`)) {
        stop("Invalid value for `session_id` when calling ThirdPartyCredentialsApi$CreateCredential, `session_id` is not nullable")
      }

      if (!missing(`third_party_name`) && is.null(`third_party_name`)) {
        stop("Invalid value for `third_party_name` when calling ThirdPartyCredentialsApi$CreateCredential, `third_party_name` is not nullable")
      }

      if (!missing(`email_address`) && is.null(`email_address`)) {
        stop("Invalid value for `email_address` when calling ThirdPartyCredentialsApi$CreateCredential, `email_address` is not nullable")
      }

      if (!missing(`signin_only_mode`) && is.null(`signin_only_mode`)) {
        stop("Invalid value for `signin_only_mode` when calling ThirdPartyCredentialsApi$CreateCredential, `signin_only_mode` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling ThirdPartyCredentialsApi$CreateCredential, `response_filters` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ThirdPartyCredentialsApi$CreateCredential, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ThirdPartyCredentialsApi$CreateCredential, `longitude` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling ThirdPartyCredentialsApi$CreateCredential, `meta_data` is not nullable")
      }

      if (!missing(`third_party_refresh_token`) && is.null(`third_party_refresh_token`)) {
        stop("Invalid value for `third_party_refresh_token` when calling ThirdPartyCredentialsApi$CreateCredential, `third_party_refresh_token` is not nullable")
      }

      if (!missing(`audience_ids_to_add`) && is.null(`audience_ids_to_add`)) {
        stop("Invalid value for `audience_ids_to_add` when calling ThirdPartyCredentialsApi$CreateCredential, `audience_ids_to_add` is not nullable")
      }

      if (!missing(`audience_ids_to_remove`) && is.null(`audience_ids_to_remove`)) {
        stop("Invalid value for `audience_ids_to_remove` when calling ThirdPartyCredentialsApi$CreateCredential, `audience_ids_to_remove` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["deviceId"]] <- `device_id`

      query_params[["sessionId"]] <- `session_id`

      query_params[["thirdPartyId"]] <- `third_party_id`

      query_params[["thirdPartyName"]] <- `third_party_name`

      query_params[["thirdPartyToken"]] <- `third_party_token`

      query_params[["networkUID"]] <- `network_uid`

      query_params[["appKey"]] <- `app_key`

      query_params[["emailAddress"]] <- `email_address`

      query_params[["signinOnlyMode"]] <- `signin_only_mode`

      query_params[["responseFilters"]] <- `response_filters`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["metaData"]] <- `meta_data`

      query_params[["thirdPartyRefreshToken"]] <- `third_party_refresh_token`

      query_params[["audienceIdsToAdd"]] <- `audience_ids_to_add`

      query_params[["audienceIdsToRemove"]] <- `audience_ids_to_remove`

      local_var_url_path <- "/thirdparty/credential/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Network
    #'
    #' @param account_id The account id making the request
    #' @param name The name of the network
    #' @param enable_introspection Whether the network uses introspection calls
    #' @param description (optional) The description of the network
    #' @param introspection_method (optional) HTTP method to use for introspection calls (e.g., GET, POST)
    #' @param introspection_url (optional) The HTTP URL of the introspection call
    #' @param introspection_params (optional) The parameters of the introspection call
    #' @param required_root_field (optional) Required response params
    #' @param enable_mfa (optional) Whether this network uses MFA
    #' @param size_mfa (optional) Size of the MFA token
    #' @param shelf_life_mfa (optional) Shelf life (seconds) of the MFA token
    #' @param oauth_token_url (optional) OAuth token endpoint URL
    #' @param oauth_private_key (optional) OAuth private key file (multipart)
    #' @param oauth_public_key (optional) OAuth public key file (multipart)
    #' @param oauth_client_id (optional) OAuth client id
    #' @param oauth_secret_key (optional) OAuth secret key
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ThirdPartyNetworkResponse
    CreateNetwork = function(account_id, name, enable_introspection, description = NULL, introspection_method = NULL, introspection_url = NULL, introspection_params = NULL, required_root_field = NULL, enable_mfa = NULL, size_mfa = NULL, shelf_life_mfa = NULL, oauth_token_url = NULL, oauth_private_key = NULL, oauth_public_key = NULL, oauth_client_id = NULL, oauth_secret_key = NULL, body = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateNetworkWithHttpInfo(account_id, name, enable_introspection, description, introspection_method, introspection_url, introspection_params, required_root_field, enable_mfa, size_mfa, shelf_life_mfa, oauth_token_url, oauth_private_key, oauth_public_key, oauth_client_id, oauth_secret_key, body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Network
    #'
    #' @param account_id The account id making the request
    #' @param name The name of the network
    #' @param enable_introspection Whether the network uses introspection calls
    #' @param description (optional) The description of the network
    #' @param introspection_method (optional) HTTP method to use for introspection calls (e.g., GET, POST)
    #' @param introspection_url (optional) The HTTP URL of the introspection call
    #' @param introspection_params (optional) The parameters of the introspection call
    #' @param required_root_field (optional) Required response params
    #' @param enable_mfa (optional) Whether this network uses MFA
    #' @param size_mfa (optional) Size of the MFA token
    #' @param shelf_life_mfa (optional) Shelf life (seconds) of the MFA token
    #' @param oauth_token_url (optional) OAuth token endpoint URL
    #' @param oauth_private_key (optional) OAuth private key file (multipart)
    #' @param oauth_public_key (optional) OAuth public key file (multipart)
    #' @param oauth_client_id (optional) OAuth client id
    #' @param oauth_secret_key (optional) OAuth secret key
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ThirdPartyNetworkResponse) with additional information such as HTTP status code, headers
    CreateNetworkWithHttpInfo = function(account_id, name, enable_introspection, description = NULL, introspection_method = NULL, introspection_url = NULL, introspection_params = NULL, required_root_field = NULL, enable_mfa = NULL, size_mfa = NULL, shelf_life_mfa = NULL, oauth_token_url = NULL, oauth_private_key = NULL, oauth_public_key = NULL, oauth_client_id = NULL, oauth_secret_key = NULL, body = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (missing(`enable_introspection`)) {
        stop("Missing required parameter `enable_introspection`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThirdPartyCredentialsApi$CreateNetwork, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling ThirdPartyCredentialsApi$CreateNetwork, `name` is not nullable")
      }

      if (!missing(`enable_introspection`) && is.null(`enable_introspection`)) {
        stop("Invalid value for `enable_introspection` when calling ThirdPartyCredentialsApi$CreateNetwork, `enable_introspection` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling ThirdPartyCredentialsApi$CreateNetwork, `description` is not nullable")
      }

      if (!missing(`introspection_method`) && is.null(`introspection_method`)) {
        stop("Invalid value for `introspection_method` when calling ThirdPartyCredentialsApi$CreateNetwork, `introspection_method` is not nullable")
      }

      if (!missing(`introspection_url`) && is.null(`introspection_url`)) {
        stop("Invalid value for `introspection_url` when calling ThirdPartyCredentialsApi$CreateNetwork, `introspection_url` is not nullable")
      }

      if (!missing(`introspection_params`) && is.null(`introspection_params`)) {
        stop("Invalid value for `introspection_params` when calling ThirdPartyCredentialsApi$CreateNetwork, `introspection_params` is not nullable")
      }

      if (!missing(`required_root_field`) && is.null(`required_root_field`)) {
        stop("Invalid value for `required_root_field` when calling ThirdPartyCredentialsApi$CreateNetwork, `required_root_field` is not nullable")
      }

      if (!missing(`enable_mfa`) && is.null(`enable_mfa`)) {
        stop("Invalid value for `enable_mfa` when calling ThirdPartyCredentialsApi$CreateNetwork, `enable_mfa` is not nullable")
      }

      if (!missing(`size_mfa`) && is.null(`size_mfa`)) {
        stop("Invalid value for `size_mfa` when calling ThirdPartyCredentialsApi$CreateNetwork, `size_mfa` is not nullable")
      }

      if (!missing(`shelf_life_mfa`) && is.null(`shelf_life_mfa`)) {
        stop("Invalid value for `shelf_life_mfa` when calling ThirdPartyCredentialsApi$CreateNetwork, `shelf_life_mfa` is not nullable")
      }

      if (!missing(`oauth_token_url`) && is.null(`oauth_token_url`)) {
        stop("Invalid value for `oauth_token_url` when calling ThirdPartyCredentialsApi$CreateNetwork, `oauth_token_url` is not nullable")
      }

      if (!missing(`oauth_private_key`) && is.null(`oauth_private_key`)) {
        stop("Invalid value for `oauth_private_key` when calling ThirdPartyCredentialsApi$CreateNetwork, `oauth_private_key` is not nullable")
      }

      if (!missing(`oauth_public_key`) && is.null(`oauth_public_key`)) {
        stop("Invalid value for `oauth_public_key` when calling ThirdPartyCredentialsApi$CreateNetwork, `oauth_public_key` is not nullable")
      }

      if (!missing(`oauth_client_id`) && is.null(`oauth_client_id`)) {
        stop("Invalid value for `oauth_client_id` when calling ThirdPartyCredentialsApi$CreateNetwork, `oauth_client_id` is not nullable")
      }

      if (!missing(`oauth_secret_key`) && is.null(`oauth_secret_key`)) {
        stop("Invalid value for `oauth_secret_key` when calling ThirdPartyCredentialsApi$CreateNetwork, `oauth_secret_key` is not nullable")
      }

      if (!missing(`body`) && is.null(`body`)) {
        stop("Invalid value for `body` when calling ThirdPartyCredentialsApi$CreateNetwork, `body` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["enableIntrospection"]] <- `enable_introspection`

      query_params[["introspectionMethod"]] <- `introspection_method`

      query_params[["introspectionURL"]] <- `introspection_url`

      query_params[["introspectionParams"]] <- `introspection_params`

      query_params[["requiredRootField"]] <- `required_root_field`

      query_params[["enableMFA"]] <- `enable_mfa`

      query_params[["sizeMFA"]] <- `size_mfa`

      query_params[["shelfLifeMFA"]] <- `shelf_life_mfa`

      query_params[["oauthTokenURL"]] <- `oauth_token_url`

      query_params[["oauthPrivateKey"]] <- `oauth_private_key`

      query_params[["oauthPublicKey"]] <- `oauth_public_key`

      query_params[["oauthClientId"]] <- `oauth_client_id`

      query_params[["oauthSecretKey"]] <- `oauth_secret_key`

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/thirdparty/network/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ThirdPartyNetworkResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Credential
    #'
    #' @param account_id The account id of the user
    #' @param network_uid The third party network identifier
    #' @param third_party_id The third party user id
    #' @param app_key the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteCredential = function(account_id, network_uid, third_party_id, app_key, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteCredentialWithHttpInfo(account_id, network_uid, third_party_id, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Credential
    #'
    #' @param account_id The account id of the user
    #' @param network_uid The third party network identifier
    #' @param third_party_id The third party user id
    #' @param app_key the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteCredentialWithHttpInfo = function(account_id, network_uid, third_party_id, app_key, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`network_uid`)) {
        stop("Missing required parameter `network_uid`.")
      }

      if (missing(`third_party_id`)) {
        stop("Missing required parameter `third_party_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThirdPartyCredentialsApi$DeleteCredential, `account_id` is not nullable")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling ThirdPartyCredentialsApi$DeleteCredential, `network_uid` is not nullable")
      }

      if (!missing(`third_party_id`) && is.null(`third_party_id`)) {
        stop("Invalid value for `third_party_id` when calling ThirdPartyCredentialsApi$DeleteCredential, `third_party_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ThirdPartyCredentialsApi$DeleteCredential, `app_key` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["networkUID"]] <- `network_uid`

      query_params[["thirdPartyId"]] <- `third_party_id`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/thirdparty/credential/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Network
    #'
    #' @param account_id the id of the logged in user
    #' @param network_uid The unique identifier for the third party network defined by Sirqul
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteNetwork = function(account_id, network_uid, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteNetworkWithHttpInfo(account_id, network_uid, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Network
    #'
    #' @param account_id the id of the logged in user
    #' @param network_uid The unique identifier for the third party network defined by Sirqul
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteNetworkWithHttpInfo = function(account_id, network_uid, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`network_uid`)) {
        stop("Missing required parameter `network_uid`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThirdPartyCredentialsApi$DeleteNetwork, `account_id` is not nullable")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling ThirdPartyCredentialsApi$DeleteNetwork, `network_uid` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["networkUID"]] <- `network_uid`

      local_var_url_path <- "/thirdparty/network/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Credential
    #'
    #' @param network_uid the access provider to authenticate against
    #' @param app_key the application key
    #' @param account_id (optional) the unique account id of a specific account that will be bound to the third-party credentials
    #' @param device_id (optional) the unique id of the device making the request
    #' @param session_id (optional) the session id for the request
    #' @param third_party_credential_id (optional) the third-party credentials id from the response of the credential/create step
    #' @param third_party_token (optional) the access token to authenticate with
    #' @param third_party_secret (optional) the secret code to authenticate with (used for MFA)
    #' @param create_new_account (optional) flag to force creation of a new account when no accountId is passed and user chooses not to use listed accounts (default value: FALSE)
    #' @param response_filters (optional) this determines how much of the profile should be returned, see ProfileFilters
    #' @param latitude (optional) the latitude of the user
    #' @param longitude (optional) the longitude of the user
    #' @param audience_ids_to_add (optional) audience ids to add to the account
    #' @param audience_ids_to_remove (optional) audience ids to remove from the account
    #' @param referral_account_id (optional) account id of the referrer (inviter-invitee relationship)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    GetCredential = function(network_uid, app_key, account_id = NULL, device_id = NULL, session_id = NULL, third_party_credential_id = NULL, third_party_token = NULL, third_party_secret = NULL, create_new_account = FALSE, response_filters = NULL, latitude = NULL, longitude = NULL, audience_ids_to_add = NULL, audience_ids_to_remove = NULL, referral_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetCredentialWithHttpInfo(network_uid, app_key, account_id, device_id, session_id, third_party_credential_id, third_party_token, third_party_secret, create_new_account, response_filters, latitude, longitude, audience_ids_to_add, audience_ids_to_remove, referral_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Credential
    #'
    #' @param network_uid the access provider to authenticate against
    #' @param app_key the application key
    #' @param account_id (optional) the unique account id of a specific account that will be bound to the third-party credentials
    #' @param device_id (optional) the unique id of the device making the request
    #' @param session_id (optional) the session id for the request
    #' @param third_party_credential_id (optional) the third-party credentials id from the response of the credential/create step
    #' @param third_party_token (optional) the access token to authenticate with
    #' @param third_party_secret (optional) the secret code to authenticate with (used for MFA)
    #' @param create_new_account (optional) flag to force creation of a new account when no accountId is passed and user chooses not to use listed accounts (default value: FALSE)
    #' @param response_filters (optional) this determines how much of the profile should be returned, see ProfileFilters
    #' @param latitude (optional) the latitude of the user
    #' @param longitude (optional) the longitude of the user
    #' @param audience_ids_to_add (optional) audience ids to add to the account
    #' @param audience_ids_to_remove (optional) audience ids to remove from the account
    #' @param referral_account_id (optional) account id of the referrer (inviter-invitee relationship)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    GetCredentialWithHttpInfo = function(network_uid, app_key, account_id = NULL, device_id = NULL, session_id = NULL, third_party_credential_id = NULL, third_party_token = NULL, third_party_secret = NULL, create_new_account = FALSE, response_filters = NULL, latitude = NULL, longitude = NULL, audience_ids_to_add = NULL, audience_ids_to_remove = NULL, referral_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`network_uid`)) {
        stop("Missing required parameter `network_uid`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling ThirdPartyCredentialsApi$GetCredential, `network_uid` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ThirdPartyCredentialsApi$GetCredential, `app_key` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThirdPartyCredentialsApi$GetCredential, `account_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ThirdPartyCredentialsApi$GetCredential, `device_id` is not nullable")
      }

      if (!missing(`session_id`) && is.null(`session_id`)) {
        stop("Invalid value for `session_id` when calling ThirdPartyCredentialsApi$GetCredential, `session_id` is not nullable")
      }

      if (!missing(`third_party_credential_id`) && is.null(`third_party_credential_id`)) {
        stop("Invalid value for `third_party_credential_id` when calling ThirdPartyCredentialsApi$GetCredential, `third_party_credential_id` is not nullable")
      }

      if (!missing(`third_party_token`) && is.null(`third_party_token`)) {
        stop("Invalid value for `third_party_token` when calling ThirdPartyCredentialsApi$GetCredential, `third_party_token` is not nullable")
      }

      if (!missing(`third_party_secret`) && is.null(`third_party_secret`)) {
        stop("Invalid value for `third_party_secret` when calling ThirdPartyCredentialsApi$GetCredential, `third_party_secret` is not nullable")
      }

      if (!missing(`create_new_account`) && is.null(`create_new_account`)) {
        stop("Invalid value for `create_new_account` when calling ThirdPartyCredentialsApi$GetCredential, `create_new_account` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling ThirdPartyCredentialsApi$GetCredential, `response_filters` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling ThirdPartyCredentialsApi$GetCredential, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling ThirdPartyCredentialsApi$GetCredential, `longitude` is not nullable")
      }

      if (!missing(`audience_ids_to_add`) && is.null(`audience_ids_to_add`)) {
        stop("Invalid value for `audience_ids_to_add` when calling ThirdPartyCredentialsApi$GetCredential, `audience_ids_to_add` is not nullable")
      }

      if (!missing(`audience_ids_to_remove`) && is.null(`audience_ids_to_remove`)) {
        stop("Invalid value for `audience_ids_to_remove` when calling ThirdPartyCredentialsApi$GetCredential, `audience_ids_to_remove` is not nullable")
      }

      if (!missing(`referral_account_id`) && is.null(`referral_account_id`)) {
        stop("Invalid value for `referral_account_id` when calling ThirdPartyCredentialsApi$GetCredential, `referral_account_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["deviceId"]] <- `device_id`

      query_params[["sessionId"]] <- `session_id`

      query_params[["thirdPartyCredentialId"]] <- `third_party_credential_id`

      query_params[["thirdPartyToken"]] <- `third_party_token`

      query_params[["thirdPartySecret"]] <- `third_party_secret`

      query_params[["createNewAccount"]] <- `create_new_account`

      query_params[["networkUID"]] <- `network_uid`

      query_params[["appKey"]] <- `app_key`

      query_params[["responseFilters"]] <- `response_filters`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["audienceIdsToAdd"]] <- `audience_ids_to_add`

      query_params[["audienceIdsToRemove"]] <- `audience_ids_to_remove`

      query_params[["referralAccountId"]] <- `referral_account_id`

      local_var_url_path <- "/thirdparty/credential/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Network
    #'
    #' @param account_id The account id making the request
    #' @param network_uid The unique identifier for the third party network defined by Sirqul
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ThirdPartyNetworkResponse
    GetNetwork = function(account_id, network_uid, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetNetworkWithHttpInfo(account_id, network_uid, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Network
    #'
    #' @param account_id The account id making the request
    #' @param network_uid The unique identifier for the third party network defined by Sirqul
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ThirdPartyNetworkResponse) with additional information such as HTTP status code, headers
    GetNetworkWithHttpInfo = function(account_id, network_uid, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`network_uid`)) {
        stop("Missing required parameter `network_uid`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThirdPartyCredentialsApi$GetNetwork, `account_id` is not nullable")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling ThirdPartyCredentialsApi$GetNetwork, `network_uid` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["networkUID"]] <- `network_uid`

      local_var_url_path <- "/thirdparty/network/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ThirdPartyNetworkResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Credentials
    #'
    #' @param account_id The account id of the user
    #' @param keyword (optional) The keyword used to search on the third party name and network string
    #' @param network_uid (optional) The network UID to filter results with
    #' @param descending (optional) The order to return the search results
    #' @param start (optional) The start of the pagination (default value: 0)
    #' @param limit (optional) The limit of the pagination (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ThirdPartyCredentialResponse]
    SearchCredentials = function(account_id, keyword = NULL, network_uid = NULL, descending = NULL, start = 0, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchCredentialsWithHttpInfo(account_id, keyword, network_uid, descending, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Credentials
    #'
    #' @param account_id The account id of the user
    #' @param keyword (optional) The keyword used to search on the third party name and network string
    #' @param network_uid (optional) The network UID to filter results with
    #' @param descending (optional) The order to return the search results
    #' @param start (optional) The start of the pagination (default value: 0)
    #' @param limit (optional) The limit of the pagination (default value: 20)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ThirdPartyCredentialResponse]) with additional information such as HTTP status code, headers
    SearchCredentialsWithHttpInfo = function(account_id, keyword = NULL, network_uid = NULL, descending = NULL, start = 0, limit = 20, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThirdPartyCredentialsApi$SearchCredentials, `account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ThirdPartyCredentialsApi$SearchCredentials, `keyword` is not nullable")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling ThirdPartyCredentialsApi$SearchCredentials, `network_uid` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling ThirdPartyCredentialsApi$SearchCredentials, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ThirdPartyCredentialsApi$SearchCredentials, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ThirdPartyCredentialsApi$SearchCredentials, `limit` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["networkUID"]] <- `network_uid`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/thirdparty/credential/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ThirdPartyCredentialResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Networks
    #'
    #' @param account_id The account id making the request
    #' @param sort_field The column to sort the search on, possible values include: UPDATED (default), CREATED, NAME
    #' @param descending The order to return the search results
    #' @param start The start of the pagination
    #' @param limit The limit of the pagination
    #' @param active_only Return only active results
    #' @param keyword (optional) The keyword used to search on the network name and description fields
    #' @param filter_billable (optional) Determines whether to only return applications that the user has access to
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ThirdPartyNetworkShortResponse]
    SearchNetworks = function(account_id, sort_field, descending, start, limit, active_only, keyword = NULL, filter_billable = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchNetworksWithHttpInfo(account_id, sort_field, descending, start, limit, active_only, keyword, filter_billable, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Networks
    #'
    #' @param account_id The account id making the request
    #' @param sort_field The column to sort the search on, possible values include: UPDATED (default), CREATED, NAME
    #' @param descending The order to return the search results
    #' @param start The start of the pagination
    #' @param limit The limit of the pagination
    #' @param active_only Return only active results
    #' @param keyword (optional) The keyword used to search on the network name and description fields
    #' @param filter_billable (optional) Determines whether to only return applications that the user has access to
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ThirdPartyNetworkShortResponse]) with additional information such as HTTP status code, headers
    SearchNetworksWithHttpInfo = function(account_id, sort_field, descending, start, limit, active_only, keyword = NULL, filter_billable = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`active_only`)) {
        stop("Missing required parameter `active_only`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThirdPartyCredentialsApi$SearchNetworks, `account_id` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling ThirdPartyCredentialsApi$SearchNetworks, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling ThirdPartyCredentialsApi$SearchNetworks, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ThirdPartyCredentialsApi$SearchNetworks, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ThirdPartyCredentialsApi$SearchNetworks, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling ThirdPartyCredentialsApi$SearchNetworks, `active_only` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ThirdPartyCredentialsApi$SearchNetworks, `keyword` is not nullable")
      }

      if (!missing(`filter_billable`) && is.null(`filter_billable`)) {
        stop("Invalid value for `filter_billable` when calling ThirdPartyCredentialsApi$SearchNetworks, `filter_billable` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY", "NAME", "DESCRIPTION"))) {
        stop("Invalid value for sort_field when calling ThirdPartyCredentialsApi$SearchNetworks. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY, NAME, DESCRIPTION].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["filterBillable"]] <- `filter_billable`

      local_var_url_path <- "/thirdparty/network/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ThirdPartyNetworkShortResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Send MFA Challenge
    #'
    #' @param network_uid the third party network provider that has MFA enabled
    #' @param app_key the application key
    #' @param third_party_token (optional) the access token to authenticate with
    #' @param third_party_credential_id (optional) optional id of the existing third party credential
    #' @param device_id (optional) the unique id of the device making the request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    SendMFAChallenge = function(network_uid, app_key, third_party_token = NULL, third_party_credential_id = NULL, device_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SendMFAChallengeWithHttpInfo(network_uid, app_key, third_party_token, third_party_credential_id, device_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Send MFA Challenge
    #'
    #' @param network_uid the third party network provider that has MFA enabled
    #' @param app_key the application key
    #' @param third_party_token (optional) the access token to authenticate with
    #' @param third_party_credential_id (optional) optional id of the existing third party credential
    #' @param device_id (optional) the unique id of the device making the request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    SendMFAChallengeWithHttpInfo = function(network_uid, app_key, third_party_token = NULL, third_party_credential_id = NULL, device_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`network_uid`)) {
        stop("Missing required parameter `network_uid`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling ThirdPartyCredentialsApi$SendMFAChallenge, `network_uid` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ThirdPartyCredentialsApi$SendMFAChallenge, `app_key` is not nullable")
      }

      if (!missing(`third_party_token`) && is.null(`third_party_token`)) {
        stop("Invalid value for `third_party_token` when calling ThirdPartyCredentialsApi$SendMFAChallenge, `third_party_token` is not nullable")
      }

      if (!missing(`third_party_credential_id`) && is.null(`third_party_credential_id`)) {
        stop("Invalid value for `third_party_credential_id` when calling ThirdPartyCredentialsApi$SendMFAChallenge, `third_party_credential_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ThirdPartyCredentialsApi$SendMFAChallenge, `device_id` is not nullable")
      }

      query_params[["thirdPartyToken"]] <- `third_party_token`

      query_params[["thirdPartyCredentialId"]] <- `third_party_credential_id`

      query_params[["networkUID"]] <- `network_uid`

      query_params[["appKey"]] <- `app_key`

      query_params[["deviceId"]] <- `device_id`

      local_var_url_path <- "/thirdparty/credential/mfa/send"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Credential
    #'
    #' @param network_uid the access provider to authenticate against
    #' @param third_party_id the third party user account id
    #' @param app_key the application key
    #' @param device_id (optional) the unique id of the device making the request
    #' @param third_party_name (optional) the third party user name
    #' @param third_party_token (optional) the access token to authenticate with (ex: username or fb token)
    #' @param response_filters (optional) this determines how much of the profile should be returned, see ProfileFilters
    #' @param meta_data (optional) External custom client defined data
    #' @param third_party_refresh_token (optional) optional refresh token for the third party
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    UpdateCredential = function(network_uid, third_party_id, app_key, device_id = NULL, third_party_name = NULL, third_party_token = NULL, response_filters = NULL, meta_data = NULL, third_party_refresh_token = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateCredentialWithHttpInfo(network_uid, third_party_id, app_key, device_id, third_party_name, third_party_token, response_filters, meta_data, third_party_refresh_token, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Credential
    #'
    #' @param network_uid the access provider to authenticate against
    #' @param third_party_id the third party user account id
    #' @param app_key the application key
    #' @param device_id (optional) the unique id of the device making the request
    #' @param third_party_name (optional) the third party user name
    #' @param third_party_token (optional) the access token to authenticate with (ex: username or fb token)
    #' @param response_filters (optional) this determines how much of the profile should be returned, see ProfileFilters
    #' @param meta_data (optional) External custom client defined data
    #' @param third_party_refresh_token (optional) optional refresh token for the third party
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    UpdateCredentialWithHttpInfo = function(network_uid, third_party_id, app_key, device_id = NULL, third_party_name = NULL, third_party_token = NULL, response_filters = NULL, meta_data = NULL, third_party_refresh_token = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`network_uid`)) {
        stop("Missing required parameter `network_uid`.")
      }

      if (missing(`third_party_id`)) {
        stop("Missing required parameter `third_party_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling ThirdPartyCredentialsApi$UpdateCredential, `network_uid` is not nullable")
      }

      if (!missing(`third_party_id`) && is.null(`third_party_id`)) {
        stop("Invalid value for `third_party_id` when calling ThirdPartyCredentialsApi$UpdateCredential, `third_party_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ThirdPartyCredentialsApi$UpdateCredential, `app_key` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling ThirdPartyCredentialsApi$UpdateCredential, `device_id` is not nullable")
      }

      if (!missing(`third_party_name`) && is.null(`third_party_name`)) {
        stop("Invalid value for `third_party_name` when calling ThirdPartyCredentialsApi$UpdateCredential, `third_party_name` is not nullable")
      }

      if (!missing(`third_party_token`) && is.null(`third_party_token`)) {
        stop("Invalid value for `third_party_token` when calling ThirdPartyCredentialsApi$UpdateCredential, `third_party_token` is not nullable")
      }

      if (!missing(`response_filters`) && is.null(`response_filters`)) {
        stop("Invalid value for `response_filters` when calling ThirdPartyCredentialsApi$UpdateCredential, `response_filters` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling ThirdPartyCredentialsApi$UpdateCredential, `meta_data` is not nullable")
      }

      if (!missing(`third_party_refresh_token`) && is.null(`third_party_refresh_token`)) {
        stop("Invalid value for `third_party_refresh_token` when calling ThirdPartyCredentialsApi$UpdateCredential, `third_party_refresh_token` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["networkUID"]] <- `network_uid`

      query_params[["thirdPartyId"]] <- `third_party_id`

      query_params[["thirdPartyName"]] <- `third_party_name`

      query_params[["thirdPartyToken"]] <- `third_party_token`

      query_params[["appKey"]] <- `app_key`

      query_params[["responseFilters"]] <- `response_filters`

      query_params[["metaData"]] <- `meta_data`

      query_params[["thirdPartyRefreshToken"]] <- `third_party_refresh_token`

      local_var_url_path <- "/thirdparty/credential/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Network
    #'
    #' @param account_id The account id making the request
    #' @param network_uid The unique identifier for the third party network defined by Sirqul
    #' @param name (optional) The name of the network
    #' @param description (optional) The description of the network
    #' @param enable_introspection (optional) Whether the network uses introspection calls
    #' @param introspection_method (optional) HTTP method to use for introspection calls (e.g., GET, POST)
    #' @param introspection_url (optional) The HTTP URL of the introspection call
    #' @param introspection_params (optional) The parameters of the introspection call
    #' @param required_root_field (optional) Required response params
    #' @param enable_mfa (optional) Whether this network uses MFA
    #' @param size_mfa (optional) Size of the MFA token
    #' @param shelf_life_mfa (optional) Shelf life (seconds) of the MFA token
    #' @param oauth_token_url (optional) OAuth token endpoint URL
    #' @param oauth_private_key (optional) OAuth private key file (multipart)
    #' @param oauth_public_key (optional) OAuth public key file (multipart)
    #' @param oauth_client_id (optional) OAuth client id
    #' @param oauth_secret_key (optional) OAuth secret key
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ThirdPartyNetworkResponse
    UpdateNetwork = function(account_id, network_uid, name = NULL, description = NULL, enable_introspection = NULL, introspection_method = NULL, introspection_url = NULL, introspection_params = NULL, required_root_field = NULL, enable_mfa = NULL, size_mfa = NULL, shelf_life_mfa = NULL, oauth_token_url = NULL, oauth_private_key = NULL, oauth_public_key = NULL, oauth_client_id = NULL, oauth_secret_key = NULL, body = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateNetworkWithHttpInfo(account_id, network_uid, name, description, enable_introspection, introspection_method, introspection_url, introspection_params, required_root_field, enable_mfa, size_mfa, shelf_life_mfa, oauth_token_url, oauth_private_key, oauth_public_key, oauth_client_id, oauth_secret_key, body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Network
    #'
    #' @param account_id The account id making the request
    #' @param network_uid The unique identifier for the third party network defined by Sirqul
    #' @param name (optional) The name of the network
    #' @param description (optional) The description of the network
    #' @param enable_introspection (optional) Whether the network uses introspection calls
    #' @param introspection_method (optional) HTTP method to use for introspection calls (e.g., GET, POST)
    #' @param introspection_url (optional) The HTTP URL of the introspection call
    #' @param introspection_params (optional) The parameters of the introspection call
    #' @param required_root_field (optional) Required response params
    #' @param enable_mfa (optional) Whether this network uses MFA
    #' @param size_mfa (optional) Size of the MFA token
    #' @param shelf_life_mfa (optional) Shelf life (seconds) of the MFA token
    #' @param oauth_token_url (optional) OAuth token endpoint URL
    #' @param oauth_private_key (optional) OAuth private key file (multipart)
    #' @param oauth_public_key (optional) OAuth public key file (multipart)
    #' @param oauth_client_id (optional) OAuth client id
    #' @param oauth_secret_key (optional) OAuth secret key
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ThirdPartyNetworkResponse) with additional information such as HTTP status code, headers
    UpdateNetworkWithHttpInfo = function(account_id, network_uid, name = NULL, description = NULL, enable_introspection = NULL, introspection_method = NULL, introspection_url = NULL, introspection_params = NULL, required_root_field = NULL, enable_mfa = NULL, size_mfa = NULL, shelf_life_mfa = NULL, oauth_token_url = NULL, oauth_private_key = NULL, oauth_public_key = NULL, oauth_client_id = NULL, oauth_secret_key = NULL, body = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`network_uid`)) {
        stop("Missing required parameter `network_uid`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ThirdPartyCredentialsApi$UpdateNetwork, `account_id` is not nullable")
      }

      if (!missing(`network_uid`) && is.null(`network_uid`)) {
        stop("Invalid value for `network_uid` when calling ThirdPartyCredentialsApi$UpdateNetwork, `network_uid` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling ThirdPartyCredentialsApi$UpdateNetwork, `name` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling ThirdPartyCredentialsApi$UpdateNetwork, `description` is not nullable")
      }

      if (!missing(`enable_introspection`) && is.null(`enable_introspection`)) {
        stop("Invalid value for `enable_introspection` when calling ThirdPartyCredentialsApi$UpdateNetwork, `enable_introspection` is not nullable")
      }

      if (!missing(`introspection_method`) && is.null(`introspection_method`)) {
        stop("Invalid value for `introspection_method` when calling ThirdPartyCredentialsApi$UpdateNetwork, `introspection_method` is not nullable")
      }

      if (!missing(`introspection_url`) && is.null(`introspection_url`)) {
        stop("Invalid value for `introspection_url` when calling ThirdPartyCredentialsApi$UpdateNetwork, `introspection_url` is not nullable")
      }

      if (!missing(`introspection_params`) && is.null(`introspection_params`)) {
        stop("Invalid value for `introspection_params` when calling ThirdPartyCredentialsApi$UpdateNetwork, `introspection_params` is not nullable")
      }

      if (!missing(`required_root_field`) && is.null(`required_root_field`)) {
        stop("Invalid value for `required_root_field` when calling ThirdPartyCredentialsApi$UpdateNetwork, `required_root_field` is not nullable")
      }

      if (!missing(`enable_mfa`) && is.null(`enable_mfa`)) {
        stop("Invalid value for `enable_mfa` when calling ThirdPartyCredentialsApi$UpdateNetwork, `enable_mfa` is not nullable")
      }

      if (!missing(`size_mfa`) && is.null(`size_mfa`)) {
        stop("Invalid value for `size_mfa` when calling ThirdPartyCredentialsApi$UpdateNetwork, `size_mfa` is not nullable")
      }

      if (!missing(`shelf_life_mfa`) && is.null(`shelf_life_mfa`)) {
        stop("Invalid value for `shelf_life_mfa` when calling ThirdPartyCredentialsApi$UpdateNetwork, `shelf_life_mfa` is not nullable")
      }

      if (!missing(`oauth_token_url`) && is.null(`oauth_token_url`)) {
        stop("Invalid value for `oauth_token_url` when calling ThirdPartyCredentialsApi$UpdateNetwork, `oauth_token_url` is not nullable")
      }

      if (!missing(`oauth_private_key`) && is.null(`oauth_private_key`)) {
        stop("Invalid value for `oauth_private_key` when calling ThirdPartyCredentialsApi$UpdateNetwork, `oauth_private_key` is not nullable")
      }

      if (!missing(`oauth_public_key`) && is.null(`oauth_public_key`)) {
        stop("Invalid value for `oauth_public_key` when calling ThirdPartyCredentialsApi$UpdateNetwork, `oauth_public_key` is not nullable")
      }

      if (!missing(`oauth_client_id`) && is.null(`oauth_client_id`)) {
        stop("Invalid value for `oauth_client_id` when calling ThirdPartyCredentialsApi$UpdateNetwork, `oauth_client_id` is not nullable")
      }

      if (!missing(`oauth_secret_key`) && is.null(`oauth_secret_key`)) {
        stop("Invalid value for `oauth_secret_key` when calling ThirdPartyCredentialsApi$UpdateNetwork, `oauth_secret_key` is not nullable")
      }

      if (!missing(`body`) && is.null(`body`)) {
        stop("Invalid value for `body` when calling ThirdPartyCredentialsApi$UpdateNetwork, `body` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["networkUID"]] <- `network_uid`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["enableIntrospection"]] <- `enable_introspection`

      query_params[["introspectionMethod"]] <- `introspection_method`

      query_params[["introspectionURL"]] <- `introspection_url`

      query_params[["introspectionParams"]] <- `introspection_params`

      query_params[["requiredRootField"]] <- `required_root_field`

      query_params[["enableMFA"]] <- `enable_mfa`

      query_params[["sizeMFA"]] <- `size_mfa`

      query_params[["shelfLifeMFA"]] <- `shelf_life_mfa`

      query_params[["oauthTokenURL"]] <- `oauth_token_url`

      query_params[["oauthPrivateKey"]] <- `oauth_private_key`

      query_params[["oauthPublicKey"]] <- `oauth_public_key`

      query_params[["oauthClientId"]] <- `oauth_client_id`

      query_params[["oauthSecretKey"]] <- `oauth_secret_key`

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/thirdparty/network/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ThirdPartyNetworkResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
