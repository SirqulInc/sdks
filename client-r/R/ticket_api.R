#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Ticket operations
#' @description TicketApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GetTicketCount  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the id of the device that owns the tickets (Optional)
#' var_account_id <- 56 # integer | the id of the account that owns the tickets (Optional)
#' var_game_type <- "game_type_example" # character | this is deprecated. (Optional)
#' var_app_key <- "app_key_example" # character | the applicationkey (Optional)
#' var_ticket_type <- "ticket_type_example" # character | the type of ticket (Optional)
#'
#' #Get Ticket Count
#' api_instance <- TicketApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetTicketCount(device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, app_key = var_app_key, ticket_type = var_ticket_typedata_file = "result.txt")
#' result <- api_instance$GetTicketCount(device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, app_key = var_app_key, ticket_type = var_ticket_type)
#' dput(result)
#'
#'
#' ####################  GetTicketList  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the id of the device that owns the tickets (Optional)
#' var_account_id <- 56 # integer | the id of the account that owns the tickets (Optional)
#' var_ticket_object_type <- "ticket_object_type_example" # character | comma separated list of TicketObjectType (Optional)
#' var_action_type <- "action_type_example" # character | comma separated list of TicketActionType (Optional)
#' var_ticket_ids <- "ticket_ids_example" # character | the ids of the tickets to get (Optional)
#' var_object_ids <- "object_ids_example" # character | the ids of the objects to get (Optional)
#' var_receipt_tokens <- "receipt_tokens_example" # character |  (Optional)
#' var_game_type <- "game_type_example" # character |  (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#'
#' #Get Ticket List
#' api_instance <- TicketApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetTicketList(device_id = var_device_id, account_id = var_account_id, ticket_object_type = var_ticket_object_type, action_type = var_action_type, ticket_ids = var_ticket_ids, object_ids = var_object_ids, receipt_tokens = var_receipt_tokens, game_type = var_game_type, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$GetTicketList(device_id = var_device_id, account_id = var_account_id, ticket_object_type = var_ticket_object_type, action_type = var_action_type, ticket_ids = var_ticket_ids, object_ids = var_object_ids, receipt_tokens = var_receipt_tokens, game_type = var_game_type, app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  GiftPurchase  ####################
#'
#' library(openapi)
#' var_receiver_account_id <- 56 # integer | the id of the account receiving the tickets
#' var_ticket_id <- 56 # integer | the id of the tickets
#' var_device_id <- "device_id_example" # character | the id of the device (Optional)
#' var_account_id <- 56 # integer | the id of the gift owner (Optional)
#' var_asset_id <- 56 # integer | the id of the asset (Optional)
#' var_custom_message <- "custom_message_example" # character | a message that can be written to go along with the gift (Optional)
#' var_game_type <- "game_type_example" # character | the type of game associated with the tickets (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#'
#' #Gift Tickets
#' api_instance <- TicketApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GiftPurchase(var_receiver_account_id, var_ticket_id, device_id = var_device_id, account_id = var_account_id, asset_id = var_asset_id, custom_message = var_custom_message, game_type = var_game_type, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$GiftPurchase(var_receiver_account_id, var_ticket_id, device_id = var_device_id, account_id = var_account_id, asset_id = var_asset_id, custom_message = var_custom_message, game_type = var_game_type, app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  SaveTicket  ####################
#'
#' library(openapi)
#' var_action_type <- "action_type_example" # character | the action being performed, values: COMPLETED, // ADD TICKETS FOR COMPLETING A MISSION, CHALLENGE, GAME, PACK, LEVEL, LEVEL OBJECT REDEEMED, // REMOVE TICKETS FOR BUYING PACKS, HINTS, AND PEN TOOLS OPTIONS, ETC USERS_PLAYED, // ADD TICKETS FOR LEVELS PLAYED BY OTHER USERS TOURNAMENT_OWNER, // ADD TICKETS FOR TOURNAMENTS BY OTHER USERS PURCHASED, // ADD TICKET VIA IN APP PURCHASING SUMATION, // SUMATION OF TICKETS EARNED FROM CHILDREN GIFTED, // TRANSFERING OF PURCHASE ITEMS TO OTHER PEOPLE REFUNDED // FOR REFUNDING TICKETS BACK TO THE USER
#' var_ticket_object_type <- "ticket_object_type_example" # character | the type of object being purchased, values: GAME_OBJECT, GAME_LEVEL, PACK, GAME, MISSION, PROFILE, APPLICATION, TICKETS, ASSET, CUSTOM
#' var_return_nulls <- "return_nulls_example" # character | whether to return nulls or not (Optional)
#' var_device_id <- "device_id_example" # character | the device id that owns the tickets (Optional)
#' var_account_id <- 56 # integer | the account id that owns the tickets (Optional)
#' var_game_type <- "game_type_example" # character | This parameter is deprecated. deprecated, use the appKey (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_object_id <- 56 # integer | the ID of the item being purchased (Optional)
#' var_purchase_code <- "purchase_code_example" # character | a unique string identifier defined by the application owner or Executive (Optional)
#' var_receipt_token <- "receipt_token_example" # character | a unique transaction identifier from the purchase provider (i.e. the transaction ID from iTunes, Google Play, etc) (Optional)
#' var_receipt_data <- "receipt_data_example" # character | the receipt/transaction data for validating a purchase via iTunes/Gooogle/etc. This should be in string format. (Optional)
#' var_count <- 56 # integer | the count of tickets (Optional)
#' var_ticket_type <- "ticket_type_example" # character | the ticket type (Optional)
#' var_purchase_provider <- "purchase_provider_example" # character | the provider for making the purchase {ITUNES, ITUNES_SANDBOX, GOOGLE} (Optional)
#' var_purchase_type <- "purchase_type_example" # character | a comma separated list of purchase types { SIRQUL, // purchasing from the sirqul store using tickets IOS, // purchasing from the itunes store for iPhone, iPod, iPod Touch GOOGLE, // purchasing from the google android store AMAZON, // purchasing from the amazon android store MAC, // purchasing from the itunes store for OSX WP8, // purhasing from the windows phone 8 store FREE } (Optional)
#' var_return_profile_response <- "return_profile_response_example" # character | returns a ProfileResponse if true, otherwise will return an AppResponse (Optional)
#' var_include_profile_response <- "include_profile_response_example" # character | if returnProfileResponse is false, will return an AppResponse with profile data if true (Optional)
#' var_app_version <- "app_version_example" # character | the application version (Optional)
#'
#' #Save Ticket
#' api_instance <- TicketApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SaveTicket(var_action_type, var_ticket_object_type, return_nulls = var_return_nulls, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, app_key = var_app_key, object_id = var_object_id, purchase_code = var_purchase_code, receipt_token = var_receipt_token, receipt_data = var_receipt_data, count = var_count, ticket_type = var_ticket_type, purchase_provider = var_purchase_provider, purchase_type = var_purchase_type, return_profile_response = var_return_profile_response, include_profile_response = var_include_profile_response, app_version = var_app_versiondata_file = "result.txt")
#' result <- api_instance$SaveTicket(var_action_type, var_ticket_object_type, return_nulls = var_return_nulls, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, app_key = var_app_key, object_id = var_object_id, purchase_code = var_purchase_code, receipt_token = var_receipt_token, receipt_data = var_receipt_data, count = var_count, ticket_type = var_ticket_type, purchase_provider = var_purchase_provider, purchase_type = var_purchase_type, return_profile_response = var_return_profile_response, include_profile_response = var_include_profile_response, app_version = var_app_version)
#' dput(result)
#'
#'
#' ####################  SaveTicketViaFileUpload  ####################
#'
#' library(openapi)
#' var_action_type <- "action_type_example" # character | the action being performed { COMPLETED, // ADD TICKETS FOR COMPLETING A MISSION, CHALLENGE, GAME, PACK, LEVEL, LEVEL OBJECT REDEEMED, // REMOVE TICKETS FOR BUYING PACKS, HINTS, AND PEN TOOLS OPTIONS, ETC USERS_PLAYED, // ADD TICKETS FOR LEVELS PLAYED BY OTHER USERS TOURNAMENT_OWNER, // ADD TICKETS FOR TOURNAMENTS BY OTHER USERS PURCHASED, // ADD TICKET VIA IN APP PURCHASING SUMATION, // SUMATION OF TICKETS EARNED FROM CHILDREN GIFTED, // TRANSFERING OF PURCHASE ITEMS TO OTHER PEOPLE REFUNDED // FOR REFUNDING TICKETS BACK TO THE USER }
#' var_ticket_object_type <- "ticket_object_type_example" # character | the type of object being purchased {GAME_OBJECT, GAME_LEVEL, PACK, GAME, MISSION, PROFILE, APPLICATION, TICKETS, ASSET, CUSTOM}
#' var_receipt_data <- File.new('/path/to/file') # data.frame | the receipt/transaction data for validating a purchase via iTunes/Gooogle/etc. This should be in binary format.
#' var_return_nulls <- "return_nulls_example" # character | whether to return nulls or not (Optional)
#' var_device_id <- "device_id_example" # character | the device id (Optional)
#' var_account_id <- 56 # integer | the account id (Optional)
#' var_game_type <- "game_type_example" # character | This parameter is deprecated. deprecated, use the appKey (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_object_id <- 56 # integer | the ID of the item being purchased (Optional)
#' var_purchase_code <- "purchase_code_example" # character | a unique string identifier defined by the application owner or Executive (Optional)
#' var_receipt_token <- "receipt_token_example" # character | a unique transaction identifier from the purchase provider (i.e. the transaction ID from iTunes, Google Play, etc) (Optional)
#' var_count <- 56 # integer | the count of tickets (Optional)
#' var_ticket_type <- "ticket_type_example" # character | the ticket type (Optional)
#' var_purchase_provider <- "purchase_provider_example" # character | the provider for making the purchase {ITUNES, ITUNES_SANDBOX, GOOGLE} (Optional)
#' var_purchase_type <- "purchase_type_example" # character | a comma separated list of purchase types { SIRQUL, // purchasing from the sirqul store using tickets IOS, // purchasing from the itunes store for iPhone, iPod, iPod Touch GOOGLE, // purchasing from the google android store AMAZON, // purchasing from the amazon android store MAC, // purchasing from the itunes store for OSX WP8, // purhasing from the windows phone 8 store FREE } (Optional)
#' var_return_profile_response <- "return_profile_response_example" # character | returns a ProfileResponse if true, otherwise will return an AppResponse (Optional)
#' var_include_profile_response <- "include_profile_response_example" # character | if returnProfileResponse is false, will return an AppResponse with profile data if true (Optional)
#' var_app_version <- "app_version_example" # character | the application version (Optional)
#'
#' #Save Ticket with Reciept
#' api_instance <- TicketApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SaveTicketViaFileUpload(var_action_type, var_ticket_object_type, var_receipt_data, return_nulls = var_return_nulls, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, app_key = var_app_key, object_id = var_object_id, purchase_code = var_purchase_code, receipt_token = var_receipt_token, count = var_count, ticket_type = var_ticket_type, purchase_provider = var_purchase_provider, purchase_type = var_purchase_type, return_profile_response = var_return_profile_response, include_profile_response = var_include_profile_response, app_version = var_app_versiondata_file = "result.txt")
#' result <- api_instance$SaveTicketViaFileUpload(var_action_type, var_ticket_object_type, var_receipt_data, return_nulls = var_return_nulls, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, app_key = var_app_key, object_id = var_object_id, purchase_code = var_purchase_code, receipt_token = var_receipt_token, count = var_count, ticket_type = var_ticket_type, purchase_provider = var_purchase_provider, purchase_type = var_purchase_type, return_profile_response = var_return_profile_response, include_profile_response = var_include_profile_response, app_version = var_app_version)
#' dput(result)
#'
#'
#' ####################  TicketOffers  ####################
#'
#' library(openapi)
#'
#' #Get Ticket Offers
#' api_instance <- TicketApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$TicketOffers(data_file = "result.txt")
#' result <- api_instance$TicketOffers()
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
TicketApi <- R6::R6Class(
  "TicketApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new TicketApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get Ticket Count
    #'
    #' @param device_id (optional) the id of the device that owns the tickets
    #' @param account_id (optional) the id of the account that owns the tickets
    #' @param game_type (optional) this is deprecated.
    #' @param app_key (optional) the applicationkey
    #' @param ticket_type (optional) the type of ticket
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CountResponse
    GetTicketCount = function(device_id = NULL, account_id = NULL, game_type = NULL, app_key = NULL, ticket_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetTicketCountWithHttpInfo(device_id, account_id, game_type, app_key, ticket_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Ticket Count
    #'
    #' @param device_id (optional) the id of the device that owns the tickets
    #' @param account_id (optional) the id of the account that owns the tickets
    #' @param game_type (optional) this is deprecated.
    #' @param app_key (optional) the applicationkey
    #' @param ticket_type (optional) the type of ticket
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CountResponse) with additional information such as HTTP status code, headers
    GetTicketCountWithHttpInfo = function(device_id = NULL, account_id = NULL, game_type = NULL, app_key = NULL, ticket_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling TicketApi$GetTicketCount, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling TicketApi$GetTicketCount, `account_id` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling TicketApi$GetTicketCount, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling TicketApi$GetTicketCount, `app_key` is not nullable")
      }

      if (!missing(`ticket_type`) && is.null(`ticket_type`)) {
        stop("Invalid value for `ticket_type` when calling TicketApi$GetTicketCount, `ticket_type` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["ticketType"]] <- `ticket_type`

      local_var_url_path <- "/ticket/count"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CountResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Ticket List
    #'
    #' @param device_id (optional) the id of the device that owns the tickets
    #' @param account_id (optional) the id of the account that owns the tickets
    #' @param ticket_object_type (optional) comma separated list of TicketObjectType
    #' @param action_type (optional) comma separated list of TicketActionType
    #' @param ticket_ids (optional) the ids of the tickets to get
    #' @param object_ids (optional) the ids of the objects to get
    #' @param receipt_tokens (optional) 
    #' @param game_type (optional) 
    #' @param app_key (optional) the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return TicketListResponse
    GetTicketList = function(device_id = NULL, account_id = NULL, ticket_object_type = NULL, action_type = NULL, ticket_ids = NULL, object_ids = NULL, receipt_tokens = NULL, game_type = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetTicketListWithHttpInfo(device_id, account_id, ticket_object_type, action_type, ticket_ids, object_ids, receipt_tokens, game_type, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Ticket List
    #'
    #' @param device_id (optional) the id of the device that owns the tickets
    #' @param account_id (optional) the id of the account that owns the tickets
    #' @param ticket_object_type (optional) comma separated list of TicketObjectType
    #' @param action_type (optional) comma separated list of TicketActionType
    #' @param ticket_ids (optional) the ids of the tickets to get
    #' @param object_ids (optional) the ids of the objects to get
    #' @param receipt_tokens (optional) 
    #' @param game_type (optional) 
    #' @param app_key (optional) the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (TicketListResponse) with additional information such as HTTP status code, headers
    GetTicketListWithHttpInfo = function(device_id = NULL, account_id = NULL, ticket_object_type = NULL, action_type = NULL, ticket_ids = NULL, object_ids = NULL, receipt_tokens = NULL, game_type = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling TicketApi$GetTicketList, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling TicketApi$GetTicketList, `account_id` is not nullable")
      }

      if (!missing(`ticket_object_type`) && is.null(`ticket_object_type`)) {
        stop("Invalid value for `ticket_object_type` when calling TicketApi$GetTicketList, `ticket_object_type` is not nullable")
      }

      if (!missing(`action_type`) && is.null(`action_type`)) {
        stop("Invalid value for `action_type` when calling TicketApi$GetTicketList, `action_type` is not nullable")
      }

      if (!missing(`ticket_ids`) && is.null(`ticket_ids`)) {
        stop("Invalid value for `ticket_ids` when calling TicketApi$GetTicketList, `ticket_ids` is not nullable")
      }

      if (!missing(`object_ids`) && is.null(`object_ids`)) {
        stop("Invalid value for `object_ids` when calling TicketApi$GetTicketList, `object_ids` is not nullable")
      }

      if (!missing(`receipt_tokens`) && is.null(`receipt_tokens`)) {
        stop("Invalid value for `receipt_tokens` when calling TicketApi$GetTicketList, `receipt_tokens` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling TicketApi$GetTicketList, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling TicketApi$GetTicketList, `app_key` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["ticketObjectType"]] <- `ticket_object_type`

      query_params[["actionType"]] <- `action_type`

      query_params[["ticketIds"]] <- `ticket_ids`

      query_params[["objectIds"]] <- `object_ids`

      query_params[["receiptTokens"]] <- `receipt_tokens`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/ticket/getList"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "TicketListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Gift Tickets
    #'
    #' @param receiver_account_id the id of the account receiving the tickets
    #' @param ticket_id the id of the tickets
    #' @param device_id (optional) the id of the device
    #' @param account_id (optional) the id of the gift owner
    #' @param asset_id (optional) the id of the asset
    #' @param custom_message (optional) a message that can be written to go along with the gift
    #' @param game_type (optional) the type of game associated with the tickets
    #' @param app_key (optional) the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    GiftPurchase = function(receiver_account_id, ticket_id, device_id = NULL, account_id = NULL, asset_id = NULL, custom_message = NULL, game_type = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GiftPurchaseWithHttpInfo(receiver_account_id, ticket_id, device_id, account_id, asset_id, custom_message, game_type, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Gift Tickets
    #'
    #' @param receiver_account_id the id of the account receiving the tickets
    #' @param ticket_id the id of the tickets
    #' @param device_id (optional) the id of the device
    #' @param account_id (optional) the id of the gift owner
    #' @param asset_id (optional) the id of the asset
    #' @param custom_message (optional) a message that can be written to go along with the gift
    #' @param game_type (optional) the type of game associated with the tickets
    #' @param app_key (optional) the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    GiftPurchaseWithHttpInfo = function(receiver_account_id, ticket_id, device_id = NULL, account_id = NULL, asset_id = NULL, custom_message = NULL, game_type = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`receiver_account_id`)) {
        stop("Missing required parameter `receiver_account_id`.")
      }

      if (missing(`ticket_id`)) {
        stop("Missing required parameter `ticket_id`.")
      }

      if (!missing(`receiver_account_id`) && is.null(`receiver_account_id`)) {
        stop("Invalid value for `receiver_account_id` when calling TicketApi$GiftPurchase, `receiver_account_id` is not nullable")
      }

      if (!missing(`ticket_id`) && is.null(`ticket_id`)) {
        stop("Invalid value for `ticket_id` when calling TicketApi$GiftPurchase, `ticket_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling TicketApi$GiftPurchase, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling TicketApi$GiftPurchase, `account_id` is not nullable")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling TicketApi$GiftPurchase, `asset_id` is not nullable")
      }

      if (!missing(`custom_message`) && is.null(`custom_message`)) {
        stop("Invalid value for `custom_message` when calling TicketApi$GiftPurchase, `custom_message` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling TicketApi$GiftPurchase, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling TicketApi$GiftPurchase, `app_key` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["receiverAccountId"]] <- `receiver_account_id`

      query_params[["ticketId"]] <- `ticket_id`

      query_params[["assetId"]] <- `asset_id`

      query_params[["customMessage"]] <- `custom_message`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/purchase/gift"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Save Ticket
    #'
    #' @param action_type the action being performed, values: COMPLETED, // ADD TICKETS FOR COMPLETING A MISSION, CHALLENGE, GAME, PACK, LEVEL, LEVEL OBJECT REDEEMED, // REMOVE TICKETS FOR BUYING PACKS, HINTS, AND PEN TOOLS OPTIONS, ETC USERS_PLAYED, // ADD TICKETS FOR LEVELS PLAYED BY OTHER USERS TOURNAMENT_OWNER, // ADD TICKETS FOR TOURNAMENTS BY OTHER USERS PURCHASED, // ADD TICKET VIA IN APP PURCHASING SUMATION, // SUMATION OF TICKETS EARNED FROM CHILDREN GIFTED, // TRANSFERING OF PURCHASE ITEMS TO OTHER PEOPLE REFUNDED // FOR REFUNDING TICKETS BACK TO THE USER
    #' @param ticket_object_type the type of object being purchased, values: GAME_OBJECT, GAME_LEVEL, PACK, GAME, MISSION, PROFILE, APPLICATION, TICKETS, ASSET, CUSTOM
    #' @param return_nulls (optional) whether to return nulls or not
    #' @param device_id (optional) the device id that owns the tickets
    #' @param account_id (optional) the account id that owns the tickets
    #' @param game_type (optional) This parameter is deprecated. deprecated, use the appKey
    #' @param app_key (optional) the application key
    #' @param object_id (optional) the ID of the item being purchased
    #' @param purchase_code (optional) a unique string identifier defined by the application owner or Executive
    #' @param receipt_token (optional) a unique transaction identifier from the purchase provider (i.e. the transaction ID from iTunes, Google Play, etc)
    #' @param receipt_data (optional) the receipt/transaction data for validating a purchase via iTunes/Gooogle/etc. This should be in string format.
    #' @param count (optional) the count of tickets
    #' @param ticket_type (optional) the ticket type
    #' @param purchase_provider (optional) the provider for making the purchase {ITUNES, ITUNES_SANDBOX, GOOGLE}
    #' @param purchase_type (optional) a comma separated list of purchase types { SIRQUL, // purchasing from the sirqul store using tickets IOS, // purchasing from the itunes store for iPhone, iPod, iPod Touch GOOGLE, // purchasing from the google android store AMAZON, // purchasing from the amazon android store MAC, // purchasing from the itunes store for OSX WP8, // purhasing from the windows phone 8 store FREE }
    #' @param return_profile_response (optional) returns a ProfileResponse if true, otherwise will return an AppResponse
    #' @param include_profile_response (optional) if returnProfileResponse is false, will return an AppResponse with profile data if true
    #' @param app_version (optional) the application version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    SaveTicket = function(action_type, ticket_object_type, return_nulls = NULL, device_id = NULL, account_id = NULL, game_type = NULL, app_key = NULL, object_id = NULL, purchase_code = NULL, receipt_token = NULL, receipt_data = NULL, count = NULL, ticket_type = NULL, purchase_provider = NULL, purchase_type = NULL, return_profile_response = NULL, include_profile_response = NULL, app_version = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SaveTicketWithHttpInfo(action_type, ticket_object_type, return_nulls, device_id, account_id, game_type, app_key, object_id, purchase_code, receipt_token, receipt_data, count, ticket_type, purchase_provider, purchase_type, return_profile_response, include_profile_response, app_version, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Save Ticket
    #'
    #' @param action_type the action being performed, values: COMPLETED, // ADD TICKETS FOR COMPLETING A MISSION, CHALLENGE, GAME, PACK, LEVEL, LEVEL OBJECT REDEEMED, // REMOVE TICKETS FOR BUYING PACKS, HINTS, AND PEN TOOLS OPTIONS, ETC USERS_PLAYED, // ADD TICKETS FOR LEVELS PLAYED BY OTHER USERS TOURNAMENT_OWNER, // ADD TICKETS FOR TOURNAMENTS BY OTHER USERS PURCHASED, // ADD TICKET VIA IN APP PURCHASING SUMATION, // SUMATION OF TICKETS EARNED FROM CHILDREN GIFTED, // TRANSFERING OF PURCHASE ITEMS TO OTHER PEOPLE REFUNDED // FOR REFUNDING TICKETS BACK TO THE USER
    #' @param ticket_object_type the type of object being purchased, values: GAME_OBJECT, GAME_LEVEL, PACK, GAME, MISSION, PROFILE, APPLICATION, TICKETS, ASSET, CUSTOM
    #' @param return_nulls (optional) whether to return nulls or not
    #' @param device_id (optional) the device id that owns the tickets
    #' @param account_id (optional) the account id that owns the tickets
    #' @param game_type (optional) This parameter is deprecated. deprecated, use the appKey
    #' @param app_key (optional) the application key
    #' @param object_id (optional) the ID of the item being purchased
    #' @param purchase_code (optional) a unique string identifier defined by the application owner or Executive
    #' @param receipt_token (optional) a unique transaction identifier from the purchase provider (i.e. the transaction ID from iTunes, Google Play, etc)
    #' @param receipt_data (optional) the receipt/transaction data for validating a purchase via iTunes/Gooogle/etc. This should be in string format.
    #' @param count (optional) the count of tickets
    #' @param ticket_type (optional) the ticket type
    #' @param purchase_provider (optional) the provider for making the purchase {ITUNES, ITUNES_SANDBOX, GOOGLE}
    #' @param purchase_type (optional) a comma separated list of purchase types { SIRQUL, // purchasing from the sirqul store using tickets IOS, // purchasing from the itunes store for iPhone, iPod, iPod Touch GOOGLE, // purchasing from the google android store AMAZON, // purchasing from the amazon android store MAC, // purchasing from the itunes store for OSX WP8, // purhasing from the windows phone 8 store FREE }
    #' @param return_profile_response (optional) returns a ProfileResponse if true, otherwise will return an AppResponse
    #' @param include_profile_response (optional) if returnProfileResponse is false, will return an AppResponse with profile data if true
    #' @param app_version (optional) the application version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    SaveTicketWithHttpInfo = function(action_type, ticket_object_type, return_nulls = NULL, device_id = NULL, account_id = NULL, game_type = NULL, app_key = NULL, object_id = NULL, purchase_code = NULL, receipt_token = NULL, receipt_data = NULL, count = NULL, ticket_type = NULL, purchase_provider = NULL, purchase_type = NULL, return_profile_response = NULL, include_profile_response = NULL, app_version = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`action_type`)) {
        stop("Missing required parameter `action_type`.")
      }

      if (missing(`ticket_object_type`)) {
        stop("Missing required parameter `ticket_object_type`.")
      }

      if (!missing(`action_type`) && is.null(`action_type`)) {
        stop("Invalid value for `action_type` when calling TicketApi$SaveTicket, `action_type` is not nullable")
      }

      if (!missing(`ticket_object_type`) && is.null(`ticket_object_type`)) {
        stop("Invalid value for `ticket_object_type` when calling TicketApi$SaveTicket, `ticket_object_type` is not nullable")
      }

      if (!missing(`return_nulls`) && is.null(`return_nulls`)) {
        stop("Invalid value for `return_nulls` when calling TicketApi$SaveTicket, `return_nulls` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling TicketApi$SaveTicket, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling TicketApi$SaveTicket, `account_id` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling TicketApi$SaveTicket, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling TicketApi$SaveTicket, `app_key` is not nullable")
      }

      if (!missing(`object_id`) && is.null(`object_id`)) {
        stop("Invalid value for `object_id` when calling TicketApi$SaveTicket, `object_id` is not nullable")
      }

      if (!missing(`purchase_code`) && is.null(`purchase_code`)) {
        stop("Invalid value for `purchase_code` when calling TicketApi$SaveTicket, `purchase_code` is not nullable")
      }

      if (!missing(`receipt_token`) && is.null(`receipt_token`)) {
        stop("Invalid value for `receipt_token` when calling TicketApi$SaveTicket, `receipt_token` is not nullable")
      }

      if (!missing(`receipt_data`) && is.null(`receipt_data`)) {
        stop("Invalid value for `receipt_data` when calling TicketApi$SaveTicket, `receipt_data` is not nullable")
      }

      if (!missing(`count`) && is.null(`count`)) {
        stop("Invalid value for `count` when calling TicketApi$SaveTicket, `count` is not nullable")
      }

      if (!missing(`ticket_type`) && is.null(`ticket_type`)) {
        stop("Invalid value for `ticket_type` when calling TicketApi$SaveTicket, `ticket_type` is not nullable")
      }

      if (!missing(`purchase_provider`) && is.null(`purchase_provider`)) {
        stop("Invalid value for `purchase_provider` when calling TicketApi$SaveTicket, `purchase_provider` is not nullable")
      }

      if (!missing(`purchase_type`) && is.null(`purchase_type`)) {
        stop("Invalid value for `purchase_type` when calling TicketApi$SaveTicket, `purchase_type` is not nullable")
      }

      if (!missing(`return_profile_response`) && is.null(`return_profile_response`)) {
        stop("Invalid value for `return_profile_response` when calling TicketApi$SaveTicket, `return_profile_response` is not nullable")
      }

      if (!missing(`include_profile_response`) && is.null(`include_profile_response`)) {
        stop("Invalid value for `include_profile_response` when calling TicketApi$SaveTicket, `include_profile_response` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling TicketApi$SaveTicket, `app_version` is not nullable")
      }

      query_params[["returnNulls"]] <- `return_nulls`

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["actionType"]] <- `action_type`

      query_params[["ticketObjectType"]] <- `ticket_object_type`

      query_params[["objectId"]] <- `object_id`

      query_params[["purchaseCode"]] <- `purchase_code`

      query_params[["receiptToken"]] <- `receipt_token`

      query_params[["receiptData"]] <- `receipt_data`

      query_params[["count"]] <- `count`

      query_params[["ticketType"]] <- `ticket_type`

      query_params[["purchaseProvider"]] <- `purchase_provider`

      query_params[["purchaseType"]] <- `purchase_type`

      query_params[["returnProfileResponse"]] <- `return_profile_response`

      query_params[["includeProfileResponse"]] <- `include_profile_response`

      query_params[["appVersion"]] <- `app_version`

      local_var_url_path <- "/ticket/save"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Save Ticket with Reciept
    #'
    #' @param action_type the action being performed { COMPLETED, // ADD TICKETS FOR COMPLETING A MISSION, CHALLENGE, GAME, PACK, LEVEL, LEVEL OBJECT REDEEMED, // REMOVE TICKETS FOR BUYING PACKS, HINTS, AND PEN TOOLS OPTIONS, ETC USERS_PLAYED, // ADD TICKETS FOR LEVELS PLAYED BY OTHER USERS TOURNAMENT_OWNER, // ADD TICKETS FOR TOURNAMENTS BY OTHER USERS PURCHASED, // ADD TICKET VIA IN APP PURCHASING SUMATION, // SUMATION OF TICKETS EARNED FROM CHILDREN GIFTED, // TRANSFERING OF PURCHASE ITEMS TO OTHER PEOPLE REFUNDED // FOR REFUNDING TICKETS BACK TO THE USER }
    #' @param ticket_object_type the type of object being purchased {GAME_OBJECT, GAME_LEVEL, PACK, GAME, MISSION, PROFILE, APPLICATION, TICKETS, ASSET, CUSTOM}
    #' @param receipt_data the receipt/transaction data for validating a purchase via iTunes/Gooogle/etc. This should be in binary format.
    #' @param return_nulls (optional) whether to return nulls or not
    #' @param device_id (optional) the device id
    #' @param account_id (optional) the account id
    #' @param game_type (optional) This parameter is deprecated. deprecated, use the appKey
    #' @param app_key (optional) the application key
    #' @param object_id (optional) the ID of the item being purchased
    #' @param purchase_code (optional) a unique string identifier defined by the application owner or Executive
    #' @param receipt_token (optional) a unique transaction identifier from the purchase provider (i.e. the transaction ID from iTunes, Google Play, etc)
    #' @param count (optional) the count of tickets
    #' @param ticket_type (optional) the ticket type
    #' @param purchase_provider (optional) the provider for making the purchase {ITUNES, ITUNES_SANDBOX, GOOGLE}
    #' @param purchase_type (optional) a comma separated list of purchase types { SIRQUL, // purchasing from the sirqul store using tickets IOS, // purchasing from the itunes store for iPhone, iPod, iPod Touch GOOGLE, // purchasing from the google android store AMAZON, // purchasing from the amazon android store MAC, // purchasing from the itunes store for OSX WP8, // purhasing from the windows phone 8 store FREE }
    #' @param return_profile_response (optional) returns a ProfileResponse if true, otherwise will return an AppResponse
    #' @param include_profile_response (optional) if returnProfileResponse is false, will return an AppResponse with profile data if true
    #' @param app_version (optional) the application version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProfileResponse
    SaveTicketViaFileUpload = function(action_type, ticket_object_type, receipt_data, return_nulls = NULL, device_id = NULL, account_id = NULL, game_type = NULL, app_key = NULL, object_id = NULL, purchase_code = NULL, receipt_token = NULL, count = NULL, ticket_type = NULL, purchase_provider = NULL, purchase_type = NULL, return_profile_response = NULL, include_profile_response = NULL, app_version = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SaveTicketViaFileUploadWithHttpInfo(action_type, ticket_object_type, receipt_data, return_nulls, device_id, account_id, game_type, app_key, object_id, purchase_code, receipt_token, count, ticket_type, purchase_provider, purchase_type, return_profile_response, include_profile_response, app_version, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Save Ticket with Reciept
    #'
    #' @param action_type the action being performed { COMPLETED, // ADD TICKETS FOR COMPLETING A MISSION, CHALLENGE, GAME, PACK, LEVEL, LEVEL OBJECT REDEEMED, // REMOVE TICKETS FOR BUYING PACKS, HINTS, AND PEN TOOLS OPTIONS, ETC USERS_PLAYED, // ADD TICKETS FOR LEVELS PLAYED BY OTHER USERS TOURNAMENT_OWNER, // ADD TICKETS FOR TOURNAMENTS BY OTHER USERS PURCHASED, // ADD TICKET VIA IN APP PURCHASING SUMATION, // SUMATION OF TICKETS EARNED FROM CHILDREN GIFTED, // TRANSFERING OF PURCHASE ITEMS TO OTHER PEOPLE REFUNDED // FOR REFUNDING TICKETS BACK TO THE USER }
    #' @param ticket_object_type the type of object being purchased {GAME_OBJECT, GAME_LEVEL, PACK, GAME, MISSION, PROFILE, APPLICATION, TICKETS, ASSET, CUSTOM}
    #' @param receipt_data the receipt/transaction data for validating a purchase via iTunes/Gooogle/etc. This should be in binary format.
    #' @param return_nulls (optional) whether to return nulls or not
    #' @param device_id (optional) the device id
    #' @param account_id (optional) the account id
    #' @param game_type (optional) This parameter is deprecated. deprecated, use the appKey
    #' @param app_key (optional) the application key
    #' @param object_id (optional) the ID of the item being purchased
    #' @param purchase_code (optional) a unique string identifier defined by the application owner or Executive
    #' @param receipt_token (optional) a unique transaction identifier from the purchase provider (i.e. the transaction ID from iTunes, Google Play, etc)
    #' @param count (optional) the count of tickets
    #' @param ticket_type (optional) the ticket type
    #' @param purchase_provider (optional) the provider for making the purchase {ITUNES, ITUNES_SANDBOX, GOOGLE}
    #' @param purchase_type (optional) a comma separated list of purchase types { SIRQUL, // purchasing from the sirqul store using tickets IOS, // purchasing from the itunes store for iPhone, iPod, iPod Touch GOOGLE, // purchasing from the google android store AMAZON, // purchasing from the amazon android store MAC, // purchasing from the itunes store for OSX WP8, // purhasing from the windows phone 8 store FREE }
    #' @param return_profile_response (optional) returns a ProfileResponse if true, otherwise will return an AppResponse
    #' @param include_profile_response (optional) if returnProfileResponse is false, will return an AppResponse with profile data if true
    #' @param app_version (optional) the application version
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProfileResponse) with additional information such as HTTP status code, headers
    SaveTicketViaFileUploadWithHttpInfo = function(action_type, ticket_object_type, receipt_data, return_nulls = NULL, device_id = NULL, account_id = NULL, game_type = NULL, app_key = NULL, object_id = NULL, purchase_code = NULL, receipt_token = NULL, count = NULL, ticket_type = NULL, purchase_provider = NULL, purchase_type = NULL, return_profile_response = NULL, include_profile_response = NULL, app_version = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`action_type`)) {
        stop("Missing required parameter `action_type`.")
      }

      if (missing(`ticket_object_type`)) {
        stop("Missing required parameter `ticket_object_type`.")
      }

      if (missing(`receipt_data`)) {
        stop("Missing required parameter `receipt_data`.")
      }

      if (!missing(`action_type`) && is.null(`action_type`)) {
        stop("Invalid value for `action_type` when calling TicketApi$SaveTicketViaFileUpload, `action_type` is not nullable")
      }

      if (!missing(`ticket_object_type`) && is.null(`ticket_object_type`)) {
        stop("Invalid value for `ticket_object_type` when calling TicketApi$SaveTicketViaFileUpload, `ticket_object_type` is not nullable")
      }

      if (!missing(`receipt_data`) && is.null(`receipt_data`)) {
        stop("Invalid value for `receipt_data` when calling TicketApi$SaveTicketViaFileUpload, `receipt_data` is not nullable")
      }

      if (!missing(`return_nulls`) && is.null(`return_nulls`)) {
        stop("Invalid value for `return_nulls` when calling TicketApi$SaveTicketViaFileUpload, `return_nulls` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling TicketApi$SaveTicketViaFileUpload, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling TicketApi$SaveTicketViaFileUpload, `account_id` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling TicketApi$SaveTicketViaFileUpload, `game_type` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling TicketApi$SaveTicketViaFileUpload, `app_key` is not nullable")
      }

      if (!missing(`object_id`) && is.null(`object_id`)) {
        stop("Invalid value for `object_id` when calling TicketApi$SaveTicketViaFileUpload, `object_id` is not nullable")
      }

      if (!missing(`purchase_code`) && is.null(`purchase_code`)) {
        stop("Invalid value for `purchase_code` when calling TicketApi$SaveTicketViaFileUpload, `purchase_code` is not nullable")
      }

      if (!missing(`receipt_token`) && is.null(`receipt_token`)) {
        stop("Invalid value for `receipt_token` when calling TicketApi$SaveTicketViaFileUpload, `receipt_token` is not nullable")
      }

      if (!missing(`count`) && is.null(`count`)) {
        stop("Invalid value for `count` when calling TicketApi$SaveTicketViaFileUpload, `count` is not nullable")
      }

      if (!missing(`ticket_type`) && is.null(`ticket_type`)) {
        stop("Invalid value for `ticket_type` when calling TicketApi$SaveTicketViaFileUpload, `ticket_type` is not nullable")
      }

      if (!missing(`purchase_provider`) && is.null(`purchase_provider`)) {
        stop("Invalid value for `purchase_provider` when calling TicketApi$SaveTicketViaFileUpload, `purchase_provider` is not nullable")
      }

      if (!missing(`purchase_type`) && is.null(`purchase_type`)) {
        stop("Invalid value for `purchase_type` when calling TicketApi$SaveTicketViaFileUpload, `purchase_type` is not nullable")
      }

      if (!missing(`return_profile_response`) && is.null(`return_profile_response`)) {
        stop("Invalid value for `return_profile_response` when calling TicketApi$SaveTicketViaFileUpload, `return_profile_response` is not nullable")
      }

      if (!missing(`include_profile_response`) && is.null(`include_profile_response`)) {
        stop("Invalid value for `include_profile_response` when calling TicketApi$SaveTicketViaFileUpload, `include_profile_response` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling TicketApi$SaveTicketViaFileUpload, `app_version` is not nullable")
      }

      query_params[["returnNulls"]] <- `return_nulls`

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["appKey"]] <- `app_key`

      query_params[["actionType"]] <- `action_type`

      query_params[["ticketObjectType"]] <- `ticket_object_type`

      query_params[["objectId"]] <- `object_id`

      query_params[["purchaseCode"]] <- `purchase_code`

      query_params[["receiptToken"]] <- `receipt_token`

      query_params[["receiptData"]] <- `receipt_data`

      query_params[["count"]] <- `count`

      query_params[["ticketType"]] <- `ticket_type`

      query_params[["purchaseProvider"]] <- `purchase_provider`

      query_params[["purchaseType"]] <- `purchase_type`

      query_params[["returnProfileResponse"]] <- `return_profile_response`

      query_params[["includeProfileResponse"]] <- `include_profile_response`

      query_params[["appVersion"]] <- `app_version`

      local_var_url_path <- "/ticket/save/fileUpload"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProfileResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Ticket Offers
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return TicketOfferResponse
    TicketOffers = function(data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$TicketOffersWithHttpInfo(data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Ticket Offers
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (TicketOfferResponse) with additional information such as HTTP status code, headers
    TicketOffersWithHttpInfo = function(data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/ticket/ticketoffers"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "TicketOfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
