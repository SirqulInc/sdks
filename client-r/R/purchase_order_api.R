#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title PurchaseOrder operations
#' @description PurchaseOrderApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateOrder  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application requesting the purchase
#' var_cart <- "cart_example" # character | ```json [   { \"orderItemType\": \"OFFER\", \"orderItemId\": 234, \"orderCustomType\": \"OfferLocation\", \"orderCustomId\": 123, \"retailerLocationId\": 1234, \"quantity\": 2 },   { \"orderItemType\": \"OFFER\", \"orderItemId\": 235, \"quantity\": 2 },   { \"orderItemType\": \"CUSTOM\", \"amount\": 10.50, \"orderCustomType\": \"ServiceFee\" },   { \"orderItemType\": \"CUSTOM\", \"amount\": 25.10, \"quantity\": 2, \"orderCustomType\": \"Hat\", \"orderCustomId\": 123 } ] ``` 
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_description <- "description_example" # character | A description of the purchase (Optional)
#' var_currency_type <- "CASH" # character | Determines the method of purchasing offer.  * VOID: used for when there is no payment involved (ie. for updating the status of a purchase order)  * CASH: use card on file  * POINTS: use account balance  * TICKETS: use tickets  * REFUND: not allowed for a create, requires a paymentTransactionId.  * CREDIT: add to the account balance  * RELOAD: charge a credit card then add to the account balance   (Optional)
#' var_payment_method_id <- 56 # integer | Use a specific payment method (CASH), if not provided use default (Optional)
#' var_external_order_id <- "external_order_id_example" # character | Store identifier from external system (Optional)
#' var_external_payment_id <- "external_payment_id_example" # character | Store identifier from external system (Optional)
#' var_remote_ref_type <- "remote_ref_type_example" # character | Remote Reference type (Optional)
#' var_external_date <- 56 # integer | External Date (Optional)
#' var_promo_code <- "promo_code_example" # character | The Promo Code (Optional)
#'
#' #Create Order
#' api_instance <- PurchaseOrderApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateOrder(var_app_key, var_cart, device_id = var_device_id, account_id = var_account_id, description = var_description, currency_type = var_currency_type, payment_method_id = var_payment_method_id, external_order_id = var_external_order_id, external_payment_id = var_external_payment_id, remote_ref_type = var_remote_ref_type, external_date = var_external_date, promo_code = var_promo_codedata_file = "result.txt")
#' result <- api_instance$CreateOrder(var_app_key, var_cart, device_id = var_device_id, account_id = var_account_id, description = var_description, currency_type = var_currency_type, payment_method_id = var_payment_method_id, external_order_id = var_external_order_id, external_payment_id = var_external_payment_id, remote_ref_type = var_remote_ref_type, external_date = var_external_date, promo_code = var_promo_code)
#' dput(result)
#'
#'
#' ####################  DeleteOrder  ####################
#'
#' library(openapi)
#' var_order_id <- 56 # integer | Order Id
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#'
#' #Delete Order
#' api_instance <- PurchaseOrderApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteOrder(var_order_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteOrder(var_order_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  GetOrder  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_order_id <- 56 # integer | The order id to get details of, either orderId or externalOrderId must be provided (Optional)
#' var_external_order_id <- "external_order_id_example" # character | The external order id to get details of, either orderId or externalOrderId must be provided (Optional)
#'
#' #Get Order
#' api_instance <- PurchaseOrderApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOrder(device_id = var_device_id, account_id = var_account_id, order_id = var_order_id, external_order_id = var_external_order_iddata_file = "result.txt")
#' result <- api_instance$GetOrder(device_id = var_device_id, account_id = var_account_id, order_id = var_order_id, external_order_id = var_external_order_id)
#' dput(result)
#'
#'
#' ####################  PreviewOrder  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application requesting the purchase
#' var_cart <- "cart_example" # character | A JSON list of items to purchase
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_description <- "description_example" # character | A description of the purchase (Optional)
#' var_currency_type <- "CASH" # character | Determines the method of purchasing offer. ```json {   \"VOID\": \"used for when there is no payment involved (ie. for updating the status of a purchase order)\",   \"CASH\": \"use card on file\",   \"POINTS\": \"use account balance\",   \"TICKETS\": \"use tickets\",   \"REFUND\": \"not allowed for a create, requires a paymentTransactionId\",   \"CREDIT\": \"add to the account balance\",   \"RELOAD\": \"charge a credit card then add to the account balance\" } ```  (Optional)
#' var_payment_method_id <- 56 # integer | Use a specific payment method (CASH), if not provided use default (Optional)
#' var_external_order_id <- "external_order_id_example" # character | Store identifier from external system (Optional)
#' var_external_payment_id <- "external_payment_id_example" # character | Store identifier from external system (Optional)
#' var_remote_ref_type <- "remote_ref_type_example" # character | Remote Reference type (Optional)
#' var_external_date <- 56 # integer | External Date (Optional)
#' var_promo_code <- "promo_code_example" # character | The Promo Code (Optional)
#'
#' #Preview Order
#' api_instance <- PurchaseOrderApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PreviewOrder(var_app_key, var_cart, device_id = var_device_id, account_id = var_account_id, description = var_description, currency_type = var_currency_type, payment_method_id = var_payment_method_id, external_order_id = var_external_order_id, external_payment_id = var_external_payment_id, remote_ref_type = var_remote_ref_type, external_date = var_external_date, promo_code = var_promo_codedata_file = "result.txt")
#' result <- api_instance$PreviewOrder(var_app_key, var_cart, device_id = var_device_id, account_id = var_account_id, description = var_description, currency_type = var_currency_type, payment_method_id = var_payment_method_id, external_order_id = var_external_order_id, external_payment_id = var_external_payment_id, remote_ref_type = var_remote_ref_type, external_date = var_external_date, promo_code = var_promo_code)
#' dput(result)
#'
#'
#' ####################  SearchOrders  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application requesting the purchase
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_start <- 0 # integer | The start index for pagination (Optional)
#' var_limit <- 20 # integer | The limit for pagination (Optional)
#' var_descending <- TRUE # character | Determines whether to return the resulting list in descending or ascending order (Optional)
#' var_active_only <- FALSE # character | Only return active orders (Optional)
#' var_ignore_customer_filter <- FALSE # character | Determines whether to ignore the customer filter (requires an Admin/Exec account) (Optional)
#' var_order_item_types <- "order_item_types_example" # character | Filter results by orderItemTypes (Optional)
#' var_order_item_ids <- "order_item_ids_example" # character | Filter results by orderItemIds (Optional)
#' var_order_custom_types <- "order_custom_types_example" # character | Filter results by orderCustomTypes (Optional)
#' var_order_custom_ids <- "order_custom_ids_example" # character | Filter results by orderCustomIds (Optional)
#' var_sort_field <- "ID" # character | Determines what to sort the results by (Optional)
#' var_offer_types <- "offer_types_example" # character | Filter results by offer type (Optional)
#' var_special_offer_types <- "special_offer_types_example" # character | Filter results by special offer type (Optional)
#' var_category_ids <- "category_ids_example" # character | Filter results by category Ids (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Filter results by filter Ids (Optional)
#' var_offer_audience_ids <- "offer_audience_ids_example" # character | Filter results by offer audience Ids (Optional)
#' var_transaction_audience_ids <- "transaction_audience_ids_example" # character | Filter results by transaction audience Ids (Optional)
#' var_offer_ids <- "offer_ids_example" # character | Filter results by offer Ids (Optional)
#' var_offer_location_ids <- "offer_location_ids_example" # character | Filter results by offer location Ids (Optional)
#' var_retailer_ids <- "retailer_ids_example" # character | Filter results by retailer Ids (Optional)
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Filter results by retailer location Ids (Optional)
#' var_statuses <- "statuses_example" # character | Filter results by offer transaction status (0: non-redeemable, 1: redeemable, 2: redeemed, 3: user deleted, 4: unclaimed/rewarded, 5: membership active, 6: membership cancelled) (Optional)
#' var_keyword <- "keyword_example" # character | The keyword to search for (Optional)
#' var_redeemable_start_date <- 56 # integer | Filter results by the offer redeemable date (Optional)
#' var_redeemable_end_date <- 56 # integer | Filter results by the offer redeemable date (Optional)
#' var_started_since <- 56 # integer | Filter results by the offer start date (Optional)
#' var_started_before <- 56 # integer | Filter results by the offer start date (Optional)
#' var_ended_since <- 56 # integer | Filter results by the offer end date (Optional)
#' var_ended_before <- 56 # integer | Filter results by the offer end date (Optional)
#'
#' #Search Orders
#' api_instance <- PurchaseOrderApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchOrders(var_app_key, device_id = var_device_id, account_id = var_account_id, start = var_start, limit = var_limit, descending = var_descending, active_only = var_active_only, ignore_customer_filter = var_ignore_customer_filter, order_item_types = var_order_item_types, order_item_ids = var_order_item_ids, order_custom_types = var_order_custom_types, order_custom_ids = var_order_custom_ids, sort_field = var_sort_field, offer_types = var_offer_types, special_offer_types = var_special_offer_types, category_ids = var_category_ids, filter_ids = var_filter_ids, offer_audience_ids = var_offer_audience_ids, transaction_audience_ids = var_transaction_audience_ids, offer_ids = var_offer_ids, offer_location_ids = var_offer_location_ids, retailer_ids = var_retailer_ids, retailer_location_ids = var_retailer_location_ids, statuses = var_statuses, keyword = var_keyword, redeemable_start_date = var_redeemable_start_date, redeemable_end_date = var_redeemable_end_date, started_since = var_started_since, started_before = var_started_before, ended_since = var_ended_since, ended_before = var_ended_beforedata_file = "result.txt")
#' result <- api_instance$SearchOrders(var_app_key, device_id = var_device_id, account_id = var_account_id, start = var_start, limit = var_limit, descending = var_descending, active_only = var_active_only, ignore_customer_filter = var_ignore_customer_filter, order_item_types = var_order_item_types, order_item_ids = var_order_item_ids, order_custom_types = var_order_custom_types, order_custom_ids = var_order_custom_ids, sort_field = var_sort_field, offer_types = var_offer_types, special_offer_types = var_special_offer_types, category_ids = var_category_ids, filter_ids = var_filter_ids, offer_audience_ids = var_offer_audience_ids, transaction_audience_ids = var_transaction_audience_ids, offer_ids = var_offer_ids, offer_location_ids = var_offer_location_ids, retailer_ids = var_retailer_ids, retailer_location_ids = var_retailer_location_ids, statuses = var_statuses, keyword = var_keyword, redeemable_start_date = var_redeemable_start_date, redeemable_end_date = var_redeemable_end_date, started_since = var_started_since, started_before = var_started_before, ended_since = var_ended_since, ended_before = var_ended_before)
#' dput(result)
#'
#'
#' ####################  UpdateOrder  ####################
#'
#' library(openapi)
#' var_order_id <- 56 # integer | The order to add the purchase to, leave null for new order.
#' var_app_key <- "app_key_example" # character | The application requesting the purchase
#' var_cart <- "cart_example" # character | ```json [   { \"orderItemType\": \"OFFER\", \"orderItemId\": 234, \"orderCustomType\": \"OfferLocation\", \"orderCustomId\": 123, \"retailerLocationId\": 1234, \"quantity\": 2 },   { \"orderItemType\": \"OFFER\", \"orderItemId\": 235, \"quantity\": 2 },   { \"orderItemType\": \"CUSTOM\", \"amount\": 10.50, \"orderCustomType\": \"ServiceFee\" },   { \"orderItemType\": \"CUSTOM\", \"amount\": 25.10, \"quantity\": 2, \"orderCustomType\": \"Hat\", \"orderCustomId\": 123 } ] ``` 
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_payment_transaction_id <- 56 # integer | The payment transaction to apply the refund to (Optional)
#' var_description <- "description_example" # character | A description of the purchase (Optional)
#' var_currency_type <- "CASH" # character | Determines the method of purchasing offer.  * VOID: used for when there is no payment involved (ie. for updating the status of a purchase order)  * CASH: use card on file  * POINTS: use account balance  * TICKETS: use tickets  * REFUND: not allowed for a create, requires a paymentTransactionId.  * CREDIT: add to the account balance  * RELOAD: charge a credit card then add to the account balance   (Optional)
#' var_payment_method_id <- 56 # integer | Use a specific payment method (CASH), if not provided use default (Optional)
#' var_external_payment_id <- "external_payment_id_example" # character | Store identifier from external system (Optional)
#' var_external_date <- 56 # integer | External Date (Optional)
#'
#' #Update Order
#' api_instance <- PurchaseOrderApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateOrder(var_order_id, var_app_key, var_cart, device_id = var_device_id, account_id = var_account_id, payment_transaction_id = var_payment_transaction_id, description = var_description, currency_type = var_currency_type, payment_method_id = var_payment_method_id, external_payment_id = var_external_payment_id, external_date = var_external_datedata_file = "result.txt")
#' result <- api_instance$UpdateOrder(var_order_id, var_app_key, var_cart, device_id = var_device_id, account_id = var_account_id, payment_transaction_id = var_payment_transaction_id, description = var_description, currency_type = var_currency_type, payment_method_id = var_payment_method_id, external_payment_id = var_external_payment_id, external_date = var_external_date)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
PurchaseOrderApi <- R6::R6Class(
  "PurchaseOrderApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new PurchaseOrderApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Order
    #'
    #' @param app_key The application requesting the purchase
    #' @param cart ```json [   { \"orderItemType\": \"OFFER\", \"orderItemId\": 234, \"orderCustomType\": \"OfferLocation\", \"orderCustomId\": 123, \"retailerLocationId\": 1234, \"quantity\": 2 },   { \"orderItemType\": \"OFFER\", \"orderItemId\": 235, \"quantity\": 2 },   { \"orderItemType\": \"CUSTOM\", \"amount\": 10.50, \"orderCustomType\": \"ServiceFee\" },   { \"orderItemType\": \"CUSTOM\", \"amount\": 25.10, \"quantity\": 2, \"orderCustomType\": \"Hat\", \"orderCustomId\": 123 } ] ``` 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param description (optional) A description of the purchase
    #' @param currency_type (optional) Determines the method of purchasing offer.  * VOID: used for when there is no payment involved (ie. for updating the status of a purchase order)  * CASH: use card on file  * POINTS: use account balance  * TICKETS: use tickets  * REFUND: not allowed for a create, requires a paymentTransactionId.  * CREDIT: add to the account balance  * RELOAD: charge a credit card then add to the account balance   (default value: "CASH")
    #' @param payment_method_id (optional) Use a specific payment method (CASH), if not provided use default
    #' @param external_order_id (optional) Store identifier from external system
    #' @param external_payment_id (optional) Store identifier from external system
    #' @param remote_ref_type (optional) Remote Reference type
    #' @param external_date (optional) External Date
    #' @param promo_code (optional) The Promo Code
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OrderResponse
    CreateOrder = function(app_key, cart, device_id = NULL, account_id = NULL, description = NULL, currency_type = "CASH", payment_method_id = NULL, external_order_id = NULL, external_payment_id = NULL, remote_ref_type = NULL, external_date = NULL, promo_code = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateOrderWithHttpInfo(app_key, cart, device_id, account_id, description, currency_type, payment_method_id, external_order_id, external_payment_id, remote_ref_type, external_date, promo_code, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Order
    #'
    #' @param app_key The application requesting the purchase
    #' @param cart ```json [   { \"orderItemType\": \"OFFER\", \"orderItemId\": 234, \"orderCustomType\": \"OfferLocation\", \"orderCustomId\": 123, \"retailerLocationId\": 1234, \"quantity\": 2 },   { \"orderItemType\": \"OFFER\", \"orderItemId\": 235, \"quantity\": 2 },   { \"orderItemType\": \"CUSTOM\", \"amount\": 10.50, \"orderCustomType\": \"ServiceFee\" },   { \"orderItemType\": \"CUSTOM\", \"amount\": 25.10, \"quantity\": 2, \"orderCustomType\": \"Hat\", \"orderCustomId\": 123 } ] ``` 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param description (optional) A description of the purchase
    #' @param currency_type (optional) Determines the method of purchasing offer.  * VOID: used for when there is no payment involved (ie. for updating the status of a purchase order)  * CASH: use card on file  * POINTS: use account balance  * TICKETS: use tickets  * REFUND: not allowed for a create, requires a paymentTransactionId.  * CREDIT: add to the account balance  * RELOAD: charge a credit card then add to the account balance   (default value: "CASH")
    #' @param payment_method_id (optional) Use a specific payment method (CASH), if not provided use default
    #' @param external_order_id (optional) Store identifier from external system
    #' @param external_payment_id (optional) Store identifier from external system
    #' @param remote_ref_type (optional) Remote Reference type
    #' @param external_date (optional) External Date
    #' @param promo_code (optional) The Promo Code
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OrderResponse) with additional information such as HTTP status code, headers
    CreateOrderWithHttpInfo = function(app_key, cart, device_id = NULL, account_id = NULL, description = NULL, currency_type = "CASH", payment_method_id = NULL, external_order_id = NULL, external_payment_id = NULL, remote_ref_type = NULL, external_date = NULL, promo_code = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`cart`)) {
        stop("Missing required parameter `cart`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling PurchaseOrderApi$CreateOrder, `app_key` is not nullable")
      }

      if (!missing(`cart`) && is.null(`cart`)) {
        stop("Invalid value for `cart` when calling PurchaseOrderApi$CreateOrder, `cart` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling PurchaseOrderApi$CreateOrder, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PurchaseOrderApi$CreateOrder, `account_id` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling PurchaseOrderApi$CreateOrder, `description` is not nullable")
      }

      if (!missing(`currency_type`) && is.null(`currency_type`)) {
        stop("Invalid value for `currency_type` when calling PurchaseOrderApi$CreateOrder, `currency_type` is not nullable")
      }

      if (!missing(`payment_method_id`) && is.null(`payment_method_id`)) {
        stop("Invalid value for `payment_method_id` when calling PurchaseOrderApi$CreateOrder, `payment_method_id` is not nullable")
      }

      if (!missing(`external_order_id`) && is.null(`external_order_id`)) {
        stop("Invalid value for `external_order_id` when calling PurchaseOrderApi$CreateOrder, `external_order_id` is not nullable")
      }

      if (!missing(`external_payment_id`) && is.null(`external_payment_id`)) {
        stop("Invalid value for `external_payment_id` when calling PurchaseOrderApi$CreateOrder, `external_payment_id` is not nullable")
      }

      if (!missing(`remote_ref_type`) && is.null(`remote_ref_type`)) {
        stop("Invalid value for `remote_ref_type` when calling PurchaseOrderApi$CreateOrder, `remote_ref_type` is not nullable")
      }

      if (!missing(`external_date`) && is.null(`external_date`)) {
        stop("Invalid value for `external_date` when calling PurchaseOrderApi$CreateOrder, `external_date` is not nullable")
      }

      if (!missing(`promo_code`) && is.null(`promo_code`)) {
        stop("Invalid value for `promo_code` when calling PurchaseOrderApi$CreateOrder, `promo_code` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["description"]] <- `description`

      if (!is.null(`currency_type`) && !(`currency_type` %in% c("VOID", "CASH", "POINTS", "TICKETS", "REFUND", "CREDIT", "RELOAD"))) {
        stop("Invalid value for currency_type when calling PurchaseOrderApi$CreateOrder. Must be [VOID, CASH, POINTS, TICKETS, REFUND, CREDIT, RELOAD].")
      }
      query_params[["currencyType"]] <- `currency_type`

      query_params[["cart"]] <- `cart`

      query_params[["paymentMethodId"]] <- `payment_method_id`

      query_params[["externalOrderId"]] <- `external_order_id`

      query_params[["externalPaymentId"]] <- `external_payment_id`

      query_params[["remoteRefType"]] <- `remote_ref_type`

      query_params[["externalDate"]] <- `external_date`

      query_params[["promoCode"]] <- `promo_code`

      local_var_url_path <- "/order/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OrderResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Order
    #'
    #' @param order_id Order Id
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteOrder = function(order_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteOrderWithHttpInfo(order_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Order
    #'
    #' @param order_id Order Id
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteOrderWithHttpInfo = function(order_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`order_id`)) {
        stop("Missing required parameter `order_id`.")
      }

      if (!missing(`order_id`) && is.null(`order_id`)) {
        stop("Invalid value for `order_id` when calling PurchaseOrderApi$DeleteOrder, `order_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling PurchaseOrderApi$DeleteOrder, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PurchaseOrderApi$DeleteOrder, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["orderId"]] <- `order_id`

      local_var_url_path <- "/order/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Order
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param order_id (optional) The order id to get details of, either orderId or externalOrderId must be provided
    #' @param external_order_id (optional) The external order id to get details of, either orderId or externalOrderId must be provided
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OrderResponse
    GetOrder = function(device_id = NULL, account_id = NULL, order_id = NULL, external_order_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetOrderWithHttpInfo(device_id, account_id, order_id, external_order_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Order
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param order_id (optional) The order id to get details of, either orderId or externalOrderId must be provided
    #' @param external_order_id (optional) The external order id to get details of, either orderId or externalOrderId must be provided
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OrderResponse) with additional information such as HTTP status code, headers
    GetOrderWithHttpInfo = function(device_id = NULL, account_id = NULL, order_id = NULL, external_order_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling PurchaseOrderApi$GetOrder, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PurchaseOrderApi$GetOrder, `account_id` is not nullable")
      }

      if (!missing(`order_id`) && is.null(`order_id`)) {
        stop("Invalid value for `order_id` when calling PurchaseOrderApi$GetOrder, `order_id` is not nullable")
      }

      if (!missing(`external_order_id`) && is.null(`external_order_id`)) {
        stop("Invalid value for `external_order_id` when calling PurchaseOrderApi$GetOrder, `external_order_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["orderId"]] <- `order_id`

      query_params[["externalOrderId"]] <- `external_order_id`

      local_var_url_path <- "/order/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OrderResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Preview Order
    #'
    #' @param app_key The application requesting the purchase
    #' @param cart A JSON list of items to purchase
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param description (optional) A description of the purchase
    #' @param currency_type (optional) Determines the method of purchasing offer. ```json {   \"VOID\": \"used for when there is no payment involved (ie. for updating the status of a purchase order)\",   \"CASH\": \"use card on file\",   \"POINTS\": \"use account balance\",   \"TICKETS\": \"use tickets\",   \"REFUND\": \"not allowed for a create, requires a paymentTransactionId\",   \"CREDIT\": \"add to the account balance\",   \"RELOAD\": \"charge a credit card then add to the account balance\" } ```  (default value: "CASH")
    #' @param payment_method_id (optional) Use a specific payment method (CASH), if not provided use default
    #' @param external_order_id (optional) Store identifier from external system
    #' @param external_payment_id (optional) Store identifier from external system
    #' @param remote_ref_type (optional) Remote Reference type
    #' @param external_date (optional) External Date
    #' @param promo_code (optional) The Promo Code
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OrderResponse
    PreviewOrder = function(app_key, cart, device_id = NULL, account_id = NULL, description = NULL, currency_type = "CASH", payment_method_id = NULL, external_order_id = NULL, external_payment_id = NULL, remote_ref_type = NULL, external_date = NULL, promo_code = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$PreviewOrderWithHttpInfo(app_key, cart, device_id, account_id, description, currency_type, payment_method_id, external_order_id, external_payment_id, remote_ref_type, external_date, promo_code, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Preview Order
    #'
    #' @param app_key The application requesting the purchase
    #' @param cart A JSON list of items to purchase
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param description (optional) A description of the purchase
    #' @param currency_type (optional) Determines the method of purchasing offer. ```json {   \"VOID\": \"used for when there is no payment involved (ie. for updating the status of a purchase order)\",   \"CASH\": \"use card on file\",   \"POINTS\": \"use account balance\",   \"TICKETS\": \"use tickets\",   \"REFUND\": \"not allowed for a create, requires a paymentTransactionId\",   \"CREDIT\": \"add to the account balance\",   \"RELOAD\": \"charge a credit card then add to the account balance\" } ```  (default value: "CASH")
    #' @param payment_method_id (optional) Use a specific payment method (CASH), if not provided use default
    #' @param external_order_id (optional) Store identifier from external system
    #' @param external_payment_id (optional) Store identifier from external system
    #' @param remote_ref_type (optional) Remote Reference type
    #' @param external_date (optional) External Date
    #' @param promo_code (optional) The Promo Code
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OrderResponse) with additional information such as HTTP status code, headers
    PreviewOrderWithHttpInfo = function(app_key, cart, device_id = NULL, account_id = NULL, description = NULL, currency_type = "CASH", payment_method_id = NULL, external_order_id = NULL, external_payment_id = NULL, remote_ref_type = NULL, external_date = NULL, promo_code = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`cart`)) {
        stop("Missing required parameter `cart`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling PurchaseOrderApi$PreviewOrder, `app_key` is not nullable")
      }

      if (!missing(`cart`) && is.null(`cart`)) {
        stop("Invalid value for `cart` when calling PurchaseOrderApi$PreviewOrder, `cart` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling PurchaseOrderApi$PreviewOrder, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PurchaseOrderApi$PreviewOrder, `account_id` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling PurchaseOrderApi$PreviewOrder, `description` is not nullable")
      }

      if (!missing(`currency_type`) && is.null(`currency_type`)) {
        stop("Invalid value for `currency_type` when calling PurchaseOrderApi$PreviewOrder, `currency_type` is not nullable")
      }

      if (!missing(`payment_method_id`) && is.null(`payment_method_id`)) {
        stop("Invalid value for `payment_method_id` when calling PurchaseOrderApi$PreviewOrder, `payment_method_id` is not nullable")
      }

      if (!missing(`external_order_id`) && is.null(`external_order_id`)) {
        stop("Invalid value for `external_order_id` when calling PurchaseOrderApi$PreviewOrder, `external_order_id` is not nullable")
      }

      if (!missing(`external_payment_id`) && is.null(`external_payment_id`)) {
        stop("Invalid value for `external_payment_id` when calling PurchaseOrderApi$PreviewOrder, `external_payment_id` is not nullable")
      }

      if (!missing(`remote_ref_type`) && is.null(`remote_ref_type`)) {
        stop("Invalid value for `remote_ref_type` when calling PurchaseOrderApi$PreviewOrder, `remote_ref_type` is not nullable")
      }

      if (!missing(`external_date`) && is.null(`external_date`)) {
        stop("Invalid value for `external_date` when calling PurchaseOrderApi$PreviewOrder, `external_date` is not nullable")
      }

      if (!missing(`promo_code`) && is.null(`promo_code`)) {
        stop("Invalid value for `promo_code` when calling PurchaseOrderApi$PreviewOrder, `promo_code` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["description"]] <- `description`

      if (!is.null(`currency_type`) && !(`currency_type` %in% c("VOID", "CASH", "POINTS", "TICKETS", "REFUND", "CREDIT", "RELOAD"))) {
        stop("Invalid value for currency_type when calling PurchaseOrderApi$PreviewOrder. Must be [VOID, CASH, POINTS, TICKETS, REFUND, CREDIT, RELOAD].")
      }
      query_params[["currencyType"]] <- `currency_type`

      query_params[["cart"]] <- `cart`

      query_params[["paymentMethodId"]] <- `payment_method_id`

      query_params[["externalOrderId"]] <- `external_order_id`

      query_params[["externalPaymentId"]] <- `external_payment_id`

      query_params[["remoteRefType"]] <- `remote_ref_type`

      query_params[["externalDate"]] <- `external_date`

      query_params[["promoCode"]] <- `promo_code`

      local_var_url_path <- "/order/preview"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OrderResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Orders
    #'
    #' @param app_key The application requesting the purchase
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param start (optional) The start index for pagination (default value: 0)
    #' @param limit (optional) The limit for pagination (default value: 20)
    #' @param descending (optional) Determines whether to return the resulting list in descending or ascending order (default value: TRUE)
    #' @param active_only (optional) Only return active orders (default value: FALSE)
    #' @param ignore_customer_filter (optional) Determines whether to ignore the customer filter (requires an Admin/Exec account) (default value: FALSE)
    #' @param order_item_types (optional) Filter results by orderItemTypes
    #' @param order_item_ids (optional) Filter results by orderItemIds
    #' @param order_custom_types (optional) Filter results by orderCustomTypes
    #' @param order_custom_ids (optional) Filter results by orderCustomIds
    #' @param sort_field (optional) Determines what to sort the results by (default value: "ID")
    #' @param offer_types (optional) Filter results by offer type
    #' @param special_offer_types (optional) Filter results by special offer type
    #' @param category_ids (optional) Filter results by category Ids
    #' @param filter_ids (optional) Filter results by filter Ids
    #' @param offer_audience_ids (optional) Filter results by offer audience Ids
    #' @param transaction_audience_ids (optional) Filter results by transaction audience Ids
    #' @param offer_ids (optional) Filter results by offer Ids
    #' @param offer_location_ids (optional) Filter results by offer location Ids
    #' @param retailer_ids (optional) Filter results by retailer Ids
    #' @param retailer_location_ids (optional) Filter results by retailer location Ids
    #' @param statuses (optional) Filter results by offer transaction status (0: non-redeemable, 1: redeemable, 2: redeemed, 3: user deleted, 4: unclaimed/rewarded, 5: membership active, 6: membership cancelled)
    #' @param keyword (optional) The keyword to search for
    #' @param redeemable_start_date (optional) Filter results by the offer redeemable date
    #' @param redeemable_end_date (optional) Filter results by the offer redeemable date
    #' @param started_since (optional) Filter results by the offer start date
    #' @param started_before (optional) Filter results by the offer start date
    #' @param ended_since (optional) Filter results by the offer end date
    #' @param ended_before (optional) Filter results by the offer end date
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[OrderResponse]
    SearchOrders = function(app_key, device_id = NULL, account_id = NULL, start = 0, limit = 20, descending = TRUE, active_only = FALSE, ignore_customer_filter = FALSE, order_item_types = NULL, order_item_ids = NULL, order_custom_types = NULL, order_custom_ids = NULL, sort_field = "ID", offer_types = NULL, special_offer_types = NULL, category_ids = NULL, filter_ids = NULL, offer_audience_ids = NULL, transaction_audience_ids = NULL, offer_ids = NULL, offer_location_ids = NULL, retailer_ids = NULL, retailer_location_ids = NULL, statuses = NULL, keyword = NULL, redeemable_start_date = NULL, redeemable_end_date = NULL, started_since = NULL, started_before = NULL, ended_since = NULL, ended_before = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchOrdersWithHttpInfo(app_key, device_id, account_id, start, limit, descending, active_only, ignore_customer_filter, order_item_types, order_item_ids, order_custom_types, order_custom_ids, sort_field, offer_types, special_offer_types, category_ids, filter_ids, offer_audience_ids, transaction_audience_ids, offer_ids, offer_location_ids, retailer_ids, retailer_location_ids, statuses, keyword, redeemable_start_date, redeemable_end_date, started_since, started_before, ended_since, ended_before, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Orders
    #'
    #' @param app_key The application requesting the purchase
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param start (optional) The start index for pagination (default value: 0)
    #' @param limit (optional) The limit for pagination (default value: 20)
    #' @param descending (optional) Determines whether to return the resulting list in descending or ascending order (default value: TRUE)
    #' @param active_only (optional) Only return active orders (default value: FALSE)
    #' @param ignore_customer_filter (optional) Determines whether to ignore the customer filter (requires an Admin/Exec account) (default value: FALSE)
    #' @param order_item_types (optional) Filter results by orderItemTypes
    #' @param order_item_ids (optional) Filter results by orderItemIds
    #' @param order_custom_types (optional) Filter results by orderCustomTypes
    #' @param order_custom_ids (optional) Filter results by orderCustomIds
    #' @param sort_field (optional) Determines what to sort the results by (default value: "ID")
    #' @param offer_types (optional) Filter results by offer type
    #' @param special_offer_types (optional) Filter results by special offer type
    #' @param category_ids (optional) Filter results by category Ids
    #' @param filter_ids (optional) Filter results by filter Ids
    #' @param offer_audience_ids (optional) Filter results by offer audience Ids
    #' @param transaction_audience_ids (optional) Filter results by transaction audience Ids
    #' @param offer_ids (optional) Filter results by offer Ids
    #' @param offer_location_ids (optional) Filter results by offer location Ids
    #' @param retailer_ids (optional) Filter results by retailer Ids
    #' @param retailer_location_ids (optional) Filter results by retailer location Ids
    #' @param statuses (optional) Filter results by offer transaction status (0: non-redeemable, 1: redeemable, 2: redeemed, 3: user deleted, 4: unclaimed/rewarded, 5: membership active, 6: membership cancelled)
    #' @param keyword (optional) The keyword to search for
    #' @param redeemable_start_date (optional) Filter results by the offer redeemable date
    #' @param redeemable_end_date (optional) Filter results by the offer redeemable date
    #' @param started_since (optional) Filter results by the offer start date
    #' @param started_before (optional) Filter results by the offer start date
    #' @param ended_since (optional) Filter results by the offer end date
    #' @param ended_before (optional) Filter results by the offer end date
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[OrderResponse]) with additional information such as HTTP status code, headers
    SearchOrdersWithHttpInfo = function(app_key, device_id = NULL, account_id = NULL, start = 0, limit = 20, descending = TRUE, active_only = FALSE, ignore_customer_filter = FALSE, order_item_types = NULL, order_item_ids = NULL, order_custom_types = NULL, order_custom_ids = NULL, sort_field = "ID", offer_types = NULL, special_offer_types = NULL, category_ids = NULL, filter_ids = NULL, offer_audience_ids = NULL, transaction_audience_ids = NULL, offer_ids = NULL, offer_location_ids = NULL, retailer_ids = NULL, retailer_location_ids = NULL, statuses = NULL, keyword = NULL, redeemable_start_date = NULL, redeemable_end_date = NULL, started_since = NULL, started_before = NULL, ended_since = NULL, ended_before = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling PurchaseOrderApi$SearchOrders, `app_key` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling PurchaseOrderApi$SearchOrders, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PurchaseOrderApi$SearchOrders, `account_id` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling PurchaseOrderApi$SearchOrders, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling PurchaseOrderApi$SearchOrders, `limit` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling PurchaseOrderApi$SearchOrders, `descending` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling PurchaseOrderApi$SearchOrders, `active_only` is not nullable")
      }

      if (!missing(`ignore_customer_filter`) && is.null(`ignore_customer_filter`)) {
        stop("Invalid value for `ignore_customer_filter` when calling PurchaseOrderApi$SearchOrders, `ignore_customer_filter` is not nullable")
      }

      if (!missing(`order_item_types`) && is.null(`order_item_types`)) {
        stop("Invalid value for `order_item_types` when calling PurchaseOrderApi$SearchOrders, `order_item_types` is not nullable")
      }

      if (!missing(`order_item_ids`) && is.null(`order_item_ids`)) {
        stop("Invalid value for `order_item_ids` when calling PurchaseOrderApi$SearchOrders, `order_item_ids` is not nullable")
      }

      if (!missing(`order_custom_types`) && is.null(`order_custom_types`)) {
        stop("Invalid value for `order_custom_types` when calling PurchaseOrderApi$SearchOrders, `order_custom_types` is not nullable")
      }

      if (!missing(`order_custom_ids`) && is.null(`order_custom_ids`)) {
        stop("Invalid value for `order_custom_ids` when calling PurchaseOrderApi$SearchOrders, `order_custom_ids` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling PurchaseOrderApi$SearchOrders, `sort_field` is not nullable")
      }

      if (!missing(`offer_types`) && is.null(`offer_types`)) {
        stop("Invalid value for `offer_types` when calling PurchaseOrderApi$SearchOrders, `offer_types` is not nullable")
      }

      if (!missing(`special_offer_types`) && is.null(`special_offer_types`)) {
        stop("Invalid value for `special_offer_types` when calling PurchaseOrderApi$SearchOrders, `special_offer_types` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling PurchaseOrderApi$SearchOrders, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling PurchaseOrderApi$SearchOrders, `filter_ids` is not nullable")
      }

      if (!missing(`offer_audience_ids`) && is.null(`offer_audience_ids`)) {
        stop("Invalid value for `offer_audience_ids` when calling PurchaseOrderApi$SearchOrders, `offer_audience_ids` is not nullable")
      }

      if (!missing(`transaction_audience_ids`) && is.null(`transaction_audience_ids`)) {
        stop("Invalid value for `transaction_audience_ids` when calling PurchaseOrderApi$SearchOrders, `transaction_audience_ids` is not nullable")
      }

      if (!missing(`offer_ids`) && is.null(`offer_ids`)) {
        stop("Invalid value for `offer_ids` when calling PurchaseOrderApi$SearchOrders, `offer_ids` is not nullable")
      }

      if (!missing(`offer_location_ids`) && is.null(`offer_location_ids`)) {
        stop("Invalid value for `offer_location_ids` when calling PurchaseOrderApi$SearchOrders, `offer_location_ids` is not nullable")
      }

      if (!missing(`retailer_ids`) && is.null(`retailer_ids`)) {
        stop("Invalid value for `retailer_ids` when calling PurchaseOrderApi$SearchOrders, `retailer_ids` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling PurchaseOrderApi$SearchOrders, `retailer_location_ids` is not nullable")
      }

      if (!missing(`statuses`) && is.null(`statuses`)) {
        stop("Invalid value for `statuses` when calling PurchaseOrderApi$SearchOrders, `statuses` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling PurchaseOrderApi$SearchOrders, `keyword` is not nullable")
      }

      if (!missing(`redeemable_start_date`) && is.null(`redeemable_start_date`)) {
        stop("Invalid value for `redeemable_start_date` when calling PurchaseOrderApi$SearchOrders, `redeemable_start_date` is not nullable")
      }

      if (!missing(`redeemable_end_date`) && is.null(`redeemable_end_date`)) {
        stop("Invalid value for `redeemable_end_date` when calling PurchaseOrderApi$SearchOrders, `redeemable_end_date` is not nullable")
      }

      if (!missing(`started_since`) && is.null(`started_since`)) {
        stop("Invalid value for `started_since` when calling PurchaseOrderApi$SearchOrders, `started_since` is not nullable")
      }

      if (!missing(`started_before`) && is.null(`started_before`)) {
        stop("Invalid value for `started_before` when calling PurchaseOrderApi$SearchOrders, `started_before` is not nullable")
      }

      if (!missing(`ended_since`) && is.null(`ended_since`)) {
        stop("Invalid value for `ended_since` when calling PurchaseOrderApi$SearchOrders, `ended_since` is not nullable")
      }

      if (!missing(`ended_before`) && is.null(`ended_before`)) {
        stop("Invalid value for `ended_before` when calling PurchaseOrderApi$SearchOrders, `ended_before` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["descending"]] <- `descending`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["ignoreCustomerFilter"]] <- `ignore_customer_filter`

      query_params[["orderItemTypes"]] <- `order_item_types`

      query_params[["orderItemIds"]] <- `order_item_ids`

      query_params[["orderCustomTypes"]] <- `order_custom_types`

      query_params[["orderCustomIds"]] <- `order_custom_ids`

      query_params[["sortField"]] <- `sort_field`

      query_params[["offerTypes"]] <- `offer_types`

      query_params[["specialOfferTypes"]] <- `special_offer_types`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["offerAudienceIds"]] <- `offer_audience_ids`

      query_params[["transactionAudienceIds"]] <- `transaction_audience_ids`

      query_params[["offerIds"]] <- `offer_ids`

      query_params[["offerLocationIds"]] <- `offer_location_ids`

      query_params[["retailerIds"]] <- `retailer_ids`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["statuses"]] <- `statuses`

      query_params[["keyword"]] <- `keyword`

      query_params[["redeemableStartDate"]] <- `redeemable_start_date`

      query_params[["redeemableEndDate"]] <- `redeemable_end_date`

      query_params[["startedSince"]] <- `started_since`

      query_params[["startedBefore"]] <- `started_before`

      query_params[["endedSince"]] <- `ended_since`

      query_params[["endedBefore"]] <- `ended_before`

      local_var_url_path <- "/order/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[OrderResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Order
    #'
    #' @param order_id The order to add the purchase to, leave null for new order.
    #' @param app_key The application requesting the purchase
    #' @param cart ```json [   { \"orderItemType\": \"OFFER\", \"orderItemId\": 234, \"orderCustomType\": \"OfferLocation\", \"orderCustomId\": 123, \"retailerLocationId\": 1234, \"quantity\": 2 },   { \"orderItemType\": \"OFFER\", \"orderItemId\": 235, \"quantity\": 2 },   { \"orderItemType\": \"CUSTOM\", \"amount\": 10.50, \"orderCustomType\": \"ServiceFee\" },   { \"orderItemType\": \"CUSTOM\", \"amount\": 25.10, \"quantity\": 2, \"orderCustomType\": \"Hat\", \"orderCustomId\": 123 } ] ``` 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param payment_transaction_id (optional) The payment transaction to apply the refund to
    #' @param description (optional) A description of the purchase
    #' @param currency_type (optional) Determines the method of purchasing offer.  * VOID: used for when there is no payment involved (ie. for updating the status of a purchase order)  * CASH: use card on file  * POINTS: use account balance  * TICKETS: use tickets  * REFUND: not allowed for a create, requires a paymentTransactionId.  * CREDIT: add to the account balance  * RELOAD: charge a credit card then add to the account balance   (default value: "CASH")
    #' @param payment_method_id (optional) Use a specific payment method (CASH), if not provided use default
    #' @param external_payment_id (optional) Store identifier from external system
    #' @param external_date (optional) External Date
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OrderResponse
    UpdateOrder = function(order_id, app_key, cart, device_id = NULL, account_id = NULL, payment_transaction_id = NULL, description = NULL, currency_type = "CASH", payment_method_id = NULL, external_payment_id = NULL, external_date = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateOrderWithHttpInfo(order_id, app_key, cart, device_id, account_id, payment_transaction_id, description, currency_type, payment_method_id, external_payment_id, external_date, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Order
    #'
    #' @param order_id The order to add the purchase to, leave null for new order.
    #' @param app_key The application requesting the purchase
    #' @param cart ```json [   { \"orderItemType\": \"OFFER\", \"orderItemId\": 234, \"orderCustomType\": \"OfferLocation\", \"orderCustomId\": 123, \"retailerLocationId\": 1234, \"quantity\": 2 },   { \"orderItemType\": \"OFFER\", \"orderItemId\": 235, \"quantity\": 2 },   { \"orderItemType\": \"CUSTOM\", \"amount\": 10.50, \"orderCustomType\": \"ServiceFee\" },   { \"orderItemType\": \"CUSTOM\", \"amount\": 25.10, \"quantity\": 2, \"orderCustomType\": \"Hat\", \"orderCustomId\": 123 } ] ``` 
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param payment_transaction_id (optional) The payment transaction to apply the refund to
    #' @param description (optional) A description of the purchase
    #' @param currency_type (optional) Determines the method of purchasing offer.  * VOID: used for when there is no payment involved (ie. for updating the status of a purchase order)  * CASH: use card on file  * POINTS: use account balance  * TICKETS: use tickets  * REFUND: not allowed for a create, requires a paymentTransactionId.  * CREDIT: add to the account balance  * RELOAD: charge a credit card then add to the account balance   (default value: "CASH")
    #' @param payment_method_id (optional) Use a specific payment method (CASH), if not provided use default
    #' @param external_payment_id (optional) Store identifier from external system
    #' @param external_date (optional) External Date
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OrderResponse) with additional information such as HTTP status code, headers
    UpdateOrderWithHttpInfo = function(order_id, app_key, cart, device_id = NULL, account_id = NULL, payment_transaction_id = NULL, description = NULL, currency_type = "CASH", payment_method_id = NULL, external_payment_id = NULL, external_date = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`order_id`)) {
        stop("Missing required parameter `order_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`cart`)) {
        stop("Missing required parameter `cart`.")
      }

      if (!missing(`order_id`) && is.null(`order_id`)) {
        stop("Invalid value for `order_id` when calling PurchaseOrderApi$UpdateOrder, `order_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling PurchaseOrderApi$UpdateOrder, `app_key` is not nullable")
      }

      if (!missing(`cart`) && is.null(`cart`)) {
        stop("Invalid value for `cart` when calling PurchaseOrderApi$UpdateOrder, `cart` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling PurchaseOrderApi$UpdateOrder, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling PurchaseOrderApi$UpdateOrder, `account_id` is not nullable")
      }

      if (!missing(`payment_transaction_id`) && is.null(`payment_transaction_id`)) {
        stop("Invalid value for `payment_transaction_id` when calling PurchaseOrderApi$UpdateOrder, `payment_transaction_id` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling PurchaseOrderApi$UpdateOrder, `description` is not nullable")
      }

      if (!missing(`currency_type`) && is.null(`currency_type`)) {
        stop("Invalid value for `currency_type` when calling PurchaseOrderApi$UpdateOrder, `currency_type` is not nullable")
      }

      if (!missing(`payment_method_id`) && is.null(`payment_method_id`)) {
        stop("Invalid value for `payment_method_id` when calling PurchaseOrderApi$UpdateOrder, `payment_method_id` is not nullable")
      }

      if (!missing(`external_payment_id`) && is.null(`external_payment_id`)) {
        stop("Invalid value for `external_payment_id` when calling PurchaseOrderApi$UpdateOrder, `external_payment_id` is not nullable")
      }

      if (!missing(`external_date`) && is.null(`external_date`)) {
        stop("Invalid value for `external_date` when calling PurchaseOrderApi$UpdateOrder, `external_date` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["orderId"]] <- `order_id`

      query_params[["paymentTransactionId"]] <- `payment_transaction_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["description"]] <- `description`

      if (!is.null(`currency_type`) && !(`currency_type` %in% c("VOID", "CASH", "POINTS", "TICKETS", "REFUND", "CREDIT", "RELOAD"))) {
        stop("Invalid value for currency_type when calling PurchaseOrderApi$UpdateOrder. Must be [VOID, CASH, POINTS, TICKETS, REFUND, CREDIT, RELOAD].")
      }
      query_params[["currencyType"]] <- `currency_type`

      query_params[["cart"]] <- `cart`

      query_params[["paymentMethodId"]] <- `payment_method_id`

      query_params[["externalPaymentId"]] <- `external_payment_id`

      query_params[["externalDate"]] <- `external_date`

      local_var_url_path <- "/order/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OrderResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
