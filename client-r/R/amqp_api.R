#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title AMQP operations
#' @description AMQPApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ConsumerCreate  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key to use when creating an analytic or service request. The account needs to have permissions to the applicaton or it will be denied.
#' var_name <- "name_example" # character | The name of the queue to connect to
#' var_hostname <- "hostname_example" # character | The hostname of the server the queue is hosted on
#' var_username <- "username_example" # character | The username to access the server the queue is hosted on
#' var_password <- "password_example" # character | The password to access the queue to connect to
#' var_data_mapping <- "data_mapping_example" # character | The data mapping information in the format of AMQPRequest
#' var_device_id <- "device_id_example" # character | The client deviceID (Optional)
#' var_account_id <- 56 # integer | The logged in user ID (Optional)
#' var_port <- 5672 # integer | The port of the server the queue is hosted on (Optional)
#' var_virtual_host <- "virtual_host_example" # character | The virtual host defined on the server the queue is associated on (Optional)
#' var_exchanger <- "exchanger_example" # character | The exchanger of the queue to connect to (Optional)
#' var_exchanger_type <- "exchanger_type_example" # character | The exchanger type of the queue to connect to (Optional)
#' var_workers <- 1 # integer | The number of workers to generate  (Optional)
#' var_use_ssl <- "use_ssl_example" # character | Use SSL (Optional)
#'
#' #Create Consumer
#' api_instance <- AMQPApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ConsumerCreate(var_app_key, var_name, var_hostname, var_username, var_password, var_data_mapping, device_id = var_device_id, account_id = var_account_id, port = var_port, virtual_host = var_virtual_host, exchanger = var_exchanger, exchanger_type = var_exchanger_type, workers = var_workers, use_ssl = var_use_ssldata_file = "result.txt")
#' result <- api_instance$ConsumerCreate(var_app_key, var_name, var_hostname, var_username, var_password, var_data_mapping, device_id = var_device_id, account_id = var_account_id, port = var_port, virtual_host = var_virtual_host, exchanger = var_exchanger, exchanger_type = var_exchanger_type, workers = var_workers, use_ssl = var_use_ssl)
#' dput(result)
#'
#'
#' ####################  ConsumerUpdate  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key to use when creating an analytic or service request. The account needs to have permissions to the applicaton or it will be denied.
#' var_queue_id <- 56 # integer | The queue to update
#' var_data_mapping <- "data_mapping_example" # character | The data mapping information in the format of AMQPRequest
#' var_device_id <- "device_id_example" # character | The client deviceID (Optional)
#' var_account_id <- 56 # integer | The logged in user ID (Optional)
#' var_use_ssl <- "use_ssl_example" # character | Use SSL (Optional)
#'
#' #Update Consumer
#' api_instance <- AMQPApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ConsumerUpdate(var_app_key, var_queue_id, var_data_mapping, device_id = var_device_id, account_id = var_account_id, use_ssl = var_use_ssldata_file = "result.txt")
#' result <- api_instance$ConsumerUpdate(var_app_key, var_queue_id, var_data_mapping, device_id = var_device_id, account_id = var_account_id, use_ssl = var_use_ssl)
#' dput(result)
#'
#'
#' ####################  QueueCreate  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key unique to each application.
#' var_name <- "name_example" # character | The name of the queue to create
#' var_device_id <- "device_id_example" # character | The client deviceID (Optional)
#' var_account_id <- 56 # integer | The logged in user ID (Optional)
#' var_workers <- 1 # integer | The number of workers to generate  (Optional)
#' var_analytic_tags <- "analytic_tags_example" # character | If provided the analytic processing will publsih to this queue instead of the default one for the provided list of tags (Optional)
#' var_hostname <- "hostname_example" # character | The hostname of the server the queue is hosted on (Optional)
#' var_port <- 56 # integer | The port of the server the queue is hosted on (Optional)
#' var_username <- "username_example" # character | The username to access the server that the queue is on (Optional)
#' var_password <- "password_example" # character | The password to access the queue to connect to (Optional)
#' var_virtual_host <- "virtual_host_example" # character | The virtual host defined on the server to queue (Optional)
#' var_use_ssl <- "use_ssl_example" # character | Use SSL (Optional)
#'
#' #Create Queue
#' api_instance <- AMQPApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$QueueCreate(var_app_key, var_name, device_id = var_device_id, account_id = var_account_id, workers = var_workers, analytic_tags = var_analytic_tags, hostname = var_hostname, port = var_port, username = var_username, password = var_password, virtual_host = var_virtual_host, use_ssl = var_use_ssldata_file = "result.txt")
#' result <- api_instance$QueueCreate(var_app_key, var_name, device_id = var_device_id, account_id = var_account_id, workers = var_workers, analytic_tags = var_analytic_tags, hostname = var_hostname, port = var_port, username = var_username, password = var_password, virtual_host = var_virtual_host, use_ssl = var_use_ssl)
#' dput(result)
#'
#'
#' ####################  QueueDelete  ####################
#'
#' library(openapi)
#' var_queue_id <- 56 # integer | The id of the queue to find
#' var_device_id <- "device_id_example" # character | The client device ID (Optional)
#' var_account_id <- 56 # integer | The logged in user ID (Optional)
#'
#' #Delete Queue
#' api_instance <- AMQPApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$QueueDelete(var_queue_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$QueueDelete(var_queue_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  QueueGet  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The client device ID (Optional)
#' var_account_id <- 56 # integer | The logged in user ID (Optional)
#' var_queue_id <- 56 # integer | The id of the queue to find (Optional)
#' var_app_key <- "app_key_example" # character | The application key the queue was assigned to (Optional)
#' var_name <- "name_example" # character | The name of the queue to find (Optional)
#' var_hostname <- "hostname_example" # character | The hostname of the queue to find (Optional)
#' var_virtual_host <- "virtual_host_example" # character | The virtual host of the queue to find (Optional)
#'
#' #Get Queue
#' api_instance <- AMQPApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$QueueGet(device_id = var_device_id, account_id = var_account_id, queue_id = var_queue_id, app_key = var_app_key, name = var_name, hostname = var_hostname, virtual_host = var_virtual_hostdata_file = "result.txt")
#' result <- api_instance$QueueGet(device_id = var_device_id, account_id = var_account_id, queue_id = var_queue_id, app_key = var_app_key, name = var_name, hostname = var_hostname, virtual_host = var_virtual_host)
#' dput(result)
#'
#'
#' ####################  QueuePublish  ####################
#'
#' library(openapi)
#' var_message <- "message_example" # character | The payload to send to the queue
#' var_queue_id <- 56 # integer | The id of the queue to publish to (Optional)
#' var_app_key <- "app_key_example" # character | The application key the queue was assigned to (Optional)
#' var_name <- "name_example" # character | The name of the queue to publish to or the analytic tag to handle if the analytic param is true (Optional)
#' var_hostname <- "hostname_example" # character | The hostname of the server the queue is hosted on (Optional)
#' var_virtual_host <- "virtual_host_example" # character | The virtual host defined on the server to queue (Optional)
#'
#' #Publish Queue
#' api_instance <- AMQPApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$QueuePublish(var_message, queue_id = var_queue_id, app_key = var_app_key, name = var_name, hostname = var_hostname, virtual_host = var_virtual_hostdata_file = "result.txt")
#' result <- api_instance$QueuePublish(var_message, queue_id = var_queue_id, app_key = var_app_key, name = var_name, hostname = var_hostname, virtual_host = var_virtual_host)
#' dput(result)
#'
#'
#' ####################  QueueSearch  ####################
#'
#' library(openapi)
#' var_queue_id <- 56 # integer | The id of the queue to find (Optional)
#' var_device_id <- "device_id_example" # character | The client device ID (Optional)
#' var_account_id <- 56 # integer | The logged in user ID (Optional)
#' var_name <- "name_example" # character | The name of the queue to find (Optional)
#' var_start <- 0 # integer | Start of the index (Optional)
#' var_limit <- 10 # integer | Limit of the index (Optional)
#'
#' #Search Queue
#' api_instance <- AMQPApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$QueueSearch(queue_id = var_queue_id, device_id = var_device_id, account_id = var_account_id, name = var_name, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$QueueSearch(queue_id = var_queue_id, device_id = var_device_id, account_id = var_account_id, name = var_name, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  QueueUpdate  ####################
#'
#' library(openapi)
#' var_queue_id <- 56 # integer | The id of the queue to update
#' var_device_id <- "device_id_example" # character | The client deviceID (Optional)
#' var_account_id <- 56 # integer | The logged in user ID (Optional)
#' var_app_key <- "app_key_example" # character | The application key unique to each application. (Optional)
#' var_workers <- 56 # integer | The number of workers to generate (Optional)
#' var_analytic_tags <- "analytic_tags_example" # character | If provided the analytic processing will publsih to this queue instead of the default one for the provided list of tags (Optional)
#' var_hostname <- "hostname_example" # character | The hostname of the server the queue is hosted on (Optional)
#' var_port <- 56 # integer | The port of the server the queue is hosted on (Optional)
#' var_username <- "username_example" # character | The username to access the server that the queue is on (Optional)
#' var_password <- "password_example" # character | The password to access the queue to connect to (Optional)
#' var_virtual_host <- "virtual_host_example" # character | The virtual host defined on the server to queue (Optional)
#' var_use_ssl <- "use_ssl_example" # character | the SSL to use (Optional)
#'
#' #Update Queue
#' api_instance <- AMQPApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$QueueUpdate(var_queue_id, device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, workers = var_workers, analytic_tags = var_analytic_tags, hostname = var_hostname, port = var_port, username = var_username, password = var_password, virtual_host = var_virtual_host, use_ssl = var_use_ssldata_file = "result.txt")
#' result <- api_instance$QueueUpdate(var_queue_id, device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, workers = var_workers, analytic_tags = var_analytic_tags, hostname = var_hostname, port = var_port, username = var_username, password = var_password, virtual_host = var_virtual_host, use_ssl = var_use_ssl)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AMQPApi <- R6::R6Class(
  "AMQPApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AMQPApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Consumer
    #'
    #' @param app_key The application key to use when creating an analytic or service request. The account needs to have permissions to the applicaton or it will be denied.
    #' @param name The name of the queue to connect to
    #' @param hostname The hostname of the server the queue is hosted on
    #' @param username The username to access the server the queue is hosted on
    #' @param password The password to access the queue to connect to
    #' @param data_mapping The data mapping information in the format of AMQPRequest
    #' @param device_id (optional) The client deviceID
    #' @param account_id (optional) The logged in user ID
    #' @param port (optional) The port of the server the queue is hosted on (default value: 5672)
    #' @param virtual_host (optional) The virtual host defined on the server the queue is associated on
    #' @param exchanger (optional) The exchanger of the queue to connect to
    #' @param exchanger_type (optional) The exchanger type of the queue to connect to
    #' @param workers (optional) The number of workers to generate  (default value: 1)
    #' @param use_ssl (optional) Use SSL
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return QueueResponse
    ConsumerCreate = function(app_key, name, hostname, username, password, data_mapping, device_id = NULL, account_id = NULL, port = 5672, virtual_host = NULL, exchanger = NULL, exchanger_type = NULL, workers = 1, use_ssl = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ConsumerCreateWithHttpInfo(app_key, name, hostname, username, password, data_mapping, device_id, account_id, port, virtual_host, exchanger, exchanger_type, workers, use_ssl, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Consumer
    #'
    #' @param app_key The application key to use when creating an analytic or service request. The account needs to have permissions to the applicaton or it will be denied.
    #' @param name The name of the queue to connect to
    #' @param hostname The hostname of the server the queue is hosted on
    #' @param username The username to access the server the queue is hosted on
    #' @param password The password to access the queue to connect to
    #' @param data_mapping The data mapping information in the format of AMQPRequest
    #' @param device_id (optional) The client deviceID
    #' @param account_id (optional) The logged in user ID
    #' @param port (optional) The port of the server the queue is hosted on (default value: 5672)
    #' @param virtual_host (optional) The virtual host defined on the server the queue is associated on
    #' @param exchanger (optional) The exchanger of the queue to connect to
    #' @param exchanger_type (optional) The exchanger type of the queue to connect to
    #' @param workers (optional) The number of workers to generate  (default value: 1)
    #' @param use_ssl (optional) Use SSL
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (QueueResponse) with additional information such as HTTP status code, headers
    ConsumerCreateWithHttpInfo = function(app_key, name, hostname, username, password, data_mapping, device_id = NULL, account_id = NULL, port = 5672, virtual_host = NULL, exchanger = NULL, exchanger_type = NULL, workers = 1, use_ssl = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (missing(`hostname`)) {
        stop("Missing required parameter `hostname`.")
      }

      if (missing(`username`)) {
        stop("Missing required parameter `username`.")
      }

      if (missing(`password`)) {
        stop("Missing required parameter `password`.")
      }

      if (missing(`data_mapping`)) {
        stop("Missing required parameter `data_mapping`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AMQPApi$ConsumerCreate, `app_key` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling AMQPApi$ConsumerCreate, `name` is not nullable")
      }

      if (!missing(`hostname`) && is.null(`hostname`)) {
        stop("Invalid value for `hostname` when calling AMQPApi$ConsumerCreate, `hostname` is not nullable")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling AMQPApi$ConsumerCreate, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling AMQPApi$ConsumerCreate, `password` is not nullable")
      }

      if (!missing(`data_mapping`) && is.null(`data_mapping`)) {
        stop("Invalid value for `data_mapping` when calling AMQPApi$ConsumerCreate, `data_mapping` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AMQPApi$ConsumerCreate, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AMQPApi$ConsumerCreate, `account_id` is not nullable")
      }

      if (!missing(`port`) && is.null(`port`)) {
        stop("Invalid value for `port` when calling AMQPApi$ConsumerCreate, `port` is not nullable")
      }

      if (!missing(`virtual_host`) && is.null(`virtual_host`)) {
        stop("Invalid value for `virtual_host` when calling AMQPApi$ConsumerCreate, `virtual_host` is not nullable")
      }

      if (!missing(`exchanger`) && is.null(`exchanger`)) {
        stop("Invalid value for `exchanger` when calling AMQPApi$ConsumerCreate, `exchanger` is not nullable")
      }

      if (!missing(`exchanger_type`) && is.null(`exchanger_type`)) {
        stop("Invalid value for `exchanger_type` when calling AMQPApi$ConsumerCreate, `exchanger_type` is not nullable")
      }

      if (!missing(`workers`) && is.null(`workers`)) {
        stop("Invalid value for `workers` when calling AMQPApi$ConsumerCreate, `workers` is not nullable")
      }

      if (!missing(`use_ssl`) && is.null(`use_ssl`)) {
        stop("Invalid value for `use_ssl` when calling AMQPApi$ConsumerCreate, `use_ssl` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["name"]] <- `name`

      query_params[["hostname"]] <- `hostname`

      query_params[["port"]] <- `port`

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      query_params[["virtualHost"]] <- `virtual_host`

      query_params[["exchanger"]] <- `exchanger`

      query_params[["exchangerType"]] <- `exchanger_type`

      query_params[["workers"]] <- `workers`

      query_params[["dataMapping"]] <- `data_mapping`

      query_params[["useSSL"]] <- `use_ssl`

      local_var_url_path <- "/queue/consumer/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QueueResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Consumer
    #'
    #' @param app_key The application key to use when creating an analytic or service request. The account needs to have permissions to the applicaton or it will be denied.
    #' @param queue_id The queue to update
    #' @param data_mapping The data mapping information in the format of AMQPRequest
    #' @param device_id (optional) The client deviceID
    #' @param account_id (optional) The logged in user ID
    #' @param use_ssl (optional) Use SSL
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return QueueResponse
    ConsumerUpdate = function(app_key, queue_id, data_mapping, device_id = NULL, account_id = NULL, use_ssl = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ConsumerUpdateWithHttpInfo(app_key, queue_id, data_mapping, device_id, account_id, use_ssl, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Consumer
    #'
    #' @param app_key The application key to use when creating an analytic or service request. The account needs to have permissions to the applicaton or it will be denied.
    #' @param queue_id The queue to update
    #' @param data_mapping The data mapping information in the format of AMQPRequest
    #' @param device_id (optional) The client deviceID
    #' @param account_id (optional) The logged in user ID
    #' @param use_ssl (optional) Use SSL
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (QueueResponse) with additional information such as HTTP status code, headers
    ConsumerUpdateWithHttpInfo = function(app_key, queue_id, data_mapping, device_id = NULL, account_id = NULL, use_ssl = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`queue_id`)) {
        stop("Missing required parameter `queue_id`.")
      }

      if (missing(`data_mapping`)) {
        stop("Missing required parameter `data_mapping`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AMQPApi$ConsumerUpdate, `app_key` is not nullable")
      }

      if (!missing(`queue_id`) && is.null(`queue_id`)) {
        stop("Invalid value for `queue_id` when calling AMQPApi$ConsumerUpdate, `queue_id` is not nullable")
      }

      if (!missing(`data_mapping`) && is.null(`data_mapping`)) {
        stop("Invalid value for `data_mapping` when calling AMQPApi$ConsumerUpdate, `data_mapping` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AMQPApi$ConsumerUpdate, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AMQPApi$ConsumerUpdate, `account_id` is not nullable")
      }

      if (!missing(`use_ssl`) && is.null(`use_ssl`)) {
        stop("Invalid value for `use_ssl` when calling AMQPApi$ConsumerUpdate, `use_ssl` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["queueId"]] <- `queue_id`

      query_params[["dataMapping"]] <- `data_mapping`

      query_params[["useSSL"]] <- `use_ssl`

      local_var_url_path <- "/queue/consumer/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QueueResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Queue
    #'
    #' @param app_key The application key unique to each application.
    #' @param name The name of the queue to create
    #' @param device_id (optional) The client deviceID
    #' @param account_id (optional) The logged in user ID
    #' @param workers (optional) The number of workers to generate  (default value: 1)
    #' @param analytic_tags (optional) If provided the analytic processing will publsih to this queue instead of the default one for the provided list of tags
    #' @param hostname (optional) The hostname of the server the queue is hosted on
    #' @param port (optional) The port of the server the queue is hosted on
    #' @param username (optional) The username to access the server that the queue is on
    #' @param password (optional) The password to access the queue to connect to
    #' @param virtual_host (optional) The virtual host defined on the server to queue
    #' @param use_ssl (optional) Use SSL
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return QueueResponse
    QueueCreate = function(app_key, name, device_id = NULL, account_id = NULL, workers = 1, analytic_tags = NULL, hostname = NULL, port = NULL, username = NULL, password = NULL, virtual_host = NULL, use_ssl = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$QueueCreateWithHttpInfo(app_key, name, device_id, account_id, workers, analytic_tags, hostname, port, username, password, virtual_host, use_ssl, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Queue
    #'
    #' @param app_key The application key unique to each application.
    #' @param name The name of the queue to create
    #' @param device_id (optional) The client deviceID
    #' @param account_id (optional) The logged in user ID
    #' @param workers (optional) The number of workers to generate  (default value: 1)
    #' @param analytic_tags (optional) If provided the analytic processing will publsih to this queue instead of the default one for the provided list of tags
    #' @param hostname (optional) The hostname of the server the queue is hosted on
    #' @param port (optional) The port of the server the queue is hosted on
    #' @param username (optional) The username to access the server that the queue is on
    #' @param password (optional) The password to access the queue to connect to
    #' @param virtual_host (optional) The virtual host defined on the server to queue
    #' @param use_ssl (optional) Use SSL
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (QueueResponse) with additional information such as HTTP status code, headers
    QueueCreateWithHttpInfo = function(app_key, name, device_id = NULL, account_id = NULL, workers = 1, analytic_tags = NULL, hostname = NULL, port = NULL, username = NULL, password = NULL, virtual_host = NULL, use_ssl = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AMQPApi$QueueCreate, `app_key` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling AMQPApi$QueueCreate, `name` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AMQPApi$QueueCreate, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AMQPApi$QueueCreate, `account_id` is not nullable")
      }

      if (!missing(`workers`) && is.null(`workers`)) {
        stop("Invalid value for `workers` when calling AMQPApi$QueueCreate, `workers` is not nullable")
      }

      if (!missing(`analytic_tags`) && is.null(`analytic_tags`)) {
        stop("Invalid value for `analytic_tags` when calling AMQPApi$QueueCreate, `analytic_tags` is not nullable")
      }

      if (!missing(`hostname`) && is.null(`hostname`)) {
        stop("Invalid value for `hostname` when calling AMQPApi$QueueCreate, `hostname` is not nullable")
      }

      if (!missing(`port`) && is.null(`port`)) {
        stop("Invalid value for `port` when calling AMQPApi$QueueCreate, `port` is not nullable")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling AMQPApi$QueueCreate, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling AMQPApi$QueueCreate, `password` is not nullable")
      }

      if (!missing(`virtual_host`) && is.null(`virtual_host`)) {
        stop("Invalid value for `virtual_host` when calling AMQPApi$QueueCreate, `virtual_host` is not nullable")
      }

      if (!missing(`use_ssl`) && is.null(`use_ssl`)) {
        stop("Invalid value for `use_ssl` when calling AMQPApi$QueueCreate, `use_ssl` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["name"]] <- `name`

      query_params[["workers"]] <- `workers`

      query_params[["analyticTags"]] <- `analytic_tags`

      query_params[["hostname"]] <- `hostname`

      query_params[["port"]] <- `port`

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      query_params[["virtualHost"]] <- `virtual_host`

      query_params[["useSSL"]] <- `use_ssl`

      local_var_url_path <- "/queue/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QueueResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Queue
    #'
    #' @param queue_id The id of the queue to find
    #' @param device_id (optional) The client device ID
    #' @param account_id (optional) The logged in user ID
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    QueueDelete = function(queue_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$QueueDeleteWithHttpInfo(queue_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Queue
    #'
    #' @param queue_id The id of the queue to find
    #' @param device_id (optional) The client device ID
    #' @param account_id (optional) The logged in user ID
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    QueueDeleteWithHttpInfo = function(queue_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`queue_id`)) {
        stop("Missing required parameter `queue_id`.")
      }

      if (!missing(`queue_id`) && is.null(`queue_id`)) {
        stop("Invalid value for `queue_id` when calling AMQPApi$QueueDelete, `queue_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AMQPApi$QueueDelete, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AMQPApi$QueueDelete, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["queueId"]] <- `queue_id`

      local_var_url_path <- "/queue/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Queue
    #'
    #' @param device_id (optional) The client device ID
    #' @param account_id (optional) The logged in user ID
    #' @param queue_id (optional) The id of the queue to find
    #' @param app_key (optional) The application key the queue was assigned to
    #' @param name (optional) The name of the queue to find
    #' @param hostname (optional) The hostname of the queue to find
    #' @param virtual_host (optional) The virtual host of the queue to find
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return QueueResponse
    QueueGet = function(device_id = NULL, account_id = NULL, queue_id = NULL, app_key = NULL, name = NULL, hostname = NULL, virtual_host = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$QueueGetWithHttpInfo(device_id, account_id, queue_id, app_key, name, hostname, virtual_host, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Queue
    #'
    #' @param device_id (optional) The client device ID
    #' @param account_id (optional) The logged in user ID
    #' @param queue_id (optional) The id of the queue to find
    #' @param app_key (optional) The application key the queue was assigned to
    #' @param name (optional) The name of the queue to find
    #' @param hostname (optional) The hostname of the queue to find
    #' @param virtual_host (optional) The virtual host of the queue to find
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (QueueResponse) with additional information such as HTTP status code, headers
    QueueGetWithHttpInfo = function(device_id = NULL, account_id = NULL, queue_id = NULL, app_key = NULL, name = NULL, hostname = NULL, virtual_host = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AMQPApi$QueueGet, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AMQPApi$QueueGet, `account_id` is not nullable")
      }

      if (!missing(`queue_id`) && is.null(`queue_id`)) {
        stop("Invalid value for `queue_id` when calling AMQPApi$QueueGet, `queue_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AMQPApi$QueueGet, `app_key` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling AMQPApi$QueueGet, `name` is not nullable")
      }

      if (!missing(`hostname`) && is.null(`hostname`)) {
        stop("Invalid value for `hostname` when calling AMQPApi$QueueGet, `hostname` is not nullable")
      }

      if (!missing(`virtual_host`) && is.null(`virtual_host`)) {
        stop("Invalid value for `virtual_host` when calling AMQPApi$QueueGet, `virtual_host` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["queueId"]] <- `queue_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["name"]] <- `name`

      query_params[["hostname"]] <- `hostname`

      query_params[["virtualHost"]] <- `virtual_host`

      local_var_url_path <- "/queue/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QueueResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Publish Queue
    #'
    #' @param message The payload to send to the queue
    #' @param queue_id (optional) The id of the queue to publish to
    #' @param app_key (optional) The application key the queue was assigned to
    #' @param name (optional) The name of the queue to publish to or the analytic tag to handle if the analytic param is true
    #' @param hostname (optional) The hostname of the server the queue is hosted on
    #' @param virtual_host (optional) The virtual host defined on the server to queue
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    QueuePublish = function(message, queue_id = NULL, app_key = NULL, name = NULL, hostname = NULL, virtual_host = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$QueuePublishWithHttpInfo(message, queue_id, app_key, name, hostname, virtual_host, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Publish Queue
    #'
    #' @param message The payload to send to the queue
    #' @param queue_id (optional) The id of the queue to publish to
    #' @param app_key (optional) The application key the queue was assigned to
    #' @param name (optional) The name of the queue to publish to or the analytic tag to handle if the analytic param is true
    #' @param hostname (optional) The hostname of the server the queue is hosted on
    #' @param virtual_host (optional) The virtual host defined on the server to queue
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    QueuePublishWithHttpInfo = function(message, queue_id = NULL, app_key = NULL, name = NULL, hostname = NULL, virtual_host = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`message`)) {
        stop("Missing required parameter `message`.")
      }

      if (!missing(`message`) && is.null(`message`)) {
        stop("Invalid value for `message` when calling AMQPApi$QueuePublish, `message` is not nullable")
      }

      if (!missing(`queue_id`) && is.null(`queue_id`)) {
        stop("Invalid value for `queue_id` when calling AMQPApi$QueuePublish, `queue_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AMQPApi$QueuePublish, `app_key` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling AMQPApi$QueuePublish, `name` is not nullable")
      }

      if (!missing(`hostname`) && is.null(`hostname`)) {
        stop("Invalid value for `hostname` when calling AMQPApi$QueuePublish, `hostname` is not nullable")
      }

      if (!missing(`virtual_host`) && is.null(`virtual_host`)) {
        stop("Invalid value for `virtual_host` when calling AMQPApi$QueuePublish, `virtual_host` is not nullable")
      }

      query_params[["queueId"]] <- `queue_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["name"]] <- `name`

      query_params[["hostname"]] <- `hostname`

      query_params[["virtualHost"]] <- `virtual_host`

      query_params[["message"]] <- `message`

      local_var_url_path <- "/queue/publish"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Queue
    #'
    #' @param queue_id (optional) The id of the queue to find
    #' @param device_id (optional) The client device ID
    #' @param account_id (optional) The logged in user ID
    #' @param name (optional) The name of the queue to find
    #' @param start (optional) Start of the index (default value: 0)
    #' @param limit (optional) Limit of the index (default value: 10)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return QueueResponse
    QueueSearch = function(queue_id = NULL, device_id = NULL, account_id = NULL, name = NULL, start = 0, limit = 10, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$QueueSearchWithHttpInfo(queue_id, device_id, account_id, name, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Queue
    #'
    #' @param queue_id (optional) The id of the queue to find
    #' @param device_id (optional) The client device ID
    #' @param account_id (optional) The logged in user ID
    #' @param name (optional) The name of the queue to find
    #' @param start (optional) Start of the index (default value: 0)
    #' @param limit (optional) Limit of the index (default value: 10)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (QueueResponse) with additional information such as HTTP status code, headers
    QueueSearchWithHttpInfo = function(queue_id = NULL, device_id = NULL, account_id = NULL, name = NULL, start = 0, limit = 10, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`queue_id`) && is.null(`queue_id`)) {
        stop("Invalid value for `queue_id` when calling AMQPApi$QueueSearch, `queue_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AMQPApi$QueueSearch, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AMQPApi$QueueSearch, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling AMQPApi$QueueSearch, `name` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AMQPApi$QueueSearch, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AMQPApi$QueueSearch, `limit` is not nullable")
      }

      query_params[["queueId"]] <- `queue_id`

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["name"]] <- `name`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/queue/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QueueResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Queue
    #'
    #' @param queue_id The id of the queue to update
    #' @param device_id (optional) The client deviceID
    #' @param account_id (optional) The logged in user ID
    #' @param app_key (optional) The application key unique to each application.
    #' @param workers (optional) The number of workers to generate
    #' @param analytic_tags (optional) If provided the analytic processing will publsih to this queue instead of the default one for the provided list of tags
    #' @param hostname (optional) The hostname of the server the queue is hosted on
    #' @param port (optional) The port of the server the queue is hosted on
    #' @param username (optional) The username to access the server that the queue is on
    #' @param password (optional) The password to access the queue to connect to
    #' @param virtual_host (optional) The virtual host defined on the server to queue
    #' @param use_ssl (optional) the SSL to use
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return QueueResponse
    QueueUpdate = function(queue_id, device_id = NULL, account_id = NULL, app_key = NULL, workers = NULL, analytic_tags = NULL, hostname = NULL, port = NULL, username = NULL, password = NULL, virtual_host = NULL, use_ssl = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$QueueUpdateWithHttpInfo(queue_id, device_id, account_id, app_key, workers, analytic_tags, hostname, port, username, password, virtual_host, use_ssl, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Queue
    #'
    #' @param queue_id The id of the queue to update
    #' @param device_id (optional) The client deviceID
    #' @param account_id (optional) The logged in user ID
    #' @param app_key (optional) The application key unique to each application.
    #' @param workers (optional) The number of workers to generate
    #' @param analytic_tags (optional) If provided the analytic processing will publsih to this queue instead of the default one for the provided list of tags
    #' @param hostname (optional) The hostname of the server the queue is hosted on
    #' @param port (optional) The port of the server the queue is hosted on
    #' @param username (optional) The username to access the server that the queue is on
    #' @param password (optional) The password to access the queue to connect to
    #' @param virtual_host (optional) The virtual host defined on the server to queue
    #' @param use_ssl (optional) the SSL to use
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (QueueResponse) with additional information such as HTTP status code, headers
    QueueUpdateWithHttpInfo = function(queue_id, device_id = NULL, account_id = NULL, app_key = NULL, workers = NULL, analytic_tags = NULL, hostname = NULL, port = NULL, username = NULL, password = NULL, virtual_host = NULL, use_ssl = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`queue_id`)) {
        stop("Missing required parameter `queue_id`.")
      }

      if (!missing(`queue_id`) && is.null(`queue_id`)) {
        stop("Invalid value for `queue_id` when calling AMQPApi$QueueUpdate, `queue_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AMQPApi$QueueUpdate, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AMQPApi$QueueUpdate, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AMQPApi$QueueUpdate, `app_key` is not nullable")
      }

      if (!missing(`workers`) && is.null(`workers`)) {
        stop("Invalid value for `workers` when calling AMQPApi$QueueUpdate, `workers` is not nullable")
      }

      if (!missing(`analytic_tags`) && is.null(`analytic_tags`)) {
        stop("Invalid value for `analytic_tags` when calling AMQPApi$QueueUpdate, `analytic_tags` is not nullable")
      }

      if (!missing(`hostname`) && is.null(`hostname`)) {
        stop("Invalid value for `hostname` when calling AMQPApi$QueueUpdate, `hostname` is not nullable")
      }

      if (!missing(`port`) && is.null(`port`)) {
        stop("Invalid value for `port` when calling AMQPApi$QueueUpdate, `port` is not nullable")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling AMQPApi$QueueUpdate, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling AMQPApi$QueueUpdate, `password` is not nullable")
      }

      if (!missing(`virtual_host`) && is.null(`virtual_host`)) {
        stop("Invalid value for `virtual_host` when calling AMQPApi$QueueUpdate, `virtual_host` is not nullable")
      }

      if (!missing(`use_ssl`) && is.null(`use_ssl`)) {
        stop("Invalid value for `use_ssl` when calling AMQPApi$QueueUpdate, `use_ssl` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["queueId"]] <- `queue_id`

      query_params[["workers"]] <- `workers`

      query_params[["analyticTags"]] <- `analytic_tags`

      query_params[["hostname"]] <- `hostname`

      query_params[["port"]] <- `port`

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      query_params[["virtualHost"]] <- `virtual_host`

      query_params[["useSSL"]] <- `use_ssl`

      local_var_url_path <- "/queue/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "QueueResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
