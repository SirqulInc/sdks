#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title AppData operations
#' @description AppDataApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GetAppData  ####################
#'
#' library(openapi)
#' var_start <- 56 # integer | start the search results at a record.
#' var_limit <- 56 # integer | limit the search results to some number.
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required). (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required). (Optional)
#' var_game_type <- "game_type_example" # character | the game to retrieve the data for, use your application key. (Optional)
#' var_include_game_data <- "include_game_data_example" # character | if true then include the game data blob, otherwise don't include. (Optional)
#' var_q <- "q_example" # character | This parameter is deprecated. (Optional)
#' var_keyword <- "keyword_example" # character | query string used to filter the search results when looking for community and saved levels. (Optional)
#' var_sort_field <- "sort_field_example" # character | how to sort the results when looking for community and saved levels. possible values are: LEVEL_NAME, LEVEL_CREATED, LEVEL_UPDATED, LEVEL_LIKES, LEVEL_DISLIKES, LEVEL_NOTES, LEVEL_PLAYS. (Optional)
#' var_descending <- "descending_example" # character | order the search results descending or ascending when looking for community and saved levels. (Optional)
#' var__i <- 56 # integer | This parameter is deprecated. (Optional)
#' var__l <- 56 # integer | This parameter is deprecated. (Optional)
#' var_game_object_count <- "game_object_count_example" # character | if true then include the game object count, otherwise don't include. (Optional)
#' var_filter <- "filter_example" # character | restrict the search to items with certain permissions, possible values are: MINE, FRIENDS, PUBLIC, ALL_PUBLIC, ALL, LIKED (Optional)
#' var_date_created <- 56 # integer | restrict the search to items created less then date (Optional)
#' var_owner_id <- 56 # integer | get levels owned by a particular account (Optional)
#' var_mission_ids <- "mission_ids_example" # character | get missions by specified id, comman seperated list of long ids (Optional)
#' var_game_ids <- "game_ids_example" # character | get games by specified id, comman seperated list of long ids (Optional)
#' var_pack_ids <- "pack_ids_example" # character | get packs by specified id, comman seperated list of long ids (Optional)
#' var_game_level_ids <- "game_level_ids_example" # character | get game levels by specified id, comman seperated list of long ids (Optional)
#' var_app_version <- "app_version_example" # character | the application version, used to versin the game level data (Optional)
#' var_include_higher_version_packs <- "include_higher_version_packs_example" # character | default is false and will not include any packs that have levels above the specified appVersion. If true then will return all levels regardless of version. (Optional)
#' var_include_higher_version_levels <- "include_higher_version_levels_example" # character | default is false and will not include any levels above the specified appVersion. If true then will return all levels regardless of version. (Optional)
#' var_response_groups <- "response_groups_example" # character | use response groups to include large parts of the structure. possible values are: * MISSION_DEFAULT (default) - include the default mission (built-in packs), excludes all other mission groups * MISSION_DEFAULT_LEVELS_SAVED - include saved levels, excludes all other mission groups * MISSION_DEFAULT_LEVELS_COMMUNITY - include community levels, excludes all other mission groups * MISSION_INVITED (default) - include challenges sent to user * PROFILE (default) - include entire profile * PROFILE_DATA - only include profile date (exclude friends) * PROFILE_FRIENDS - include friends list  (Optional)
#' var_purchase_type <- "purchase_type_example" # character | the will return the correct in app purchases for the device, possible values are: * SIRQUL (default) - purchasing from the sirqul store using tickets * IOS - purchasing from the itunes store for iPhone, iPod, iPod Touch * GOOGLE - purchasing from the google android store * AMAZON - purchasing from the amazon android store * MAC - purchasing from the itunes store for OSX * FREE - the item is free to purchase  (Optional)
#'
#' #Get App Data
#' api_instance <- AppDataApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAppData(var_start, var_limit, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, include_game_data = var_include_game_data, q = var_q, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, _i = var__i, _l = var__l, game_object_count = var_game_object_count, filter = var_filter, date_created = var_date_created, owner_id = var_owner_id, mission_ids = var_mission_ids, game_ids = var_game_ids, pack_ids = var_pack_ids, game_level_ids = var_game_level_ids, app_version = var_app_version, include_higher_version_packs = var_include_higher_version_packs, include_higher_version_levels = var_include_higher_version_levels, response_groups = var_response_groups, purchase_type = var_purchase_typedata_file = "result.txt")
#' result <- api_instance$GetAppData(var_start, var_limit, device_id = var_device_id, account_id = var_account_id, game_type = var_game_type, include_game_data = var_include_game_data, q = var_q, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, _i = var__i, _l = var__l, game_object_count = var_game_object_count, filter = var_filter, date_created = var_date_created, owner_id = var_owner_id, mission_ids = var_mission_ids, game_ids = var_game_ids, pack_ids = var_pack_ids, game_level_ids = var_game_level_ids, app_version = var_app_version, include_higher_version_packs = var_include_higher_version_packs, include_higher_version_levels = var_include_higher_version_levels, response_groups = var_response_groups, purchase_type = var_purchase_type)
#' dput(result)
#'
#'
#' ####################  PostAppData  ####################
#'
#' library(openapi)
#' var_game_type <- "game_type_example" # character | the game to retrieve the data for, use your application key.
#' var_start <- 56 # integer | start the search results at a record.
#' var_limit <- 56 # integer | limit the search results to some number.
#' var_data <- "data_example" # character | The analytic data AnalyticListResponse
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required). (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required). (Optional)
#' var_include_game_data <- "include_game_data_example" # character | if true then include the game data blob, otherwise don't include. (Optional)
#' var_q <- "q_example" # character | This parameter is deprecated. (Optional)
#' var_keyword <- "keyword_example" # character | query string used to filter the search results when looking for community and saved levels. (Optional)
#' var_sort_field <- "sort_field_example" # character | how to sort the results when looking for community and saved levels. possible values are: LEVEL_NAME, LEVEL_CREATED, LEVEL_UPDATED, LEVEL_LIKES, LEVEL_DISLIKES, LEVEL_NOTES, LEVEL_PLAYS. (Optional)
#' var_descending <- "descending_example" # character | order the search results descending or ascending when looking for community and saved levels. (Optional)
#' var__i <- 56 # integer | This parameter is deprecated. (Optional)
#' var__l <- 56 # integer | This parameter is deprecated. (Optional)
#' var_game_object_count <- "game_object_count_example" # character | if true then include the game object count, otherwise don't include. (Optional)
#' var_filter <- "filter_example" # character | restrict the search to items with certain permissions, possible values are: MINE, FRIENDS, PUBLIC, ALL_PUBLIC, ALL, LIKED (Optional)
#' var_date_created <- 56 # integer | restrict the search to items created less then date (Optional)
#' var_owner_id <- 56 # integer | get levels owned by a particular account (Optional)
#' var_mission_ids <- "mission_ids_example" # character | get missions by specified id, comman seperated list of long ids (Optional)
#' var_game_ids <- "game_ids_example" # character | get games by specified id, comman seperated list of long ids (Optional)
#' var_pack_ids <- "pack_ids_example" # character | get packs by specified id, comman seperated list of long ids (Optional)
#' var_game_level_ids <- "game_level_ids_example" # character | get game levels by specified id, comman seperated list of long ids (Optional)
#' var_app_version <- "app_version_example" # character | the application version, used to versin the game level data (Optional)
#' var_include_higher_version_packs <- "include_higher_version_packs_example" # character | default is false and will not include any packs that have levels above the specified appVersion. If true then will return all levels regardless of version. (Optional)
#' var_include_higher_version_levels <- "include_higher_version_levels_example" # character | default is false and will not include any levels above the specified appVersion. If true then will return all levels regardless of version. (Optional)
#' var_response_groups <- "response_groups_example" # character | use response groups to include large parts of the structure. possible values are: * MISSION_DEFAULT (default) - include the default mission (built-in packs), excludes all other mission groups * MISSION_DEFAULT_LEVELS_SAVED - include saved levels, excludes all other mission groups * MISSION_DEFAULT_LEVELS_COMMUNITY - include community levels, excludes all other mission groups * MISSION_INVITED (default) - include challenges sent to user * PROFILE (default) - include entire profile * PROFILE_DATA - only include profile date (exclude friends) * PROFILE_FRIENDS - include friends list  (Optional)
#' var_purchase_type <- "purchase_type_example" # character | the will return the correct in app purchases for the device, possible values are: * SIRQUL (default) - purchasing from the sirqul store using tickets * IOS - purchasing from the itunes store for iPhone, iPod, iPod Touch * GOOGLE - purchasing from the google android store * AMAZON - purchasing from the amazon android store * MAC - purchasing from the itunes store for OSX * FREE - the item is free to purchase  (Optional)
#'
#' #Create App Data
#' api_instance <- AppDataApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostAppData(var_game_type, var_start, var_limit, var_data, device_id = var_device_id, account_id = var_account_id, include_game_data = var_include_game_data, q = var_q, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, _i = var__i, _l = var__l, game_object_count = var_game_object_count, filter = var_filter, date_created = var_date_created, owner_id = var_owner_id, mission_ids = var_mission_ids, game_ids = var_game_ids, pack_ids = var_pack_ids, game_level_ids = var_game_level_ids, app_version = var_app_version, include_higher_version_packs = var_include_higher_version_packs, include_higher_version_levels = var_include_higher_version_levels, response_groups = var_response_groups, purchase_type = var_purchase_typedata_file = "result.txt")
#' result <- api_instance$PostAppData(var_game_type, var_start, var_limit, var_data, device_id = var_device_id, account_id = var_account_id, include_game_data = var_include_game_data, q = var_q, keyword = var_keyword, sort_field = var_sort_field, descending = var_descending, _i = var__i, _l = var__l, game_object_count = var_game_object_count, filter = var_filter, date_created = var_date_created, owner_id = var_owner_id, mission_ids = var_mission_ids, game_ids = var_game_ids, pack_ids = var_pack_ids, game_level_ids = var_game_level_ids, app_version = var_app_version, include_higher_version_packs = var_include_higher_version_packs, include_higher_version_levels = var_include_higher_version_levels, response_groups = var_response_groups, purchase_type = var_purchase_type)
#' dput(result)
#'
#'
#' ####################  RegenAppData  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account id of the user (Optional)
#' var_app_key <- "app_key_example" # character | process a specific application, if null process all apps with caches (Optional)
#' var_build_version <- "build_version_example" # character | create a specific version, if null use current version. Be careful if processing all (Optional)
#' var_api_version <- "api_version_example" # character | create a specific version, if null use current version. Be careful if processing all (Optional)
#'
#' #Regenerate App Data
#' api_instance <- AppDataApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RegenAppData(account_id = var_account_id, app_key = var_app_key, build_version = var_build_version, api_version = var_api_versiondata_file = "result.txt")
#' result <- api_instance$RegenAppData(account_id = var_account_id, app_key = var_app_key, build_version = var_build_version, api_version = var_api_version)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AppDataApi <- R6::R6Class(
  "AppDataApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AppDataApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get App Data
    #'
    #' @param start start the search results at a record.
    #' @param limit limit the search results to some number.
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param game_type (optional) the game to retrieve the data for, use your application key.
    #' @param include_game_data (optional) if true then include the game data blob, otherwise don't include.
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) query string used to filter the search results when looking for community and saved levels.
    #' @param sort_field (optional) how to sort the results when looking for community and saved levels. possible values are: LEVEL_NAME, LEVEL_CREATED, LEVEL_UPDATED, LEVEL_LIKES, LEVEL_DISLIKES, LEVEL_NOTES, LEVEL_PLAYS.
    #' @param descending (optional) order the search results descending or ascending when looking for community and saved levels.
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param game_object_count (optional) if true then include the game object count, otherwise don't include.
    #' @param filter (optional) restrict the search to items with certain permissions, possible values are: MINE, FRIENDS, PUBLIC, ALL_PUBLIC, ALL, LIKED
    #' @param date_created (optional) restrict the search to items created less then date
    #' @param owner_id (optional) get levels owned by a particular account
    #' @param mission_ids (optional) get missions by specified id, comman seperated list of long ids
    #' @param game_ids (optional) get games by specified id, comman seperated list of long ids
    #' @param pack_ids (optional) get packs by specified id, comman seperated list of long ids
    #' @param game_level_ids (optional) get game levels by specified id, comman seperated list of long ids
    #' @param app_version (optional) the application version, used to versin the game level data
    #' @param include_higher_version_packs (optional) default is false and will not include any packs that have levels above the specified appVersion. If true then will return all levels regardless of version.
    #' @param include_higher_version_levels (optional) default is false and will not include any levels above the specified appVersion. If true then will return all levels regardless of version.
    #' @param response_groups (optional) use response groups to include large parts of the structure. possible values are: * MISSION_DEFAULT (default) - include the default mission (built-in packs), excludes all other mission groups * MISSION_DEFAULT_LEVELS_SAVED - include saved levels, excludes all other mission groups * MISSION_DEFAULT_LEVELS_COMMUNITY - include community levels, excludes all other mission groups * MISSION_INVITED (default) - include challenges sent to user * PROFILE (default) - include entire profile * PROFILE_DATA - only include profile date (exclude friends) * PROFILE_FRIENDS - include friends list 
    #' @param purchase_type (optional) the will return the correct in app purchases for the device, possible values are: * SIRQUL (default) - purchasing from the sirqul store using tickets * IOS - purchasing from the itunes store for iPhone, iPod, iPod Touch * GOOGLE - purchasing from the google android store * AMAZON - purchasing from the amazon android store * MAC - purchasing from the itunes store for OSX * FREE - the item is free to purchase 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AppResponse
    GetAppData = function(start, limit, device_id = NULL, account_id = NULL, game_type = NULL, include_game_data = NULL, q = NULL, keyword = NULL, sort_field = NULL, descending = NULL, _i = NULL, _l = NULL, game_object_count = NULL, filter = NULL, date_created = NULL, owner_id = NULL, mission_ids = NULL, game_ids = NULL, pack_ids = NULL, game_level_ids = NULL, app_version = NULL, include_higher_version_packs = NULL, include_higher_version_levels = NULL, response_groups = NULL, purchase_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetAppDataWithHttpInfo(start, limit, device_id, account_id, game_type, include_game_data, q, keyword, sort_field, descending, _i, _l, game_object_count, filter, date_created, owner_id, mission_ids, game_ids, pack_ids, game_level_ids, app_version, include_higher_version_packs, include_higher_version_levels, response_groups, purchase_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get App Data
    #'
    #' @param start start the search results at a record.
    #' @param limit limit the search results to some number.
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param game_type (optional) the game to retrieve the data for, use your application key.
    #' @param include_game_data (optional) if true then include the game data blob, otherwise don't include.
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) query string used to filter the search results when looking for community and saved levels.
    #' @param sort_field (optional) how to sort the results when looking for community and saved levels. possible values are: LEVEL_NAME, LEVEL_CREATED, LEVEL_UPDATED, LEVEL_LIKES, LEVEL_DISLIKES, LEVEL_NOTES, LEVEL_PLAYS.
    #' @param descending (optional) order the search results descending or ascending when looking for community and saved levels.
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param game_object_count (optional) if true then include the game object count, otherwise don't include.
    #' @param filter (optional) restrict the search to items with certain permissions, possible values are: MINE, FRIENDS, PUBLIC, ALL_PUBLIC, ALL, LIKED
    #' @param date_created (optional) restrict the search to items created less then date
    #' @param owner_id (optional) get levels owned by a particular account
    #' @param mission_ids (optional) get missions by specified id, comman seperated list of long ids
    #' @param game_ids (optional) get games by specified id, comman seperated list of long ids
    #' @param pack_ids (optional) get packs by specified id, comman seperated list of long ids
    #' @param game_level_ids (optional) get game levels by specified id, comman seperated list of long ids
    #' @param app_version (optional) the application version, used to versin the game level data
    #' @param include_higher_version_packs (optional) default is false and will not include any packs that have levels above the specified appVersion. If true then will return all levels regardless of version.
    #' @param include_higher_version_levels (optional) default is false and will not include any levels above the specified appVersion. If true then will return all levels regardless of version.
    #' @param response_groups (optional) use response groups to include large parts of the structure. possible values are: * MISSION_DEFAULT (default) - include the default mission (built-in packs), excludes all other mission groups * MISSION_DEFAULT_LEVELS_SAVED - include saved levels, excludes all other mission groups * MISSION_DEFAULT_LEVELS_COMMUNITY - include community levels, excludes all other mission groups * MISSION_INVITED (default) - include challenges sent to user * PROFILE (default) - include entire profile * PROFILE_DATA - only include profile date (exclude friends) * PROFILE_FRIENDS - include friends list 
    #' @param purchase_type (optional) the will return the correct in app purchases for the device, possible values are: * SIRQUL (default) - purchasing from the sirqul store using tickets * IOS - purchasing from the itunes store for iPhone, iPod, iPod Touch * GOOGLE - purchasing from the google android store * AMAZON - purchasing from the amazon android store * MAC - purchasing from the itunes store for OSX * FREE - the item is free to purchase 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AppResponse) with additional information such as HTTP status code, headers
    GetAppDataWithHttpInfo = function(start, limit, device_id = NULL, account_id = NULL, game_type = NULL, include_game_data = NULL, q = NULL, keyword = NULL, sort_field = NULL, descending = NULL, _i = NULL, _l = NULL, game_object_count = NULL, filter = NULL, date_created = NULL, owner_id = NULL, mission_ids = NULL, game_ids = NULL, pack_ids = NULL, game_level_ids = NULL, app_version = NULL, include_higher_version_packs = NULL, include_higher_version_levels = NULL, response_groups = NULL, purchase_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AppDataApi$GetAppData, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AppDataApi$GetAppData, `limit` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AppDataApi$GetAppData, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AppDataApi$GetAppData, `account_id` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling AppDataApi$GetAppData, `game_type` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling AppDataApi$GetAppData, `include_game_data` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling AppDataApi$GetAppData, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling AppDataApi$GetAppData, `keyword` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AppDataApi$GetAppData, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AppDataApi$GetAppData, `descending` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling AppDataApi$GetAppData, `_i` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling AppDataApi$GetAppData, `_l` is not nullable")
      }

      if (!missing(`game_object_count`) && is.null(`game_object_count`)) {
        stop("Invalid value for `game_object_count` when calling AppDataApi$GetAppData, `game_object_count` is not nullable")
      }

      if (!missing(`filter`) && is.null(`filter`)) {
        stop("Invalid value for `filter` when calling AppDataApi$GetAppData, `filter` is not nullable")
      }

      if (!missing(`date_created`) && is.null(`date_created`)) {
        stop("Invalid value for `date_created` when calling AppDataApi$GetAppData, `date_created` is not nullable")
      }

      if (!missing(`owner_id`) && is.null(`owner_id`)) {
        stop("Invalid value for `owner_id` when calling AppDataApi$GetAppData, `owner_id` is not nullable")
      }

      if (!missing(`mission_ids`) && is.null(`mission_ids`)) {
        stop("Invalid value for `mission_ids` when calling AppDataApi$GetAppData, `mission_ids` is not nullable")
      }

      if (!missing(`game_ids`) && is.null(`game_ids`)) {
        stop("Invalid value for `game_ids` when calling AppDataApi$GetAppData, `game_ids` is not nullable")
      }

      if (!missing(`pack_ids`) && is.null(`pack_ids`)) {
        stop("Invalid value for `pack_ids` when calling AppDataApi$GetAppData, `pack_ids` is not nullable")
      }

      if (!missing(`game_level_ids`) && is.null(`game_level_ids`)) {
        stop("Invalid value for `game_level_ids` when calling AppDataApi$GetAppData, `game_level_ids` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling AppDataApi$GetAppData, `app_version` is not nullable")
      }

      if (!missing(`include_higher_version_packs`) && is.null(`include_higher_version_packs`)) {
        stop("Invalid value for `include_higher_version_packs` when calling AppDataApi$GetAppData, `include_higher_version_packs` is not nullable")
      }

      if (!missing(`include_higher_version_levels`) && is.null(`include_higher_version_levels`)) {
        stop("Invalid value for `include_higher_version_levels` when calling AppDataApi$GetAppData, `include_higher_version_levels` is not nullable")
      }

      if (!missing(`response_groups`) && is.null(`response_groups`)) {
        stop("Invalid value for `response_groups` when calling AppDataApi$GetAppData, `response_groups` is not nullable")
      }

      if (!missing(`purchase_type`) && is.null(`purchase_type`)) {
        stop("Invalid value for `purchase_type` when calling AppDataApi$GetAppData, `purchase_type` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["includeGameData"]] <- `include_game_data`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["gameObjectCount"]] <- `game_object_count`

      query_params[["filter"]] <- `filter`

      query_params[["dateCreated"]] <- `date_created`

      query_params[["ownerId"]] <- `owner_id`

      query_params[["missionIds"]] <- `mission_ids`

      query_params[["gameIds"]] <- `game_ids`

      query_params[["packIds"]] <- `pack_ids`

      query_params[["gameLevelIds"]] <- `game_level_ids`

      query_params[["appVersion"]] <- `app_version`

      query_params[["includeHigherVersionPacks"]] <- `include_higher_version_packs`

      query_params[["includeHigherVersionLevels"]] <- `include_higher_version_levels`

      query_params[["responseGroups"]] <- `response_groups`

      query_params[["purchaseType"]] <- `purchase_type`

      local_var_url_path <- "/app/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AppResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create App Data
    #'
    #' @param game_type the game to retrieve the data for, use your application key.
    #' @param start start the search results at a record.
    #' @param limit limit the search results to some number.
    #' @param data The analytic data AnalyticListResponse
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param include_game_data (optional) if true then include the game data blob, otherwise don't include.
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) query string used to filter the search results when looking for community and saved levels.
    #' @param sort_field (optional) how to sort the results when looking for community and saved levels. possible values are: LEVEL_NAME, LEVEL_CREATED, LEVEL_UPDATED, LEVEL_LIKES, LEVEL_DISLIKES, LEVEL_NOTES, LEVEL_PLAYS.
    #' @param descending (optional) order the search results descending or ascending when looking for community and saved levels.
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param game_object_count (optional) if true then include the game object count, otherwise don't include.
    #' @param filter (optional) restrict the search to items with certain permissions, possible values are: MINE, FRIENDS, PUBLIC, ALL_PUBLIC, ALL, LIKED
    #' @param date_created (optional) restrict the search to items created less then date
    #' @param owner_id (optional) get levels owned by a particular account
    #' @param mission_ids (optional) get missions by specified id, comman seperated list of long ids
    #' @param game_ids (optional) get games by specified id, comman seperated list of long ids
    #' @param pack_ids (optional) get packs by specified id, comman seperated list of long ids
    #' @param game_level_ids (optional) get game levels by specified id, comman seperated list of long ids
    #' @param app_version (optional) the application version, used to versin the game level data
    #' @param include_higher_version_packs (optional) default is false and will not include any packs that have levels above the specified appVersion. If true then will return all levels regardless of version.
    #' @param include_higher_version_levels (optional) default is false and will not include any levels above the specified appVersion. If true then will return all levels regardless of version.
    #' @param response_groups (optional) use response groups to include large parts of the structure. possible values are: * MISSION_DEFAULT (default) - include the default mission (built-in packs), excludes all other mission groups * MISSION_DEFAULT_LEVELS_SAVED - include saved levels, excludes all other mission groups * MISSION_DEFAULT_LEVELS_COMMUNITY - include community levels, excludes all other mission groups * MISSION_INVITED (default) - include challenges sent to user * PROFILE (default) - include entire profile * PROFILE_DATA - only include profile date (exclude friends) * PROFILE_FRIENDS - include friends list 
    #' @param purchase_type (optional) the will return the correct in app purchases for the device, possible values are: * SIRQUL (default) - purchasing from the sirqul store using tickets * IOS - purchasing from the itunes store for iPhone, iPod, iPod Touch * GOOGLE - purchasing from the google android store * AMAZON - purchasing from the amazon android store * MAC - purchasing from the itunes store for OSX * FREE - the item is free to purchase 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AppResponse
    PostAppData = function(game_type, start, limit, data, device_id = NULL, account_id = NULL, include_game_data = NULL, q = NULL, keyword = NULL, sort_field = NULL, descending = NULL, _i = NULL, _l = NULL, game_object_count = NULL, filter = NULL, date_created = NULL, owner_id = NULL, mission_ids = NULL, game_ids = NULL, pack_ids = NULL, game_level_ids = NULL, app_version = NULL, include_higher_version_packs = NULL, include_higher_version_levels = NULL, response_groups = NULL, purchase_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$PostAppDataWithHttpInfo(game_type, start, limit, data, device_id, account_id, include_game_data, q, keyword, sort_field, descending, _i, _l, game_object_count, filter, date_created, owner_id, mission_ids, game_ids, pack_ids, game_level_ids, app_version, include_higher_version_packs, include_higher_version_levels, response_groups, purchase_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create App Data
    #'
    #' @param game_type the game to retrieve the data for, use your application key.
    #' @param start start the search results at a record.
    #' @param limit limit the search results to some number.
    #' @param data The analytic data AnalyticListResponse
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param include_game_data (optional) if true then include the game data blob, otherwise don't include.
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) query string used to filter the search results when looking for community and saved levels.
    #' @param sort_field (optional) how to sort the results when looking for community and saved levels. possible values are: LEVEL_NAME, LEVEL_CREATED, LEVEL_UPDATED, LEVEL_LIKES, LEVEL_DISLIKES, LEVEL_NOTES, LEVEL_PLAYS.
    #' @param descending (optional) order the search results descending or ascending when looking for community and saved levels.
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param game_object_count (optional) if true then include the game object count, otherwise don't include.
    #' @param filter (optional) restrict the search to items with certain permissions, possible values are: MINE, FRIENDS, PUBLIC, ALL_PUBLIC, ALL, LIKED
    #' @param date_created (optional) restrict the search to items created less then date
    #' @param owner_id (optional) get levels owned by a particular account
    #' @param mission_ids (optional) get missions by specified id, comman seperated list of long ids
    #' @param game_ids (optional) get games by specified id, comman seperated list of long ids
    #' @param pack_ids (optional) get packs by specified id, comman seperated list of long ids
    #' @param game_level_ids (optional) get game levels by specified id, comman seperated list of long ids
    #' @param app_version (optional) the application version, used to versin the game level data
    #' @param include_higher_version_packs (optional) default is false and will not include any packs that have levels above the specified appVersion. If true then will return all levels regardless of version.
    #' @param include_higher_version_levels (optional) default is false and will not include any levels above the specified appVersion. If true then will return all levels regardless of version.
    #' @param response_groups (optional) use response groups to include large parts of the structure. possible values are: * MISSION_DEFAULT (default) - include the default mission (built-in packs), excludes all other mission groups * MISSION_DEFAULT_LEVELS_SAVED - include saved levels, excludes all other mission groups * MISSION_DEFAULT_LEVELS_COMMUNITY - include community levels, excludes all other mission groups * MISSION_INVITED (default) - include challenges sent to user * PROFILE (default) - include entire profile * PROFILE_DATA - only include profile date (exclude friends) * PROFILE_FRIENDS - include friends list 
    #' @param purchase_type (optional) the will return the correct in app purchases for the device, possible values are: * SIRQUL (default) - purchasing from the sirqul store using tickets * IOS - purchasing from the itunes store for iPhone, iPod, iPod Touch * GOOGLE - purchasing from the google android store * AMAZON - purchasing from the amazon android store * MAC - purchasing from the itunes store for OSX * FREE - the item is free to purchase 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AppResponse) with additional information such as HTTP status code, headers
    PostAppDataWithHttpInfo = function(game_type, start, limit, data, device_id = NULL, account_id = NULL, include_game_data = NULL, q = NULL, keyword = NULL, sort_field = NULL, descending = NULL, _i = NULL, _l = NULL, game_object_count = NULL, filter = NULL, date_created = NULL, owner_id = NULL, mission_ids = NULL, game_ids = NULL, pack_ids = NULL, game_level_ids = NULL, app_version = NULL, include_higher_version_packs = NULL, include_higher_version_levels = NULL, response_groups = NULL, purchase_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`game_type`)) {
        stop("Missing required parameter `game_type`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`data`)) {
        stop("Missing required parameter `data`.")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling AppDataApi$PostAppData, `game_type` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling AppDataApi$PostAppData, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling AppDataApi$PostAppData, `limit` is not nullable")
      }

      if (!missing(`data`) && is.null(`data`)) {
        stop("Invalid value for `data` when calling AppDataApi$PostAppData, `data` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling AppDataApi$PostAppData, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AppDataApi$PostAppData, `account_id` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling AppDataApi$PostAppData, `include_game_data` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling AppDataApi$PostAppData, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling AppDataApi$PostAppData, `keyword` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling AppDataApi$PostAppData, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling AppDataApi$PostAppData, `descending` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling AppDataApi$PostAppData, `_i` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling AppDataApi$PostAppData, `_l` is not nullable")
      }

      if (!missing(`game_object_count`) && is.null(`game_object_count`)) {
        stop("Invalid value for `game_object_count` when calling AppDataApi$PostAppData, `game_object_count` is not nullable")
      }

      if (!missing(`filter`) && is.null(`filter`)) {
        stop("Invalid value for `filter` when calling AppDataApi$PostAppData, `filter` is not nullable")
      }

      if (!missing(`date_created`) && is.null(`date_created`)) {
        stop("Invalid value for `date_created` when calling AppDataApi$PostAppData, `date_created` is not nullable")
      }

      if (!missing(`owner_id`) && is.null(`owner_id`)) {
        stop("Invalid value for `owner_id` when calling AppDataApi$PostAppData, `owner_id` is not nullable")
      }

      if (!missing(`mission_ids`) && is.null(`mission_ids`)) {
        stop("Invalid value for `mission_ids` when calling AppDataApi$PostAppData, `mission_ids` is not nullable")
      }

      if (!missing(`game_ids`) && is.null(`game_ids`)) {
        stop("Invalid value for `game_ids` when calling AppDataApi$PostAppData, `game_ids` is not nullable")
      }

      if (!missing(`pack_ids`) && is.null(`pack_ids`)) {
        stop("Invalid value for `pack_ids` when calling AppDataApi$PostAppData, `pack_ids` is not nullable")
      }

      if (!missing(`game_level_ids`) && is.null(`game_level_ids`)) {
        stop("Invalid value for `game_level_ids` when calling AppDataApi$PostAppData, `game_level_ids` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling AppDataApi$PostAppData, `app_version` is not nullable")
      }

      if (!missing(`include_higher_version_packs`) && is.null(`include_higher_version_packs`)) {
        stop("Invalid value for `include_higher_version_packs` when calling AppDataApi$PostAppData, `include_higher_version_packs` is not nullable")
      }

      if (!missing(`include_higher_version_levels`) && is.null(`include_higher_version_levels`)) {
        stop("Invalid value for `include_higher_version_levels` when calling AppDataApi$PostAppData, `include_higher_version_levels` is not nullable")
      }

      if (!missing(`response_groups`) && is.null(`response_groups`)) {
        stop("Invalid value for `response_groups` when calling AppDataApi$PostAppData, `response_groups` is not nullable")
      }

      if (!missing(`purchase_type`) && is.null(`purchase_type`)) {
        stop("Invalid value for `purchase_type` when calling AppDataApi$PostAppData, `purchase_type` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["gameType"]] <- `game_type`

      query_params[["includeGameData"]] <- `include_game_data`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["gameObjectCount"]] <- `game_object_count`

      query_params[["filter"]] <- `filter`

      query_params[["dateCreated"]] <- `date_created`

      query_params[["ownerId"]] <- `owner_id`

      query_params[["missionIds"]] <- `mission_ids`

      query_params[["gameIds"]] <- `game_ids`

      query_params[["packIds"]] <- `pack_ids`

      query_params[["gameLevelIds"]] <- `game_level_ids`

      query_params[["appVersion"]] <- `app_version`

      query_params[["includeHigherVersionPacks"]] <- `include_higher_version_packs`

      query_params[["includeHigherVersionLevels"]] <- `include_higher_version_levels`

      query_params[["responseGroups"]] <- `response_groups`

      query_params[["purchaseType"]] <- `purchase_type`

      query_params[["data"]] <- `data`

      local_var_url_path <- "/app/post"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AppResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Regenerate App Data
    #'
    #' @param account_id (optional) the account id of the user
    #' @param app_key (optional) process a specific application, if null process all apps with caches
    #' @param build_version (optional) create a specific version, if null use current version. Be careful if processing all
    #' @param api_version (optional) create a specific version, if null use current version. Be careful if processing all
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    RegenAppData = function(account_id = NULL, app_key = NULL, build_version = NULL, api_version = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RegenAppDataWithHttpInfo(account_id, app_key, build_version, api_version, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Regenerate App Data
    #'
    #' @param account_id (optional) the account id of the user
    #' @param app_key (optional) process a specific application, if null process all apps with caches
    #' @param build_version (optional) create a specific version, if null use current version. Be careful if processing all
    #' @param api_version (optional) create a specific version, if null use current version. Be careful if processing all
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    RegenAppDataWithHttpInfo = function(account_id = NULL, app_key = NULL, build_version = NULL, api_version = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling AppDataApi$RegenAppData, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling AppDataApi$RegenAppData, `app_key` is not nullable")
      }

      if (!missing(`build_version`) && is.null(`build_version`)) {
        stop("Invalid value for `build_version` when calling AppDataApi$RegenAppData, `build_version` is not nullable")
      }

      if (!missing(`api_version`) && is.null(`api_version`)) {
        stop("Invalid value for `api_version` when calling AppDataApi$RegenAppData, `api_version` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["buildVersion"]] <- `build_version`

      query_params[["apiVersion"]] <- `api_version`

      local_var_url_path <- "/app/regen"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
