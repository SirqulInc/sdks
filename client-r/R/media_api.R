#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Media operations
#' @description MediaApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateMedia  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_title <- "title_example" # character | The title (255 char limit)
#' var_barcode_type <- "barcode_type_example" # character | The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA}
#' var_no_expiration <- "no_expiration_example" # character | Overrides the expiration date so that the offer does not expire
#' var_available_limit <- 56 # integer | The limit of how many times the offer can be used by consumers
#' var_available_limit_per_user <- 56 # integer | The limit of how many times a user can used the same offer
#' var_added_limit <- 56 # integer | The limit of how many times the offer can be added to consumer wallets
#' var_view_limit <- 56 # integer | The limit of how many times the offer can be viewed
#' var_max_prints <- 56 # integer | The maximum number of times the offer can be printed
#' var_ticket_price <- 56 # integer | The cost of the offer in tickets
#' var_full_price <- 3.4 # numeric | The retail/full price cost of the offer in real currency
#' var_discount_price <- 3.4 # numeric | The cost of the offer at a discounted price (what the consumer pays)
#' var_special_offer_type <- "special_offer_type_example" # character | The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
#' var_offer_visibility <- "offer_visibility_example" # character | The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE}
#' var_active <- "active_example" # character | Sets the active flag
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Comma separated list of retailer location ids. This will assign the offer to these retailer locations. (Optional)
#' var_sub_title <- "sub_title_example" # character | The sub title (255 char limit) (Optional)
#' var_details <- "details_example" # character | The details of the media (Optional)
#' var_sub_details <- "sub_details_example" # character | A string for custom details (255 char limit) (Optional)
#' var_fine_print <- "fine_print_example" # character | The fine print of the media (Optional)
#' var_barcode_entry <- "barcode_entry_example" # character | The bar code entry string (Optional)
#' var_external_redeem_options <- "external_redeem_options_example" # character | An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer (Optional)
#' var_external_url <- "external_url_example" # character | The clickUrl of the offer (Optional)
#' var_tickets_reward_type <- "tickets_reward_type_example" # character | The type of ticket to reward, null means default type (Optional)
#' var_tickets_reward <- 56 # integer | Determines how many tickets are awarded (Optional)
#' var_activated <- 56 # integer | The date of when the offer will be visible to consumers (Optional)
#' var_expires <- 56 # integer | The date of when the offer expires (Optional)
#' var_ticket_price_type <- "ticket_price_type_example" # character | the type of ticket needed to buy offer (Optional)
#' var_show_remaining <- "show_remaining_example" # character | show how many of the media is remaining (if there is a limit) (Optional)
#' var_show_redeemed <- "show_redeemed_example" # character | show how many of the media has been redeemed (Optional)
#' var_replaced <- "replaced_example" # character | if the media has been replaced (Optional)
#' var_featured <- "featured_example" # character | if the media should be featured or not (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category ids (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Comma separated list of filter ids (Optional)
#' var_barcode_asset_id <- 56 # integer | The id of the barcode asset (Optional)
#' var_image_asset_id <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id1 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id2 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id3 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id4 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id5 <- 56 # integer | The id of the an offer asset (Optional)
#' var_publisher <- "publisher_example" # character | The maker of the item. (Optional)
#' var_redeemable_start <- 56 # integer | The redeemable start date/time of the offer. (Optional)
#' var_redeemable_end <- 56 # integer | The redeemable start date/time of the offer. (Optional)
#' var_condition_type <- "condition_type_example" # character | The condition type of the media (Optional)
#' var_isbn <- "isbn_example" # character | The ISBN id (Optional)
#' var_asin <- "asin_example" # character | The ASIN id (Optional)
#' var_catalog_numbers <- "catalog_numbers_example" # character | The list of catelog numbers, comma seperated (Optional)
#' var_parental_rating <- "parental_rating_example" # character | The parental control rating (Optional)
#' var_availability_date <- 56 # integer | The date the media is available to use (Optional)
#' var_media_type <- "media_type_example" # character | the media type (Optional)
#' var_duration <- 56 # integer | The total playing time of the media item (Optional)
#' var_author <- "author_example" # character | The created/author of the media item (Optional)
#' var_release_date <- 56 # integer | The date/time of when the media item was originally released (Optional)
#' var_collection_ids <- "collection_ids_example" # character | the IDs of the collections this media should be assigned to (Optional)
#' var_availability <- "availability_example" # character | ability to assign if this media should active or not (Optional)
#' var_availability_summary <- "availability_summary_example" # character | ability to assign when the media expires (Optional)
#'
#' #Create Media
#' api_instance <- MediaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateMedia(var_account_id, var_title, var_barcode_type, var_no_expiration, var_available_limit, var_available_limit_per_user, var_added_limit, var_view_limit, var_max_prints, var_ticket_price, var_full_price, var_discount_price, var_special_offer_type, var_offer_visibility, var_active, retailer_location_ids = var_retailer_location_ids, sub_title = var_sub_title, details = var_details, sub_details = var_sub_details, fine_print = var_fine_print, barcode_entry = var_barcode_entry, external_redeem_options = var_external_redeem_options, external_url = var_external_url, tickets_reward_type = var_tickets_reward_type, tickets_reward = var_tickets_reward, activated = var_activated, expires = var_expires, ticket_price_type = var_ticket_price_type, show_remaining = var_show_remaining, show_redeemed = var_show_redeemed, replaced = var_replaced, featured = var_featured, category_ids = var_category_ids, filter_ids = var_filter_ids, barcode_asset_id = var_barcode_asset_id, image_asset_id = var_image_asset_id, image_asset_id1 = var_image_asset_id1, image_asset_id2 = var_image_asset_id2, image_asset_id3 = var_image_asset_id3, image_asset_id4 = var_image_asset_id4, image_asset_id5 = var_image_asset_id5, publisher = var_publisher, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end, condition_type = var_condition_type, isbn = var_isbn, asin = var_asin, catalog_numbers = var_catalog_numbers, parental_rating = var_parental_rating, availability_date = var_availability_date, media_type = var_media_type, duration = var_duration, author = var_author, release_date = var_release_date, collection_ids = var_collection_ids, availability = var_availability, availability_summary = var_availability_summarydata_file = "result.txt")
#' result <- api_instance$CreateMedia(var_account_id, var_title, var_barcode_type, var_no_expiration, var_available_limit, var_available_limit_per_user, var_added_limit, var_view_limit, var_max_prints, var_ticket_price, var_full_price, var_discount_price, var_special_offer_type, var_offer_visibility, var_active, retailer_location_ids = var_retailer_location_ids, sub_title = var_sub_title, details = var_details, sub_details = var_sub_details, fine_print = var_fine_print, barcode_entry = var_barcode_entry, external_redeem_options = var_external_redeem_options, external_url = var_external_url, tickets_reward_type = var_tickets_reward_type, tickets_reward = var_tickets_reward, activated = var_activated, expires = var_expires, ticket_price_type = var_ticket_price_type, show_remaining = var_show_remaining, show_redeemed = var_show_redeemed, replaced = var_replaced, featured = var_featured, category_ids = var_category_ids, filter_ids = var_filter_ids, barcode_asset_id = var_barcode_asset_id, image_asset_id = var_image_asset_id, image_asset_id1 = var_image_asset_id1, image_asset_id2 = var_image_asset_id2, image_asset_id3 = var_image_asset_id3, image_asset_id4 = var_image_asset_id4, image_asset_id5 = var_image_asset_id5, publisher = var_publisher, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end, condition_type = var_condition_type, isbn = var_isbn, asin = var_asin, catalog_numbers = var_catalog_numbers, parental_rating = var_parental_rating, availability_date = var_availability_date, media_type = var_media_type, duration = var_duration, author = var_author, release_date = var_release_date, collection_ids = var_collection_ids, availability = var_availability, availability_summary = var_availability_summary)
#' dput(result)
#'
#'
#' ####################  DeleteMedia  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_media_id <- 56 # integer | the ID of the media to delete
#'
#' #Delete Media
#' api_instance <- MediaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteMedia(var_account_id, var_media_iddata_file = "result.txt")
#' result <- api_instance$DeleteMedia(var_account_id, var_media_id)
#' dput(result)
#'
#'
#' ####################  GetMedia  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_media_id <- 56 # integer | the id of the media to get
#'
#' #Media Get
#' api_instance <- MediaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetMedia(var_account_id, var_media_iddata_file = "result.txt")
#' result <- api_instance$GetMedia(var_account_id, var_media_id)
#' dput(result)
#'
#'
#' ####################  SearchMedia  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_active_only <- "active_only_example" # character | Return only active results
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY, AVAILABILITY_DATE, RELEASE_DATE
#' var_descending <- "descending_example" # character | The order to return the search results
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_category_ids <- "category_ids_example" # character |  (Optional)
#' var_filter_ids <- "filter_ids_example" # character |  (Optional)
#' var_start <- 56 # integer | The record to begin the return set on (Optional)
#' var_limit <- 56 # integer | The number of records to return (Optional)
#'
#' #Search Media
#' api_instance <- MediaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchMedia(var_account_id, var_active_only, var_sort_field, var_descending, keyword = var_keyword, category_ids = var_category_ids, filter_ids = var_filter_ids, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchMedia(var_account_id, var_active_only, var_sort_field, var_descending, keyword = var_keyword, category_ids = var_category_ids, filter_ids = var_filter_ids, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  UpdateMedia  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account used to perform the update, must have rights to edit the offer (deviceId or accountId required)
#' var_media_id <- 56 # integer | 
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Comma separated list of retailer location ids. This will assign the offer to these retailer locations. (Optional)
#' var_offer_locations <- "offer_locations_example" # character | A list of json data that has offer location specific values. (Optional)
#' var_title <- "title_example" # character | The title (255 char limit) (Optional)
#' var_sub_title <- "sub_title_example" # character | The sub title (255 char limit) (Optional)
#' var_details <- "details_example" # character | The details (Optional)
#' var_sub_details <- "sub_details_example" # character | A string for custom details (255 char limit) (Optional)
#' var_fine_print <- "fine_print_example" # character | The fine print (Optional)
#' var_barcode_type <- "barcode_type_example" # character | The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA} (Optional)
#' var_barcode_entry <- "barcode_entry_example" # character | The bar code entry string (Optional)
#' var_external_redeem_options <- "external_redeem_options_example" # character | An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer. (Optional)
#' var_external_url <- "external_url_example" # character | The clickUrl of the offer (Optional)
#' var_tickets_reward_type <- "tickets_reward_type_example" # character | The type of ticket to reward, null means default type (Optional)
#' var_tickets_reward <- 56 # integer | Determines how many tickets are awarded (Optional)
#' var_activated <- 56 # integer | The date of when the offer will be visible to consumers (Optional)
#' var_expires <- 56 # integer | The date of when the offer expires (Optional)
#' var_no_expiration <- "no_expiration_example" # character | Overrides the expiration date so that the offer does not expire (Optional)
#' var_available_limit <- 56 # integer | The limit of how many times the offer can be used by consumers (Optional)
#' var_available_limit_per_user <- 56 # integer | The limit of how many times a user can used the same offer (Optional)
#' var_added_limit <- 56 # integer | The limit of how many times the offer can be added to consumer wallets (Optional)
#' var_view_limit <- 56 # integer |  (Optional)
#' var_max_prints <- 56 # integer | The maximum number of times the offer can be printed (Optional)
#' var_ticket_price_type <- "ticket_price_type_example" # character | the type of ticket needed to buy the offer (Optional)
#' var_ticket_price <- 56 # integer | The cost of the offer in tickets (Optional)
#' var_full_price <- 3.4 # numeric | The retail/full price cost of the offer in real currency (Optional)
#' var_discount_price <- 3.4 # numeric | The cost of the offer at a discounted price (what the consumer pays) (Optional)
#' var_show_remaining <- "show_remaining_example" # character | The show remaining (Optional)
#' var_show_redeemed <- "show_redeemed_example" # character | The show redeemed (Optional)
#' var_replaced <- "replaced_example" # character | The replaced (Optional)
#' var_featured <- "featured_example" # character | The featured (Optional)
#' var_special_offer_type <- "special_offer_type_example" # character | The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW} (Optional)
#' var_offer_visibility <- "offer_visibility_example" # character | The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE} (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category ids (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Comma separated list of filter ids (Optional)
#' var_active <- "active_example" # character | Sets the active flag (Optional)
#' var_barcode_asset_id <- 56 # integer | The id of the barcode asset (Optional)
#' var_image_asset_id <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id1 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id2 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id3 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id4 <- 56 # integer | The id of the an offer asset (Optional)
#' var_image_asset_id5 <- 56 # integer | The id of the an offer asset (Optional)
#' var_publisher <- "publisher_example" # character | The maker of the item. (Optional)
#' var_redeemable_start <- 56 # integer | The redeemable start date/time of the offer. (Optional)
#' var_redeemable_end <- 56 # integer | The redeemable start date/time of the offer. (Optional)
#' var_condition_type <- "condition_type_example" # character | The condition. OfferType PRODUCT only. (Optional)
#' var_isbn <- "isbn_example" # character | The ISBN id. OfferType PRODUCT only. (Optional)
#' var_asin <- "asin_example" # character | The ASIN id. OfferType PRODUCT only. (Optional)
#' var_catalog_numbers <- "catalog_numbers_example" # character | The list of catelog numbers, comma seperated. OfferType PRODUCT only. (Optional)
#' var_availability_date <- 56 # integer | The date available. OfferType PRODUCT only. (Optional)
#' var_parental_rating <- "parental_rating_example" # character | The parental control rating. OfferType PRODUCT only. (Optional)
#' var_media_type <- "media_type_example" # character |  (Optional)
#' var_duration <- 56 # integer | The total playing time of the media item. OfferType MEDIA only. (Optional)
#' var_author <- "author_example" # character | The created/author of the media item. OfferType MEDIA only. (Optional)
#' var_release_date <- 56 # integer | The date/time of when the media item was originally released. OfferType MEDIA only. (Optional)
#' var_collection_ids <- "collection_ids_example" # character |  (Optional)
#' var_availability <- "availability_example" # character |  (Optional)
#' var_availability_summary <- "availability_summary_example" # character |  (Optional)
#'
#' #Update Media
#' api_instance <- MediaApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateMedia(var_account_id, var_media_id, retailer_location_ids = var_retailer_location_ids, offer_locations = var_offer_locations, title = var_title, sub_title = var_sub_title, details = var_details, sub_details = var_sub_details, fine_print = var_fine_print, barcode_type = var_barcode_type, barcode_entry = var_barcode_entry, external_redeem_options = var_external_redeem_options, external_url = var_external_url, tickets_reward_type = var_tickets_reward_type, tickets_reward = var_tickets_reward, activated = var_activated, expires = var_expires, no_expiration = var_no_expiration, available_limit = var_available_limit, available_limit_per_user = var_available_limit_per_user, added_limit = var_added_limit, view_limit = var_view_limit, max_prints = var_max_prints, ticket_price_type = var_ticket_price_type, ticket_price = var_ticket_price, full_price = var_full_price, discount_price = var_discount_price, show_remaining = var_show_remaining, show_redeemed = var_show_redeemed, replaced = var_replaced, featured = var_featured, special_offer_type = var_special_offer_type, offer_visibility = var_offer_visibility, category_ids = var_category_ids, filter_ids = var_filter_ids, active = var_active, barcode_asset_id = var_barcode_asset_id, image_asset_id = var_image_asset_id, image_asset_id1 = var_image_asset_id1, image_asset_id2 = var_image_asset_id2, image_asset_id3 = var_image_asset_id3, image_asset_id4 = var_image_asset_id4, image_asset_id5 = var_image_asset_id5, publisher = var_publisher, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end, condition_type = var_condition_type, isbn = var_isbn, asin = var_asin, catalog_numbers = var_catalog_numbers, availability_date = var_availability_date, parental_rating = var_parental_rating, media_type = var_media_type, duration = var_duration, author = var_author, release_date = var_release_date, collection_ids = var_collection_ids, availability = var_availability, availability_summary = var_availability_summarydata_file = "result.txt")
#' result <- api_instance$UpdateMedia(var_account_id, var_media_id, retailer_location_ids = var_retailer_location_ids, offer_locations = var_offer_locations, title = var_title, sub_title = var_sub_title, details = var_details, sub_details = var_sub_details, fine_print = var_fine_print, barcode_type = var_barcode_type, barcode_entry = var_barcode_entry, external_redeem_options = var_external_redeem_options, external_url = var_external_url, tickets_reward_type = var_tickets_reward_type, tickets_reward = var_tickets_reward, activated = var_activated, expires = var_expires, no_expiration = var_no_expiration, available_limit = var_available_limit, available_limit_per_user = var_available_limit_per_user, added_limit = var_added_limit, view_limit = var_view_limit, max_prints = var_max_prints, ticket_price_type = var_ticket_price_type, ticket_price = var_ticket_price, full_price = var_full_price, discount_price = var_discount_price, show_remaining = var_show_remaining, show_redeemed = var_show_redeemed, replaced = var_replaced, featured = var_featured, special_offer_type = var_special_offer_type, offer_visibility = var_offer_visibility, category_ids = var_category_ids, filter_ids = var_filter_ids, active = var_active, barcode_asset_id = var_barcode_asset_id, image_asset_id = var_image_asset_id, image_asset_id1 = var_image_asset_id1, image_asset_id2 = var_image_asset_id2, image_asset_id3 = var_image_asset_id3, image_asset_id4 = var_image_asset_id4, image_asset_id5 = var_image_asset_id5, publisher = var_publisher, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end, condition_type = var_condition_type, isbn = var_isbn, asin = var_asin, catalog_numbers = var_catalog_numbers, availability_date = var_availability_date, parental_rating = var_parental_rating, media_type = var_media_type, duration = var_duration, author = var_author, release_date = var_release_date, collection_ids = var_collection_ids, availability = var_availability, availability_summary = var_availability_summary)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
MediaApi <- R6::R6Class(
  "MediaApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new MediaApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Media
    #'
    #' @param account_id The account id of the logged in user
    #' @param title The title (255 char limit)
    #' @param barcode_type The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA}
    #' @param no_expiration Overrides the expiration date so that the offer does not expire
    #' @param available_limit The limit of how many times the offer can be used by consumers
    #' @param available_limit_per_user The limit of how many times a user can used the same offer
    #' @param added_limit The limit of how many times the offer can be added to consumer wallets
    #' @param view_limit The limit of how many times the offer can be viewed
    #' @param max_prints The maximum number of times the offer can be printed
    #' @param ticket_price The cost of the offer in tickets
    #' @param full_price The retail/full price cost of the offer in real currency
    #' @param discount_price The cost of the offer at a discounted price (what the consumer pays)
    #' @param special_offer_type The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
    #' @param offer_visibility The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE}
    #' @param active Sets the active flag
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids. This will assign the offer to these retailer locations.
    #' @param sub_title (optional) The sub title (255 char limit)
    #' @param details (optional) The details of the media
    #' @param sub_details (optional) A string for custom details (255 char limit)
    #' @param fine_print (optional) The fine print of the media
    #' @param barcode_entry (optional) The bar code entry string
    #' @param external_redeem_options (optional) An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer
    #' @param external_url (optional) The clickUrl of the offer
    #' @param tickets_reward_type (optional) The type of ticket to reward, null means default type
    #' @param tickets_reward (optional) Determines how many tickets are awarded
    #' @param activated (optional) The date of when the offer will be visible to consumers
    #' @param expires (optional) The date of when the offer expires
    #' @param ticket_price_type (optional) the type of ticket needed to buy offer
    #' @param show_remaining (optional) show how many of the media is remaining (if there is a limit)
    #' @param show_redeemed (optional) show how many of the media has been redeemed
    #' @param replaced (optional) if the media has been replaced
    #' @param featured (optional) if the media should be featured or not
    #' @param category_ids (optional) Comma separated list of category ids
    #' @param filter_ids (optional) Comma separated list of filter ids
    #' @param barcode_asset_id (optional) The id of the barcode asset
    #' @param image_asset_id (optional) The id of the an offer asset
    #' @param image_asset_id1 (optional) The id of the an offer asset
    #' @param image_asset_id2 (optional) The id of the an offer asset
    #' @param image_asset_id3 (optional) The id of the an offer asset
    #' @param image_asset_id4 (optional) The id of the an offer asset
    #' @param image_asset_id5 (optional) The id of the an offer asset
    #' @param publisher (optional) The maker of the item.
    #' @param redeemable_start (optional) The redeemable start date/time of the offer.
    #' @param redeemable_end (optional) The redeemable start date/time of the offer.
    #' @param condition_type (optional) The condition type of the media
    #' @param isbn (optional) The ISBN id
    #' @param asin (optional) The ASIN id
    #' @param catalog_numbers (optional) The list of catelog numbers, comma seperated
    #' @param parental_rating (optional) The parental control rating
    #' @param availability_date (optional) The date the media is available to use
    #' @param media_type (optional) the media type
    #' @param duration (optional) The total playing time of the media item
    #' @param author (optional) The created/author of the media item
    #' @param release_date (optional) The date/time of when the media item was originally released
    #' @param collection_ids (optional) the IDs of the collections this media should be assigned to
    #' @param availability (optional) ability to assign if this media should active or not
    #' @param availability_summary (optional) ability to assign when the media expires
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MediaOfferResponse
    CreateMedia = function(account_id, title, barcode_type, no_expiration, available_limit, available_limit_per_user, added_limit, view_limit, max_prints, ticket_price, full_price, discount_price, special_offer_type, offer_visibility, active, retailer_location_ids = NULL, sub_title = NULL, details = NULL, sub_details = NULL, fine_print = NULL, barcode_entry = NULL, external_redeem_options = NULL, external_url = NULL, tickets_reward_type = NULL, tickets_reward = NULL, activated = NULL, expires = NULL, ticket_price_type = NULL, show_remaining = NULL, show_redeemed = NULL, replaced = NULL, featured = NULL, category_ids = NULL, filter_ids = NULL, barcode_asset_id = NULL, image_asset_id = NULL, image_asset_id1 = NULL, image_asset_id2 = NULL, image_asset_id3 = NULL, image_asset_id4 = NULL, image_asset_id5 = NULL, publisher = NULL, redeemable_start = NULL, redeemable_end = NULL, condition_type = NULL, isbn = NULL, asin = NULL, catalog_numbers = NULL, parental_rating = NULL, availability_date = NULL, media_type = NULL, duration = NULL, author = NULL, release_date = NULL, collection_ids = NULL, availability = NULL, availability_summary = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateMediaWithHttpInfo(account_id, title, barcode_type, no_expiration, available_limit, available_limit_per_user, added_limit, view_limit, max_prints, ticket_price, full_price, discount_price, special_offer_type, offer_visibility, active, retailer_location_ids, sub_title, details, sub_details, fine_print, barcode_entry, external_redeem_options, external_url, tickets_reward_type, tickets_reward, activated, expires, ticket_price_type, show_remaining, show_redeemed, replaced, featured, category_ids, filter_ids, barcode_asset_id, image_asset_id, image_asset_id1, image_asset_id2, image_asset_id3, image_asset_id4, image_asset_id5, publisher, redeemable_start, redeemable_end, condition_type, isbn, asin, catalog_numbers, parental_rating, availability_date, media_type, duration, author, release_date, collection_ids, availability, availability_summary, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Media
    #'
    #' @param account_id The account id of the logged in user
    #' @param title The title (255 char limit)
    #' @param barcode_type The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA}
    #' @param no_expiration Overrides the expiration date so that the offer does not expire
    #' @param available_limit The limit of how many times the offer can be used by consumers
    #' @param available_limit_per_user The limit of how many times a user can used the same offer
    #' @param added_limit The limit of how many times the offer can be added to consumer wallets
    #' @param view_limit The limit of how many times the offer can be viewed
    #' @param max_prints The maximum number of times the offer can be printed
    #' @param ticket_price The cost of the offer in tickets
    #' @param full_price The retail/full price cost of the offer in real currency
    #' @param discount_price The cost of the offer at a discounted price (what the consumer pays)
    #' @param special_offer_type The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
    #' @param offer_visibility The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE}
    #' @param active Sets the active flag
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids. This will assign the offer to these retailer locations.
    #' @param sub_title (optional) The sub title (255 char limit)
    #' @param details (optional) The details of the media
    #' @param sub_details (optional) A string for custom details (255 char limit)
    #' @param fine_print (optional) The fine print of the media
    #' @param barcode_entry (optional) The bar code entry string
    #' @param external_redeem_options (optional) An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer
    #' @param external_url (optional) The clickUrl of the offer
    #' @param tickets_reward_type (optional) The type of ticket to reward, null means default type
    #' @param tickets_reward (optional) Determines how many tickets are awarded
    #' @param activated (optional) The date of when the offer will be visible to consumers
    #' @param expires (optional) The date of when the offer expires
    #' @param ticket_price_type (optional) the type of ticket needed to buy offer
    #' @param show_remaining (optional) show how many of the media is remaining (if there is a limit)
    #' @param show_redeemed (optional) show how many of the media has been redeemed
    #' @param replaced (optional) if the media has been replaced
    #' @param featured (optional) if the media should be featured or not
    #' @param category_ids (optional) Comma separated list of category ids
    #' @param filter_ids (optional) Comma separated list of filter ids
    #' @param barcode_asset_id (optional) The id of the barcode asset
    #' @param image_asset_id (optional) The id of the an offer asset
    #' @param image_asset_id1 (optional) The id of the an offer asset
    #' @param image_asset_id2 (optional) The id of the an offer asset
    #' @param image_asset_id3 (optional) The id of the an offer asset
    #' @param image_asset_id4 (optional) The id of the an offer asset
    #' @param image_asset_id5 (optional) The id of the an offer asset
    #' @param publisher (optional) The maker of the item.
    #' @param redeemable_start (optional) The redeemable start date/time of the offer.
    #' @param redeemable_end (optional) The redeemable start date/time of the offer.
    #' @param condition_type (optional) The condition type of the media
    #' @param isbn (optional) The ISBN id
    #' @param asin (optional) The ASIN id
    #' @param catalog_numbers (optional) The list of catelog numbers, comma seperated
    #' @param parental_rating (optional) The parental control rating
    #' @param availability_date (optional) The date the media is available to use
    #' @param media_type (optional) the media type
    #' @param duration (optional) The total playing time of the media item
    #' @param author (optional) The created/author of the media item
    #' @param release_date (optional) The date/time of when the media item was originally released
    #' @param collection_ids (optional) the IDs of the collections this media should be assigned to
    #' @param availability (optional) ability to assign if this media should active or not
    #' @param availability_summary (optional) ability to assign when the media expires
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MediaOfferResponse) with additional information such as HTTP status code, headers
    CreateMediaWithHttpInfo = function(account_id, title, barcode_type, no_expiration, available_limit, available_limit_per_user, added_limit, view_limit, max_prints, ticket_price, full_price, discount_price, special_offer_type, offer_visibility, active, retailer_location_ids = NULL, sub_title = NULL, details = NULL, sub_details = NULL, fine_print = NULL, barcode_entry = NULL, external_redeem_options = NULL, external_url = NULL, tickets_reward_type = NULL, tickets_reward = NULL, activated = NULL, expires = NULL, ticket_price_type = NULL, show_remaining = NULL, show_redeemed = NULL, replaced = NULL, featured = NULL, category_ids = NULL, filter_ids = NULL, barcode_asset_id = NULL, image_asset_id = NULL, image_asset_id1 = NULL, image_asset_id2 = NULL, image_asset_id3 = NULL, image_asset_id4 = NULL, image_asset_id5 = NULL, publisher = NULL, redeemable_start = NULL, redeemable_end = NULL, condition_type = NULL, isbn = NULL, asin = NULL, catalog_numbers = NULL, parental_rating = NULL, availability_date = NULL, media_type = NULL, duration = NULL, author = NULL, release_date = NULL, collection_ids = NULL, availability = NULL, availability_summary = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`title`)) {
        stop("Missing required parameter `title`.")
      }

      if (missing(`barcode_type`)) {
        stop("Missing required parameter `barcode_type`.")
      }

      if (missing(`no_expiration`)) {
        stop("Missing required parameter `no_expiration`.")
      }

      if (missing(`available_limit`)) {
        stop("Missing required parameter `available_limit`.")
      }

      if (missing(`available_limit_per_user`)) {
        stop("Missing required parameter `available_limit_per_user`.")
      }

      if (missing(`added_limit`)) {
        stop("Missing required parameter `added_limit`.")
      }

      if (missing(`view_limit`)) {
        stop("Missing required parameter `view_limit`.")
      }

      if (missing(`max_prints`)) {
        stop("Missing required parameter `max_prints`.")
      }

      if (missing(`ticket_price`)) {
        stop("Missing required parameter `ticket_price`.")
      }

      if (missing(`full_price`)) {
        stop("Missing required parameter `full_price`.")
      }

      if (missing(`discount_price`)) {
        stop("Missing required parameter `discount_price`.")
      }

      if (missing(`special_offer_type`)) {
        stop("Missing required parameter `special_offer_type`.")
      }

      if (missing(`offer_visibility`)) {
        stop("Missing required parameter `offer_visibility`.")
      }

      if (missing(`active`)) {
        stop("Missing required parameter `active`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MediaApi$CreateMedia, `account_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling MediaApi$CreateMedia, `title` is not nullable")
      }

      if (!missing(`barcode_type`) && is.null(`barcode_type`)) {
        stop("Invalid value for `barcode_type` when calling MediaApi$CreateMedia, `barcode_type` is not nullable")
      }

      if (!missing(`no_expiration`) && is.null(`no_expiration`)) {
        stop("Invalid value for `no_expiration` when calling MediaApi$CreateMedia, `no_expiration` is not nullable")
      }

      if (!missing(`available_limit`) && is.null(`available_limit`)) {
        stop("Invalid value for `available_limit` when calling MediaApi$CreateMedia, `available_limit` is not nullable")
      }

      if (!missing(`available_limit_per_user`) && is.null(`available_limit_per_user`)) {
        stop("Invalid value for `available_limit_per_user` when calling MediaApi$CreateMedia, `available_limit_per_user` is not nullable")
      }

      if (!missing(`added_limit`) && is.null(`added_limit`)) {
        stop("Invalid value for `added_limit` when calling MediaApi$CreateMedia, `added_limit` is not nullable")
      }

      if (!missing(`view_limit`) && is.null(`view_limit`)) {
        stop("Invalid value for `view_limit` when calling MediaApi$CreateMedia, `view_limit` is not nullable")
      }

      if (!missing(`max_prints`) && is.null(`max_prints`)) {
        stop("Invalid value for `max_prints` when calling MediaApi$CreateMedia, `max_prints` is not nullable")
      }

      if (!missing(`ticket_price`) && is.null(`ticket_price`)) {
        stop("Invalid value for `ticket_price` when calling MediaApi$CreateMedia, `ticket_price` is not nullable")
      }

      if (!missing(`full_price`) && is.null(`full_price`)) {
        stop("Invalid value for `full_price` when calling MediaApi$CreateMedia, `full_price` is not nullable")
      }

      if (!missing(`discount_price`) && is.null(`discount_price`)) {
        stop("Invalid value for `discount_price` when calling MediaApi$CreateMedia, `discount_price` is not nullable")
      }

      if (!missing(`special_offer_type`) && is.null(`special_offer_type`)) {
        stop("Invalid value for `special_offer_type` when calling MediaApi$CreateMedia, `special_offer_type` is not nullable")
      }

      if (!missing(`offer_visibility`) && is.null(`offer_visibility`)) {
        stop("Invalid value for `offer_visibility` when calling MediaApi$CreateMedia, `offer_visibility` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling MediaApi$CreateMedia, `active` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling MediaApi$CreateMedia, `retailer_location_ids` is not nullable")
      }

      if (!missing(`sub_title`) && is.null(`sub_title`)) {
        stop("Invalid value for `sub_title` when calling MediaApi$CreateMedia, `sub_title` is not nullable")
      }

      if (!missing(`details`) && is.null(`details`)) {
        stop("Invalid value for `details` when calling MediaApi$CreateMedia, `details` is not nullable")
      }

      if (!missing(`sub_details`) && is.null(`sub_details`)) {
        stop("Invalid value for `sub_details` when calling MediaApi$CreateMedia, `sub_details` is not nullable")
      }

      if (!missing(`fine_print`) && is.null(`fine_print`)) {
        stop("Invalid value for `fine_print` when calling MediaApi$CreateMedia, `fine_print` is not nullable")
      }

      if (!missing(`barcode_entry`) && is.null(`barcode_entry`)) {
        stop("Invalid value for `barcode_entry` when calling MediaApi$CreateMedia, `barcode_entry` is not nullable")
      }

      if (!missing(`external_redeem_options`) && is.null(`external_redeem_options`)) {
        stop("Invalid value for `external_redeem_options` when calling MediaApi$CreateMedia, `external_redeem_options` is not nullable")
      }

      if (!missing(`external_url`) && is.null(`external_url`)) {
        stop("Invalid value for `external_url` when calling MediaApi$CreateMedia, `external_url` is not nullable")
      }

      if (!missing(`tickets_reward_type`) && is.null(`tickets_reward_type`)) {
        stop("Invalid value for `tickets_reward_type` when calling MediaApi$CreateMedia, `tickets_reward_type` is not nullable")
      }

      if (!missing(`tickets_reward`) && is.null(`tickets_reward`)) {
        stop("Invalid value for `tickets_reward` when calling MediaApi$CreateMedia, `tickets_reward` is not nullable")
      }

      if (!missing(`activated`) && is.null(`activated`)) {
        stop("Invalid value for `activated` when calling MediaApi$CreateMedia, `activated` is not nullable")
      }

      if (!missing(`expires`) && is.null(`expires`)) {
        stop("Invalid value for `expires` when calling MediaApi$CreateMedia, `expires` is not nullable")
      }

      if (!missing(`ticket_price_type`) && is.null(`ticket_price_type`)) {
        stop("Invalid value for `ticket_price_type` when calling MediaApi$CreateMedia, `ticket_price_type` is not nullable")
      }

      if (!missing(`show_remaining`) && is.null(`show_remaining`)) {
        stop("Invalid value for `show_remaining` when calling MediaApi$CreateMedia, `show_remaining` is not nullable")
      }

      if (!missing(`show_redeemed`) && is.null(`show_redeemed`)) {
        stop("Invalid value for `show_redeemed` when calling MediaApi$CreateMedia, `show_redeemed` is not nullable")
      }

      if (!missing(`replaced`) && is.null(`replaced`)) {
        stop("Invalid value for `replaced` when calling MediaApi$CreateMedia, `replaced` is not nullable")
      }

      if (!missing(`featured`) && is.null(`featured`)) {
        stop("Invalid value for `featured` when calling MediaApi$CreateMedia, `featured` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling MediaApi$CreateMedia, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling MediaApi$CreateMedia, `filter_ids` is not nullable")
      }

      if (!missing(`barcode_asset_id`) && is.null(`barcode_asset_id`)) {
        stop("Invalid value for `barcode_asset_id` when calling MediaApi$CreateMedia, `barcode_asset_id` is not nullable")
      }

      if (!missing(`image_asset_id`) && is.null(`image_asset_id`)) {
        stop("Invalid value for `image_asset_id` when calling MediaApi$CreateMedia, `image_asset_id` is not nullable")
      }

      if (!missing(`image_asset_id1`) && is.null(`image_asset_id1`)) {
        stop("Invalid value for `image_asset_id1` when calling MediaApi$CreateMedia, `image_asset_id1` is not nullable")
      }

      if (!missing(`image_asset_id2`) && is.null(`image_asset_id2`)) {
        stop("Invalid value for `image_asset_id2` when calling MediaApi$CreateMedia, `image_asset_id2` is not nullable")
      }

      if (!missing(`image_asset_id3`) && is.null(`image_asset_id3`)) {
        stop("Invalid value for `image_asset_id3` when calling MediaApi$CreateMedia, `image_asset_id3` is not nullable")
      }

      if (!missing(`image_asset_id4`) && is.null(`image_asset_id4`)) {
        stop("Invalid value for `image_asset_id4` when calling MediaApi$CreateMedia, `image_asset_id4` is not nullable")
      }

      if (!missing(`image_asset_id5`) && is.null(`image_asset_id5`)) {
        stop("Invalid value for `image_asset_id5` when calling MediaApi$CreateMedia, `image_asset_id5` is not nullable")
      }

      if (!missing(`publisher`) && is.null(`publisher`)) {
        stop("Invalid value for `publisher` when calling MediaApi$CreateMedia, `publisher` is not nullable")
      }

      if (!missing(`redeemable_start`) && is.null(`redeemable_start`)) {
        stop("Invalid value for `redeemable_start` when calling MediaApi$CreateMedia, `redeemable_start` is not nullable")
      }

      if (!missing(`redeemable_end`) && is.null(`redeemable_end`)) {
        stop("Invalid value for `redeemable_end` when calling MediaApi$CreateMedia, `redeemable_end` is not nullable")
      }

      if (!missing(`condition_type`) && is.null(`condition_type`)) {
        stop("Invalid value for `condition_type` when calling MediaApi$CreateMedia, `condition_type` is not nullable")
      }

      if (!missing(`isbn`) && is.null(`isbn`)) {
        stop("Invalid value for `isbn` when calling MediaApi$CreateMedia, `isbn` is not nullable")
      }

      if (!missing(`asin`) && is.null(`asin`)) {
        stop("Invalid value for `asin` when calling MediaApi$CreateMedia, `asin` is not nullable")
      }

      if (!missing(`catalog_numbers`) && is.null(`catalog_numbers`)) {
        stop("Invalid value for `catalog_numbers` when calling MediaApi$CreateMedia, `catalog_numbers` is not nullable")
      }

      if (!missing(`parental_rating`) && is.null(`parental_rating`)) {
        stop("Invalid value for `parental_rating` when calling MediaApi$CreateMedia, `parental_rating` is not nullable")
      }

      if (!missing(`availability_date`) && is.null(`availability_date`)) {
        stop("Invalid value for `availability_date` when calling MediaApi$CreateMedia, `availability_date` is not nullable")
      }

      if (!missing(`media_type`) && is.null(`media_type`)) {
        stop("Invalid value for `media_type` when calling MediaApi$CreateMedia, `media_type` is not nullable")
      }

      if (!missing(`duration`) && is.null(`duration`)) {
        stop("Invalid value for `duration` when calling MediaApi$CreateMedia, `duration` is not nullable")
      }

      if (!missing(`author`) && is.null(`author`)) {
        stop("Invalid value for `author` when calling MediaApi$CreateMedia, `author` is not nullable")
      }

      if (!missing(`release_date`) && is.null(`release_date`)) {
        stop("Invalid value for `release_date` when calling MediaApi$CreateMedia, `release_date` is not nullable")
      }

      if (!missing(`collection_ids`) && is.null(`collection_ids`)) {
        stop("Invalid value for `collection_ids` when calling MediaApi$CreateMedia, `collection_ids` is not nullable")
      }

      if (!missing(`availability`) && is.null(`availability`)) {
        stop("Invalid value for `availability` when calling MediaApi$CreateMedia, `availability` is not nullable")
      }

      if (!missing(`availability_summary`) && is.null(`availability_summary`)) {
        stop("Invalid value for `availability_summary` when calling MediaApi$CreateMedia, `availability_summary` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["title"]] <- `title`

      query_params[["subTitle"]] <- `sub_title`

      query_params[["details"]] <- `details`

      query_params[["subDetails"]] <- `sub_details`

      query_params[["finePrint"]] <- `fine_print`

      if (!is.null(`barcode_type`) && !(`barcode_type` %in% c("NONE", "UPC", "CODE_128", "QR", "CUSTOM_MEDIA"))) {
        stop("Invalid value for barcode_type when calling MediaApi$CreateMedia. Must be [NONE, UPC, CODE_128, QR, CUSTOM_MEDIA].")
      }
      query_params[["barcodeType"]] <- `barcode_type`

      query_params[["barcodeEntry"]] <- `barcode_entry`

      query_params[["externalRedeemOptions"]] <- `external_redeem_options`

      query_params[["externalUrl"]] <- `external_url`

      query_params[["ticketsRewardType"]] <- `tickets_reward_type`

      query_params[["ticketsReward"]] <- `tickets_reward`

      query_params[["activated"]] <- `activated`

      query_params[["expires"]] <- `expires`

      query_params[["noExpiration"]] <- `no_expiration`

      query_params[["availableLimit"]] <- `available_limit`

      query_params[["availableLimitPerUser"]] <- `available_limit_per_user`

      query_params[["addedLimit"]] <- `added_limit`

      query_params[["viewLimit"]] <- `view_limit`

      query_params[["maxPrints"]] <- `max_prints`

      query_params[["ticketPriceType"]] <- `ticket_price_type`

      query_params[["ticketPrice"]] <- `ticket_price`

      query_params[["fullPrice"]] <- `full_price`

      query_params[["discountPrice"]] <- `discount_price`

      query_params[["showRemaining"]] <- `show_remaining`

      query_params[["showRedeemed"]] <- `show_redeemed`

      query_params[["replaced"]] <- `replaced`

      query_params[["featured"]] <- `featured`

      if (!is.null(`special_offer_type`) && !(`special_offer_type` %in% c("ALL", "RESERVABLE", "REGULAR_OFFER", "ACT_NOW", "GET_THERE_NOW", "SQOOT", "TICKETS", "YIPIT"))) {
        stop("Invalid value for special_offer_type when calling MediaApi$CreateMedia. Must be [ALL, RESERVABLE, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW, SQOOT, TICKETS, YIPIT].")
      }
      query_params[["specialOfferType"]] <- `special_offer_type`

      if (!is.null(`offer_visibility`) && !(`offer_visibility` %in% c("PUBLIC", "LISTABLE", "REWARDABLE", "TRIGGERABLE", "PRIVATE"))) {
        stop("Invalid value for offer_visibility when calling MediaApi$CreateMedia. Must be [PUBLIC, LISTABLE, REWARDABLE, TRIGGERABLE, PRIVATE].")
      }
      query_params[["offerVisibility"]] <- `offer_visibility`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["active"]] <- `active`

      query_params[["barcodeAssetId"]] <- `barcode_asset_id`

      query_params[["imageAssetId"]] <- `image_asset_id`

      query_params[["imageAssetId1"]] <- `image_asset_id1`

      query_params[["imageAssetId2"]] <- `image_asset_id2`

      query_params[["imageAssetId3"]] <- `image_asset_id3`

      query_params[["imageAssetId4"]] <- `image_asset_id4`

      query_params[["imageAssetId5"]] <- `image_asset_id5`

      query_params[["publisher"]] <- `publisher`

      query_params[["redeemableStart"]] <- `redeemable_start`

      query_params[["redeemableEnd"]] <- `redeemable_end`

      if (!is.null(`condition_type`) && !(`condition_type` %in% c("NEW", "USED", "REFURBISHED", "OTHER"))) {
        stop("Invalid value for condition_type when calling MediaApi$CreateMedia. Must be [NEW, USED, REFURBISHED, OTHER].")
      }
      query_params[["conditionType"]] <- `condition_type`

      query_params[["isbn"]] <- `isbn`

      query_params[["asin"]] <- `asin`

      query_params[["catalogNumbers"]] <- `catalog_numbers`

      query_params[["parentalRating"]] <- `parental_rating`

      query_params[["availabilityDate"]] <- `availability_date`

      if (!is.null(`media_type`) && !(`media_type` %in% c("APK", "APPLICATION", "IMAGE", "AUDIO", "VIDEO", "MULTIPART", "BAR_CODE", "TEXT", "OTHER"))) {
        stop("Invalid value for media_type when calling MediaApi$CreateMedia. Must be [APK, APPLICATION, IMAGE, AUDIO, VIDEO, MULTIPART, BAR_CODE, TEXT, OTHER].")
      }
      query_params[["mediaType"]] <- `media_type`

      query_params[["duration"]] <- `duration`

      query_params[["author"]] <- `author`

      query_params[["releaseDate"]] <- `release_date`

      query_params[["collectionIds"]] <- `collection_ids`

      query_params[["availability"]] <- `availability`

      query_params[["availabilitySummary"]] <- `availability_summary`

      local_var_url_path <- "/media/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MediaOfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Media
    #'
    #' @param account_id the id of the logged in user
    #' @param media_id the ID of the media to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteMedia = function(account_id, media_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteMediaWithHttpInfo(account_id, media_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Media
    #'
    #' @param account_id the id of the logged in user
    #' @param media_id the ID of the media to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteMediaWithHttpInfo = function(account_id, media_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`media_id`)) {
        stop("Missing required parameter `media_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MediaApi$DeleteMedia, `account_id` is not nullable")
      }

      if (!missing(`media_id`) && is.null(`media_id`)) {
        stop("Invalid value for `media_id` when calling MediaApi$DeleteMedia, `media_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["mediaId"]] <- `media_id`

      local_var_url_path <- "/media/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Media Get
    #'
    #' @param account_id the id of the logged in user
    #' @param media_id the id of the media to get
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MediaOfferResponse
    GetMedia = function(account_id, media_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetMediaWithHttpInfo(account_id, media_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Media Get
    #'
    #' @param account_id the id of the logged in user
    #' @param media_id the id of the media to get
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MediaOfferResponse) with additional information such as HTTP status code, headers
    GetMediaWithHttpInfo = function(account_id, media_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`media_id`)) {
        stop("Missing required parameter `media_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MediaApi$GetMedia, `account_id` is not nullable")
      }

      if (!missing(`media_id`) && is.null(`media_id`)) {
        stop("Invalid value for `media_id` when calling MediaApi$GetMedia, `media_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["mediaId"]] <- `media_id`

      local_var_url_path <- "/media/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MediaOfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Media
    #'
    #' @param account_id The logged in user.
    #' @param active_only Return only active results
    #' @param sort_field The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY, AVAILABILITY_DATE, RELEASE_DATE
    #' @param descending The order to return the search results
    #' @param keyword (optional) The keyword used to search
    #' @param category_ids (optional) 
    #' @param filter_ids (optional) 
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[MediaOfferResponse]
    SearchMedia = function(account_id, active_only, sort_field, descending, keyword = NULL, category_ids = NULL, filter_ids = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchMediaWithHttpInfo(account_id, active_only, sort_field, descending, keyword, category_ids, filter_ids, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Media
    #'
    #' @param account_id The logged in user.
    #' @param active_only Return only active results
    #' @param sort_field The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY, AVAILABILITY_DATE, RELEASE_DATE
    #' @param descending The order to return the search results
    #' @param keyword (optional) The keyword used to search
    #' @param category_ids (optional) 
    #' @param filter_ids (optional) 
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[MediaOfferResponse]) with additional information such as HTTP status code, headers
    SearchMediaWithHttpInfo = function(account_id, active_only, sort_field, descending, keyword = NULL, category_ids = NULL, filter_ids = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`active_only`)) {
        stop("Missing required parameter `active_only`.")
      }

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MediaApi$SearchMedia, `account_id` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling MediaApi$SearchMedia, `active_only` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling MediaApi$SearchMedia, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling MediaApi$SearchMedia, `descending` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling MediaApi$SearchMedia, `keyword` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling MediaApi$SearchMedia, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling MediaApi$SearchMedia, `filter_ids` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling MediaApi$SearchMedia, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling MediaApi$SearchMedia, `limit` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "ACTIVATED", "EXPIRES", "REDEEMABLE_START", "REDEEMABLE_END", "TITLE", "SUBTITLE", "DETAILS", "OFFER_TYPE", "SPECIAL_OFFER_TYPE", "OFFER_VISIBILITY", "ESTIMATED_VALUE", "VOUCHER_PRICE", "VOUCHER_DISCOUNT_PRICE", "FULL_PRICE", "DICOUNT_PRICE", "TICKETS_REWARD", "AVAILABILITY_DATE", "RELEASE_DATE", "RETAILER_ID", "RETAILER_NAME", "RETAILER_LOCATION_ID", "RETAILER_LOCATION_NAME", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY"))) {
        stop("Invalid value for sort_field when calling MediaApi$SearchMedia. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, REDEEMABLE_START, REDEEMABLE_END, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, VOUCHER_DISCOUNT_PRICE, FULL_PRICE, DICOUNT_PRICE, TICKETS_REWARD, AVAILABILITY_DATE, RELEASE_DATE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/media/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[MediaOfferResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Media
    #'
    #' @param account_id The account used to perform the update, must have rights to edit the offer (deviceId or accountId required)
    #' @param media_id 
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids. This will assign the offer to these retailer locations.
    #' @param offer_locations (optional) A list of json data that has offer location specific values.
    #' @param title (optional) The title (255 char limit)
    #' @param sub_title (optional) The sub title (255 char limit)
    #' @param details (optional) The details
    #' @param sub_details (optional) A string for custom details (255 char limit)
    #' @param fine_print (optional) The fine print
    #' @param barcode_type (optional) The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA}
    #' @param barcode_entry (optional) The bar code entry string
    #' @param external_redeem_options (optional) An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer.
    #' @param external_url (optional) The clickUrl of the offer
    #' @param tickets_reward_type (optional) The type of ticket to reward, null means default type
    #' @param tickets_reward (optional) Determines how many tickets are awarded
    #' @param activated (optional) The date of when the offer will be visible to consumers
    #' @param expires (optional) The date of when the offer expires
    #' @param no_expiration (optional) Overrides the expiration date so that the offer does not expire
    #' @param available_limit (optional) The limit of how many times the offer can be used by consumers
    #' @param available_limit_per_user (optional) The limit of how many times a user can used the same offer
    #' @param added_limit (optional) The limit of how many times the offer can be added to consumer wallets
    #' @param view_limit (optional) 
    #' @param max_prints (optional) The maximum number of times the offer can be printed
    #' @param ticket_price_type (optional) the type of ticket needed to buy the offer
    #' @param ticket_price (optional) The cost of the offer in tickets
    #' @param full_price (optional) The retail/full price cost of the offer in real currency
    #' @param discount_price (optional) The cost of the offer at a discounted price (what the consumer pays)
    #' @param show_remaining (optional) The show remaining
    #' @param show_redeemed (optional) The show redeemed
    #' @param replaced (optional) The replaced
    #' @param featured (optional) The featured
    #' @param special_offer_type (optional) The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
    #' @param offer_visibility (optional) The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE}
    #' @param category_ids (optional) Comma separated list of category ids
    #' @param filter_ids (optional) Comma separated list of filter ids
    #' @param active (optional) Sets the active flag
    #' @param barcode_asset_id (optional) The id of the barcode asset
    #' @param image_asset_id (optional) The id of the an offer asset
    #' @param image_asset_id1 (optional) The id of the an offer asset
    #' @param image_asset_id2 (optional) The id of the an offer asset
    #' @param image_asset_id3 (optional) The id of the an offer asset
    #' @param image_asset_id4 (optional) The id of the an offer asset
    #' @param image_asset_id5 (optional) The id of the an offer asset
    #' @param publisher (optional) The maker of the item.
    #' @param redeemable_start (optional) The redeemable start date/time of the offer.
    #' @param redeemable_end (optional) The redeemable start date/time of the offer.
    #' @param condition_type (optional) The condition. OfferType PRODUCT only.
    #' @param isbn (optional) The ISBN id. OfferType PRODUCT only.
    #' @param asin (optional) The ASIN id. OfferType PRODUCT only.
    #' @param catalog_numbers (optional) The list of catelog numbers, comma seperated. OfferType PRODUCT only.
    #' @param availability_date (optional) The date available. OfferType PRODUCT only.
    #' @param parental_rating (optional) The parental control rating. OfferType PRODUCT only.
    #' @param media_type (optional) 
    #' @param duration (optional) The total playing time of the media item. OfferType MEDIA only.
    #' @param author (optional) The created/author of the media item. OfferType MEDIA only.
    #' @param release_date (optional) The date/time of when the media item was originally released. OfferType MEDIA only.
    #' @param collection_ids (optional) 
    #' @param availability (optional) 
    #' @param availability_summary (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MediaOfferResponse
    UpdateMedia = function(account_id, media_id, retailer_location_ids = NULL, offer_locations = NULL, title = NULL, sub_title = NULL, details = NULL, sub_details = NULL, fine_print = NULL, barcode_type = NULL, barcode_entry = NULL, external_redeem_options = NULL, external_url = NULL, tickets_reward_type = NULL, tickets_reward = NULL, activated = NULL, expires = NULL, no_expiration = NULL, available_limit = NULL, available_limit_per_user = NULL, added_limit = NULL, view_limit = NULL, max_prints = NULL, ticket_price_type = NULL, ticket_price = NULL, full_price = NULL, discount_price = NULL, show_remaining = NULL, show_redeemed = NULL, replaced = NULL, featured = NULL, special_offer_type = NULL, offer_visibility = NULL, category_ids = NULL, filter_ids = NULL, active = NULL, barcode_asset_id = NULL, image_asset_id = NULL, image_asset_id1 = NULL, image_asset_id2 = NULL, image_asset_id3 = NULL, image_asset_id4 = NULL, image_asset_id5 = NULL, publisher = NULL, redeemable_start = NULL, redeemable_end = NULL, condition_type = NULL, isbn = NULL, asin = NULL, catalog_numbers = NULL, availability_date = NULL, parental_rating = NULL, media_type = NULL, duration = NULL, author = NULL, release_date = NULL, collection_ids = NULL, availability = NULL, availability_summary = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateMediaWithHttpInfo(account_id, media_id, retailer_location_ids, offer_locations, title, sub_title, details, sub_details, fine_print, barcode_type, barcode_entry, external_redeem_options, external_url, tickets_reward_type, tickets_reward, activated, expires, no_expiration, available_limit, available_limit_per_user, added_limit, view_limit, max_prints, ticket_price_type, ticket_price, full_price, discount_price, show_remaining, show_redeemed, replaced, featured, special_offer_type, offer_visibility, category_ids, filter_ids, active, barcode_asset_id, image_asset_id, image_asset_id1, image_asset_id2, image_asset_id3, image_asset_id4, image_asset_id5, publisher, redeemable_start, redeemable_end, condition_type, isbn, asin, catalog_numbers, availability_date, parental_rating, media_type, duration, author, release_date, collection_ids, availability, availability_summary, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Media
    #'
    #' @param account_id The account used to perform the update, must have rights to edit the offer (deviceId or accountId required)
    #' @param media_id 
    #' @param retailer_location_ids (optional) Comma separated list of retailer location ids. This will assign the offer to these retailer locations.
    #' @param offer_locations (optional) A list of json data that has offer location specific values.
    #' @param title (optional) The title (255 char limit)
    #' @param sub_title (optional) The sub title (255 char limit)
    #' @param details (optional) The details
    #' @param sub_details (optional) A string for custom details (255 char limit)
    #' @param fine_print (optional) The fine print
    #' @param barcode_type (optional) The bar code type {NONE, UPC, CODE_128, QR, CUSTOM_MEDIA}
    #' @param barcode_entry (optional) The bar code entry string
    #' @param external_redeem_options (optional) An external identifier. What the identifier will be used for and how it is formatted is up to the frontend developer.
    #' @param external_url (optional) The clickUrl of the offer
    #' @param tickets_reward_type (optional) The type of ticket to reward, null means default type
    #' @param tickets_reward (optional) Determines how many tickets are awarded
    #' @param activated (optional) The date of when the offer will be visible to consumers
    #' @param expires (optional) The date of when the offer expires
    #' @param no_expiration (optional) Overrides the expiration date so that the offer does not expire
    #' @param available_limit (optional) The limit of how many times the offer can be used by consumers
    #' @param available_limit_per_user (optional) The limit of how many times a user can used the same offer
    #' @param added_limit (optional) The limit of how many times the offer can be added to consumer wallets
    #' @param view_limit (optional) 
    #' @param max_prints (optional) The maximum number of times the offer can be printed
    #' @param ticket_price_type (optional) the type of ticket needed to buy the offer
    #' @param ticket_price (optional) The cost of the offer in tickets
    #' @param full_price (optional) The retail/full price cost of the offer in real currency
    #' @param discount_price (optional) The cost of the offer at a discounted price (what the consumer pays)
    #' @param show_remaining (optional) The show remaining
    #' @param show_redeemed (optional) The show redeemed
    #' @param replaced (optional) The replaced
    #' @param featured (optional) The featured
    #' @param special_offer_type (optional) The special offer type {ALL, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW}
    #' @param offer_visibility (optional) The offer visibility {PUBLIC, REWARDABLE, TRIGGERABLE}
    #' @param category_ids (optional) Comma separated list of category ids
    #' @param filter_ids (optional) Comma separated list of filter ids
    #' @param active (optional) Sets the active flag
    #' @param barcode_asset_id (optional) The id of the barcode asset
    #' @param image_asset_id (optional) The id of the an offer asset
    #' @param image_asset_id1 (optional) The id of the an offer asset
    #' @param image_asset_id2 (optional) The id of the an offer asset
    #' @param image_asset_id3 (optional) The id of the an offer asset
    #' @param image_asset_id4 (optional) The id of the an offer asset
    #' @param image_asset_id5 (optional) The id of the an offer asset
    #' @param publisher (optional) The maker of the item.
    #' @param redeemable_start (optional) The redeemable start date/time of the offer.
    #' @param redeemable_end (optional) The redeemable start date/time of the offer.
    #' @param condition_type (optional) The condition. OfferType PRODUCT only.
    #' @param isbn (optional) The ISBN id. OfferType PRODUCT only.
    #' @param asin (optional) The ASIN id. OfferType PRODUCT only.
    #' @param catalog_numbers (optional) The list of catelog numbers, comma seperated. OfferType PRODUCT only.
    #' @param availability_date (optional) The date available. OfferType PRODUCT only.
    #' @param parental_rating (optional) The parental control rating. OfferType PRODUCT only.
    #' @param media_type (optional) 
    #' @param duration (optional) The total playing time of the media item. OfferType MEDIA only.
    #' @param author (optional) The created/author of the media item. OfferType MEDIA only.
    #' @param release_date (optional) The date/time of when the media item was originally released. OfferType MEDIA only.
    #' @param collection_ids (optional) 
    #' @param availability (optional) 
    #' @param availability_summary (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MediaOfferResponse) with additional information such as HTTP status code, headers
    UpdateMediaWithHttpInfo = function(account_id, media_id, retailer_location_ids = NULL, offer_locations = NULL, title = NULL, sub_title = NULL, details = NULL, sub_details = NULL, fine_print = NULL, barcode_type = NULL, barcode_entry = NULL, external_redeem_options = NULL, external_url = NULL, tickets_reward_type = NULL, tickets_reward = NULL, activated = NULL, expires = NULL, no_expiration = NULL, available_limit = NULL, available_limit_per_user = NULL, added_limit = NULL, view_limit = NULL, max_prints = NULL, ticket_price_type = NULL, ticket_price = NULL, full_price = NULL, discount_price = NULL, show_remaining = NULL, show_redeemed = NULL, replaced = NULL, featured = NULL, special_offer_type = NULL, offer_visibility = NULL, category_ids = NULL, filter_ids = NULL, active = NULL, barcode_asset_id = NULL, image_asset_id = NULL, image_asset_id1 = NULL, image_asset_id2 = NULL, image_asset_id3 = NULL, image_asset_id4 = NULL, image_asset_id5 = NULL, publisher = NULL, redeemable_start = NULL, redeemable_end = NULL, condition_type = NULL, isbn = NULL, asin = NULL, catalog_numbers = NULL, availability_date = NULL, parental_rating = NULL, media_type = NULL, duration = NULL, author = NULL, release_date = NULL, collection_ids = NULL, availability = NULL, availability_summary = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`media_id`)) {
        stop("Missing required parameter `media_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MediaApi$UpdateMedia, `account_id` is not nullable")
      }

      if (!missing(`media_id`) && is.null(`media_id`)) {
        stop("Invalid value for `media_id` when calling MediaApi$UpdateMedia, `media_id` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling MediaApi$UpdateMedia, `retailer_location_ids` is not nullable")
      }

      if (!missing(`offer_locations`) && is.null(`offer_locations`)) {
        stop("Invalid value for `offer_locations` when calling MediaApi$UpdateMedia, `offer_locations` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling MediaApi$UpdateMedia, `title` is not nullable")
      }

      if (!missing(`sub_title`) && is.null(`sub_title`)) {
        stop("Invalid value for `sub_title` when calling MediaApi$UpdateMedia, `sub_title` is not nullable")
      }

      if (!missing(`details`) && is.null(`details`)) {
        stop("Invalid value for `details` when calling MediaApi$UpdateMedia, `details` is not nullable")
      }

      if (!missing(`sub_details`) && is.null(`sub_details`)) {
        stop("Invalid value for `sub_details` when calling MediaApi$UpdateMedia, `sub_details` is not nullable")
      }

      if (!missing(`fine_print`) && is.null(`fine_print`)) {
        stop("Invalid value for `fine_print` when calling MediaApi$UpdateMedia, `fine_print` is not nullable")
      }

      if (!missing(`barcode_type`) && is.null(`barcode_type`)) {
        stop("Invalid value for `barcode_type` when calling MediaApi$UpdateMedia, `barcode_type` is not nullable")
      }

      if (!missing(`barcode_entry`) && is.null(`barcode_entry`)) {
        stop("Invalid value for `barcode_entry` when calling MediaApi$UpdateMedia, `barcode_entry` is not nullable")
      }

      if (!missing(`external_redeem_options`) && is.null(`external_redeem_options`)) {
        stop("Invalid value for `external_redeem_options` when calling MediaApi$UpdateMedia, `external_redeem_options` is not nullable")
      }

      if (!missing(`external_url`) && is.null(`external_url`)) {
        stop("Invalid value for `external_url` when calling MediaApi$UpdateMedia, `external_url` is not nullable")
      }

      if (!missing(`tickets_reward_type`) && is.null(`tickets_reward_type`)) {
        stop("Invalid value for `tickets_reward_type` when calling MediaApi$UpdateMedia, `tickets_reward_type` is not nullable")
      }

      if (!missing(`tickets_reward`) && is.null(`tickets_reward`)) {
        stop("Invalid value for `tickets_reward` when calling MediaApi$UpdateMedia, `tickets_reward` is not nullable")
      }

      if (!missing(`activated`) && is.null(`activated`)) {
        stop("Invalid value for `activated` when calling MediaApi$UpdateMedia, `activated` is not nullable")
      }

      if (!missing(`expires`) && is.null(`expires`)) {
        stop("Invalid value for `expires` when calling MediaApi$UpdateMedia, `expires` is not nullable")
      }

      if (!missing(`no_expiration`) && is.null(`no_expiration`)) {
        stop("Invalid value for `no_expiration` when calling MediaApi$UpdateMedia, `no_expiration` is not nullable")
      }

      if (!missing(`available_limit`) && is.null(`available_limit`)) {
        stop("Invalid value for `available_limit` when calling MediaApi$UpdateMedia, `available_limit` is not nullable")
      }

      if (!missing(`available_limit_per_user`) && is.null(`available_limit_per_user`)) {
        stop("Invalid value for `available_limit_per_user` when calling MediaApi$UpdateMedia, `available_limit_per_user` is not nullable")
      }

      if (!missing(`added_limit`) && is.null(`added_limit`)) {
        stop("Invalid value for `added_limit` when calling MediaApi$UpdateMedia, `added_limit` is not nullable")
      }

      if (!missing(`view_limit`) && is.null(`view_limit`)) {
        stop("Invalid value for `view_limit` when calling MediaApi$UpdateMedia, `view_limit` is not nullable")
      }

      if (!missing(`max_prints`) && is.null(`max_prints`)) {
        stop("Invalid value for `max_prints` when calling MediaApi$UpdateMedia, `max_prints` is not nullable")
      }

      if (!missing(`ticket_price_type`) && is.null(`ticket_price_type`)) {
        stop("Invalid value for `ticket_price_type` when calling MediaApi$UpdateMedia, `ticket_price_type` is not nullable")
      }

      if (!missing(`ticket_price`) && is.null(`ticket_price`)) {
        stop("Invalid value for `ticket_price` when calling MediaApi$UpdateMedia, `ticket_price` is not nullable")
      }

      if (!missing(`full_price`) && is.null(`full_price`)) {
        stop("Invalid value for `full_price` when calling MediaApi$UpdateMedia, `full_price` is not nullable")
      }

      if (!missing(`discount_price`) && is.null(`discount_price`)) {
        stop("Invalid value for `discount_price` when calling MediaApi$UpdateMedia, `discount_price` is not nullable")
      }

      if (!missing(`show_remaining`) && is.null(`show_remaining`)) {
        stop("Invalid value for `show_remaining` when calling MediaApi$UpdateMedia, `show_remaining` is not nullable")
      }

      if (!missing(`show_redeemed`) && is.null(`show_redeemed`)) {
        stop("Invalid value for `show_redeemed` when calling MediaApi$UpdateMedia, `show_redeemed` is not nullable")
      }

      if (!missing(`replaced`) && is.null(`replaced`)) {
        stop("Invalid value for `replaced` when calling MediaApi$UpdateMedia, `replaced` is not nullable")
      }

      if (!missing(`featured`) && is.null(`featured`)) {
        stop("Invalid value for `featured` when calling MediaApi$UpdateMedia, `featured` is not nullable")
      }

      if (!missing(`special_offer_type`) && is.null(`special_offer_type`)) {
        stop("Invalid value for `special_offer_type` when calling MediaApi$UpdateMedia, `special_offer_type` is not nullable")
      }

      if (!missing(`offer_visibility`) && is.null(`offer_visibility`)) {
        stop("Invalid value for `offer_visibility` when calling MediaApi$UpdateMedia, `offer_visibility` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling MediaApi$UpdateMedia, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling MediaApi$UpdateMedia, `filter_ids` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling MediaApi$UpdateMedia, `active` is not nullable")
      }

      if (!missing(`barcode_asset_id`) && is.null(`barcode_asset_id`)) {
        stop("Invalid value for `barcode_asset_id` when calling MediaApi$UpdateMedia, `barcode_asset_id` is not nullable")
      }

      if (!missing(`image_asset_id`) && is.null(`image_asset_id`)) {
        stop("Invalid value for `image_asset_id` when calling MediaApi$UpdateMedia, `image_asset_id` is not nullable")
      }

      if (!missing(`image_asset_id1`) && is.null(`image_asset_id1`)) {
        stop("Invalid value for `image_asset_id1` when calling MediaApi$UpdateMedia, `image_asset_id1` is not nullable")
      }

      if (!missing(`image_asset_id2`) && is.null(`image_asset_id2`)) {
        stop("Invalid value for `image_asset_id2` when calling MediaApi$UpdateMedia, `image_asset_id2` is not nullable")
      }

      if (!missing(`image_asset_id3`) && is.null(`image_asset_id3`)) {
        stop("Invalid value for `image_asset_id3` when calling MediaApi$UpdateMedia, `image_asset_id3` is not nullable")
      }

      if (!missing(`image_asset_id4`) && is.null(`image_asset_id4`)) {
        stop("Invalid value for `image_asset_id4` when calling MediaApi$UpdateMedia, `image_asset_id4` is not nullable")
      }

      if (!missing(`image_asset_id5`) && is.null(`image_asset_id5`)) {
        stop("Invalid value for `image_asset_id5` when calling MediaApi$UpdateMedia, `image_asset_id5` is not nullable")
      }

      if (!missing(`publisher`) && is.null(`publisher`)) {
        stop("Invalid value for `publisher` when calling MediaApi$UpdateMedia, `publisher` is not nullable")
      }

      if (!missing(`redeemable_start`) && is.null(`redeemable_start`)) {
        stop("Invalid value for `redeemable_start` when calling MediaApi$UpdateMedia, `redeemable_start` is not nullable")
      }

      if (!missing(`redeemable_end`) && is.null(`redeemable_end`)) {
        stop("Invalid value for `redeemable_end` when calling MediaApi$UpdateMedia, `redeemable_end` is not nullable")
      }

      if (!missing(`condition_type`) && is.null(`condition_type`)) {
        stop("Invalid value for `condition_type` when calling MediaApi$UpdateMedia, `condition_type` is not nullable")
      }

      if (!missing(`isbn`) && is.null(`isbn`)) {
        stop("Invalid value for `isbn` when calling MediaApi$UpdateMedia, `isbn` is not nullable")
      }

      if (!missing(`asin`) && is.null(`asin`)) {
        stop("Invalid value for `asin` when calling MediaApi$UpdateMedia, `asin` is not nullable")
      }

      if (!missing(`catalog_numbers`) && is.null(`catalog_numbers`)) {
        stop("Invalid value for `catalog_numbers` when calling MediaApi$UpdateMedia, `catalog_numbers` is not nullable")
      }

      if (!missing(`availability_date`) && is.null(`availability_date`)) {
        stop("Invalid value for `availability_date` when calling MediaApi$UpdateMedia, `availability_date` is not nullable")
      }

      if (!missing(`parental_rating`) && is.null(`parental_rating`)) {
        stop("Invalid value for `parental_rating` when calling MediaApi$UpdateMedia, `parental_rating` is not nullable")
      }

      if (!missing(`media_type`) && is.null(`media_type`)) {
        stop("Invalid value for `media_type` when calling MediaApi$UpdateMedia, `media_type` is not nullable")
      }

      if (!missing(`duration`) && is.null(`duration`)) {
        stop("Invalid value for `duration` when calling MediaApi$UpdateMedia, `duration` is not nullable")
      }

      if (!missing(`author`) && is.null(`author`)) {
        stop("Invalid value for `author` when calling MediaApi$UpdateMedia, `author` is not nullable")
      }

      if (!missing(`release_date`) && is.null(`release_date`)) {
        stop("Invalid value for `release_date` when calling MediaApi$UpdateMedia, `release_date` is not nullable")
      }

      if (!missing(`collection_ids`) && is.null(`collection_ids`)) {
        stop("Invalid value for `collection_ids` when calling MediaApi$UpdateMedia, `collection_ids` is not nullable")
      }

      if (!missing(`availability`) && is.null(`availability`)) {
        stop("Invalid value for `availability` when calling MediaApi$UpdateMedia, `availability` is not nullable")
      }

      if (!missing(`availability_summary`) && is.null(`availability_summary`)) {
        stop("Invalid value for `availability_summary` when calling MediaApi$UpdateMedia, `availability_summary` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["mediaId"]] <- `media_id`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["offerLocations"]] <- `offer_locations`

      query_params[["title"]] <- `title`

      query_params[["subTitle"]] <- `sub_title`

      query_params[["details"]] <- `details`

      query_params[["subDetails"]] <- `sub_details`

      query_params[["finePrint"]] <- `fine_print`

      if (!is.null(`barcode_type`) && !(`barcode_type` %in% c("NONE", "UPC", "CODE_128", "QR", "CUSTOM_MEDIA"))) {
        stop("Invalid value for barcode_type when calling MediaApi$UpdateMedia. Must be [NONE, UPC, CODE_128, QR, CUSTOM_MEDIA].")
      }
      query_params[["barcodeType"]] <- `barcode_type`

      query_params[["barcodeEntry"]] <- `barcode_entry`

      query_params[["externalRedeemOptions"]] <- `external_redeem_options`

      query_params[["externalUrl"]] <- `external_url`

      query_params[["ticketsRewardType"]] <- `tickets_reward_type`

      query_params[["ticketsReward"]] <- `tickets_reward`

      query_params[["activated"]] <- `activated`

      query_params[["expires"]] <- `expires`

      query_params[["noExpiration"]] <- `no_expiration`

      query_params[["availableLimit"]] <- `available_limit`

      query_params[["availableLimitPerUser"]] <- `available_limit_per_user`

      query_params[["addedLimit"]] <- `added_limit`

      query_params[["viewLimit"]] <- `view_limit`

      query_params[["maxPrints"]] <- `max_prints`

      query_params[["ticketPriceType"]] <- `ticket_price_type`

      query_params[["ticketPrice"]] <- `ticket_price`

      query_params[["fullPrice"]] <- `full_price`

      query_params[["discountPrice"]] <- `discount_price`

      query_params[["showRemaining"]] <- `show_remaining`

      query_params[["showRedeemed"]] <- `show_redeemed`

      query_params[["replaced"]] <- `replaced`

      query_params[["featured"]] <- `featured`

      if (!is.null(`special_offer_type`) && !(`special_offer_type` %in% c("ALL", "RESERVABLE", "REGULAR_OFFER", "ACT_NOW", "GET_THERE_NOW", "SQOOT", "TICKETS", "YIPIT"))) {
        stop("Invalid value for special_offer_type when calling MediaApi$UpdateMedia. Must be [ALL, RESERVABLE, REGULAR_OFFER, ACT_NOW, GET_THERE_NOW, SQOOT, TICKETS, YIPIT].")
      }
      query_params[["specialOfferType"]] <- `special_offer_type`

      if (!is.null(`offer_visibility`) && !(`offer_visibility` %in% c("PUBLIC", "LISTABLE", "REWARDABLE", "TRIGGERABLE", "PRIVATE"))) {
        stop("Invalid value for offer_visibility when calling MediaApi$UpdateMedia. Must be [PUBLIC, LISTABLE, REWARDABLE, TRIGGERABLE, PRIVATE].")
      }
      query_params[["offerVisibility"]] <- `offer_visibility`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["active"]] <- `active`

      query_params[["barcodeAssetId"]] <- `barcode_asset_id`

      query_params[["imageAssetId"]] <- `image_asset_id`

      query_params[["imageAssetId1"]] <- `image_asset_id1`

      query_params[["imageAssetId2"]] <- `image_asset_id2`

      query_params[["imageAssetId3"]] <- `image_asset_id3`

      query_params[["imageAssetId4"]] <- `image_asset_id4`

      query_params[["imageAssetId5"]] <- `image_asset_id5`

      query_params[["publisher"]] <- `publisher`

      query_params[["redeemableStart"]] <- `redeemable_start`

      query_params[["redeemableEnd"]] <- `redeemable_end`

      if (!is.null(`condition_type`) && !(`condition_type` %in% c("NEW", "USED", "REFURBISHED", "OTHER"))) {
        stop("Invalid value for condition_type when calling MediaApi$UpdateMedia. Must be [NEW, USED, REFURBISHED, OTHER].")
      }
      query_params[["conditionType"]] <- `condition_type`

      query_params[["isbn"]] <- `isbn`

      query_params[["asin"]] <- `asin`

      query_params[["catalogNumbers"]] <- `catalog_numbers`

      query_params[["availabilityDate"]] <- `availability_date`

      query_params[["parentalRating"]] <- `parental_rating`

      if (!is.null(`media_type`) && !(`media_type` %in% c("APK", "APPLICATION", "IMAGE", "AUDIO", "VIDEO", "MULTIPART", "BAR_CODE", "TEXT", "OTHER"))) {
        stop("Invalid value for media_type when calling MediaApi$UpdateMedia. Must be [APK, APPLICATION, IMAGE, AUDIO, VIDEO, MULTIPART, BAR_CODE, TEXT, OTHER].")
      }
      query_params[["mediaType"]] <- `media_type`

      query_params[["duration"]] <- `duration`

      query_params[["author"]] <- `author`

      query_params[["releaseDate"]] <- `release_date`

      query_params[["collectionIds"]] <- `collection_ids`

      query_params[["availability"]] <- `availability`

      query_params[["availabilitySummary"]] <- `availability_summary`

      local_var_url_path <- "/media/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MediaOfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
