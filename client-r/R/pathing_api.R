#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Pathing operations
#' @description PathingApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ComputePath  ####################
#'
#' library(openapi)
#' var_data <- "data_example" # character | the data to with start, end point and exclusion points
#' var_units <- "units_example" # character | the system of measurement for directions: {METRIC, IMPERIAL}
#' var_reduce_path <- "reduce_path_example" # character | determines whether to reduce the path to go in diagonal lines
#' var_directions <- "directions_example" # character | determines whether to return text directions
#'
#' #Calculate Path
#' api_instance <- PathingApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ComputePath(var_data, var_units, var_reduce_path, var_directionsdata_file = "result.txt")
#' result <- api_instance$ComputePath(var_data, var_units, var_reduce_path, var_directions)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
PathingApi <- R6::R6Class(
  "PathingApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new PathingApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Calculate Path
    #'
    #' @param data the data to with start, end point and exclusion points
    #' @param units the system of measurement for directions: {METRIC, IMPERIAL}
    #' @param reduce_path determines whether to reduce the path to go in diagonal lines
    #' @param directions determines whether to return text directions
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return PathingResponse
    ComputePath = function(data, units, reduce_path, directions, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ComputePathWithHttpInfo(data, units, reduce_path, directions, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Calculate Path
    #'
    #' @param data the data to with start, end point and exclusion points
    #' @param units the system of measurement for directions: {METRIC, IMPERIAL}
    #' @param reduce_path determines whether to reduce the path to go in diagonal lines
    #' @param directions determines whether to return text directions
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (PathingResponse) with additional information such as HTTP status code, headers
    ComputePathWithHttpInfo = function(data, units, reduce_path, directions, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`data`)) {
        stop("Missing required parameter `data`.")
      }

      if (missing(`units`)) {
        stop("Missing required parameter `units`.")
      }

      if (missing(`reduce_path`)) {
        stop("Missing required parameter `reduce_path`.")
      }

      if (missing(`directions`)) {
        stop("Missing required parameter `directions`.")
      }

      if (!missing(`data`) && is.null(`data`)) {
        stop("Invalid value for `data` when calling PathingApi$ComputePath, `data` is not nullable")
      }

      if (!missing(`units`) && is.null(`units`)) {
        stop("Invalid value for `units` when calling PathingApi$ComputePath, `units` is not nullable")
      }

      if (!missing(`reduce_path`) && is.null(`reduce_path`)) {
        stop("Invalid value for `reduce_path` when calling PathingApi$ComputePath, `reduce_path` is not nullable")
      }

      if (!missing(`directions`) && is.null(`directions`)) {
        stop("Invalid value for `directions` when calling PathingApi$ComputePath, `directions` is not nullable")
      }

      query_params[["data"]] <- `data`

      if (!is.null(`units`) && !(`units` %in% c("METRIC", "IMPERIAL"))) {
        stop("Invalid value for units when calling PathingApi$ComputePath. Must be [METRIC, IMPERIAL].")
      }
      query_params[["units"]] <- `units`

      query_params[["reducePath"]] <- `reduce_path`

      query_params[["directions"]] <- `directions`

      local_var_url_path <- "/pathing/compute"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PathingResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
