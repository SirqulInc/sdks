#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Wallet operations
#' @description WalletApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateOfferTransaction  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_offer_id <- 56 # integer | The id of the offer being added (offerId or offeLocationId required) (Optional)
#' var_offer_location_id <- 56 # integer | The id of the offer location being added (offerId or offeLocationId required) (Optional)
#' var_offer_cart <- "offer_cart_example" # character | A JSON list of offers to purchase. ```json [   {     \"offerId\": 123,     \"offerLocationId\": 234,     \"quantity\": 2   },   {     \"offerId\": 456,     \"offerLocationId\": 567,     \"quantity\": 1   } ] ```  (Optional)
#' var_promo_code <- "promo_code_example" # character | The promoCode (Optional)
#' var_currency_type <- "CASH" # character | Determines the method of purchasing offer. CASH = use card on file, POINTS = use points, TICKETS = use tickets (Optional)
#' var_use_points <- "use_points_example" # character | Sets the currencyType to POINTS (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_app_key <- "app_key_example" # character | The application requesting the purchase, required when currencyType is TICKETS (Optional)
#' var_status <- 56 # integer | Custom status value to change to (0 or 1 for redeem, 5 or 6 for membership) (Optional)
#'
#' #Create Wallet Offers
#' api_instance <- WalletApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateOfferTransaction(device_id = var_device_id, account_id = var_account_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, offer_cart = var_offer_cart, promo_code = var_promo_code, currency_type = var_currency_type, use_points = var_use_points, meta_data = var_meta_data, app_key = var_app_key, status = var_statusdata_file = "result.txt")
#' result <- api_instance$CreateOfferTransaction(device_id = var_device_id, account_id = var_account_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, offer_cart = var_offer_cart, promo_code = var_promo_code, currency_type = var_currency_type, use_points = var_use_points, meta_data = var_meta_data, app_key = var_app_key, status = var_status)
#' dput(result)
#'
#'
#' ####################  DeleteOfferTransaction  ####################
#'
#' library(openapi)
#' var_transaction_id <- 56 # integer | The offer transaction id to remove
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#'
#' #Delete Wallet Offer
#' api_instance <- WalletApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteOfferTransaction(var_transaction_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteOfferTransaction(var_transaction_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  GetOfferTransaction  ####################
#'
#' library(openapi)
#' var_transaction_id <- 56 # integer | The offer transaction id to get details of
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_include_mission <- FALSE # character | If true then include mission data, false to not include (Optional)
#' var_latitude <- 3.4 # numeric | The latitude location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The latitude location of the user (Optional)
#' var_return_full_response <- TRUE # character | Determines whether to return a detailed version of the response (Optional)
#'
#' #Get Wallet Offer
#' api_instance <- WalletApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOfferTransaction(var_transaction_id, device_id = var_device_id, account_id = var_account_id, include_mission = var_include_mission, latitude = var_latitude, longitude = var_longitude, return_full_response = var_return_full_responsedata_file = "result.txt")
#' result <- api_instance$GetOfferTransaction(var_transaction_id, device_id = var_device_id, account_id = var_account_id, include_mission = var_include_mission, latitude = var_latitude, longitude = var_longitude, return_full_response = var_return_full_response)
#' dput(result)
#'
#'
#' ####################  PreviewOfferTransaction  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_offer_id <- 56 # integer | The id of the offer being added (offerId or offeLocationId required) (Optional)
#' var_offer_location_id <- 56 # integer | The id of the offer location being added (offerId or offeLocationId required) (Optional)
#' var_offer_cart <- "offer_cart_example" # character | A JSON list of offers to purchase. (Optional)
#' var_promo_code <- "promo_code_example" # character | The promoCode (Optional)
#' var_currency_type <- "CASH" # character | Determines the method of purchasing offer. CASH = use card on file, POINTS = use points, TICKETS = use tickets (Optional)
#' var_use_points <- "use_points_example" # character | Sets the currencyType to POINTS (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_app_key <- "app_key_example" # character | The application requesting the purchase, required when currencyType is TICKETS (Optional)
#'
#' #Preview Wallet Offers
#' api_instance <- WalletApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PreviewOfferTransaction(device_id = var_device_id, account_id = var_account_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, offer_cart = var_offer_cart, promo_code = var_promo_code, currency_type = var_currency_type, use_points = var_use_points, meta_data = var_meta_data, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$PreviewOfferTransaction(device_id = var_device_id, account_id = var_account_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, offer_cart = var_offer_cart, promo_code = var_promo_code, currency_type = var_currency_type, use_points = var_use_points, meta_data = var_meta_data, app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  SearchOfferTransactions  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_keyword <- "keyword_example" # character | The keyword to search for (Optional)
#' var_retailer_id <- 56 # integer | Filter results for this retailer (Optional)
#' var_retailer_ids <- "retailer_ids_example" # character | Filter results for a list of retailers (Optional)
#' var_retailer_location_id <- 56 # integer | Filter results for this retailer location (Optional)
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | Filter results for a list of retailer locations (Optional)
#' var_exclude_retailer_location_ids <- "exclude_retailer_location_ids_example" # character | Filter results to exclude retailer locations (Optional)
#' var_offer_id <- 56 # integer | Filter results for this offer (Optional)
#' var_offer_ids <- "offer_ids_example" # character | Filter results for a list of offer (Optional)
#' var_offer_location_id <- 56 # integer | Filter results for this offer location (Optional)
#' var_offer_location_ids <- "offer_location_ids_example" # character | Filter results for a list of offer locations (Optional)
#' var_offer_type <- "offer_type_example" # character | Filter results to return a specific offer type (Optional)
#' var_offer_types <- "offer_types_example" # character | Filter results to return specific offer types (Optional)
#' var_special_offer_type <- "special_offer_type_example" # character | Filter results to return a specific special offer type (Optional)
#' var_special_offer_types <- "special_offer_types_example" # character | Filter results to return specific special offer types (Optional)
#' var_category_ids <- "category_ids_example" # character | Category Ids (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Filter Ids (Optional)
#' var_offer_audience_ids <- "offer_audience_ids_example" # character | Offer Audience Ids (Optional)
#' var_sort_field <- "CREATED" # character | Determines what to sort the results by (Optional)
#' var_descending <- TRUE # character | Determines whether the results are in descending order (Optional)
#' var_start <- 0 # integer | The start index for pagination (Optional)
#' var_limit <- 20 # integer | The limit for pagination (Optional)
#' var_latitude <- 3.4 # numeric | The latitude location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The latitude location of the user (Optional)
#' var_redeemable_start_date <- 56 # integer | Filter results by the offer redeemable date (Optional)
#' var_redeemable_end_date <- 56 # integer | Filter results by the offer redeemable date (Optional)
#' var_filter_by_parent_offer <- FALSE # character | Apply params to offer's parent (Optional)
#' var_started_since <- 56 # integer | Filter results by the offer start date (Optional)
#' var_started_before <- 56 # integer | Filter results by the offer start date (Optional)
#' var_ended_since <- 56 # integer | Filter results by the offer end date (Optional)
#' var_ended_before <- 56 # integer | Filter results by the offer end date (Optional)
#' var_redeemed <- FALSE # character | If true return redeemed transactions (status 2), otherwise return active transactions (status 0 or 1) (Optional)
#' var_statuses <- "statuses_example" # character | Custom status value to change to (0 or 1 for redeem, 5 or 6 for membership) (Optional)
#' var_reservations_only <- FALSE # character | Returns only reservation transactions if true (Optional)
#' var_active_only <- FALSE # character | Active Only (Optional)
#' var_return_full_response <- FALSE # character | Determines whether to return a detailed version of the response (Optional)
#' var_recurring_started_since <- 56 # integer | Filter results by the recurring billing start date (Optional)
#' var_recurring_started_before <- 56 # integer | Filter results by the recurring billing start date (Optional)
#' var_recurring_expiration_since <- 56 # integer | Filter results by the recurring billing expiration date (Optional)
#' var_recurring_expiration_before <- 56 # integer | Filter results by the recurring billing expiration date (Optional)
#'
#' #Search Wallet Offers
#' api_instance <- WalletApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchOfferTransactions(device_id = var_device_id, account_id = var_account_id, keyword = var_keyword, retailer_id = var_retailer_id, retailer_ids = var_retailer_ids, retailer_location_id = var_retailer_location_id, retailer_location_ids = var_retailer_location_ids, exclude_retailer_location_ids = var_exclude_retailer_location_ids, offer_id = var_offer_id, offer_ids = var_offer_ids, offer_location_id = var_offer_location_id, offer_location_ids = var_offer_location_ids, offer_type = var_offer_type, offer_types = var_offer_types, special_offer_type = var_special_offer_type, special_offer_types = var_special_offer_types, category_ids = var_category_ids, filter_ids = var_filter_ids, offer_audience_ids = var_offer_audience_ids, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, latitude = var_latitude, longitude = var_longitude, redeemable_start_date = var_redeemable_start_date, redeemable_end_date = var_redeemable_end_date, filter_by_parent_offer = var_filter_by_parent_offer, started_since = var_started_since, started_before = var_started_before, ended_since = var_ended_since, ended_before = var_ended_before, redeemed = var_redeemed, statuses = var_statuses, reservations_only = var_reservations_only, active_only = var_active_only, return_full_response = var_return_full_response, recurring_started_since = var_recurring_started_since, recurring_started_before = var_recurring_started_before, recurring_expiration_since = var_recurring_expiration_since, recurring_expiration_before = var_recurring_expiration_beforedata_file = "result.txt")
#' result <- api_instance$SearchOfferTransactions(device_id = var_device_id, account_id = var_account_id, keyword = var_keyword, retailer_id = var_retailer_id, retailer_ids = var_retailer_ids, retailer_location_id = var_retailer_location_id, retailer_location_ids = var_retailer_location_ids, exclude_retailer_location_ids = var_exclude_retailer_location_ids, offer_id = var_offer_id, offer_ids = var_offer_ids, offer_location_id = var_offer_location_id, offer_location_ids = var_offer_location_ids, offer_type = var_offer_type, offer_types = var_offer_types, special_offer_type = var_special_offer_type, special_offer_types = var_special_offer_types, category_ids = var_category_ids, filter_ids = var_filter_ids, offer_audience_ids = var_offer_audience_ids, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit, latitude = var_latitude, longitude = var_longitude, redeemable_start_date = var_redeemable_start_date, redeemable_end_date = var_redeemable_end_date, filter_by_parent_offer = var_filter_by_parent_offer, started_since = var_started_since, started_before = var_started_before, ended_since = var_ended_since, ended_before = var_ended_before, redeemed = var_redeemed, statuses = var_statuses, reservations_only = var_reservations_only, active_only = var_active_only, return_full_response = var_return_full_response, recurring_started_since = var_recurring_started_since, recurring_started_before = var_recurring_started_before, recurring_expiration_since = var_recurring_expiration_since, recurring_expiration_before = var_recurring_expiration_before)
#' dput(result)
#'
#'
#' ####################  UpdateOfferTransaction  ####################
#'
#' library(openapi)
#' var_transaction_id <- 56 # integer | The offer transaction id to remove
#' var_status <- 56 # integer | The status value to change to (0 or 1)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_offer_location_id <- 56 # integer | Offer Location Id (Optional)
#' var_currency_type <- "CASH" # character | Currency Type (Optional)
#' var_use_points <- "use_points_example" # character | Use Points (Optional)
#' var_app_key <- "app_key_example" # character | App Key (Optional)
#' var_latitude <- 3.4 # numeric | The latitude location of the user (Optional)
#' var_longitude <- 3.4 # numeric | The latitude location of the user (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_return_full_response <- FALSE # character | Determines whether to return a detailed version of the response (Optional)
#' var_exception_membership_offer_ids <- "exception_membership_offer_ids_example" # character | Exception Offers, transaction audiences of these offers won't be removed out of the account when up (Optional)
#'
#' #Update Wallet Offer
#' api_instance <- WalletApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateOfferTransaction(var_transaction_id, var_status, device_id = var_device_id, account_id = var_account_id, offer_location_id = var_offer_location_id, currency_type = var_currency_type, use_points = var_use_points, app_key = var_app_key, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, return_full_response = var_return_full_response, exception_membership_offer_ids = var_exception_membership_offer_idsdata_file = "result.txt")
#' result <- api_instance$UpdateOfferTransaction(var_transaction_id, var_status, device_id = var_device_id, account_id = var_account_id, offer_location_id = var_offer_location_id, currency_type = var_currency_type, use_points = var_use_points, app_key = var_app_key, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, return_full_response = var_return_full_response, exception_membership_offer_ids = var_exception_membership_offer_ids)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
WalletApi <- R6::R6Class(
  "WalletApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new WalletApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Wallet Offers
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param offer_id (optional) The id of the offer being added (offerId or offeLocationId required)
    #' @param offer_location_id (optional) The id of the offer location being added (offerId or offeLocationId required)
    #' @param offer_cart (optional) A JSON list of offers to purchase. ```json [   {     \"offerId\": 123,     \"offerLocationId\": 234,     \"quantity\": 2   },   {     \"offerId\": 456,     \"offerLocationId\": 567,     \"quantity\": 1   } ] ``` 
    #' @param promo_code (optional) The promoCode
    #' @param currency_type (optional) Determines the method of purchasing offer. CASH = use card on file, POINTS = use points, TICKETS = use tickets (default value: "CASH")
    #' @param use_points (optional) Sets the currencyType to POINTS
    #' @param meta_data (optional) External custom client defined data
    #' @param app_key (optional) The application requesting the purchase, required when currencyType is TICKETS
    #' @param status (optional) Custom status value to change to (0 or 1 for redeem, 5 or 6 for membership)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[OfferTransactionResponse]
    CreateOfferTransaction = function(device_id = NULL, account_id = NULL, offer_id = NULL, offer_location_id = NULL, offer_cart = NULL, promo_code = NULL, currency_type = "CASH", use_points = NULL, meta_data = NULL, app_key = NULL, status = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateOfferTransactionWithHttpInfo(device_id, account_id, offer_id, offer_location_id, offer_cart, promo_code, currency_type, use_points, meta_data, app_key, status, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Wallet Offers
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param offer_id (optional) The id of the offer being added (offerId or offeLocationId required)
    #' @param offer_location_id (optional) The id of the offer location being added (offerId or offeLocationId required)
    #' @param offer_cart (optional) A JSON list of offers to purchase. ```json [   {     \"offerId\": 123,     \"offerLocationId\": 234,     \"quantity\": 2   },   {     \"offerId\": 456,     \"offerLocationId\": 567,     \"quantity\": 1   } ] ``` 
    #' @param promo_code (optional) The promoCode
    #' @param currency_type (optional) Determines the method of purchasing offer. CASH = use card on file, POINTS = use points, TICKETS = use tickets (default value: "CASH")
    #' @param use_points (optional) Sets the currencyType to POINTS
    #' @param meta_data (optional) External custom client defined data
    #' @param app_key (optional) The application requesting the purchase, required when currencyType is TICKETS
    #' @param status (optional) Custom status value to change to (0 or 1 for redeem, 5 or 6 for membership)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[OfferTransactionResponse]) with additional information such as HTTP status code, headers
    CreateOfferTransactionWithHttpInfo = function(device_id = NULL, account_id = NULL, offer_id = NULL, offer_location_id = NULL, offer_cart = NULL, promo_code = NULL, currency_type = "CASH", use_points = NULL, meta_data = NULL, app_key = NULL, status = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling WalletApi$CreateOfferTransaction, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling WalletApi$CreateOfferTransaction, `account_id` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling WalletApi$CreateOfferTransaction, `offer_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling WalletApi$CreateOfferTransaction, `offer_location_id` is not nullable")
      }

      if (!missing(`offer_cart`) && is.null(`offer_cart`)) {
        stop("Invalid value for `offer_cart` when calling WalletApi$CreateOfferTransaction, `offer_cart` is not nullable")
      }

      if (!missing(`promo_code`) && is.null(`promo_code`)) {
        stop("Invalid value for `promo_code` when calling WalletApi$CreateOfferTransaction, `promo_code` is not nullable")
      }

      if (!missing(`currency_type`) && is.null(`currency_type`)) {
        stop("Invalid value for `currency_type` when calling WalletApi$CreateOfferTransaction, `currency_type` is not nullable")
      }

      if (!missing(`use_points`) && is.null(`use_points`)) {
        stop("Invalid value for `use_points` when calling WalletApi$CreateOfferTransaction, `use_points` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling WalletApi$CreateOfferTransaction, `meta_data` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling WalletApi$CreateOfferTransaction, `app_key` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling WalletApi$CreateOfferTransaction, `status` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["offerCart"]] <- `offer_cart`

      query_params[["promoCode"]] <- `promo_code`

      query_params[["currencyType"]] <- `currency_type`

      query_params[["usePoints"]] <- `use_points`

      query_params[["metaData"]] <- `meta_data`

      query_params[["appKey"]] <- `app_key`

      query_params[["status"]] <- `status`

      local_var_url_path <- "/wallet/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[OfferTransactionResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Wallet Offer
    #'
    #' @param transaction_id The offer transaction id to remove
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteOfferTransaction = function(transaction_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteOfferTransactionWithHttpInfo(transaction_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Wallet Offer
    #'
    #' @param transaction_id The offer transaction id to remove
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteOfferTransactionWithHttpInfo = function(transaction_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`transaction_id`)) {
        stop("Missing required parameter `transaction_id`.")
      }

      if (!missing(`transaction_id`) && is.null(`transaction_id`)) {
        stop("Invalid value for `transaction_id` when calling WalletApi$DeleteOfferTransaction, `transaction_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling WalletApi$DeleteOfferTransaction, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling WalletApi$DeleteOfferTransaction, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["transactionId"]] <- `transaction_id`

      local_var_url_path <- "/wallet/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Wallet Offer
    #'
    #' @param transaction_id The offer transaction id to get details of
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param include_mission (optional) If true then include mission data, false to not include (default value: FALSE)
    #' @param latitude (optional) The latitude location of the user
    #' @param longitude (optional) The latitude location of the user
    #' @param return_full_response (optional) Determines whether to return a detailed version of the response (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferTransactionResponse
    GetOfferTransaction = function(transaction_id, device_id = NULL, account_id = NULL, include_mission = FALSE, latitude = NULL, longitude = NULL, return_full_response = TRUE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetOfferTransactionWithHttpInfo(transaction_id, device_id, account_id, include_mission, latitude, longitude, return_full_response, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Wallet Offer
    #'
    #' @param transaction_id The offer transaction id to get details of
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param include_mission (optional) If true then include mission data, false to not include (default value: FALSE)
    #' @param latitude (optional) The latitude location of the user
    #' @param longitude (optional) The latitude location of the user
    #' @param return_full_response (optional) Determines whether to return a detailed version of the response (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferTransactionResponse) with additional information such as HTTP status code, headers
    GetOfferTransactionWithHttpInfo = function(transaction_id, device_id = NULL, account_id = NULL, include_mission = FALSE, latitude = NULL, longitude = NULL, return_full_response = TRUE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`transaction_id`)) {
        stop("Missing required parameter `transaction_id`.")
      }

      if (!missing(`transaction_id`) && is.null(`transaction_id`)) {
        stop("Invalid value for `transaction_id` when calling WalletApi$GetOfferTransaction, `transaction_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling WalletApi$GetOfferTransaction, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling WalletApi$GetOfferTransaction, `account_id` is not nullable")
      }

      if (!missing(`include_mission`) && is.null(`include_mission`)) {
        stop("Invalid value for `include_mission` when calling WalletApi$GetOfferTransaction, `include_mission` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling WalletApi$GetOfferTransaction, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling WalletApi$GetOfferTransaction, `longitude` is not nullable")
      }

      if (!missing(`return_full_response`) && is.null(`return_full_response`)) {
        stop("Invalid value for `return_full_response` when calling WalletApi$GetOfferTransaction, `return_full_response` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["transactionId"]] <- `transaction_id`

      query_params[["includeMission"]] <- `include_mission`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["returnFullResponse"]] <- `return_full_response`

      local_var_url_path <- "/wallet/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferTransactionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Preview Wallet Offers
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param offer_id (optional) The id of the offer being added (offerId or offeLocationId required)
    #' @param offer_location_id (optional) The id of the offer location being added (offerId or offeLocationId required)
    #' @param offer_cart (optional) A JSON list of offers to purchase.
    #' @param promo_code (optional) The promoCode
    #' @param currency_type (optional) Determines the method of purchasing offer. CASH = use card on file, POINTS = use points, TICKETS = use tickets (default value: "CASH")
    #' @param use_points (optional) Sets the currencyType to POINTS
    #' @param meta_data (optional) External custom client defined data
    #' @param app_key (optional) The application requesting the purchase, required when currencyType is TICKETS
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[OfferTransactionResponse]
    PreviewOfferTransaction = function(device_id = NULL, account_id = NULL, offer_id = NULL, offer_location_id = NULL, offer_cart = NULL, promo_code = NULL, currency_type = "CASH", use_points = NULL, meta_data = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$PreviewOfferTransactionWithHttpInfo(device_id, account_id, offer_id, offer_location_id, offer_cart, promo_code, currency_type, use_points, meta_data, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Preview Wallet Offers
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param offer_id (optional) The id of the offer being added (offerId or offeLocationId required)
    #' @param offer_location_id (optional) The id of the offer location being added (offerId or offeLocationId required)
    #' @param offer_cart (optional) A JSON list of offers to purchase.
    #' @param promo_code (optional) The promoCode
    #' @param currency_type (optional) Determines the method of purchasing offer. CASH = use card on file, POINTS = use points, TICKETS = use tickets (default value: "CASH")
    #' @param use_points (optional) Sets the currencyType to POINTS
    #' @param meta_data (optional) External custom client defined data
    #' @param app_key (optional) The application requesting the purchase, required when currencyType is TICKETS
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[OfferTransactionResponse]) with additional information such as HTTP status code, headers
    PreviewOfferTransactionWithHttpInfo = function(device_id = NULL, account_id = NULL, offer_id = NULL, offer_location_id = NULL, offer_cart = NULL, promo_code = NULL, currency_type = "CASH", use_points = NULL, meta_data = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling WalletApi$PreviewOfferTransaction, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling WalletApi$PreviewOfferTransaction, `account_id` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling WalletApi$PreviewOfferTransaction, `offer_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling WalletApi$PreviewOfferTransaction, `offer_location_id` is not nullable")
      }

      if (!missing(`offer_cart`) && is.null(`offer_cart`)) {
        stop("Invalid value for `offer_cart` when calling WalletApi$PreviewOfferTransaction, `offer_cart` is not nullable")
      }

      if (!missing(`promo_code`) && is.null(`promo_code`)) {
        stop("Invalid value for `promo_code` when calling WalletApi$PreviewOfferTransaction, `promo_code` is not nullable")
      }

      if (!missing(`currency_type`) && is.null(`currency_type`)) {
        stop("Invalid value for `currency_type` when calling WalletApi$PreviewOfferTransaction, `currency_type` is not nullable")
      }

      if (!missing(`use_points`) && is.null(`use_points`)) {
        stop("Invalid value for `use_points` when calling WalletApi$PreviewOfferTransaction, `use_points` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling WalletApi$PreviewOfferTransaction, `meta_data` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling WalletApi$PreviewOfferTransaction, `app_key` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["offerCart"]] <- `offer_cart`

      query_params[["promoCode"]] <- `promo_code`

      query_params[["currencyType"]] <- `currency_type`

      query_params[["usePoints"]] <- `use_points`

      query_params[["metaData"]] <- `meta_data`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/wallet/preview"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[OfferTransactionResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Wallet Offers
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param keyword (optional) The keyword to search for
    #' @param retailer_id (optional) Filter results for this retailer
    #' @param retailer_ids (optional) Filter results for a list of retailers
    #' @param retailer_location_id (optional) Filter results for this retailer location
    #' @param retailer_location_ids (optional) Filter results for a list of retailer locations
    #' @param exclude_retailer_location_ids (optional) Filter results to exclude retailer locations
    #' @param offer_id (optional) Filter results for this offer
    #' @param offer_ids (optional) Filter results for a list of offer
    #' @param offer_location_id (optional) Filter results for this offer location
    #' @param offer_location_ids (optional) Filter results for a list of offer locations
    #' @param offer_type (optional) Filter results to return a specific offer type
    #' @param offer_types (optional) Filter results to return specific offer types
    #' @param special_offer_type (optional) Filter results to return a specific special offer type
    #' @param special_offer_types (optional) Filter results to return specific special offer types
    #' @param category_ids (optional) Category Ids
    #' @param filter_ids (optional) Filter Ids
    #' @param offer_audience_ids (optional) Offer Audience Ids
    #' @param sort_field (optional) Determines what to sort the results by (default value: "CREATED")
    #' @param descending (optional) Determines whether the results are in descending order (default value: TRUE)
    #' @param start (optional) The start index for pagination (default value: 0)
    #' @param limit (optional) The limit for pagination (default value: 20)
    #' @param latitude (optional) The latitude location of the user
    #' @param longitude (optional) The latitude location of the user
    #' @param redeemable_start_date (optional) Filter results by the offer redeemable date
    #' @param redeemable_end_date (optional) Filter results by the offer redeemable date
    #' @param filter_by_parent_offer (optional) Apply params to offer's parent (default value: FALSE)
    #' @param started_since (optional) Filter results by the offer start date
    #' @param started_before (optional) Filter results by the offer start date
    #' @param ended_since (optional) Filter results by the offer end date
    #' @param ended_before (optional) Filter results by the offer end date
    #' @param redeemed (optional) If true return redeemed transactions (status 2), otherwise return active transactions (status 0 or 1) (default value: FALSE)
    #' @param statuses (optional) Custom status value to change to (0 or 1 for redeem, 5 or 6 for membership)
    #' @param reservations_only (optional) Returns only reservation transactions if true (default value: FALSE)
    #' @param active_only (optional) Active Only (default value: FALSE)
    #' @param return_full_response (optional) Determines whether to return a detailed version of the response (default value: FALSE)
    #' @param recurring_started_since (optional) Filter results by the recurring billing start date
    #' @param recurring_started_before (optional) Filter results by the recurring billing start date
    #' @param recurring_expiration_since (optional) Filter results by the recurring billing expiration date
    #' @param recurring_expiration_before (optional) Filter results by the recurring billing expiration date
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[OfferTransactionResponse]
    SearchOfferTransactions = function(device_id = NULL, account_id = NULL, keyword = NULL, retailer_id = NULL, retailer_ids = NULL, retailer_location_id = NULL, retailer_location_ids = NULL, exclude_retailer_location_ids = NULL, offer_id = NULL, offer_ids = NULL, offer_location_id = NULL, offer_location_ids = NULL, offer_type = NULL, offer_types = NULL, special_offer_type = NULL, special_offer_types = NULL, category_ids = NULL, filter_ids = NULL, offer_audience_ids = NULL, sort_field = "CREATED", descending = TRUE, start = 0, limit = 20, latitude = NULL, longitude = NULL, redeemable_start_date = NULL, redeemable_end_date = NULL, filter_by_parent_offer = FALSE, started_since = NULL, started_before = NULL, ended_since = NULL, ended_before = NULL, redeemed = FALSE, statuses = NULL, reservations_only = FALSE, active_only = FALSE, return_full_response = FALSE, recurring_started_since = NULL, recurring_started_before = NULL, recurring_expiration_since = NULL, recurring_expiration_before = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchOfferTransactionsWithHttpInfo(device_id, account_id, keyword, retailer_id, retailer_ids, retailer_location_id, retailer_location_ids, exclude_retailer_location_ids, offer_id, offer_ids, offer_location_id, offer_location_ids, offer_type, offer_types, special_offer_type, special_offer_types, category_ids, filter_ids, offer_audience_ids, sort_field, descending, start, limit, latitude, longitude, redeemable_start_date, redeemable_end_date, filter_by_parent_offer, started_since, started_before, ended_since, ended_before, redeemed, statuses, reservations_only, active_only, return_full_response, recurring_started_since, recurring_started_before, recurring_expiration_since, recurring_expiration_before, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Wallet Offers
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param keyword (optional) The keyword to search for
    #' @param retailer_id (optional) Filter results for this retailer
    #' @param retailer_ids (optional) Filter results for a list of retailers
    #' @param retailer_location_id (optional) Filter results for this retailer location
    #' @param retailer_location_ids (optional) Filter results for a list of retailer locations
    #' @param exclude_retailer_location_ids (optional) Filter results to exclude retailer locations
    #' @param offer_id (optional) Filter results for this offer
    #' @param offer_ids (optional) Filter results for a list of offer
    #' @param offer_location_id (optional) Filter results for this offer location
    #' @param offer_location_ids (optional) Filter results for a list of offer locations
    #' @param offer_type (optional) Filter results to return a specific offer type
    #' @param offer_types (optional) Filter results to return specific offer types
    #' @param special_offer_type (optional) Filter results to return a specific special offer type
    #' @param special_offer_types (optional) Filter results to return specific special offer types
    #' @param category_ids (optional) Category Ids
    #' @param filter_ids (optional) Filter Ids
    #' @param offer_audience_ids (optional) Offer Audience Ids
    #' @param sort_field (optional) Determines what to sort the results by (default value: "CREATED")
    #' @param descending (optional) Determines whether the results are in descending order (default value: TRUE)
    #' @param start (optional) The start index for pagination (default value: 0)
    #' @param limit (optional) The limit for pagination (default value: 20)
    #' @param latitude (optional) The latitude location of the user
    #' @param longitude (optional) The latitude location of the user
    #' @param redeemable_start_date (optional) Filter results by the offer redeemable date
    #' @param redeemable_end_date (optional) Filter results by the offer redeemable date
    #' @param filter_by_parent_offer (optional) Apply params to offer's parent (default value: FALSE)
    #' @param started_since (optional) Filter results by the offer start date
    #' @param started_before (optional) Filter results by the offer start date
    #' @param ended_since (optional) Filter results by the offer end date
    #' @param ended_before (optional) Filter results by the offer end date
    #' @param redeemed (optional) If true return redeemed transactions (status 2), otherwise return active transactions (status 0 or 1) (default value: FALSE)
    #' @param statuses (optional) Custom status value to change to (0 or 1 for redeem, 5 or 6 for membership)
    #' @param reservations_only (optional) Returns only reservation transactions if true (default value: FALSE)
    #' @param active_only (optional) Active Only (default value: FALSE)
    #' @param return_full_response (optional) Determines whether to return a detailed version of the response (default value: FALSE)
    #' @param recurring_started_since (optional) Filter results by the recurring billing start date
    #' @param recurring_started_before (optional) Filter results by the recurring billing start date
    #' @param recurring_expiration_since (optional) Filter results by the recurring billing expiration date
    #' @param recurring_expiration_before (optional) Filter results by the recurring billing expiration date
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[OfferTransactionResponse]) with additional information such as HTTP status code, headers
    SearchOfferTransactionsWithHttpInfo = function(device_id = NULL, account_id = NULL, keyword = NULL, retailer_id = NULL, retailer_ids = NULL, retailer_location_id = NULL, retailer_location_ids = NULL, exclude_retailer_location_ids = NULL, offer_id = NULL, offer_ids = NULL, offer_location_id = NULL, offer_location_ids = NULL, offer_type = NULL, offer_types = NULL, special_offer_type = NULL, special_offer_types = NULL, category_ids = NULL, filter_ids = NULL, offer_audience_ids = NULL, sort_field = "CREATED", descending = TRUE, start = 0, limit = 20, latitude = NULL, longitude = NULL, redeemable_start_date = NULL, redeemable_end_date = NULL, filter_by_parent_offer = FALSE, started_since = NULL, started_before = NULL, ended_since = NULL, ended_before = NULL, redeemed = FALSE, statuses = NULL, reservations_only = FALSE, active_only = FALSE, return_full_response = FALSE, recurring_started_since = NULL, recurring_started_before = NULL, recurring_expiration_since = NULL, recurring_expiration_before = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling WalletApi$SearchOfferTransactions, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling WalletApi$SearchOfferTransactions, `account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling WalletApi$SearchOfferTransactions, `keyword` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling WalletApi$SearchOfferTransactions, `retailer_id` is not nullable")
      }

      if (!missing(`retailer_ids`) && is.null(`retailer_ids`)) {
        stop("Invalid value for `retailer_ids` when calling WalletApi$SearchOfferTransactions, `retailer_ids` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling WalletApi$SearchOfferTransactions, `retailer_location_id` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling WalletApi$SearchOfferTransactions, `retailer_location_ids` is not nullable")
      }

      if (!missing(`exclude_retailer_location_ids`) && is.null(`exclude_retailer_location_ids`)) {
        stop("Invalid value for `exclude_retailer_location_ids` when calling WalletApi$SearchOfferTransactions, `exclude_retailer_location_ids` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling WalletApi$SearchOfferTransactions, `offer_id` is not nullable")
      }

      if (!missing(`offer_ids`) && is.null(`offer_ids`)) {
        stop("Invalid value for `offer_ids` when calling WalletApi$SearchOfferTransactions, `offer_ids` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling WalletApi$SearchOfferTransactions, `offer_location_id` is not nullable")
      }

      if (!missing(`offer_location_ids`) && is.null(`offer_location_ids`)) {
        stop("Invalid value for `offer_location_ids` when calling WalletApi$SearchOfferTransactions, `offer_location_ids` is not nullable")
      }

      if (!missing(`offer_type`) && is.null(`offer_type`)) {
        stop("Invalid value for `offer_type` when calling WalletApi$SearchOfferTransactions, `offer_type` is not nullable")
      }

      if (!missing(`offer_types`) && is.null(`offer_types`)) {
        stop("Invalid value for `offer_types` when calling WalletApi$SearchOfferTransactions, `offer_types` is not nullable")
      }

      if (!missing(`special_offer_type`) && is.null(`special_offer_type`)) {
        stop("Invalid value for `special_offer_type` when calling WalletApi$SearchOfferTransactions, `special_offer_type` is not nullable")
      }

      if (!missing(`special_offer_types`) && is.null(`special_offer_types`)) {
        stop("Invalid value for `special_offer_types` when calling WalletApi$SearchOfferTransactions, `special_offer_types` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling WalletApi$SearchOfferTransactions, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling WalletApi$SearchOfferTransactions, `filter_ids` is not nullable")
      }

      if (!missing(`offer_audience_ids`) && is.null(`offer_audience_ids`)) {
        stop("Invalid value for `offer_audience_ids` when calling WalletApi$SearchOfferTransactions, `offer_audience_ids` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling WalletApi$SearchOfferTransactions, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling WalletApi$SearchOfferTransactions, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling WalletApi$SearchOfferTransactions, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling WalletApi$SearchOfferTransactions, `limit` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling WalletApi$SearchOfferTransactions, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling WalletApi$SearchOfferTransactions, `longitude` is not nullable")
      }

      if (!missing(`redeemable_start_date`) && is.null(`redeemable_start_date`)) {
        stop("Invalid value for `redeemable_start_date` when calling WalletApi$SearchOfferTransactions, `redeemable_start_date` is not nullable")
      }

      if (!missing(`redeemable_end_date`) && is.null(`redeemable_end_date`)) {
        stop("Invalid value for `redeemable_end_date` when calling WalletApi$SearchOfferTransactions, `redeemable_end_date` is not nullable")
      }

      if (!missing(`filter_by_parent_offer`) && is.null(`filter_by_parent_offer`)) {
        stop("Invalid value for `filter_by_parent_offer` when calling WalletApi$SearchOfferTransactions, `filter_by_parent_offer` is not nullable")
      }

      if (!missing(`started_since`) && is.null(`started_since`)) {
        stop("Invalid value for `started_since` when calling WalletApi$SearchOfferTransactions, `started_since` is not nullable")
      }

      if (!missing(`started_before`) && is.null(`started_before`)) {
        stop("Invalid value for `started_before` when calling WalletApi$SearchOfferTransactions, `started_before` is not nullable")
      }

      if (!missing(`ended_since`) && is.null(`ended_since`)) {
        stop("Invalid value for `ended_since` when calling WalletApi$SearchOfferTransactions, `ended_since` is not nullable")
      }

      if (!missing(`ended_before`) && is.null(`ended_before`)) {
        stop("Invalid value for `ended_before` when calling WalletApi$SearchOfferTransactions, `ended_before` is not nullable")
      }

      if (!missing(`redeemed`) && is.null(`redeemed`)) {
        stop("Invalid value for `redeemed` when calling WalletApi$SearchOfferTransactions, `redeemed` is not nullable")
      }

      if (!missing(`statuses`) && is.null(`statuses`)) {
        stop("Invalid value for `statuses` when calling WalletApi$SearchOfferTransactions, `statuses` is not nullable")
      }

      if (!missing(`reservations_only`) && is.null(`reservations_only`)) {
        stop("Invalid value for `reservations_only` when calling WalletApi$SearchOfferTransactions, `reservations_only` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling WalletApi$SearchOfferTransactions, `active_only` is not nullable")
      }

      if (!missing(`return_full_response`) && is.null(`return_full_response`)) {
        stop("Invalid value for `return_full_response` when calling WalletApi$SearchOfferTransactions, `return_full_response` is not nullable")
      }

      if (!missing(`recurring_started_since`) && is.null(`recurring_started_since`)) {
        stop("Invalid value for `recurring_started_since` when calling WalletApi$SearchOfferTransactions, `recurring_started_since` is not nullable")
      }

      if (!missing(`recurring_started_before`) && is.null(`recurring_started_before`)) {
        stop("Invalid value for `recurring_started_before` when calling WalletApi$SearchOfferTransactions, `recurring_started_before` is not nullable")
      }

      if (!missing(`recurring_expiration_since`) && is.null(`recurring_expiration_since`)) {
        stop("Invalid value for `recurring_expiration_since` when calling WalletApi$SearchOfferTransactions, `recurring_expiration_since` is not nullable")
      }

      if (!missing(`recurring_expiration_before`) && is.null(`recurring_expiration_before`)) {
        stop("Invalid value for `recurring_expiration_before` when calling WalletApi$SearchOfferTransactions, `recurring_expiration_before` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["retailerIds"]] <- `retailer_ids`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["excludeRetailerLocationIds"]] <- `exclude_retailer_location_ids`

      query_params[["offerId"]] <- `offer_id`

      query_params[["offerIds"]] <- `offer_ids`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["offerLocationIds"]] <- `offer_location_ids`

      if (!is.null(`offer_type`) && !(`offer_type` %in% c("VOUCHER", "COUPON", "PRODUCT", "MEDIA", "EVENT", "DEVICE"))) {
        stop("Invalid value for offer_type when calling WalletApi$SearchOfferTransactions. Must be [VOUCHER, COUPON, PRODUCT, MEDIA, EVENT, DEVICE].")
      }
      query_params[["offerType"]] <- `offer_type`

      query_params[["offerTypes"]] <- `offer_types`

      query_params[["specialOfferType"]] <- `special_offer_type`

      query_params[["specialOfferTypes"]] <- `special_offer_types`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["offerAudienceIds"]] <- `offer_audience_ids`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "ACTIVATED", "EXPIRES", "TITLE", "SUBTITLE", "DETAILS", "OFFER_TYPE", "SPECIAL_OFFER_TYPE", "OFFER_VISIBILITY", "REDEEMABLE_START", "REDEEMABLE_END", "CUSTOMER_ID", "CUSTOMER_DISPLAY", "RETAILER_ID", "RETAILER_NAME", "RETAILER_LOCATION_ID", "RETAILER_LOCATION_NAME", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY"))) {
        stop("Invalid value for sort_field when calling WalletApi$SearchOfferTransactions. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, REDEEMABLE_START, REDEEMABLE_END, CUSTOMER_ID, CUSTOMER_DISPLAY, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["redeemableStartDate"]] <- `redeemable_start_date`

      query_params[["redeemableEndDate"]] <- `redeemable_end_date`

      query_params[["filterByParentOffer"]] <- `filter_by_parent_offer`

      query_params[["startedSince"]] <- `started_since`

      query_params[["startedBefore"]] <- `started_before`

      query_params[["endedSince"]] <- `ended_since`

      query_params[["endedBefore"]] <- `ended_before`

      query_params[["redeemed"]] <- `redeemed`

      query_params[["statuses"]] <- `statuses`

      query_params[["reservationsOnly"]] <- `reservations_only`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["returnFullResponse"]] <- `return_full_response`

      query_params[["recurringStartedSince"]] <- `recurring_started_since`

      query_params[["recurringStartedBefore"]] <- `recurring_started_before`

      query_params[["recurringExpirationSince"]] <- `recurring_expiration_since`

      query_params[["recurringExpirationBefore"]] <- `recurring_expiration_before`

      local_var_url_path <- "/wallet/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[OfferTransactionResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Wallet Offer
    #'
    #' @param transaction_id The offer transaction id to remove
    #' @param status The status value to change to (0 or 1)
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param offer_location_id (optional) Offer Location Id
    #' @param currency_type (optional) Currency Type (default value: "CASH")
    #' @param use_points (optional) Use Points
    #' @param app_key (optional) App Key
    #' @param latitude (optional) The latitude location of the user
    #' @param longitude (optional) The latitude location of the user
    #' @param meta_data (optional) External custom client defined data
    #' @param return_full_response (optional) Determines whether to return a detailed version of the response (default value: FALSE)
    #' @param exception_membership_offer_ids (optional) Exception Offers, transaction audiences of these offers won't be removed out of the account when up
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferTransactionResponse
    UpdateOfferTransaction = function(transaction_id, status, device_id = NULL, account_id = NULL, offer_location_id = NULL, currency_type = "CASH", use_points = NULL, app_key = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, return_full_response = FALSE, exception_membership_offer_ids = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateOfferTransactionWithHttpInfo(transaction_id, status, device_id, account_id, offer_location_id, currency_type, use_points, app_key, latitude, longitude, meta_data, return_full_response, exception_membership_offer_ids, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Wallet Offer
    #'
    #' @param transaction_id The offer transaction id to remove
    #' @param status The status value to change to (0 or 1)
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param offer_location_id (optional) Offer Location Id
    #' @param currency_type (optional) Currency Type (default value: "CASH")
    #' @param use_points (optional) Use Points
    #' @param app_key (optional) App Key
    #' @param latitude (optional) The latitude location of the user
    #' @param longitude (optional) The latitude location of the user
    #' @param meta_data (optional) External custom client defined data
    #' @param return_full_response (optional) Determines whether to return a detailed version of the response (default value: FALSE)
    #' @param exception_membership_offer_ids (optional) Exception Offers, transaction audiences of these offers won't be removed out of the account when up
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferTransactionResponse) with additional information such as HTTP status code, headers
    UpdateOfferTransactionWithHttpInfo = function(transaction_id, status, device_id = NULL, account_id = NULL, offer_location_id = NULL, currency_type = "CASH", use_points = NULL, app_key = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, return_full_response = FALSE, exception_membership_offer_ids = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`transaction_id`)) {
        stop("Missing required parameter `transaction_id`.")
      }

      if (missing(`status`)) {
        stop("Missing required parameter `status`.")
      }

      if (!missing(`transaction_id`) && is.null(`transaction_id`)) {
        stop("Invalid value for `transaction_id` when calling WalletApi$UpdateOfferTransaction, `transaction_id` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling WalletApi$UpdateOfferTransaction, `status` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling WalletApi$UpdateOfferTransaction, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling WalletApi$UpdateOfferTransaction, `account_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling WalletApi$UpdateOfferTransaction, `offer_location_id` is not nullable")
      }

      if (!missing(`currency_type`) && is.null(`currency_type`)) {
        stop("Invalid value for `currency_type` when calling WalletApi$UpdateOfferTransaction, `currency_type` is not nullable")
      }

      if (!missing(`use_points`) && is.null(`use_points`)) {
        stop("Invalid value for `use_points` when calling WalletApi$UpdateOfferTransaction, `use_points` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling WalletApi$UpdateOfferTransaction, `app_key` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling WalletApi$UpdateOfferTransaction, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling WalletApi$UpdateOfferTransaction, `longitude` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling WalletApi$UpdateOfferTransaction, `meta_data` is not nullable")
      }

      if (!missing(`return_full_response`) && is.null(`return_full_response`)) {
        stop("Invalid value for `return_full_response` when calling WalletApi$UpdateOfferTransaction, `return_full_response` is not nullable")
      }

      if (!missing(`exception_membership_offer_ids`) && is.null(`exception_membership_offer_ids`)) {
        stop("Invalid value for `exception_membership_offer_ids` when calling WalletApi$UpdateOfferTransaction, `exception_membership_offer_ids` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["transactionId"]] <- `transaction_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["currencyType"]] <- `currency_type`

      query_params[["usePoints"]] <- `use_points`

      query_params[["appKey"]] <- `app_key`

      query_params[["status"]] <- `status`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["metaData"]] <- `meta_data`

      query_params[["returnFullResponse"]] <- `return_full_response`

      query_params[["exceptionMembershipOfferIds"]] <- `exception_membership_offer_ids`

      local_var_url_path <- "/wallet/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferTransactionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
