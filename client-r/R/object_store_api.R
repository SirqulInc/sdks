#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ObjectStore operations
#' @description ObjectStoreApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AddField  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_app_key <- "app_key_example" # character | The application key for updating an existing application
#' var_object_name <- "object_name_example" # character | The name of the object to add the field to
#' var_field_name <- "field_name_example" # character | field name The name of the field to add.
#' var_field_type <- "field_type_example" # character | field type The field type to create, supported types are: STRING, DATE, NUMBER, BOOLEAN, IDENTITY
#'
#' #Create Field
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AddField(var_account_id, var_app_key, var_object_name, var_field_name, var_field_typedata_file = "result.txt")
#' result <- api_instance$AddField(var_account_id, var_app_key, var_object_name, var_field_name, var_field_type)
#' dput(result)
#'
#'
#' ####################  CreateData  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | the name of the object to create data for
#' var_account_id <- 56 # integer | the account id (Optional)
#' var_body <- "body_example" # character |  (Optional)
#'
#' #Create Data
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateData(var_object_name, account_id = var_account_id, body = var_bodydata_file = "result.txt")
#' result <- api_instance$CreateData(var_object_name, account_id = var_account_id, body = var_body)
#' dput(result)
#'
#'
#' ####################  CreateObject  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_app_key <- "app_key_example" # character | The application key for updating an existing application
#' var_object_name <- "object_name_example" # character | The name of the object to create
#'
#' #Create Object
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateObject(var_account_id, var_app_key, var_object_namedata_file = "result.txt")
#' result <- api_instance$CreateObject(var_account_id, var_app_key, var_object_name)
#' dput(result)
#'
#'
#' ####################  DeleteData  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The name of the object to search upon
#' var_object_id <- "object_id_example" # character | objectId The id of the record to return
#' var_account_id <- 56 # integer | The account id of the logged in user (Optional)
#'
#' #Delete Data
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteData(var_object_name, var_object_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteData(var_object_name, var_object_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  DeleteField  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_app_key <- "app_key_example" # character | The application key for updating an existing application
#' var_object_name <- "object_name_example" # character | The name of the object to remove the field from
#' var_field_name <- "field_name_example" # character | field name The name of the field to remove.
#'
#' #Delete Field
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteField(var_account_id, var_app_key, var_object_name, var_field_namedata_file = "result.txt")
#' result <- api_instance$DeleteField(var_account_id, var_app_key, var_object_name, var_field_name)
#' dput(result)
#'
#'
#' ####################  DeleteObject  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_app_key <- "app_key_example" # character | the application key
#' var_object_name <- "object_name_example" # character | the name of the object to delete
#'
#' #Delete Object
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteObject(var_account_id, var_app_key, var_object_namedata_file = "result.txt")
#' result <- api_instance$DeleteObject(var_account_id, var_app_key, var_object_name)
#' dput(result)
#'
#'
#' ####################  GetData  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The name of the object to search upon
#' var_object_id <- "object_id_example" # character | objectId The id of the record to return
#' var_account_id <- 56 # integer | The account id of the logged in user (Optional)
#' var_include <- "include_example" # character |  (Optional)
#'
#' #Get Data
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetData(var_object_name, var_object_id, account_id = var_account_id, include = var_includedata_file = "result.txt")
#' result <- api_instance$GetData(var_object_name, var_object_id, account_id = var_account_id, include = var_include)
#' dput(result)
#'
#'
#' ####################  GetObject  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_app_key <- "app_key_example" # character | The application key for updating an existing application
#' var_object_name <- "object_name_example" # character | The name of the object to get the definition for
#'
#' #Get Object
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetObject(var_account_id, var_app_key, var_object_namedata_file = "result.txt")
#' result <- api_instance$GetObject(var_account_id, var_app_key, var_object_name)
#' dput(result)
#'
#'
#' ####################  SearchData  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The name of the object to search upon
#' var_count <- "count_example" # character | If true just return the record count of the search. False (default) will return the actual records
#' var_start <- 56 # integer | The start of the pagination
#' var_limit <- 56 # integer | The limit of the pagination
#' var_account_id <- 56 # integer | The account id of the logged in user (Optional)
#' var_criteria <- "criteria_example" # character | The search criteria (Optional)
#' var_order <- "order_example" # character | The order of results; comma seperated list of field names. Illegal field names will be ignored. Direction by defualt is ascending. Prepend a minus to the field name to make that field descending. (Optional)
#' var_include <- "include_example" # character |  (Optional)
#'
#' #Search Data
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchData(var_object_name, var_count, var_start, var_limit, account_id = var_account_id, criteria = var_criteria, order = var_order, include = var_includedata_file = "result.txt")
#' result <- api_instance$SearchData(var_object_name, var_count, var_start, var_limit, account_id = var_account_id, criteria = var_criteria, order = var_order, include = var_include)
#' dput(result)
#'
#'
#' ####################  SearchObject  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the logged in user
#' var_app_key <- "app_key_example" # character | The application key for updating an existing application
#' var_start <- 56 # integer | The start of the pagination
#' var_limit <- 56 # integer | The limit of the pagination
#' var_keyword <- "keyword_example" # character | The name of the object(s) to search for, can be a partial match (Optional)
#'
#' #Search Objects
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchObject(var_account_id, var_app_key, var_start, var_limit, keyword = var_keyworddata_file = "result.txt")
#' result <- api_instance$SearchObject(var_account_id, var_app_key, var_start, var_limit, keyword = var_keyword)
#' dput(result)
#'
#'
#' ####################  UpdateData  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The name of the object to search upon
#' var_object_id <- "object_id_example" # character | objectId The id of the record to return
#' var_account_id <- 56 # integer | The account id of the logged in user (Optional)
#' var_body <- "body_example" # character |  (Optional)
#'
#' #Update Data
#' api_instance <- ObjectStoreApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateData(var_object_name, var_object_id, account_id = var_account_id, body = var_bodydata_file = "result.txt")
#' result <- api_instance$UpdateData(var_object_name, var_object_id, account_id = var_account_id, body = var_body)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ObjectStoreApi <- R6::R6Class(
  "ObjectStoreApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ObjectStoreApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Field
    #'
    #' @param account_id The account id of the logged in user
    #' @param app_key The application key for updating an existing application
    #' @param object_name The name of the object to add the field to
    #' @param field_name field name The name of the field to add.
    #' @param field_type field type The field type to create, supported types are: STRING, DATE, NUMBER, BOOLEAN, IDENTITY
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    AddField = function(account_id, app_key, object_name, field_name, field_type, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AddFieldWithHttpInfo(account_id, app_key, object_name, field_name, field_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Field
    #'
    #' @param account_id The account id of the logged in user
    #' @param app_key The application key for updating an existing application
    #' @param object_name The name of the object to add the field to
    #' @param field_name field name The name of the field to add.
    #' @param field_type field type The field type to create, supported types are: STRING, DATE, NUMBER, BOOLEAN, IDENTITY
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    AddFieldWithHttpInfo = function(account_id, app_key, object_name, field_name, field_type, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`field_name`)) {
        stop("Missing required parameter `field_name`.")
      }

      if (missing(`field_type`)) {
        stop("Missing required parameter `field_type`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$AddField, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ObjectStoreApi$AddField, `app_key` is not nullable")
      }

      if (!missing(`object_name`) && is.null(`object_name`)) {
        stop("Invalid value for `object_name` when calling ObjectStoreApi$AddField, `object_name` is not nullable")
      }

      if (!missing(`field_name`) && is.null(`field_name`)) {
        stop("Invalid value for `field_name` when calling ObjectStoreApi$AddField, `field_name` is not nullable")
      }

      if (!missing(`field_type`) && is.null(`field_type`)) {
        stop("Invalid value for `field_type` when calling ObjectStoreApi$AddField, `field_type` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["objectName"]] <- `object_name`

      query_params[["fieldName"]] <- `field_name`

      query_params[["fieldType"]] <- `field_type`

      local_var_url_path <- "/object/field/add"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Data
    #'
    #' @param object_name the name of the object to create data for
    #' @param account_id (optional) the account id
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    CreateData = function(object_name, account_id = NULL, body = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateDataWithHttpInfo(object_name, account_id, body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Data
    #'
    #' @param object_name the name of the object to create data for
    #' @param account_id (optional) the account id
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    CreateDataWithHttpInfo = function(object_name, account_id = NULL, body = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (!missing(`object_name`) && is.null(`object_name`)) {
        stop("Invalid value for `object_name` when calling ObjectStoreApi$CreateData, `object_name` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$CreateData, `account_id` is not nullable")
      }

      if (!missing(`body`) && is.null(`body`)) {
        stop("Invalid value for `body` when calling ObjectStoreApi$CreateData, `body` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/object/data/{objectName}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{objectName\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Object
    #'
    #' @param account_id The account id of the logged in user
    #' @param app_key The application key for updating an existing application
    #' @param object_name The name of the object to create
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    CreateObject = function(account_id, app_key, object_name, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateObjectWithHttpInfo(account_id, app_key, object_name, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Object
    #'
    #' @param account_id The account id of the logged in user
    #' @param app_key The application key for updating an existing application
    #' @param object_name The name of the object to create
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    CreateObjectWithHttpInfo = function(account_id, app_key, object_name, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$CreateObject, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ObjectStoreApi$CreateObject, `app_key` is not nullable")
      }

      if (!missing(`object_name`) && is.null(`object_name`)) {
        stop("Invalid value for `object_name` when calling ObjectStoreApi$CreateObject, `object_name` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["objectName"]] <- `object_name`

      local_var_url_path <- "/object/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Data
    #'
    #' @param object_name The name of the object to search upon
    #' @param object_id objectId The id of the record to return
    #' @param account_id (optional) The account id of the logged in user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    DeleteData = function(object_name, object_id, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteDataWithHttpInfo(object_name, object_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Data
    #'
    #' @param object_name The name of the object to search upon
    #' @param object_id objectId The id of the record to return
    #' @param account_id (optional) The account id of the logged in user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    DeleteDataWithHttpInfo = function(object_name, object_id, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_id`)) {
        stop("Missing required parameter `object_id`.")
      }

      if (!missing(`object_name`) && is.null(`object_name`)) {
        stop("Invalid value for `object_name` when calling ObjectStoreApi$DeleteData, `object_name` is not nullable")
      }

      if (!missing(`object_id`) && is.null(`object_id`)) {
        stop("Invalid value for `object_id` when calling ObjectStoreApi$DeleteData, `object_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$DeleteData, `account_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      local_var_url_path <- "/object/data/{objectName}/{objectId}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{objectName\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_id`)) {
        local_var_url_path <- gsub("\\{objectId\\}", URLencode(as.character(`object_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Field
    #'
    #' @param account_id The account id of the logged in user
    #' @param app_key The application key for updating an existing application
    #' @param object_name The name of the object to remove the field from
    #' @param field_name field name The name of the field to remove.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    DeleteField = function(account_id, app_key, object_name, field_name, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteFieldWithHttpInfo(account_id, app_key, object_name, field_name, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Field
    #'
    #' @param account_id The account id of the logged in user
    #' @param app_key The application key for updating an existing application
    #' @param object_name The name of the object to remove the field from
    #' @param field_name field name The name of the field to remove.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    DeleteFieldWithHttpInfo = function(account_id, app_key, object_name, field_name, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`field_name`)) {
        stop("Missing required parameter `field_name`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$DeleteField, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ObjectStoreApi$DeleteField, `app_key` is not nullable")
      }

      if (!missing(`object_name`) && is.null(`object_name`)) {
        stop("Invalid value for `object_name` when calling ObjectStoreApi$DeleteField, `object_name` is not nullable")
      }

      if (!missing(`field_name`) && is.null(`field_name`)) {
        stop("Invalid value for `field_name` when calling ObjectStoreApi$DeleteField, `field_name` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["objectName"]] <- `object_name`

      query_params[["fieldName"]] <- `field_name`

      local_var_url_path <- "/object/field/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Object
    #'
    #' @param account_id the id of the logged in user
    #' @param app_key the application key
    #' @param object_name the name of the object to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    DeleteObject = function(account_id, app_key, object_name, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteObjectWithHttpInfo(account_id, app_key, object_name, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Object
    #'
    #' @param account_id the id of the logged in user
    #' @param app_key the application key
    #' @param object_name the name of the object to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    DeleteObjectWithHttpInfo = function(account_id, app_key, object_name, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$DeleteObject, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ObjectStoreApi$DeleteObject, `app_key` is not nullable")
      }

      if (!missing(`object_name`) && is.null(`object_name`)) {
        stop("Invalid value for `object_name` when calling ObjectStoreApi$DeleteObject, `object_name` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["objectName"]] <- `object_name`

      local_var_url_path <- "/object/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Data
    #'
    #' @param object_name The name of the object to search upon
    #' @param object_id objectId The id of the record to return
    #' @param account_id (optional) The account id of the logged in user
    #' @param include (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    GetData = function(object_name, object_id, account_id = NULL, include = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetDataWithHttpInfo(object_name, object_id, account_id, include, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Data
    #'
    #' @param object_name The name of the object to search upon
    #' @param object_id objectId The id of the record to return
    #' @param account_id (optional) The account id of the logged in user
    #' @param include (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    GetDataWithHttpInfo = function(object_name, object_id, account_id = NULL, include = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_id`)) {
        stop("Missing required parameter `object_id`.")
      }

      if (!missing(`object_name`) && is.null(`object_name`)) {
        stop("Invalid value for `object_name` when calling ObjectStoreApi$GetData, `object_name` is not nullable")
      }

      if (!missing(`object_id`) && is.null(`object_id`)) {
        stop("Invalid value for `object_id` when calling ObjectStoreApi$GetData, `object_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$GetData, `account_id` is not nullable")
      }

      if (!missing(`include`) && is.null(`include`)) {
        stop("Invalid value for `include` when calling ObjectStoreApi$GetData, `include` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["include"]] <- `include`

      local_var_url_path <- "/object/data/{objectName}/{objectId}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{objectName\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_id`)) {
        local_var_url_path <- gsub("\\{objectId\\}", URLencode(as.character(`object_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Object
    #'
    #' @param account_id The account id of the logged in user
    #' @param app_key The application key for updating an existing application
    #' @param object_name The name of the object to get the definition for
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    GetObject = function(account_id, app_key, object_name, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetObjectWithHttpInfo(account_id, app_key, object_name, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Object
    #'
    #' @param account_id The account id of the logged in user
    #' @param app_key The application key for updating an existing application
    #' @param object_name The name of the object to get the definition for
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    GetObjectWithHttpInfo = function(account_id, app_key, object_name, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$GetObject, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ObjectStoreApi$GetObject, `app_key` is not nullable")
      }

      if (!missing(`object_name`) && is.null(`object_name`)) {
        stop("Invalid value for `object_name` when calling ObjectStoreApi$GetObject, `object_name` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["objectName"]] <- `object_name`

      local_var_url_path <- "/object/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Data
    #'
    #' @param object_name The name of the object to search upon
    #' @param count If true just return the record count of the search. False (default) will return the actual records
    #' @param start The start of the pagination
    #' @param limit The limit of the pagination
    #' @param account_id (optional) The account id of the logged in user
    #' @param criteria (optional) The search criteria
    #' @param order (optional) The order of results; comma seperated list of field names. Illegal field names will be ignored. Direction by defualt is ascending. Prepend a minus to the field name to make that field descending.
    #' @param include (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    SearchData = function(object_name, count, start, limit, account_id = NULL, criteria = NULL, order = NULL, include = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchDataWithHttpInfo(object_name, count, start, limit, account_id, criteria, order, include, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Data
    #'
    #' @param object_name The name of the object to search upon
    #' @param count If true just return the record count of the search. False (default) will return the actual records
    #' @param start The start of the pagination
    #' @param limit The limit of the pagination
    #' @param account_id (optional) The account id of the logged in user
    #' @param criteria (optional) The search criteria
    #' @param order (optional) The order of results; comma seperated list of field names. Illegal field names will be ignored. Direction by defualt is ascending. Prepend a minus to the field name to make that field descending.
    #' @param include (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    SearchDataWithHttpInfo = function(object_name, count, start, limit, account_id = NULL, criteria = NULL, order = NULL, include = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`count`)) {
        stop("Missing required parameter `count`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`object_name`) && is.null(`object_name`)) {
        stop("Invalid value for `object_name` when calling ObjectStoreApi$SearchData, `object_name` is not nullable")
      }

      if (!missing(`count`) && is.null(`count`)) {
        stop("Invalid value for `count` when calling ObjectStoreApi$SearchData, `count` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ObjectStoreApi$SearchData, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ObjectStoreApi$SearchData, `limit` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$SearchData, `account_id` is not nullable")
      }

      if (!missing(`criteria`) && is.null(`criteria`)) {
        stop("Invalid value for `criteria` when calling ObjectStoreApi$SearchData, `criteria` is not nullable")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling ObjectStoreApi$SearchData, `order` is not nullable")
      }

      if (!missing(`include`) && is.null(`include`)) {
        stop("Invalid value for `include` when calling ObjectStoreApi$SearchData, `include` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["criteria"]] <- `criteria`

      query_params[["count"]] <- `count`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["order"]] <- `order`

      query_params[["include"]] <- `include`

      local_var_url_path <- "/object/data/{objectName}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{objectName\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Objects
    #'
    #' @param account_id The account id of the logged in user
    #' @param app_key The application key for updating an existing application
    #' @param start The start of the pagination
    #' @param limit The limit of the pagination
    #' @param keyword (optional) The name of the object(s) to search for, can be a partial match
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    SearchObject = function(account_id, app_key, start, limit, keyword = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchObjectWithHttpInfo(account_id, app_key, start, limit, keyword, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Objects
    #'
    #' @param account_id The account id of the logged in user
    #' @param app_key The application key for updating an existing application
    #' @param start The start of the pagination
    #' @param limit The limit of the pagination
    #' @param keyword (optional) The name of the object(s) to search for, can be a partial match
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    SearchObjectWithHttpInfo = function(account_id, app_key, start, limit, keyword = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$SearchObject, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling ObjectStoreApi$SearchObject, `app_key` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling ObjectStoreApi$SearchObject, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling ObjectStoreApi$SearchObject, `limit` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling ObjectStoreApi$SearchObject, `keyword` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["keyword"]] <- `keyword`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/object/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Data
    #'
    #' @param object_name The name of the object to search upon
    #' @param object_id objectId The id of the record to return
    #' @param account_id (optional) The account id of the logged in user
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ObjectStoreResponse
    UpdateData = function(object_name, object_id, account_id = NULL, body = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateDataWithHttpInfo(object_name, object_id, account_id, body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Data
    #'
    #' @param object_name The name of the object to search upon
    #' @param object_id objectId The id of the record to return
    #' @param account_id (optional) The account id of the logged in user
    #' @param body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ObjectStoreResponse) with additional information such as HTTP status code, headers
    UpdateDataWithHttpInfo = function(object_name, object_id, account_id = NULL, body = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_id`)) {
        stop("Missing required parameter `object_id`.")
      }

      if (!missing(`object_name`) && is.null(`object_name`)) {
        stop("Invalid value for `object_name` when calling ObjectStoreApi$UpdateData, `object_name` is not nullable")
      }

      if (!missing(`object_id`) && is.null(`object_id`)) {
        stop("Invalid value for `object_id` when calling ObjectStoreApi$UpdateData, `object_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling ObjectStoreApi$UpdateData, `account_id` is not nullable")
      }

      if (!missing(`body`) && is.null(`body`)) {
        stop("Invalid value for `body` when calling ObjectStoreApi$UpdateData, `body` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/object/data/{objectName}/{objectId}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{objectName\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_id`)) {
        local_var_url_path <- gsub("\\{objectId\\}", URLencode(as.character(`object_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ObjectStoreResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
