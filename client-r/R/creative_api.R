#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Creative operations
#' @description CreativeApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AddPreview  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the account
#' var_creative_id <- 56 # integer | The id of the creative that want to enable preview. The type of the creative should be CONFIG, otherwise no action will be applied.
#'
#' #Add Preview
#' api_instance <- CreativeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AddPreview(var_account_id, var_creative_iddata_file = "result.txt")
#' result <- api_instance$AddPreview(var_account_id, var_creative_id)
#' dput(result)
#'
#'
#' ####################  AdsFind  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key, if provided return missions specific for the app. Will always return mission levels that are app agnostic.
#' var_randomize <- "randomize_example" # character | return a random set of results, default is true. If false returns in nature order.
#' var_targeted_ads_only <- "targeted_ads_only_example" # character | return only ads targets to the specific app, no global ads.
#' var_type <- "type_example" # character | The type of ads to get, possible values are: BANNER, LEADERBOARD, SKYSCRAPER, FULL, VIDEO, ZIP, CONFIG. Use this instead of suffix. (Optional)
#' var_account_id <- 56 # integer | The logged in user. (Optional)
#' var_app_version <- "app_version_example" # character | The version of the application, will not return levels newer than the appVersion. (Optional)
#' var_latitude <- 3.4 # numeric | The current location of the requesting device (Optional)
#' var_longitude <- 3.4 # numeric | The current location of the requesting device (Optional)
#' var_device <- "device_example" # character | Should use deviceId if possible. The name of the requesting device; possible values are: Android, iPhone, iPad, iPod, etc. use /audience/devices for list. (Optional)
#' var_device_identifier <- 56 # integer | The device ID of the requesting device, use /audience/devices for list (Optional)
#' var_device_version <- "device_version_example" # character | The requesting device version; examples are: 2.3, 5.1.4, 6.1.4. Supports X, X.X, or X.X.X formated string. (Optional)
#' var_start <- 56 # integer | The index into the record set to start with. (Optional)
#' var_limit <- 56 # integer | The total number of record to return. (Optional)
#' var_include_audiences <- "include_audiences_example" # character | If true then return the audience data in the response. Default is false. (Optional)
#' var_allocates_tickets <- "allocates_tickets_example" # character | If true/false only return missions whose game levels allocate (or don't allocate) tickets. Do not provide a value to return both. (Optional)
#' var_mission_ids <- "mission_ids_example" # character | return only ads from the specified campaigns. (Optional)
#'
#' #Find Missions
#' api_instance <- CreativeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdsFind(var_app_key, var_randomize, var_targeted_ads_only, type = var_type, account_id = var_account_id, app_version = var_app_version, latitude = var_latitude, longitude = var_longitude, device = var_device, device_identifier = var_device_identifier, device_version = var_device_version, start = var_start, limit = var_limit, include_audiences = var_include_audiences, allocates_tickets = var_allocates_tickets, mission_ids = var_mission_idsdata_file = "result.txt")
#' result <- api_instance$AdsFind(var_app_key, var_randomize, var_targeted_ads_only, type = var_type, account_id = var_account_id, app_version = var_app_version, latitude = var_latitude, longitude = var_longitude, device = var_device, device_identifier = var_device_identifier, device_version = var_device_version, start = var_start, limit = var_limit, include_audiences = var_include_audiences, allocates_tickets = var_allocates_tickets, mission_ids = var_mission_ids)
#' dput(result)
#'
#'
#' ####################  CreateCreative  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_name <- "name_example" # character | The name of the level.
#' var_active <- "active_example" # character | If true set the game level as active. Default is false.
#' var_wait_for_asset <- "wait_for_asset_example" # character | determines whether the response will wait until the asset gets created
#' var_description <- "description_example" # character | The description of the level. (Optional)
#' var_asset_image_id <- 56 # integer | The asset Id of the level image. (Optional)
#' var_action <- "action_example" # character | This parameter is deprecated. deprecated use data field (Optional)
#' var_data <- "data_example" # character | The creative data, json based format depending on type. If not using action then data is required. (Optional)
#' var_suffix <- "suffix_example" # character | This parameter is deprecated. deprecated use type field (Optional)
#' var_type <- "type_example" # character | The type of creative. If not using suffix then type is required. (Optional)
#' var_balance <- 3.4 # numeric | Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited. (Optional)
#' var_reference_id <- 56 # integer | if creative related so some other content provided the id (Optional)
#' var_app_version <- "app_version_example" # character | The version of the application, will not return creatives newer than the appVersion. Only used when requesting application configuration creatives. (Optional)
#' var_mission_id <- 56 # integer | Assign the creative to a campaign for timing and audience matching. (Optional)
#' var_offer_id <- 56 # integer | the id of the offer (Optional)
#'
#' #Create Creative
#' api_instance <- CreativeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateCreative(var_account_id, var_name, var_active, var_wait_for_asset, description = var_description, asset_image_id = var_asset_image_id, action = var_action, data = var_data, suffix = var_suffix, type = var_type, balance = var_balance, reference_id = var_reference_id, app_version = var_app_version, mission_id = var_mission_id, offer_id = var_offer_iddata_file = "result.txt")
#' result <- api_instance$CreateCreative(var_account_id, var_name, var_active, var_wait_for_asset, description = var_description, asset_image_id = var_asset_image_id, action = var_action, data = var_data, suffix = var_suffix, type = var_type, balance = var_balance, reference_id = var_reference_id, app_version = var_app_version, mission_id = var_mission_id, offer_id = var_offer_id)
#' dput(result)
#'
#'
#' ####################  DeleteCreative  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_creative_id <- 56 # integer | the id of the creative to delete
#'
#' #Delete Creative
#' api_instance <- CreativeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteCreative(var_account_id, var_creative_iddata_file = "result.txt")
#' result <- api_instance$DeleteCreative(var_account_id, var_creative_id)
#' dput(result)
#'
#'
#' ####################  GetCreative  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_creative_id <- 56 # integer | the ID of the creative to get
#'
#' #Get Creative
#' api_instance <- CreativeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCreative(var_account_id, var_creative_iddata_file = "result.txt")
#' result <- api_instance$GetCreative(var_account_id, var_creative_id)
#' dput(result)
#'
#'
#' ####################  GetCreativesByApplication  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_app_key <- "app_key_example" # character | the application key
#' var_start <- 56 # integer | Start the result set at some index.
#' var_limit <- 56 # integer | Limit the result to some number.
#' var_mission_id <- 56 # integer | Creatives contained in the provided mission. (Optional)
#' var_keyword <- "keyword_example" # character | Match the keyword to the owner name or level name. (Optional)
#'
#' #Search Creatives
#' api_instance <- CreativeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCreativesByApplication(var_account_id, var_app_key, var_start, var_limit, mission_id = var_mission_id, keyword = var_keyworddata_file = "result.txt")
#' result <- api_instance$GetCreativesByApplication(var_account_id, var_app_key, var_start, var_limit, mission_id = var_mission_id, keyword = var_keyword)
#' dput(result)
#'
#'
#' ####################  RemovePreview  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the ID of the logged in user
#' var_creative_id <- 56 # integer | the ID of the creative to remove preview
#'
#' #Remove Preview
#' api_instance <- CreativeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RemovePreview(var_account_id, var_creative_iddata_file = "result.txt")
#' result <- api_instance$RemovePreview(var_account_id, var_creative_id)
#' dput(result)
#'
#'
#' ####################  UpdateCreative  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_creative_id <- 56 # integer | the creative Id to upate.
#' var_name <- "name_example" # character | The name of the level. (Optional)
#' var_description <- "description_example" # character | The description of the level. (Optional)
#' var_asset_image_id <- 56 # integer | The asset Id of the level image. (Optional)
#' var_action <- "action_example" # character | This parameter is deprecated. deprecated use data field (Optional)
#' var_data <- "data_example" # character | The creative data, json based format depending on type (Optional)
#' var_suffix <- "suffix_example" # character | This parameter is deprecated. deprecated use type field (Optional)
#' var_type <- "type_example" # character | The type of creative. (Optional)
#' var_balance <- 3.4 # numeric | Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited. (Optional)
#' var_active <- "active_example" # character | If true set the game level as active. Default is false. (Optional)
#' var_reference_id <- 56 # integer | if creative related so some other content provided the id (Optional)
#' var_app_version <- "app_version_example" # character | The version of the application, will not return creatives newer than the appVersion. Only used when requesting application configuration creatives. (Optional)
#' var_mission_id <- 56 # integer | Assign the creative to a campaign for timing and audience matching. (Optional)
#'
#' #Update Creative
#' api_instance <- CreativeApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateCreative(var_account_id, var_creative_id, name = var_name, description = var_description, asset_image_id = var_asset_image_id, action = var_action, data = var_data, suffix = var_suffix, type = var_type, balance = var_balance, active = var_active, reference_id = var_reference_id, app_version = var_app_version, mission_id = var_mission_iddata_file = "result.txt")
#' result <- api_instance$UpdateCreative(var_account_id, var_creative_id, name = var_name, description = var_description, asset_image_id = var_asset_image_id, action = var_action, data = var_data, suffix = var_suffix, type = var_type, balance = var_balance, active = var_active, reference_id = var_reference_id, app_version = var_app_version, mission_id = var_mission_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CreativeApi <- R6::R6Class(
  "CreativeApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CreativeApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Add Preview
    #'
    #' @param account_id the id of the account
    #' @param creative_id The id of the creative that want to enable preview. The type of the creative should be CONFIG, otherwise no action will be applied.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    AddPreview = function(account_id, creative_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AddPreviewWithHttpInfo(account_id, creative_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Add Preview
    #'
    #' @param account_id the id of the account
    #' @param creative_id The id of the creative that want to enable preview. The type of the creative should be CONFIG, otherwise no action will be applied.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    AddPreviewWithHttpInfo = function(account_id, creative_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`creative_id`)) {
        stop("Missing required parameter `creative_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CreativeApi$AddPreview, `account_id` is not nullable")
      }

      if (!missing(`creative_id`) && is.null(`creative_id`)) {
        stop("Invalid value for `creative_id` when calling CreativeApi$AddPreview, `creative_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["creativeId"]] <- `creative_id`

      local_var_url_path <- "/creative/addpreview"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Find Missions
    #'
    #' @param app_key The application key, if provided return missions specific for the app. Will always return mission levels that are app agnostic.
    #' @param randomize return a random set of results, default is true. If false returns in nature order.
    #' @param targeted_ads_only return only ads targets to the specific app, no global ads.
    #' @param type (optional) The type of ads to get, possible values are: BANNER, LEADERBOARD, SKYSCRAPER, FULL, VIDEO, ZIP, CONFIG. Use this instead of suffix.
    #' @param account_id (optional) The logged in user.
    #' @param app_version (optional) The version of the application, will not return levels newer than the appVersion.
    #' @param latitude (optional) The current location of the requesting device
    #' @param longitude (optional) The current location of the requesting device
    #' @param device (optional) Should use deviceId if possible. The name of the requesting device; possible values are: Android, iPhone, iPad, iPod, etc. use /audience/devices for list.
    #' @param device_identifier (optional) The device ID of the requesting device, use /audience/devices for list
    #' @param device_version (optional) The requesting device version; examples are: 2.3, 5.1.4, 6.1.4. Supports X, X.X, or X.X.X formated string.
    #' @param start (optional) The index into the record set to start with.
    #' @param limit (optional) The total number of record to return.
    #' @param include_audiences (optional) If true then return the audience data in the response. Default is false.
    #' @param allocates_tickets (optional) If true/false only return missions whose game levels allocate (or don't allocate) tickets. Do not provide a value to return both.
    #' @param mission_ids (optional) return only ads from the specified campaigns.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[MissionResponse]
    AdsFind = function(app_key, randomize, targeted_ads_only, type = NULL, account_id = NULL, app_version = NULL, latitude = NULL, longitude = NULL, device = NULL, device_identifier = NULL, device_version = NULL, start = NULL, limit = NULL, include_audiences = NULL, allocates_tickets = NULL, mission_ids = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdsFindWithHttpInfo(app_key, randomize, targeted_ads_only, type, account_id, app_version, latitude, longitude, device, device_identifier, device_version, start, limit, include_audiences, allocates_tickets, mission_ids, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Find Missions
    #'
    #' @param app_key The application key, if provided return missions specific for the app. Will always return mission levels that are app agnostic.
    #' @param randomize return a random set of results, default is true. If false returns in nature order.
    #' @param targeted_ads_only return only ads targets to the specific app, no global ads.
    #' @param type (optional) The type of ads to get, possible values are: BANNER, LEADERBOARD, SKYSCRAPER, FULL, VIDEO, ZIP, CONFIG. Use this instead of suffix.
    #' @param account_id (optional) The logged in user.
    #' @param app_version (optional) The version of the application, will not return levels newer than the appVersion.
    #' @param latitude (optional) The current location of the requesting device
    #' @param longitude (optional) The current location of the requesting device
    #' @param device (optional) Should use deviceId if possible. The name of the requesting device; possible values are: Android, iPhone, iPad, iPod, etc. use /audience/devices for list.
    #' @param device_identifier (optional) The device ID of the requesting device, use /audience/devices for list
    #' @param device_version (optional) The requesting device version; examples are: 2.3, 5.1.4, 6.1.4. Supports X, X.X, or X.X.X formated string.
    #' @param start (optional) The index into the record set to start with.
    #' @param limit (optional) The total number of record to return.
    #' @param include_audiences (optional) If true then return the audience data in the response. Default is false.
    #' @param allocates_tickets (optional) If true/false only return missions whose game levels allocate (or don't allocate) tickets. Do not provide a value to return both.
    #' @param mission_ids (optional) return only ads from the specified campaigns.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[MissionResponse]) with additional information such as HTTP status code, headers
    AdsFindWithHttpInfo = function(app_key, randomize, targeted_ads_only, type = NULL, account_id = NULL, app_version = NULL, latitude = NULL, longitude = NULL, device = NULL, device_identifier = NULL, device_version = NULL, start = NULL, limit = NULL, include_audiences = NULL, allocates_tickets = NULL, mission_ids = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`randomize`)) {
        stop("Missing required parameter `randomize`.")
      }

      if (missing(`targeted_ads_only`)) {
        stop("Missing required parameter `targeted_ads_only`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling CreativeApi$AdsFind, `app_key` is not nullable")
      }

      if (!missing(`randomize`) && is.null(`randomize`)) {
        stop("Invalid value for `randomize` when calling CreativeApi$AdsFind, `randomize` is not nullable")
      }

      if (!missing(`targeted_ads_only`) && is.null(`targeted_ads_only`)) {
        stop("Invalid value for `targeted_ads_only` when calling CreativeApi$AdsFind, `targeted_ads_only` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling CreativeApi$AdsFind, `type` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CreativeApi$AdsFind, `account_id` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling CreativeApi$AdsFind, `app_version` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling CreativeApi$AdsFind, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling CreativeApi$AdsFind, `longitude` is not nullable")
      }

      if (!missing(`device`) && is.null(`device`)) {
        stop("Invalid value for `device` when calling CreativeApi$AdsFind, `device` is not nullable")
      }

      if (!missing(`device_identifier`) && is.null(`device_identifier`)) {
        stop("Invalid value for `device_identifier` when calling CreativeApi$AdsFind, `device_identifier` is not nullable")
      }

      if (!missing(`device_version`) && is.null(`device_version`)) {
        stop("Invalid value for `device_version` when calling CreativeApi$AdsFind, `device_version` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling CreativeApi$AdsFind, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling CreativeApi$AdsFind, `limit` is not nullable")
      }

      if (!missing(`include_audiences`) && is.null(`include_audiences`)) {
        stop("Invalid value for `include_audiences` when calling CreativeApi$AdsFind, `include_audiences` is not nullable")
      }

      if (!missing(`allocates_tickets`) && is.null(`allocates_tickets`)) {
        stop("Invalid value for `allocates_tickets` when calling CreativeApi$AdsFind, `allocates_tickets` is not nullable")
      }

      if (!missing(`mission_ids`) && is.null(`mission_ids`)) {
        stop("Invalid value for `mission_ids` when calling CreativeApi$AdsFind, `mission_ids` is not nullable")
      }

      query_params[["appKey"]] <- `app_key`

      query_params[["type"]] <- `type`

      query_params[["accountId"]] <- `account_id`

      query_params[["appVersion"]] <- `app_version`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["device"]] <- `device`

      query_params[["deviceIdentifier"]] <- `device_identifier`

      query_params[["deviceVersion"]] <- `device_version`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["includeAudiences"]] <- `include_audiences`

      query_params[["allocatesTickets"]] <- `allocates_tickets`

      query_params[["randomize"]] <- `randomize`

      query_params[["targetedAdsOnly"]] <- `targeted_ads_only`

      query_params[["missionIds"]] <- `mission_ids`

      local_var_url_path <- "/ads/find"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[MissionResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Creative
    #'
    #' @param account_id The logged in user.
    #' @param name The name of the level.
    #' @param active If true set the game level as active. Default is false.
    #' @param wait_for_asset determines whether the response will wait until the asset gets created
    #' @param description (optional) The description of the level.
    #' @param asset_image_id (optional) The asset Id of the level image.
    #' @param action (optional) This parameter is deprecated. deprecated use data field
    #' @param data (optional) The creative data, json based format depending on type. If not using action then data is required.
    #' @param suffix (optional) This parameter is deprecated. deprecated use type field
    #' @param type (optional) The type of creative. If not using suffix then type is required.
    #' @param balance (optional) Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited.
    #' @param reference_id (optional) if creative related so some other content provided the id
    #' @param app_version (optional) The version of the application, will not return creatives newer than the appVersion. Only used when requesting application configuration creatives.
    #' @param mission_id (optional) Assign the creative to a campaign for timing and audience matching.
    #' @param offer_id (optional) the id of the offer
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CreativeResponse
    CreateCreative = function(account_id, name, active, wait_for_asset, description = NULL, asset_image_id = NULL, action = NULL, data = NULL, suffix = NULL, type = NULL, balance = NULL, reference_id = NULL, app_version = NULL, mission_id = NULL, offer_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateCreativeWithHttpInfo(account_id, name, active, wait_for_asset, description, asset_image_id, action, data, suffix, type, balance, reference_id, app_version, mission_id, offer_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Creative
    #'
    #' @param account_id The logged in user.
    #' @param name The name of the level.
    #' @param active If true set the game level as active. Default is false.
    #' @param wait_for_asset determines whether the response will wait until the asset gets created
    #' @param description (optional) The description of the level.
    #' @param asset_image_id (optional) The asset Id of the level image.
    #' @param action (optional) This parameter is deprecated. deprecated use data field
    #' @param data (optional) The creative data, json based format depending on type. If not using action then data is required.
    #' @param suffix (optional) This parameter is deprecated. deprecated use type field
    #' @param type (optional) The type of creative. If not using suffix then type is required.
    #' @param balance (optional) Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited.
    #' @param reference_id (optional) if creative related so some other content provided the id
    #' @param app_version (optional) The version of the application, will not return creatives newer than the appVersion. Only used when requesting application configuration creatives.
    #' @param mission_id (optional) Assign the creative to a campaign for timing and audience matching.
    #' @param offer_id (optional) the id of the offer
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CreativeResponse) with additional information such as HTTP status code, headers
    CreateCreativeWithHttpInfo = function(account_id, name, active, wait_for_asset, description = NULL, asset_image_id = NULL, action = NULL, data = NULL, suffix = NULL, type = NULL, balance = NULL, reference_id = NULL, app_version = NULL, mission_id = NULL, offer_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (missing(`active`)) {
        stop("Missing required parameter `active`.")
      }

      if (missing(`wait_for_asset`)) {
        stop("Missing required parameter `wait_for_asset`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CreativeApi$CreateCreative, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling CreativeApi$CreateCreative, `name` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling CreativeApi$CreateCreative, `active` is not nullable")
      }

      if (!missing(`wait_for_asset`) && is.null(`wait_for_asset`)) {
        stop("Invalid value for `wait_for_asset` when calling CreativeApi$CreateCreative, `wait_for_asset` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling CreativeApi$CreateCreative, `description` is not nullable")
      }

      if (!missing(`asset_image_id`) && is.null(`asset_image_id`)) {
        stop("Invalid value for `asset_image_id` when calling CreativeApi$CreateCreative, `asset_image_id` is not nullable")
      }

      if (!missing(`action`) && is.null(`action`)) {
        stop("Invalid value for `action` when calling CreativeApi$CreateCreative, `action` is not nullable")
      }

      if (!missing(`data`) && is.null(`data`)) {
        stop("Invalid value for `data` when calling CreativeApi$CreateCreative, `data` is not nullable")
      }

      if (!missing(`suffix`) && is.null(`suffix`)) {
        stop("Invalid value for `suffix` when calling CreativeApi$CreateCreative, `suffix` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling CreativeApi$CreateCreative, `type` is not nullable")
      }

      if (!missing(`balance`) && is.null(`balance`)) {
        stop("Invalid value for `balance` when calling CreativeApi$CreateCreative, `balance` is not nullable")
      }

      if (!missing(`reference_id`) && is.null(`reference_id`)) {
        stop("Invalid value for `reference_id` when calling CreativeApi$CreateCreative, `reference_id` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling CreativeApi$CreateCreative, `app_version` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling CreativeApi$CreateCreative, `mission_id` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling CreativeApi$CreateCreative, `offer_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["assetImageId"]] <- `asset_image_id`

      query_params[["action"]] <- `action`

      query_params[["data"]] <- `data`

      query_params[["suffix"]] <- `suffix`

      query_params[["type"]] <- `type`

      query_params[["balance"]] <- `balance`

      query_params[["active"]] <- `active`

      query_params[["referenceId"]] <- `reference_id`

      query_params[["appVersion"]] <- `app_version`

      query_params[["missionId"]] <- `mission_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["waitForAsset"]] <- `wait_for_asset`

      local_var_url_path <- "/creative/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CreativeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Creative
    #'
    #' @param account_id the id of the logged in user
    #' @param creative_id the id of the creative to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteCreative = function(account_id, creative_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteCreativeWithHttpInfo(account_id, creative_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Creative
    #'
    #' @param account_id the id of the logged in user
    #' @param creative_id the id of the creative to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteCreativeWithHttpInfo = function(account_id, creative_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`creative_id`)) {
        stop("Missing required parameter `creative_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CreativeApi$DeleteCreative, `account_id` is not nullable")
      }

      if (!missing(`creative_id`) && is.null(`creative_id`)) {
        stop("Invalid value for `creative_id` when calling CreativeApi$DeleteCreative, `creative_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["creativeId"]] <- `creative_id`

      local_var_url_path <- "/creative/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Creative
    #'
    #' @param account_id the id of the logged in user
    #' @param creative_id the ID of the creative to get
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CreativeResponse
    GetCreative = function(account_id, creative_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetCreativeWithHttpInfo(account_id, creative_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Creative
    #'
    #' @param account_id the id of the logged in user
    #' @param creative_id the ID of the creative to get
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CreativeResponse) with additional information such as HTTP status code, headers
    GetCreativeWithHttpInfo = function(account_id, creative_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`creative_id`)) {
        stop("Missing required parameter `creative_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CreativeApi$GetCreative, `account_id` is not nullable")
      }

      if (!missing(`creative_id`) && is.null(`creative_id`)) {
        stop("Invalid value for `creative_id` when calling CreativeApi$GetCreative, `creative_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["creativeId"]] <- `creative_id`

      local_var_url_path <- "/creative/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CreativeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Creatives
    #'
    #' @param account_id The logged in user.
    #' @param app_key the application key
    #' @param start Start the result set at some index.
    #' @param limit Limit the result to some number.
    #' @param mission_id (optional) Creatives contained in the provided mission.
    #' @param keyword (optional) Match the keyword to the owner name or level name.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[CreativeResponse]
    GetCreativesByApplication = function(account_id, app_key, start, limit, mission_id = NULL, keyword = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetCreativesByApplicationWithHttpInfo(account_id, app_key, start, limit, mission_id, keyword, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Creatives
    #'
    #' @param account_id The logged in user.
    #' @param app_key the application key
    #' @param start Start the result set at some index.
    #' @param limit Limit the result to some number.
    #' @param mission_id (optional) Creatives contained in the provided mission.
    #' @param keyword (optional) Match the keyword to the owner name or level name.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[CreativeResponse]) with additional information such as HTTP status code, headers
    GetCreativesByApplicationWithHttpInfo = function(account_id, app_key, start, limit, mission_id = NULL, keyword = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CreativeApi$GetCreativesByApplication, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling CreativeApi$GetCreativesByApplication, `app_key` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling CreativeApi$GetCreativesByApplication, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling CreativeApi$GetCreativesByApplication, `limit` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling CreativeApi$GetCreativesByApplication, `mission_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling CreativeApi$GetCreativesByApplication, `keyword` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["missionId"]] <- `mission_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/creative/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[CreativeResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Remove Preview
    #'
    #' @param account_id the ID of the logged in user
    #' @param creative_id the ID of the creative to remove preview
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    RemovePreview = function(account_id, creative_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RemovePreviewWithHttpInfo(account_id, creative_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Remove Preview
    #'
    #' @param account_id the ID of the logged in user
    #' @param creative_id the ID of the creative to remove preview
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    RemovePreviewWithHttpInfo = function(account_id, creative_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`creative_id`)) {
        stop("Missing required parameter `creative_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CreativeApi$RemovePreview, `account_id` is not nullable")
      }

      if (!missing(`creative_id`) && is.null(`creative_id`)) {
        stop("Invalid value for `creative_id` when calling CreativeApi$RemovePreview, `creative_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["creativeId"]] <- `creative_id`

      local_var_url_path <- "/creative/removepreview"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Creative
    #'
    #' @param account_id The logged in user.
    #' @param creative_id the creative Id to upate.
    #' @param name (optional) The name of the level.
    #' @param description (optional) The description of the level.
    #' @param asset_image_id (optional) The asset Id of the level image.
    #' @param action (optional) This parameter is deprecated. deprecated use data field
    #' @param data (optional) The creative data, json based format depending on type
    #' @param suffix (optional) This parameter is deprecated. deprecated use type field
    #' @param type (optional) The type of creative.
    #' @param balance (optional) Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited.
    #' @param active (optional) If true set the game level as active. Default is false.
    #' @param reference_id (optional) if creative related so some other content provided the id
    #' @param app_version (optional) The version of the application, will not return creatives newer than the appVersion. Only used when requesting application configuration creatives.
    #' @param mission_id (optional) Assign the creative to a campaign for timing and audience matching.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CreativeResponse
    UpdateCreative = function(account_id, creative_id, name = NULL, description = NULL, asset_image_id = NULL, action = NULL, data = NULL, suffix = NULL, type = NULL, balance = NULL, active = NULL, reference_id = NULL, app_version = NULL, mission_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateCreativeWithHttpInfo(account_id, creative_id, name, description, asset_image_id, action, data, suffix, type, balance, active, reference_id, app_version, mission_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Creative
    #'
    #' @param account_id The logged in user.
    #' @param creative_id the creative Id to upate.
    #' @param name (optional) The name of the level.
    #' @param description (optional) The description of the level.
    #' @param asset_image_id (optional) The asset Id of the level image.
    #' @param action (optional) This parameter is deprecated. deprecated use data field
    #' @param data (optional) The creative data, json based format depending on type
    #' @param suffix (optional) This parameter is deprecated. deprecated use type field
    #' @param type (optional) The type of creative.
    #' @param balance (optional) Set the amount of money available to spend, once 0 deactivate level. Set to a negative value for unlimited.
    #' @param active (optional) If true set the game level as active. Default is false.
    #' @param reference_id (optional) if creative related so some other content provided the id
    #' @param app_version (optional) The version of the application, will not return creatives newer than the appVersion. Only used when requesting application configuration creatives.
    #' @param mission_id (optional) Assign the creative to a campaign for timing and audience matching.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CreativeResponse) with additional information such as HTTP status code, headers
    UpdateCreativeWithHttpInfo = function(account_id, creative_id, name = NULL, description = NULL, asset_image_id = NULL, action = NULL, data = NULL, suffix = NULL, type = NULL, balance = NULL, active = NULL, reference_id = NULL, app_version = NULL, mission_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`creative_id`)) {
        stop("Missing required parameter `creative_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling CreativeApi$UpdateCreative, `account_id` is not nullable")
      }

      if (!missing(`creative_id`) && is.null(`creative_id`)) {
        stop("Invalid value for `creative_id` when calling CreativeApi$UpdateCreative, `creative_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling CreativeApi$UpdateCreative, `name` is not nullable")
      }

      if (!missing(`description`) && is.null(`description`)) {
        stop("Invalid value for `description` when calling CreativeApi$UpdateCreative, `description` is not nullable")
      }

      if (!missing(`asset_image_id`) && is.null(`asset_image_id`)) {
        stop("Invalid value for `asset_image_id` when calling CreativeApi$UpdateCreative, `asset_image_id` is not nullable")
      }

      if (!missing(`action`) && is.null(`action`)) {
        stop("Invalid value for `action` when calling CreativeApi$UpdateCreative, `action` is not nullable")
      }

      if (!missing(`data`) && is.null(`data`)) {
        stop("Invalid value for `data` when calling CreativeApi$UpdateCreative, `data` is not nullable")
      }

      if (!missing(`suffix`) && is.null(`suffix`)) {
        stop("Invalid value for `suffix` when calling CreativeApi$UpdateCreative, `suffix` is not nullable")
      }

      if (!missing(`type`) && is.null(`type`)) {
        stop("Invalid value for `type` when calling CreativeApi$UpdateCreative, `type` is not nullable")
      }

      if (!missing(`balance`) && is.null(`balance`)) {
        stop("Invalid value for `balance` when calling CreativeApi$UpdateCreative, `balance` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling CreativeApi$UpdateCreative, `active` is not nullable")
      }

      if (!missing(`reference_id`) && is.null(`reference_id`)) {
        stop("Invalid value for `reference_id` when calling CreativeApi$UpdateCreative, `reference_id` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling CreativeApi$UpdateCreative, `app_version` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling CreativeApi$UpdateCreative, `mission_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["creativeId"]] <- `creative_id`

      query_params[["name"]] <- `name`

      query_params[["description"]] <- `description`

      query_params[["assetImageId"]] <- `asset_image_id`

      query_params[["action"]] <- `action`

      query_params[["data"]] <- `data`

      query_params[["suffix"]] <- `suffix`

      query_params[["type"]] <- `type`

      query_params[["balance"]] <- `balance`

      query_params[["active"]] <- `active`

      query_params[["referenceId"]] <- `reference_id`

      query_params[["appVersion"]] <- `app_version`

      query_params[["missionId"]] <- `mission_id`

      local_var_url_path <- "/creative/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CreativeResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
