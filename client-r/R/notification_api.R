#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Notification operations
#' @description NotificationApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateNotificationTemplate  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account ID of the user.
#' var_conduit <- "conduit_example" # character | Filter results by notification type: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
#' var_title <- "title_example" # character | title of the notification template
#' var_body <- "body_example" # character | body of the notification template
#' var_app_key <- "app_key_example" # character | Filter results by application. (Optional)
#' var_event <- "event_example" # character | Filter results by event. (Optional)
#' var_tags <- "tags_example" # character | tags associated with the note template (Optional)
#'
#' #Create Notification Template
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateNotificationTemplate(var_account_id, var_conduit, var_title, var_body, app_key = var_app_key, event = var_event, tags = var_tagsdata_file = "result.txt")
#' result <- api_instance$CreateNotificationTemplate(var_account_id, var_conduit, var_title, var_body, app_key = var_app_key, event = var_event, tags = var_tags)
#' dput(result)
#'
#'
#' ####################  CreateOrUpdateBlockedNotifications  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key
#' var_data <- "data_example" # character | batch data payload (application specific)
#' var_account_id <- 56 # integer | the account id of the user (Optional)
#'
#' #Create or update blocked notification settings
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateOrUpdateBlockedNotifications(var_app_key, var_data, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$CreateOrUpdateBlockedNotifications(var_app_key, var_data, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  DeleteNotificationTemplate  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account id of the user
#' var_notification_template_id <- 56 # integer | the id of the notification template to delete
#'
#' #Delete Notification Template
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteNotificationTemplate(var_account_id, var_notification_template_iddata_file = "result.txt")
#' result <- api_instance$DeleteNotificationTemplate(var_account_id, var_notification_template_id)
#' dput(result)
#'
#'
#' ####################  GetNotificationTemplate  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the account
#' var_notification_template_id <- 56 # integer | the id of the notification template to get
#'
#' #Get Notification Template
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetNotificationTemplate(var_account_id, var_notification_template_iddata_file = "result.txt")
#' result <- api_instance$GetNotificationTemplate(var_account_id, var_notification_template_id)
#' dput(result)
#'
#'
#' ####################  GetNotifications  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_connection_account_id <- 56 # integer | the account id used to view another person's notifications (Optional)
#' var_app_key <- "app_key_example" # character | the application key to filter messages by application (Optional)
#' var_event_type <- "event_type_example" # character | comma separated list of EVENTS. Filters search results to only include these events. Don't include this parameter or pass in an empty string to return all event types. (Optional)
#' var_content_ids <- "content_ids_example" # character | comma separated list of content ids to search notifications on (Optional)
#' var_content_types <- "content_types_example" # character | comma separated list of content types to search notifications on (Optional)
#' var_parent_ids <- "parent_ids_example" # character | comma separated list of parent ids to search notifications on (Optional)
#' var_parent_types <- "parent_types_example" # character | comma separated list of parent types to search notifications on (Optional)
#' var_action_category <- "action_category_example" # character | Action category used to filter notifications (Optional)
#' var_conduits <- "conduits_example" # character | comma separated list of conduits to search notifications on (Optional)
#' var_keyword <- "keyword_example" # character | search notifications via keyword (Optional)
#' var_return_read_messages <- "return_read_messages_example" # character | if set to true, will return notifications that have been marked as read (Optional)
#' var_mark_as_read <- "mark_as_read_example" # character | if set to true, the returned notifications will be marked as \\\"read\\\" after the response has been sent (Optional)
#' var_from_date <- 56 # integer | filter notifications from this date (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#' var_return_sent <- "return_sent_example" # character | whether to include notifications sent by the requester in the response (Optional)
#' var_ignore_flagged <- "ignore_flagged_example" # character | whether to ignore flagged notifications (Optional)
#' var_start <- 56 # integer | start of the pagination (Optional)
#' var_limit <- 56 # integer | limit of the pagination (Optional)
#'
#' #Get Notifications
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetNotifications(device_id = var_device_id, account_id = var_account_id, connection_account_id = var_connection_account_id, app_key = var_app_key, event_type = var_event_type, content_ids = var_content_ids, content_types = var_content_types, parent_ids = var_parent_ids, parent_types = var_parent_types, action_category = var_action_category, conduits = var_conduits, keyword = var_keyword, return_read_messages = var_return_read_messages, mark_as_read = var_mark_as_read, from_date = var_from_date, latitude = var_latitude, longitude = var_longitude, return_sent = var_return_sent, ignore_flagged = var_ignore_flagged, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GetNotifications(device_id = var_device_id, account_id = var_account_id, connection_account_id = var_connection_account_id, app_key = var_app_key, event_type = var_event_type, content_ids = var_content_ids, content_types = var_content_types, parent_ids = var_parent_ids, parent_types = var_parent_types, action_category = var_action_category, conduits = var_conduits, keyword = var_keyword, return_read_messages = var_return_read_messages, mark_as_read = var_mark_as_read, from_date = var_from_date, latitude = var_latitude, longitude = var_longitude, return_sent = var_return_sent, ignore_flagged = var_ignore_flagged, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  RegisterNotificationToken  ####################
#'
#' library(openapi)
#' var_token <- "token_example" # character | A token that is generated by the device to sign requests for the notification service providers
#' var_push_type <- "push_type_example" # character | The type of push notification. Possible values include: APNS, GCM
#' var_device_id <- "device_id_example" # character | The unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_environment <- "environment_example" # character | Determines if the token is a DEVELOPMENT or PRODUCTION token (Optional)
#' var_app_key <- "app_key_example" # character | The application key (Optional)
#' var_game_type <- "game_type_example" # character | This parameter is deprecated (use appKey instead) (Optional)
#' var_active <- "active_example" # character | Sets whether the token is active or not (non-active tokens are not used) (Optional)
#' var_latitude <- 3.4 # numeric | Latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | Longitude used to update the user's current location (Optional)
#'
#' #Register Notification Token
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RegisterNotificationToken(var_token, var_push_type, device_id = var_device_id, account_id = var_account_id, environment = var_environment, app_key = var_app_key, game_type = var_game_type, active = var_active, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$RegisterNotificationToken(var_token, var_push_type, device_id = var_device_id, account_id = var_account_id, environment = var_environment, app_key = var_app_key, game_type = var_game_type, active = var_active, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  SearchBlockedNotifications  ####################
#'
#' library(openapi)
#' var_app_key <- "app_key_example" # character | The application key
#' var_account_id <- 56 # integer | the account id of the user (Optional)
#' var_search_tags <- "search_tags_example" # character | search tags to filter results (Optional)
#' var_events <- "events_example" # character | events to filter by (comma separated) (Optional)
#' var_conduits <- "conduits_example" # character | conduits to filter by (comma separated) (Optional)
#' var_custom_types <- "custom_types_example" # character | custom types to filter by (comma separated) (Optional)
#' var_content_types <- "content_types_example" # character | content types to filter by (comma separated) (Optional)
#' var_content_ids <- "content_ids_example" # character | content ids to filter by (comma separated) (Optional)
#' var_sort_field <- "sort_field_example" # character | sort field for results (Optional)
#' var_descending <- "descending_example" # character | whether to sort descending (Optional)
#' var_start <- 56 # integer | start of the pagination (Optional)
#' var_limit <- 56 # integer | limit of the pagination (Optional)
#'
#' #Search on the user's blocked notification settings
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchBlockedNotifications(var_app_key, account_id = var_account_id, search_tags = var_search_tags, events = var_events, conduits = var_conduits, custom_types = var_custom_types, content_types = var_content_types, content_ids = var_content_ids, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchBlockedNotifications(var_app_key, account_id = var_account_id, search_tags = var_search_tags, events = var_events, conduits = var_conduits, custom_types = var_custom_types, content_types = var_content_types, content_ids = var_content_ids, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  SearchNotificationTemplate  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account ID of the user.
#' var_sort_field <- "sort_field_example" # character | Specifies how results are ordered.ID - order results by the notificationTemplateId CREATED - order results by the created date UPDATED - order results by the updated date TITLE - order results by title EVENT - order results by event CONDUIT - order results by conduit APP_NAME - order results by the application name ('global' templates will not have an application and will be returned last if 'descending' is set to false.
#' var_descending <- "descending_example" # character | Specified whether the results are returned in descending or ascending order.
#' var_start <- 56 # integer | The start of the pagination.
#' var_limit <- 56 # integer | The limit of the pagination.
#' var_app_key <- "app_key_example" # character | Filter results by application. (Optional)
#' var_event <- "event_example" # character | Filter results by event. (Optional)
#' var_conduit <- "conduit_example" # character | Filter results by notification type: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION. (Optional)
#' var_global_only <- "global_only_example" # character | Returns only templates that have been reserved for system use on all applications (only for admin accounts). (Optional)
#' var_reserved_only <- "reserved_only_example" # character | Returns only templates that use reserved events. (Optional)
#' var_keyword <- "keyword_example" # character | Filter results by keyword on the title, tags. (Optional)
#'
#' #Search Notification Templates
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchNotificationTemplate(var_account_id, var_sort_field, var_descending, var_start, var_limit, app_key = var_app_key, event = var_event, conduit = var_conduit, global_only = var_global_only, reserved_only = var_reserved_only, keyword = var_keyworddata_file = "result.txt")
#' result <- api_instance$SearchNotificationTemplate(var_account_id, var_sort_field, var_descending, var_start, var_limit, app_key = var_app_key, event = var_event, conduit = var_conduit, global_only = var_global_only, reserved_only = var_reserved_only, keyword = var_keyword)
#' dput(result)
#'
#'
#' ####################  SearchRecipients  ####################
#'
#' library(openapi)
#' var_sort_field <- "sort_field_example" # character | The field to sort by. Possible values include: {ACCOUNT_DISPLAY, CREATED, UPDATED, ACTIVE, DELETED, LAST_LOGGED_IN, CONTACT_EMAIL, RETAILER_LOCATION_NAME, RETAILER_NAME, APPLICATION_NAME}
#' var_device_id <- "device_id_example" # character | the unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_app_key <- "app_key_example" # character | filters results by application. If this is empty, will return all recipients for all applications that the user has access to. (Optional)
#' var_conduit <- "conduit_example" # character | the type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION. (Optional)
#' var_keyword <- "keyword_example" # character | search by keyword on user's display name and email (Optional)
#' var_audience_id <- 56 # integer | This parameter is deprecated. filter results by audience (Optional)
#' var_audience_ids <- "audience_ids_example" # character | filter results by audiences (comma separated list of audience ids) (Optional)
#' var_connection_group_ids <- "connection_group_ids_example" # character | filter results by connection groups (comma separated list of connection group ids) (Optional)
#' var_recipient_account_ids <- "recipient_account_ids_example" # character | filter results by accounts (comma separated list of account ids) (Optional)
#' var_descending <- "descending_example" # character | Determines whether the sorted list is in descending or ascending order (Optional)
#' var_start <- 56 # integer | start of the pagination (Optional)
#' var_limit <- 56 # integer | limit of the pagination (hard limit of 1000) (Optional)
#'
#' #Search for Recipients
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchRecipients(var_sort_field, device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, conduit = var_conduit, keyword = var_keyword, audience_id = var_audience_id, audience_ids = var_audience_ids, connection_group_ids = var_connection_group_ids, recipient_account_ids = var_recipient_account_ids, descending = var_descending, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchRecipients(var_sort_field, device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, conduit = var_conduit, keyword = var_keyword, audience_id = var_audience_id, audience_ids = var_audience_ids, connection_group_ids = var_connection_group_ids, recipient_account_ids = var_recipient_account_ids, descending = var_descending, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  SearchRecipientsCount  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_app_key <- "app_key_example" # character | filters results by application. If this is empty, will return all recipients for all applications that the user has access to. (Optional)
#' var_conduit <- "conduit_example" # character | the type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION. (Optional)
#' var_keyword <- "keyword_example" # character | search by keyword on user's display name and email (Optional)
#' var_audience_id <- 56 # integer | This parameter is deprecated. filter results by audience (Optional)
#' var_audience_ids <- "audience_ids_example" # character | filter results by audiences (comma separated list of audience ids) (Optional)
#' var_connection_group_ids <- "connection_group_ids_example" # character | filter results by connection groups (comma separated list of connection group ids) (Optional)
#' var_sort_field <- "sort_field_example" # character | The field to sort by (see API docs for allowed values). (Optional)
#' var_descending <- "descending_example" # character | Determines whether the sorted list is in descending or ascending order (Optional)
#' var_start <- 56 # integer | start of the pagination (Optional)
#' var_limit <- 56 # integer | limit of the pagination (Optional)
#'
#' #Search for Recipients (Counts/Grouped)
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchRecipientsCount(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, conduit = var_conduit, keyword = var_keyword, audience_id = var_audience_id, audience_ids = var_audience_ids, connection_group_ids = var_connection_group_ids, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchRecipientsCount(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, conduit = var_conduit, keyword = var_keyword, audience_id = var_audience_id, audience_ids = var_audience_ids, connection_group_ids = var_connection_group_ids, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  SendBatchNotifications  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account id of the application owner/manager
#' var_app_key <- "app_key_example" # character | The application key for updating an existing application
#' var_custom_message <- "custom_message_example" # character | Message string that will be displayed in on the notification
#' var_conduit <- "conduit_example" # character | The type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION. (Optional)
#' var_content_id <- 56 # integer | Default notification pay-load field (usage is dependent on the app and the type of event) (Optional)
#' var_content_name <- "content_name_example" # character | Default notification pay-load field (usage is dependent on the app and the type of event) (Optional)
#' var_content_type <- "content_type_example" # character | Default notification pay-load field (usage is dependent on the app and the type of event) (Optional)
#' var_parent_id <- 56 # integer | Default notification pay-load field (usage is dependent on the app and the type of event) (Optional)
#' var_parent_type <- "parent_type_example" # character | Default notification pay-load field (usage is dependent on the app and the type of event) (Optional)
#'
#' #Send Batch Notifications
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SendBatchNotifications(var_account_id, var_app_key, var_custom_message, conduit = var_conduit, content_id = var_content_id, content_name = var_content_name, content_type = var_content_type, parent_id = var_parent_id, parent_type = var_parent_typedata_file = "result.txt")
#' result <- api_instance$SendBatchNotifications(var_account_id, var_app_key, var_custom_message, conduit = var_conduit, content_id = var_content_id, content_name = var_content_name, content_type = var_content_type, parent_id = var_parent_id, parent_type = var_parent_type)
#' dput(result)
#'
#'
#' ####################  SendCustomNotifications  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the unique id of the device making the request (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_receiver_account_ids <- "receiver_account_ids_example" # character | comma separated list of account IDs that will receive the notification (Optional)
#' var_include_friend_group <- "include_friend_group_example" # character | determines whether to send to all of the user's friends, this flag must be true or receiverAccountIds must not be empty (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_game_type <- "game_type_example" # character | This parameter is deprecated. (Optional)
#' var_conduit <- "conduit_example" # character | the type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION. (Optional)
#' var_content_id <- 56 # integer | default notification pay-load field (usage is dependent on the app and the type of event) (Optional)
#' var_content_name <- "content_name_example" # character | default notification pay-load field (usage is dependent on the app and the type of event) (Optional)
#' var_content_type <- "content_type_example" # character | default notification pay-load field (usage is dependent on the app and the type of event) (Optional)
#' var_parent_id <- 56 # integer | default notification pay-load field (usage is dependent on the app and the type of event) (Optional)
#' var_parent_type <- "parent_type_example" # character | default notification pay-load field (usage is dependent on the app and the type of event) (Optional)
#' var_action_category <- "action_category_example" # character |  (Optional)
#' var_subject <- "subject_example" # character | the subject line of an email #@param customPayload custom json definition of notification pay-load (usage is dependent on the app and the type of event) (Optional)
#' var_custom_message <- "custom_message_example" # character | message string that will be displayed in on the notification (Optional)
#' var_friend_only_apns <- "friend_only_apns_example" # character | only sends APNS to people who are friends of the user (still saves the notification message for feed polling) (Optional)
#' var_latitude <- 3.4 # numeric | latitude used to update the user's current location (Optional)
#' var_longitude <- 3.4 # numeric | longitude used to update the user's current location (Optional)
#'
#' #Send Custom Notifications
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SendCustomNotifications(device_id = var_device_id, account_id = var_account_id, receiver_account_ids = var_receiver_account_ids, include_friend_group = var_include_friend_group, app_key = var_app_key, game_type = var_game_type, conduit = var_conduit, content_id = var_content_id, content_name = var_content_name, content_type = var_content_type, parent_id = var_parent_id, parent_type = var_parent_type, action_category = var_action_category, subject = var_subject, custom_message = var_custom_message, friend_only_apns = var_friend_only_apns, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$SendCustomNotifications(device_id = var_device_id, account_id = var_account_id, receiver_account_ids = var_receiver_account_ids, include_friend_group = var_include_friend_group, app_key = var_app_key, game_type = var_game_type, conduit = var_conduit, content_id = var_content_id, content_name = var_content_name, content_type = var_content_type, parent_id = var_parent_id, parent_type = var_parent_type, action_category = var_action_category, subject = var_subject, custom_message = var_custom_message, friend_only_apns = var_friend_only_apns, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  UpdateNotificationTemplate  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The account ID of the user.
#' var_notification_template_id <- 56 # integer | The notification template ID to update.
#' var_title <- "title_example" # character | The title of the message (this would become the subject title for emails). There is a 191 character limit. (Optional)
#' var_body <- "body_example" # character | The body of the message. (Optional)
#' var_tags <- "tags_example" # character | The search tags on the template used during search queries. (Optional)
#'
#' #Update Notification Template
#' api_instance <- NotificationApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateNotificationTemplate(var_account_id, var_notification_template_id, title = var_title, body = var_body, tags = var_tagsdata_file = "result.txt")
#' result <- api_instance$UpdateNotificationTemplate(var_account_id, var_notification_template_id, title = var_title, body = var_body, tags = var_tags)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
NotificationApi <- R6::R6Class(
  "NotificationApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new NotificationApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Notification Template
    #'
    #' @param account_id The account ID of the user.
    #' @param conduit Filter results by notification type: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param title title of the notification template
    #' @param body body of the notification template
    #' @param app_key (optional) Filter results by application.
    #' @param event (optional) Filter results by event.
    #' @param tags (optional) tags associated with the note template
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return NotificationTemplateResponse
    CreateNotificationTemplate = function(account_id, conduit, title, body, app_key = NULL, event = NULL, tags = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateNotificationTemplateWithHttpInfo(account_id, conduit, title, body, app_key, event, tags, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Notification Template
    #'
    #' @param account_id The account ID of the user.
    #' @param conduit Filter results by notification type: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param title title of the notification template
    #' @param body body of the notification template
    #' @param app_key (optional) Filter results by application.
    #' @param event (optional) Filter results by event.
    #' @param tags (optional) tags associated with the note template
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (NotificationTemplateResponse) with additional information such as HTTP status code, headers
    CreateNotificationTemplateWithHttpInfo = function(account_id, conduit, title, body, app_key = NULL, event = NULL, tags = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`conduit`)) {
        stop("Missing required parameter `conduit`.")
      }

      if (missing(`title`)) {
        stop("Missing required parameter `title`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$CreateNotificationTemplate, `account_id` is not nullable")
      }

      if (!missing(`conduit`) && is.null(`conduit`)) {
        stop("Invalid value for `conduit` when calling NotificationApi$CreateNotificationTemplate, `conduit` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling NotificationApi$CreateNotificationTemplate, `title` is not nullable")
      }

      if (!missing(`body`) && is.null(`body`)) {
        stop("Invalid value for `body` when calling NotificationApi$CreateNotificationTemplate, `body` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NotificationApi$CreateNotificationTemplate, `app_key` is not nullable")
      }

      if (!missing(`event`) && is.null(`event`)) {
        stop("Invalid value for `event` when calling NotificationApi$CreateNotificationTemplate, `event` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling NotificationApi$CreateNotificationTemplate, `tags` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["event"]] <- `event`

      query_params[["conduit"]] <- `conduit`

      query_params[["title"]] <- `title`

      query_params[["body"]] <- `body`

      query_params[["tags"]] <- `tags`

      local_var_url_path <- "/notification/template/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "NotificationTemplateResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create or update blocked notification settings
    #'
    #' @param app_key The application key
    #' @param data batch data payload (application specific)
    #' @param account_id (optional) the account id of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BlockedNotificationResponse
    CreateOrUpdateBlockedNotifications = function(app_key, data, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateOrUpdateBlockedNotificationsWithHttpInfo(app_key, data, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create or update blocked notification settings
    #'
    #' @param app_key The application key
    #' @param data batch data payload (application specific)
    #' @param account_id (optional) the account id of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BlockedNotificationResponse) with additional information such as HTTP status code, headers
    CreateOrUpdateBlockedNotificationsWithHttpInfo = function(app_key, data, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`data`)) {
        stop("Missing required parameter `data`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NotificationApi$CreateOrUpdateBlockedNotifications, `app_key` is not nullable")
      }

      if (!missing(`data`) && is.null(`data`)) {
        stop("Invalid value for `data` when calling NotificationApi$CreateOrUpdateBlockedNotifications, `data` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$CreateOrUpdateBlockedNotifications, `account_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["data"]] <- `data`

      local_var_url_path <- "/notification/blocked/batch"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BlockedNotificationResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Notification Template
    #'
    #' @param account_id the account id of the user
    #' @param notification_template_id the id of the notification template to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return NotificationTemplateResponse
    DeleteNotificationTemplate = function(account_id, notification_template_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteNotificationTemplateWithHttpInfo(account_id, notification_template_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Notification Template
    #'
    #' @param account_id the account id of the user
    #' @param notification_template_id the id of the notification template to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (NotificationTemplateResponse) with additional information such as HTTP status code, headers
    DeleteNotificationTemplateWithHttpInfo = function(account_id, notification_template_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`notification_template_id`)) {
        stop("Missing required parameter `notification_template_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$DeleteNotificationTemplate, `account_id` is not nullable")
      }

      if (!missing(`notification_template_id`) && is.null(`notification_template_id`)) {
        stop("Invalid value for `notification_template_id` when calling NotificationApi$DeleteNotificationTemplate, `notification_template_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["notificationTemplateId"]] <- `notification_template_id`

      local_var_url_path <- "/notification/template/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "NotificationTemplateResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Notification Template
    #'
    #' @param account_id the id of the account
    #' @param notification_template_id the id of the notification template to get
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return NotificationTemplateResponse
    GetNotificationTemplate = function(account_id, notification_template_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetNotificationTemplateWithHttpInfo(account_id, notification_template_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Notification Template
    #'
    #' @param account_id the id of the account
    #' @param notification_template_id the id of the notification template to get
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (NotificationTemplateResponse) with additional information such as HTTP status code, headers
    GetNotificationTemplateWithHttpInfo = function(account_id, notification_template_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`notification_template_id`)) {
        stop("Missing required parameter `notification_template_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$GetNotificationTemplate, `account_id` is not nullable")
      }

      if (!missing(`notification_template_id`) && is.null(`notification_template_id`)) {
        stop("Invalid value for `notification_template_id` when calling NotificationApi$GetNotificationTemplate, `notification_template_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["notificationTemplateId"]] <- `notification_template_id`

      local_var_url_path <- "/notification/template/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "NotificationTemplateResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Notifications
    #'
    #' @param device_id (optional) the unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param connection_account_id (optional) the account id used to view another person's notifications
    #' @param app_key (optional) the application key to filter messages by application
    #' @param event_type (optional) comma separated list of EVENTS. Filters search results to only include these events. Don't include this parameter or pass in an empty string to return all event types.
    #' @param content_ids (optional) comma separated list of content ids to search notifications on
    #' @param content_types (optional) comma separated list of content types to search notifications on
    #' @param parent_ids (optional) comma separated list of parent ids to search notifications on
    #' @param parent_types (optional) comma separated list of parent types to search notifications on
    #' @param action_category (optional) Action category used to filter notifications
    #' @param conduits (optional) comma separated list of conduits to search notifications on
    #' @param keyword (optional) search notifications via keyword
    #' @param return_read_messages (optional) if set to true, will return notifications that have been marked as read
    #' @param mark_as_read (optional) if set to true, the returned notifications will be marked as \\\"read\\\" after the response has been sent
    #' @param from_date (optional) filter notifications from this date
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param return_sent (optional) whether to include notifications sent by the requester in the response
    #' @param ignore_flagged (optional) whether to ignore flagged notifications
    #' @param start (optional) start of the pagination
    #' @param limit (optional) limit of the pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return NotificationMessageListResponse
    GetNotifications = function(device_id = NULL, account_id = NULL, connection_account_id = NULL, app_key = NULL, event_type = NULL, content_ids = NULL, content_types = NULL, parent_ids = NULL, parent_types = NULL, action_category = NULL, conduits = NULL, keyword = NULL, return_read_messages = NULL, mark_as_read = NULL, from_date = NULL, latitude = NULL, longitude = NULL, return_sent = NULL, ignore_flagged = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetNotificationsWithHttpInfo(device_id, account_id, connection_account_id, app_key, event_type, content_ids, content_types, parent_ids, parent_types, action_category, conduits, keyword, return_read_messages, mark_as_read, from_date, latitude, longitude, return_sent, ignore_flagged, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Notifications
    #'
    #' @param device_id (optional) the unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param connection_account_id (optional) the account id used to view another person's notifications
    #' @param app_key (optional) the application key to filter messages by application
    #' @param event_type (optional) comma separated list of EVENTS. Filters search results to only include these events. Don't include this parameter or pass in an empty string to return all event types.
    #' @param content_ids (optional) comma separated list of content ids to search notifications on
    #' @param content_types (optional) comma separated list of content types to search notifications on
    #' @param parent_ids (optional) comma separated list of parent ids to search notifications on
    #' @param parent_types (optional) comma separated list of parent types to search notifications on
    #' @param action_category (optional) Action category used to filter notifications
    #' @param conduits (optional) comma separated list of conduits to search notifications on
    #' @param keyword (optional) search notifications via keyword
    #' @param return_read_messages (optional) if set to true, will return notifications that have been marked as read
    #' @param mark_as_read (optional) if set to true, the returned notifications will be marked as \\\"read\\\" after the response has been sent
    #' @param from_date (optional) filter notifications from this date
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param return_sent (optional) whether to include notifications sent by the requester in the response
    #' @param ignore_flagged (optional) whether to ignore flagged notifications
    #' @param start (optional) start of the pagination
    #' @param limit (optional) limit of the pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (NotificationMessageListResponse) with additional information such as HTTP status code, headers
    GetNotificationsWithHttpInfo = function(device_id = NULL, account_id = NULL, connection_account_id = NULL, app_key = NULL, event_type = NULL, content_ids = NULL, content_types = NULL, parent_ids = NULL, parent_types = NULL, action_category = NULL, conduits = NULL, keyword = NULL, return_read_messages = NULL, mark_as_read = NULL, from_date = NULL, latitude = NULL, longitude = NULL, return_sent = NULL, ignore_flagged = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NotificationApi$GetNotifications, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$GetNotifications, `account_id` is not nullable")
      }

      if (!missing(`connection_account_id`) && is.null(`connection_account_id`)) {
        stop("Invalid value for `connection_account_id` when calling NotificationApi$GetNotifications, `connection_account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NotificationApi$GetNotifications, `app_key` is not nullable")
      }

      if (!missing(`event_type`) && is.null(`event_type`)) {
        stop("Invalid value for `event_type` when calling NotificationApi$GetNotifications, `event_type` is not nullable")
      }

      if (!missing(`content_ids`) && is.null(`content_ids`)) {
        stop("Invalid value for `content_ids` when calling NotificationApi$GetNotifications, `content_ids` is not nullable")
      }

      if (!missing(`content_types`) && is.null(`content_types`)) {
        stop("Invalid value for `content_types` when calling NotificationApi$GetNotifications, `content_types` is not nullable")
      }

      if (!missing(`parent_ids`) && is.null(`parent_ids`)) {
        stop("Invalid value for `parent_ids` when calling NotificationApi$GetNotifications, `parent_ids` is not nullable")
      }

      if (!missing(`parent_types`) && is.null(`parent_types`)) {
        stop("Invalid value for `parent_types` when calling NotificationApi$GetNotifications, `parent_types` is not nullable")
      }

      if (!missing(`action_category`) && is.null(`action_category`)) {
        stop("Invalid value for `action_category` when calling NotificationApi$GetNotifications, `action_category` is not nullable")
      }

      if (!missing(`conduits`) && is.null(`conduits`)) {
        stop("Invalid value for `conduits` when calling NotificationApi$GetNotifications, `conduits` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling NotificationApi$GetNotifications, `keyword` is not nullable")
      }

      if (!missing(`return_read_messages`) && is.null(`return_read_messages`)) {
        stop("Invalid value for `return_read_messages` when calling NotificationApi$GetNotifications, `return_read_messages` is not nullable")
      }

      if (!missing(`mark_as_read`) && is.null(`mark_as_read`)) {
        stop("Invalid value for `mark_as_read` when calling NotificationApi$GetNotifications, `mark_as_read` is not nullable")
      }

      if (!missing(`from_date`) && is.null(`from_date`)) {
        stop("Invalid value for `from_date` when calling NotificationApi$GetNotifications, `from_date` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling NotificationApi$GetNotifications, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling NotificationApi$GetNotifications, `longitude` is not nullable")
      }

      if (!missing(`return_sent`) && is.null(`return_sent`)) {
        stop("Invalid value for `return_sent` when calling NotificationApi$GetNotifications, `return_sent` is not nullable")
      }

      if (!missing(`ignore_flagged`) && is.null(`ignore_flagged`)) {
        stop("Invalid value for `ignore_flagged` when calling NotificationApi$GetNotifications, `ignore_flagged` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling NotificationApi$GetNotifications, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling NotificationApi$GetNotifications, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["connectionAccountId"]] <- `connection_account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["eventType"]] <- `event_type`

      query_params[["contentIds"]] <- `content_ids`

      query_params[["contentTypes"]] <- `content_types`

      query_params[["parentIds"]] <- `parent_ids`

      query_params[["parentTypes"]] <- `parent_types`

      query_params[["actionCategory"]] <- `action_category`

      query_params[["conduits"]] <- `conduits`

      query_params[["keyword"]] <- `keyword`

      query_params[["returnReadMessages"]] <- `return_read_messages`

      query_params[["markAsRead"]] <- `mark_as_read`

      query_params[["fromDate"]] <- `from_date`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["returnSent"]] <- `return_sent`

      query_params[["ignoreFlagged"]] <- `ignore_flagged`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/notification/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "NotificationMessageListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Register Notification Token
    #'
    #' @param token A token that is generated by the device to sign requests for the notification service providers
    #' @param push_type The type of push notification. Possible values include: APNS, GCM
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param environment (optional) Determines if the token is a DEVELOPMENT or PRODUCTION token
    #' @param app_key (optional) The application key
    #' @param game_type (optional) This parameter is deprecated (use appKey instead)
    #' @param active (optional) Sets whether the token is active or not (non-active tokens are not used)
    #' @param latitude (optional) Latitude used to update the user's current location
    #' @param longitude (optional) Longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    RegisterNotificationToken = function(token, push_type, device_id = NULL, account_id = NULL, environment = NULL, app_key = NULL, game_type = NULL, active = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RegisterNotificationTokenWithHttpInfo(token, push_type, device_id, account_id, environment, app_key, game_type, active, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Register Notification Token
    #'
    #' @param token A token that is generated by the device to sign requests for the notification service providers
    #' @param push_type The type of push notification. Possible values include: APNS, GCM
    #' @param device_id (optional) The unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param environment (optional) Determines if the token is a DEVELOPMENT or PRODUCTION token
    #' @param app_key (optional) The application key
    #' @param game_type (optional) This parameter is deprecated (use appKey instead)
    #' @param active (optional) Sets whether the token is active or not (non-active tokens are not used)
    #' @param latitude (optional) Latitude used to update the user's current location
    #' @param longitude (optional) Longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    RegisterNotificationTokenWithHttpInfo = function(token, push_type, device_id = NULL, account_id = NULL, environment = NULL, app_key = NULL, game_type = NULL, active = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`token`)) {
        stop("Missing required parameter `token`.")
      }

      if (missing(`push_type`)) {
        stop("Missing required parameter `push_type`.")
      }

      if (!missing(`token`) && is.null(`token`)) {
        stop("Invalid value for `token` when calling NotificationApi$RegisterNotificationToken, `token` is not nullable")
      }

      if (!missing(`push_type`) && is.null(`push_type`)) {
        stop("Invalid value for `push_type` when calling NotificationApi$RegisterNotificationToken, `push_type` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NotificationApi$RegisterNotificationToken, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$RegisterNotificationToken, `account_id` is not nullable")
      }

      if (!missing(`environment`) && is.null(`environment`)) {
        stop("Invalid value for `environment` when calling NotificationApi$RegisterNotificationToken, `environment` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NotificationApi$RegisterNotificationToken, `app_key` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling NotificationApi$RegisterNotificationToken, `game_type` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling NotificationApi$RegisterNotificationToken, `active` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling NotificationApi$RegisterNotificationToken, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling NotificationApi$RegisterNotificationToken, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["token"]] <- `token`

      if (!is.null(`push_type`) && !(`push_type` %in% c("APNS", "GCM", "BAIDU", "XGPUSH", "JPUSH"))) {
        stop("Invalid value for push_type when calling NotificationApi$RegisterNotificationToken. Must be [APNS, GCM, BAIDU, XGPUSH, JPUSH].")
      }
      query_params[["pushType"]] <- `push_type`

      query_params[["environment"]] <- `environment`

      query_params[["appKey"]] <- `app_key`

      query_params[["gameType"]] <- `game_type`

      query_params[["active"]] <- `active`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/notification/token"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search on the user's blocked notification settings
    #'
    #' @param app_key The application key
    #' @param account_id (optional) the account id of the user
    #' @param search_tags (optional) search tags to filter results
    #' @param events (optional) events to filter by (comma separated)
    #' @param conduits (optional) conduits to filter by (comma separated)
    #' @param custom_types (optional) custom types to filter by (comma separated)
    #' @param content_types (optional) content types to filter by (comma separated)
    #' @param content_ids (optional) content ids to filter by (comma separated)
    #' @param sort_field (optional) sort field for results
    #' @param descending (optional) whether to sort descending
    #' @param start (optional) start of the pagination
    #' @param limit (optional) limit of the pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BlockedNotificationResponse
    SearchBlockedNotifications = function(app_key, account_id = NULL, search_tags = NULL, events = NULL, conduits = NULL, custom_types = NULL, content_types = NULL, content_ids = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchBlockedNotificationsWithHttpInfo(app_key, account_id, search_tags, events, conduits, custom_types, content_types, content_ids, sort_field, descending, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search on the user's blocked notification settings
    #'
    #' @param app_key The application key
    #' @param account_id (optional) the account id of the user
    #' @param search_tags (optional) search tags to filter results
    #' @param events (optional) events to filter by (comma separated)
    #' @param conduits (optional) conduits to filter by (comma separated)
    #' @param custom_types (optional) custom types to filter by (comma separated)
    #' @param content_types (optional) content types to filter by (comma separated)
    #' @param content_ids (optional) content ids to filter by (comma separated)
    #' @param sort_field (optional) sort field for results
    #' @param descending (optional) whether to sort descending
    #' @param start (optional) start of the pagination
    #' @param limit (optional) limit of the pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BlockedNotificationResponse) with additional information such as HTTP status code, headers
    SearchBlockedNotificationsWithHttpInfo = function(app_key, account_id = NULL, search_tags = NULL, events = NULL, conduits = NULL, custom_types = NULL, content_types = NULL, content_ids = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NotificationApi$SearchBlockedNotifications, `app_key` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$SearchBlockedNotifications, `account_id` is not nullable")
      }

      if (!missing(`search_tags`) && is.null(`search_tags`)) {
        stop("Invalid value for `search_tags` when calling NotificationApi$SearchBlockedNotifications, `search_tags` is not nullable")
      }

      if (!missing(`events`) && is.null(`events`)) {
        stop("Invalid value for `events` when calling NotificationApi$SearchBlockedNotifications, `events` is not nullable")
      }

      if (!missing(`conduits`) && is.null(`conduits`)) {
        stop("Invalid value for `conduits` when calling NotificationApi$SearchBlockedNotifications, `conduits` is not nullable")
      }

      if (!missing(`custom_types`) && is.null(`custom_types`)) {
        stop("Invalid value for `custom_types` when calling NotificationApi$SearchBlockedNotifications, `custom_types` is not nullable")
      }

      if (!missing(`content_types`) && is.null(`content_types`)) {
        stop("Invalid value for `content_types` when calling NotificationApi$SearchBlockedNotifications, `content_types` is not nullable")
      }

      if (!missing(`content_ids`) && is.null(`content_ids`)) {
        stop("Invalid value for `content_ids` when calling NotificationApi$SearchBlockedNotifications, `content_ids` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling NotificationApi$SearchBlockedNotifications, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling NotificationApi$SearchBlockedNotifications, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling NotificationApi$SearchBlockedNotifications, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling NotificationApi$SearchBlockedNotifications, `limit` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["searchTags"]] <- `search_tags`

      query_params[["events"]] <- `events`

      query_params[["conduits"]] <- `conduits`

      query_params[["customTypes"]] <- `custom_types`

      query_params[["contentTypes"]] <- `content_types`

      query_params[["contentIds"]] <- `content_ids`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/notification/blocked/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BlockedNotificationResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Notification Templates
    #'
    #' @param account_id The account ID of the user.
    #' @param sort_field Specifies how results are ordered.ID - order results by the notificationTemplateId CREATED - order results by the created date UPDATED - order results by the updated date TITLE - order results by title EVENT - order results by event CONDUIT - order results by conduit APP_NAME - order results by the application name ('global' templates will not have an application and will be returned last if 'descending' is set to false.
    #' @param descending Specified whether the results are returned in descending or ascending order.
    #' @param start The start of the pagination.
    #' @param limit The limit of the pagination.
    #' @param app_key (optional) Filter results by application.
    #' @param event (optional) Filter results by event.
    #' @param conduit (optional) Filter results by notification type: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param global_only (optional) Returns only templates that have been reserved for system use on all applications (only for admin accounts).
    #' @param reserved_only (optional) Returns only templates that use reserved events.
    #' @param keyword (optional) Filter results by keyword on the title, tags.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return NotificationTemplateResponse
    SearchNotificationTemplate = function(account_id, sort_field, descending, start, limit, app_key = NULL, event = NULL, conduit = NULL, global_only = NULL, reserved_only = NULL, keyword = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchNotificationTemplateWithHttpInfo(account_id, sort_field, descending, start, limit, app_key, event, conduit, global_only, reserved_only, keyword, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Notification Templates
    #'
    #' @param account_id The account ID of the user.
    #' @param sort_field Specifies how results are ordered.ID - order results by the notificationTemplateId CREATED - order results by the created date UPDATED - order results by the updated date TITLE - order results by title EVENT - order results by event CONDUIT - order results by conduit APP_NAME - order results by the application name ('global' templates will not have an application and will be returned last if 'descending' is set to false.
    #' @param descending Specified whether the results are returned in descending or ascending order.
    #' @param start The start of the pagination.
    #' @param limit The limit of the pagination.
    #' @param app_key (optional) Filter results by application.
    #' @param event (optional) Filter results by event.
    #' @param conduit (optional) Filter results by notification type: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param global_only (optional) Returns only templates that have been reserved for system use on all applications (only for admin accounts).
    #' @param reserved_only (optional) Returns only templates that use reserved events.
    #' @param keyword (optional) Filter results by keyword on the title, tags.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (NotificationTemplateResponse) with additional information such as HTTP status code, headers
    SearchNotificationTemplateWithHttpInfo = function(account_id, sort_field, descending, start, limit, app_key = NULL, event = NULL, conduit = NULL, global_only = NULL, reserved_only = NULL, keyword = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$SearchNotificationTemplate, `account_id` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling NotificationApi$SearchNotificationTemplate, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling NotificationApi$SearchNotificationTemplate, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling NotificationApi$SearchNotificationTemplate, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling NotificationApi$SearchNotificationTemplate, `limit` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NotificationApi$SearchNotificationTemplate, `app_key` is not nullable")
      }

      if (!missing(`event`) && is.null(`event`)) {
        stop("Invalid value for `event` when calling NotificationApi$SearchNotificationTemplate, `event` is not nullable")
      }

      if (!missing(`conduit`) && is.null(`conduit`)) {
        stop("Invalid value for `conduit` when calling NotificationApi$SearchNotificationTemplate, `conduit` is not nullable")
      }

      if (!missing(`global_only`) && is.null(`global_only`)) {
        stop("Invalid value for `global_only` when calling NotificationApi$SearchNotificationTemplate, `global_only` is not nullable")
      }

      if (!missing(`reserved_only`) && is.null(`reserved_only`)) {
        stop("Invalid value for `reserved_only` when calling NotificationApi$SearchNotificationTemplate, `reserved_only` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling NotificationApi$SearchNotificationTemplate, `keyword` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["event"]] <- `event`

      query_params[["conduit"]] <- `conduit`

      query_params[["globalOnly"]] <- `global_only`

      query_params[["reservedOnly"]] <- `reserved_only`

      query_params[["keyword"]] <- `keyword`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/notification/template/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "NotificationTemplateResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search for Recipients
    #'
    #' @param sort_field The field to sort by. Possible values include: {ACCOUNT_DISPLAY, CREATED, UPDATED, ACTIVE, DELETED, LAST_LOGGED_IN, CONTACT_EMAIL, RETAILER_LOCATION_NAME, RETAILER_NAME, APPLICATION_NAME}
    #' @param device_id (optional) the unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param app_key (optional) filters results by application. If this is empty, will return all recipients for all applications that the user has access to.
    #' @param conduit (optional) the type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param keyword (optional) search by keyword on user's display name and email
    #' @param audience_id (optional) This parameter is deprecated. filter results by audience
    #' @param audience_ids (optional) filter results by audiences (comma separated list of audience ids)
    #' @param connection_group_ids (optional) filter results by connection groups (comma separated list of connection group ids)
    #' @param recipient_account_ids (optional) filter results by accounts (comma separated list of account ids)
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order
    #' @param start (optional) start of the pagination
    #' @param limit (optional) limit of the pagination (hard limit of 1000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[NotificationRecipientResponse]
    SearchRecipients = function(sort_field, device_id = NULL, account_id = NULL, app_key = NULL, conduit = NULL, keyword = NULL, audience_id = NULL, audience_ids = NULL, connection_group_ids = NULL, recipient_account_ids = NULL, descending = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchRecipientsWithHttpInfo(sort_field, device_id, account_id, app_key, conduit, keyword, audience_id, audience_ids, connection_group_ids, recipient_account_ids, descending, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search for Recipients
    #'
    #' @param sort_field The field to sort by. Possible values include: {ACCOUNT_DISPLAY, CREATED, UPDATED, ACTIVE, DELETED, LAST_LOGGED_IN, CONTACT_EMAIL, RETAILER_LOCATION_NAME, RETAILER_NAME, APPLICATION_NAME}
    #' @param device_id (optional) the unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param app_key (optional) filters results by application. If this is empty, will return all recipients for all applications that the user has access to.
    #' @param conduit (optional) the type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param keyword (optional) search by keyword on user's display name and email
    #' @param audience_id (optional) This parameter is deprecated. filter results by audience
    #' @param audience_ids (optional) filter results by audiences (comma separated list of audience ids)
    #' @param connection_group_ids (optional) filter results by connection groups (comma separated list of connection group ids)
    #' @param recipient_account_ids (optional) filter results by accounts (comma separated list of account ids)
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order
    #' @param start (optional) start of the pagination
    #' @param limit (optional) limit of the pagination (hard limit of 1000)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[NotificationRecipientResponse]) with additional information such as HTTP status code, headers
    SearchRecipientsWithHttpInfo = function(sort_field, device_id = NULL, account_id = NULL, app_key = NULL, conduit = NULL, keyword = NULL, audience_id = NULL, audience_ids = NULL, connection_group_ids = NULL, recipient_account_ids = NULL, descending = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling NotificationApi$SearchRecipients, `sort_field` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NotificationApi$SearchRecipients, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$SearchRecipients, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NotificationApi$SearchRecipients, `app_key` is not nullable")
      }

      if (!missing(`conduit`) && is.null(`conduit`)) {
        stop("Invalid value for `conduit` when calling NotificationApi$SearchRecipients, `conduit` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling NotificationApi$SearchRecipients, `keyword` is not nullable")
      }

      if (!missing(`audience_id`) && is.null(`audience_id`)) {
        stop("Invalid value for `audience_id` when calling NotificationApi$SearchRecipients, `audience_id` is not nullable")
      }

      if (!missing(`audience_ids`) && is.null(`audience_ids`)) {
        stop("Invalid value for `audience_ids` when calling NotificationApi$SearchRecipients, `audience_ids` is not nullable")
      }

      if (!missing(`connection_group_ids`) && is.null(`connection_group_ids`)) {
        stop("Invalid value for `connection_group_ids` when calling NotificationApi$SearchRecipients, `connection_group_ids` is not nullable")
      }

      if (!missing(`recipient_account_ids`) && is.null(`recipient_account_ids`)) {
        stop("Invalid value for `recipient_account_ids` when calling NotificationApi$SearchRecipients, `recipient_account_ids` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling NotificationApi$SearchRecipients, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling NotificationApi$SearchRecipients, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling NotificationApi$SearchRecipients, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["conduit"]] <- `conduit`

      query_params[["keyword"]] <- `keyword`

      query_params[["audienceId"]] <- `audience_id`

      query_params[["audienceIds"]] <- `audience_ids`

      query_params[["connectionGroupIds"]] <- `connection_group_ids`

      query_params[["recipientAccountIds"]] <- `recipient_account_ids`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "HAS_SMS", "HAS_EMAIL", "HAS_APNS", "HAS_GCM", "APPLICATION_ID", "APPLICATION_NAME", "ACCOUNT_ID", "ACCOUNT_USERNAME", "ACCOUNT_DISPLAY", "ACCOUNT_TYPE"))) {
        stop("Invalid value for sort_field when calling NotificationApi$SearchRecipients. Must be [ID, HAS_SMS, HAS_EMAIL, HAS_APNS, HAS_GCM, APPLICATION_ID, APPLICATION_NAME, ACCOUNT_ID, ACCOUNT_USERNAME, ACCOUNT_DISPLAY, ACCOUNT_TYPE].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/notification/recipient/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[NotificationRecipientResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search for Recipients (Counts/Grouped)
    #'
    #' @param device_id (optional) the unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param app_key (optional) filters results by application. If this is empty, will return all recipients for all applications that the user has access to.
    #' @param conduit (optional) the type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param keyword (optional) search by keyword on user's display name and email
    #' @param audience_id (optional) This parameter is deprecated. filter results by audience
    #' @param audience_ids (optional) filter results by audiences (comma separated list of audience ids)
    #' @param connection_group_ids (optional) filter results by connection groups (comma separated list of connection group ids)
    #' @param sort_field (optional) The field to sort by (see API docs for allowed values).
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order
    #' @param start (optional) start of the pagination
    #' @param limit (optional) limit of the pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return NotificationRecipientResponseListResponse
    SearchRecipientsCount = function(device_id = NULL, account_id = NULL, app_key = NULL, conduit = NULL, keyword = NULL, audience_id = NULL, audience_ids = NULL, connection_group_ids = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchRecipientsCountWithHttpInfo(device_id, account_id, app_key, conduit, keyword, audience_id, audience_ids, connection_group_ids, sort_field, descending, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search for Recipients (Counts/Grouped)
    #'
    #' @param device_id (optional) the unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param app_key (optional) filters results by application. If this is empty, will return all recipients for all applications that the user has access to.
    #' @param conduit (optional) the type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param keyword (optional) search by keyword on user's display name and email
    #' @param audience_id (optional) This parameter is deprecated. filter results by audience
    #' @param audience_ids (optional) filter results by audiences (comma separated list of audience ids)
    #' @param connection_group_ids (optional) filter results by connection groups (comma separated list of connection group ids)
    #' @param sort_field (optional) The field to sort by (see API docs for allowed values).
    #' @param descending (optional) Determines whether the sorted list is in descending or ascending order
    #' @param start (optional) start of the pagination
    #' @param limit (optional) limit of the pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (NotificationRecipientResponseListResponse) with additional information such as HTTP status code, headers
    SearchRecipientsCountWithHttpInfo = function(device_id = NULL, account_id = NULL, app_key = NULL, conduit = NULL, keyword = NULL, audience_id = NULL, audience_ids = NULL, connection_group_ids = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NotificationApi$SearchRecipientsCount, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$SearchRecipientsCount, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NotificationApi$SearchRecipientsCount, `app_key` is not nullable")
      }

      if (!missing(`conduit`) && is.null(`conduit`)) {
        stop("Invalid value for `conduit` when calling NotificationApi$SearchRecipientsCount, `conduit` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling NotificationApi$SearchRecipientsCount, `keyword` is not nullable")
      }

      if (!missing(`audience_id`) && is.null(`audience_id`)) {
        stop("Invalid value for `audience_id` when calling NotificationApi$SearchRecipientsCount, `audience_id` is not nullable")
      }

      if (!missing(`audience_ids`) && is.null(`audience_ids`)) {
        stop("Invalid value for `audience_ids` when calling NotificationApi$SearchRecipientsCount, `audience_ids` is not nullable")
      }

      if (!missing(`connection_group_ids`) && is.null(`connection_group_ids`)) {
        stop("Invalid value for `connection_group_ids` when calling NotificationApi$SearchRecipientsCount, `connection_group_ids` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling NotificationApi$SearchRecipientsCount, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling NotificationApi$SearchRecipientsCount, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling NotificationApi$SearchRecipientsCount, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling NotificationApi$SearchRecipientsCount, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["conduit"]] <- `conduit`

      query_params[["keyword"]] <- `keyword`

      query_params[["audienceId"]] <- `audience_id`

      query_params[["audienceIds"]] <- `audience_ids`

      query_params[["connectionGroupIds"]] <- `connection_group_ids`

      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/notification/recipient/search/count"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "NotificationRecipientResponseListResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Send Batch Notifications
    #'
    #' @param account_id The account id of the application owner/manager
    #' @param app_key The application key for updating an existing application
    #' @param custom_message Message string that will be displayed in on the notification
    #' @param conduit (optional) The type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param content_id (optional) Default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param content_name (optional) Default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param content_type (optional) Default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param parent_id (optional) Default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param parent_type (optional) Default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    SendBatchNotifications = function(account_id, app_key, custom_message, conduit = NULL, content_id = NULL, content_name = NULL, content_type = NULL, parent_id = NULL, parent_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SendBatchNotificationsWithHttpInfo(account_id, app_key, custom_message, conduit, content_id, content_name, content_type, parent_id, parent_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Send Batch Notifications
    #'
    #' @param account_id The account id of the application owner/manager
    #' @param app_key The application key for updating an existing application
    #' @param custom_message Message string that will be displayed in on the notification
    #' @param conduit (optional) The type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param content_id (optional) Default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param content_name (optional) Default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param content_type (optional) Default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param parent_id (optional) Default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param parent_type (optional) Default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    SendBatchNotificationsWithHttpInfo = function(account_id, app_key, custom_message, conduit = NULL, content_id = NULL, content_name = NULL, content_type = NULL, parent_id = NULL, parent_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`custom_message`)) {
        stop("Missing required parameter `custom_message`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$SendBatchNotifications, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NotificationApi$SendBatchNotifications, `app_key` is not nullable")
      }

      if (!missing(`custom_message`) && is.null(`custom_message`)) {
        stop("Invalid value for `custom_message` when calling NotificationApi$SendBatchNotifications, `custom_message` is not nullable")
      }

      if (!missing(`conduit`) && is.null(`conduit`)) {
        stop("Invalid value for `conduit` when calling NotificationApi$SendBatchNotifications, `conduit` is not nullable")
      }

      if (!missing(`content_id`) && is.null(`content_id`)) {
        stop("Invalid value for `content_id` when calling NotificationApi$SendBatchNotifications, `content_id` is not nullable")
      }

      if (!missing(`content_name`) && is.null(`content_name`)) {
        stop("Invalid value for `content_name` when calling NotificationApi$SendBatchNotifications, `content_name` is not nullable")
      }

      if (!missing(`content_type`) && is.null(`content_type`)) {
        stop("Invalid value for `content_type` when calling NotificationApi$SendBatchNotifications, `content_type` is not nullable")
      }

      if (!missing(`parent_id`) && is.null(`parent_id`)) {
        stop("Invalid value for `parent_id` when calling NotificationApi$SendBatchNotifications, `parent_id` is not nullable")
      }

      if (!missing(`parent_type`) && is.null(`parent_type`)) {
        stop("Invalid value for `parent_type` when calling NotificationApi$SendBatchNotifications, `parent_type` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["conduit"]] <- `conduit`

      query_params[["customMessage"]] <- `custom_message`

      query_params[["contentId"]] <- `content_id`

      query_params[["contentName"]] <- `content_name`

      query_params[["contentType"]] <- `content_type`

      query_params[["parentId"]] <- `parent_id`

      query_params[["parentType"]] <- `parent_type`

      local_var_url_path <- "/notification/batch"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Send Custom Notifications
    #'
    #' @param device_id (optional) the unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param receiver_account_ids (optional) comma separated list of account IDs that will receive the notification
    #' @param include_friend_group (optional) determines whether to send to all of the user's friends, this flag must be true or receiverAccountIds must not be empty
    #' @param app_key (optional) the application key
    #' @param game_type (optional) This parameter is deprecated.
    #' @param conduit (optional) the type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param content_id (optional) default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param content_name (optional) default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param content_type (optional) default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param parent_id (optional) default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param parent_type (optional) default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param action_category (optional) 
    #' @param subject (optional) the subject line of an email #@param customPayload custom json definition of notification pay-load (usage is dependent on the app and the type of event)
    #' @param custom_message (optional) message string that will be displayed in on the notification
    #' @param friend_only_apns (optional) only sends APNS to people who are friends of the user (still saves the notification message for feed polling)
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    SendCustomNotifications = function(device_id = NULL, account_id = NULL, receiver_account_ids = NULL, include_friend_group = NULL, app_key = NULL, game_type = NULL, conduit = NULL, content_id = NULL, content_name = NULL, content_type = NULL, parent_id = NULL, parent_type = NULL, action_category = NULL, subject = NULL, custom_message = NULL, friend_only_apns = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SendCustomNotificationsWithHttpInfo(device_id, account_id, receiver_account_ids, include_friend_group, app_key, game_type, conduit, content_id, content_name, content_type, parent_id, parent_type, action_category, subject, custom_message, friend_only_apns, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Send Custom Notifications
    #'
    #' @param device_id (optional) the unique id of the device making the request (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param receiver_account_ids (optional) comma separated list of account IDs that will receive the notification
    #' @param include_friend_group (optional) determines whether to send to all of the user's friends, this flag must be true or receiverAccountIds must not be empty
    #' @param app_key (optional) the application key
    #' @param game_type (optional) This parameter is deprecated.
    #' @param conduit (optional) the type of notification to send: EMAIL, SMS, PUSH, MOBILE_NOTIFICATION.
    #' @param content_id (optional) default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param content_name (optional) default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param content_type (optional) default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param parent_id (optional) default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param parent_type (optional) default notification pay-load field (usage is dependent on the app and the type of event)
    #' @param action_category (optional) 
    #' @param subject (optional) the subject line of an email #@param customPayload custom json definition of notification pay-load (usage is dependent on the app and the type of event)
    #' @param custom_message (optional) message string that will be displayed in on the notification
    #' @param friend_only_apns (optional) only sends APNS to people who are friends of the user (still saves the notification message for feed polling)
    #' @param latitude (optional) latitude used to update the user's current location
    #' @param longitude (optional) longitude used to update the user's current location
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    SendCustomNotificationsWithHttpInfo = function(device_id = NULL, account_id = NULL, receiver_account_ids = NULL, include_friend_group = NULL, app_key = NULL, game_type = NULL, conduit = NULL, content_id = NULL, content_name = NULL, content_type = NULL, parent_id = NULL, parent_type = NULL, action_category = NULL, subject = NULL, custom_message = NULL, friend_only_apns = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling NotificationApi$SendCustomNotifications, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$SendCustomNotifications, `account_id` is not nullable")
      }

      if (!missing(`receiver_account_ids`) && is.null(`receiver_account_ids`)) {
        stop("Invalid value for `receiver_account_ids` when calling NotificationApi$SendCustomNotifications, `receiver_account_ids` is not nullable")
      }

      if (!missing(`include_friend_group`) && is.null(`include_friend_group`)) {
        stop("Invalid value for `include_friend_group` when calling NotificationApi$SendCustomNotifications, `include_friend_group` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling NotificationApi$SendCustomNotifications, `app_key` is not nullable")
      }

      if (!missing(`game_type`) && is.null(`game_type`)) {
        stop("Invalid value for `game_type` when calling NotificationApi$SendCustomNotifications, `game_type` is not nullable")
      }

      if (!missing(`conduit`) && is.null(`conduit`)) {
        stop("Invalid value for `conduit` when calling NotificationApi$SendCustomNotifications, `conduit` is not nullable")
      }

      if (!missing(`content_id`) && is.null(`content_id`)) {
        stop("Invalid value for `content_id` when calling NotificationApi$SendCustomNotifications, `content_id` is not nullable")
      }

      if (!missing(`content_name`) && is.null(`content_name`)) {
        stop("Invalid value for `content_name` when calling NotificationApi$SendCustomNotifications, `content_name` is not nullable")
      }

      if (!missing(`content_type`) && is.null(`content_type`)) {
        stop("Invalid value for `content_type` when calling NotificationApi$SendCustomNotifications, `content_type` is not nullable")
      }

      if (!missing(`parent_id`) && is.null(`parent_id`)) {
        stop("Invalid value for `parent_id` when calling NotificationApi$SendCustomNotifications, `parent_id` is not nullable")
      }

      if (!missing(`parent_type`) && is.null(`parent_type`)) {
        stop("Invalid value for `parent_type` when calling NotificationApi$SendCustomNotifications, `parent_type` is not nullable")
      }

      if (!missing(`action_category`) && is.null(`action_category`)) {
        stop("Invalid value for `action_category` when calling NotificationApi$SendCustomNotifications, `action_category` is not nullable")
      }

      if (!missing(`subject`) && is.null(`subject`)) {
        stop("Invalid value for `subject` when calling NotificationApi$SendCustomNotifications, `subject` is not nullable")
      }

      if (!missing(`custom_message`) && is.null(`custom_message`)) {
        stop("Invalid value for `custom_message` when calling NotificationApi$SendCustomNotifications, `custom_message` is not nullable")
      }

      if (!missing(`friend_only_apns`) && is.null(`friend_only_apns`)) {
        stop("Invalid value for `friend_only_apns` when calling NotificationApi$SendCustomNotifications, `friend_only_apns` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling NotificationApi$SendCustomNotifications, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling NotificationApi$SendCustomNotifications, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["receiverAccountIds"]] <- `receiver_account_ids`

      query_params[["includeFriendGroup"]] <- `include_friend_group`

      query_params[["appKey"]] <- `app_key`

      query_params[["gameType"]] <- `game_type`

      query_params[["conduit"]] <- `conduit`

      query_params[["contentId"]] <- `content_id`

      query_params[["contentName"]] <- `content_name`

      query_params[["contentType"]] <- `content_type`

      query_params[["parentId"]] <- `parent_id`

      query_params[["parentType"]] <- `parent_type`

      query_params[["actionCategory"]] <- `action_category`

      query_params[["subject"]] <- `subject`

      query_params[["customMessage"]] <- `custom_message`

      query_params[["friendOnlyAPNS"]] <- `friend_only_apns`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/notification/custom"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Notification Template
    #'
    #' @param account_id The account ID of the user.
    #' @param notification_template_id The notification template ID to update.
    #' @param title (optional) The title of the message (this would become the subject title for emails). There is a 191 character limit.
    #' @param body (optional) The body of the message.
    #' @param tags (optional) The search tags on the template used during search queries.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return NotificationTemplateResponse
    UpdateNotificationTemplate = function(account_id, notification_template_id, title = NULL, body = NULL, tags = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateNotificationTemplateWithHttpInfo(account_id, notification_template_id, title, body, tags, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Notification Template
    #'
    #' @param account_id The account ID of the user.
    #' @param notification_template_id The notification template ID to update.
    #' @param title (optional) The title of the message (this would become the subject title for emails). There is a 191 character limit.
    #' @param body (optional) The body of the message.
    #' @param tags (optional) The search tags on the template used during search queries.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (NotificationTemplateResponse) with additional information such as HTTP status code, headers
    UpdateNotificationTemplateWithHttpInfo = function(account_id, notification_template_id, title = NULL, body = NULL, tags = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`notification_template_id`)) {
        stop("Missing required parameter `notification_template_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling NotificationApi$UpdateNotificationTemplate, `account_id` is not nullable")
      }

      if (!missing(`notification_template_id`) && is.null(`notification_template_id`)) {
        stop("Invalid value for `notification_template_id` when calling NotificationApi$UpdateNotificationTemplate, `notification_template_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling NotificationApi$UpdateNotificationTemplate, `title` is not nullable")
      }

      if (!missing(`body`) && is.null(`body`)) {
        stop("Invalid value for `body` when calling NotificationApi$UpdateNotificationTemplate, `body` is not nullable")
      }

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling NotificationApi$UpdateNotificationTemplate, `tags` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["notificationTemplateId"]] <- `notification_template_id`

      query_params[["title"]] <- `title`

      query_params[["body"]] <- `body`

      query_params[["tags"]] <- `tags`

      local_var_url_path <- "/notification/template/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "NotificationTemplateResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
