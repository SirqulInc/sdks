#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Event operations
#' @description EventApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AttendEvent  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id (deviceId or accountId required) (Optional)
#' var_app_key <- "app_key_example" # character | The application of where to send notifications about the attend action (Optional)
#' var_listing_id <- 56 # integer | The scheduled broadcast or marketing experience id (Optional)
#' var_retailer_location_id <- 56 # integer | The retailer location where the event is being held (Optional)
#' var_offer_location_id <- 56 # integer | The actual event being held (Optional)
#' var_transaction_id <- 56 # integer | The wallet item to update the status of, if provided then ignore the listingId, retailerLocationId, and the offerLocationId (Optional)
#' var_status <- 56 # integer | Sets whether the user is: undecided (0), attending (1), attending and checked in (2), or not attending (3) (Optional)
#' var_latitude <- 3.4 # numeric | The location of the status update (Optional)
#' var_longitude <- 3.4 # numeric | The location of the status update (Optional)
#'
#' #Attend Event
#' api_instance <- EventApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AttendEvent(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, listing_id = var_listing_id, retailer_location_id = var_retailer_location_id, offer_location_id = var_offer_location_id, transaction_id = var_transaction_id, status = var_status, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$AttendEvent(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, listing_id = var_listing_id, retailer_location_id = var_retailer_location_id, offer_location_id = var_offer_location_id, transaction_id = var_transaction_id, status = var_status, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  CreateEvent  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_title <- "title_example" # character | The event title
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | The retailer location to have the event at (Optional)
#' var_sub_title <- "sub_title_example" # character | The event sub title (Optional)
#' var_details <- "details_example" # character | The event details (Optional)
#' var_category_ids <- "category_ids_example" # character | The categories the associate the event with (Optional)
#' var_filter_ids <- "filter_ids_example" # character | The filters the associate the event with (Optional)
#' var_active <- "active_example" # character | Is this event active (Optional)
#' var_image_asset_id <- 56 # integer | The image to show for the event (Optional)
#' var_redeemable_start <- 56 # integer | The event start date/time (Optional)
#' var_redeemable_end <- 56 # integer | The event end date/time (Optional)
#' var_meta_data <- "meta_data_example" # character | external custom client defined data (Optional)
#'
#' #Create Event
#' api_instance <- EventApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateEvent(var_account_id, var_title, retailer_location_ids = var_retailer_location_ids, sub_title = var_sub_title, details = var_details, category_ids = var_category_ids, filter_ids = var_filter_ids, active = var_active, image_asset_id = var_image_asset_id, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end, meta_data = var_meta_datadata_file = "result.txt")
#' result <- api_instance$CreateEvent(var_account_id, var_title, retailer_location_ids = var_retailer_location_ids, sub_title = var_sub_title, details = var_details, category_ids = var_category_ids, filter_ids = var_filter_ids, active = var_active, image_asset_id = var_image_asset_id, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end, meta_data = var_meta_data)
#' dput(result)
#'
#'
#' ####################  DeleteEvent  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_event_id <- 56 # integer | the id of the event to update
#'
#' #Delete Event
#' api_instance <- EventApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteEvent(var_account_id, var_event_iddata_file = "result.txt")
#' result <- api_instance$DeleteEvent(var_account_id, var_event_id)
#' dput(result)
#'
#'
#' ####################  GetEvent  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the id of the logged in user
#' var_event_id <- 56 # integer | The id of the event to return
#'
#' #Get Event
#' api_instance <- EventApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetEvent(var_account_id, var_event_iddata_file = "result.txt")
#' result <- api_instance$GetEvent(var_account_id, var_event_id)
#' dput(result)
#'
#'
#' ####################  SearchEventTransactions  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_app_key <- "app_key_example" # character | The application key (Optional)
#' var_keyword <- "keyword_example" # character | The keyword to search for (Optional)
#' var_retailer_id <- 56 # integer | Filter results for this retailer (Optional)
#' var_retailer_location_id <- 56 # integer | Filter results for this retailer location (Optional)
#' var_exclude_retailer_location_id <- 56 # integer | Exclude results from this retailer location (Optional)
#' var_listing_id <- 56 # integer | Filter results for this event listing (Optional)
#' var_offer_id <- 56 # integer | Filter results for this offer (Optional)
#' var_offer_location_id <- 56 # integer | Filter results for this offer location (Optional)
#' var_customer_account_ids <- "customer_account_ids_example" # character | Filter results by accounts (Optional)
#' var_affiliated_category_ids <- "affiliated_category_ids_example" # character | Comma separated list of category ids to determine whether the attendee is affiliated with the category (Optional)
#' var_start_date <- 56 # integer | Filter on attendance starting on or after this date (milliseconds since epoch) (Optional)
#' var_end_date <- 56 # integer | Filter on attendance starting on or before this date (milliseconds since epoch) (Optional)
#' var_statuses <- "statuses_example" # character | Comma separated list of transaction statuses to filter on (Optional)
#' var_sort_field <- "sort_field_example" # character | Determines what to sort the results by {CREATED, UPDATED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, CUSTOMER_ID, CUSTOMER_DISPLAY, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY} (Optional)
#' var_descending <- "descending_example" # character | Determines whether the results are in descending order (Optional)
#' var_start <- 56 # integer | The start index for pagination (Optional)
#' var_limit <- 56 # integer | The limit for pagination (Optional)
#'
#' #Search Event Attendance
#' api_instance <- EventApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchEventTransactions(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, keyword = var_keyword, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, exclude_retailer_location_id = var_exclude_retailer_location_id, listing_id = var_listing_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, customer_account_ids = var_customer_account_ids, affiliated_category_ids = var_affiliated_category_ids, start_date = var_start_date, end_date = var_end_date, statuses = var_statuses, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchEventTransactions(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, keyword = var_keyword, retailer_id = var_retailer_id, retailer_location_id = var_retailer_location_id, exclude_retailer_location_id = var_exclude_retailer_location_id, listing_id = var_listing_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, customer_account_ids = var_customer_account_ids, affiliated_category_ids = var_affiliated_category_ids, start_date = var_start_date, end_date = var_end_date, statuses = var_statuses, sort_field = var_sort_field, descending = var_descending, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  SearchEvents  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_active_only <- "active_only_example" # character | Return only active results (Optional)
#' var_category_ids <- "category_ids_example" # character | the IDs of the categories that this event is associated with (Optional)
#' var_filter_ids <- "filter_ids_example" # character | the IDs of the filters that this event is associated with (Optional)
#' var_offer_audience_ids <- "offer_audience_ids_example" # character | Offer audience ids to filter on (Optional)
#' var_transaction_audience_ids <- "transaction_audience_ids_example" # character | Transaction audience ids to filter on (Optional)
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY (Optional)
#' var_descending <- "descending_example" # character | The order to return the search results (Optional)
#' var_start_date <- 56 # integer | Filter the events to return only those that start on or after the date (Optional)
#' var_end_date <- 56 # integer | Filter the events to return only those that start on or before the date (Optional)
#' var_start <- 56 # integer | The record to begin the return set on (Optional)
#' var_limit <- 56 # integer | The number of records to return (Optional)
#'
#' #Search Events
#' api_instance <- EventApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchEvents(var_account_id, keyword = var_keyword, active_only = var_active_only, category_ids = var_category_ids, filter_ids = var_filter_ids, offer_audience_ids = var_offer_audience_ids, transaction_audience_ids = var_transaction_audience_ids, sort_field = var_sort_field, descending = var_descending, start_date = var_start_date, end_date = var_end_date, start = var_start, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchEvents(var_account_id, keyword = var_keyword, active_only = var_active_only, category_ids = var_category_ids, filter_ids = var_filter_ids, offer_audience_ids = var_offer_audience_ids, transaction_audience_ids = var_transaction_audience_ids, sort_field = var_sort_field, descending = var_descending, start_date = var_start_date, end_date = var_end_date, start = var_start, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  UpdateEvent  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | The logged in user.
#' var_event_id <- 56 # integer | The id of the event to update
#' var_retailer_location_ids <- "retailer_location_ids_example" # character | The retailer location to have the event at (Optional)
#' var_title <- "title_example" # character | The event title (Optional)
#' var_sub_title <- "sub_title_example" # character | The event sub title (Optional)
#' var_details <- "details_example" # character | The event details (Optional)
#' var_category_ids <- "category_ids_example" # character | The categories the associate the event with (Optional)
#' var_filter_ids <- "filter_ids_example" # character | The filters the associate the event with (Optional)
#' var_active <- "active_example" # character | Is this event active (Optional)
#' var_image_asset_id <- 56 # integer | The image to show for the event (Optional)
#' var_redeemable_start <- 56 # integer | The event start date/time (Optional)
#' var_redeemable_end <- 56 # integer | The event end date/time (Optional)
#'
#' #Update Event
#' api_instance <- EventApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateEvent(var_account_id, var_event_id, retailer_location_ids = var_retailer_location_ids, title = var_title, sub_title = var_sub_title, details = var_details, category_ids = var_category_ids, filter_ids = var_filter_ids, active = var_active, image_asset_id = var_image_asset_id, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_enddata_file = "result.txt")
#' result <- api_instance$UpdateEvent(var_account_id, var_event_id, retailer_location_ids = var_retailer_location_ids, title = var_title, sub_title = var_sub_title, details = var_details, category_ids = var_category_ids, filter_ids = var_filter_ids, active = var_active, image_asset_id = var_image_asset_id, redeemable_start = var_redeemable_start, redeemable_end = var_redeemable_end)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
EventApi <- R6::R6Class(
  "EventApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new EventApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Attend Event
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id (deviceId or accountId required)
    #' @param app_key (optional) The application of where to send notifications about the attend action
    #' @param listing_id (optional) The scheduled broadcast or marketing experience id
    #' @param retailer_location_id (optional) The retailer location where the event is being held
    #' @param offer_location_id (optional) The actual event being held
    #' @param transaction_id (optional) The wallet item to update the status of, if provided then ignore the listingId, retailerLocationId, and the offerLocationId
    #' @param status (optional) Sets whether the user is: undecided (0), attending (1), attending and checked in (2), or not attending (3)
    #' @param latitude (optional) The location of the status update
    #' @param longitude (optional) The location of the status update
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferResponse
    AttendEvent = function(device_id = NULL, account_id = NULL, app_key = NULL, listing_id = NULL, retailer_location_id = NULL, offer_location_id = NULL, transaction_id = NULL, status = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AttendEventWithHttpInfo(device_id, account_id, app_key, listing_id, retailer_location_id, offer_location_id, transaction_id, status, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Attend Event
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id (deviceId or accountId required)
    #' @param app_key (optional) The application of where to send notifications about the attend action
    #' @param listing_id (optional) The scheduled broadcast or marketing experience id
    #' @param retailer_location_id (optional) The retailer location where the event is being held
    #' @param offer_location_id (optional) The actual event being held
    #' @param transaction_id (optional) The wallet item to update the status of, if provided then ignore the listingId, retailerLocationId, and the offerLocationId
    #' @param status (optional) Sets whether the user is: undecided (0), attending (1), attending and checked in (2), or not attending (3)
    #' @param latitude (optional) The location of the status update
    #' @param longitude (optional) The location of the status update
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferResponse) with additional information such as HTTP status code, headers
    AttendEventWithHttpInfo = function(device_id = NULL, account_id = NULL, app_key = NULL, listing_id = NULL, retailer_location_id = NULL, offer_location_id = NULL, transaction_id = NULL, status = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling EventApi$AttendEvent, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EventApi$AttendEvent, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling EventApi$AttendEvent, `app_key` is not nullable")
      }

      if (!missing(`listing_id`) && is.null(`listing_id`)) {
        stop("Invalid value for `listing_id` when calling EventApi$AttendEvent, `listing_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling EventApi$AttendEvent, `retailer_location_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling EventApi$AttendEvent, `offer_location_id` is not nullable")
      }

      if (!missing(`transaction_id`) && is.null(`transaction_id`)) {
        stop("Invalid value for `transaction_id` when calling EventApi$AttendEvent, `transaction_id` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling EventApi$AttendEvent, `status` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling EventApi$AttendEvent, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling EventApi$AttendEvent, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["listingId"]] <- `listing_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["transactionId"]] <- `transaction_id`

      query_params[["status"]] <- `status`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/event/attend"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Event
    #'
    #' @param account_id The logged in user.
    #' @param title The event title
    #' @param retailer_location_ids (optional) The retailer location to have the event at
    #' @param sub_title (optional) The event sub title
    #' @param details (optional) The event details
    #' @param category_ids (optional) The categories the associate the event with
    #' @param filter_ids (optional) The filters the associate the event with
    #' @param active (optional) Is this event active
    #' @param image_asset_id (optional) The image to show for the event
    #' @param redeemable_start (optional) The event start date/time
    #' @param redeemable_end (optional) The event end date/time
    #' @param meta_data (optional) external custom client defined data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferResponse
    CreateEvent = function(account_id, title, retailer_location_ids = NULL, sub_title = NULL, details = NULL, category_ids = NULL, filter_ids = NULL, active = NULL, image_asset_id = NULL, redeemable_start = NULL, redeemable_end = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateEventWithHttpInfo(account_id, title, retailer_location_ids, sub_title, details, category_ids, filter_ids, active, image_asset_id, redeemable_start, redeemable_end, meta_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Event
    #'
    #' @param account_id The logged in user.
    #' @param title The event title
    #' @param retailer_location_ids (optional) The retailer location to have the event at
    #' @param sub_title (optional) The event sub title
    #' @param details (optional) The event details
    #' @param category_ids (optional) The categories the associate the event with
    #' @param filter_ids (optional) The filters the associate the event with
    #' @param active (optional) Is this event active
    #' @param image_asset_id (optional) The image to show for the event
    #' @param redeemable_start (optional) The event start date/time
    #' @param redeemable_end (optional) The event end date/time
    #' @param meta_data (optional) external custom client defined data
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferResponse) with additional information such as HTTP status code, headers
    CreateEventWithHttpInfo = function(account_id, title, retailer_location_ids = NULL, sub_title = NULL, details = NULL, category_ids = NULL, filter_ids = NULL, active = NULL, image_asset_id = NULL, redeemable_start = NULL, redeemable_end = NULL, meta_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`title`)) {
        stop("Missing required parameter `title`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EventApi$CreateEvent, `account_id` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling EventApi$CreateEvent, `title` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling EventApi$CreateEvent, `retailer_location_ids` is not nullable")
      }

      if (!missing(`sub_title`) && is.null(`sub_title`)) {
        stop("Invalid value for `sub_title` when calling EventApi$CreateEvent, `sub_title` is not nullable")
      }

      if (!missing(`details`) && is.null(`details`)) {
        stop("Invalid value for `details` when calling EventApi$CreateEvent, `details` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling EventApi$CreateEvent, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling EventApi$CreateEvent, `filter_ids` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling EventApi$CreateEvent, `active` is not nullable")
      }

      if (!missing(`image_asset_id`) && is.null(`image_asset_id`)) {
        stop("Invalid value for `image_asset_id` when calling EventApi$CreateEvent, `image_asset_id` is not nullable")
      }

      if (!missing(`redeemable_start`) && is.null(`redeemable_start`)) {
        stop("Invalid value for `redeemable_start` when calling EventApi$CreateEvent, `redeemable_start` is not nullable")
      }

      if (!missing(`redeemable_end`) && is.null(`redeemable_end`)) {
        stop("Invalid value for `redeemable_end` when calling EventApi$CreateEvent, `redeemable_end` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling EventApi$CreateEvent, `meta_data` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["title"]] <- `title`

      query_params[["subTitle"]] <- `sub_title`

      query_params[["details"]] <- `details`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["active"]] <- `active`

      query_params[["imageAssetId"]] <- `image_asset_id`

      query_params[["redeemableStart"]] <- `redeemable_start`

      query_params[["redeemableEnd"]] <- `redeemable_end`

      query_params[["metaData"]] <- `meta_data`

      local_var_url_path <- "/event/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Event
    #'
    #' @param account_id the id of the logged in user
    #' @param event_id the id of the event to update
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteEvent = function(account_id, event_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteEventWithHttpInfo(account_id, event_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Event
    #'
    #' @param account_id the id of the logged in user
    #' @param event_id the id of the event to update
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteEventWithHttpInfo = function(account_id, event_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`event_id`)) {
        stop("Missing required parameter `event_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EventApi$DeleteEvent, `account_id` is not nullable")
      }

      if (!missing(`event_id`) && is.null(`event_id`)) {
        stop("Invalid value for `event_id` when calling EventApi$DeleteEvent, `event_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["eventId"]] <- `event_id`

      local_var_url_path <- "/event/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Event
    #'
    #' @param account_id the id of the logged in user
    #' @param event_id The id of the event to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferResponse
    GetEvent = function(account_id, event_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetEventWithHttpInfo(account_id, event_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Event
    #'
    #' @param account_id the id of the logged in user
    #' @param event_id The id of the event to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferResponse) with additional information such as HTTP status code, headers
    GetEventWithHttpInfo = function(account_id, event_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`event_id`)) {
        stop("Missing required parameter `event_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EventApi$GetEvent, `account_id` is not nullable")
      }

      if (!missing(`event_id`) && is.null(`event_id`)) {
        stop("Invalid value for `event_id` when calling EventApi$GetEvent, `event_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["eventId"]] <- `event_id`

      local_var_url_path <- "/event/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Event Attendance
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param app_key (optional) The application key
    #' @param keyword (optional) The keyword to search for
    #' @param retailer_id (optional) Filter results for this retailer
    #' @param retailer_location_id (optional) Filter results for this retailer location
    #' @param exclude_retailer_location_id (optional) Exclude results from this retailer location
    #' @param listing_id (optional) Filter results for this event listing
    #' @param offer_id (optional) Filter results for this offer
    #' @param offer_location_id (optional) Filter results for this offer location
    #' @param customer_account_ids (optional) Filter results by accounts
    #' @param affiliated_category_ids (optional) Comma separated list of category ids to determine whether the attendee is affiliated with the category
    #' @param start_date (optional) Filter on attendance starting on or after this date (milliseconds since epoch)
    #' @param end_date (optional) Filter on attendance starting on or before this date (milliseconds since epoch)
    #' @param statuses (optional) Comma separated list of transaction statuses to filter on
    #' @param sort_field (optional) Determines what to sort the results by {CREATED, UPDATED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, CUSTOMER_ID, CUSTOMER_DISPLAY, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY}
    #' @param descending (optional) Determines whether the results are in descending order
    #' @param start (optional) The start index for pagination
    #' @param limit (optional) The limit for pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[EventAttendanceResponse]
    SearchEventTransactions = function(device_id = NULL, account_id = NULL, app_key = NULL, keyword = NULL, retailer_id = NULL, retailer_location_id = NULL, exclude_retailer_location_id = NULL, listing_id = NULL, offer_id = NULL, offer_location_id = NULL, customer_account_ids = NULL, affiliated_category_ids = NULL, start_date = NULL, end_date = NULL, statuses = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchEventTransactionsWithHttpInfo(device_id, account_id, app_key, keyword, retailer_id, retailer_location_id, exclude_retailer_location_id, listing_id, offer_id, offer_location_id, customer_account_ids, affiliated_category_ids, start_date, end_date, statuses, sort_field, descending, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Event Attendance
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param app_key (optional) The application key
    #' @param keyword (optional) The keyword to search for
    #' @param retailer_id (optional) Filter results for this retailer
    #' @param retailer_location_id (optional) Filter results for this retailer location
    #' @param exclude_retailer_location_id (optional) Exclude results from this retailer location
    #' @param listing_id (optional) Filter results for this event listing
    #' @param offer_id (optional) Filter results for this offer
    #' @param offer_location_id (optional) Filter results for this offer location
    #' @param customer_account_ids (optional) Filter results by accounts
    #' @param affiliated_category_ids (optional) Comma separated list of category ids to determine whether the attendee is affiliated with the category
    #' @param start_date (optional) Filter on attendance starting on or after this date (milliseconds since epoch)
    #' @param end_date (optional) Filter on attendance starting on or before this date (milliseconds since epoch)
    #' @param statuses (optional) Comma separated list of transaction statuses to filter on
    #' @param sort_field (optional) Determines what to sort the results by {CREATED, UPDATED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, CUSTOMER_ID, CUSTOMER_DISPLAY, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY}
    #' @param descending (optional) Determines whether the results are in descending order
    #' @param start (optional) The start index for pagination
    #' @param limit (optional) The limit for pagination
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[EventAttendanceResponse]) with additional information such as HTTP status code, headers
    SearchEventTransactionsWithHttpInfo = function(device_id = NULL, account_id = NULL, app_key = NULL, keyword = NULL, retailer_id = NULL, retailer_location_id = NULL, exclude_retailer_location_id = NULL, listing_id = NULL, offer_id = NULL, offer_location_id = NULL, customer_account_ids = NULL, affiliated_category_ids = NULL, start_date = NULL, end_date = NULL, statuses = NULL, sort_field = NULL, descending = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling EventApi$SearchEventTransactions, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EventApi$SearchEventTransactions, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling EventApi$SearchEventTransactions, `app_key` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling EventApi$SearchEventTransactions, `keyword` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling EventApi$SearchEventTransactions, `retailer_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling EventApi$SearchEventTransactions, `retailer_location_id` is not nullable")
      }

      if (!missing(`exclude_retailer_location_id`) && is.null(`exclude_retailer_location_id`)) {
        stop("Invalid value for `exclude_retailer_location_id` when calling EventApi$SearchEventTransactions, `exclude_retailer_location_id` is not nullable")
      }

      if (!missing(`listing_id`) && is.null(`listing_id`)) {
        stop("Invalid value for `listing_id` when calling EventApi$SearchEventTransactions, `listing_id` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling EventApi$SearchEventTransactions, `offer_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling EventApi$SearchEventTransactions, `offer_location_id` is not nullable")
      }

      if (!missing(`customer_account_ids`) && is.null(`customer_account_ids`)) {
        stop("Invalid value for `customer_account_ids` when calling EventApi$SearchEventTransactions, `customer_account_ids` is not nullable")
      }

      if (!missing(`affiliated_category_ids`) && is.null(`affiliated_category_ids`)) {
        stop("Invalid value for `affiliated_category_ids` when calling EventApi$SearchEventTransactions, `affiliated_category_ids` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling EventApi$SearchEventTransactions, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling EventApi$SearchEventTransactions, `end_date` is not nullable")
      }

      if (!missing(`statuses`) && is.null(`statuses`)) {
        stop("Invalid value for `statuses` when calling EventApi$SearchEventTransactions, `statuses` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling EventApi$SearchEventTransactions, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling EventApi$SearchEventTransactions, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling EventApi$SearchEventTransactions, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling EventApi$SearchEventTransactions, `limit` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["keyword"]] <- `keyword`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["excludeRetailerLocationId"]] <- `exclude_retailer_location_id`

      query_params[["listingId"]] <- `listing_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["customerAccountIds"]] <- `customer_account_ids`

      query_params[["affiliatedCategoryIds"]] <- `affiliated_category_ids`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["statuses"]] <- `statuses`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "ACTIVATED", "EXPIRES", "TITLE", "SUBTITLE", "DETAILS", "OFFER_TYPE", "SPECIAL_OFFER_TYPE", "OFFER_VISIBILITY", "REDEEMABLE_START", "REDEEMABLE_END", "CUSTOMER_ID", "CUSTOMER_DISPLAY", "RETAILER_ID", "RETAILER_NAME", "RETAILER_LOCATION_ID", "RETAILER_LOCATION_NAME", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY"))) {
        stop("Invalid value for sort_field when calling EventApi$SearchEventTransactions. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, REDEEMABLE_START, REDEEMABLE_END, CUSTOMER_ID, CUSTOMER_DISPLAY, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/event/attendance/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[EventAttendanceResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Events
    #'
    #' @param account_id The logged in user.
    #' @param keyword (optional) The keyword used to search
    #' @param active_only (optional) Return only active results
    #' @param category_ids (optional) the IDs of the categories that this event is associated with
    #' @param filter_ids (optional) the IDs of the filters that this event is associated with
    #' @param offer_audience_ids (optional) Offer audience ids to filter on
    #' @param transaction_audience_ids (optional) Transaction audience ids to filter on
    #' @param sort_field (optional) The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY
    #' @param descending (optional) The order to return the search results
    #' @param start_date (optional) Filter the events to return only those that start on or after the date
    #' @param end_date (optional) Filter the events to return only those that start on or before the date
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[OfferShortResponse]
    SearchEvents = function(account_id, keyword = NULL, active_only = NULL, category_ids = NULL, filter_ids = NULL, offer_audience_ids = NULL, transaction_audience_ids = NULL, sort_field = NULL, descending = NULL, start_date = NULL, end_date = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchEventsWithHttpInfo(account_id, keyword, active_only, category_ids, filter_ids, offer_audience_ids, transaction_audience_ids, sort_field, descending, start_date, end_date, start, limit, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Events
    #'
    #' @param account_id The logged in user.
    #' @param keyword (optional) The keyword used to search
    #' @param active_only (optional) Return only active results
    #' @param category_ids (optional) the IDs of the categories that this event is associated with
    #' @param filter_ids (optional) the IDs of the filters that this event is associated with
    #' @param offer_audience_ids (optional) Offer audience ids to filter on
    #' @param transaction_audience_ids (optional) Transaction audience ids to filter on
    #' @param sort_field (optional) The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, ACTIVATED, EXPIRES, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY
    #' @param descending (optional) The order to return the search results
    #' @param start_date (optional) Filter the events to return only those that start on or after the date
    #' @param end_date (optional) Filter the events to return only those that start on or before the date
    #' @param start (optional) The record to begin the return set on
    #' @param limit (optional) The number of records to return
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[OfferShortResponse]) with additional information such as HTTP status code, headers
    SearchEventsWithHttpInfo = function(account_id, keyword = NULL, active_only = NULL, category_ids = NULL, filter_ids = NULL, offer_audience_ids = NULL, transaction_audience_ids = NULL, sort_field = NULL, descending = NULL, start_date = NULL, end_date = NULL, start = NULL, limit = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EventApi$SearchEvents, `account_id` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling EventApi$SearchEvents, `keyword` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling EventApi$SearchEvents, `active_only` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling EventApi$SearchEvents, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling EventApi$SearchEvents, `filter_ids` is not nullable")
      }

      if (!missing(`offer_audience_ids`) && is.null(`offer_audience_ids`)) {
        stop("Invalid value for `offer_audience_ids` when calling EventApi$SearchEvents, `offer_audience_ids` is not nullable")
      }

      if (!missing(`transaction_audience_ids`) && is.null(`transaction_audience_ids`)) {
        stop("Invalid value for `transaction_audience_ids` when calling EventApi$SearchEvents, `transaction_audience_ids` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling EventApi$SearchEvents, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling EventApi$SearchEvents, `descending` is not nullable")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling EventApi$SearchEvents, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling EventApi$SearchEvents, `end_date` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling EventApi$SearchEvents, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling EventApi$SearchEvents, `limit` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["keyword"]] <- `keyword`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["offerAudienceIds"]] <- `offer_audience_ids`

      query_params[["transactionAudienceIds"]] <- `transaction_audience_ids`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "ACTIVATED", "EXPIRES", "REDEEMABLE_START", "REDEEMABLE_END", "TITLE", "SUBTITLE", "DETAILS", "OFFER_TYPE", "SPECIAL_OFFER_TYPE", "OFFER_VISIBILITY", "ESTIMATED_VALUE", "VOUCHER_PRICE", "VOUCHER_DISCOUNT_PRICE", "FULL_PRICE", "DICOUNT_PRICE", "TICKETS_REWARD", "AVAILABILITY_DATE", "RELEASE_DATE", "RETAILER_ID", "RETAILER_NAME", "RETAILER_LOCATION_ID", "RETAILER_LOCATION_NAME", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY"))) {
        stop("Invalid value for sort_field when calling EventApi$SearchEvents. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, ACTIVATED, EXPIRES, REDEEMABLE_START, REDEEMABLE_END, TITLE, SUBTITLE, DETAILS, OFFER_TYPE, SPECIAL_OFFER_TYPE, OFFER_VISIBILITY, ESTIMATED_VALUE, VOUCHER_PRICE, VOUCHER_DISCOUNT_PRICE, FULL_PRICE, DICOUNT_PRICE, TICKETS_REWARD, AVAILABILITY_DATE, RELEASE_DATE, RETAILER_ID, RETAILER_NAME, RETAILER_LOCATION_ID, RETAILER_LOCATION_NAME, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/event/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[OfferShortResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Event
    #'
    #' @param account_id The logged in user.
    #' @param event_id The id of the event to update
    #' @param retailer_location_ids (optional) The retailer location to have the event at
    #' @param title (optional) The event title
    #' @param sub_title (optional) The event sub title
    #' @param details (optional) The event details
    #' @param category_ids (optional) The categories the associate the event with
    #' @param filter_ids (optional) The filters the associate the event with
    #' @param active (optional) Is this event active
    #' @param image_asset_id (optional) The image to show for the event
    #' @param redeemable_start (optional) The event start date/time
    #' @param redeemable_end (optional) The event end date/time
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OfferResponse
    UpdateEvent = function(account_id, event_id, retailer_location_ids = NULL, title = NULL, sub_title = NULL, details = NULL, category_ids = NULL, filter_ids = NULL, active = NULL, image_asset_id = NULL, redeemable_start = NULL, redeemable_end = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateEventWithHttpInfo(account_id, event_id, retailer_location_ids, title, sub_title, details, category_ids, filter_ids, active, image_asset_id, redeemable_start, redeemable_end, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Event
    #'
    #' @param account_id The logged in user.
    #' @param event_id The id of the event to update
    #' @param retailer_location_ids (optional) The retailer location to have the event at
    #' @param title (optional) The event title
    #' @param sub_title (optional) The event sub title
    #' @param details (optional) The event details
    #' @param category_ids (optional) The categories the associate the event with
    #' @param filter_ids (optional) The filters the associate the event with
    #' @param active (optional) Is this event active
    #' @param image_asset_id (optional) The image to show for the event
    #' @param redeemable_start (optional) The event start date/time
    #' @param redeemable_end (optional) The event end date/time
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OfferResponse) with additional information such as HTTP status code, headers
    UpdateEventWithHttpInfo = function(account_id, event_id, retailer_location_ids = NULL, title = NULL, sub_title = NULL, details = NULL, category_ids = NULL, filter_ids = NULL, active = NULL, image_asset_id = NULL, redeemable_start = NULL, redeemable_end = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`event_id`)) {
        stop("Missing required parameter `event_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling EventApi$UpdateEvent, `account_id` is not nullable")
      }

      if (!missing(`event_id`) && is.null(`event_id`)) {
        stop("Invalid value for `event_id` when calling EventApi$UpdateEvent, `event_id` is not nullable")
      }

      if (!missing(`retailer_location_ids`) && is.null(`retailer_location_ids`)) {
        stop("Invalid value for `retailer_location_ids` when calling EventApi$UpdateEvent, `retailer_location_ids` is not nullable")
      }

      if (!missing(`title`) && is.null(`title`)) {
        stop("Invalid value for `title` when calling EventApi$UpdateEvent, `title` is not nullable")
      }

      if (!missing(`sub_title`) && is.null(`sub_title`)) {
        stop("Invalid value for `sub_title` when calling EventApi$UpdateEvent, `sub_title` is not nullable")
      }

      if (!missing(`details`) && is.null(`details`)) {
        stop("Invalid value for `details` when calling EventApi$UpdateEvent, `details` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling EventApi$UpdateEvent, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling EventApi$UpdateEvent, `filter_ids` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling EventApi$UpdateEvent, `active` is not nullable")
      }

      if (!missing(`image_asset_id`) && is.null(`image_asset_id`)) {
        stop("Invalid value for `image_asset_id` when calling EventApi$UpdateEvent, `image_asset_id` is not nullable")
      }

      if (!missing(`redeemable_start`) && is.null(`redeemable_start`)) {
        stop("Invalid value for `redeemable_start` when calling EventApi$UpdateEvent, `redeemable_start` is not nullable")
      }

      if (!missing(`redeemable_end`) && is.null(`redeemable_end`)) {
        stop("Invalid value for `redeemable_end` when calling EventApi$UpdateEvent, `redeemable_end` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["eventId"]] <- `event_id`

      query_params[["retailerLocationIds"]] <- `retailer_location_ids`

      query_params[["title"]] <- `title`

      query_params[["subTitle"]] <- `sub_title`

      query_params[["details"]] <- `details`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["active"]] <- `active`

      query_params[["imageAssetId"]] <- `image_asset_id`

      query_params[["redeemableStart"]] <- `redeemable_start`

      query_params[["redeemableEnd"]] <- `redeemable_end`

      local_var_url_path <- "/event/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OfferResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
