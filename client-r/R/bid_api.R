#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Bid operations
#' @description BidApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateBid  ####################
#'
#' library(openapi)
#' var_biddable_type <- "biddable_type_example" # character | A biddable object type. Possible values include: CREATIVE (ads).
#' var_biddable_id <- 56 # integer | The id of the biddable object
#' var_amount_per_view <- 3.4 # numeric | The bid amount for views. For ads, this is the amount that will be taken for each impression.
#' var_amount_per_action <- 3.4 # numeric | The bid amount for actions. For ads, this is the amount that will be taken for each click.
#' var_budget_amount <- 3.4 # numeric | The allocated budget amount that will be used
#' var_budget_schedule <- "budget_schedule_example" # character | The schedule for when the allocated budget amount is reset
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#'
#' #Create Bid
#' api_instance <- BidApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateBid(var_biddable_type, var_biddable_id, var_amount_per_view, var_amount_per_action, var_budget_amount, var_budget_schedule, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$CreateBid(var_biddable_type, var_biddable_id, var_amount_per_view, var_amount_per_action, var_budget_amount, var_budget_schedule, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  DeleteBid  ####################
#'
#' library(openapi)
#' var_bid_id <- 56 # integer | The bid id
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#'
#' #Delete Bid
#' api_instance <- BidApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteBid(var_bid_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$DeleteBid(var_bid_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  GetBid  ####################
#'
#' library(openapi)
#' var_bid_id <- 56 # integer | The bid id
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#'
#' #Get Bid
#' api_instance <- BidApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetBid(var_bid_id, device_id = var_device_id, account_id = var_account_iddata_file = "result.txt")
#' result <- api_instance$GetBid(var_bid_id, device_id = var_device_id, account_id = var_account_id)
#' dput(result)
#'
#'
#' ####################  UpdateBid  ####################
#'
#' library(openapi)
#' var_bid_id <- 56 # integer | The bid id
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_amount_per_view <- 3.4 # numeric | The bid amount for views. For ads, this is the amount that will be taken for each impression. (Optional)
#' var_amount_per_action <- 3.4 # numeric | The bid amount for actions. For ads, this is the amount that will be taken for each click. (Optional)
#' var_budget_amount <- 3.4 # numeric | The allocated budget amount that will be used (Optional)
#' var_budget_schedule <- "budget_schedule_example" # character | The schedule for when the allocated budget amount is reset (Optional)
#'
#' #Update Bid
#' api_instance <- BidApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateBid(var_bid_id, device_id = var_device_id, account_id = var_account_id, amount_per_view = var_amount_per_view, amount_per_action = var_amount_per_action, budget_amount = var_budget_amount, budget_schedule = var_budget_scheduledata_file = "result.txt")
#' result <- api_instance$UpdateBid(var_bid_id, device_id = var_device_id, account_id = var_account_id, amount_per_view = var_amount_per_view, amount_per_action = var_amount_per_action, budget_amount = var_budget_amount, budget_schedule = var_budget_schedule)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
BidApi <- R6::R6Class(
  "BidApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new BidApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Bid
    #'
    #' @param biddable_type A biddable object type. Possible values include: CREATIVE (ads).
    #' @param biddable_id The id of the biddable object
    #' @param amount_per_view The bid amount for views. For ads, this is the amount that will be taken for each impression.
    #' @param amount_per_action The bid amount for actions. For ads, this is the amount that will be taken for each click.
    #' @param budget_amount The allocated budget amount that will be used
    #' @param budget_schedule The schedule for when the allocated budget amount is reset
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BidResponse
    CreateBid = function(biddable_type, biddable_id, amount_per_view, amount_per_action, budget_amount, budget_schedule, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateBidWithHttpInfo(biddable_type, biddable_id, amount_per_view, amount_per_action, budget_amount, budget_schedule, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Bid
    #'
    #' @param biddable_type A biddable object type. Possible values include: CREATIVE (ads).
    #' @param biddable_id The id of the biddable object
    #' @param amount_per_view The bid amount for views. For ads, this is the amount that will be taken for each impression.
    #' @param amount_per_action The bid amount for actions. For ads, this is the amount that will be taken for each click.
    #' @param budget_amount The allocated budget amount that will be used
    #' @param budget_schedule The schedule for when the allocated budget amount is reset
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BidResponse) with additional information such as HTTP status code, headers
    CreateBidWithHttpInfo = function(biddable_type, biddable_id, amount_per_view, amount_per_action, budget_amount, budget_schedule, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`biddable_type`)) {
        stop("Missing required parameter `biddable_type`.")
      }

      if (missing(`biddable_id`)) {
        stop("Missing required parameter `biddable_id`.")
      }

      if (missing(`amount_per_view`)) {
        stop("Missing required parameter `amount_per_view`.")
      }

      if (missing(`amount_per_action`)) {
        stop("Missing required parameter `amount_per_action`.")
      }

      if (missing(`budget_amount`)) {
        stop("Missing required parameter `budget_amount`.")
      }

      if (missing(`budget_schedule`)) {
        stop("Missing required parameter `budget_schedule`.")
      }

      if (!missing(`biddable_type`) && is.null(`biddable_type`)) {
        stop("Invalid value for `biddable_type` when calling BidApi$CreateBid, `biddable_type` is not nullable")
      }

      if (!missing(`biddable_id`) && is.null(`biddable_id`)) {
        stop("Invalid value for `biddable_id` when calling BidApi$CreateBid, `biddable_id` is not nullable")
      }

      if (!missing(`amount_per_view`) && is.null(`amount_per_view`)) {
        stop("Invalid value for `amount_per_view` when calling BidApi$CreateBid, `amount_per_view` is not nullable")
      }

      if (!missing(`amount_per_action`) && is.null(`amount_per_action`)) {
        stop("Invalid value for `amount_per_action` when calling BidApi$CreateBid, `amount_per_action` is not nullable")
      }

      if (!missing(`budget_amount`) && is.null(`budget_amount`)) {
        stop("Invalid value for `budget_amount` when calling BidApi$CreateBid, `budget_amount` is not nullable")
      }

      if (!missing(`budget_schedule`) && is.null(`budget_schedule`)) {
        stop("Invalid value for `budget_schedule` when calling BidApi$CreateBid, `budget_schedule` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling BidApi$CreateBid, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BidApi$CreateBid, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["biddableType"]] <- `biddable_type`

      query_params[["biddableId"]] <- `biddable_id`

      query_params[["amountPerView"]] <- `amount_per_view`

      query_params[["amountPerAction"]] <- `amount_per_action`

      query_params[["budgetAmount"]] <- `budget_amount`

      query_params[["budgetSchedule"]] <- `budget_schedule`

      local_var_url_path <- "/bid/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BidResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Bid
    #'
    #' @param bid_id The bid id
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteBid = function(bid_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteBidWithHttpInfo(bid_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Bid
    #'
    #' @param bid_id The bid id
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteBidWithHttpInfo = function(bid_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`bid_id`)) {
        stop("Missing required parameter `bid_id`.")
      }

      if (!missing(`bid_id`) && is.null(`bid_id`)) {
        stop("Invalid value for `bid_id` when calling BidApi$DeleteBid, `bid_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling BidApi$DeleteBid, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BidApi$DeleteBid, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["bidId"]] <- `bid_id`

      local_var_url_path <- "/bid/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Bid
    #'
    #' @param bid_id The bid id
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BidResponse
    GetBid = function(bid_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetBidWithHttpInfo(bid_id, device_id, account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Bid
    #'
    #' @param bid_id The bid id
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BidResponse) with additional information such as HTTP status code, headers
    GetBidWithHttpInfo = function(bid_id, device_id = NULL, account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`bid_id`)) {
        stop("Missing required parameter `bid_id`.")
      }

      if (!missing(`bid_id`) && is.null(`bid_id`)) {
        stop("Invalid value for `bid_id` when calling BidApi$GetBid, `bid_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling BidApi$GetBid, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BidApi$GetBid, `account_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["bidId"]] <- `bid_id`

      local_var_url_path <- "/bid/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BidResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Bid
    #'
    #' @param bid_id The bid id
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param amount_per_view (optional) The bid amount for views. For ads, this is the amount that will be taken for each impression.
    #' @param amount_per_action (optional) The bid amount for actions. For ads, this is the amount that will be taken for each click.
    #' @param budget_amount (optional) The allocated budget amount that will be used
    #' @param budget_schedule (optional) The schedule for when the allocated budget amount is reset
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BidResponse
    UpdateBid = function(bid_id, device_id = NULL, account_id = NULL, amount_per_view = NULL, amount_per_action = NULL, budget_amount = NULL, budget_schedule = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateBidWithHttpInfo(bid_id, device_id, account_id, amount_per_view, amount_per_action, budget_amount, budget_schedule, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Bid
    #'
    #' @param bid_id The bid id
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param amount_per_view (optional) The bid amount for views. For ads, this is the amount that will be taken for each impression.
    #' @param amount_per_action (optional) The bid amount for actions. For ads, this is the amount that will be taken for each click.
    #' @param budget_amount (optional) The allocated budget amount that will be used
    #' @param budget_schedule (optional) The schedule for when the allocated budget amount is reset
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BidResponse) with additional information such as HTTP status code, headers
    UpdateBidWithHttpInfo = function(bid_id, device_id = NULL, account_id = NULL, amount_per_view = NULL, amount_per_action = NULL, budget_amount = NULL, budget_schedule = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`bid_id`)) {
        stop("Missing required parameter `bid_id`.")
      }

      if (!missing(`bid_id`) && is.null(`bid_id`)) {
        stop("Invalid value for `bid_id` when calling BidApi$UpdateBid, `bid_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling BidApi$UpdateBid, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling BidApi$UpdateBid, `account_id` is not nullable")
      }

      if (!missing(`amount_per_view`) && is.null(`amount_per_view`)) {
        stop("Invalid value for `amount_per_view` when calling BidApi$UpdateBid, `amount_per_view` is not nullable")
      }

      if (!missing(`amount_per_action`) && is.null(`amount_per_action`)) {
        stop("Invalid value for `amount_per_action` when calling BidApi$UpdateBid, `amount_per_action` is not nullable")
      }

      if (!missing(`budget_amount`) && is.null(`budget_amount`)) {
        stop("Invalid value for `budget_amount` when calling BidApi$UpdateBid, `budget_amount` is not nullable")
      }

      if (!missing(`budget_schedule`) && is.null(`budget_schedule`)) {
        stop("Invalid value for `budget_schedule` when calling BidApi$UpdateBid, `budget_schedule` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["bidId"]] <- `bid_id`

      query_params[["amountPerView"]] <- `amount_per_view`

      query_params[["amountPerAction"]] <- `amount_per_action`

      query_params[["budgetAmount"]] <- `budget_amount`

      query_params[["budgetSchedule"]] <- `budget_schedule`

      local_var_url_path <- "/bid/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BidResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
