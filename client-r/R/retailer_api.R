#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Retailer operations
#' @description RetailerApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateRetailer  ####################
#'
#' library(openapi)
#' var_name <- "name_example" # character | The name of the retailer
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_street_address <- "street_address_example" # character | The street address of the retailer (Optional)
#' var_street_address2 <- "street_address2_example" # character | Additional address information (such as a suite number, floor number, building name, or PO Box) (Optional)
#' var_city <- "city_example" # character | The city of the retailer (Optional)
#' var_state <- "state_example" # character | The state of the retailer (Optional)
#' var_postal_code <- "postal_code_example" # character | The postal code of the retailer (Optional)
#' var_country <- "country_example" # character | the country of the retailer (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone number of the retailer (Optional)
#' var_business_phone_ext <- "business_phone_ext_example" # character | The business phone extension of the retailer (Optional)
#' var_website <- "website_example" # character | The website of the retailer (Optional)
#' var_email <- "email_example" # character | The email of the retailer (Optional)
#' var_facebook_url <- "facebook_url_example" # character | The facebook URL of the retailer (Optional)
#' var_twitter_url <- "twitter_url_example" # character | The twitter URL of the retailer (Optional)
#' var_logo <- File.new('/path/to/file') # data.frame | The retailer logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before) (Optional)
#' var_logo_asset_id <- 56 # integer | The retailer logo asset id (Optional)
#' var_picture1 <- File.new('/path/to/file') # data.frame | Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before) (Optional)
#' var_picture1_asset_id <- 56 # integer | An asset id (Optional)
#' var_picture2 <- File.new('/path/to/file') # data.frame | Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before) (Optional)
#' var_picture2_asset_id <- 56 # integer | An asset id (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category IDs used to filter retailers by categories (Optional)
#' var_category_ids_to_add <- "category_ids_to_add_example" # character | Comma separated list of category IDs to add to the retailer (Optional)
#' var_category_ids_to_remove <- "category_ids_to_remove_example" # character | Comma separated list of category IDs to remove from the retailer (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Comma separated list of filter IDs used to filter retailers (Optional)
#' var_latitude <- 3.4 # numeric | the latitude of the retailer (Optional)
#' var_longitude <- 3.4 # numeric | the longitude of the retailer (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_search_tags <- "search_tags_example" # character | External custom search keywords (Optional)
#' var_retailer_type <- "retailer_type_example" # character | External custom type identifier (Optional)
#' var_visibility <- "visibility_example" # character |  (Optional)
#' var_create_default_location <- "create_default_location_example" # character | Determines whether to create a default location using the retailer information (Optional)
#' var_response_format <- "response_format_example" # character | The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets} (Optional)
#'
#' #Create Retailer
#' api_instance <- RetailerApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateRetailer(var_name, device_id = var_device_id, account_id = var_account_id, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, website = var_website, email = var_email, facebook_url = var_facebook_url, twitter_url = var_twitter_url, logo = var_logo, logo_asset_id = var_logo_asset_id, picture1 = var_picture1, picture1_asset_id = var_picture1_asset_id, picture2 = var_picture2, picture2_asset_id = var_picture2_asset_id, category_ids = var_category_ids, category_ids_to_add = var_category_ids_to_add, category_ids_to_remove = var_category_ids_to_remove, filter_ids = var_filter_ids, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, search_tags = var_search_tags, retailer_type = var_retailer_type, visibility = var_visibility, create_default_location = var_create_default_location, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$CreateRetailer(var_name, device_id = var_device_id, account_id = var_account_id, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, website = var_website, email = var_email, facebook_url = var_facebook_url, twitter_url = var_twitter_url, logo = var_logo, logo_asset_id = var_logo_asset_id, picture1 = var_picture1, picture1_asset_id = var_picture1_asset_id, picture2 = var_picture2, picture2_asset_id = var_picture2_asset_id, category_ids = var_category_ids, category_ids_to_add = var_category_ids_to_add, category_ids_to_remove = var_category_ids_to_remove, filter_ids = var_filter_ids, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, search_tags = var_search_tags, retailer_type = var_retailer_type, visibility = var_visibility, create_default_location = var_create_default_location, response_format = var_response_format)
#' dput(result)
#'
#'
#' ####################  DeleteRetailer  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account used to perform the delete, must have rights to edit the retailer. (Optional)
#' var_retailer_id <- 56 # integer | The ID of the retailer to be deleted (Optional)
#'
#' #Delete Retailer
#' api_instance <- RetailerApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteRetailer(device_id = var_device_id, account_id = var_account_id, retailer_id = var_retailer_iddata_file = "result.txt")
#' result <- api_instance$DeleteRetailer(device_id = var_device_id, account_id = var_account_id, retailer_id = var_retailer_id)
#' dput(result)
#'
#'
#' ####################  GetRetailer  ####################
#'
#' library(openapi)
#' var_retailer_id <- 56 # integer | the ID of the retailer
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required) (Optional)
#' var_include_counts <- "include_counts_example" # character | Determines whether to include counts in the response (default true) (Optional)
#'
#' #Get Retailer
#' api_instance <- RetailerApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRetailer(var_retailer_id, device_id = var_device_id, account_id = var_account_id, include_counts = var_include_countsdata_file = "result.txt")
#' result <- api_instance$GetRetailer(var_retailer_id, device_id = var_device_id, account_id = var_account_id, include_counts = var_include_counts)
#' dput(result)
#'
#'
#' ####################  GetRetailers  ####################
#'
#' library(openapi)
#' var_visibility <- "visibility_example" # character | 
#' var_sort_field <- "sort_field_example" # character | The column to sort the search on
#' var_descending <- "descending_example" # character | The order to return the search results
#' var_start <- 56 # integer | The record to begin the return set on
#' var_limit <- 56 # integer | The number of records to return
#' var_active_only <- "active_only_example" # character | Return only active results
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_q <- "q_example" # character | This parameter is deprecated. (Optional)
#' var_keyword <- "keyword_example" # character | The keyword used to search (Optional)
#' var_category_ids <- "category_ids_example" # character | the categories that the retailer is associated with (Optional)
#' var_filter_ids <- "filter_ids_example" # character | the filters that the retailer is associated with (Optional)
#' var__i <- 56 # integer | This parameter is deprecated. (Optional)
#' var__l <- 56 # integer | This parameter is deprecated. (Optional)
#'
#' #Search Retailers
#' api_instance <- RetailerApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRetailers(var_visibility, var_sort_field, var_descending, var_start, var_limit, var_active_only, device_id = var_device_id, account_id = var_account_id, q = var_q, keyword = var_keyword, category_ids = var_category_ids, filter_ids = var_filter_ids, _i = var__i, _l = var__ldata_file = "result.txt")
#' result <- api_instance$GetRetailers(var_visibility, var_sort_field, var_descending, var_start, var_limit, var_active_only, device_id = var_device_id, account_id = var_account_id, q = var_q, keyword = var_keyword, category_ids = var_category_ids, filter_ids = var_filter_ids, _i = var__i, _l = var__l)
#' dput(result)
#'
#'
#' ####################  RetailerLoginCheck  ####################
#'
#' library(openapi)
#' var_username <- "username_example" # character | the user's email address they used to sign-up
#' var_password <- "password_example" # character | the password
#' var_device_id <- "device_id_example" # character | the device id (optional) (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#'
#' #Login Retailer
#' api_instance <- RetailerApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RetailerLoginCheck(var_username, var_password, device_id = var_device_id, latitude = var_latitude, longitude = var_longitude, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$RetailerLoginCheck(var_username, var_password, device_id = var_device_id, latitude = var_latitude, longitude = var_longitude, app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  UpdateRetailer  ####################
#'
#' library(openapi)
#' var_retailer_id <- 56 # integer | The ID of the retailer to update
#' var_device_id <- "device_id_example" # character | The device id (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | The account id of the user (deviceId or accountId required) (Optional)
#' var_name <- "name_example" # character | The name of the retailer (Optional)
#' var_street_address <- "street_address_example" # character | The street address of the retailer (Optional)
#' var_street_address2 <- "street_address2_example" # character | Additional address information (such as a suite number, floor number, building name, or PO Box) (Optional)
#' var_city <- "city_example" # character | The city of the retailer (Optional)
#' var_state <- "state_example" # character | The state of the retailer (Optional)
#' var_postal_code <- "postal_code_example" # character | The postal code of the retailer (Optional)
#' var_country <- "country_example" # character | the country of the retailer (Optional)
#' var_business_phone <- "business_phone_example" # character | The business phone of the retailer (Optional)
#' var_business_phone_ext <- "business_phone_ext_example" # character | The business phone extension of the retailer (Optional)
#' var_website <- "website_example" # character | The website of the retailer (Optional)
#' var_email <- "email_example" # character | The email of the retailer (Optional)
#' var_facebook_url <- "facebook_url_example" # character | The facebook URL of the retailer (Optional)
#' var_twitter_url <- "twitter_url_example" # character | The twitter URL of the retailer (Optional)
#' var_logo <- File.new('/path/to/file') # data.frame | The retailer logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before) (Optional)
#' var_logo_asset_id <- 56 # integer | The retailer logo asset id (Optional)
#' var_picture1 <- File.new('/path/to/file') # data.frame | Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before) (Optional)
#' var_picture1_asset_id <- 56 # integer | An asset id (Optional)
#' var_picture2 <- File.new('/path/to/file') # data.frame | Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before) (Optional)
#' var_picture2_asset_id <- 56 # integer | An asset id (Optional)
#' var_category_ids <- "category_ids_example" # character | Comma separated list of category IDs used to filter retailers by categories (Optional)
#' var_filter_ids <- "filter_ids_example" # character | Comma separated list of filter IDs used to filter retailers (Optional)
#' var_latitude <- 3.4 # numeric | the latitude of the retailer (Optional)
#' var_longitude <- 3.4 # numeric | the longitude of the retailer (Optional)
#' var_meta_data <- "meta_data_example" # character | External custom client defined data (Optional)
#' var_search_tags <- "search_tags_example" # character | External custom search keywords (Optional)
#' var_retailer_type <- "retailer_type_example" # character | External custom type identifier (Optional)
#' var_visibility <- "visibility_example" # character |  (Optional)
#' var_active <- "active_example" # character | Sets whether the retailer is active or inactive (hidden from consumers) (Optional)
#' var_response_format <- "response_format_example" # character | The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets} (Optional)
#'
#' #Update Retailer
#' api_instance <- RetailerApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateRetailer(var_retailer_id, device_id = var_device_id, account_id = var_account_id, name = var_name, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, website = var_website, email = var_email, facebook_url = var_facebook_url, twitter_url = var_twitter_url, logo = var_logo, logo_asset_id = var_logo_asset_id, picture1 = var_picture1, picture1_asset_id = var_picture1_asset_id, picture2 = var_picture2, picture2_asset_id = var_picture2_asset_id, category_ids = var_category_ids, filter_ids = var_filter_ids, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, search_tags = var_search_tags, retailer_type = var_retailer_type, visibility = var_visibility, active = var_active, response_format = var_response_formatdata_file = "result.txt")
#' result <- api_instance$UpdateRetailer(var_retailer_id, device_id = var_device_id, account_id = var_account_id, name = var_name, street_address = var_street_address, street_address2 = var_street_address2, city = var_city, state = var_state, postal_code = var_postal_code, country = var_country, business_phone = var_business_phone, business_phone_ext = var_business_phone_ext, website = var_website, email = var_email, facebook_url = var_facebook_url, twitter_url = var_twitter_url, logo = var_logo, logo_asset_id = var_logo_asset_id, picture1 = var_picture1, picture1_asset_id = var_picture1_asset_id, picture2 = var_picture2, picture2_asset_id = var_picture2_asset_id, category_ids = var_category_ids, filter_ids = var_filter_ids, latitude = var_latitude, longitude = var_longitude, meta_data = var_meta_data, search_tags = var_search_tags, retailer_type = var_retailer_type, visibility = var_visibility, active = var_active, response_format = var_response_format)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
RetailerApi <- R6::R6Class(
  "RetailerApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new RetailerApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Retailer
    #'
    #' @param name The name of the retailer
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param street_address (optional) The street address of the retailer
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the retailer
    #' @param state (optional) The state of the retailer
    #' @param postal_code (optional) The postal code of the retailer
    #' @param country (optional) the country of the retailer
    #' @param business_phone (optional) The business phone number of the retailer
    #' @param business_phone_ext (optional) The business phone extension of the retailer
    #' @param website (optional) The website of the retailer
    #' @param email (optional) The email of the retailer
    #' @param facebook_url (optional) The facebook URL of the retailer
    #' @param twitter_url (optional) The twitter URL of the retailer
    #' @param logo (optional) The retailer logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before)
    #' @param logo_asset_id (optional) The retailer logo asset id
    #' @param picture1 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before)
    #' @param picture1_asset_id (optional) An asset id
    #' @param picture2 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before)
    #' @param picture2_asset_id (optional) An asset id
    #' @param category_ids (optional) Comma separated list of category IDs used to filter retailers by categories
    #' @param category_ids_to_add (optional) Comma separated list of category IDs to add to the retailer
    #' @param category_ids_to_remove (optional) Comma separated list of category IDs to remove from the retailer
    #' @param filter_ids (optional) Comma separated list of filter IDs used to filter retailers
    #' @param latitude (optional) the latitude of the retailer
    #' @param longitude (optional) the longitude of the retailer
    #' @param meta_data (optional) External custom client defined data
    #' @param search_tags (optional) External custom search keywords
    #' @param retailer_type (optional) External custom type identifier
    #' @param visibility (optional) 
    #' @param create_default_location (optional) Determines whether to create a default location using the retailer information
    #' @param response_format (optional) The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets}
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerFullResponse
    CreateRetailer = function(name, device_id = NULL, account_id = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, business_phone = NULL, business_phone_ext = NULL, website = NULL, email = NULL, facebook_url = NULL, twitter_url = NULL, logo = NULL, logo_asset_id = NULL, picture1 = NULL, picture1_asset_id = NULL, picture2 = NULL, picture2_asset_id = NULL, category_ids = NULL, category_ids_to_add = NULL, category_ids_to_remove = NULL, filter_ids = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, search_tags = NULL, retailer_type = NULL, visibility = NULL, create_default_location = NULL, response_format = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateRetailerWithHttpInfo(name, device_id, account_id, street_address, street_address2, city, state, postal_code, country, business_phone, business_phone_ext, website, email, facebook_url, twitter_url, logo, logo_asset_id, picture1, picture1_asset_id, picture2, picture2_asset_id, category_ids, category_ids_to_add, category_ids_to_remove, filter_ids, latitude, longitude, meta_data, search_tags, retailer_type, visibility, create_default_location, response_format, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Retailer
    #'
    #' @param name The name of the retailer
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param street_address (optional) The street address of the retailer
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the retailer
    #' @param state (optional) The state of the retailer
    #' @param postal_code (optional) The postal code of the retailer
    #' @param country (optional) the country of the retailer
    #' @param business_phone (optional) The business phone number of the retailer
    #' @param business_phone_ext (optional) The business phone extension of the retailer
    #' @param website (optional) The website of the retailer
    #' @param email (optional) The email of the retailer
    #' @param facebook_url (optional) The facebook URL of the retailer
    #' @param twitter_url (optional) The twitter URL of the retailer
    #' @param logo (optional) The retailer logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before)
    #' @param logo_asset_id (optional) The retailer logo asset id
    #' @param picture1 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before)
    #' @param picture1_asset_id (optional) An asset id
    #' @param picture2 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before)
    #' @param picture2_asset_id (optional) An asset id
    #' @param category_ids (optional) Comma separated list of category IDs used to filter retailers by categories
    #' @param category_ids_to_add (optional) Comma separated list of category IDs to add to the retailer
    #' @param category_ids_to_remove (optional) Comma separated list of category IDs to remove from the retailer
    #' @param filter_ids (optional) Comma separated list of filter IDs used to filter retailers
    #' @param latitude (optional) the latitude of the retailer
    #' @param longitude (optional) the longitude of the retailer
    #' @param meta_data (optional) External custom client defined data
    #' @param search_tags (optional) External custom search keywords
    #' @param retailer_type (optional) External custom type identifier
    #' @param visibility (optional) 
    #' @param create_default_location (optional) Determines whether to create a default location using the retailer information
    #' @param response_format (optional) The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets}
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerFullResponse) with additional information such as HTTP status code, headers
    CreateRetailerWithHttpInfo = function(name, device_id = NULL, account_id = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, business_phone = NULL, business_phone_ext = NULL, website = NULL, email = NULL, facebook_url = NULL, twitter_url = NULL, logo = NULL, logo_asset_id = NULL, picture1 = NULL, picture1_asset_id = NULL, picture2 = NULL, picture2_asset_id = NULL, category_ids = NULL, category_ids_to_add = NULL, category_ids_to_remove = NULL, filter_ids = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, search_tags = NULL, retailer_type = NULL, visibility = NULL, create_default_location = NULL, response_format = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling RetailerApi$CreateRetailer, `name` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerApi$CreateRetailer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerApi$CreateRetailer, `account_id` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling RetailerApi$CreateRetailer, `street_address` is not nullable")
      }

      if (!missing(`street_address2`) && is.null(`street_address2`)) {
        stop("Invalid value for `street_address2` when calling RetailerApi$CreateRetailer, `street_address2` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling RetailerApi$CreateRetailer, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling RetailerApi$CreateRetailer, `state` is not nullable")
      }

      if (!missing(`postal_code`) && is.null(`postal_code`)) {
        stop("Invalid value for `postal_code` when calling RetailerApi$CreateRetailer, `postal_code` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling RetailerApi$CreateRetailer, `country` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling RetailerApi$CreateRetailer, `business_phone` is not nullable")
      }

      if (!missing(`business_phone_ext`) && is.null(`business_phone_ext`)) {
        stop("Invalid value for `business_phone_ext` when calling RetailerApi$CreateRetailer, `business_phone_ext` is not nullable")
      }

      if (!missing(`website`) && is.null(`website`)) {
        stop("Invalid value for `website` when calling RetailerApi$CreateRetailer, `website` is not nullable")
      }

      if (!missing(`email`) && is.null(`email`)) {
        stop("Invalid value for `email` when calling RetailerApi$CreateRetailer, `email` is not nullable")
      }

      if (!missing(`facebook_url`) && is.null(`facebook_url`)) {
        stop("Invalid value for `facebook_url` when calling RetailerApi$CreateRetailer, `facebook_url` is not nullable")
      }

      if (!missing(`twitter_url`) && is.null(`twitter_url`)) {
        stop("Invalid value for `twitter_url` when calling RetailerApi$CreateRetailer, `twitter_url` is not nullable")
      }

      if (!missing(`logo`) && is.null(`logo`)) {
        stop("Invalid value for `logo` when calling RetailerApi$CreateRetailer, `logo` is not nullable")
      }

      if (!missing(`logo_asset_id`) && is.null(`logo_asset_id`)) {
        stop("Invalid value for `logo_asset_id` when calling RetailerApi$CreateRetailer, `logo_asset_id` is not nullable")
      }

      if (!missing(`picture1`) && is.null(`picture1`)) {
        stop("Invalid value for `picture1` when calling RetailerApi$CreateRetailer, `picture1` is not nullable")
      }

      if (!missing(`picture1_asset_id`) && is.null(`picture1_asset_id`)) {
        stop("Invalid value for `picture1_asset_id` when calling RetailerApi$CreateRetailer, `picture1_asset_id` is not nullable")
      }

      if (!missing(`picture2`) && is.null(`picture2`)) {
        stop("Invalid value for `picture2` when calling RetailerApi$CreateRetailer, `picture2` is not nullable")
      }

      if (!missing(`picture2_asset_id`) && is.null(`picture2_asset_id`)) {
        stop("Invalid value for `picture2_asset_id` when calling RetailerApi$CreateRetailer, `picture2_asset_id` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling RetailerApi$CreateRetailer, `category_ids` is not nullable")
      }

      if (!missing(`category_ids_to_add`) && is.null(`category_ids_to_add`)) {
        stop("Invalid value for `category_ids_to_add` when calling RetailerApi$CreateRetailer, `category_ids_to_add` is not nullable")
      }

      if (!missing(`category_ids_to_remove`) && is.null(`category_ids_to_remove`)) {
        stop("Invalid value for `category_ids_to_remove` when calling RetailerApi$CreateRetailer, `category_ids_to_remove` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling RetailerApi$CreateRetailer, `filter_ids` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RetailerApi$CreateRetailer, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RetailerApi$CreateRetailer, `longitude` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling RetailerApi$CreateRetailer, `meta_data` is not nullable")
      }

      if (!missing(`search_tags`) && is.null(`search_tags`)) {
        stop("Invalid value for `search_tags` when calling RetailerApi$CreateRetailer, `search_tags` is not nullable")
      }

      if (!missing(`retailer_type`) && is.null(`retailer_type`)) {
        stop("Invalid value for `retailer_type` when calling RetailerApi$CreateRetailer, `retailer_type` is not nullable")
      }

      if (!missing(`visibility`) && is.null(`visibility`)) {
        stop("Invalid value for `visibility` when calling RetailerApi$CreateRetailer, `visibility` is not nullable")
      }

      if (!missing(`create_default_location`) && is.null(`create_default_location`)) {
        stop("Invalid value for `create_default_location` when calling RetailerApi$CreateRetailer, `create_default_location` is not nullable")
      }

      if (!missing(`response_format`) && is.null(`response_format`)) {
        stop("Invalid value for `response_format` when calling RetailerApi$CreateRetailer, `response_format` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["name"]] <- `name`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["streetAddress2"]] <- `street_address2`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["postalCode"]] <- `postal_code`

      query_params[["country"]] <- `country`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["businessPhoneExt"]] <- `business_phone_ext`

      query_params[["website"]] <- `website`

      query_params[["email"]] <- `email`

      query_params[["facebookUrl"]] <- `facebook_url`

      query_params[["twitterUrl"]] <- `twitter_url`

      query_params[["logo"]] <- `logo`

      query_params[["logoAssetId"]] <- `logo_asset_id`

      query_params[["picture1"]] <- `picture1`

      query_params[["picture1AssetId"]] <- `picture1_asset_id`

      query_params[["picture2"]] <- `picture2`

      query_params[["picture2AssetId"]] <- `picture2_asset_id`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["categoryIdsToAdd"]] <- `category_ids_to_add`

      query_params[["categoryIdsToRemove"]] <- `category_ids_to_remove`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["metaData"]] <- `meta_data`

      query_params[["searchTags"]] <- `search_tags`

      query_params[["retailerType"]] <- `retailer_type`

      if (!is.null(`visibility`) && !(`visibility` %in% c("PUBLIC", "PRIVATE", "FRIENDS"))) {
        stop("Invalid value for visibility when calling RetailerApi$CreateRetailer. Must be [PUBLIC, PRIVATE, FRIENDS].")
      }
      query_params[["visibility"]] <- `visibility`

      query_params[["createDefaultLocation"]] <- `create_default_location`

      if (!is.null(`response_format`) && !(`response_format` %in% c("HTML", "XML", "JSON", "CSV"))) {
        stop("Invalid value for response_format when calling RetailerApi$CreateRetailer. Must be [HTML, XML, JSON, CSV].")
      }
      query_params[["responseFormat"]] <- `response_format`

      local_var_url_path <- "/retailer/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerFullResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Retailer
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the retailer.
    #' @param retailer_id (optional) The ID of the retailer to be deleted
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteRetailer = function(device_id = NULL, account_id = NULL, retailer_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteRetailerWithHttpInfo(device_id, account_id, retailer_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Retailer
    #'
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account used to perform the delete, must have rights to edit the retailer.
    #' @param retailer_id (optional) The ID of the retailer to be deleted
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteRetailerWithHttpInfo = function(device_id = NULL, account_id = NULL, retailer_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerApi$DeleteRetailer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerApi$DeleteRetailer, `account_id` is not nullable")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling RetailerApi$DeleteRetailer, `retailer_id` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["retailerId"]] <- `retailer_id`

      local_var_url_path <- "/retailer/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Retailer
    #'
    #' @param retailer_id the ID of the retailer
    #' @param device_id (optional) the device id (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param include_counts (optional) Determines whether to include counts in the response (default true)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerFullResponse
    GetRetailer = function(retailer_id, device_id = NULL, account_id = NULL, include_counts = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetRetailerWithHttpInfo(retailer_id, device_id, account_id, include_counts, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Retailer
    #'
    #' @param retailer_id the ID of the retailer
    #' @param device_id (optional) the device id (deviceId or accountId required)
    #' @param account_id (optional) the account id of the user (deviceId or accountId required)
    #' @param include_counts (optional) Determines whether to include counts in the response (default true)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerFullResponse) with additional information such as HTTP status code, headers
    GetRetailerWithHttpInfo = function(retailer_id, device_id = NULL, account_id = NULL, include_counts = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`retailer_id`)) {
        stop("Missing required parameter `retailer_id`.")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling RetailerApi$GetRetailer, `retailer_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerApi$GetRetailer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerApi$GetRetailer, `account_id` is not nullable")
      }

      if (!missing(`include_counts`) && is.null(`include_counts`)) {
        stop("Invalid value for `include_counts` when calling RetailerApi$GetRetailer, `include_counts` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["includeCounts"]] <- `include_counts`

      local_var_url_path <- "/retailer/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerFullResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Retailers
    #'
    #' @param visibility 
    #' @param sort_field The column to sort the search on
    #' @param descending The order to return the search results
    #' @param start The record to begin the return set on
    #' @param limit The number of records to return
    #' @param active_only Return only active results
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) The keyword used to search
    #' @param category_ids (optional) the categories that the retailer is associated with
    #' @param filter_ids (optional) the filters that the retailer is associated with
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[RetailerResponse]
    GetRetailers = function(visibility, sort_field, descending, start, limit, active_only, device_id = NULL, account_id = NULL, q = NULL, keyword = NULL, category_ids = NULL, filter_ids = NULL, _i = NULL, _l = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetRetailersWithHttpInfo(visibility, sort_field, descending, start, limit, active_only, device_id, account_id, q, keyword, category_ids, filter_ids, _i, _l, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Retailers
    #'
    #' @param visibility 
    #' @param sort_field The column to sort the search on
    #' @param descending The order to return the search results
    #' @param start The record to begin the return set on
    #' @param limit The number of records to return
    #' @param active_only Return only active results
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param q (optional) This parameter is deprecated.
    #' @param keyword (optional) The keyword used to search
    #' @param category_ids (optional) the categories that the retailer is associated with
    #' @param filter_ids (optional) the filters that the retailer is associated with
    #' @param _i (optional) This parameter is deprecated.
    #' @param _l (optional) This parameter is deprecated.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[RetailerResponse]) with additional information such as HTTP status code, headers
    GetRetailersWithHttpInfo = function(visibility, sort_field, descending, start, limit, active_only, device_id = NULL, account_id = NULL, q = NULL, keyword = NULL, category_ids = NULL, filter_ids = NULL, _i = NULL, _l = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`visibility`)) {
        stop("Missing required parameter `visibility`.")
      }

      if (missing(`sort_field`)) {
        stop("Missing required parameter `sort_field`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`start`)) {
        stop("Missing required parameter `start`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`active_only`)) {
        stop("Missing required parameter `active_only`.")
      }

      if (!missing(`visibility`) && is.null(`visibility`)) {
        stop("Invalid value for `visibility` when calling RetailerApi$GetRetailers, `visibility` is not nullable")
      }

      if (!missing(`sort_field`) && is.null(`sort_field`)) {
        stop("Invalid value for `sort_field` when calling RetailerApi$GetRetailers, `sort_field` is not nullable")
      }

      if (!missing(`descending`) && is.null(`descending`)) {
        stop("Invalid value for `descending` when calling RetailerApi$GetRetailers, `descending` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling RetailerApi$GetRetailers, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling RetailerApi$GetRetailers, `limit` is not nullable")
      }

      if (!missing(`active_only`) && is.null(`active_only`)) {
        stop("Invalid value for `active_only` when calling RetailerApi$GetRetailers, `active_only` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerApi$GetRetailers, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerApi$GetRetailers, `account_id` is not nullable")
      }

      if (!missing(`q`) && is.null(`q`)) {
        stop("Invalid value for `q` when calling RetailerApi$GetRetailers, `q` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling RetailerApi$GetRetailers, `keyword` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling RetailerApi$GetRetailers, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling RetailerApi$GetRetailers, `filter_ids` is not nullable")
      }

      if (!missing(`_i`) && is.null(`_i`)) {
        stop("Invalid value for `_i` when calling RetailerApi$GetRetailers, `_i` is not nullable")
      }

      if (!missing(`_l`) && is.null(`_l`)) {
        stop("Invalid value for `_l` when calling RetailerApi$GetRetailers, `_l` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["q"]] <- `q`

      query_params[["keyword"]] <- `keyword`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      if (!is.null(`visibility`) && !(`visibility` %in% c("PUBLIC", "PRIVATE", "FRIENDS"))) {
        stop("Invalid value for visibility when calling RetailerApi$GetRetailers. Must be [PUBLIC, PRIVATE, FRIENDS].")
      }
      query_params[["visibility"]] <- `visibility`

      if (!is.null(`sort_field`) && !(`sort_field` %in% c("ID", "CREATED", "UPDATED", "DELETED", "SEARCH_TAGS", "ACTIVE", "BILLABLE_ENTITY_ID", "BILLABLE_ENTITY_NAME", "RESPONSIBLE_DISPLAY", "ADDRESS_STREET", "ADDRESS_CITY", "ADDRESS_STATE", "ADDRESS_POSTAL_CODE", "ADDRESS_COUNTRY", "FILTERS", "CATEGORIES", "VISIBILITY", "NAME"))) {
        stop("Invalid value for sort_field when calling RetailerApi$GetRetailers. Must be [ID, CREATED, UPDATED, DELETED, SEARCH_TAGS, ACTIVE, BILLABLE_ENTITY_ID, BILLABLE_ENTITY_NAME, RESPONSIBLE_DISPLAY, ADDRESS_STREET, ADDRESS_CITY, ADDRESS_STATE, ADDRESS_POSTAL_CODE, ADDRESS_COUNTRY, FILTERS, CATEGORIES, VISIBILITY, NAME].")
      }
      query_params[["sortField"]] <- `sort_field`

      query_params[["descending"]] <- `descending`

      query_params[["_i"]] <- `_i`

      query_params[["start"]] <- `start`

      query_params[["_l"]] <- `_l`

      query_params[["limit"]] <- `limit`

      query_params[["activeOnly"]] <- `active_only`

      local_var_url_path <- "/retailer/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[RetailerResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Login Retailer
    #'
    #' @param username the user's email address they used to sign-up
    #' @param password the password
    #' @param device_id (optional) the device id (optional)
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param app_key (optional) the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AccountLoginResponse
    RetailerLoginCheck = function(username, password, device_id = NULL, latitude = NULL, longitude = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RetailerLoginCheckWithHttpInfo(username, password, device_id, latitude, longitude, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Login Retailer
    #'
    #' @param username the user's email address they used to sign-up
    #' @param password the password
    #' @param device_id (optional) the device id (optional)
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param app_key (optional) the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AccountLoginResponse) with additional information such as HTTP status code, headers
    RetailerLoginCheckWithHttpInfo = function(username, password, device_id = NULL, latitude = NULL, longitude = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`username`)) {
        stop("Missing required parameter `username`.")
      }

      if (missing(`password`)) {
        stop("Missing required parameter `password`.")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling RetailerApi$RetailerLoginCheck, `username` is not nullable")
      }

      if (!missing(`password`) && is.null(`password`)) {
        stop("Invalid value for `password` when calling RetailerApi$RetailerLoginCheck, `password` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerApi$RetailerLoginCheck, `device_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RetailerApi$RetailerLoginCheck, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RetailerApi$RetailerLoginCheck, `longitude` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling RetailerApi$RetailerLoginCheck, `app_key` is not nullable")
      }

      query_params[["username"]] <- `username`

      query_params[["password"]] <- `password`

      query_params[["deviceId"]] <- `device_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/retailer/login"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AccountLoginResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Retailer
    #'
    #' @param retailer_id The ID of the retailer to update
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param name (optional) The name of the retailer
    #' @param street_address (optional) The street address of the retailer
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the retailer
    #' @param state (optional) The state of the retailer
    #' @param postal_code (optional) The postal code of the retailer
    #' @param country (optional) the country of the retailer
    #' @param business_phone (optional) The business phone of the retailer
    #' @param business_phone_ext (optional) The business phone extension of the retailer
    #' @param website (optional) The website of the retailer
    #' @param email (optional) The email of the retailer
    #' @param facebook_url (optional) The facebook URL of the retailer
    #' @param twitter_url (optional) The twitter URL of the retailer
    #' @param logo (optional) The retailer logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before)
    #' @param logo_asset_id (optional) The retailer logo asset id
    #' @param picture1 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before)
    #' @param picture1_asset_id (optional) An asset id
    #' @param picture2 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before)
    #' @param picture2_asset_id (optional) An asset id
    #' @param category_ids (optional) Comma separated list of category IDs used to filter retailers by categories
    #' @param filter_ids (optional) Comma separated list of filter IDs used to filter retailers
    #' @param latitude (optional) the latitude of the retailer
    #' @param longitude (optional) the longitude of the retailer
    #' @param meta_data (optional) External custom client defined data
    #' @param search_tags (optional) External custom search keywords
    #' @param retailer_type (optional) External custom type identifier
    #' @param visibility (optional) 
    #' @param active (optional) Sets whether the retailer is active or inactive (hidden from consumers)
    #' @param response_format (optional) The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets}
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return RetailerFullResponse
    UpdateRetailer = function(retailer_id, device_id = NULL, account_id = NULL, name = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, business_phone = NULL, business_phone_ext = NULL, website = NULL, email = NULL, facebook_url = NULL, twitter_url = NULL, logo = NULL, logo_asset_id = NULL, picture1 = NULL, picture1_asset_id = NULL, picture2 = NULL, picture2_asset_id = NULL, category_ids = NULL, filter_ids = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, search_tags = NULL, retailer_type = NULL, visibility = NULL, active = NULL, response_format = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateRetailerWithHttpInfo(retailer_id, device_id, account_id, name, street_address, street_address2, city, state, postal_code, country, business_phone, business_phone_ext, website, email, facebook_url, twitter_url, logo, logo_asset_id, picture1, picture1_asset_id, picture2, picture2_asset_id, category_ids, filter_ids, latitude, longitude, meta_data, search_tags, retailer_type, visibility, active, response_format, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Retailer
    #'
    #' @param retailer_id The ID of the retailer to update
    #' @param device_id (optional) The device id (deviceId or accountId required)
    #' @param account_id (optional) The account id of the user (deviceId or accountId required)
    #' @param name (optional) The name of the retailer
    #' @param street_address (optional) The street address of the retailer
    #' @param street_address2 (optional) Additional address information (such as a suite number, floor number, building name, or PO Box)
    #' @param city (optional) The city of the retailer
    #' @param state (optional) The state of the retailer
    #' @param postal_code (optional) The postal code of the retailer
    #' @param country (optional) the country of the retailer
    #' @param business_phone (optional) The business phone of the retailer
    #' @param business_phone_ext (optional) The business phone extension of the retailer
    #' @param website (optional) The website of the retailer
    #' @param email (optional) The email of the retailer
    #' @param facebook_url (optional) The facebook URL of the retailer
    #' @param twitter_url (optional) The twitter URL of the retailer
    #' @param logo (optional) The retailer logo sent as a multipart binary file (you can optionally use logoAssetId if the asset has already been uploaded before)
    #' @param logo_asset_id (optional) The retailer logo asset id
    #' @param picture1 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture1AssetId if the asset has already been uploaded before)
    #' @param picture1_asset_id (optional) An asset id
    #' @param picture2 (optional) Optional store-front pictures or product pictures or etc. A multipart binary file (you can optionally use picture2AssetId if the asset has already been uploaded before)
    #' @param picture2_asset_id (optional) An asset id
    #' @param category_ids (optional) Comma separated list of category IDs used to filter retailers by categories
    #' @param filter_ids (optional) Comma separated list of filter IDs used to filter retailers
    #' @param latitude (optional) the latitude of the retailer
    #' @param longitude (optional) the longitude of the retailer
    #' @param meta_data (optional) External custom client defined data
    #' @param search_tags (optional) External custom search keywords
    #' @param retailer_type (optional) External custom type identifier
    #' @param visibility (optional) 
    #' @param active (optional) Sets whether the retailer is active or inactive (hidden from consumers)
    #' @param response_format (optional) The format of the returned response {JSON // default , HTML // for Dojo support when uploading assets}
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (RetailerFullResponse) with additional information such as HTTP status code, headers
    UpdateRetailerWithHttpInfo = function(retailer_id, device_id = NULL, account_id = NULL, name = NULL, street_address = NULL, street_address2 = NULL, city = NULL, state = NULL, postal_code = NULL, country = NULL, business_phone = NULL, business_phone_ext = NULL, website = NULL, email = NULL, facebook_url = NULL, twitter_url = NULL, logo = NULL, logo_asset_id = NULL, picture1 = NULL, picture1_asset_id = NULL, picture2 = NULL, picture2_asset_id = NULL, category_ids = NULL, filter_ids = NULL, latitude = NULL, longitude = NULL, meta_data = NULL, search_tags = NULL, retailer_type = NULL, visibility = NULL, active = NULL, response_format = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`retailer_id`)) {
        stop("Missing required parameter `retailer_id`.")
      }

      if (!missing(`retailer_id`) && is.null(`retailer_id`)) {
        stop("Invalid value for `retailer_id` when calling RetailerApi$UpdateRetailer, `retailer_id` is not nullable")
      }

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling RetailerApi$UpdateRetailer, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling RetailerApi$UpdateRetailer, `account_id` is not nullable")
      }

      if (!missing(`name`) && is.null(`name`)) {
        stop("Invalid value for `name` when calling RetailerApi$UpdateRetailer, `name` is not nullable")
      }

      if (!missing(`street_address`) && is.null(`street_address`)) {
        stop("Invalid value for `street_address` when calling RetailerApi$UpdateRetailer, `street_address` is not nullable")
      }

      if (!missing(`street_address2`) && is.null(`street_address2`)) {
        stop("Invalid value for `street_address2` when calling RetailerApi$UpdateRetailer, `street_address2` is not nullable")
      }

      if (!missing(`city`) && is.null(`city`)) {
        stop("Invalid value for `city` when calling RetailerApi$UpdateRetailer, `city` is not nullable")
      }

      if (!missing(`state`) && is.null(`state`)) {
        stop("Invalid value for `state` when calling RetailerApi$UpdateRetailer, `state` is not nullable")
      }

      if (!missing(`postal_code`) && is.null(`postal_code`)) {
        stop("Invalid value for `postal_code` when calling RetailerApi$UpdateRetailer, `postal_code` is not nullable")
      }

      if (!missing(`country`) && is.null(`country`)) {
        stop("Invalid value for `country` when calling RetailerApi$UpdateRetailer, `country` is not nullable")
      }

      if (!missing(`business_phone`) && is.null(`business_phone`)) {
        stop("Invalid value for `business_phone` when calling RetailerApi$UpdateRetailer, `business_phone` is not nullable")
      }

      if (!missing(`business_phone_ext`) && is.null(`business_phone_ext`)) {
        stop("Invalid value for `business_phone_ext` when calling RetailerApi$UpdateRetailer, `business_phone_ext` is not nullable")
      }

      if (!missing(`website`) && is.null(`website`)) {
        stop("Invalid value for `website` when calling RetailerApi$UpdateRetailer, `website` is not nullable")
      }

      if (!missing(`email`) && is.null(`email`)) {
        stop("Invalid value for `email` when calling RetailerApi$UpdateRetailer, `email` is not nullable")
      }

      if (!missing(`facebook_url`) && is.null(`facebook_url`)) {
        stop("Invalid value for `facebook_url` when calling RetailerApi$UpdateRetailer, `facebook_url` is not nullable")
      }

      if (!missing(`twitter_url`) && is.null(`twitter_url`)) {
        stop("Invalid value for `twitter_url` when calling RetailerApi$UpdateRetailer, `twitter_url` is not nullable")
      }

      if (!missing(`logo`) && is.null(`logo`)) {
        stop("Invalid value for `logo` when calling RetailerApi$UpdateRetailer, `logo` is not nullable")
      }

      if (!missing(`logo_asset_id`) && is.null(`logo_asset_id`)) {
        stop("Invalid value for `logo_asset_id` when calling RetailerApi$UpdateRetailer, `logo_asset_id` is not nullable")
      }

      if (!missing(`picture1`) && is.null(`picture1`)) {
        stop("Invalid value for `picture1` when calling RetailerApi$UpdateRetailer, `picture1` is not nullable")
      }

      if (!missing(`picture1_asset_id`) && is.null(`picture1_asset_id`)) {
        stop("Invalid value for `picture1_asset_id` when calling RetailerApi$UpdateRetailer, `picture1_asset_id` is not nullable")
      }

      if (!missing(`picture2`) && is.null(`picture2`)) {
        stop("Invalid value for `picture2` when calling RetailerApi$UpdateRetailer, `picture2` is not nullable")
      }

      if (!missing(`picture2_asset_id`) && is.null(`picture2_asset_id`)) {
        stop("Invalid value for `picture2_asset_id` when calling RetailerApi$UpdateRetailer, `picture2_asset_id` is not nullable")
      }

      if (!missing(`category_ids`) && is.null(`category_ids`)) {
        stop("Invalid value for `category_ids` when calling RetailerApi$UpdateRetailer, `category_ids` is not nullable")
      }

      if (!missing(`filter_ids`) && is.null(`filter_ids`)) {
        stop("Invalid value for `filter_ids` when calling RetailerApi$UpdateRetailer, `filter_ids` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling RetailerApi$UpdateRetailer, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling RetailerApi$UpdateRetailer, `longitude` is not nullable")
      }

      if (!missing(`meta_data`) && is.null(`meta_data`)) {
        stop("Invalid value for `meta_data` when calling RetailerApi$UpdateRetailer, `meta_data` is not nullable")
      }

      if (!missing(`search_tags`) && is.null(`search_tags`)) {
        stop("Invalid value for `search_tags` when calling RetailerApi$UpdateRetailer, `search_tags` is not nullable")
      }

      if (!missing(`retailer_type`) && is.null(`retailer_type`)) {
        stop("Invalid value for `retailer_type` when calling RetailerApi$UpdateRetailer, `retailer_type` is not nullable")
      }

      if (!missing(`visibility`) && is.null(`visibility`)) {
        stop("Invalid value for `visibility` when calling RetailerApi$UpdateRetailer, `visibility` is not nullable")
      }

      if (!missing(`active`) && is.null(`active`)) {
        stop("Invalid value for `active` when calling RetailerApi$UpdateRetailer, `active` is not nullable")
      }

      if (!missing(`response_format`) && is.null(`response_format`)) {
        stop("Invalid value for `response_format` when calling RetailerApi$UpdateRetailer, `response_format` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["retailerId"]] <- `retailer_id`

      query_params[["name"]] <- `name`

      query_params[["streetAddress"]] <- `street_address`

      query_params[["streetAddress2"]] <- `street_address2`

      query_params[["city"]] <- `city`

      query_params[["state"]] <- `state`

      query_params[["postalCode"]] <- `postal_code`

      query_params[["country"]] <- `country`

      query_params[["businessPhone"]] <- `business_phone`

      query_params[["businessPhoneExt"]] <- `business_phone_ext`

      query_params[["website"]] <- `website`

      query_params[["email"]] <- `email`

      query_params[["facebookUrl"]] <- `facebook_url`

      query_params[["twitterUrl"]] <- `twitter_url`

      query_params[["logo"]] <- `logo`

      query_params[["logoAssetId"]] <- `logo_asset_id`

      query_params[["picture1"]] <- `picture1`

      query_params[["picture1AssetId"]] <- `picture1_asset_id`

      query_params[["picture2"]] <- `picture2`

      query_params[["picture2AssetId"]] <- `picture2_asset_id`

      query_params[["categoryIds"]] <- `category_ids`

      query_params[["filterIds"]] <- `filter_ids`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      query_params[["metaData"]] <- `meta_data`

      query_params[["searchTags"]] <- `search_tags`

      query_params[["retailerType"]] <- `retailer_type`

      if (!is.null(`visibility`) && !(`visibility` %in% c("PUBLIC", "PRIVATE", "FRIENDS"))) {
        stop("Invalid value for visibility when calling RetailerApi$UpdateRetailer. Must be [PUBLIC, PRIVATE, FRIENDS].")
      }
      query_params[["visibility"]] <- `visibility`

      query_params[["active"]] <- `active`

      if (!is.null(`response_format`) && !(`response_format` %in% c("HTML", "XML", "JSON", "CSV"))) {
        stop("Invalid value for response_format when calling RetailerApi$UpdateRetailer. Must be [HTML, XML, JSON, CSV].")
      }
      query_params[["responseFormat"]] <- `response_format`

      local_var_url_path <- "/retailer/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "RetailerFullResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
