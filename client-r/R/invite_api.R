#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Invite operations
#' @description InviteApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AcceptInvite  ####################
#'
#' library(openapi)
#' var_token <- "token_example" # character | the invite token
#' var_account_id <- 56 # integer | the accountId of the user who is accepting the invite
#' var_album_id <- 56 # integer | the album id associated with this invite (if applicable) (Optional)
#' var_mission_id <- 56 # integer | the mission id associated with this invite (if applicable) (Optional)
#' var_album_contest_id <- 56 # integer | the album contest id associated with this invite (if applicable) (Optional)
#' var_offer_id <- 56 # integer | the offer id associated with this invite (if applicable) (Optional)
#' var_offer_location_id <- 56 # integer | the offer location id associated with this invite (if applicable) (Optional)
#' var_retailer_location_id <- 56 # integer | the retailer location id associated with this invite (if applicable) (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_auto_friend <- TRUE # character | whether to auto-friend the invite sender and receiver (Optional)
#' var_auto_attend_event <- FALSE # character | whether to mark the event as attending automatically after invite is accepted (Optional)
#' var_auto_favorite_offer <- FALSE # character | whether to mark the offer as favorited automatically after invite is accepted (Optional)
#' var_auto_favorite_offer_location <- FALSE # character | whether to mark the offer location as favorited automatically after invite is accepted (Optional)
#' var_auto_favorite_retailer_location <- FALSE # character | whether to mark the retailer location as favorited automatically after invite is accepted (Optional)
#'
#' #Accept Invite
#' api_instance <- InviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AcceptInvite(var_token, var_account_id, album_id = var_album_id, mission_id = var_mission_id, album_contest_id = var_album_contest_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, retailer_location_id = var_retailer_location_id, app_key = var_app_key, auto_friend = var_auto_friend, auto_attend_event = var_auto_attend_event, auto_favorite_offer = var_auto_favorite_offer, auto_favorite_offer_location = var_auto_favorite_offer_location, auto_favorite_retailer_location = var_auto_favorite_retailer_locationdata_file = "result.txt")
#' result <- api_instance$AcceptInvite(var_token, var_account_id, album_id = var_album_id, mission_id = var_mission_id, album_contest_id = var_album_contest_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, retailer_location_id = var_retailer_location_id, app_key = var_app_key, auto_friend = var_auto_friend, auto_attend_event = var_auto_attend_event, auto_favorite_offer = var_auto_favorite_offer, auto_favorite_offer_location = var_auto_favorite_offer_location, auto_favorite_retailer_location = var_auto_favorite_retailer_location)
#' dput(result)
#'
#'
#' ####################  AlbumContestInvite  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_app_id <- 56 # integer | This parameter is deprecated. (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_album_contest_id <- 56 # integer | the album contest to share (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#'
#' #Invite to Contest
#' api_instance <- InviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AlbumContestInvite(device_id = var_device_id, account_id = var_account_id, app_id = var_app_id, app_key = var_app_key, album_contest_id = var_album_contest_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$AlbumContestInvite(device_id = var_device_id, account_id = var_account_id, app_id = var_app_id, app_key = var_app_key, album_contest_id = var_album_contest_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  AlbumInvite  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_app_id <- 56 # integer | This parameter is deprecated. (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_album_id <- 56 # integer | the album to share (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#'
#' #Invite to Collection
#' api_instance <- InviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AlbumInvite(device_id = var_device_id, account_id = var_account_id, app_id = var_app_id, app_key = var_app_key, album_id = var_album_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$AlbumInvite(device_id = var_device_id, account_id = var_account_id, app_id = var_app_id, app_key = var_app_key, album_id = var_album_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  EventInvite  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account ID of the user making the share
#' var_app_key <- "app_key_example" # character | the application key
#' var_listing_id <- 56 # integer | The ID of the event listing
#' var_receiver_account_ids <- "receiver_account_ids_example" # character | the account ID of a Sirqul user they would like to share an event with (Optional)
#' var_retailer_location_id <- 56 # integer | The retailer location id of where the event will take place (Optional)
#'
#' #Invite to Event
#' api_instance <- InviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$EventInvite(var_account_id, var_app_key, var_listing_id, receiver_account_ids = var_receiver_account_ids, retailer_location_id = var_retailer_location_iddata_file = "result.txt")
#' result <- api_instance$EventInvite(var_account_id, var_app_key, var_listing_id, receiver_account_ids = var_receiver_account_ids, retailer_location_id = var_retailer_location_id)
#' dput(result)
#'
#'
#' ####################  GameInvite  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_app_id <- 56 # integer | This parameter is deprecated. (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_game_level_id <- 56 # integer | the game level that the user owns and is giving access to (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#'
#' #Invite to Game Level
#' api_instance <- InviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GameInvite(device_id = var_device_id, account_id = var_account_id, app_id = var_app_id, app_key = var_app_key, game_level_id = var_game_level_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$GameInvite(device_id = var_device_id, account_id = var_account_id, app_id = var_app_id, app_key = var_app_key, game_level_id = var_game_level_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  GetInvite  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | Account ID of the user if they are logged in (Optional)
#' var_token <- "token_example" # character | the invite token (Optional)
#' var_album_id <- 56 # integer | album id to match the invite against (if applicable) (Optional)
#' var_mission_id <- 56 # integer | mission id to match the invite against (if applicable) (Optional)
#' var_album_contest_id <- 56 # integer | album contest id to match the invite against (if applicable) (Optional)
#' var_offer_id <- 56 # integer | offer id to match the invite against (if applicable) (Optional)
#' var_offer_location_id <- 56 # integer | offer location id to match the invite against (if applicable) (Optional)
#' var_retailer_location_id <- 56 # integer | retailer location id to match the invite against (if applicable) (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#'
#' #Get Invite
#' api_instance <- InviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetInvite(account_id = var_account_id, token = var_token, album_id = var_album_id, mission_id = var_mission_id, album_contest_id = var_album_contest_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, retailer_location_id = var_retailer_location_id, app_key = var_app_keydata_file = "result.txt")
#' result <- api_instance$GetInvite(account_id = var_account_id, token = var_token, album_id = var_album_id, mission_id = var_mission_id, album_contest_id = var_album_contest_id, offer_id = var_offer_id, offer_location_id = var_offer_location_id, retailer_location_id = var_retailer_location_id, app_key = var_app_key)
#' dput(result)
#'
#'
#' ####################  MissionInvite  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | a unique ID given by the device (deviceId or accountId required) (Optional)
#' var_account_id <- 56 # integer | the account ID of the user (deviceId or accountId required) (Optional)
#' var_app_id <- 56 # integer | This parameter is deprecated. (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_mission_id <- 56 # integer | the mission to share (Optional)
#' var_latitude <- 3.4 # numeric | the current latitude of the user (Optional)
#' var_longitude <- 3.4 # numeric | the current longitude of the user (Optional)
#'
#' #Invite to Mission
#' api_instance <- InviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MissionInvite(device_id = var_device_id, account_id = var_account_id, app_id = var_app_id, app_key = var_app_key, mission_id = var_mission_id, latitude = var_latitude, longitude = var_longitudedata_file = "result.txt")
#' result <- api_instance$MissionInvite(device_id = var_device_id, account_id = var_account_id, app_id = var_app_id, app_key = var_app_key, mission_id = var_mission_id, latitude = var_latitude, longitude = var_longitude)
#' dput(result)
#'
#'
#' ####################  OfferInvite  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account ID of the user making the share
#' var_app_key <- "app_key_example" # character | the application key
#' var_offer_id <- 56 # integer | the ID of the offer used to invite to favorite
#'
#' #Invite to Offer
#' api_instance <- InviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OfferInvite(var_account_id, var_app_key, var_offer_iddata_file = "result.txt")
#' result <- api_instance$OfferInvite(var_account_id, var_app_key, var_offer_id)
#' dput(result)
#'
#'
#' ####################  OfferLocationInvite  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account ID of the user making the share
#' var_app_key <- "app_key_example" # character | the application key
#' var_offer_location_id <- 56 # integer | the id of the offer location to share
#'
#' #Invite to Offer Location
#' api_instance <- InviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OfferLocationInvite(var_account_id, var_app_key, var_offer_location_iddata_file = "result.txt")
#' result <- api_instance$OfferLocationInvite(var_account_id, var_app_key, var_offer_location_id)
#' dput(result)
#'
#'
#' ####################  RetailerLocationInvite  ####################
#'
#' library(openapi)
#' var_account_id <- 56 # integer | the account ID of the user making the share
#' var_app_key <- "app_key_example" # character | the application key
#' var_retailer_location_id <- 56 # integer | The retailer location id of where the event will take place
#' var_album_id <- 56 # integer | Optional album id to link with the invite (Optional)
#'
#' #Invite to Retailer Location
#' api_instance <- InviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RetailerLocationInvite(var_account_id, var_app_key, var_retailer_location_id, album_id = var_album_iddata_file = "result.txt")
#' result <- api_instance$RetailerLocationInvite(var_account_id, var_app_key, var_retailer_location_id, album_id = var_album_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
InviteApi <- R6::R6Class(
  "InviteApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new InviteApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Accept Invite
    #'
    #' @param token the invite token
    #' @param account_id the accountId of the user who is accepting the invite
    #' @param album_id (optional) the album id associated with this invite (if applicable)
    #' @param mission_id (optional) the mission id associated with this invite (if applicable)
    #' @param album_contest_id (optional) the album contest id associated with this invite (if applicable)
    #' @param offer_id (optional) the offer id associated with this invite (if applicable)
    #' @param offer_location_id (optional) the offer location id associated with this invite (if applicable)
    #' @param retailer_location_id (optional) the retailer location id associated with this invite (if applicable)
    #' @param app_key (optional) the application key
    #' @param auto_friend (optional) whether to auto-friend the invite sender and receiver (default value: TRUE)
    #' @param auto_attend_event (optional) whether to mark the event as attending automatically after invite is accepted (default value: FALSE)
    #' @param auto_favorite_offer (optional) whether to mark the offer as favorited automatically after invite is accepted (default value: FALSE)
    #' @param auto_favorite_offer_location (optional) whether to mark the offer location as favorited automatically after invite is accepted (default value: FALSE)
    #' @param auto_favorite_retailer_location (optional) whether to mark the retailer location as favorited automatically after invite is accepted (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ConsumerInviteResponse
    AcceptInvite = function(token, account_id, album_id = NULL, mission_id = NULL, album_contest_id = NULL, offer_id = NULL, offer_location_id = NULL, retailer_location_id = NULL, app_key = NULL, auto_friend = TRUE, auto_attend_event = FALSE, auto_favorite_offer = FALSE, auto_favorite_offer_location = FALSE, auto_favorite_retailer_location = FALSE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AcceptInviteWithHttpInfo(token, account_id, album_id, mission_id, album_contest_id, offer_id, offer_location_id, retailer_location_id, app_key, auto_friend, auto_attend_event, auto_favorite_offer, auto_favorite_offer_location, auto_favorite_retailer_location, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Accept Invite
    #'
    #' @param token the invite token
    #' @param account_id the accountId of the user who is accepting the invite
    #' @param album_id (optional) the album id associated with this invite (if applicable)
    #' @param mission_id (optional) the mission id associated with this invite (if applicable)
    #' @param album_contest_id (optional) the album contest id associated with this invite (if applicable)
    #' @param offer_id (optional) the offer id associated with this invite (if applicable)
    #' @param offer_location_id (optional) the offer location id associated with this invite (if applicable)
    #' @param retailer_location_id (optional) the retailer location id associated with this invite (if applicable)
    #' @param app_key (optional) the application key
    #' @param auto_friend (optional) whether to auto-friend the invite sender and receiver (default value: TRUE)
    #' @param auto_attend_event (optional) whether to mark the event as attending automatically after invite is accepted (default value: FALSE)
    #' @param auto_favorite_offer (optional) whether to mark the offer as favorited automatically after invite is accepted (default value: FALSE)
    #' @param auto_favorite_offer_location (optional) whether to mark the offer location as favorited automatically after invite is accepted (default value: FALSE)
    #' @param auto_favorite_retailer_location (optional) whether to mark the retailer location as favorited automatically after invite is accepted (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ConsumerInviteResponse) with additional information such as HTTP status code, headers
    AcceptInviteWithHttpInfo = function(token, account_id, album_id = NULL, mission_id = NULL, album_contest_id = NULL, offer_id = NULL, offer_location_id = NULL, retailer_location_id = NULL, app_key = NULL, auto_friend = TRUE, auto_attend_event = FALSE, auto_favorite_offer = FALSE, auto_favorite_offer_location = FALSE, auto_favorite_retailer_location = FALSE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`token`)) {
        stop("Missing required parameter `token`.")
      }

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (!missing(`token`) && is.null(`token`)) {
        stop("Invalid value for `token` when calling InviteApi$AcceptInvite, `token` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling InviteApi$AcceptInvite, `account_id` is not nullable")
      }

      if (!missing(`album_id`) && is.null(`album_id`)) {
        stop("Invalid value for `album_id` when calling InviteApi$AcceptInvite, `album_id` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling InviteApi$AcceptInvite, `mission_id` is not nullable")
      }

      if (!missing(`album_contest_id`) && is.null(`album_contest_id`)) {
        stop("Invalid value for `album_contest_id` when calling InviteApi$AcceptInvite, `album_contest_id` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling InviteApi$AcceptInvite, `offer_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling InviteApi$AcceptInvite, `offer_location_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling InviteApi$AcceptInvite, `retailer_location_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling InviteApi$AcceptInvite, `app_key` is not nullable")
      }

      if (!missing(`auto_friend`) && is.null(`auto_friend`)) {
        stop("Invalid value for `auto_friend` when calling InviteApi$AcceptInvite, `auto_friend` is not nullable")
      }

      if (!missing(`auto_attend_event`) && is.null(`auto_attend_event`)) {
        stop("Invalid value for `auto_attend_event` when calling InviteApi$AcceptInvite, `auto_attend_event` is not nullable")
      }

      if (!missing(`auto_favorite_offer`) && is.null(`auto_favorite_offer`)) {
        stop("Invalid value for `auto_favorite_offer` when calling InviteApi$AcceptInvite, `auto_favorite_offer` is not nullable")
      }

      if (!missing(`auto_favorite_offer_location`) && is.null(`auto_favorite_offer_location`)) {
        stop("Invalid value for `auto_favorite_offer_location` when calling InviteApi$AcceptInvite, `auto_favorite_offer_location` is not nullable")
      }

      if (!missing(`auto_favorite_retailer_location`) && is.null(`auto_favorite_retailer_location`)) {
        stop("Invalid value for `auto_favorite_retailer_location` when calling InviteApi$AcceptInvite, `auto_favorite_retailer_location` is not nullable")
      }

      query_params[["token"]] <- `token`

      query_params[["accountId"]] <- `account_id`

      query_params[["albumId"]] <- `album_id`

      query_params[["missionId"]] <- `mission_id`

      query_params[["albumContestId"]] <- `album_contest_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["autoFriend"]] <- `auto_friend`

      query_params[["autoAttendEvent"]] <- `auto_attend_event`

      query_params[["autoFavoriteOffer"]] <- `auto_favorite_offer`

      query_params[["autoFavoriteOfferLocation"]] <- `auto_favorite_offer_location`

      query_params[["autoFavoriteRetailerLocation"]] <- `auto_favorite_retailer_location`

      local_var_url_path <- "/invite/accept"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ConsumerInviteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Invite to Contest
    #'
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param app_id (optional) This parameter is deprecated.
    #' @param app_key (optional) the application key
    #' @param album_contest_id (optional) the album contest to share
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return InviteResponse
    AlbumContestInvite = function(device_id = NULL, account_id = NULL, app_id = NULL, app_key = NULL, album_contest_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AlbumContestInviteWithHttpInfo(device_id, account_id, app_id, app_key, album_contest_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Invite to Contest
    #'
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param app_id (optional) This parameter is deprecated.
    #' @param app_key (optional) the application key
    #' @param album_contest_id (optional) the album contest to share
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (InviteResponse) with additional information such as HTTP status code, headers
    AlbumContestInviteWithHttpInfo = function(device_id = NULL, account_id = NULL, app_id = NULL, app_key = NULL, album_contest_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling InviteApi$AlbumContestInvite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling InviteApi$AlbumContestInvite, `account_id` is not nullable")
      }

      if (!missing(`app_id`) && is.null(`app_id`)) {
        stop("Invalid value for `app_id` when calling InviteApi$AlbumContestInvite, `app_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling InviteApi$AlbumContestInvite, `app_key` is not nullable")
      }

      if (!missing(`album_contest_id`) && is.null(`album_contest_id`)) {
        stop("Invalid value for `album_contest_id` when calling InviteApi$AlbumContestInvite, `album_contest_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling InviteApi$AlbumContestInvite, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling InviteApi$AlbumContestInvite, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appId"]] <- `app_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["albumContestId"]] <- `album_contest_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/invite/albumContest"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "InviteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Invite to Collection
    #'
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param app_id (optional) This parameter is deprecated.
    #' @param app_key (optional) the application key
    #' @param album_id (optional) the album to share
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return InviteResponse
    AlbumInvite = function(device_id = NULL, account_id = NULL, app_id = NULL, app_key = NULL, album_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AlbumInviteWithHttpInfo(device_id, account_id, app_id, app_key, album_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Invite to Collection
    #'
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param app_id (optional) This parameter is deprecated.
    #' @param app_key (optional) the application key
    #' @param album_id (optional) the album to share
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (InviteResponse) with additional information such as HTTP status code, headers
    AlbumInviteWithHttpInfo = function(device_id = NULL, account_id = NULL, app_id = NULL, app_key = NULL, album_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling InviteApi$AlbumInvite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling InviteApi$AlbumInvite, `account_id` is not nullable")
      }

      if (!missing(`app_id`) && is.null(`app_id`)) {
        stop("Invalid value for `app_id` when calling InviteApi$AlbumInvite, `app_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling InviteApi$AlbumInvite, `app_key` is not nullable")
      }

      if (!missing(`album_id`) && is.null(`album_id`)) {
        stop("Invalid value for `album_id` when calling InviteApi$AlbumInvite, `album_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling InviteApi$AlbumInvite, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling InviteApi$AlbumInvite, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appId"]] <- `app_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["albumId"]] <- `album_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/invite/album"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "InviteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Invite to Event
    #'
    #' @param account_id the account ID of the user making the share
    #' @param app_key the application key
    #' @param listing_id The ID of the event listing
    #' @param receiver_account_ids (optional) the account ID of a Sirqul user they would like to share an event with
    #' @param retailer_location_id (optional) The retailer location id of where the event will take place
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return InviteResponse
    EventInvite = function(account_id, app_key, listing_id, receiver_account_ids = NULL, retailer_location_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$EventInviteWithHttpInfo(account_id, app_key, listing_id, receiver_account_ids, retailer_location_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Invite to Event
    #'
    #' @param account_id the account ID of the user making the share
    #' @param app_key the application key
    #' @param listing_id The ID of the event listing
    #' @param receiver_account_ids (optional) the account ID of a Sirqul user they would like to share an event with
    #' @param retailer_location_id (optional) The retailer location id of where the event will take place
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (InviteResponse) with additional information such as HTTP status code, headers
    EventInviteWithHttpInfo = function(account_id, app_key, listing_id, receiver_account_ids = NULL, retailer_location_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`listing_id`)) {
        stop("Missing required parameter `listing_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling InviteApi$EventInvite, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling InviteApi$EventInvite, `app_key` is not nullable")
      }

      if (!missing(`listing_id`) && is.null(`listing_id`)) {
        stop("Invalid value for `listing_id` when calling InviteApi$EventInvite, `listing_id` is not nullable")
      }

      if (!missing(`receiver_account_ids`) && is.null(`receiver_account_ids`)) {
        stop("Invalid value for `receiver_account_ids` when calling InviteApi$EventInvite, `receiver_account_ids` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling InviteApi$EventInvite, `retailer_location_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["receiverAccountIds"]] <- `receiver_account_ids`

      query_params[["appKey"]] <- `app_key`

      query_params[["listingId"]] <- `listing_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      local_var_url_path <- "/invite/event"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "InviteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Invite to Game Level
    #'
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param app_id (optional) This parameter is deprecated.
    #' @param app_key (optional) the application key
    #' @param game_level_id (optional) the game level that the user owns and is giving access to
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return InviteResponse
    GameInvite = function(device_id = NULL, account_id = NULL, app_id = NULL, app_key = NULL, game_level_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GameInviteWithHttpInfo(device_id, account_id, app_id, app_key, game_level_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Invite to Game Level
    #'
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param app_id (optional) This parameter is deprecated.
    #' @param app_key (optional) the application key
    #' @param game_level_id (optional) the game level that the user owns and is giving access to
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (InviteResponse) with additional information such as HTTP status code, headers
    GameInviteWithHttpInfo = function(device_id = NULL, account_id = NULL, app_id = NULL, app_key = NULL, game_level_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling InviteApi$GameInvite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling InviteApi$GameInvite, `account_id` is not nullable")
      }

      if (!missing(`app_id`) && is.null(`app_id`)) {
        stop("Invalid value for `app_id` when calling InviteApi$GameInvite, `app_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling InviteApi$GameInvite, `app_key` is not nullable")
      }

      if (!missing(`game_level_id`) && is.null(`game_level_id`)) {
        stop("Invalid value for `game_level_id` when calling InviteApi$GameInvite, `game_level_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling InviteApi$GameInvite, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling InviteApi$GameInvite, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appId"]] <- `app_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["gameLevelId"]] <- `game_level_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/invite/gameLevel"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "InviteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Invite
    #'
    #' @param account_id (optional) Account ID of the user if they are logged in
    #' @param token (optional) the invite token
    #' @param album_id (optional) album id to match the invite against (if applicable)
    #' @param mission_id (optional) mission id to match the invite against (if applicable)
    #' @param album_contest_id (optional) album contest id to match the invite against (if applicable)
    #' @param offer_id (optional) offer id to match the invite against (if applicable)
    #' @param offer_location_id (optional) offer location id to match the invite against (if applicable)
    #' @param retailer_location_id (optional) retailer location id to match the invite against (if applicable)
    #' @param app_key (optional) the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    GetInvite = function(account_id = NULL, token = NULL, album_id = NULL, mission_id = NULL, album_contest_id = NULL, offer_id = NULL, offer_location_id = NULL, retailer_location_id = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetInviteWithHttpInfo(account_id, token, album_id, mission_id, album_contest_id, offer_id, offer_location_id, retailer_location_id, app_key, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Invite
    #'
    #' @param account_id (optional) Account ID of the user if they are logged in
    #' @param token (optional) the invite token
    #' @param album_id (optional) album id to match the invite against (if applicable)
    #' @param mission_id (optional) mission id to match the invite against (if applicable)
    #' @param album_contest_id (optional) album contest id to match the invite against (if applicable)
    #' @param offer_id (optional) offer id to match the invite against (if applicable)
    #' @param offer_location_id (optional) offer location id to match the invite against (if applicable)
    #' @param retailer_location_id (optional) retailer location id to match the invite against (if applicable)
    #' @param app_key (optional) the application key
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    GetInviteWithHttpInfo = function(account_id = NULL, token = NULL, album_id = NULL, mission_id = NULL, album_contest_id = NULL, offer_id = NULL, offer_location_id = NULL, retailer_location_id = NULL, app_key = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling InviteApi$GetInvite, `account_id` is not nullable")
      }

      if (!missing(`token`) && is.null(`token`)) {
        stop("Invalid value for `token` when calling InviteApi$GetInvite, `token` is not nullable")
      }

      if (!missing(`album_id`) && is.null(`album_id`)) {
        stop("Invalid value for `album_id` when calling InviteApi$GetInvite, `album_id` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling InviteApi$GetInvite, `mission_id` is not nullable")
      }

      if (!missing(`album_contest_id`) && is.null(`album_contest_id`)) {
        stop("Invalid value for `album_contest_id` when calling InviteApi$GetInvite, `album_contest_id` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling InviteApi$GetInvite, `offer_id` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling InviteApi$GetInvite, `offer_location_id` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling InviteApi$GetInvite, `retailer_location_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling InviteApi$GetInvite, `app_key` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["token"]] <- `token`

      query_params[["albumId"]] <- `album_id`

      query_params[["missionId"]] <- `mission_id`

      query_params[["albumContestId"]] <- `album_contest_id`

      query_params[["offerId"]] <- `offer_id`

      query_params[["offerLocationId"]] <- `offer_location_id`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["appKey"]] <- `app_key`

      local_var_url_path <- "/invite/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Invite to Mission
    #'
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param app_id (optional) This parameter is deprecated.
    #' @param app_key (optional) the application key
    #' @param mission_id (optional) the mission to share
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return InviteResponse
    MissionInvite = function(device_id = NULL, account_id = NULL, app_id = NULL, app_key = NULL, mission_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$MissionInviteWithHttpInfo(device_id, account_id, app_id, app_key, mission_id, latitude, longitude, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Invite to Mission
    #'
    #' @param device_id (optional) a unique ID given by the device (deviceId or accountId required)
    #' @param account_id (optional) the account ID of the user (deviceId or accountId required)
    #' @param app_id (optional) This parameter is deprecated.
    #' @param app_key (optional) the application key
    #' @param mission_id (optional) the mission to share
    #' @param latitude (optional) the current latitude of the user
    #' @param longitude (optional) the current longitude of the user
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (InviteResponse) with additional information such as HTTP status code, headers
    MissionInviteWithHttpInfo = function(device_id = NULL, account_id = NULL, app_id = NULL, app_key = NULL, mission_id = NULL, latitude = NULL, longitude = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling InviteApi$MissionInvite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling InviteApi$MissionInvite, `account_id` is not nullable")
      }

      if (!missing(`app_id`) && is.null(`app_id`)) {
        stop("Invalid value for `app_id` when calling InviteApi$MissionInvite, `app_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling InviteApi$MissionInvite, `app_key` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling InviteApi$MissionInvite, `mission_id` is not nullable")
      }

      if (!missing(`latitude`) && is.null(`latitude`)) {
        stop("Invalid value for `latitude` when calling InviteApi$MissionInvite, `latitude` is not nullable")
      }

      if (!missing(`longitude`) && is.null(`longitude`)) {
        stop("Invalid value for `longitude` when calling InviteApi$MissionInvite, `longitude` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appId"]] <- `app_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["missionId"]] <- `mission_id`

      query_params[["latitude"]] <- `latitude`

      query_params[["longitude"]] <- `longitude`

      local_var_url_path <- "/invite/mission"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "InviteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Invite to Offer
    #'
    #' @param account_id the account ID of the user making the share
    #' @param app_key the application key
    #' @param offer_id the ID of the offer used to invite to favorite
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return InviteResponse
    OfferInvite = function(account_id, app_key, offer_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$OfferInviteWithHttpInfo(account_id, app_key, offer_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Invite to Offer
    #'
    #' @param account_id the account ID of the user making the share
    #' @param app_key the application key
    #' @param offer_id the ID of the offer used to invite to favorite
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (InviteResponse) with additional information such as HTTP status code, headers
    OfferInviteWithHttpInfo = function(account_id, app_key, offer_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`offer_id`)) {
        stop("Missing required parameter `offer_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling InviteApi$OfferInvite, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling InviteApi$OfferInvite, `app_key` is not nullable")
      }

      if (!missing(`offer_id`) && is.null(`offer_id`)) {
        stop("Invalid value for `offer_id` when calling InviteApi$OfferInvite, `offer_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["offerId"]] <- `offer_id`

      local_var_url_path <- "/invite/offer"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "InviteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Invite to Offer Location
    #'
    #' @param account_id the account ID of the user making the share
    #' @param app_key the application key
    #' @param offer_location_id the id of the offer location to share
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return InviteResponse
    OfferLocationInvite = function(account_id, app_key, offer_location_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$OfferLocationInviteWithHttpInfo(account_id, app_key, offer_location_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Invite to Offer Location
    #'
    #' @param account_id the account ID of the user making the share
    #' @param app_key the application key
    #' @param offer_location_id the id of the offer location to share
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (InviteResponse) with additional information such as HTTP status code, headers
    OfferLocationInviteWithHttpInfo = function(account_id, app_key, offer_location_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`offer_location_id`)) {
        stop("Missing required parameter `offer_location_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling InviteApi$OfferLocationInvite, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling InviteApi$OfferLocationInvite, `app_key` is not nullable")
      }

      if (!missing(`offer_location_id`) && is.null(`offer_location_id`)) {
        stop("Invalid value for `offer_location_id` when calling InviteApi$OfferLocationInvite, `offer_location_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["offerLocationId"]] <- `offer_location_id`

      local_var_url_path <- "/invite/offerLocation"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "InviteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Invite to Retailer Location
    #'
    #' @param account_id the account ID of the user making the share
    #' @param app_key the application key
    #' @param retailer_location_id The retailer location id of where the event will take place
    #' @param album_id (optional) Optional album id to link with the invite
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return InviteResponse
    RetailerLocationInvite = function(account_id, app_key, retailer_location_id, album_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$RetailerLocationInviteWithHttpInfo(account_id, app_key, retailer_location_id, album_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Invite to Retailer Location
    #'
    #' @param account_id the account ID of the user making the share
    #' @param app_key the application key
    #' @param retailer_location_id The retailer location id of where the event will take place
    #' @param album_id (optional) Optional album id to link with the invite
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (InviteResponse) with additional information such as HTTP status code, headers
    RetailerLocationInviteWithHttpInfo = function(account_id, app_key, retailer_location_id, album_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`account_id`)) {
        stop("Missing required parameter `account_id`.")
      }

      if (missing(`app_key`)) {
        stop("Missing required parameter `app_key`.")
      }

      if (missing(`retailer_location_id`)) {
        stop("Missing required parameter `retailer_location_id`.")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling InviteApi$RetailerLocationInvite, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling InviteApi$RetailerLocationInvite, `app_key` is not nullable")
      }

      if (!missing(`retailer_location_id`) && is.null(`retailer_location_id`)) {
        stop("Invalid value for `retailer_location_id` when calling InviteApi$RetailerLocationInvite, `retailer_location_id` is not nullable")
      }

      if (!missing(`album_id`) && is.null(`album_id`)) {
        stop("Invalid value for `album_id` when calling InviteApi$RetailerLocationInvite, `album_id` is not nullable")
      }

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["retailerLocationId"]] <- `retailer_location_id`

      query_params[["albumId"]] <- `album_id`

      local_var_url_path <- "/invite/retailerLocation"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "InviteResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
