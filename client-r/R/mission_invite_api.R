#' Sirqul IoT Platform
#'
#' Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
#'
#' The version of the OpenAPI document: 3.16
#' Contact: info@sirqul.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title MissionInvite operations
#' @description MissionInviteApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateMissionInvite  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required). (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required). (Optional)
#' var_mission_id <- 56 # integer | The mission to find the invite for. (Optional)
#' var_join_code <- "join_code_example" # character | code to be entered for user to join the mission (Optional)
#' var_include_game_data <- "include_game_data_example" # character | Include the game level data with the mission. (Optional)
#'
#' #Create Mission Invite
#' api_instance <- MissionInviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateMissionInvite(device_id = var_device_id, account_id = var_account_id, mission_id = var_mission_id, join_code = var_join_code, include_game_data = var_include_game_datadata_file = "result.txt")
#' result <- api_instance$CreateMissionInvite(device_id = var_device_id, account_id = var_account_id, mission_id = var_mission_id, join_code = var_join_code, include_game_data = var_include_game_data)
#' dput(result)
#'
#'
#' ####################  DeleteMissionInvite  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required). (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required). (Optional)
#' var_mission_id <- 56 # integer | The mission to find the invite for (missionId or missionInviteId requried). (Optional)
#' var_mission_invite_id <- 56 # integer | The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried). (Optional)
#' var_include_game_data <- "include_game_data_example" # character | Include the game level data with the mission. (Optional)
#'
#' #Delete Mission Invite
#' api_instance <- MissionInviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteMissionInvite(device_id = var_device_id, account_id = var_account_id, mission_id = var_mission_id, mission_invite_id = var_mission_invite_id, include_game_data = var_include_game_datadata_file = "result.txt")
#' result <- api_instance$DeleteMissionInvite(device_id = var_device_id, account_id = var_account_id, mission_id = var_mission_id, mission_invite_id = var_mission_invite_id, include_game_data = var_include_game_data)
#' dput(result)
#'
#'
#' ####################  GetMissionInvite  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required). (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required). (Optional)
#' var_mission_id <- 56 # integer | The mission to find the invite for (missionId or missionInviteId requried). (Optional)
#' var_mission_invite_id <- 56 # integer | The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried). (Optional)
#' var_include_game_data <- "include_game_data_example" # character | Include the game level data with the mission. (Optional)
#' var_include_scores <- "include_scores_example" # character | include the scores with the mission (Optional)
#'
#' #Get Mission Invite
#' api_instance <- MissionInviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetMissionInvite(device_id = var_device_id, account_id = var_account_id, mission_id = var_mission_id, mission_invite_id = var_mission_invite_id, include_game_data = var_include_game_data, include_scores = var_include_scoresdata_file = "result.txt")
#' result <- api_instance$GetMissionInvite(device_id = var_device_id, account_id = var_account_id, mission_id = var_mission_id, mission_invite_id = var_mission_invite_id, include_game_data = var_include_game_data, include_scores = var_include_scores)
#' dput(result)
#'
#'
#' ####################  SearchMissionInvites  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required). (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required). (Optional)
#' var_app_key <- "app_key_example" # character | the app to retrieve the data for, use your application key. (Optional)
#' var_app_version <- "app_version_example" # character | the application version, used to version the game level data (Optional)
#' var_mission_id <- 56 # integer | the mission id to filter on (Optional)
#' var_status <- "status_example" # character | The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED} (Optional)
#' var_last_updated <- 56 # integer | Only return invites that have been updated since this date/time (long) (Optional)
#' var_start <- 56 # integer | The index into the record set to start with. (Optional)
#' var_limit <- 56 # integer | The total number of record to return. (Optional)
#' var_keyword <- "keyword_example" # character | the keyword to search on (Optional)
#' var_mission_types <- "mission_types_example" # character |  (Optional)
#' var_filter_by_billable <- "filter_by_billable_example" # character | filter results by the account's billable (Optional)
#' var_include_game_data <- "include_game_data_example" # character | Include the game level data with the mission. (Optional)
#'
#' #Search Mission Invites
#' api_instance <- MissionInviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchMissionInvites(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, app_version = var_app_version, mission_id = var_mission_id, status = var_status, last_updated = var_last_updated, start = var_start, limit = var_limit, keyword = var_keyword, mission_types = var_mission_types, filter_by_billable = var_filter_by_billable, include_game_data = var_include_game_datadata_file = "result.txt")
#' result <- api_instance$SearchMissionInvites(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, app_version = var_app_version, mission_id = var_mission_id, status = var_status, last_updated = var_last_updated, start = var_start, limit = var_limit, keyword = var_keyword, mission_types = var_mission_types, filter_by_billable = var_filter_by_billable, include_game_data = var_include_game_data)
#' dput(result)
#'
#'
#' ####################  UpdateMissionInvite  ####################
#'
#' library(openapi)
#' var_device_id <- "device_id_example" # character | the device id (deviceId or accountId required). (Optional)
#' var_account_id <- 56 # integer | the account id of the user (deviceId or accountId required). (Optional)
#' var_app_key <- "app_key_example" # character | the application key (Optional)
#' var_mission_id <- 56 # integer | The mission to find the invite for (missionId or missionInviteId requried). (Optional)
#' var_mission_invite_id <- 56 # integer | The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried). (Optional)
#' var_pack_id <- 56 # integer | the pack id to find the invite for (Optional)
#' var_game_level_id <- 56 # integer | the game level id to find the invite for (Optional)
#' var_status <- "status_example" # character | The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED} (Optional)
#' var_permissionable_type <- "permissionable_type_example" # character | This is the content type for missions that require user submission. Note that user submitted content must require a status of PENDING_REVIEW to be accpeted. {ACCOUNT, GAMELEVEL, COLLECTION, ALBUM_CONTEST, THEME_DESCRIPTOR, ALBUM} (Optional)
#' var_permissionable_id <- 56 # integer | The id of the content being submitted. (Optional)
#' var_include_game_data <- "include_game_data_example" # character | Include the game level data with the mission. (Optional)
#'
#' #Update Mission Invite
#' api_instance <- MissionInviteApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateMissionInvite(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, mission_id = var_mission_id, mission_invite_id = var_mission_invite_id, pack_id = var_pack_id, game_level_id = var_game_level_id, status = var_status, permissionable_type = var_permissionable_type, permissionable_id = var_permissionable_id, include_game_data = var_include_game_datadata_file = "result.txt")
#' result <- api_instance$UpdateMissionInvite(device_id = var_device_id, account_id = var_account_id, app_key = var_app_key, mission_id = var_mission_id, mission_invite_id = var_mission_invite_id, pack_id = var_pack_id, game_level_id = var_game_level_id, status = var_status, permissionable_type = var_permissionable_type, permissionable_id = var_permissionable_id, include_game_data = var_include_game_data)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
MissionInviteApi <- R6::R6Class(
  "MissionInviteApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new MissionInviteApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create Mission Invite
    #'
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param mission_id (optional) The mission to find the invite for.
    #' @param join_code (optional) code to be entered for user to join the mission
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MissionResponse
    CreateMissionInvite = function(device_id = NULL, account_id = NULL, mission_id = NULL, join_code = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateMissionInviteWithHttpInfo(device_id, account_id, mission_id, join_code, include_game_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Mission Invite
    #'
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param mission_id (optional) The mission to find the invite for.
    #' @param join_code (optional) code to be entered for user to join the mission
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MissionResponse) with additional information such as HTTP status code, headers
    CreateMissionInviteWithHttpInfo = function(device_id = NULL, account_id = NULL, mission_id = NULL, join_code = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling MissionInviteApi$CreateMissionInvite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionInviteApi$CreateMissionInvite, `account_id` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling MissionInviteApi$CreateMissionInvite, `mission_id` is not nullable")
      }

      if (!missing(`join_code`) && is.null(`join_code`)) {
        stop("Invalid value for `join_code` when calling MissionInviteApi$CreateMissionInvite, `join_code` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling MissionInviteApi$CreateMissionInvite, `include_game_data` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["missionId"]] <- `mission_id`

      query_params[["joinCode"]] <- `join_code`

      query_params[["includeGameData"]] <- `include_game_data`

      local_var_url_path <- "/mission/invite/create"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MissionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Mission Invite
    #'
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param mission_id (optional) The mission to find the invite for (missionId or missionInviteId requried).
    #' @param mission_invite_id (optional) The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried).
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SirqulResponse
    DeleteMissionInvite = function(device_id = NULL, account_id = NULL, mission_id = NULL, mission_invite_id = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeleteMissionInviteWithHttpInfo(device_id, account_id, mission_id, mission_invite_id, include_game_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Mission Invite
    #'
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param mission_id (optional) The mission to find the invite for (missionId or missionInviteId requried).
    #' @param mission_invite_id (optional) The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried).
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SirqulResponse) with additional information such as HTTP status code, headers
    DeleteMissionInviteWithHttpInfo = function(device_id = NULL, account_id = NULL, mission_id = NULL, mission_invite_id = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling MissionInviteApi$DeleteMissionInvite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionInviteApi$DeleteMissionInvite, `account_id` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling MissionInviteApi$DeleteMissionInvite, `mission_id` is not nullable")
      }

      if (!missing(`mission_invite_id`) && is.null(`mission_invite_id`)) {
        stop("Invalid value for `mission_invite_id` when calling MissionInviteApi$DeleteMissionInvite, `mission_invite_id` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling MissionInviteApi$DeleteMissionInvite, `include_game_data` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["missionId"]] <- `mission_id`

      query_params[["missionInviteId"]] <- `mission_invite_id`

      query_params[["includeGameData"]] <- `include_game_data`

      local_var_url_path <- "/mission/invite/delete"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SirqulResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Mission Invite
    #'
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param mission_id (optional) The mission to find the invite for (missionId or missionInviteId requried).
    #' @param mission_invite_id (optional) The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried).
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param include_scores (optional) include the scores with the mission
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MissionResponse
    GetMissionInvite = function(device_id = NULL, account_id = NULL, mission_id = NULL, mission_invite_id = NULL, include_game_data = NULL, include_scores = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetMissionInviteWithHttpInfo(device_id, account_id, mission_id, mission_invite_id, include_game_data, include_scores, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Mission Invite
    #'
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param mission_id (optional) The mission to find the invite for (missionId or missionInviteId requried).
    #' @param mission_invite_id (optional) The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried).
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param include_scores (optional) include the scores with the mission
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MissionResponse) with additional information such as HTTP status code, headers
    GetMissionInviteWithHttpInfo = function(device_id = NULL, account_id = NULL, mission_id = NULL, mission_invite_id = NULL, include_game_data = NULL, include_scores = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling MissionInviteApi$GetMissionInvite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionInviteApi$GetMissionInvite, `account_id` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling MissionInviteApi$GetMissionInvite, `mission_id` is not nullable")
      }

      if (!missing(`mission_invite_id`) && is.null(`mission_invite_id`)) {
        stop("Invalid value for `mission_invite_id` when calling MissionInviteApi$GetMissionInvite, `mission_invite_id` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling MissionInviteApi$GetMissionInvite, `include_game_data` is not nullable")
      }

      if (!missing(`include_scores`) && is.null(`include_scores`)) {
        stop("Invalid value for `include_scores` when calling MissionInviteApi$GetMissionInvite, `include_scores` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["missionId"]] <- `mission_id`

      query_params[["missionInviteId"]] <- `mission_invite_id`

      query_params[["includeGameData"]] <- `include_game_data`

      query_params[["includeScores"]] <- `include_scores`

      local_var_url_path <- "/mission/invite/get"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MissionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Search Mission Invites
    #'
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param app_key (optional) the app to retrieve the data for, use your application key.
    #' @param app_version (optional) the application version, used to version the game level data
    #' @param mission_id (optional) the mission id to filter on
    #' @param status (optional) The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED}
    #' @param last_updated (optional) Only return invites that have been updated since this date/time (long)
    #' @param start (optional) The index into the record set to start with.
    #' @param limit (optional) The total number of record to return.
    #' @param keyword (optional) the keyword to search on
    #' @param mission_types (optional) 
    #' @param filter_by_billable (optional) filter results by the account's billable
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[MissionResponse]
    SearchMissionInvites = function(device_id = NULL, account_id = NULL, app_key = NULL, app_version = NULL, mission_id = NULL, status = NULL, last_updated = NULL, start = NULL, limit = NULL, keyword = NULL, mission_types = NULL, filter_by_billable = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SearchMissionInvitesWithHttpInfo(device_id, account_id, app_key, app_version, mission_id, status, last_updated, start, limit, keyword, mission_types, filter_by_billable, include_game_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Search Mission Invites
    #'
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param app_key (optional) the app to retrieve the data for, use your application key.
    #' @param app_version (optional) the application version, used to version the game level data
    #' @param mission_id (optional) the mission id to filter on
    #' @param status (optional) The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED}
    #' @param last_updated (optional) Only return invites that have been updated since this date/time (long)
    #' @param start (optional) The index into the record set to start with.
    #' @param limit (optional) The total number of record to return.
    #' @param keyword (optional) the keyword to search on
    #' @param mission_types (optional) 
    #' @param filter_by_billable (optional) filter results by the account's billable
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[MissionResponse]) with additional information such as HTTP status code, headers
    SearchMissionInvitesWithHttpInfo = function(device_id = NULL, account_id = NULL, app_key = NULL, app_version = NULL, mission_id = NULL, status = NULL, last_updated = NULL, start = NULL, limit = NULL, keyword = NULL, mission_types = NULL, filter_by_billable = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling MissionInviteApi$SearchMissionInvites, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionInviteApi$SearchMissionInvites, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling MissionInviteApi$SearchMissionInvites, `app_key` is not nullable")
      }

      if (!missing(`app_version`) && is.null(`app_version`)) {
        stop("Invalid value for `app_version` when calling MissionInviteApi$SearchMissionInvites, `app_version` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling MissionInviteApi$SearchMissionInvites, `mission_id` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling MissionInviteApi$SearchMissionInvites, `status` is not nullable")
      }

      if (!missing(`last_updated`) && is.null(`last_updated`)) {
        stop("Invalid value for `last_updated` when calling MissionInviteApi$SearchMissionInvites, `last_updated` is not nullable")
      }

      if (!missing(`start`) && is.null(`start`)) {
        stop("Invalid value for `start` when calling MissionInviteApi$SearchMissionInvites, `start` is not nullable")
      }

      if (!missing(`limit`) && is.null(`limit`)) {
        stop("Invalid value for `limit` when calling MissionInviteApi$SearchMissionInvites, `limit` is not nullable")
      }

      if (!missing(`keyword`) && is.null(`keyword`)) {
        stop("Invalid value for `keyword` when calling MissionInviteApi$SearchMissionInvites, `keyword` is not nullable")
      }

      if (!missing(`mission_types`) && is.null(`mission_types`)) {
        stop("Invalid value for `mission_types` when calling MissionInviteApi$SearchMissionInvites, `mission_types` is not nullable")
      }

      if (!missing(`filter_by_billable`) && is.null(`filter_by_billable`)) {
        stop("Invalid value for `filter_by_billable` when calling MissionInviteApi$SearchMissionInvites, `filter_by_billable` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling MissionInviteApi$SearchMissionInvites, `include_game_data` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["appVersion"]] <- `app_version`

      query_params[["missionId"]] <- `mission_id`

      query_params[["status"]] <- `status`

      query_params[["lastUpdated"]] <- `last_updated`

      query_params[["start"]] <- `start`

      query_params[["limit"]] <- `limit`

      query_params[["keyword"]] <- `keyword`

      query_params[["missionTypes"]] <- `mission_types`

      query_params[["filterByBillable"]] <- `filter_by_billable`

      query_params[["includeGameData"]] <- `include_game_data`

      local_var_url_path <- "/mission/invite/search"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[MissionResponse]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Mission Invite
    #'
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param app_key (optional) the application key
    #' @param mission_id (optional) The mission to find the invite for (missionId or missionInviteId requried).
    #' @param mission_invite_id (optional) The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried).
    #' @param pack_id (optional) the pack id to find the invite for
    #' @param game_level_id (optional) the game level id to find the invite for
    #' @param status (optional) The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED}
    #' @param permissionable_type (optional) This is the content type for missions that require user submission. Note that user submitted content must require a status of PENDING_REVIEW to be accpeted. {ACCOUNT, GAMELEVEL, COLLECTION, ALBUM_CONTEST, THEME_DESCRIPTOR, ALBUM}
    #' @param permissionable_id (optional) The id of the content being submitted.
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MissionResponse
    UpdateMissionInvite = function(device_id = NULL, account_id = NULL, app_key = NULL, mission_id = NULL, mission_invite_id = NULL, pack_id = NULL, game_level_id = NULL, status = NULL, permissionable_type = NULL, permissionable_id = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateMissionInviteWithHttpInfo(device_id, account_id, app_key, mission_id, mission_invite_id, pack_id, game_level_id, status, permissionable_type, permissionable_id, include_game_data, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Mission Invite
    #'
    #' @param device_id (optional) the device id (deviceId or accountId required).
    #' @param account_id (optional) the account id of the user (deviceId or accountId required).
    #' @param app_key (optional) the application key
    #' @param mission_id (optional) The mission to find the invite for (missionId or missionInviteId requried).
    #' @param mission_invite_id (optional) The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried).
    #' @param pack_id (optional) the pack id to find the invite for
    #' @param game_level_id (optional) the game level id to find the invite for
    #' @param status (optional) The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED}
    #' @param permissionable_type (optional) This is the content type for missions that require user submission. Note that user submitted content must require a status of PENDING_REVIEW to be accpeted. {ACCOUNT, GAMELEVEL, COLLECTION, ALBUM_CONTEST, THEME_DESCRIPTOR, ALBUM}
    #' @param permissionable_id (optional) The id of the content being submitted.
    #' @param include_game_data (optional) Include the game level data with the mission.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MissionResponse) with additional information such as HTTP status code, headers
    UpdateMissionInviteWithHttpInfo = function(device_id = NULL, account_id = NULL, app_key = NULL, mission_id = NULL, mission_invite_id = NULL, pack_id = NULL, game_level_id = NULL, status = NULL, permissionable_type = NULL, permissionable_id = NULL, include_game_data = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`device_id`) && is.null(`device_id`)) {
        stop("Invalid value for `device_id` when calling MissionInviteApi$UpdateMissionInvite, `device_id` is not nullable")
      }

      if (!missing(`account_id`) && is.null(`account_id`)) {
        stop("Invalid value for `account_id` when calling MissionInviteApi$UpdateMissionInvite, `account_id` is not nullable")
      }

      if (!missing(`app_key`) && is.null(`app_key`)) {
        stop("Invalid value for `app_key` when calling MissionInviteApi$UpdateMissionInvite, `app_key` is not nullable")
      }

      if (!missing(`mission_id`) && is.null(`mission_id`)) {
        stop("Invalid value for `mission_id` when calling MissionInviteApi$UpdateMissionInvite, `mission_id` is not nullable")
      }

      if (!missing(`mission_invite_id`) && is.null(`mission_invite_id`)) {
        stop("Invalid value for `mission_invite_id` when calling MissionInviteApi$UpdateMissionInvite, `mission_invite_id` is not nullable")
      }

      if (!missing(`pack_id`) && is.null(`pack_id`)) {
        stop("Invalid value for `pack_id` when calling MissionInviteApi$UpdateMissionInvite, `pack_id` is not nullable")
      }

      if (!missing(`game_level_id`) && is.null(`game_level_id`)) {
        stop("Invalid value for `game_level_id` when calling MissionInviteApi$UpdateMissionInvite, `game_level_id` is not nullable")
      }

      if (!missing(`status`) && is.null(`status`)) {
        stop("Invalid value for `status` when calling MissionInviteApi$UpdateMissionInvite, `status` is not nullable")
      }

      if (!missing(`permissionable_type`) && is.null(`permissionable_type`)) {
        stop("Invalid value for `permissionable_type` when calling MissionInviteApi$UpdateMissionInvite, `permissionable_type` is not nullable")
      }

      if (!missing(`permissionable_id`) && is.null(`permissionable_id`)) {
        stop("Invalid value for `permissionable_id` when calling MissionInviteApi$UpdateMissionInvite, `permissionable_id` is not nullable")
      }

      if (!missing(`include_game_data`) && is.null(`include_game_data`)) {
        stop("Invalid value for `include_game_data` when calling MissionInviteApi$UpdateMissionInvite, `include_game_data` is not nullable")
      }

      query_params[["deviceId"]] <- `device_id`

      query_params[["accountId"]] <- `account_id`

      query_params[["appKey"]] <- `app_key`

      query_params[["missionId"]] <- `mission_id`

      query_params[["missionInviteId"]] <- `mission_invite_id`

      query_params[["packId"]] <- `pack_id`

      query_params[["gameLevelId"]] <- `game_level_id`

      query_params[["status"]] <- `status`

      query_params[["permissionableType"]] <- `permissionable_type`

      query_params[["permissionableId"]] <- `permissionable_id`

      query_params[["includeGameData"]] <- `include_game_data`

      local_var_url_path <- "/mission/invite/update"

      # The Accept request HTTP header
      local_var_accepts <- list("*/*")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MissionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
