# 

Sirqul provides an Engagement-as-a-Service (EaaS)
           IoT Platform with Smart Mesh network technology to drive
           engagement, operational efficiency, rapid innovation and new
           revenue streams. Please visit https://dev.sirqul.com/ for more
           documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>

This Groovy package, using the [http-builder-ng library](https://http-builder-ng.github.io/http-builder-ng/), is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 3.16
- Package version: 3.16
- Build date: 2026-02-11T18:58:45.372530228Z[Etc/UTC]
- Generator version: 7.20.0-SNAPSHOT
- Build package: org.openapitools.codegen.languages.GroovyClientCodegen
For more information, please visit [https://corp.sirqul.com/contact-us/](https://corp.sirqul.com/contact-us/)

## Requirements

* Groovy 2.5.7
* Gradle 4.9

## Build

First, create the gradle wrapper script:

```
gradle wrapper
```

Then, run:

```
./gradlew check assemble
```

## Getting Started


```groovy
def apiInstance = new AccountApi()
def deviceId = "deviceId_example" // String | The device id (deviceId or accountId required)
def accountId = 56L // Long | The account id of the user (deviceId or accountId required)
def q = "q_example" // String | Deprecated - legacy query parameter
def keyword = "keyword_example" // String | An optional keyword to search on, will be ignore if empty (NOT implemented yet)
def postalCode = "postalCode_example" // String | The postal code to search on, either postalCode or the user's exact location is required
def latitude = 3.4D // Double | The latitude of the user, either postalCode or the user's exact location is required
def longitude = 3.4D // Double | The longitude of the user, either postalCode or the user's exact location is required
def appKey = "appKey_example" // String | The application key
def range = 10D // Double | The range to search on
def locationLastUpdated = 56L // Long | Searches for user's that has updated their location since this date
def gender = "gender_example" // String | The preferred gender
def minAge = 56 // Integer | The preferred min age
def maxAge = 56 // Integer | The preferred max age
def companionshipIndex = 56 // Integer | The preferred companionship index
def i = 56 // Integer | this is the start index of a query
def start = 0 // Integer | Start of the pagination
def l = 56 // Integer | this is the limit index of a query
def limit = 100 // Integer | Limit of the pagination
def searchMode = "searchMode_example" // String | Search mode to use for index searches (e.g. CLOUDINDEX, OPENSEARCH)
def sortField = "sortField_example" // String | Sorting field for results (default: DISTANCE)
def descending = true // Boolean | Whether to sort descending (default: false)
def roles = "roles_example" // String | Roles to filter on
def tags = "tags_example" // String | Tags to filter on
def experience = "experience_example" // String | The experience to filter on
def categoryIds = "categoryIds_example" // String | The category ids to filter on (comma separated)
def audienceIds = "audienceIds_example" // String | The audience ids to filter on (comma separated)
def audienceOperator = "AND" // String | Operator used to combine audience filters (default: AND)
def updateCurrentLocation = false // Boolean | Whether to use the given lat & long to update the user's current location
def updatePreferredSettings = false // Boolean | Whether to use the given parameters to update the user's preferred settings
def showExactLocations = true // Boolean | Determines whether to always display user exact locations
def showConnectionToSearcher = false // Boolean | Return connection of the accounts in the result to the passed in account if there exists any
def flagCountMinimum = 56L // Long | Return any results that have a minimum of the specified flag count (even ones that have met the flag threshold)
def verifiedUserOnly = true // Boolean | Returns only verified users
def contentAdminOnly = true // Boolean | Returns only content admin users

apiInstance.accountLocationSearch(deviceId, accountId, q, keyword, postalCode, latitude, longitude, appKey, range, locationLastUpdated, gender, minAge, maxAge, companionshipIndex, i, start, l, limit, searchMode, sortField, descending, roles, tags, experience, categoryIds, audienceIds, audienceOperator, updateCurrentLocation, updatePreferredSettings, showExactLocations, showConnectionToSearcher, flagCountMinimum, verifiedUserOnly, contentAdminOnly)
    {
    // on success
    def result = (UserLocationSearchResponse)it
    println result
    
}
    {
    // on failure
    statusCode, message ->
        println "${statusCode} ${message}"
};
```

