//
// WeatherAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech

import Foundation
import Combine
import OpenAPITransport


open class WeatherAPI {
    private let transport: OpenAPITransport
    public var encoder: JSONEncoder = {
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .formatted(OpenISO8601DateFormatter())
        return encoder
    }()
    public var decoder: JSONDecoder = {
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .formatted(OpenISO8601DateFormatter())
        return decoder
    }()
    public var baseURL = URL(string: "http://localhost")

    public init(_ transport: OpenAPITransport) {
        self.transport = transport
    }


    /// Search Weather
    /// - GET /api/{version}/weather/search
    /// - Search the weather forcast for the next 5 days
    /// - parameter version: (path)  
    /// - parameter regionId: (query) Region Id (optional)
    /// - parameter latitude: (query) Latitude (optional)
    /// - parameter longitude: (query) Longitude (optional)
    /// - parameter timezoneOffset: (query) Timezone Offset (optional, default to -6)
    /// - returns: AnyPublisher<WeatherResponse, Error> 
    open func searchWeather(version: Double, regionId: Int64? = nil, latitude: Double? = nil, longitude: Double? = nil, timezoneOffset: Int64? = nil) -> AnyPublisher<WeatherResponse, Error> {
        Deferred {
            Result<URLRequest, Error> {
                guard let baseURL = self.transport.baseURL ?? self.baseURL else {
                    throw OpenAPITransportError.badURLError()
                }
                var localVarPath = "/api/{version}/weather/search"
                localVarPath = localVarPath.replacingOccurrences(of: "{version}", with: "\(version)")
                let localVarURL = baseURL.appendingPathComponent(localVarPath)
                var components = URLComponents(url: localVarURL, resolvingAgainstBaseURL: false)
                var queryItems: [URLQueryItem] = []
                if let regionId = regionId { queryItems.append(URLQueryItem(name: "regionId", value: "\(regionId)")) } 
                if let latitude = latitude { queryItems.append(URLQueryItem(name: "latitude", value: "\(latitude)")) } 
                if let longitude = longitude { queryItems.append(URLQueryItem(name: "longitude", value: "\(longitude)")) } 
                if let timezoneOffset = timezoneOffset { queryItems.append(URLQueryItem(name: "timezoneOffset", value: "\(timezoneOffset)")) } 
                components?.queryItems = queryItems
                guard let requestURL = components?.url else {
                    throw OpenAPITransportError.badURLError()
                }
                var request = URLRequest(url: requestURL)
                request.httpMethod = "GET"
                return request
            }.publisher
        }.flatMap { request -> AnyPublisher<WeatherResponse, Error> in 
            return self.transport.send(request: request)
                .tryMap { response in
                    try self.decoder.decode(WeatherResponse.self, from: response.data)
                }
                .eraseToAnyPublisher()
        }.eraseToAnyPublisher()
    }
}
