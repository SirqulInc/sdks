//
// PathingAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech

import Foundation
import Combine
import OpenAPITransport


open class PathingAPI {
    private let transport: OpenAPITransport
    public var encoder: JSONEncoder = {
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .formatted(OpenISO8601DateFormatter())
        return encoder
    }()
    public var decoder: JSONDecoder = {
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .formatted(OpenISO8601DateFormatter())
        return decoder
    }()
    public var baseURL = URL(string: "https://dev.sirqul.com/api/3.18")

    public init(_ transport: OpenAPITransport) {
        self.transport = transport
    }

    ///
    /// Enum for parameter units
    ///
    public enum ComputePathUnits: String, Codable, CaseIterable {
        case metric = "METRIC"
        case imperial = "IMPERIAL"
    }

    /// Calculate Path
    /// - GET /pathing/compute
    /// - Calculates the shortest path from point to point on a grid
    /// - parameter data: (query) the data to with start, end point and exclusion points 
    /// - parameter units: (query) the system of measurement for directions: {METRIC, IMPERIAL} 
    /// - parameter reducePath: (query) determines whether to reduce the path to go in diagonal lines 
    /// - parameter directions: (query) determines whether to return text directions 
    /// - returns: AnyPublisher<PathingResponse, Error> 
    open func computePath(data: String, units: ComputePathUnits, reducePath: Bool, directions: Bool) -> AnyPublisher<PathingResponse, Error> {
        Deferred {
            Result<URLRequest, Error> {
                guard let baseURL = self.transport.baseURL ?? self.baseURL else {
                    throw OpenAPITransportError.badURLError()
                }
                let localVarPath = "/pathing/compute"
                let localVarURL = baseURL.appendingPathComponent(localVarPath)
                var components = URLComponents(url: localVarURL, resolvingAgainstBaseURL: false)
                var queryItems: [URLQueryItem] = []
                queryItems.append(URLQueryItem(name: "data", value: data))
                queryItems.append(URLQueryItem(name: "units", value: units.rawValue))
                queryItems.append(URLQueryItem(name: "reducePath", value: reducePath ? "true" : "false"))
                queryItems.append(URLQueryItem(name: "directions", value: directions ? "true" : "false"))
                components?.queryItems = queryItems
                guard let requestURL = components?.url else {
                    throw OpenAPITransportError.badURLError()
                }
                var request = URLRequest(url: requestURL)
                request.httpMethod = "GET"
                return request
            }.publisher
        }.flatMap { request -> AnyPublisher<PathingResponse, Error> in 
            return self.transport.send(request: request)
                .tryMap { response in
                    try self.decoder.decode(PathingResponse.self, from: response.data)
                }
                .eraseToAnyPublisher()
        }.eraseToAnyPublisher()
    }
}
