# coding: utf-8

"""
    Sirqul IoT Platform

    Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>

    The version of the OpenAPI document: 3.16
    Contact: info@sirqul.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class AssignmentStatus(BaseModel):
    """
    AssignmentStatus
    """ # noqa: E501
    id: Optional[StrictInt] = None
    active: Optional[StrictBool] = None
    valid: Optional[StrictBool] = None
    assignment: Optional[Assignment] = None
    creator: Optional[Account] = None
    to_do: Optional[StrictStr] = Field(default=None, alias="toDo")
    connection: Optional[StrictStr] = None
    method: Optional[StrictStr] = None
    status: Optional[StrictStr] = None
    closure: Optional[StrictStr] = None
    message: Optional[StrictStr] = None
    follow_up: Optional[datetime] = Field(default=None, alias="followUp")
    notification: Optional[ScheduledNotification] = None
    __properties: ClassVar[List[str]] = ["id", "active", "valid", "assignment", "creator", "toDo", "connection", "method", "status", "closure", "message", "followUp", "notification"]

    @field_validator('to_do')
    def to_do_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SITE_VISIT', 'PHONE', 'RECONTACT', 'RENEWAL', 'CREDIT']):
            raise ValueError("must be one of enum values ('SITE_VISIT', 'PHONE', 'RECONTACT', 'RENEWAL', 'CREDIT')")
        return value

    @field_validator('connection')
    def connection_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['INITIAL', 'FOLLOW_UP', 'DECLINED']):
            raise ValueError("must be one of enum values ('INITIAL', 'FOLLOW_UP', 'DECLINED')")
        return value

    @field_validator('method')
    def method_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['PHONE', 'SITE_VISIT', 'EMAIL']):
            raise ValueError("must be one of enum values ('PHONE', 'SITE_VISIT', 'EMAIL')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ARCHIVED', 'SUBSCRIBED', 'UNSUBSCRIBED', 'CONTACTED', 'DECLINED', 'NOT_CONTACTED']):
            raise ValueError("must be one of enum values ('ARCHIVED', 'SUBSCRIBED', 'UNSUBSCRIBED', 'CONTACTED', 'DECLINED', 'NOT_CONTACTED')")
        return value

    @field_validator('closure')
    def closure_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['PHONE', 'SITE_VISIT', 'PHONE_SITE', 'WEB']):
            raise ValueError("must be one of enum values ('PHONE', 'SITE_VISIT', 'PHONE_SITE', 'WEB')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AssignmentStatus from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of assignment
        if self.assignment:
            _dict['assignment'] = self.assignment.to_dict()
        # override the default output from pydantic by calling `to_dict()` of creator
        if self.creator:
            _dict['creator'] = self.creator.to_dict()
        # override the default output from pydantic by calling `to_dict()` of notification
        if self.notification:
            _dict['notification'] = self.notification.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AssignmentStatus from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "active": obj.get("active"),
            "valid": obj.get("valid"),
            "assignment": Assignment.from_dict(obj["assignment"]) if obj.get("assignment") is not None else None,
            "creator": Account.from_dict(obj["creator"]) if obj.get("creator") is not None else None,
            "toDo": obj.get("toDo"),
            "connection": obj.get("connection"),
            "method": obj.get("method"),
            "status": obj.get("status"),
            "closure": obj.get("closure"),
            "message": obj.get("message"),
            "followUp": obj.get("followUp"),
            "notification": ScheduledNotification.from_dict(obj["notification"]) if obj.get("notification") is not None else None
        })
        return _obj

from openapi_client.models.account import Account
from openapi_client.models.assignment import Assignment
from openapi_client.models.scheduled_notification import ScheduledNotification
# TODO: Rewrite to not use raise_errors
AssignmentStatus.model_rebuild(raise_errors=False)

