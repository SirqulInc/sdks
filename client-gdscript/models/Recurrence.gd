extends Resource
class_name Recurrence

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# Recurrence Model


# Required: False
# isArray: false
# Allowed values: "DAILY", "WEEKLY", "MONTHLY", "ANNUALLY", "WEEK_DAY_ONLY"
@export var frequency: String = "":
	set(value):
		if str(value) != "" and not (str(value) in __frequency__allowable__values):
			push_error("Recurrence: tried to set property `frequency` to a value that is not allowed." +
				"  Allowed values: `DAILY`, `WEEKLY`, `MONTHLY`, `ANNUALLY`, `WEEK_DAY_ONLY`")
			return
		__frequency__was__set = true
		frequency = value
var __frequency__was__set := false
var __frequency__allowable__values := ["DAILY", "WEEKLY", "MONTHLY", "ANNUALLY", "WEEK_DAY_ONLY"]

# Required: False
# isArray: true
@export var daysOfWeek: Array:
	set(value):
		__daysOfWeek__was__set = true
		daysOfWeek = value
var __daysOfWeek__was__set := false

#       (but it's actually a Date ; no timezones support in Gdscript)
# Required: False
# isArray: false
@export var start: String:
	set(value):
		__start__was__set = true
		start = value
var __start__was__set := false

# Required: False
# isArray: false
@export var count: int:
	set(value):
		__count__was__set = true
		count = value
var __count__was__set := false

#       (but it's actually a Date ; no timezones support in Gdscript)
# Required: False
# isArray: false
@export var until: String:
	set(value):
		__until__was__set = true
		until = value
var __until__was__set := false


func bzz_collect_missing_properties() -> Array:
	var bzz_missing_properties := Array()
	return bzz_missing_properties


func bzz_normalize() -> Dictionary:
	var bzz_dictionary := Dictionary()
	if self.__frequency__was__set:
		bzz_dictionary["frequency"] = self.frequency
	if self.__daysOfWeek__was__set:
		bzz_dictionary["daysOfWeek"] = self.daysOfWeek
	if self.__start__was__set:
		bzz_dictionary["start"] = self.start
	if self.__count__was__set:
		bzz_dictionary["count"] = self.count
	if self.__until__was__set:
		bzz_dictionary["until"] = self.until
	return bzz_dictionary


# Won't work for JSON+LD
static func bzz_denormalize_single(from_dict: Dictionary):
	var me := new()
	if from_dict.has("frequency"):
		me.frequency = from_dict["frequency"]
	if from_dict.has("daysOfWeek"):
		me.daysOfWeek = from_dict["daysOfWeek"]
	if from_dict.has("start"):
		me.start = from_dict["start"]
	if from_dict.has("count"):
		me.count = from_dict["count"]
	if from_dict.has("until"):
		me.until = from_dict["until"]
	return me


# Won't work for JSON+LD
static func bzz_denormalize_multiple(from_array: Array):
	var mes := Array()
	for element in from_array:
		if element is Array:
			mes.append(bzz_denormalize_multiple(element))
		elif element is Dictionary:
			# TODO: perhaps check first if it looks like a match or an intermediate container
			mes.append(bzz_denormalize_single(element))
		else:
			mes.append(element)
	return mes

