extends ApiBee
class_name CategoryApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API CategoryApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "CategoryApi"


# Operation categoryDistanceSearch → GET /category/distancesearch
# Search Categories by Distance
#
# Search for categories by distance.
func category_distance_search(
	# accountId: float   Eg: 789
	# The account id of the user
	accountId = null,
	# keyword: String = ""   Eg: keyword_example
	# The keyword string to search on
	keyword = "",
	# appKey: String = ""   Eg: appKey_example
	# the appKey of the application to retrieve categories for, if not specified then search on the global application.
	appKey = "",
	# categoryIds: String = ""   Eg: categoryIds_example
	# Restrict the search by specific categories
	categoryIds = "",
	# parentCategoryIds: String = ""   Eg: parentCategoryIds_example
	# Restrict the search by specific parent categories so that only its sub children are searched.
	parentCategoryIds = "",
	# rootOnly: bool   Eg: true
	# Restrict the search to only those categories with no parent category assigned.
	rootOnly = null,
	# sortField: String = "DISPLAY"   Eg: sortField_example
	# The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, NAME, DISPLAY
	sortField = "DISPLAY",
	# responseGroup: String = ""   Eg: responseGroup_example
	# The group of categories to return: ALL, GLOBAL or MINE. ALL returns both global and application specific matches, GLOBAL only returns global matches, and MINE only returns the application specific matches. If not provided search on the application provided.
	responseGroup = "",
	# descending: bool = false   Eg: true
	# The order to return the search results
	descending = false,
	# start: int = 0   Eg: 56
	# The record to begin the return set on
	start = 0,
	# limit: int = 20   Eg: 56
	# The number of records to return
	limit = 20,
	# activeOnly: bool = true   Eg: true
	# Determines whether to return only active categories
	activeOnly = true,
	# returnExternal: bool   Eg: true
	# Determines whether to return extra info about the category's \"Participant\" reference
	returnExternal = null,
	# exactMatch: bool   Eg: true
	# If true search categories using the exact keyword, if false then do a partial match (like) search.
	exactMatch = null,
	# type: String = ""   Eg: type_example
	# Filters results by the Category's type
	type = "",
	# externalType: String = ""   Eg: externalType_example
	# Filters results by externalType
	externalType = "",
	# minOfferCount: int   Eg: 56
	# Filters results to only return Categories that have been referenced by a minimum number of Offers
	minOfferCount = null,
	# latitude: int   Eg: 1.2
	# the latitude of where the search is centered on
	latitude = null,
	# longitude: int   Eg: 1.2
	# the longitude of where the search is centered on
	longitude = null,
	# some_range: int   Eg: 1.2
	# the maximum range the category can be from the center
	some_range = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/category/distancesearch"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["keyword"] = keyword
	bzz_query["appKey"] = appKey
	bzz_query["categoryIds"] = categoryIds
	bzz_query["parentCategoryIds"] = parentCategoryIds
	bzz_query["rootOnly"] = rootOnly
	bzz_query["sortField"] = sortField
	bzz_query["responseGroup"] = responseGroup
	bzz_query["descending"] = descending
	bzz_query["start"] = start
	bzz_query["limit"] = limit
	bzz_query["activeOnly"] = activeOnly
	bzz_query["returnExternal"] = returnExternal
	bzz_query["exactMatch"] = exactMatch
	bzz_query["type"] = type
	bzz_query["externalType"] = externalType
	bzz_query["minOfferCount"] = minOfferCount
	bzz_query["latitude"] = latitude
	bzz_query["longitude"] = longitude
	bzz_query["range"] = some_range

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CategoryResponse.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func category_distance_search_threaded(
	# accountId: float   Eg: 789
	# The account id of the user
	accountId = null,
	# keyword: String = ""   Eg: keyword_example
	# The keyword string to search on
	keyword = "",
	# appKey: String = ""   Eg: appKey_example
	# the appKey of the application to retrieve categories for, if not specified then search on the global application.
	appKey = "",
	# categoryIds: String = ""   Eg: categoryIds_example
	# Restrict the search by specific categories
	categoryIds = "",
	# parentCategoryIds: String = ""   Eg: parentCategoryIds_example
	# Restrict the search by specific parent categories so that only its sub children are searched.
	parentCategoryIds = "",
	# rootOnly: bool   Eg: true
	# Restrict the search to only those categories with no parent category assigned.
	rootOnly = null,
	# sortField: String = "DISPLAY"   Eg: sortField_example
	# The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, NAME, DISPLAY
	sortField = "DISPLAY",
	# responseGroup: String = ""   Eg: responseGroup_example
	# The group of categories to return: ALL, GLOBAL or MINE. ALL returns both global and application specific matches, GLOBAL only returns global matches, and MINE only returns the application specific matches. If not provided search on the application provided.
	responseGroup = "",
	# descending: bool = false   Eg: true
	# The order to return the search results
	descending = false,
	# start: int = 0   Eg: 56
	# The record to begin the return set on
	start = 0,
	# limit: int = 20   Eg: 56
	# The number of records to return
	limit = 20,
	# activeOnly: bool = true   Eg: true
	# Determines whether to return only active categories
	activeOnly = true,
	# returnExternal: bool   Eg: true
	# Determines whether to return extra info about the category's \"Participant\" reference
	returnExternal = null,
	# exactMatch: bool   Eg: true
	# If true search categories using the exact keyword, if false then do a partial match (like) search.
	exactMatch = null,
	# type: String = ""   Eg: type_example
	# Filters results by the Category's type
	type = "",
	# externalType: String = ""   Eg: externalType_example
	# Filters results by externalType
	externalType = "",
	# minOfferCount: int   Eg: 56
	# Filters results to only return Categories that have been referenced by a minimum number of Offers
	minOfferCount = null,
	# latitude: int   Eg: 1.2
	# the latitude of where the search is centered on
	latitude = null,
	# longitude: int   Eg: 1.2
	# the longitude of where the search is centered on
	longitude = null,
	# some_range: int   Eg: 1.2
	# the maximum range the category can be from the center
	some_range = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "category_distance_search")
	bzz_callable.bind(
		accountId,
		keyword,
		appKey,
		categoryIds,
		parentCategoryIds,
		rootOnly,
		sortField,
		responseGroup,
		descending,
		start,
		limit,
		activeOnly,
		returnExternal,
		exactMatch,
		type,
		externalType,
		minOfferCount,
		latitude,
		longitude,
		some_range,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation createCategory → POST /category/create
# Create Category
#
# Create a new category.
func create_category(
	# accountId: float   Eg: 789
	# The account id of the user (must have permissions to the target application)
	accountId: float,
	# name: String = ""   Eg: name_example
	# The name of the category
	name: String,
	# appKey: String = ""   Eg: appKey_example
	# The appKey of the application to assign the category to, if not provided then the category will be applied to the global application (if the account has permissions)
	appKey = "",
	# parentCategoryId: float   Eg: 789
	# The ID of the parent category, if not provided then the parent category will be null
	parentCategoryId = null,
	# description: String = ""   Eg: description_example
	# The description of the category
	description = "",
	# type: String = ""   Eg: type_example
	# The type of the category
	type = "",
	# assetId: float   Eg: 789
	# The ID of the image asset previously uploaded using the media service
	assetId = null,
	# externalId: String = ""   Eg: externalId_example
	# A string identifier used by client applications to store external information
	externalId = "",
	# externalType: String = ""   Eg: externalType_example
	# A string type used by client applications to store external information
	externalType = "",
	# externalCategorySlug: String = ""   Eg: externalCategorySlug_example
	# external category slug
	externalCategorySlug = "",
	# sqootSlug: String = ""   Eg: sqootSlug_example
	# sqoot slug
	sqootSlug = "",
	# active: bool   Eg: true
	# Sets whether the category is active or inactive (hidden from consumers)
	active = null,
	# metaData: String = ""   Eg: metaData_example
	# external custom client defined data
	metaData = "",
	# searchTags: String = ""   Eg: searchTags_example
	# user defined strings for searching
	searchTags = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/category/create"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["appKey"] = appKey
	bzz_query["accountId"] = accountId
	bzz_query["parentCategoryId"] = parentCategoryId
	bzz_query["name"] = name
	bzz_query["description"] = description
	bzz_query["type"] = type
	bzz_query["assetId"] = assetId
	bzz_query["externalId"] = externalId
	bzz_query["externalType"] = externalType
	bzz_query["externalCategorySlug"] = externalCategorySlug
	bzz_query["sqootSlug"] = sqootSlug
	bzz_query["active"] = active
	bzz_query["metaData"] = metaData
	bzz_query["searchTags"] = searchTags

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CategoryTreeResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_category_threaded(
	# accountId: float   Eg: 789
	# The account id of the user (must have permissions to the target application)
	accountId: float,
	# name: String = ""   Eg: name_example
	# The name of the category
	name: String,
	# appKey: String = ""   Eg: appKey_example
	# The appKey of the application to assign the category to, if not provided then the category will be applied to the global application (if the account has permissions)
	appKey = "",
	# parentCategoryId: float   Eg: 789
	# The ID of the parent category, if not provided then the parent category will be null
	parentCategoryId = null,
	# description: String = ""   Eg: description_example
	# The description of the category
	description = "",
	# type: String = ""   Eg: type_example
	# The type of the category
	type = "",
	# assetId: float   Eg: 789
	# The ID of the image asset previously uploaded using the media service
	assetId = null,
	# externalId: String = ""   Eg: externalId_example
	# A string identifier used by client applications to store external information
	externalId = "",
	# externalType: String = ""   Eg: externalType_example
	# A string type used by client applications to store external information
	externalType = "",
	# externalCategorySlug: String = ""   Eg: externalCategorySlug_example
	# external category slug
	externalCategorySlug = "",
	# sqootSlug: String = ""   Eg: sqootSlug_example
	# sqoot slug
	sqootSlug = "",
	# active: bool   Eg: true
	# Sets whether the category is active or inactive (hidden from consumers)
	active = null,
	# metaData: String = ""   Eg: metaData_example
	# external custom client defined data
	metaData = "",
	# searchTags: String = ""   Eg: searchTags_example
	# user defined strings for searching
	searchTags = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_category")
	bzz_callable.bind(
		accountId,
		name,
		appKey,
		parentCategoryId,
		description,
		type,
		assetId,
		externalId,
		externalType,
		externalCategorySlug,
		sqootSlug,
		active,
		metaData,
		searchTags,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation deleteCategory → POST /category/delete
# Delete Category
#
# Delete a category.
func delete_category(
	# accountId: float   Eg: 789
	# the ID of the account
	accountId: float,
	# categoryId: float   Eg: 789
	# the ID of the category
	categoryId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/category/delete"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["categoryId"] = categoryId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func delete_category_threaded(
	# accountId: float   Eg: 789
	# the ID of the account
	accountId: float,
	# categoryId: float   Eg: 789
	# the ID of the category
	categoryId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "delete_category")
	bzz_callable.bind(
		accountId,
		categoryId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation duplicateCategory → POST /category/duplicate
# Duplicate Category
#
# Duplicate a category, including all its children.
func duplicate_category(
	# accountId: float   Eg: 789
	# The account id of the user (must have permissions to the target application)
	accountId: float,
	# categoryId: float   Eg: 789
	# The category ID to duplicate (includes all children)
	categoryId: float,
	# appKey: String = ""   Eg: appKey_example
	# The application to assign the new category to, may be different then the application the source category is assigned to
	appKey = "",
	# parentCategoryId: float   Eg: 789
	# The parent category ID to add the target category to.
	parentCategoryId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/category/duplicate"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["appKey"] = appKey
	bzz_query["accountId"] = accountId
	bzz_query["categoryId"] = categoryId
	bzz_query["parentCategoryId"] = parentCategoryId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CategoryTreeResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func duplicate_category_threaded(
	# accountId: float   Eg: 789
	# The account id of the user (must have permissions to the target application)
	accountId: float,
	# categoryId: float   Eg: 789
	# The category ID to duplicate (includes all children)
	categoryId: float,
	# appKey: String = ""   Eg: appKey_example
	# The application to assign the new category to, may be different then the application the source category is assigned to
	appKey = "",
	# parentCategoryId: float   Eg: 789
	# The parent category ID to add the target category to.
	parentCategoryId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "duplicate_category")
	bzz_callable.bind(
		accountId,
		categoryId,
		appKey,
		parentCategoryId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getCategory → GET /category/get
# Get Category
#
# Get the details of a specific category. Recursively include all child categories and their children.
func get_category(
	# categoryId: float   Eg: 789
	# the ID of the category
	categoryId: float,
	# returnExternal: bool = true   Eg: true
	# Determines whether to return extra info about the category's \"Participant\" reference
	returnExternal = true,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/category/get"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["categoryId"] = categoryId
	bzz_query["returnExternal"] = returnExternal

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CategoryTreeResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_category_threaded(
	# categoryId: float   Eg: 789
	# the ID of the category
	categoryId: float,
	# returnExternal: bool = true   Eg: true
	# Determines whether to return extra info about the category's \"Participant\" reference
	returnExternal = true,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_category")
	bzz_callable.bind(
		categoryId,
		returnExternal,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation searchCategories → GET /category/search
# Search Categories
#
# Search for categories.
func search_categories(
	# accountId: float   Eg: 789
	# The account id of the user
	accountId = null,
	# keyword: String = ""   Eg: keyword_example
	# The string to search on
	keyword = "",
	# appKey: String = ""   Eg: appKey_example
	# the appKey of the application to retrieve categories for, if not specified then search on the global application.
	appKey = "",
	# categoryId: String = ""   Eg: categoryId_example
	# @Deprecated, use parentCategoryIds instead
	categoryId = "",
	# categoryIds: String = ""   Eg: categoryIds_example
	# Restrict the search by specific categories
	categoryIds = "",
	# parentCategoryIds: String = ""   Eg: parentCategoryIds_example
	# Restrict the search by specific parent categories so that only its sub children are searched.
	parentCategoryIds = "",
	# rootOnly: bool   Eg: true
	# Restrict the search to only those categories with no parent category assigned.
	rootOnly = null,
	# sortField: String = "DISPLAY"   Eg: sortField_example
	# The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, NAME, DISPLAY
	sortField = "DISPLAY",
	# responseGroup: String = ""   Eg: responseGroup_example
	# The group of categories to return: ALL, GLOBAL or MINE. ALL returns both global and application specific matches, GLOBAL only returns global matches, and MINE only returns the application specific matches. If not provided search on the application provided.
	responseGroup = "",
	# descending: bool = false   Eg: true
	# The order to return the search results
	descending = false,
	# start: int = 0   Eg: 56
	# The record to begin the return set on
	start = 0,
	# limit: int = 20   Eg: 56
	# The number of records to return
	limit = 20,
	# activeOnly: bool = true   Eg: true
	# Determines whether to return only active categories
	activeOnly = true,
	# returnExternal: bool = true   Eg: true
	# Determines whether to return extra info about the category's \"Participant\" reference
	returnExternal = true,
	# exactMatch: bool = false   Eg: true
	# If true search categories using the exact keyword, if false then do a partial match (like) search. Default is false.
	exactMatch = false,
	# type: String = ""   Eg: type_example
	# Filters results by the Category's type
	type = "",
	# externalType: String = ""   Eg: externalType_example
	# Filters results by externalType
	externalType = "",
	# excludeExternalType: bool   Eg: true
	# Determines whether the \"externalType\" param is excluded from the search results
	excludeExternalType = null,
	# minOfferCount: int   Eg: 56
	# Filters results to only return Categories that have been referenced by a minimum number of Offers
	minOfferCount = null,
	# searchDepth: int = 4   Eg: 56
	# When searching by a specific parent category (to return sub children), this determines the number of child layers to search in. The minimum is 1, the maximum is 4.
	searchDepth = 4,
	# searchMode: String = ""   Eg: searchMode_example
	# The search index mode to use (e.g. OPENSEARCH or RDS)
	searchMode = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/category/search"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["keyword"] = keyword
	bzz_query["appKey"] = appKey
	bzz_query["categoryId"] = categoryId
	bzz_query["categoryIds"] = categoryIds
	bzz_query["parentCategoryIds"] = parentCategoryIds
	bzz_query["rootOnly"] = rootOnly
	bzz_query["sortField"] = sortField
	bzz_query["responseGroup"] = responseGroup
	bzz_query["descending"] = descending
	bzz_query["start"] = start
	bzz_query["limit"] = limit
	bzz_query["activeOnly"] = activeOnly
	bzz_query["returnExternal"] = returnExternal
	bzz_query["exactMatch"] = exactMatch
	bzz_query["type"] = type
	bzz_query["externalType"] = externalType
	bzz_query["excludeExternalType"] = excludeExternalType
	bzz_query["minOfferCount"] = minOfferCount
	bzz_query["searchDepth"] = searchDepth
	bzz_query["searchMode"] = searchMode

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CategoryResponse.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func search_categories_threaded(
	# accountId: float   Eg: 789
	# The account id of the user
	accountId = null,
	# keyword: String = ""   Eg: keyword_example
	# The string to search on
	keyword = "",
	# appKey: String = ""   Eg: appKey_example
	# the appKey of the application to retrieve categories for, if not specified then search on the global application.
	appKey = "",
	# categoryId: String = ""   Eg: categoryId_example
	# @Deprecated, use parentCategoryIds instead
	categoryId = "",
	# categoryIds: String = ""   Eg: categoryIds_example
	# Restrict the search by specific categories
	categoryIds = "",
	# parentCategoryIds: String = ""   Eg: parentCategoryIds_example
	# Restrict the search by specific parent categories so that only its sub children are searched.
	parentCategoryIds = "",
	# rootOnly: bool   Eg: true
	# Restrict the search to only those categories with no parent category assigned.
	rootOnly = null,
	# sortField: String = "DISPLAY"   Eg: sortField_example
	# The column to sort the search on. Possible values include: ID, CREATED, UPDATED, ACTIVE, NAME, DISPLAY
	sortField = "DISPLAY",
	# responseGroup: String = ""   Eg: responseGroup_example
	# The group of categories to return: ALL, GLOBAL or MINE. ALL returns both global and application specific matches, GLOBAL only returns global matches, and MINE only returns the application specific matches. If not provided search on the application provided.
	responseGroup = "",
	# descending: bool = false   Eg: true
	# The order to return the search results
	descending = false,
	# start: int = 0   Eg: 56
	# The record to begin the return set on
	start = 0,
	# limit: int = 20   Eg: 56
	# The number of records to return
	limit = 20,
	# activeOnly: bool = true   Eg: true
	# Determines whether to return only active categories
	activeOnly = true,
	# returnExternal: bool = true   Eg: true
	# Determines whether to return extra info about the category's \"Participant\" reference
	returnExternal = true,
	# exactMatch: bool = false   Eg: true
	# If true search categories using the exact keyword, if false then do a partial match (like) search. Default is false.
	exactMatch = false,
	# type: String = ""   Eg: type_example
	# Filters results by the Category's type
	type = "",
	# externalType: String = ""   Eg: externalType_example
	# Filters results by externalType
	externalType = "",
	# excludeExternalType: bool   Eg: true
	# Determines whether the \"externalType\" param is excluded from the search results
	excludeExternalType = null,
	# minOfferCount: int   Eg: 56
	# Filters results to only return Categories that have been referenced by a minimum number of Offers
	minOfferCount = null,
	# searchDepth: int = 4   Eg: 56
	# When searching by a specific parent category (to return sub children), this determines the number of child layers to search in. The minimum is 1, the maximum is 4.
	searchDepth = 4,
	# searchMode: String = ""   Eg: searchMode_example
	# The search index mode to use (e.g. OPENSEARCH or RDS)
	searchMode = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "search_categories")
	bzz_callable.bind(
		accountId,
		keyword,
		appKey,
		categoryId,
		categoryIds,
		parentCategoryIds,
		rootOnly,
		sortField,
		responseGroup,
		descending,
		start,
		limit,
		activeOnly,
		returnExternal,
		exactMatch,
		type,
		externalType,
		excludeExternalType,
		minOfferCount,
		searchDepth,
		searchMode,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation updateCategory → POST /category/update
# Update Category
#
# Update a category.
func update_category(
	# accountId: float   Eg: 789
	# The account id of the user
	accountId: float,
	# categoryId: float   Eg: 789
	# The ID of the category to edit
	categoryId: float,
	# parentCategoryId: float   Eg: 789
	# The ID of the parent category, if not provided then the parent category will be null
	parentCategoryId = null,
	# name: String = ""   Eg: name_example
	# The name of the category
	name = "",
	# description: String = ""   Eg: description_example
	# The description of the category
	description = "",
	# type: String = ""   Eg: type_example
	# The type of the category
	type = "",
	# assetId: float   Eg: 789
	# The ID of the image asset previously uploaded using the media service
	assetId = null,
	# externalId: String = ""   Eg: externalId_example
	# A string identifier used by client applications to store external information
	externalId = "",
	# externalType: String = ""   Eg: externalType_example
	# A string type used by client applications to store external information
	externalType = "",
	# externalCategorySlug: String = ""   Eg: externalCategorySlug_example
	# external category slug
	externalCategorySlug = "",
	# sqootSlug: String = ""   Eg: sqootSlug_example
	# sqoot slug
	sqootSlug = "",
	# active: bool   Eg: true
	# Sets whether the category is active or inactive (hidden from consumers)
	active = null,
	# metaData: String = ""   Eg: metaData_example
	# external custom client defined data
	metaData = "",
	# searchTags: String = ""   Eg: searchTags_example
	# user defined strings for searching
	searchTags = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/category/update"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["categoryId"] = categoryId
	bzz_query["parentCategoryId"] = parentCategoryId
	bzz_query["name"] = name
	bzz_query["description"] = description
	bzz_query["type"] = type
	bzz_query["assetId"] = assetId
	bzz_query["externalId"] = externalId
	bzz_query["externalType"] = externalType
	bzz_query["externalCategorySlug"] = externalCategorySlug
	bzz_query["sqootSlug"] = sqootSlug
	bzz_query["active"] = active
	bzz_query["metaData"] = metaData
	bzz_query["searchTags"] = searchTags

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CategoryTreeResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func update_category_threaded(
	# accountId: float   Eg: 789
	# The account id of the user
	accountId: float,
	# categoryId: float   Eg: 789
	# The ID of the category to edit
	categoryId: float,
	# parentCategoryId: float   Eg: 789
	# The ID of the parent category, if not provided then the parent category will be null
	parentCategoryId = null,
	# name: String = ""   Eg: name_example
	# The name of the category
	name = "",
	# description: String = ""   Eg: description_example
	# The description of the category
	description = "",
	# type: String = ""   Eg: type_example
	# The type of the category
	type = "",
	# assetId: float   Eg: 789
	# The ID of the image asset previously uploaded using the media service
	assetId = null,
	# externalId: String = ""   Eg: externalId_example
	# A string identifier used by client applications to store external information
	externalId = "",
	# externalType: String = ""   Eg: externalType_example
	# A string type used by client applications to store external information
	externalType = "",
	# externalCategorySlug: String = ""   Eg: externalCategorySlug_example
	# external category slug
	externalCategorySlug = "",
	# sqootSlug: String = ""   Eg: sqootSlug_example
	# sqoot slug
	sqootSlug = "",
	# active: bool   Eg: true
	# Sets whether the category is active or inactive (hidden from consumers)
	active = null,
	# metaData: String = ""   Eg: metaData_example
	# external custom client defined data
	metaData = "",
	# searchTags: String = ""   Eg: searchTags_example
	# user defined strings for searching
	searchTags = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "update_category")
	bzz_callable.bind(
		accountId,
		categoryId,
		parentCategoryId,
		name,
		description,
		type,
		assetId,
		externalId,
		externalType,
		externalCategorySlug,
		sqootSlug,
		active,
		metaData,
		searchTags,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


