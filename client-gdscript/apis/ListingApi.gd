extends ApiBee
class_name ListingApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API ListingApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "ListingApi"


# Operation createListing → POST /listing/create
# Create Listing
#
# Creates a listing.
func create_listing(
	# accountId: float   Eg: 789
	# the user's account ID
	accountId: float,
	# name: String = ""   Eg: name_example
	# the name of the listing
	name: String,
	# filterIds: String = ""   Eg: filterIds_example
	# comma separated list of filter IDs
	filterIds = "",
	# description: String = ""   Eg: description_example
	# the description of the listing
	description = "",
	# start: float   Eg: 789
	# the start date of the listing
	start = null,
	# end: float   Eg: 789
	# the end date of the listing
	end = null,
	# locationName: String = ""   Eg: locationName_example
	# the name of the location the listing will be held at
	locationName = "",
	# locationDescription: String = ""   Eg: locationDescription_example
	# the description of the location the listing will be held at
	locationDescription = "",
	# isPrivate: bool = false   Eg: true
	# determines whether the listing is public or private
	isPrivate = false,
	# externalId: String = ""   Eg: externalId_example
	# external identifier used by a third party
	externalId = "",
	# externalId2: String = ""   Eg: externalId2_example
	# secondary external identifier used by a third party
	externalId2 = "",
	# externalGroupId: String = ""   Eg: externalGroupId_example
	# external group identifier used by a third party
	externalGroupId = "",
	# active: bool   Eg: true
	# Sets the active flag
	active = null,
	# metaData: String = ""   Eg: metaData_example
	# external custom client defined data
	metaData = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/listing/create"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["name"] = name
	bzz_query["filterIds"] = filterIds
	bzz_query["description"] = description
	bzz_query["start"] = start
	bzz_query["end"] = end
	bzz_query["locationName"] = locationName
	bzz_query["locationDescription"] = locationDescription
	bzz_query["isPrivate"] = isPrivate
	bzz_query["externalId"] = externalId
	bzz_query["externalId2"] = externalId2
	bzz_query["externalGroupId"] = externalGroupId
	bzz_query["active"] = active
	bzz_query["metaData"] = metaData

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ListingFullResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_listing_threaded(
	# accountId: float   Eg: 789
	# the user's account ID
	accountId: float,
	# name: String = ""   Eg: name_example
	# the name of the listing
	name: String,
	# filterIds: String = ""   Eg: filterIds_example
	# comma separated list of filter IDs
	filterIds = "",
	# description: String = ""   Eg: description_example
	# the description of the listing
	description = "",
	# start: float   Eg: 789
	# the start date of the listing
	start = null,
	# end: float   Eg: 789
	# the end date of the listing
	end = null,
	# locationName: String = ""   Eg: locationName_example
	# the name of the location the listing will be held at
	locationName = "",
	# locationDescription: String = ""   Eg: locationDescription_example
	# the description of the location the listing will be held at
	locationDescription = "",
	# isPrivate: bool = false   Eg: true
	# determines whether the listing is public or private
	isPrivate = false,
	# externalId: String = ""   Eg: externalId_example
	# external identifier used by a third party
	externalId = "",
	# externalId2: String = ""   Eg: externalId2_example
	# secondary external identifier used by a third party
	externalId2 = "",
	# externalGroupId: String = ""   Eg: externalGroupId_example
	# external group identifier used by a third party
	externalGroupId = "",
	# active: bool   Eg: true
	# Sets the active flag
	active = null,
	# metaData: String = ""   Eg: metaData_example
	# external custom client defined data
	metaData = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_listing")
	bzz_callable.bind(
		accountId,
		name,
		filterIds,
		description,
		start,
		end,
		locationName,
		locationDescription,
		isPrivate,
		externalId,
		externalId2,
		externalGroupId,
		active,
		metaData,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation deleteListing → POST /listing/delete
# Delete Listing
#
# Delete a listing.
func delete_listing(
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId: float,
	# listingId: float   Eg: 789
	# the id of the listing to delete
	listingId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/listing/delete"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["listingId"] = listingId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func delete_listing_threaded(
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId: float,
	# listingId: float   Eg: 789
	# the id of the listing to delete
	listingId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "delete_listing")
	bzz_callable.bind(
		accountId,
		listingId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getListing → GET /listing/get
# Get Listing
#
# Get a listing by id.
func get_listing(
	# listingId: float   Eg: 789
	# the id of the listing to get
	listingId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/listing/get"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["listingId"] = listingId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ListingFullResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_listing_threaded(
	# listingId: float   Eg: 789
	# the id of the listing to get
	listingId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_listing")
	bzz_callable.bind(
		listingId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation searchListing → GET /listing/search
# Search Listings
#
# Search for event listings from the start time to end time
func search_listing(
	# accountId: float   Eg: 789
	# the account id of the user
	accountId = null,
	# keyword: String = ""   Eg: keyword_example
	# search the event name and description for this keyword
	keyword = "",
	# start: int = 0   Eg: 56
	# the record to begin the return set on
	start = 0,
	# limit: int = 20   Eg: 56
	# the number of records to return
	limit = 20,
	# activeOnly: bool = false   Eg: true
	# whether to search on active listings only
	activeOnly = false,
	# latitude: int   Eg: 1.2
	# the current latitude of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# the current longitude of the user
	longitude = null,
	# startDate: float   Eg: 789
	# the start date to search from
	startDate = null,
	# endDate: float   Eg: 789
	# the end date to search to
	endDate = null,
	# categoryIds: String = ""   Eg: categoryIds_example
	# only return items of these categories
	categoryIds = "",
	# filterIds: String = ""   Eg: filterIds_example
	filterIds = "",
	# useListingOrderIds: bool = true   Eg: true
	# determines whether to use configured listing order ids
	useListingOrderIds = true,
	# externalId: String = ""   Eg: externalId_example
	# external identifier used by a third party
	externalId = "",
	# externalId2: String = ""   Eg: externalId2_example
	# secondary external identifier used by a third party
	externalId2 = "",
	# externalGroupId: String = ""   Eg: externalGroupId_example
	# external group identifier used by a third party
	externalGroupId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/listing/search"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["keyword"] = keyword
	bzz_query["start"] = start
	bzz_query["limit"] = limit
	bzz_query["activeOnly"] = activeOnly
	bzz_query["latitude"] = latitude
	bzz_query["longitude"] = longitude
	bzz_query["startDate"] = startDate
	bzz_query["endDate"] = endDate
	bzz_query["categoryIds"] = categoryIds
	bzz_query["filterIds"] = filterIds
	bzz_query["useListingOrderIds"] = useListingOrderIds
	bzz_query["externalId"] = externalId
	bzz_query["externalId2"] = externalId2
	bzz_query["externalGroupId"] = externalGroupId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ListingResponse.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func search_listing_threaded(
	# accountId: float   Eg: 789
	# the account id of the user
	accountId = null,
	# keyword: String = ""   Eg: keyword_example
	# search the event name and description for this keyword
	keyword = "",
	# start: int = 0   Eg: 56
	# the record to begin the return set on
	start = 0,
	# limit: int = 20   Eg: 56
	# the number of records to return
	limit = 20,
	# activeOnly: bool = false   Eg: true
	# whether to search on active listings only
	activeOnly = false,
	# latitude: int   Eg: 1.2
	# the current latitude of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# the current longitude of the user
	longitude = null,
	# startDate: float   Eg: 789
	# the start date to search from
	startDate = null,
	# endDate: float   Eg: 789
	# the end date to search to
	endDate = null,
	# categoryIds: String = ""   Eg: categoryIds_example
	# only return items of these categories
	categoryIds = "",
	# filterIds: String = ""   Eg: filterIds_example
	filterIds = "",
	# useListingOrderIds: bool = true   Eg: true
	# determines whether to use configured listing order ids
	useListingOrderIds = true,
	# externalId: String = ""   Eg: externalId_example
	# external identifier used by a third party
	externalId = "",
	# externalId2: String = ""   Eg: externalId2_example
	# secondary external identifier used by a third party
	externalId2 = "",
	# externalGroupId: String = ""   Eg: externalGroupId_example
	# external group identifier used by a third party
	externalGroupId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "search_listing")
	bzz_callable.bind(
		accountId,
		keyword,
		start,
		limit,
		activeOnly,
		latitude,
		longitude,
		startDate,
		endDate,
		categoryIds,
		filterIds,
		useListingOrderIds,
		externalId,
		externalId2,
		externalGroupId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation summaryListing → GET /listing/summary
# Summary Listing
#
# Search for a list of summary listings from the start time up to 8 days out.
func summary_listing(
	# accountId: float   Eg: 789
	# the account id of the user
	accountId = null,
	# startDate: float   Eg: 789
	# the start date to search from
	startDate = null,
	# categoryIds: String = ""   Eg: categoryIds_example
	# the list of categories to search on
	categoryIds = "",
	# daysToInclude: int = 15   Eg: 56
	# how far out to search, in days
	daysToInclude = 15,
	# useListingOrderIds: bool = true   Eg: true
	# determines whether to use configured listing order ids
	useListingOrderIds = true,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/listing/summary"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["startDate"] = startDate
	bzz_query["categoryIds"] = categoryIds
	bzz_query["daysToInclude"] = daysToInclude
	bzz_query["useListingOrderIds"] = useListingOrderIds

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ListingGroupResponse.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func summary_listing_threaded(
	# accountId: float   Eg: 789
	# the account id of the user
	accountId = null,
	# startDate: float   Eg: 789
	# the start date to search from
	startDate = null,
	# categoryIds: String = ""   Eg: categoryIds_example
	# the list of categories to search on
	categoryIds = "",
	# daysToInclude: int = 15   Eg: 56
	# how far out to search, in days
	daysToInclude = 15,
	# useListingOrderIds: bool = true   Eg: true
	# determines whether to use configured listing order ids
	useListingOrderIds = true,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "summary_listing")
	bzz_callable.bind(
		accountId,
		startDate,
		categoryIds,
		daysToInclude,
		useListingOrderIds,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation updateListing → POST /listing/update
# Update Listing
#
# Updates a listing.
func update_listing(
	# accountId: float   Eg: 789
	# the user's account ID
	accountId: float,
	# listingId: float   Eg: 789
	# the listing to update
	listingId: float,
	# filterIds: String = ""   Eg: filterIds_example
	# comma separated list of filter IDs
	filterIds = "",
	# name: String = ""   Eg: name_example
	# the name of the listing
	name = "",
	# description: String = ""   Eg: description_example
	# the description of the listing
	description = "",
	# start: float   Eg: 789
	# the start date of the listing
	start = null,
	# end: float   Eg: 789
	# the end date of the listing
	end = null,
	# locationName: String = ""   Eg: locationName_example
	# the name of the location the listing will be held at
	locationName = "",
	# locationDescription: String = ""   Eg: locationDescription_example
	# the description of the location the listing will be held at
	locationDescription = "",
	# isPrivate: bool   Eg: true
	# determines whether the listing is public or private
	isPrivate = null,
	# externalId: String = ""   Eg: externalId_example
	# external identifier used by a third party
	externalId = "",
	# externalId2: String = ""   Eg: externalId2_example
	# secondary external identifier used by a third party
	externalId2 = "",
	# externalGroupId: String = ""   Eg: externalGroupId_example
	# external group identifier used by a third party
	externalGroupId = "",
	# active: bool   Eg: true
	# Sets the active flag
	active = null,
	# metaData: String = ""   Eg: metaData_example
	# external custom client defined data
	metaData = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/listing/update"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["listingId"] = listingId
	bzz_query["filterIds"] = filterIds
	bzz_query["name"] = name
	bzz_query["description"] = description
	bzz_query["start"] = start
	bzz_query["end"] = end
	bzz_query["locationName"] = locationName
	bzz_query["locationDescription"] = locationDescription
	bzz_query["isPrivate"] = isPrivate
	bzz_query["externalId"] = externalId
	bzz_query["externalId2"] = externalId2
	bzz_query["externalGroupId"] = externalGroupId
	bzz_query["active"] = active
	bzz_query["metaData"] = metaData

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ListingFullResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func update_listing_threaded(
	# accountId: float   Eg: 789
	# the user's account ID
	accountId: float,
	# listingId: float   Eg: 789
	# the listing to update
	listingId: float,
	# filterIds: String = ""   Eg: filterIds_example
	# comma separated list of filter IDs
	filterIds = "",
	# name: String = ""   Eg: name_example
	# the name of the listing
	name = "",
	# description: String = ""   Eg: description_example
	# the description of the listing
	description = "",
	# start: float   Eg: 789
	# the start date of the listing
	start = null,
	# end: float   Eg: 789
	# the end date of the listing
	end = null,
	# locationName: String = ""   Eg: locationName_example
	# the name of the location the listing will be held at
	locationName = "",
	# locationDescription: String = ""   Eg: locationDescription_example
	# the description of the location the listing will be held at
	locationDescription = "",
	# isPrivate: bool   Eg: true
	# determines whether the listing is public or private
	isPrivate = null,
	# externalId: String = ""   Eg: externalId_example
	# external identifier used by a third party
	externalId = "",
	# externalId2: String = ""   Eg: externalId2_example
	# secondary external identifier used by a third party
	externalId2 = "",
	# externalGroupId: String = ""   Eg: externalGroupId_example
	# external group identifier used by a third party
	externalGroupId = "",
	# active: bool   Eg: true
	# Sets the active flag
	active = null,
	# metaData: String = ""   Eg: metaData_example
	# external custom client defined data
	metaData = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "update_listing")
	bzz_callable.bind(
		accountId,
		listingId,
		filterIds,
		name,
		description,
		start,
		end,
		locationName,
		locationDescription,
		isPrivate,
		externalId,
		externalId2,
		externalGroupId,
		active,
		metaData,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


