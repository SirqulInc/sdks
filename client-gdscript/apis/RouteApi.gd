extends ApiBee
class_name RouteApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API RouteApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "RouteApi"


# Operation approveRoute → POST /api/{version}/route/{routeId}/approve
# Approve Route
#
# Approve a route
func approve_route(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to approve
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/approve".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Route.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func approve_route_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to approve
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "approve_route")
	bzz_callable.bind(
		version,
		routeId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation copyRoute → POST /api/{version}/route/{routeId}/copy
# Copy Route
#
# Make an copy of the given route with optional overriding properties
func copy_route(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to duplicate
	routeId: float,
	# body: Route
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/copy".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = body

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Route.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func copy_route_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to duplicate
	routeId: float,
	# body: Route
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "copy_route")
	bzz_callable.bind(
		version,
		routeId,
		body,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation createRoute → POST /api/{version}/route
# Create Route
#
# Create new route
func create_route(
	# version: float   Eg: 3.16
	version: float,
	# body: Route
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = body

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Route.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_route_threaded(
	# version: float   Eg: 3.16
	version: float,
	# body: Route
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_route")
	bzz_callable.bind(
		version,
		body,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation createRouteDirections → PUT /api/{version}/route/{routeId}/directions
# Update Route Directions
#
# Regenerate the directions of a route
func create_route_directions(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to update directions for
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("PUT")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/directions".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Direction.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_route_directions_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to update directions for
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_route_directions")
	bzz_callable.bind(
		version,
		routeId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation createRoutePolyline → PUT /api/{version}/route/{routeId}/polyline
# Create Route Polyline
#
# Update the polyline of the requested route
func create_route_polyline(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to create a polyline for
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("PUT")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/polyline".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Route.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_route_polyline_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to create a polyline for
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_route_polyline")
	bzz_callable.bind(
		version,
		routeId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation deleteRoute → DELETE /api/{version}/route/{routeId}
# Delete Route
#
# Delete an existing route
func delete_route(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("DELETE")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func delete_route_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "delete_route")
	bzz_callable.bind(
		version,
		routeId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation disapproveRoute → POST /api/{version}/route/{routeId}/disapprove
# Disapprove Route
#
# Disapprove a route
func disapprove_route(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to reject
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/disapprove".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Route.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func disapprove_route_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to reject
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "disapprove_route")
	bzz_callable.bind(
		version,
		routeId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getRoute → GET /api/{version}/route/{routeId}
# Get Route
#
# Get an existing route
func get_route(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to get
	routeId: float,
	# showInheritedProperties: bool   Eg: true
	# return inherited properties from parent or not
	showInheritedProperties: bool,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["showInheritedProperties"] = showInheritedProperties

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Route.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_route_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to get
	routeId: float,
	# showInheritedProperties: bool   Eg: true
	# return inherited properties from parent or not
	showInheritedProperties: bool,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_route")
	bzz_callable.bind(
		version,
		routeId,
		showInheritedProperties,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getRouteDirections → GET /api/{version}/route/{routeId}/directions
# Get Route Directions
#
# Get the directions of a route
func get_route_directions(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to get directions for
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/directions".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Direction.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_route_directions_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to get directions for
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_route_directions")
	bzz_callable.bind(
		version,
		routeId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getRouteShipments → GET /api/{version}/route/{routeId}/shipments
# Get Route Shipments
#
# Get the shipments on the requested route
func get_route_shipments(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to get shipments for
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/shipments".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Shipment.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_route_shipments_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to get shipments for
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_route_shipments")
	bzz_callable.bind(
		version,
		routeId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getRouteStop → GET /api/{version}/route/{routeId}/stop/{stopId}
# Get Route Stop
#
# Get the specific stop on a route
func get_route_stop(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to get stops for
	routeId: float,
	# stopId: float   Eg: 789
	# the id of the specific stop on the route
	stopId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/stop/{stopId}".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId)).replace("{" + "stopId" + "}", _bzz_urlize_path_param(stopId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Stop.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_route_stop_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to get stops for
	routeId: float,
	# stopId: float   Eg: 789
	# the id of the specific stop on the route
	stopId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_route_stop")
	bzz_callable.bind(
		version,
		routeId,
		stopId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getRouteStops → GET /api/{version}/route/{routeId}/stops
# Get Route Stops
#
# The stops of the route requested
func get_route_stops(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# confirmedOnly: bool   Eg: true
	# only get stops that have been confirmed or not
	confirmedOnly: bool,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/stops".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["confirmedOnly"] = confirmedOnly

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Stop.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_route_stops_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# confirmedOnly: bool   Eg: true
	# only get stops that have been confirmed or not
	confirmedOnly: bool,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_route_stops")
	bzz_callable.bind(
		version,
		routeId,
		confirmedOnly,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getShipmentsAtStop → GET /api/{version}/route/{routeId}/stop/{stopId}/shipments
# Get Shipments At Stop
#
# Get the list of shipments on the requested route at a stop
func get_shipments_at_stop(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# stopId: float   Eg: 789
	# the id of the stop to get shipments on
	stopId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/stop/{stopId}/shipments".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId)).replace("{" + "stopId" + "}", _bzz_urlize_path_param(stopId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Shipment.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_shipments_at_stop_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# stopId: float   Eg: 789
	# the id of the stop to get shipments on
	stopId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_shipments_at_stop")
	bzz_callable.bind(
		version,
		routeId,
		stopId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation optimizeRoute → POST /api/{version}/route/{routeId}/optimize
# Optimize Route
#
# Optimize a route. The optimization method based on how the server is configured.
func optimize_route(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to optimize
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/optimize".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func optimize_route_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to optimize
	routeId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "optimize_route")
	bzz_callable.bind(
		version,
		routeId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation removeStop → DELETE /api/{version}/route/{routeId}/stop/{stopId}
# Delete Stop
#
# Delete a stop on a route
func remove_stop(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# stopId: float   Eg: 789
	# the id of the specific stop to delete on the route
	stopId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("DELETE")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/stop/{stopId}".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId)).replace("{" + "stopId" + "}", _bzz_urlize_path_param(stopId))

	# Collect the headers
	var bzz_headers := Dictionary()

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func remove_stop_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# stopId: float   Eg: 789
	# the id of the specific stop to delete on the route
	stopId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "remove_stop")
	bzz_callable.bind(
		version,
		routeId,
		stopId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation reorderRouteStopsPatch → PATCH /api/{version}/route/{routeId}/stops/reorder
# Reorder Route Stops
#
# Reordering the stops on the route with and update route distance, time, direction, and polyline
func reorder_route_stops_patch(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# body: Array
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("PATCH")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/stops/reorder".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = body

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Stop.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func reorder_route_stops_patch_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# body: Array
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "reorder_route_stops_patch")
	bzz_callable.bind(
		version,
		routeId,
		body,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation reorderRouteStopsPost → POST /api/{version}/route/{routeId}/stops/reorder
# Reorder Route Stops
#
# Reordering the stops on the route with and update route distance, time, direction, and polyline
func reorder_route_stops_post(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# body: Array
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/stops/reorder".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = body

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Stop.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func reorder_route_stops_post_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# body: Array
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "reorder_route_stops_post")
	bzz_callable.bind(
		version,
		routeId,
		body,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation searchRoutes → GET /api/{version}/route
# Search Routes
#
# Search for routes.
func search_routes(
	# version: float   Eg: 3.16
	version: float,
	# sortField: String = ""   Eg: sortField_example
	# The field to sort by
	sortField: String,
	# descending: bool   Eg: true
	# Determines whether the sorted list is in descending or ascending order
	descending: bool,
	# start: int   Eg: 56
	# The start index for pagination
	start: int,
	# limit: int   Eg: 56
	# The limit for pagination
	limit: int,
	# activeOnly: bool   Eg: true
	# Return only active results
	activeOnly: bool,
	# includesEmpty: bool   Eg: true
	# Include empty routes or not
	includesEmpty: bool,
	# rootOnly: bool   Eg: true
	# Only return root instance routes
	rootOnly: bool,
	# showInheritedProperties: bool   Eg: true
	# Display root route properties
	showInheritedProperties: bool,
	# hubId: float   Eg: 789
	# Filter results by service hub
	hubId = null,
	# programId: float   Eg: 789
	# Filter results by program
	programId = null,
	# scheduledStart: float   Eg: 789
	# The start date to filter the results by
	scheduledStart = null,
	# scheduledEnd: float   Eg: 789
	# The end date to filter the results by
	scheduledEnd = null,
	# updatedStart: float   Eg: 789
	# The lower bound of updated date
	updatedStart = null,
	# updatedEnd: float   Eg: 789
	# The upper bound of updated date
	updatedEnd = null,
	# featured: bool   Eg: true
	# The route is featured or not
	featured = null,
	# seatCount: int   Eg: 56
	# Has at least this many seat available
	seatCount = null,
	# approved: bool   Eg: true
	# Has been approved or not
	approved = null,
	# started: bool   Eg: true
	# Has started or not
	started = null,
	# completed: bool   Eg: true
	# Has completed or not
	completed = null,
	# valid: bool   Eg: true
	# Is valid or not
	valid = null,
	# parentId: float   Eg: 789
	# If it is a recurring route based on the parent route
	parentId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["hubId"] = hubId
	bzz_query["programId"] = programId
	bzz_query["scheduledStart"] = scheduledStart
	bzz_query["scheduledEnd"] = scheduledEnd
	bzz_query["updatedStart"] = updatedStart
	bzz_query["updatedEnd"] = updatedEnd
	bzz_query["featured"] = featured
	bzz_query["seatCount"] = seatCount
	bzz_query["approved"] = approved
	bzz_query["started"] = started
	bzz_query["completed"] = completed
	bzz_query["valid"] = valid
	bzz_query["parentId"] = parentId
	bzz_query["sortField"] = sortField
	bzz_query["descending"] = descending
	bzz_query["start"] = start
	bzz_query["limit"] = limit
	bzz_query["activeOnly"] = activeOnly
	bzz_query["includesEmpty"] = includesEmpty
	bzz_query["rootOnly"] = rootOnly
	bzz_query["showInheritedProperties"] = showInheritedProperties

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Route.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func search_routes_threaded(
	# version: float   Eg: 3.16
	version: float,
	# sortField: String = ""   Eg: sortField_example
	# The field to sort by
	sortField: String,
	# descending: bool   Eg: true
	# Determines whether the sorted list is in descending or ascending order
	descending: bool,
	# start: int   Eg: 56
	# The start index for pagination
	start: int,
	# limit: int   Eg: 56
	# The limit for pagination
	limit: int,
	# activeOnly: bool   Eg: true
	# Return only active results
	activeOnly: bool,
	# includesEmpty: bool   Eg: true
	# Include empty routes or not
	includesEmpty: bool,
	# rootOnly: bool   Eg: true
	# Only return root instance routes
	rootOnly: bool,
	# showInheritedProperties: bool   Eg: true
	# Display root route properties
	showInheritedProperties: bool,
	# hubId: float   Eg: 789
	# Filter results by service hub
	hubId = null,
	# programId: float   Eg: 789
	# Filter results by program
	programId = null,
	# scheduledStart: float   Eg: 789
	# The start date to filter the results by
	scheduledStart = null,
	# scheduledEnd: float   Eg: 789
	# The end date to filter the results by
	scheduledEnd = null,
	# updatedStart: float   Eg: 789
	# The lower bound of updated date
	updatedStart = null,
	# updatedEnd: float   Eg: 789
	# The upper bound of updated date
	updatedEnd = null,
	# featured: bool   Eg: true
	# The route is featured or not
	featured = null,
	# seatCount: int   Eg: 56
	# Has at least this many seat available
	seatCount = null,
	# approved: bool   Eg: true
	# Has been approved or not
	approved = null,
	# started: bool   Eg: true
	# Has started or not
	started = null,
	# completed: bool   Eg: true
	# Has completed or not
	completed = null,
	# valid: bool   Eg: true
	# Is valid or not
	valid = null,
	# parentId: float   Eg: 789
	# If it is a recurring route based on the parent route
	parentId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "search_routes")
	bzz_callable.bind(
		version,
		sortField,
		descending,
		start,
		limit,
		activeOnly,
		includesEmpty,
		rootOnly,
		showInheritedProperties,
		hubId,
		programId,
		scheduledStart,
		scheduledEnd,
		updatedStart,
		updatedEnd,
		featured,
		seatCount,
		approved,
		started,
		completed,
		valid,
		parentId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation setDriver → POST /api/{version}/route/{id}/driver/{driverId}
# Set Driver
#
# Update the driver of the route.
func set_driver(
	# version: float   Eg: 3.16
	version: float,
	# id: float   Eg: 789
	# the id of the route
	id: float,
	# driverId: float   Eg: 789
	# the id of the driver
	driverId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{id}/driver/{driverId}".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "id" + "}", _bzz_urlize_path_param(id)).replace("{" + "driverId" + "}", _bzz_urlize_path_param(driverId))

	# Collect the headers
	var bzz_headers := Dictionary()

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func set_driver_threaded(
	# version: float   Eg: 3.16
	version: float,
	# id: float   Eg: 789
	# the id of the route
	id: float,
	# driverId: float   Eg: 789
	# the id of the driver
	driverId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "set_driver")
	bzz_callable.bind(
		version,
		id,
		driverId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation updateRoute → PUT /api/{version}/route/{routeId}
# Update Route
#
# Update an existing route
func update_route(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# body: Route
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("PUT")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = body

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Route.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func update_route_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route
	routeId: float,
	# body: Route
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "update_route")
	bzz_callable.bind(
		version,
		routeId,
		body,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation updateRouteStop → PUT /api/{version}/route/{routeId}/stop/{stopId}
# Update Route Stop
#
# Update a stop on a specified route
func update_route_stop(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to update stops for
	routeId: float,
	# stopId: float   Eg: 789
	# the id of the specific stop to update on the route
	stopId: float,
	# body: Stop
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("PUT")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/route/{routeId}/stop/{stopId}".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "routeId" + "}", _bzz_urlize_path_param(routeId)).replace("{" + "stopId" + "}", _bzz_urlize_path_param(stopId))

	# Collect the headers
	var bzz_headers := Dictionary()

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = body

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func update_route_stop_threaded(
	# version: float   Eg: 3.16
	version: float,
	# routeId: float   Eg: 789
	# the id of the route to update stops for
	routeId: float,
	# stopId: float   Eg: 789
	# the id of the specific stop to update on the route
	stopId: float,
	# body: Stop
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "update_route_stop")
	bzz_callable.bind(
		version,
		routeId,
		stopId,
		body,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


