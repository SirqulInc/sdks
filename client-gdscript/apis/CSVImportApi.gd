extends ApiBee
class_name CSVImportApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API CSVImportApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "CSVImportApi"


# Operation getStatusCSV → GET /api/{version}/csvimport/batch/status/details
# Detail Status
func get_status_csv(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId: float,
	# batchId: float   Eg: 789
	# the id of the batch
	batchId: float,
	# responseGroup: String = ""   Eg: responseGroup_example
	# The group of categories to return: SUMMARY, DETAILS, ERRORS, OR ALL
	responseGroup: String,
	# start: float   Eg: 789
	# the start of the pagination
	start: float,
	# limit: float   Eg: 789
	# the limit of the pagination
	limit: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/csvimport/batch/status/details".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["batchId"] = batchId
	bzz_query["responseGroup"] = responseGroup
	bzz_query["start"] = start
	bzz_query["limit"] = limit

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_status_csv_threaded(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId: float,
	# batchId: float   Eg: 789
	# the id of the batch
	batchId: float,
	# responseGroup: String = ""   Eg: responseGroup_example
	# The group of categories to return: SUMMARY, DETAILS, ERRORS, OR ALL
	responseGroup: String,
	# start: float   Eg: 789
	# the start of the pagination
	start: float,
	# limit: float   Eg: 789
	# the limit of the pagination
	limit: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_status_csv")
	bzz_callable.bind(
		version,
		accountId,
		batchId,
		responseGroup,
		start,
		limit,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation listStatusCSV → GET /api/{version}/csvimport/batch/list
# Search Status
#
# Retrieves batches for a user.
func list_status_csv(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the account
	accountId: float,
	# start: int   Eg: 56
	# the start of the pagination
	start: int,
	# limit: int   Eg: 56
	# the limit of the pagination
	limit: int,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/csvimport/batch/list".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["start"] = start
	bzz_query["limit"] = limit

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CsvImportResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func list_status_csv_threaded(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the account
	accountId: float,
	# start: int   Eg: 56
	# the start of the pagination
	start: int,
	# limit: int   Eg: 56
	# the limit of the pagination
	limit: int,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "list_status_csv")
	bzz_callable.bind(
		version,
		accountId,
		start,
		limit,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation statusCSV → GET /api/{version}/csvimport/batch/status
# Batch Status
#
# Checks status of batch upload.
func status_csv(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the account
	accountId: float,
	# batchId: float   Eg: 789
	# the id of the batch to get its status
	batchId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/csvimport/batch/status".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["batchId"] = batchId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CsvImportResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func status_csv_threaded(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the account
	accountId: float,
	# batchId: float   Eg: 789
	# the id of the batch to get its status
	batchId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "status_csv")
	bzz_callable.bind(
		version,
		accountId,
		batchId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation uploadCSV → POST /api/{version}/csvimport/upload
# Upload CSV
#
# Uploads a CSV import file.
func upload_csv(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the account
	accountId: float,
	# uploadType: String = ""   Eg: uploadType_example
	# the upload type: OFFERS, RETAILERS, RETAILERLOCATIONS, CATEGORIES, OR FILTERS
	uploadType: String,
	# importFile: String   Eg: BINARY_DATA_HERE
	# the import file to reference
	importFile: String,
	# fileFormat: String = ""   Eg: fileFormat_example
	# the format of the file
	fileFormat: String,
	# appKey: String = ""   Eg: appKey_example
	# the application key
	appKey = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/csvimport/upload".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["uploadType"] = uploadType
	bzz_query["importFile"] = importFile
	bzz_query["appKey"] = appKey
	bzz_query["fileFormat"] = fileFormat

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CsvImportResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func upload_csv_threaded(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the account
	accountId: float,
	# uploadType: String = ""   Eg: uploadType_example
	# the upload type: OFFERS, RETAILERS, RETAILERLOCATIONS, CATEGORIES, OR FILTERS
	uploadType: String,
	# importFile: String   Eg: BINARY_DATA_HERE
	# the import file to reference
	importFile: String,
	# fileFormat: String = ""   Eg: fileFormat_example
	# the format of the file
	fileFormat: String,
	# appKey: String = ""   Eg: appKey_example
	# the application key
	appKey = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "upload_csv")
	bzz_callable.bind(
		version,
		accountId,
		uploadType,
		importFile,
		fileFormat,
		appKey,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


