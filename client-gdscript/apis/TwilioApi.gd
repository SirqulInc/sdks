extends ApiBee
class_name TwilioApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API TwilioApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "TwilioApi"


# Operation smsBuyOffer → POST /api/{version}/sms/buyoffer/{appKey}
# Buy Offer by SMS
#
# Recieve an SMS payload from Twillio to purchase an offer.
func sms_buy_offer(
	# version: float   Eg: 3.16
	version: float,
	# appKey: String = ""   Eg: appKey_example
	# the application key
	appKey: String,
	# body: String = ""   Eg: body_example
	# the message of the text
	body: String,
	# from: String = ""   Eg: from_example
	# the sender of the sms
	from: String,
	# currencyType: String = ""   Eg: currencyType_example
	# the type of currency
	currencyType: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/sms/buyoffer/{appKey}".replace("{" + "version" + "}", _bzz_urlize_path_param(version)).replace("{" + "appKey" + "}", _bzz_urlize_path_param(appKey))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/xml']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["Body"] = body
	bzz_query["From"] = from
	bzz_query["currencyType"] = currencyType

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = TwiMLResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func sms_buy_offer_threaded(
	# version: float   Eg: 3.16
	version: float,
	# appKey: String = ""   Eg: appKey_example
	# the application key
	appKey: String,
	# body: String = ""   Eg: body_example
	# the message of the text
	body: String,
	# from: String = ""   Eg: from_example
	# the sender of the sms
	from: String,
	# currencyType: String = ""   Eg: currencyType_example
	# the type of currency
	currencyType: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "sms_buy_offer")
	bzz_callable.bind(
		version,
		appKey,
		body,
		from,
		currencyType,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


