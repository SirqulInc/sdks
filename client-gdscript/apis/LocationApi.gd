extends ApiBee
class_name LocationApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API LocationApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "LocationApi"


# Operation cacheTrilaterationData → POST /api/{version}/location/trilaterate/cache
# Create Trilateration Data with File
#
# Creates trilateration samples for a source device (i.e. a router).
func cache_trilateration_data(
	# version: float   Eg: 3.16
	version: float,
	# udid: String = ""   Eg: udid_example
	# The unique identifier of the source device
	udid: String,
	# sourceTime: float   Eg: 789
	# The current timestamp of the source device
	sourceTime = null,
	# minimumSampleSize: int   Eg: 56
	# the minimum number of Edysen devices that must be used to be able to trilaterate a device
	minimumSampleSize = null,
	# data: String = ""   Eg: data_example
	# The json formated sample data:  ```json {    \"count\": 2,   \"timespan\": 10,    \"samples\": [     {       \"deviceId\": \"device1\",       \"randomizedId\": true,        \"deviceSignature\": \"probe:xyz...\",        \"alternativeId\":\"adc123\",        \"rssi\": [-63, -75]     },      {       \"deviceId\": \"device2\",       \"randomizedId\": true,        \"deviceSignature\": \"probe:xyz...\",        \"alternativeId\": \"adc123\",        \"rssi\": [-83, -79]     }   ] } ``` 
	data = "",
	# dataFile: String   Eg: BINARY_DATA_HERE
	# Binary file containing data (multipart upload)
	dataFile = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/location/trilaterate/cache".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["udid"] = udid
	bzz_query["sourceTime"] = sourceTime
	bzz_query["minimumSampleSize"] = minimumSampleSize
	bzz_query["data"] = data
	bzz_query["dataFile"] = dataFile

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func cache_trilateration_data_threaded(
	# version: float   Eg: 3.16
	version: float,
	# udid: String = ""   Eg: udid_example
	# The unique identifier of the source device
	udid: String,
	# sourceTime: float   Eg: 789
	# The current timestamp of the source device
	sourceTime = null,
	# minimumSampleSize: int   Eg: 56
	# the minimum number of Edysen devices that must be used to be able to trilaterate a device
	minimumSampleSize = null,
	# data: String = ""   Eg: data_example
	# The json formated sample data:  ```json {    \"count\": 2,   \"timespan\": 10,    \"samples\": [     {       \"deviceId\": \"device1\",       \"randomizedId\": true,        \"deviceSignature\": \"probe:xyz...\",        \"alternativeId\":\"adc123\",        \"rssi\": [-63, -75]     },      {       \"deviceId\": \"device2\",       \"randomizedId\": true,        \"deviceSignature\": \"probe:xyz...\",        \"alternativeId\": \"adc123\",        \"rssi\": [-83, -79]     }   ] } ``` 
	data = "",
	# dataFile: String   Eg: BINARY_DATA_HERE
	# Binary file containing data (multipart upload)
	dataFile = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "cache_trilateration_data")
	bzz_callable.bind(
		version,
		udid,
		sourceTime,
		minimumSampleSize,
		data,
		dataFile,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation cacheTrilaterationDataGzip → POST /api/{version}/location/trilaterate/cache/submit
# Create Trilateration Data with Rest
#
# Creates trilateration samples for a source device (i.e. a router).
func cache_trilateration_data_gzip(
	# version: float   Eg: 3.16
	version: float,
	# body: TrilatCacheRequest
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/location/trilaterate/cache/submit".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = body

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func cache_trilateration_data_gzip_threaded(
	# version: float   Eg: 3.16
	version: float,
	# body: TrilatCacheRequest
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "cache_trilateration_data_gzip")
	bzz_callable.bind(
		version,
		body,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getLocationByIp → GET /api/{version}/location/ip
# Get Location by IP
#
# Get location information based on an IP address.
func get_location_by_ip(
	# version: float   Eg: 3.16
	version: float,
	# some_ip: String = ""   Eg: some_ip_example
	# the ip address of the client device
	some_ip = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/location/ip".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["ip"] = some_ip

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CoordsResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_location_by_ip_threaded(
	# version: float   Eg: 3.16
	version: float,
	# some_ip: String = ""   Eg: some_ip_example
	# the ip address of the client device
	some_ip = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_location_by_ip")
	bzz_callable.bind(
		version,
		some_ip,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getLocationByTrilateration → GET /api/{version}/account/location/trilaterate
# Get Location by Trilateration
#
# Send in device data and calculate a position based on signal strengths.
func get_location_by_trilateration(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# The account making the request, if provided the last know location will be updated
	accountId = null,
	# latitude: int   Eg: 1.2
	# The known GPS latitude to compare to the calculated version
	latitude = null,
	# longitude: int   Eg: 1.2
	# The known GPS longitude to compare to the calculated version
	longitude = null,
	# data: String = ""   Eg: data_example
	# The json formated sample data:  ```json {    \"count\": 2,   \"timespan\": 10,    \"samples\": [     {       \"deviceId\": \"device1\",       \"rssi\": [-63, -75]     },      {       \"deviceId\": \"device2\",       \"rssi\": [-83, -79]     }   ] } ``` 
	data = "",
	# responseFilters: String = ""   Eg: responseFilters_example
	# Optional response filters (not used currently)
	responseFilters = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/account/location/trilaterate".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["latitude"] = latitude
	bzz_query["longitude"] = longitude
	bzz_query["data"] = data
	bzz_query["responseFilters"] = responseFilters

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = GeoPointResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_location_by_trilateration_threaded(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# The account making the request, if provided the last know location will be updated
	accountId = null,
	# latitude: int   Eg: 1.2
	# The known GPS latitude to compare to the calculated version
	latitude = null,
	# longitude: int   Eg: 1.2
	# The known GPS longitude to compare to the calculated version
	longitude = null,
	# data: String = ""   Eg: data_example
	# The json formated sample data:  ```json {    \"count\": 2,   \"timespan\": 10,    \"samples\": [     {       \"deviceId\": \"device1\",       \"rssi\": [-63, -75]     },      {       \"deviceId\": \"device2\",       \"rssi\": [-83, -79]     }   ] } ``` 
	data = "",
	# responseFilters: String = ""   Eg: responseFilters_example
	# Optional response filters (not used currently)
	responseFilters = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_location_by_trilateration")
	bzz_callable.bind(
		version,
		accountId,
		latitude,
		longitude,
		data,
		responseFilters,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getLocations → GET /api/{version}/location/search
# Search Regions or Postal Codes
#
# Searches geographic locations by proximity via address or keyword.
func get_locations(
	# version: float   Eg: 3.16
	version: float,
	# deviceId: String = ""   Eg: deviceId_example
	# the device id
	deviceId = "",
	# accountId: float   Eg: 789
	# the account id
	accountId = null,
	# currentlatitude: int   Eg: 1.2
	# This parameter is deprecated.
	currentlatitude = null,
	# currentlongitude: int   Eg: 1.2
	# This parameter is deprecated.
	currentlongitude = null,
	# currentLatitude: int   Eg: 1.2
	# the current latitude of the user
	currentLatitude = null,
	# currentLongitude: int   Eg: 1.2
	# the current longitude of the user
	currentLongitude = null,
	# query: String = ""   Eg: query_example
	# the query results by keyword or address
	query = "",
	# zipcode: String = ""   Eg: zipcode_example
	# This parameter is deprecated.
	zipcode = "",
	# zipCode: String = ""   Eg: zipCode_example
	# the zip code to filter results
	zipCode = "",
	# selectedMaplatitude: int   Eg: 1.2
	# This parameter is deprecated.
	selectedMaplatitude = null,
	# selectedMaplongitude: int   Eg: 1.2
	# This parameter is deprecated.
	selectedMaplongitude = null,
	# selectedMapLatitude: int   Eg: 1.2
	# the latitude of where the search should originate from
	selectedMapLatitude = null,
	# selectedMapLongitude: int   Eg: 1.2
	# the longitude of where the search should originate from
	selectedMapLongitude = null,
	# searchRange: int = 5   Eg: 1.2
	# the search range of the search in miles
	searchRange = 5,
	# useGeocode: bool = false   Eg: true
	# determines whether to allow searches via address
	useGeocode = false,
	# i: int   Eg: 56
	# This parameter is deprecated.
	i = null,
	# start: int = 0   Eg: 56
	# the start index for pagination
	start = 0,
	# l: int   Eg: 56
	# This parameter is deprecated.
	l = null,
	# limit: int = 20   Eg: 56
	# the limit for pagination
	limit = 20,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/location/search".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["currentlatitude"] = currentlatitude
	bzz_query["currentlongitude"] = currentlongitude
	bzz_query["currentLatitude"] = currentLatitude
	bzz_query["currentLongitude"] = currentLongitude
	bzz_query["query"] = query
	bzz_query["zipcode"] = zipcode
	bzz_query["zipCode"] = zipCode
	bzz_query["selectedMaplatitude"] = selectedMaplatitude
	bzz_query["selectedMaplongitude"] = selectedMaplongitude
	bzz_query["selectedMapLatitude"] = selectedMapLatitude
	bzz_query["selectedMapLongitude"] = selectedMapLongitude
	bzz_query["searchRange"] = searchRange
	bzz_query["useGeocode"] = useGeocode
	bzz_query["_i"] = i
	bzz_query["start"] = start
	bzz_query["_l"] = l
	bzz_query["limit"] = limit

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LocationSearchResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_locations_threaded(
	# version: float   Eg: 3.16
	version: float,
	# deviceId: String = ""   Eg: deviceId_example
	# the device id
	deviceId = "",
	# accountId: float   Eg: 789
	# the account id
	accountId = null,
	# currentlatitude: int   Eg: 1.2
	# This parameter is deprecated.
	currentlatitude = null,
	# currentlongitude: int   Eg: 1.2
	# This parameter is deprecated.
	currentlongitude = null,
	# currentLatitude: int   Eg: 1.2
	# the current latitude of the user
	currentLatitude = null,
	# currentLongitude: int   Eg: 1.2
	# the current longitude of the user
	currentLongitude = null,
	# query: String = ""   Eg: query_example
	# the query results by keyword or address
	query = "",
	# zipcode: String = ""   Eg: zipcode_example
	# This parameter is deprecated.
	zipcode = "",
	# zipCode: String = ""   Eg: zipCode_example
	# the zip code to filter results
	zipCode = "",
	# selectedMaplatitude: int   Eg: 1.2
	# This parameter is deprecated.
	selectedMaplatitude = null,
	# selectedMaplongitude: int   Eg: 1.2
	# This parameter is deprecated.
	selectedMaplongitude = null,
	# selectedMapLatitude: int   Eg: 1.2
	# the latitude of where the search should originate from
	selectedMapLatitude = null,
	# selectedMapLongitude: int   Eg: 1.2
	# the longitude of where the search should originate from
	selectedMapLongitude = null,
	# searchRange: int = 5   Eg: 1.2
	# the search range of the search in miles
	searchRange = 5,
	# useGeocode: bool = false   Eg: true
	# determines whether to allow searches via address
	useGeocode = false,
	# i: int   Eg: 56
	# This parameter is deprecated.
	i = null,
	# start: int = 0   Eg: 56
	# the start index for pagination
	start = 0,
	# l: int   Eg: 56
	# This parameter is deprecated.
	l = null,
	# limit: int = 20   Eg: 56
	# the limit for pagination
	limit = 20,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_locations")
	bzz_callable.bind(
		version,
		deviceId,
		accountId,
		currentlatitude,
		currentlongitude,
		currentLatitude,
		currentLongitude,
		query,
		zipcode,
		zipCode,
		selectedMaplatitude,
		selectedMaplongitude,
		selectedMapLatitude,
		selectedMapLongitude,
		searchRange,
		useGeocode,
		i,
		start,
		l,
		limit,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


