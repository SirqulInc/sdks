extends ApiBee
class_name BidApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API BidApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "BidApi"


# Operation createBid → POST /bid/create
# Create Bid
#
# Creates a bid on a biddable object
func create_bid(
	# biddableType: String = ""   Eg: biddableType_example
	# A biddable object type. Possible values include: CREATIVE (ads).
	biddableType: String,
	# biddableId: float   Eg: 789
	# The id of the biddable object
	biddableId: float,
	# amountPerView: int   Eg: 1.2
	# The bid amount for views. For ads, this is the amount that will be taken for each impression.
	amountPerView: int,
	# amountPerAction: int   Eg: 1.2
	# The bid amount for actions. For ads, this is the amount that will be taken for each click.
	amountPerAction: int,
	# budgetAmount: int   Eg: 1.2
	# The allocated budget amount that will be used
	budgetAmount: int,
	# budgetSchedule: String = ""   Eg: budgetSchedule_example
	# The schedule for when the allocated budget amount is reset
	budgetSchedule: String,
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/bid/create"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["biddableType"] = biddableType
	bzz_query["biddableId"] = biddableId
	bzz_query["amountPerView"] = amountPerView
	bzz_query["amountPerAction"] = amountPerAction
	bzz_query["budgetAmount"] = budgetAmount
	bzz_query["budgetSchedule"] = budgetSchedule

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = BidResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_bid_threaded(
	# biddableType: String = ""   Eg: biddableType_example
	# A biddable object type. Possible values include: CREATIVE (ads).
	biddableType: String,
	# biddableId: float   Eg: 789
	# The id of the biddable object
	biddableId: float,
	# amountPerView: int   Eg: 1.2
	# The bid amount for views. For ads, this is the amount that will be taken for each impression.
	amountPerView: int,
	# amountPerAction: int   Eg: 1.2
	# The bid amount for actions. For ads, this is the amount that will be taken for each click.
	amountPerAction: int,
	# budgetAmount: int   Eg: 1.2
	# The allocated budget amount that will be used
	budgetAmount: int,
	# budgetSchedule: String = ""   Eg: budgetSchedule_example
	# The schedule for when the allocated budget amount is reset
	budgetSchedule: String,
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_bid")
	bzz_callable.bind(
		biddableType,
		biddableId,
		amountPerView,
		amountPerAction,
		budgetAmount,
		budgetSchedule,
		deviceId,
		accountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation deleteBid → POST /bid/delete
# Delete Bid
#
# Deleted a bid on a biddable object
func delete_bid(
	# bidId: float   Eg: 789
	# The bid id
	bidId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/bid/delete"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["bidId"] = bidId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func delete_bid_threaded(
	# bidId: float   Eg: 789
	# The bid id
	bidId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "delete_bid")
	bzz_callable.bind(
		bidId,
		deviceId,
		accountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getBid → GET /bid/get
# Get Bid
#
# Get the bid details of a biddable object
func get_bid(
	# bidId: float   Eg: 789
	# The bid id
	bidId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/bid/get"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["bidId"] = bidId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = BidResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_bid_threaded(
	# bidId: float   Eg: 789
	# The bid id
	bidId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_bid")
	bzz_callable.bind(
		bidId,
		deviceId,
		accountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation updateBid → POST /bid/update
# Update Bid
#
# Updates a bid on a biddable object
func update_bid(
	# bidId: float   Eg: 789
	# The bid id
	bidId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	# amountPerView: int   Eg: 1.2
	# The bid amount for views. For ads, this is the amount that will be taken for each impression.
	amountPerView = null,
	# amountPerAction: int   Eg: 1.2
	# The bid amount for actions. For ads, this is the amount that will be taken for each click.
	amountPerAction = null,
	# budgetAmount: int   Eg: 1.2
	# The allocated budget amount that will be used
	budgetAmount = null,
	# budgetSchedule: String = ""   Eg: budgetSchedule_example
	# The schedule for when the allocated budget amount is reset
	budgetSchedule = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/bid/update"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["bidId"] = bidId
	bzz_query["amountPerView"] = amountPerView
	bzz_query["amountPerAction"] = amountPerAction
	bzz_query["budgetAmount"] = budgetAmount
	bzz_query["budgetSchedule"] = budgetSchedule

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = BidResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func update_bid_threaded(
	# bidId: float   Eg: 789
	# The bid id
	bidId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	# amountPerView: int   Eg: 1.2
	# The bid amount for views. For ads, this is the amount that will be taken for each impression.
	amountPerView = null,
	# amountPerAction: int   Eg: 1.2
	# The bid amount for actions. For ads, this is the amount that will be taken for each click.
	amountPerAction = null,
	# budgetAmount: int   Eg: 1.2
	# The allocated budget amount that will be used
	budgetAmount = null,
	# budgetSchedule: String = ""   Eg: budgetSchedule_example
	# The schedule for when the allocated budget amount is reset
	budgetSchedule = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "update_bid")
	bzz_callable.bind(
		bidId,
		deviceId,
		accountId,
		amountPerView,
		amountPerAction,
		budgetAmount,
		budgetSchedule,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


