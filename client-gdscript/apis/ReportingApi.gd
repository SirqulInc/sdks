extends ApiBee
class_name ReportingApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API ReportingApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "ReportingApi"


# Operation createBatch → POST /api/{version}/report/batch/create
# Create Offline Report
#
# Create an entry for the batch for offline report
func create_batch(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# The account id of the user for passing account related params
	accountId: float,
	# status: String = ""   Eg: status_example
	# the status of the report
	status: String,
	# previewLimit: int   Eg: 56
	# the limit on how much you can preview of the batch report
	previewLimit: int,
	# appKey: String = ""   Eg: appKey_example
	# The application key for passing application related params
	appKey = "",
	# endpoint: String = ""   Eg: endpoint_example
	endpoint = "",
	# parameters: String = ""   Eg: parameters_example
	# a json structure list of the parameter values, example: ```json {   \"string\":\"value\",    \"number\":3.345,   \"date\":\"2014-05-01 00:00:00\" } ``` 
	parameters = "",
	# name: String = ""   Eg: name_example
	# name of the batch report
	name = "",
	# startDate: float   Eg: 789
	# the start date of the batch report
	startDate = null,
	# endDate: float   Eg: 789
	# the end date of the batch report
	endDate = null,
	# description: String = ""   Eg: description_example
	# the description of the batch report
	description = "",
	# pageUrl: String = ""   Eg: pageUrl_example
	pageUrl = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/report/batch/create".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["appKey"] = appKey
	bzz_query["status"] = status
	bzz_query["endpoint"] = endpoint
	bzz_query["parameters"] = parameters
	bzz_query["name"] = name
	bzz_query["startDate"] = startDate
	bzz_query["endDate"] = endDate
	bzz_query["description"] = description
	bzz_query["previewLimit"] = previewLimit
	bzz_query["pageUrl"] = pageUrl

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ReportBatchResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_batch_threaded(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# The account id of the user for passing account related params
	accountId: float,
	# status: String = ""   Eg: status_example
	# the status of the report
	status: String,
	# previewLimit: int   Eg: 56
	# the limit on how much you can preview of the batch report
	previewLimit: int,
	# appKey: String = ""   Eg: appKey_example
	# The application key for passing application related params
	appKey = "",
	# endpoint: String = ""   Eg: endpoint_example
	endpoint = "",
	# parameters: String = ""   Eg: parameters_example
	# a json structure list of the parameter values, example: ```json {   \"string\":\"value\",    \"number\":3.345,   \"date\":\"2014-05-01 00:00:00\" } ``` 
	parameters = "",
	# name: String = ""   Eg: name_example
	# name of the batch report
	name = "",
	# startDate: float   Eg: 789
	# the start date of the batch report
	startDate = null,
	# endDate: float   Eg: 789
	# the end date of the batch report
	endDate = null,
	# description: String = ""   Eg: description_example
	# the description of the batch report
	description = "",
	# pageUrl: String = ""   Eg: pageUrl_example
	pageUrl = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_batch")
	bzz_callable.bind(
		version,
		accountId,
		status,
		previewLimit,
		appKey,
		endpoint,
		parameters,
		name,
		startDate,
		endDate,
		description,
		pageUrl,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation createRegionLegSummaryBatch → POST /api/{version}/report/region/summary/batch
# Create Offline Report
#
# Create an entry for the batch for offline report
func create_region_leg_summary_batch(
	# version: float   Eg: 3.16
	version: float,
	# body: Array
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/report/region/summary/batch".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = body

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ReportRegionLegSummaryBatchResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_region_leg_summary_batch_threaded(
	# version: float   Eg: 3.16
	version: float,
	# body: Array
	body = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_region_leg_summary_batch")
	bzz_callable.bind(
		version,
		body,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation deleteBatch → POST /api/{version}/report/batch/delete
# Delete Offline Report
#
# Deletes a batch report.
func delete_batch(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the account
	accountId: float,
	# batchId: float   Eg: 789
	# the id of the batch to delete
	batchId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/report/batch/delete".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["batchId"] = batchId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func delete_batch_threaded(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the account
	accountId: float,
	# batchId: float   Eg: 789
	# the id of the batch to delete
	batchId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "delete_batch")
	bzz_callable.bind(
		version,
		accountId,
		batchId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getReportBatch → GET /api/{version}/report/batch/get
# Get Offline Report
#
# Checks status of batch report.
func get_report_batch(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId: float,
	# batchId: float   Eg: 789
	# returned by /report/batch/create
	batchId: float,
	# allResults: bool   Eg: true
	# whether to return all batch results or not
	allResults: bool,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/report/batch/get".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["batchId"] = batchId
	bzz_query["allResults"] = allResults

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ReportBatchResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_report_batch_threaded(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId: float,
	# batchId: float   Eg: 789
	# returned by /report/batch/create
	batchId: float,
	# allResults: bool   Eg: true
	# whether to return all batch results or not
	allResults: bool,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_report_batch")
	bzz_callable.bind(
		version,
		accountId,
		batchId,
		allResults,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation runReport → POST /api/{version}/report/run
# Run Report
#
#  This endpoint allows you to run a set of predefined reports that can be used to understand your users' behavior as well as trends within your application.
func run_report(
	# version: float   Eg: 3.16
	version: float,
	# desc: bool   Eg: true
	# If true then descending order, false is ascending
	desc: bool,
	# accountId: float   Eg: 789
	# The account id of the user for passing account related params
	accountId = null,
	# query: String = ""   Eg: query_example
	# The named identifier of the query
	query = "",
	# parameters: String = ""   Eg: parameters_example
	# Parameter values used in the query in JSON format, example: ```json {   \"string\":\"value\",    \"number\":3.345,   \"date\":\"2014-05-01 00:00:00\" } ``` 
	parameters = "",
	# order: String = ""   Eg: order_example
	# The order to use, must be a column name (see response results for list of column names)
	order = "",
	# start: float   Eg: 789
	# The start of the pagination
	start = null,
	# limit: float   Eg: 789
	# The limit of the pagination
	limit = null,
	# responseFormat: String = ""   Eg: responseFormat_example
	# Determines what response format to return. Options are: JSON or CSV
	responseFormat = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/report/run".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["query"] = query
	bzz_query["parameters"] = parameters
	bzz_query["order"] = order
	bzz_query["desc"] = desc
	bzz_query["start"] = start
	bzz_query["limit"] = limit
	bzz_query["responseFormat"] = responseFormat

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ReportResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func run_report_threaded(
	# version: float   Eg: 3.16
	version: float,
	# desc: bool   Eg: true
	# If true then descending order, false is ascending
	desc: bool,
	# accountId: float   Eg: 789
	# The account id of the user for passing account related params
	accountId = null,
	# query: String = ""   Eg: query_example
	# The named identifier of the query
	query = "",
	# parameters: String = ""   Eg: parameters_example
	# Parameter values used in the query in JSON format, example: ```json {   \"string\":\"value\",    \"number\":3.345,   \"date\":\"2014-05-01 00:00:00\" } ``` 
	parameters = "",
	# order: String = ""   Eg: order_example
	# The order to use, must be a column name (see response results for list of column names)
	order = "",
	# start: float   Eg: 789
	# The start of the pagination
	start = null,
	# limit: float   Eg: 789
	# The limit of the pagination
	limit = null,
	# responseFormat: String = ""   Eg: responseFormat_example
	# Determines what response format to return. Options are: JSON or CSV
	responseFormat = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "run_report")
	bzz_callable.bind(
		version,
		desc,
		accountId,
		query,
		parameters,
		order,
		start,
		limit,
		responseFormat,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation searchBatch → GET /api/{version}/report/batch/search
# Search Offline Reports
#
# Retrieves batches for a user..
func search_batch(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the account logged in
	accountId: float,
	# start: int   Eg: 56
	# the start of the index and/or pagination
	start: int,
	# limit: int   Eg: 56
	# the limit of the index and/or pagination
	limit: int,
	# names: String = ""   Eg: names_example
	# the names of the report batch to search on
	names = "",
	# appKey: String = ""   Eg: appKey_example
	# the application key
	appKey = "",
	# status: String = ""   Eg: status_example
	# the report batch status
	status = "",
	# globalAppSearch: bool   Eg: true
	# the global app to search on
	globalAppSearch = null,
	# startDate: float   Eg: 789
	# the start date of the report batch to search on
	startDate = null,
	# endDate: float   Eg: 789
	# the end date of the report batch to search on
	endDate = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/{version}/report/batch/search".replace("{" + "version" + "}", _bzz_urlize_path_param(version))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["names"] = names
	bzz_query["appKey"] = appKey
	bzz_query["status"] = status
	bzz_query["globalAppSearch"] = globalAppSearch
	bzz_query["startDate"] = startDate
	bzz_query["endDate"] = endDate
	bzz_query["start"] = start
	bzz_query["limit"] = limit

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ReportBatchResponse.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func search_batch_threaded(
	# version: float   Eg: 3.16
	version: float,
	# accountId: float   Eg: 789
	# the id of the account logged in
	accountId: float,
	# start: int   Eg: 56
	# the start of the index and/or pagination
	start: int,
	# limit: int   Eg: 56
	# the limit of the index and/or pagination
	limit: int,
	# names: String = ""   Eg: names_example
	# the names of the report batch to search on
	names = "",
	# appKey: String = ""   Eg: appKey_example
	# the application key
	appKey = "",
	# status: String = ""   Eg: status_example
	# the report batch status
	status = "",
	# globalAppSearch: bool   Eg: true
	# the global app to search on
	globalAppSearch = null,
	# startDate: float   Eg: 789
	# the start date of the report batch to search on
	startDate = null,
	# endDate: float   Eg: 789
	# the end date of the report batch to search on
	endDate = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "search_batch")
	bzz_callable.bind(
		version,
		accountId,
		start,
		limit,
		names,
		appKey,
		status,
		globalAppSearch,
		startDate,
		endDate,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


