extends ApiBee
class_name LeaderboardApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API LeaderboardApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "LeaderboardApi"


# Operation createLeaderboard → POST /leaderboard/create
# Create a leaderboard based on the rankingType, rankMode(leaderboardMode), sortField and limitation
#
# Create a leaderboard based on the rankingType, rankMode(leaderboardMode), sortField and limitation
func create_leaderboard(
	# accountId: float   Eg: 789
	# The account id of the user creating the leaderboard.
	accountId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key
	appKey = "",
	# rankType: String = ""   Eg: rankType_example
	# a unique label for identifying the ranking. This can be any alphanumeric string with a maximum length of 64 characters. There are also default rank types to use which include: POINTS, DOWNLOADS, INVITATIONS, CREATIONS, VOTES, REDEEMED, ACTIONS
	rankType = "",
	# leaderboardMode: String = ""   Eg: leaderboardMode_example
	# the type of search to perform. Possible values include: GLOBAL - searches scores globally (all users in the system are included in the ranking); LOCAL - filters results by select users and on users that have logged into the same device; SEARCH - does a GLOBAL search by keyword; CUSTOM - does a LOCAL search by keyword
	leaderboardMode = "",
	# iconMedia: String   Eg: BINARY_DATA_HERE
	# a MultipartFile containing the icon image of the leaderboard (this will only be used if \"iconAssetId\" is empty)
	iconMedia = null,
	# iconAssetId: float   Eg: 789
	# The asset ID to set the leaderboard icon
	iconAssetId = null,
	# bannerMedia: String   Eg: BINARY_DATA_HERE
	# a MultipartFile containing the icon banner of the leaderboard (this will only be used if \"bannerAssetId\" is empty)
	bannerMedia = null,
	# bannerAssetId: float   Eg: 789
	# The asset ID to set the leaderboard banner
	bannerAssetId = null,
	# limitation: int   Eg: 56
	# limit number of rankings for each leaderboard
	limitation = null,
	# sortField: String = ""   Eg: sortField_example
	# determines how to order and rank the results. Possible values include: TOTAL, WEEKLY, DAILY, TOP, LOWEST
	sortField = "",
	# title: String = ""   Eg: title_example
	# leaderboard's title
	title = "",
	# description: String = ""   Eg: description_example
	# leaderboard's description
	description = "",
	# metaData: String = ""   Eg: metaData_example
	# custom meta data for the leaderboard
	metaData = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/leaderboard/create"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["appKey"] = appKey
	bzz_query["rankType"] = rankType
	bzz_query["leaderboardMode"] = leaderboardMode
	bzz_query["iconMedia"] = iconMedia
	bzz_query["iconAssetId"] = iconAssetId
	bzz_query["bannerMedia"] = bannerMedia
	bzz_query["bannerAssetId"] = bannerAssetId
	bzz_query["limitation"] = limitation
	bzz_query["sortField"] = sortField
	bzz_query["title"] = title
	bzz_query["description"] = description
	bzz_query["metaData"] = metaData

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LeaderboardResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_leaderboard_threaded(
	# accountId: float   Eg: 789
	# The account id of the user creating the leaderboard.
	accountId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key
	appKey = "",
	# rankType: String = ""   Eg: rankType_example
	# a unique label for identifying the ranking. This can be any alphanumeric string with a maximum length of 64 characters. There are also default rank types to use which include: POINTS, DOWNLOADS, INVITATIONS, CREATIONS, VOTES, REDEEMED, ACTIONS
	rankType = "",
	# leaderboardMode: String = ""   Eg: leaderboardMode_example
	# the type of search to perform. Possible values include: GLOBAL - searches scores globally (all users in the system are included in the ranking); LOCAL - filters results by select users and on users that have logged into the same device; SEARCH - does a GLOBAL search by keyword; CUSTOM - does a LOCAL search by keyword
	leaderboardMode = "",
	# iconMedia: String   Eg: BINARY_DATA_HERE
	# a MultipartFile containing the icon image of the leaderboard (this will only be used if \"iconAssetId\" is empty)
	iconMedia = null,
	# iconAssetId: float   Eg: 789
	# The asset ID to set the leaderboard icon
	iconAssetId = null,
	# bannerMedia: String   Eg: BINARY_DATA_HERE
	# a MultipartFile containing the icon banner of the leaderboard (this will only be used if \"bannerAssetId\" is empty)
	bannerMedia = null,
	# bannerAssetId: float   Eg: 789
	# The asset ID to set the leaderboard banner
	bannerAssetId = null,
	# limitation: int   Eg: 56
	# limit number of rankings for each leaderboard
	limitation = null,
	# sortField: String = ""   Eg: sortField_example
	# determines how to order and rank the results. Possible values include: TOTAL, WEEKLY, DAILY, TOP, LOWEST
	sortField = "",
	# title: String = ""   Eg: title_example
	# leaderboard's title
	title = "",
	# description: String = ""   Eg: description_example
	# leaderboard's description
	description = "",
	# metaData: String = ""   Eg: metaData_example
	# custom meta data for the leaderboard
	metaData = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_leaderboard")
	bzz_callable.bind(
		accountId,
		appKey,
		rankType,
		leaderboardMode,
		iconMedia,
		iconAssetId,
		bannerMedia,
		bannerAssetId,
		limitation,
		sortField,
		title,
		description,
		metaData,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation deleteLeaderboard → POST /leaderboard/delete
# Delete the Leader Board
#
# Removes a leader board id.
func delete_leaderboard(
	# leaderboardId: float   Eg: 789
	# The leaderboard id to delete.
	leaderboardId: float,
	# accountId: float   Eg: 789
	# The account id of the user making the request.
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/leaderboard/delete"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["leaderboardId"] = leaderboardId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func delete_leaderboard_threaded(
	# leaderboardId: float   Eg: 789
	# The leaderboard id to delete.
	leaderboardId: float,
	# accountId: float   Eg: 789
	# The account id of the user making the request.
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "delete_leaderboard")
	bzz_callable.bind(
		leaderboardId,
		accountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getLeaderboard → GET /leaderboard/get
# Read a leaderboard by id and retrieve the matching ranking list
#
# Read a leaderboard by id and retrieve the matching ranking list
func get_leaderboard(
	# leaderboardId: float   Eg: 789
	# The leaderboard id.
	leaderboardId: float,
	# accountId: float   Eg: 789
	# A valid account.
	accountId = null,
	# includeFullRankingList: bool   Eg: true
	# set to true if need to return the leaderboard's full ranking list
	includeFullRankingList = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/leaderboard/get"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["leaderboardId"] = leaderboardId
	bzz_query["includeFullRankingList"] = includeFullRankingList

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LeaderboardResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_leaderboard_threaded(
	# leaderboardId: float   Eg: 789
	# The leaderboard id.
	leaderboardId: float,
	# accountId: float   Eg: 789
	# A valid account.
	accountId = null,
	# includeFullRankingList: bool   Eg: true
	# set to true if need to return the leaderboard's full ranking list
	includeFullRankingList = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_leaderboard")
	bzz_callable.bind(
		leaderboardId,
		accountId,
		includeFullRankingList,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation searchLeaderboards → GET /leaderboard/search
# Search leaderboard and retrieve the matching ranking list
#
# Search leaderboard and retrieve the matching ranking list
func search_leaderboards(
	# accountId: float   Eg: 789
	# The account id of the user requesting the search.
	accountId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key.
	appKey = "",
	# globalOnly: bool   Eg: true
	# only include global leaderboards (this overrides the appKey filter)
	globalOnly = null,
	# keyword: String = ""   Eg: keyword_example
	# keyword to search by title
	keyword = "",
	# leaderboardIds: String = ""   Eg: leaderboardIds_example
	# comma separated list of leaderboard ids to filter results with
	leaderboardIds = "",
	# rankTypes: String = ""   Eg: rankTypes_example
	# comma separated list of rankType to filter results with
	rankTypes = "",
	# sortField: String = ""   Eg: sortField_example
	# sortField of the result, from LeaderboardApiMap (TITLE, DESCRIPTION, CREATED, UPDATED, RANK_TYPE, RANK_MODE)
	sortField = "",
	# descending: bool   Eg: true
	# Determines whether the sorted list is in descending or ascending order
	descending = null,
	# includeInactive: bool   Eg: true
	# include inactive in the result
	includeInactive = null,
	# includeAppResponse: bool   Eg: true
	# determines whether to include the application response for each leaderboard
	includeAppResponse = null,
	# start: int   Eg: 56
	# Start the result set at some index.
	start = null,
	# limit: int   Eg: 56
	# Limit the result to some number.
	limit = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/leaderboard/search"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["appKey"] = appKey
	bzz_query["globalOnly"] = globalOnly
	bzz_query["keyword"] = keyword
	bzz_query["leaderboardIds"] = leaderboardIds
	bzz_query["rankTypes"] = rankTypes
	bzz_query["sortField"] = sortField
	bzz_query["descending"] = descending
	bzz_query["includeInactive"] = includeInactive
	bzz_query["includeAppResponse"] = includeAppResponse
	bzz_query["start"] = start
	bzz_query["limit"] = limit

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LeaderboardResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func search_leaderboards_threaded(
	# accountId: float   Eg: 789
	# The account id of the user requesting the search.
	accountId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key.
	appKey = "",
	# globalOnly: bool   Eg: true
	# only include global leaderboards (this overrides the appKey filter)
	globalOnly = null,
	# keyword: String = ""   Eg: keyword_example
	# keyword to search by title
	keyword = "",
	# leaderboardIds: String = ""   Eg: leaderboardIds_example
	# comma separated list of leaderboard ids to filter results with
	leaderboardIds = "",
	# rankTypes: String = ""   Eg: rankTypes_example
	# comma separated list of rankType to filter results with
	rankTypes = "",
	# sortField: String = ""   Eg: sortField_example
	# sortField of the result, from LeaderboardApiMap (TITLE, DESCRIPTION, CREATED, UPDATED, RANK_TYPE, RANK_MODE)
	sortField = "",
	# descending: bool   Eg: true
	# Determines whether the sorted list is in descending or ascending order
	descending = null,
	# includeInactive: bool   Eg: true
	# include inactive in the result
	includeInactive = null,
	# includeAppResponse: bool   Eg: true
	# determines whether to include the application response for each leaderboard
	includeAppResponse = null,
	# start: int   Eg: 56
	# Start the result set at some index.
	start = null,
	# limit: int   Eg: 56
	# Limit the result to some number.
	limit = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "search_leaderboards")
	bzz_callable.bind(
		accountId,
		appKey,
		globalOnly,
		keyword,
		leaderboardIds,
		rankTypes,
		sortField,
		descending,
		includeInactive,
		includeAppResponse,
		start,
		limit,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation updateLeaderboard → POST /leaderboard/update
# Update a leaderboard based on the rankingType, rankMode(leaderboardMode), sortField and limitation
#
# Update a leaderboard based on the rankingType, rankMode(leaderboardMode), sortField and limitation
func update_leaderboard(
	# leaderboardId: float   Eg: 789
	# The leaderboard id to update.
	leaderboardId: float,
	# accountId: float   Eg: 789
	# The account id of the user updating the leaderboard.
	accountId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key
	appKey = "",
	# rankType: String = ""   Eg: rankType_example
	# a unique label for identifying the ranking. This can be any alphanumeric string with a maximum length of 64 characters.
	rankType = "",
	# leaderboardMode: String = ""   Eg: leaderboardMode_example
	# the type of search to perform. Possible values include: GLOBAL, LOCAL, SEARCH, CUSTOM
	leaderboardMode = "",
	# sortField: String = ""   Eg: sortField_example
	# determines how to order and rank the results. Possible values include: TOTAL, WEEKLY, DAILY, TOP, LOWEST
	sortField = "",
	# iconMedia: String   Eg: BINARY_DATA_HERE
	# a MultipartFile containing the icon image of the leaderboard (this will only be used if \"iconAssetId\" is empty)
	iconMedia = null,
	# iconAssetId: float   Eg: 789
	# The asset ID to set the leaderboard icon
	iconAssetId = null,
	# bannerMedia: String   Eg: BINARY_DATA_HERE
	# a MultipartFile containing the icon banner of the leaderboard (this will only be used if \"bannerAssetId\" is empty)
	bannerMedia = null,
	# bannerAssetId: float   Eg: 789
	# The asset ID to set the leaderboard banner
	bannerAssetId = null,
	# limitation: int   Eg: 56
	# limit number of rankings for each leaderboard
	limitation = null,
	# active: bool   Eg: true
	# Whether the leaderboard is active
	active = null,
	# title: String = ""   Eg: title_example
	# leaderboard's title
	title = "",
	# description: String = ""   Eg: description_example
	# leaderboard's description
	description = "",
	# metaData: String = ""   Eg: metaData_example
	# custom meta data for the leaderboard
	metaData = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/leaderboard/update"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["accountId"] = accountId
	bzz_query["appKey"] = appKey
	bzz_query["leaderboardId"] = leaderboardId
	bzz_query["rankType"] = rankType
	bzz_query["leaderboardMode"] = leaderboardMode
	bzz_query["sortField"] = sortField
	bzz_query["iconMedia"] = iconMedia
	bzz_query["iconAssetId"] = iconAssetId
	bzz_query["bannerMedia"] = bannerMedia
	bzz_query["bannerAssetId"] = bannerAssetId
	bzz_query["limitation"] = limitation
	bzz_query["active"] = active
	bzz_query["title"] = title
	bzz_query["description"] = description
	bzz_query["metaData"] = metaData

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LeaderboardResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func update_leaderboard_threaded(
	# leaderboardId: float   Eg: 789
	# The leaderboard id to update.
	leaderboardId: float,
	# accountId: float   Eg: 789
	# The account id of the user updating the leaderboard.
	accountId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key
	appKey = "",
	# rankType: String = ""   Eg: rankType_example
	# a unique label for identifying the ranking. This can be any alphanumeric string with a maximum length of 64 characters.
	rankType = "",
	# leaderboardMode: String = ""   Eg: leaderboardMode_example
	# the type of search to perform. Possible values include: GLOBAL, LOCAL, SEARCH, CUSTOM
	leaderboardMode = "",
	# sortField: String = ""   Eg: sortField_example
	# determines how to order and rank the results. Possible values include: TOTAL, WEEKLY, DAILY, TOP, LOWEST
	sortField = "",
	# iconMedia: String   Eg: BINARY_DATA_HERE
	# a MultipartFile containing the icon image of the leaderboard (this will only be used if \"iconAssetId\" is empty)
	iconMedia = null,
	# iconAssetId: float   Eg: 789
	# The asset ID to set the leaderboard icon
	iconAssetId = null,
	# bannerMedia: String   Eg: BINARY_DATA_HERE
	# a MultipartFile containing the icon banner of the leaderboard (this will only be used if \"bannerAssetId\" is empty)
	bannerMedia = null,
	# bannerAssetId: float   Eg: 789
	# The asset ID to set the leaderboard banner
	bannerAssetId = null,
	# limitation: int   Eg: 56
	# limit number of rankings for each leaderboard
	limitation = null,
	# active: bool   Eg: true
	# Whether the leaderboard is active
	active = null,
	# title: String = ""   Eg: title_example
	# leaderboard's title
	title = "",
	# description: String = ""   Eg: description_example
	# leaderboard's description
	description = "",
	# metaData: String = ""   Eg: metaData_example
	# custom meta data for the leaderboard
	metaData = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "update_leaderboard")
	bzz_callable.bind(
		leaderboardId,
		accountId,
		appKey,
		rankType,
		leaderboardMode,
		sortField,
		iconMedia,
		iconAssetId,
		bannerMedia,
		bannerAssetId,
		limitation,
		active,
		title,
		description,
		metaData,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


