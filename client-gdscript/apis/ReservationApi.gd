extends ApiBee
class_name ReservationApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API ReservationApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "ReservationApi"


# Operation createReservation → POST /reservation/create
# Create Reservation
#
# Creates a reservation on an offer object
func create_reservation(
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	# startDate: float   Eg: 789
	# The start date
	startDate = null,
	# endDate: float   Eg: 789
	# The end date
	endDate = null,
	# offerId: float   Eg: 789
	# The id of the offer being added (offerId or offeLocationId required)
	offerId = null,
	# offerLocationId: float   Eg: 789
	# The id of the offer location being added (offerId or offeLocationId required)
	offerLocationId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application requesting the reservation
	appKey = "",
	# metaData: String = ""   Eg: metaData_example
	# External custom client defined data
	metaData = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/reservation/create"

	# Collect the headers
	var bzz_headers := Dictionary()

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["startDate"] = startDate
	bzz_query["endDate"] = endDate
	bzz_query["offerId"] = offerId
	bzz_query["offerLocationId"] = offerLocationId
	bzz_query["appKey"] = appKey
	bzz_query["metaData"] = metaData

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_reservation_threaded(
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	# startDate: float   Eg: 789
	# The start date
	startDate = null,
	# endDate: float   Eg: 789
	# The end date
	endDate = null,
	# offerId: float   Eg: 789
	# The id of the offer being added (offerId or offeLocationId required)
	offerId = null,
	# offerLocationId: float   Eg: 789
	# The id of the offer location being added (offerId or offeLocationId required)
	offerLocationId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application requesting the reservation
	appKey = "",
	# metaData: String = ""   Eg: metaData_example
	# External custom client defined data
	metaData = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_reservation")
	bzz_callable.bind(
		deviceId,
		accountId,
		startDate,
		endDate,
		offerId,
		offerLocationId,
		appKey,
		metaData,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation deleteReservation → POST /reservation/delete
# Delete Reservation
#
# Deleted a reservation on a reservation object
func delete_reservation(
	# reservationId: float   Eg: 789
	# The reservation id
	reservationId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/reservation/delete"

	# Collect the headers
	var bzz_headers := Dictionary()

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["reservationId"] = reservationId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func delete_reservation_threaded(
	# reservationId: float   Eg: 789
	# The reservation id
	reservationId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The device id (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "delete_reservation")
	bzz_callable.bind(
		reservationId,
		deviceId,
		accountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation reservableAvailability → POST /reservable/availability/update
# Update Availability
func reservable_availability(
	# reservableId: float   Eg: 789
	# the id of the reservation
	reservableId: float,
	# reservableType: String = ""   Eg: reservableType_example
	# the type of reservation
	reservableType: String,
	# deviceId: String = ""   Eg: deviceId_example
	# the device id of the reservation
	deviceId = "",
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId = null,
	# availability: String = ""   Eg: availability_example
	# Availability
	availability = "",
	# availabilitySummary: String = ""   Eg: availabilitySummary_example
	# Availability Summary
	availabilitySummary = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/reservable/availability/update"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["reservableId"] = reservableId
	bzz_query["reservableType"] = reservableType
	bzz_query["availability"] = availability
	bzz_query["availabilitySummary"] = availabilitySummary

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = AvailabilityResponse.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func reservable_availability_threaded(
	# reservableId: float   Eg: 789
	# the id of the reservation
	reservableId: float,
	# reservableType: String = ""   Eg: reservableType_example
	# the type of reservation
	reservableType: String,
	# deviceId: String = ""   Eg: deviceId_example
	# the device id of the reservation
	deviceId = "",
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId = null,
	# availability: String = ""   Eg: availability_example
	# Availability
	availability = "",
	# availabilitySummary: String = ""   Eg: availabilitySummary_example
	# Availability Summary
	availabilitySummary = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "reservable_availability")
	bzz_callable.bind(
		reservableId,
		reservableType,
		deviceId,
		accountId,
		availability,
		availabilitySummary,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation searchAvailability → GET /reservable/availability/search
# Search Availability
func search_availability(
	# reservableId: float   Eg: 789
	# the id of the reservation
	reservableId: float,
	# reservableType: String = ""   Eg: reservableType_example
	# the reservable type
	reservableType: String,
	# deviceId: String = ""   Eg: deviceId_example
	# the device ID that the reservation is on
	deviceId = "",
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId = null,
	# startDate: float   Eg: 789
	# the start date of the reservation
	startDate = null,
	# endDate: float   Eg: 789
	# the end date of the reservation
	endDate = null,
	# start: int = 0   Eg: 56
	# the start of the index and/or pagination
	start = 0,
	# limit: int = 100   Eg: 56
	# the limit of the index and/or pagination
	limit = 100,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/reservable/availability/search"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["reservableId"] = reservableId
	bzz_query["reservableType"] = reservableType
	bzz_query["startDate"] = startDate
	bzz_query["endDate"] = endDate
	bzz_query["start"] = start
	bzz_query["limit"] = limit

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = AvailabilityResponse.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func search_availability_threaded(
	# reservableId: float   Eg: 789
	# the id of the reservation
	reservableId: float,
	# reservableType: String = ""   Eg: reservableType_example
	# the reservable type
	reservableType: String,
	# deviceId: String = ""   Eg: deviceId_example
	# the device ID that the reservation is on
	deviceId = "",
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId = null,
	# startDate: float   Eg: 789
	# the start date of the reservation
	startDate = null,
	# endDate: float   Eg: 789
	# the end date of the reservation
	endDate = null,
	# start: int = 0   Eg: 56
	# the start of the index and/or pagination
	start = 0,
	# limit: int = 100   Eg: 56
	# the limit of the index and/or pagination
	limit = 100,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "search_availability")
	bzz_callable.bind(
		reservableId,
		reservableType,
		deviceId,
		accountId,
		startDate,
		endDate,
		start,
		limit,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation searchReservations → GET /reservation/search
# Search Reservations
func search_reservations(
	# deviceId: String = ""   Eg: deviceId_example
	# Device Id
	deviceId = "",
	# appKey: String = ""   Eg: appKey_example
	# Appilcation Key
	appKey = "",
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId = null,
	# filterAccountId: float   Eg: 789
	# filter reservations by account ID
	filterAccountId = null,
	# reservableId: float   Eg: 789
	# the reservation ID
	reservableId = null,
	# reservableType: String = ""   Eg: reservableType_example
	# the type of reservation
	reservableType = "",
	# keyword: String = ""   Eg: keyword_example
	# the keyword to search the reservation on
	keyword = "",
	# startDate: float   Eg: 789
	# the start date of the reservation search
	startDate = null,
	# endDate: float   Eg: 789
	# the end date of the reservation search
	endDate = null,
	# start: int = 0   Eg: 56
	# the start of the index and/or pagination
	start = 0,
	# limit: int = 100   Eg: 56
	# the limit of the index and/or pagination
	limit = 100,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/reservation/search"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["appKey"] = appKey
	bzz_query["accountId"] = accountId
	bzz_query["filterAccountId"] = filterAccountId
	bzz_query["reservableId"] = reservableId
	bzz_query["reservableType"] = reservableType
	bzz_query["keyword"] = keyword
	bzz_query["startDate"] = startDate
	bzz_query["endDate"] = endDate
	bzz_query["start"] = start
	bzz_query["limit"] = limit

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ReservationResponse.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func search_reservations_threaded(
	# deviceId: String = ""   Eg: deviceId_example
	# Device Id
	deviceId = "",
	# appKey: String = ""   Eg: appKey_example
	# Appilcation Key
	appKey = "",
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId = null,
	# filterAccountId: float   Eg: 789
	# filter reservations by account ID
	filterAccountId = null,
	# reservableId: float   Eg: 789
	# the reservation ID
	reservableId = null,
	# reservableType: String = ""   Eg: reservableType_example
	# the type of reservation
	reservableType = "",
	# keyword: String = ""   Eg: keyword_example
	# the keyword to search the reservation on
	keyword = "",
	# startDate: float   Eg: 789
	# the start date of the reservation search
	startDate = null,
	# endDate: float   Eg: 789
	# the end date of the reservation search
	endDate = null,
	# start: int = 0   Eg: 56
	# the start of the index and/or pagination
	start = 0,
	# limit: int = 100   Eg: 56
	# the limit of the index and/or pagination
	limit = 100,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "search_reservations")
	bzz_callable.bind(
		deviceId,
		appKey,
		accountId,
		filterAccountId,
		reservableId,
		reservableType,
		keyword,
		startDate,
		endDate,
		start,
		limit,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation searchSchedule → GET /reservable/schedule/search
# Search Schedule
func search_schedule(
	# reservableId: float   Eg: 789
	# the id of the reservation
	reservableId: float,
	# reservableType: String = ""   Eg: reservableType_example
	# the reservation type
	reservableType: String,
	# startDate: float   Eg: 789
	# the start date of the reservation
	startDate: float,
	# endDate: float   Eg: 789
	# the end date of the reservation
	endDate: float,
	# deviceId: String = ""   Eg: deviceId_example
	# the id of the device that the reservation is on
	deviceId = "",
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId = null,
	# timeBucketMins: int = 30   Eg: 56
	# the length of time in minutes to search on for reservation
	timeBucketMins = 30,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/reservable/schedule/search"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["reservableId"] = reservableId
	bzz_query["reservableType"] = reservableType
	bzz_query["startDate"] = startDate
	bzz_query["endDate"] = endDate
	bzz_query["timeBucketMins"] = timeBucketMins

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = TimeSlotResponse.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func search_schedule_threaded(
	# reservableId: float   Eg: 789
	# the id of the reservation
	reservableId: float,
	# reservableType: String = ""   Eg: reservableType_example
	# the reservation type
	reservableType: String,
	# startDate: float   Eg: 789
	# the start date of the reservation
	startDate: float,
	# endDate: float   Eg: 789
	# the end date of the reservation
	endDate: float,
	# deviceId: String = ""   Eg: deviceId_example
	# the id of the device that the reservation is on
	deviceId = "",
	# accountId: float   Eg: 789
	# the id of the logged in user
	accountId = null,
	# timeBucketMins: int = 30   Eg: 56
	# the length of time in minutes to search on for reservation
	timeBucketMins = 30,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "search_schedule")
	bzz_callable.bind(
		reservableId,
		reservableType,
		startDate,
		endDate,
		deviceId,
		accountId,
		timeBucketMins,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


