extends ApiBee
class_name AnalyticsApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API AnalyticsApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "AnalyticsApi"


# Operation activities → GET /analytics/useractivity
# Get User Activity
#
# Get an activity feed by user.
func activities(
	# start: int   Eg: 56
	# The start of the pagination
	start: int,
	# limit: int   Eg: 56
	# The limit of the pagination
	limit: int,
	# accountId: float   Eg: 789
	# the account id of the user
	accountId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/analytics/useractivity"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["start"] = start
	bzz_query["limit"] = limit
	bzz_query["accountId"] = accountId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = UserActivityResponse.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func activities_threaded(
	# start: int   Eg: 56
	# The start of the pagination
	start: int,
	# limit: int   Eg: 56
	# The limit of the pagination
	limit: int,
	# accountId: float   Eg: 789
	# the account id of the user
	accountId: float,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "activities")
	bzz_callable.bind(
		start,
		limit,
		accountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation aggregatedFilteredUsage → GET /analytics/aggregatedFilteredUsage
# Get Aggregated Filtered Usage
#
# Query analytics to get data used for nested graphs and charts
func aggregated_filtered_usage(
	# deviceId: String = ""   Eg: deviceId_example
	# The unique id of the device making the request (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	# applicationId: float   Eg: 789
	# This parameter is deprecated.
	applicationId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key used to filter results by application
	appKey = "",
	# startDate: float   Eg: 789
	# Used to search for results after or equal to this date (UNIX time-stamp in milliseconds)
	startDate = null,
	# endDate: float   Eg: 789
	# Used to search for results before or equal to this date (UNIX time-stamp in milliseconds)
	endDate = null,
	# deviceType: String = ""   Eg: deviceType_example
	# The device type to filter results by (performs a LIKE search)
	deviceType = "",
	# device: String = ""   Eg: device_example
	# The device to filter results by (performs a LIKE search)
	device = "",
	# deviceOS: String = ""   Eg: deviceOS_example
	# The device OS to filter results by (performs a LIKE search)
	deviceOS = "",
	# gender: String = ""   Eg: gender_example
	# The gender to filter results by {MALE, FEMALE}
	gender = "",
	# ageGroup: String = ""   Eg: ageGroup_example
	# Comma separated list of age groups to filter by {AGE_0_13, AGE_14_17, AGE_18_22, AGE_23_30, AGE_31_54, AGE_55_PLUS}
	ageGroup = "",
	# country: String = ""   Eg: country_example
	# The country to filter results by (performs a wild-card search)
	country = "",
	# state: String = ""   Eg: state_example
	# The state to filter results by (performs a wild-card search)
	state = "",
	# city: String = ""   Eg: city_example
	# The city to filter results by (performs a wild-card search)
	city = "",
	# zip: String = ""   Eg: zip_example
	# The zip to filter results by (performs a wild-card search)
	zip = "",
	# model: String = ""   Eg: model_example
	# The model to filter results by (performs a wild-card search)
	model = "",
	# tag: String = ""   Eg: tag_example
	# The tag to filter results by (performs a wild-card search)
	tag = "",
	# userAccountId: float   Eg: 789
	# The account id to filter results for a particular user
	userAccountId = null,
	# userAccountDisplay: String = ""   Eg: userAccountDisplay_example
	# The user display to filter results by (performs a wild-card search)
	userAccountDisplay = "",
	# userAccountUsername: String = ""   Eg: userAccountUsername_example
	# The username to filter results by (performs a wild-card search)
	userAccountUsername = "",
	# groupByRoot: String = ""   Eg: groupByRoot_example
	# Determines how to group results. For example, passing in 'CREATED' will return results by date. Passing in 'ACCOUNT' will return results by user {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, APPLICATION_NAME_BY_ID}
	groupByRoot = "",
	# groupBy: String = ""   Eg: groupBy_example
	# Determines how to group the nested data. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, APPLICATION_NAME_BY_ID}
	groupBy = "",
	# distinctCount: String = ""   Eg: distinctCount_example
	# Performs a unique query on the specified column. For example, passing in 'ACCOUNT' will return the number of unique users. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, NEW_ACCOUNT}
	distinctCount = "",
	# sortField: String = ""   Eg: sortField_example
	# Determines whether the results will be sorted by day or by number of hits {CREATED, TAG_COUNT}
	sortField = "",
	# descending: bool   Eg: true
	# Determines whether to return results in descending order
	descending = null,
	# hideUnknown: bool   Eg: true
	# Determines whether to return data that has empty or unknown values
	hideUnknown = null,
	# responseFormat: String = ""   Eg: responseFormat_example
	# Determines whether to return a JOSN or XML representation of the graph results
	responseFormat = "",
	# l: int   Eg: 56
	# This parameter is deprecated.
	l = null,
	# limit: int   Eg: 56
	# Used to limit results to get a cleaner graph. The results that gets filtered out will be combined
	limit = null,
	# latitude: int   Eg: 1.2
	# the current latitude of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# the current longitude of the user
	longitude = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/analytics/aggregatedFilteredUsage"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["applicationId"] = applicationId
	bzz_query["appKey"] = appKey
	bzz_query["startDate"] = startDate
	bzz_query["endDate"] = endDate
	bzz_query["deviceType"] = deviceType
	bzz_query["device"] = device
	bzz_query["deviceOS"] = deviceOS
	bzz_query["gender"] = gender
	bzz_query["ageGroup"] = ageGroup
	bzz_query["country"] = country
	bzz_query["state"] = state
	bzz_query["city"] = city
	bzz_query["zip"] = zip
	bzz_query["model"] = model
	bzz_query["tag"] = tag
	bzz_query["userAccountId"] = userAccountId
	bzz_query["userAccountDisplay"] = userAccountDisplay
	bzz_query["userAccountUsername"] = userAccountUsername
	bzz_query["groupByRoot"] = groupByRoot
	bzz_query["groupBy"] = groupBy
	bzz_query["distinctCount"] = distinctCount
	bzz_query["sortField"] = sortField
	bzz_query["descending"] = descending
	bzz_query["hideUnknown"] = hideUnknown
	bzz_query["responseFormat"] = responseFormat
	bzz_query["_l"] = l
	bzz_query["limit"] = limit
	bzz_query["latitude"] = latitude
	bzz_query["longitude"] = longitude

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ChartData.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func aggregated_filtered_usage_threaded(
	# deviceId: String = ""   Eg: deviceId_example
	# The unique id of the device making the request (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	# applicationId: float   Eg: 789
	# This parameter is deprecated.
	applicationId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key used to filter results by application
	appKey = "",
	# startDate: float   Eg: 789
	# Used to search for results after or equal to this date (UNIX time-stamp in milliseconds)
	startDate = null,
	# endDate: float   Eg: 789
	# Used to search for results before or equal to this date (UNIX time-stamp in milliseconds)
	endDate = null,
	# deviceType: String = ""   Eg: deviceType_example
	# The device type to filter results by (performs a LIKE search)
	deviceType = "",
	# device: String = ""   Eg: device_example
	# The device to filter results by (performs a LIKE search)
	device = "",
	# deviceOS: String = ""   Eg: deviceOS_example
	# The device OS to filter results by (performs a LIKE search)
	deviceOS = "",
	# gender: String = ""   Eg: gender_example
	# The gender to filter results by {MALE, FEMALE}
	gender = "",
	# ageGroup: String = ""   Eg: ageGroup_example
	# Comma separated list of age groups to filter by {AGE_0_13, AGE_14_17, AGE_18_22, AGE_23_30, AGE_31_54, AGE_55_PLUS}
	ageGroup = "",
	# country: String = ""   Eg: country_example
	# The country to filter results by (performs a wild-card search)
	country = "",
	# state: String = ""   Eg: state_example
	# The state to filter results by (performs a wild-card search)
	state = "",
	# city: String = ""   Eg: city_example
	# The city to filter results by (performs a wild-card search)
	city = "",
	# zip: String = ""   Eg: zip_example
	# The zip to filter results by (performs a wild-card search)
	zip = "",
	# model: String = ""   Eg: model_example
	# The model to filter results by (performs a wild-card search)
	model = "",
	# tag: String = ""   Eg: tag_example
	# The tag to filter results by (performs a wild-card search)
	tag = "",
	# userAccountId: float   Eg: 789
	# The account id to filter results for a particular user
	userAccountId = null,
	# userAccountDisplay: String = ""   Eg: userAccountDisplay_example
	# The user display to filter results by (performs a wild-card search)
	userAccountDisplay = "",
	# userAccountUsername: String = ""   Eg: userAccountUsername_example
	# The username to filter results by (performs a wild-card search)
	userAccountUsername = "",
	# groupByRoot: String = ""   Eg: groupByRoot_example
	# Determines how to group results. For example, passing in 'CREATED' will return results by date. Passing in 'ACCOUNT' will return results by user {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, APPLICATION_NAME_BY_ID}
	groupByRoot = "",
	# groupBy: String = ""   Eg: groupBy_example
	# Determines how to group the nested data. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, APPLICATION_NAME_BY_ID}
	groupBy = "",
	# distinctCount: String = ""   Eg: distinctCount_example
	# Performs a unique query on the specified column. For example, passing in 'ACCOUNT' will return the number of unique users. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID, NEW_ACCOUNT}
	distinctCount = "",
	# sortField: String = ""   Eg: sortField_example
	# Determines whether the results will be sorted by day or by number of hits {CREATED, TAG_COUNT}
	sortField = "",
	# descending: bool   Eg: true
	# Determines whether to return results in descending order
	descending = null,
	# hideUnknown: bool   Eg: true
	# Determines whether to return data that has empty or unknown values
	hideUnknown = null,
	# responseFormat: String = ""   Eg: responseFormat_example
	# Determines whether to return a JOSN or XML representation of the graph results
	responseFormat = "",
	# l: int   Eg: 56
	# This parameter is deprecated.
	l = null,
	# limit: int   Eg: 56
	# Used to limit results to get a cleaner graph. The results that gets filtered out will be combined
	limit = null,
	# latitude: int   Eg: 1.2
	# the current latitude of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# the current longitude of the user
	longitude = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "aggregated_filtered_usage")
	bzz_callable.bind(
		deviceId,
		accountId,
		applicationId,
		appKey,
		startDate,
		endDate,
		deviceType,
		device,
		deviceOS,
		gender,
		ageGroup,
		country,
		state,
		city,
		zip,
		model,
		tag,
		userAccountId,
		userAccountDisplay,
		userAccountUsername,
		groupByRoot,
		groupBy,
		distinctCount,
		sortField,
		descending,
		hideUnknown,
		responseFormat,
		l,
		limit,
		latitude,
		longitude,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation filteredUsage → GET /analytics/filteredUsage
# Get Filtered Usage
#
# Query analytics to get data used for graphs and charts
func filtered_usage(
	# deviceId: String = ""   Eg: deviceId_example
	# The unique id of the device making the request (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	# applicationId: float   Eg: 789
	# This parameter is deprecated.
	applicationId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key used to filter results by application
	appKey = "",
	# startDate: float   Eg: 789
	# Used to search for results after or equal to this date (UNIX time-stamp in milliseconds)
	startDate = null,
	# endDate: float   Eg: 789
	# Used to search for results before or equal to this date (UNIX time-stamp in milliseconds)
	endDate = null,
	# deviceType: String = ""   Eg: deviceType_example
	# The device type to filter results by (performs a LIKE search)
	deviceType = "",
	# device: String = ""   Eg: device_example
	# The device to filter results by (performs a LIKE search)
	device = "",
	# deviceOS: String = ""   Eg: deviceOS_example
	# The device OS to filter results by (performs a LIKE search)
	deviceOS = "",
	# gender: String = ""   Eg: gender_example
	# The gender to filter results by {MALE, FEMALE}
	gender = "",
	# ageGroup: String = ""   Eg: ageGroup_example
	# Comma separated list of age groups to filter by {AGE_0_13, AGE_14_17, AGE_18_22, AGE_23_30, AGE_31_54, AGE_55_PLUS}
	ageGroup = "",
	# country: String = ""   Eg: country_example
	# The country to filter results by (performs a wild-card search)
	country = "",
	# state: String = ""   Eg: state_example
	# The state to filter results by (performs a wild-card search)
	state = "",
	# city: String = ""   Eg: city_example
	# The city to filter results by (performs a wild-card search)
	city = "",
	# zip: String = ""   Eg: zip_example
	# The zip to filter results by (performs a wild-card search)
	zip = "",
	# model: String = ""   Eg: model_example
	# The model to filter results by (performs a wild-card search)
	model = "",
	# tag: String = ""   Eg: tag_example
	# The tag to filter results by (performs a wild-card search)
	tag = "",
	# userAccountId: float   Eg: 789
	# The account id to filter results for a particular user
	userAccountId = null,
	# userAccountDisplay: String = ""   Eg: userAccountDisplay_example
	# The user display to filter results by (performs a wild-card search)
	userAccountDisplay = "",
	# userAccountUsername: String = ""   Eg: userAccountUsername_example
	# The username to filter results by (performs a wild-card search)
	userAccountUsername = "",
	# customId: float   Eg: 789
	# The ID for the custom table column
	customId = null,
	# customType: String = ""   Eg: customType_example
	# The type defined for the custom table column
	customType = "",
	# customValue: int   Eg: 1.2
	# The value for the custom table column
	customValue = null,
	# customValue2: int   Eg: 1.2
	# The value for another custom table column
	customValue2 = null,
	# customLong: float   Eg: 789
	# The value that supports Longs for custom table column
	customLong = null,
	# customLong2: float   Eg: 789
	# The value that supports Longs for custom table column
	customLong2 = null,
	# customMessage: String = ""   Eg: customMessage_example
	# The message for the custom table column
	customMessage = "",
	# customMessage2: String = ""   Eg: customMessage2_example
	# The message for the custom table column
	customMessage2 = "",
	# groupBy: String = ""   Eg: groupBy_example
	# Determines how to group results. For example, passing in 'CREATED' will return results by date. Passing in 'ACCOUNT' will return results by user {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID}
	groupBy = "",
	# distinctCount: String = ""   Eg: distinctCount_example
	# Performs a unique query on the specified column. For example, passing in 'ACCOUNT' will return the number of unique users. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID}
	distinctCount = "",
	# sumColumn: String = ""   Eg: sumColumn_example
	# sum of the column values
	sumColumn = "",
	# sortField: String = ""   Eg: sortField_example
	# Determines whether the results will be sorted by day or by number of hits {CREATED, TAG_COUNT}
	sortField = "",
	# descending: bool   Eg: true
	# Determines whether to return results in descending order
	descending = null,
	# hideUnknown: bool   Eg: true
	# Determines whether to return data that has empty or unknown values
	hideUnknown = null,
	# responseFormat: String = ""   Eg: responseFormat_example
	# Determines whether to return a JOSN or XML representation of the graph results
	responseFormat = "",
	# l: int   Eg: 56
	# This parameter is deprecated.
	l = null,
	# limit: int   Eg: 56
	# Used to limit results to get a cleaner graph. The results that gets filtered out will be combined
	limit = null,
	# latitude: int   Eg: 1.2
	# the current latitude of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# the current longitude of the user
	longitude = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/analytics/filteredUsage"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["applicationId"] = applicationId
	bzz_query["appKey"] = appKey
	bzz_query["startDate"] = startDate
	bzz_query["endDate"] = endDate
	bzz_query["deviceType"] = deviceType
	bzz_query["device"] = device
	bzz_query["deviceOS"] = deviceOS
	bzz_query["gender"] = gender
	bzz_query["ageGroup"] = ageGroup
	bzz_query["country"] = country
	bzz_query["state"] = state
	bzz_query["city"] = city
	bzz_query["zip"] = zip
	bzz_query["model"] = model
	bzz_query["tag"] = tag
	bzz_query["userAccountId"] = userAccountId
	bzz_query["userAccountDisplay"] = userAccountDisplay
	bzz_query["userAccountUsername"] = userAccountUsername
	bzz_query["customId"] = customId
	bzz_query["customType"] = customType
	bzz_query["customValue"] = customValue
	bzz_query["customValue2"] = customValue2
	bzz_query["customLong"] = customLong
	bzz_query["customLong2"] = customLong2
	bzz_query["customMessage"] = customMessage
	bzz_query["customMessage2"] = customMessage2
	bzz_query["groupBy"] = groupBy
	bzz_query["distinctCount"] = distinctCount
	bzz_query["sumColumn"] = sumColumn
	bzz_query["sortField"] = sortField
	bzz_query["descending"] = descending
	bzz_query["hideUnknown"] = hideUnknown
	bzz_query["responseFormat"] = responseFormat
	bzz_query["_l"] = l
	bzz_query["limit"] = limit
	bzz_query["latitude"] = latitude
	bzz_query["longitude"] = longitude

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ChartData.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func filtered_usage_threaded(
	# deviceId: String = ""   Eg: deviceId_example
	# The unique id of the device making the request (deviceId or accountId required)
	deviceId = "",
	# accountId: float   Eg: 789
	# The account id of the user (deviceId or accountId required)
	accountId = null,
	# applicationId: float   Eg: 789
	# This parameter is deprecated.
	applicationId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key used to filter results by application
	appKey = "",
	# startDate: float   Eg: 789
	# Used to search for results after or equal to this date (UNIX time-stamp in milliseconds)
	startDate = null,
	# endDate: float   Eg: 789
	# Used to search for results before or equal to this date (UNIX time-stamp in milliseconds)
	endDate = null,
	# deviceType: String = ""   Eg: deviceType_example
	# The device type to filter results by (performs a LIKE search)
	deviceType = "",
	# device: String = ""   Eg: device_example
	# The device to filter results by (performs a LIKE search)
	device = "",
	# deviceOS: String = ""   Eg: deviceOS_example
	# The device OS to filter results by (performs a LIKE search)
	deviceOS = "",
	# gender: String = ""   Eg: gender_example
	# The gender to filter results by {MALE, FEMALE}
	gender = "",
	# ageGroup: String = ""   Eg: ageGroup_example
	# Comma separated list of age groups to filter by {AGE_0_13, AGE_14_17, AGE_18_22, AGE_23_30, AGE_31_54, AGE_55_PLUS}
	ageGroup = "",
	# country: String = ""   Eg: country_example
	# The country to filter results by (performs a wild-card search)
	country = "",
	# state: String = ""   Eg: state_example
	# The state to filter results by (performs a wild-card search)
	state = "",
	# city: String = ""   Eg: city_example
	# The city to filter results by (performs a wild-card search)
	city = "",
	# zip: String = ""   Eg: zip_example
	# The zip to filter results by (performs a wild-card search)
	zip = "",
	# model: String = ""   Eg: model_example
	# The model to filter results by (performs a wild-card search)
	model = "",
	# tag: String = ""   Eg: tag_example
	# The tag to filter results by (performs a wild-card search)
	tag = "",
	# userAccountId: float   Eg: 789
	# The account id to filter results for a particular user
	userAccountId = null,
	# userAccountDisplay: String = ""   Eg: userAccountDisplay_example
	# The user display to filter results by (performs a wild-card search)
	userAccountDisplay = "",
	# userAccountUsername: String = ""   Eg: userAccountUsername_example
	# The username to filter results by (performs a wild-card search)
	userAccountUsername = "",
	# customId: float   Eg: 789
	# The ID for the custom table column
	customId = null,
	# customType: String = ""   Eg: customType_example
	# The type defined for the custom table column
	customType = "",
	# customValue: int   Eg: 1.2
	# The value for the custom table column
	customValue = null,
	# customValue2: int   Eg: 1.2
	# The value for another custom table column
	customValue2 = null,
	# customLong: float   Eg: 789
	# The value that supports Longs for custom table column
	customLong = null,
	# customLong2: float   Eg: 789
	# The value that supports Longs for custom table column
	customLong2 = null,
	# customMessage: String = ""   Eg: customMessage_example
	# The message for the custom table column
	customMessage = "",
	# customMessage2: String = ""   Eg: customMessage2_example
	# The message for the custom table column
	customMessage2 = "",
	# groupBy: String = ""   Eg: groupBy_example
	# Determines how to group results. For example, passing in 'CREATED' will return results by date. Passing in 'ACCOUNT' will return results by user {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID}
	groupBy = "",
	# distinctCount: String = ""   Eg: distinctCount_example
	# Performs a unique query on the specified column. For example, passing in 'ACCOUNT' will return the number of unique users. {TAG, CUSTOM_ID, TOKEN, MODEL, DEVICE_TYPE, DEVICE, DEVICE_OS, DEVICE_ID, IP_ADDRESS, STATE, CITY, ZIP, COUNTRY, ACCOUNT, APPLICATION_ID}
	distinctCount = "",
	# sumColumn: String = ""   Eg: sumColumn_example
	# sum of the column values
	sumColumn = "",
	# sortField: String = ""   Eg: sortField_example
	# Determines whether the results will be sorted by day or by number of hits {CREATED, TAG_COUNT}
	sortField = "",
	# descending: bool   Eg: true
	# Determines whether to return results in descending order
	descending = null,
	# hideUnknown: bool   Eg: true
	# Determines whether to return data that has empty or unknown values
	hideUnknown = null,
	# responseFormat: String = ""   Eg: responseFormat_example
	# Determines whether to return a JOSN or XML representation of the graph results
	responseFormat = "",
	# l: int   Eg: 56
	# This parameter is deprecated.
	l = null,
	# limit: int   Eg: 56
	# Used to limit results to get a cleaner graph. The results that gets filtered out will be combined
	limit = null,
	# latitude: int   Eg: 1.2
	# the current latitude of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# the current longitude of the user
	longitude = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "filtered_usage")
	bzz_callable.bind(
		deviceId,
		accountId,
		applicationId,
		appKey,
		startDate,
		endDate,
		deviceType,
		device,
		deviceOS,
		gender,
		ageGroup,
		country,
		state,
		city,
		zip,
		model,
		tag,
		userAccountId,
		userAccountDisplay,
		userAccountUsername,
		customId,
		customType,
		customValue,
		customValue2,
		customLong,
		customLong2,
		customMessage,
		customMessage2,
		groupBy,
		distinctCount,
		sumColumn,
		sortField,
		descending,
		hideUnknown,
		responseFormat,
		l,
		limit,
		latitude,
		longitude,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation usage → POST /analytics/usage
# Create Usage Record
#
# Record an analytic record for a known state within the application.
func usage(
	# tag: String = ""   Eg: tag_example
	# The tag to apply: the name of the action or thing being logged.
	tag: String,
	# deviceId: String = ""   Eg: deviceId_example
	# The client deviceID
	deviceId = "",
	# accountId: float   Eg: 789
	# The logged in user ID
	accountId = null,
	# applicationId: float   Eg: 789
	# This parameter is deprecated. This is deprecated, use appKey.
	applicationId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key unique to each application.
	appKey = "",
	# appVersion: String = ""   Eg: appVersion_example
	# The current build version of the application
	appVersion = "",
	# device: String = ""   Eg: device_example
	# The name of the device being used (iPhone5,1 , HTC Nexus One, x86_64, etc.)
	device = "",
	# deviceType: String = ""   Eg: deviceType_example
	# The type of device (Handheld or Desktop)
	deviceType = "",
	# deviceOS: String = ""   Eg: deviceOS_example
	# The operating system that the device is using (iPhone OS 5.0, Android OS 2.3.6, Windows 7 Service Pack 1, etc)
	deviceOS = "",
	# model: String = ""   Eg: model_example
	# The model of the device (iPhone5,1 , Nexus One, etc.)
	model = "",
	# latitude: int   Eg: 1.2
	# The current position of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# The current position of the user
	longitude = null,
	# customId: float   Eg: 789
	# A customId used to associate the usage record with some other record in the system
	customId = null,
	# customType: String = ""   Eg: customType_example
	# The type of record the customId is recorded for
	customType = "",
	# achievementIncrement: float   Eg: 789
	# This parameter is deprecated. This is deprecated, use \"customValue\". Determines how many achievement points and/or ranking points the user gets if this analytic is associated with an achievement
	achievementIncrement = null,
	# city: String = ""   Eg: city_example
	# The current city of the user
	city = "",
	# state: String = ""   Eg: state_example
	# The current state of the user
	state = "",
	# country: String = ""   Eg: country_example
	# The current country of the user
	country = "",
	# zip: String = ""   Eg: zip_example
	# The current zip of the user
	zip = "",
	# locationDescription: String = ""   Eg: locationDescription_example
	# The general description of the user's location
	locationDescription = "",
	# clientTime: float   Eg: 789
	# Timestamp sent from device
	clientTime = null,
	# errorMessage: String = ""   Eg: errorMessage_example
	# This parameter is deprecated.
	errorMessage = "",
	# some_ip: String = ""   Eg: some_ip_example
	# the ip address of the client device
	some_ip = "",
	# userAgent: String = ""   Eg: userAgent_example
	# this is a description of the client (might contain the device's OS, browser version etc - its a common term)
	userAgent = "",
	# backgroundEvent: bool   Eg: true
	# This is deprecated.
	backgroundEvent = null,
	# customMessage: String = ""   Eg: customMessage_example
	# a custom message for the usage record
	customMessage = "",
	# customMessage2: String = ""   Eg: customMessage2_example
	# a custom message for the usage record
	customMessage2 = "",
	# customValue: int   Eg: 1.2
	# a custom value for the usage record
	customValue = null,
	# customValue2: int   Eg: 1.2
	# a custom value for the usage record
	customValue2 = null,
	# customLong: float   Eg: 789
	# a custom long value for the usage record
	customLong = null,
	# customLong2: float   Eg: 789
	# a custom long value for the usage record
	customLong2 = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/analytics/usage"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["tag"] = tag
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["applicationId"] = applicationId
	bzz_query["appKey"] = appKey
	bzz_query["appVersion"] = appVersion
	bzz_query["device"] = device
	bzz_query["deviceType"] = deviceType
	bzz_query["deviceOS"] = deviceOS
	bzz_query["model"] = model
	bzz_query["latitude"] = latitude
	bzz_query["longitude"] = longitude
	bzz_query["customId"] = customId
	bzz_query["customType"] = customType
	bzz_query["achievementIncrement"] = achievementIncrement
	bzz_query["city"] = city
	bzz_query["state"] = state
	bzz_query["country"] = country
	bzz_query["zip"] = zip
	bzz_query["locationDescription"] = locationDescription
	bzz_query["clientTime"] = clientTime
	bzz_query["errorMessage"] = errorMessage
	bzz_query["ip"] = some_ip
	bzz_query["userAgent"] = userAgent
	bzz_query["backgroundEvent"] = backgroundEvent
	bzz_query["customMessage"] = customMessage
	bzz_query["customMessage2"] = customMessage2
	bzz_query["customValue"] = customValue
	bzz_query["customValue2"] = customValue2
	bzz_query["customLong"] = customLong
	bzz_query["customLong2"] = customLong2

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func usage_threaded(
	# tag: String = ""   Eg: tag_example
	# The tag to apply: the name of the action or thing being logged.
	tag: String,
	# deviceId: String = ""   Eg: deviceId_example
	# The client deviceID
	deviceId = "",
	# accountId: float   Eg: 789
	# The logged in user ID
	accountId = null,
	# applicationId: float   Eg: 789
	# This parameter is deprecated. This is deprecated, use appKey.
	applicationId = null,
	# appKey: String = ""   Eg: appKey_example
	# The application key unique to each application.
	appKey = "",
	# appVersion: String = ""   Eg: appVersion_example
	# The current build version of the application
	appVersion = "",
	# device: String = ""   Eg: device_example
	# The name of the device being used (iPhone5,1 , HTC Nexus One, x86_64, etc.)
	device = "",
	# deviceType: String = ""   Eg: deviceType_example
	# The type of device (Handheld or Desktop)
	deviceType = "",
	# deviceOS: String = ""   Eg: deviceOS_example
	# The operating system that the device is using (iPhone OS 5.0, Android OS 2.3.6, Windows 7 Service Pack 1, etc)
	deviceOS = "",
	# model: String = ""   Eg: model_example
	# The model of the device (iPhone5,1 , Nexus One, etc.)
	model = "",
	# latitude: int   Eg: 1.2
	# The current position of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# The current position of the user
	longitude = null,
	# customId: float   Eg: 789
	# A customId used to associate the usage record with some other record in the system
	customId = null,
	# customType: String = ""   Eg: customType_example
	# The type of record the customId is recorded for
	customType = "",
	# achievementIncrement: float   Eg: 789
	# This parameter is deprecated. This is deprecated, use \"customValue\". Determines how many achievement points and/or ranking points the user gets if this analytic is associated with an achievement
	achievementIncrement = null,
	# city: String = ""   Eg: city_example
	# The current city of the user
	city = "",
	# state: String = ""   Eg: state_example
	# The current state of the user
	state = "",
	# country: String = ""   Eg: country_example
	# The current country of the user
	country = "",
	# zip: String = ""   Eg: zip_example
	# The current zip of the user
	zip = "",
	# locationDescription: String = ""   Eg: locationDescription_example
	# The general description of the user's location
	locationDescription = "",
	# clientTime: float   Eg: 789
	# Timestamp sent from device
	clientTime = null,
	# errorMessage: String = ""   Eg: errorMessage_example
	# This parameter is deprecated.
	errorMessage = "",
	# some_ip: String = ""   Eg: some_ip_example
	# the ip address of the client device
	some_ip = "",
	# userAgent: String = ""   Eg: userAgent_example
	# this is a description of the client (might contain the device's OS, browser version etc - its a common term)
	userAgent = "",
	# backgroundEvent: bool   Eg: true
	# This is deprecated.
	backgroundEvent = null,
	# customMessage: String = ""   Eg: customMessage_example
	# a custom message for the usage record
	customMessage = "",
	# customMessage2: String = ""   Eg: customMessage2_example
	# a custom message for the usage record
	customMessage2 = "",
	# customValue: int   Eg: 1.2
	# a custom value for the usage record
	customValue = null,
	# customValue2: int   Eg: 1.2
	# a custom value for the usage record
	customValue2 = null,
	# customLong: float   Eg: 789
	# a custom long value for the usage record
	customLong = null,
	# customLong2: float   Eg: 789
	# a custom long value for the usage record
	customLong2 = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "usage")
	bzz_callable.bind(
		tag,
		deviceId,
		accountId,
		applicationId,
		appKey,
		appVersion,
		device,
		deviceType,
		deviceOS,
		model,
		latitude,
		longitude,
		customId,
		customType,
		achievementIncrement,
		city,
		state,
		country,
		zip,
		locationDescription,
		clientTime,
		errorMessage,
		some_ip,
		userAgent,
		backgroundEvent,
		customMessage,
		customMessage2,
		customValue,
		customValue2,
		customLong,
		customLong2,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation usageBatch → POST /analytics/usage/batch
# Create Multiple Usage Records
#
# Sends multiple analytics. Can be used to send in the user's stored usage when they did not have internet access. Should not include more than 100 items per batch.
func usage_batch(
	# appKey: String = ""   Eg: appKey_example
	# The application key unique to each application.
	appKey: String,
	# device: String = ""   Eg: device_example
	# The name of the device being used (iPhone5,1 , HTC Nexus One, x86_64, etc.)
	device: String,
	# data: String = ""   Eg: data_example
	# The analytic data AnalyticListResponse
	data: String,
	# deviceId: String = ""   Eg: deviceId_example
	# The unique id of the device making the request
	deviceId = "",
	# accountId: float   Eg: 789
	# The account ID of the logged in user making the request
	accountId = null,
	# appVersion: String = ""   Eg: appVersion_example
	# The current build version of the application
	appVersion = "",
	# deviceType: String = ""   Eg: deviceType_example
	# The type of device (Handheld or Desktop)
	deviceType = "",
	# deviceOS: String = ""   Eg: deviceOS_example
	# The operating system that the device is using (iPhone OS 5.0, Android OS 2.3.6, Windows 7 Service Pack 1, etc.)
	deviceOS = "",
	# model: String = ""   Eg: model_example
	# The model of the device (iPhone5,1 , Nexus One, etc.)
	model = "",
	# updateRanking: bool   Eg: true
	# Will create a leaderboard if one does not exist for the \"tag\" yet
	updateRanking = null,
	# returnSummaryResponse: bool   Eg: true
	# Returns a summary response of the achievements that have been completed due to the analytics
	returnSummaryResponse = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/analytics/usage/batch"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["appKey"] = appKey
	bzz_query["appVersion"] = appVersion
	bzz_query["device"] = device
	bzz_query["deviceType"] = deviceType
	bzz_query["deviceOS"] = deviceOS
	bzz_query["model"] = model
	bzz_query["data"] = data
	bzz_query["updateRanking"] = updateRanking
	bzz_query["returnSummaryResponse"] = returnSummaryResponse

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func usage_batch_threaded(
	# appKey: String = ""   Eg: appKey_example
	# The application key unique to each application.
	appKey: String,
	# device: String = ""   Eg: device_example
	# The name of the device being used (iPhone5,1 , HTC Nexus One, x86_64, etc.)
	device: String,
	# data: String = ""   Eg: data_example
	# The analytic data AnalyticListResponse
	data: String,
	# deviceId: String = ""   Eg: deviceId_example
	# The unique id of the device making the request
	deviceId = "",
	# accountId: float   Eg: 789
	# The account ID of the logged in user making the request
	accountId = null,
	# appVersion: String = ""   Eg: appVersion_example
	# The current build version of the application
	appVersion = "",
	# deviceType: String = ""   Eg: deviceType_example
	# The type of device (Handheld or Desktop)
	deviceType = "",
	# deviceOS: String = ""   Eg: deviceOS_example
	# The operating system that the device is using (iPhone OS 5.0, Android OS 2.3.6, Windows 7 Service Pack 1, etc.)
	deviceOS = "",
	# model: String = ""   Eg: model_example
	# The model of the device (iPhone5,1 , Nexus One, etc.)
	model = "",
	# updateRanking: bool   Eg: true
	# Will create a leaderboard if one does not exist for the \"tag\" yet
	updateRanking = null,
	# returnSummaryResponse: bool   Eg: true
	# Returns a summary response of the achievements that have been completed due to the analytics
	returnSummaryResponse = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "usage_batch")
	bzz_callable.bind(
		appKey,
		device,
		data,
		deviceId,
		accountId,
		appVersion,
		deviceType,
		deviceOS,
		model,
		updateRanking,
		returnSummaryResponse,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


