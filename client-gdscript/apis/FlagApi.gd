extends ApiBee
class_name FlagApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API FlagApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "FlagApi"


# Operation createFlag → POST /flag/create
# Create Flag
#
# Allows a user to flag an object that the user deems inappropriate or offensive. Flagable objects include accounts, albums, album contests, assets, game levels, and theme descriptors
func create_flag(
	# flagableType: String = ""   Eg: flagableType_example
	# The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, NOTE, OFFER}
	flagableType: String,
	# flagableId: float   Eg: 789
	# The flagable object id
	flagableId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The unique device identifier that made the request (either deviceId or accountId must be used)
	deviceId = "",
	# accountId: float   Eg: 789
	# The unique accountId that made the request (either deviceId or accountId must be used)
	accountId = null,
	# flagDescription: String = ""   Eg: flagDescription_example
	# An optional description of why is it being flagged
	flagDescription = "",
	# latitude: int   Eg: 1.2
	# The current location of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# The current location of the user
	longitude = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/flag/create"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["flagableType"] = flagableType
	bzz_query["flagableId"] = flagableId
	bzz_query["flagDescription"] = flagDescription
	bzz_query["latitude"] = latitude
	bzz_query["longitude"] = longitude

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func create_flag_threaded(
	# flagableType: String = ""   Eg: flagableType_example
	# The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, NOTE, OFFER}
	flagableType: String,
	# flagableId: float   Eg: 789
	# The flagable object id
	flagableId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The unique device identifier that made the request (either deviceId or accountId must be used)
	deviceId = "",
	# accountId: float   Eg: 789
	# The unique accountId that made the request (either deviceId or accountId must be used)
	accountId = null,
	# flagDescription: String = ""   Eg: flagDescription_example
	# An optional description of why is it being flagged
	flagDescription = "",
	# latitude: int   Eg: 1.2
	# The current location of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# The current location of the user
	longitude = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "create_flag")
	bzz_callable.bind(
		flagableType,
		flagableId,
		deviceId,
		accountId,
		flagDescription,
		latitude,
		longitude,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation deleteFlag → POST /flag/delete
# Delete Flag
#
# Deletes a flag.
func delete_flag(
	# deviceId: String = ""   Eg: deviceId_example
	# The unique device identifier that made the request (either deviceId or accountId must be used)
	deviceId = "",
	# accountId: float   Eg: 789
	# The unique accountId that made the request (either deviceId or accountId must be used)
	accountId = null,
	# itemBeingFlaggedType: String = ""   Eg: itemBeingFlaggedType_example
	# This parameter is deprecated.
	itemBeingFlaggedType = "",
	# itemBeingFlaggedId: float   Eg: 789
	# This parameter is deprecated.
	itemBeingFlaggedId = null,
	# flagableType: String = ""   Eg: flagableType_example
	# The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
	flagableType = "",
	# flagableId: float   Eg: 789
	# The flagable object id
	flagableId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/flag/delete"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["itemBeingFlaggedType"] = itemBeingFlaggedType
	bzz_query["itemBeingFlaggedId"] = itemBeingFlaggedId
	bzz_query["flagableType"] = flagableType
	bzz_query["flagableId"] = flagableId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = SirqulResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func delete_flag_threaded(
	# deviceId: String = ""   Eg: deviceId_example
	# The unique device identifier that made the request (either deviceId or accountId must be used)
	deviceId = "",
	# accountId: float   Eg: 789
	# The unique accountId that made the request (either deviceId or accountId must be used)
	accountId = null,
	# itemBeingFlaggedType: String = ""   Eg: itemBeingFlaggedType_example
	# This parameter is deprecated.
	itemBeingFlaggedType = "",
	# itemBeingFlaggedId: float   Eg: 789
	# This parameter is deprecated.
	itemBeingFlaggedId = null,
	# flagableType: String = ""   Eg: flagableType_example
	# The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
	flagableType = "",
	# flagableId: float   Eg: 789
	# The flagable object id
	flagableId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "delete_flag")
	bzz_callable.bind(
		deviceId,
		accountId,
		itemBeingFlaggedType,
		itemBeingFlaggedId,
		flagableType,
		flagableId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getFlag → GET /flag/get
# Get Flag
#
# Gets the details on whether the user has flagged a particular flagable object.
func get_flag(
	# flagableType: String = ""   Eg: flagableType_example
	# The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, NOTE, OFFER}
	flagableType: String,
	# flagableId: float   Eg: 789
	# The flagable object id
	flagableId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The unique device identifier that made the request (either deviceId or accountId must be used)
	deviceId = "",
	# accountId: float   Eg: 789
	# The unique accountId that made the request (either deviceId or accountId must be used)
	accountId = null,
	# latitude: int   Eg: 1.2
	# The current location of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# The current location of the user
	longitude = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/flag/get"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["flagableType"] = flagableType
	bzz_query["flagableId"] = flagableId
	bzz_query["latitude"] = latitude
	bzz_query["longitude"] = longitude

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = FlagResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_flag_threaded(
	# flagableType: String = ""   Eg: flagableType_example
	# The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, NOTE, OFFER}
	flagableType: String,
	# flagableId: float   Eg: 789
	# The flagable object id
	flagableId: float,
	# deviceId: String = ""   Eg: deviceId_example
	# The unique device identifier that made the request (either deviceId or accountId must be used)
	deviceId = "",
	# accountId: float   Eg: 789
	# The unique accountId that made the request (either deviceId or accountId must be used)
	accountId = null,
	# latitude: int   Eg: 1.2
	# The current location of the user
	latitude = null,
	# longitude: int   Eg: 1.2
	# The current location of the user
	longitude = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_flag")
	bzz_callable.bind(
		flagableType,
		flagableId,
		deviceId,
		accountId,
		latitude,
		longitude,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation getFlagThreshold → GET /flag/threshold/get
# Get Flag Threshold
#
# Get the flag threshold value on an object type for a particular application.
func get_flag_threshold(
	# itemBeingFlaggedType: String = ""   Eg: itemBeingFlaggedType_example
	# The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
	itemBeingFlaggedType: String,
	# appKey: String = ""   Eg: appKey_example
	# The application key
	appKey: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/flag/threshold/get"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["itemBeingFlaggedType"] = itemBeingFlaggedType
	bzz_query["appKey"] = appKey

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CountResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get_flag_threshold_threaded(
	# itemBeingFlaggedType: String = ""   Eg: itemBeingFlaggedType_example
	# The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
	itemBeingFlaggedType: String,
	# appKey: String = ""   Eg: appKey_example
	# The application key
	appKey: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get_flag_threshold")
	bzz_callable.bind(
		itemBeingFlaggedType,
		appKey,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation updateFlagThreshold → POST /flag/threshold/update
# Update Flag Threshold
#
# Update the flag threshold on an object type for a particular application.
func update_flag_threshold(
	# itemBeingFlaggedType: String = ""   Eg: itemBeingFlaggedType_example
	# The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
	itemBeingFlaggedType: String,
	# threshold: float   Eg: 789
	# The threshold value
	threshold: float,
	# appKey: String = ""   Eg: appKey_example
	# The application key
	appKey: String,
	# deviceId: String = ""   Eg: deviceId_example
	# The unique device identifier that made the request (either deviceId or accountId must be used)
	deviceId = "",
	# accountId: float   Eg: 789
	# The unique accountId that made the request (either deviceId or accountId must be used)
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/flag/threshold/update"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["deviceId"] = deviceId
	bzz_query["accountId"] = accountId
	bzz_query["itemBeingFlaggedType"] = itemBeingFlaggedType
	bzz_query["threshold"] = threshold
	bzz_query["appKey"] = appKey

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CountResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func update_flag_threshold_threaded(
	# itemBeingFlaggedType: String = ""   Eg: itemBeingFlaggedType_example
	# The flagable object type {ACCOUNT, ALBUM, ALBUM_CONTEST, ASSET, GAME_LEVEL, THEME_DESCRIPTOR, OFFER, NOTE}
	itemBeingFlaggedType: String,
	# threshold: float   Eg: 789
	# The threshold value
	threshold: float,
	# appKey: String = ""   Eg: appKey_example
	# The application key
	appKey: String,
	# deviceId: String = ""   Eg: deviceId_example
	# The unique device identifier that made the request (either deviceId or accountId must be used)
	deviceId = "",
	# accountId: float   Eg: 789
	# The unique accountId that made the request (either deviceId or accountId must be used)
	accountId = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "update_flag_threshold")
	bzz_callable.bind(
		itemBeingFlaggedType,
		threshold,
		appKey,
		deviceId,
		accountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


