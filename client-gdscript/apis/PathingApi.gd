extends ApiBee
class_name PathingApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API PathingApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "PathingApi"


# Operation computePath → GET /pathing/compute
# Calculate Path
#
# Calculates the shortest path from point to point on a grid
func compute_path(
	# data: String = ""   Eg: data_example
	# the data to with start, end point and exclusion points
	data: String,
	# units: String = ""   Eg: units_example
	# the system of measurement for directions: {METRIC, IMPERIAL}
	units: String,
	# reducePath: bool   Eg: true
	# determines whether to reduce the path to go in diagonal lines
	reducePath: bool,
	# directions: bool   Eg: true
	# determines whether to return text directions
	directions: bool,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/api/3.18/pathing/compute"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['*/*']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["data"] = data
	bzz_query["units"] = units
	bzz_query["reducePath"] = reducePath
	bzz_query["directions"] = directions

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = PathingResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func compute_path_threaded(
	# data: String = ""   Eg: data_example
	# the data to with start, end point and exclusion points
	data: String,
	# units: String = ""   Eg: units_example
	# the system of measurement for directions: {METRIC, IMPERIAL}
	units: String,
	# reducePath: bool   Eg: true
	# determines whether to reduce the path to go in diagonal lines
	reducePath: bool,
	# directions: bool   Eg: true
	# determines whether to return text directions
	directions: bool,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "compute_path")
	bzz_callable.bind(
		data,
		units,
		reducePath,
		directions,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


