/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Account from './Account';
import Asset from './Asset';
import Connection from './Connection';
import GroupPermissions from './GroupPermissions';

/**
 * The ConnectionGroup model module.
 * @module model/ConnectionGroup
 * @version 3.16
 */
class ConnectionGroup {
    /**
     * Constructs a new <code>ConnectionGroup</code>.
     * @alias module:model/ConnectionGroup
     */
    constructor() { 
        
        ConnectionGroup.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ConnectionGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ConnectionGroup} obj Optional instance to populate.
     * @return {module:model/ConnectionGroup} The populated <code>ConnectionGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ConnectionGroup();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('valid')) {
                obj['valid'] = ApiClient.convertToType(data['valid'], 'Boolean');
            }
            if (data.hasOwnProperty('owner')) {
                obj['owner'] = Account.constructFromObject(data['owner']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('connections')) {
                obj['connections'] = ApiClient.convertToType(data['connections'], [Connection]);
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = GroupPermissions.constructFromObject(data['permissions']);
            }
            if (data.hasOwnProperty('image')) {
                obj['image'] = Asset.constructFromObject(data['image']);
            }
            if (data.hasOwnProperty('subGroups')) {
                obj['subGroups'] = ApiClient.convertToType(data['subGroups'], [ConnectionGroup]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ConnectionGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ConnectionGroup</code>.
     */
    static validateJSON(data) {
        // validate the optional field `owner`
        if (data['owner']) { // data not null
          Account.validateJSON(data['owner']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['connections']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['connections'])) {
                throw new Error("Expected the field `connections` to be an array in the JSON data but got " + data['connections']);
            }
            // validate the optional field `connections` (array)
            for (const item of data['connections']) {
                Connection.validateJSON(item);
            };
        }
        // validate the optional field `permissions`
        if (data['permissions']) { // data not null
          GroupPermissions.validateJSON(data['permissions']);
        }
        // validate the optional field `image`
        if (data['image']) { // data not null
          Asset.validateJSON(data['image']);
        }
        if (data['subGroups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['subGroups'])) {
                throw new Error("Expected the field `subGroups` to be an array in the JSON data but got " + data['subGroups']);
            }
            // validate the optional field `subGroups` (array)
            for (const item of data['subGroups']) {
                ConnectionGroup.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Number} id
 */
ConnectionGroup.prototype['id'] = undefined;

/**
 * @member {Boolean} active
 */
ConnectionGroup.prototype['active'] = undefined;

/**
 * @member {Boolean} valid
 */
ConnectionGroup.prototype['valid'] = undefined;

/**
 * @member {module:model/Account} owner
 */
ConnectionGroup.prototype['owner'] = undefined;

/**
 * @member {String} name
 */
ConnectionGroup.prototype['name'] = undefined;

/**
 * @member {String} description
 */
ConnectionGroup.prototype['description'] = undefined;

/**
 * @member {Array.<module:model/Connection>} connections
 */
ConnectionGroup.prototype['connections'] = undefined;

/**
 * @member {module:model/GroupPermissions} permissions
 */
ConnectionGroup.prototype['permissions'] = undefined;

/**
 * @member {module:model/Asset} image
 */
ConnectionGroup.prototype['image'] = undefined;

/**
 * @member {Array.<module:model/ConnectionGroup>} subGroups
 */
ConnectionGroup.prototype['subGroups'] = undefined;






export default ConnectionGroup;

