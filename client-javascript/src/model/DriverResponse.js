/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AvailabilityResponse from './AvailabilityResponse';
import LocationResponse from './LocationResponse';

/**
 * The DriverResponse model module.
 * @module model/DriverResponse
 * @version 3.16
 */
class DriverResponse {
    /**
     * Constructs a new <code>DriverResponse</code>.
     * @alias module:model/DriverResponse
     */
    constructor() { 
        
        DriverResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DriverResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DriverResponse} obj Optional instance to populate.
     * @return {module:model/DriverResponse} The populated <code>DriverResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DriverResponse();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('depot')) {
                obj['depot'] = LocationResponse.constructFromObject(data['depot']);
            }
            if (data.hasOwnProperty('availability')) {
                obj['availability'] = ApiClient.convertToType(data['availability'], [AvailabilityResponse]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DriverResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DriverResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `depot`
        if (data['depot']) { // data not null
          LocationResponse.validateJSON(data['depot']);
        }
        if (data['availability']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['availability'])) {
                throw new Error("Expected the field `availability` to be an array in the JSON data but got " + data['availability']);
            }
            // validate the optional field `availability` (array)
            for (const item of data['availability']) {
                AvailabilityResponse.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {String} id
 */
DriverResponse.prototype['id'] = undefined;

/**
 * @member {String} name
 */
DriverResponse.prototype['name'] = undefined;

/**
 * @member {module:model/LocationResponse} depot
 */
DriverResponse.prototype['depot'] = undefined;

/**
 * @member {Array.<module:model/AvailabilityResponse>} availability
 */
DriverResponse.prototype['availability'] = undefined;






export default DriverResponse;

