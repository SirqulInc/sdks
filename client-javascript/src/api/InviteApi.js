/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import ConsumerInviteResponse from '../model/ConsumerInviteResponse';
import InviteResponse from '../model/InviteResponse';
import SirqulResponse from '../model/SirqulResponse';

/**
* Invite service.
* @module api/InviteApi
* @version 3.16
*/
export default class InviteApi {

    /**
    * Constructs a new InviteApi. 
    * @alias module:api/InviteApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the acceptInvite operation.
     * @callback module:api/InviteApi~acceptInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ConsumerInviteResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Accept Invite
     * Allows a user to accept an invite. The user could also become the inviter's friend.
     * @param {Number} version 
     * @param {String} token the invite token
     * @param {Number} accountId the accountId of the user who is accepting the invite
     * @param {Object} opts Optional parameters
     * @param {Number} [albumId] the album id associated with this invite (if applicable)
     * @param {Number} [missionId] the mission id associated with this invite (if applicable)
     * @param {Number} [albumContestId] the album contest id associated with this invite (if applicable)
     * @param {Number} [offerId] the offer id associated with this invite (if applicable)
     * @param {Number} [offerLocationId] the offer location id associated with this invite (if applicable)
     * @param {Number} [retailerLocationId] the retailer location id associated with this invite (if applicable)
     * @param {String} [appKey] the application key
     * @param {Boolean} [autoFriend = true)] whether to auto-friend the invite sender and receiver
     * @param {Boolean} [autoAttendEvent = false)] whether to mark the event as attending automatically after invite is accepted
     * @param {Boolean} [autoFavoriteOffer = false)] whether to mark the offer as favorited automatically after invite is accepted
     * @param {Boolean} [autoFavoriteOfferLocation = false)] whether to mark the offer location as favorited automatically after invite is accepted
     * @param {Boolean} [autoFavoriteRetailerLocation = false)] whether to mark the retailer location as favorited automatically after invite is accepted
     * @param {module:api/InviteApi~acceptInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ConsumerInviteResponse}
     */
    acceptInvite(version, token, accountId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling acceptInvite");
      }
      // verify the required parameter 'token' is set
      if (token === undefined || token === null) {
        throw new Error("Missing the required parameter 'token' when calling acceptInvite");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling acceptInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'token': token,
        'accountId': accountId,
        'albumId': opts['albumId'],
        'missionId': opts['missionId'],
        'albumContestId': opts['albumContestId'],
        'offerId': opts['offerId'],
        'offerLocationId': opts['offerLocationId'],
        'retailerLocationId': opts['retailerLocationId'],
        'appKey': opts['appKey'],
        'autoFriend': opts['autoFriend'],
        'autoAttendEvent': opts['autoAttendEvent'],
        'autoFavoriteOffer': opts['autoFavoriteOffer'],
        'autoFavoriteOfferLocation': opts['autoFavoriteOfferLocation'],
        'autoFavoriteRetailerLocation': opts['autoFavoriteRetailerLocation']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = ConsumerInviteResponse;
      return this.apiClient.callApi(
        '/api/{version}/invite/accept', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the albumContestInvite operation.
     * @callback module:api/InviteApi~albumContestInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/InviteResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Invite to Contest
     * Allows a user to invite people to gain access to a contest. This will generate an invite token, which when used, will give the invitee access to a contest (whether it is private or not). The invitee will also become the user's friend when the invitation is accepted.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique ID given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account ID of the user (deviceId or accountId required)
     * @param {Number} [appId] This parameter is deprecated.
     * @param {String} [appKey] the application key
     * @param {Number} [albumContestId] the album contest to share
     * @param {Number} [latitude] the current latitude of the user
     * @param {Number} [longitude] the current longitude of the user
     * @param {module:api/InviteApi~albumContestInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/InviteResponse}
     */
    albumContestInvite(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling albumContestInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appId': opts['appId'],
        'appKey': opts['appKey'],
        'albumContestId': opts['albumContestId'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = InviteResponse;
      return this.apiClient.callApi(
        '/api/{version}/invite/albumContest', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the albumInvite operation.
     * @callback module:api/InviteApi~albumInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/InviteResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Invite to Collection
     * Allows a user to invite people to gain access to a collection. This will generate an invite token, which when used, will give the invitee access to a collection (whether it is private or not). The invitee will also become the user's friend when the invitation is accepted.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique ID given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account ID of the user (deviceId or accountId required)
     * @param {Number} [appId] This parameter is deprecated.
     * @param {String} [appKey] the application key
     * @param {Number} [albumId] the album to share
     * @param {Number} [latitude] the current latitude of the user
     * @param {Number} [longitude] the current longitude of the user
     * @param {module:api/InviteApi~albumInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/InviteResponse}
     */
    albumInvite(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling albumInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appId': opts['appId'],
        'appKey': opts['appKey'],
        'albumId': opts['albumId'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = InviteResponse;
      return this.apiClient.callApi(
        '/api/{version}/invite/album', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the eventInvite operation.
     * @callback module:api/InviteApi~eventInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/InviteResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Invite to Event
     * Allows a user to invite people to attend an event. This will generate an invite token, which when used, will allow the invitee to add the offer to their wallet.
     * @param {Number} version 
     * @param {Number} accountId the account ID of the user making the share
     * @param {String} appKey the application key
     * @param {Number} listingId The ID of the event listing
     * @param {Object} opts Optional parameters
     * @param {String} [receiverAccountIds] the account ID of a Sirqul user they would like to share an event with
     * @param {Number} [retailerLocationId] The retailer location id of where the event will take place
     * @param {module:api/InviteApi~eventInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/InviteResponse}
     */
    eventInvite(version, accountId, appKey, listingId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling eventInvite");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling eventInvite");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling eventInvite");
      }
      // verify the required parameter 'listingId' is set
      if (listingId === undefined || listingId === null) {
        throw new Error("Missing the required parameter 'listingId' when calling eventInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'receiverAccountIds': opts['receiverAccountIds'],
        'appKey': appKey,
        'listingId': listingId,
        'retailerLocationId': opts['retailerLocationId']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = InviteResponse;
      return this.apiClient.callApi(
        '/api/{version}/invite/event', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the gameInvite operation.
     * @callback module:api/InviteApi~gameInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/InviteResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Invite to Game Level
     * Allows a user to invite people to gain access to an album. This will generate an invite token, which when used, will give the invitee access to an album (whether it is private or not). The invitee will also become the user's friend when the invitation is accepted.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique ID given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account ID of the user (deviceId or accountId required)
     * @param {Number} [appId] This parameter is deprecated.
     * @param {String} [appKey] the application key
     * @param {Number} [gameLevelId] the game level that the user owns and is giving access to
     * @param {Number} [latitude] the current latitude of the user
     * @param {Number} [longitude] the current longitude of the user
     * @param {module:api/InviteApi~gameInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/InviteResponse}
     */
    gameInvite(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling gameInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appId': opts['appId'],
        'appKey': opts['appKey'],
        'gameLevelId': opts['gameLevelId'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = InviteResponse;
      return this.apiClient.callApi(
        '/api/{version}/invite/gameLevel', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getInvite operation.
     * @callback module:api/InviteApi~getInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Invite
     * This is used to determine whether an invite token is valid. If the token is valid, this will also return information about who invited the user, and what they are invited to.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {Number} [accountId] Account ID of the user if they are logged in
     * @param {String} [token] the invite token
     * @param {Number} [albumId] album id to match the invite against (if applicable)
     * @param {Number} [missionId] mission id to match the invite against (if applicable)
     * @param {Number} [albumContestId] album contest id to match the invite against (if applicable)
     * @param {Number} [offerId] offer id to match the invite against (if applicable)
     * @param {Number} [offerLocationId] offer location id to match the invite against (if applicable)
     * @param {Number} [retailerLocationId] retailer location id to match the invite against (if applicable)
     * @param {String} [appKey] the application key
     * @param {module:api/InviteApi~getInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    getInvite(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling getInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': opts['accountId'],
        'token': opts['token'],
        'albumId': opts['albumId'],
        'missionId': opts['missionId'],
        'albumContestId': opts['albumContestId'],
        'offerId': opts['offerId'],
        'offerLocationId': opts['offerLocationId'],
        'retailerLocationId': opts['retailerLocationId'],
        'appKey': opts['appKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/api/{version}/invite/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the missionInvite operation.
     * @callback module:api/InviteApi~missionInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/InviteResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Invite to Mission
     * Allows a user to invite people to gain access to a mission. This will generate an invite token, which when used, will give the invitee access to a mission (whether it is private or not). The invitee will also become the user's friend when the invitation is accepted.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique ID given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account ID of the user (deviceId or accountId required)
     * @param {Number} [appId] This parameter is deprecated.
     * @param {String} [appKey] the application key
     * @param {Number} [missionId] the mission to share
     * @param {Number} [latitude] the current latitude of the user
     * @param {Number} [longitude] the current longitude of the user
     * @param {module:api/InviteApi~missionInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/InviteResponse}
     */
    missionInvite(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling missionInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appId': opts['appId'],
        'appKey': opts['appKey'],
        'missionId': opts['missionId'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = InviteResponse;
      return this.apiClient.callApi(
        '/api/{version}/invite/mission', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the offerInvite operation.
     * @callback module:api/InviteApi~offerInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/InviteResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Invite to Offer
     * Allows a user to invite people to favorite an offer. This will generate an invite token, which when used, will give the invitee the offer in their favorite's list.
     * @param {Number} version 
     * @param {Number} accountId the account ID of the user making the share
     * @param {String} appKey the application key
     * @param {Number} offerId the ID of the offer used to invite to favorite
     * @param {module:api/InviteApi~offerInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/InviteResponse}
     */
    offerInvite(version, accountId, appKey, offerId, callback) {
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling offerInvite");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling offerInvite");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling offerInvite");
      }
      // verify the required parameter 'offerId' is set
      if (offerId === undefined || offerId === null) {
        throw new Error("Missing the required parameter 'offerId' when calling offerInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'appKey': appKey,
        'offerId': offerId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = InviteResponse;
      return this.apiClient.callApi(
        '/api/{version}/invite/offer', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the offerLocationInvite operation.
     * @callback module:api/InviteApi~offerLocationInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/InviteResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Invite to Offer Location
     * Allows a user to invite people to favorite an offer location. This will generate an invite token, which when used, will give the invitee the offer location in their favorite's list.
     * @param {Number} version 
     * @param {Number} accountId the account ID of the user making the share
     * @param {String} appKey the application key
     * @param {Number} offerLocationId the id of the offer location to share
     * @param {module:api/InviteApi~offerLocationInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/InviteResponse}
     */
    offerLocationInvite(version, accountId, appKey, offerLocationId, callback) {
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling offerLocationInvite");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling offerLocationInvite");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling offerLocationInvite");
      }
      // verify the required parameter 'offerLocationId' is set
      if (offerLocationId === undefined || offerLocationId === null) {
        throw new Error("Missing the required parameter 'offerLocationId' when calling offerLocationInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'appKey': appKey,
        'offerLocationId': offerLocationId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = InviteResponse;
      return this.apiClient.callApi(
        '/api/{version}/invite/offerLocation', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the retailerLocationInvite operation.
     * @callback module:api/InviteApi~retailerLocationInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/InviteResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Invite to Retailer Location
     * Allows a user to invite people to favorite a retailer location. This will generate an invite token, which when used, will give the invitee the retailer location in their favorite's list.
     * @param {Number} version 
     * @param {Number} accountId the account ID of the user making the share
     * @param {String} appKey the application key
     * @param {Number} retailerLocationId The retailer location id of where the event will take place
     * @param {Object} opts Optional parameters
     * @param {Number} [albumId] Optional album id to link with the invite
     * @param {module:api/InviteApi~retailerLocationInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/InviteResponse}
     */
    retailerLocationInvite(version, accountId, appKey, retailerLocationId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling retailerLocationInvite");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling retailerLocationInvite");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling retailerLocationInvite");
      }
      // verify the required parameter 'retailerLocationId' is set
      if (retailerLocationId === undefined || retailerLocationId === null) {
        throw new Error("Missing the required parameter 'retailerLocationId' when calling retailerLocationInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'appKey': appKey,
        'retailerLocationId': retailerLocationId,
        'albumId': opts['albumId']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = InviteResponse;
      return this.apiClient.callApi(
        '/api/{version}/invite/retailerLocation', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
