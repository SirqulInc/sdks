/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import MissionResponse from '../model/MissionResponse';
import SirqulResponse from '../model/SirqulResponse';

/**
* MissionInvite service.
* @module api/MissionInviteApi
* @version 3.16
*/
export default class MissionInviteApi {

    /**
    * Constructs a new MissionInviteApi. 
    * @alias module:api/MissionInviteApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the createMissionInvite operation.
     * @callback module:api/MissionInviteApi~createMissionInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/MissionResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Mission Invite
     * Create the mission invite. An account can only be invited to a mission one time. For missions that require user submission and reviewing the permissionableType and permissionableId need to be provided.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the device id (deviceId or accountId required).
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required).
     * @param {Number} [missionId] The mission to find the invite for.
     * @param {String} [joinCode] code to be entered for user to join the mission
     * @param {Boolean} [includeGameData] Include the game level data with the mission.
     * @param {module:api/MissionInviteApi~createMissionInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/MissionResponse}
     */
    createMissionInvite(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling createMissionInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'missionId': opts['missionId'],
        'joinCode': opts['joinCode'],
        'includeGameData': opts['includeGameData']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = MissionResponse;
      return this.apiClient.callApi(
        '/api/{version}/mission/invite/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the deleteMissionInvite operation.
     * @callback module:api/MissionInviteApi~deleteMissionInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete Mission Invite
     * Update the mission invite status to quit.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the device id (deviceId or accountId required).
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required).
     * @param {Number} [missionId] The mission to find the invite for (missionId or missionInviteId requried).
     * @param {Number} [missionInviteId] The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried).
     * @param {Boolean} [includeGameData] Include the game level data with the mission.
     * @param {module:api/MissionInviteApi~deleteMissionInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    deleteMissionInvite(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling deleteMissionInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'missionId': opts['missionId'],
        'missionInviteId': opts['missionInviteId'],
        'includeGameData': opts['includeGameData']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/api/{version}/mission/invite/delete', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getMissionInvite operation.
     * @callback module:api/MissionInviteApi~getMissionInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/MissionResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Mission Invite
     * Get the mission invite. An account can only be invited to a mission one time.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the device id (deviceId or accountId required).
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required).
     * @param {Number} [missionId] The mission to find the invite for (missionId or missionInviteId requried).
     * @param {Number} [missionInviteId] The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried).
     * @param {Boolean} [includeGameData] Include the game level data with the mission.
     * @param {String} [includeScores] include the scores with the mission
     * @param {module:api/MissionInviteApi~getMissionInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/MissionResponse}
     */
    getMissionInvite(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling getMissionInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'missionId': opts['missionId'],
        'missionInviteId': opts['missionInviteId'],
        'includeGameData': opts['includeGameData'],
        'includeScores': opts['includeScores']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = MissionResponse;
      return this.apiClient.callApi(
        '/api/{version}/mission/invite/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchMissionInvites operation.
     * @callback module:api/MissionInviteApi~searchMissionInvitesCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/MissionResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Mission Invites
     * Get a list of mission invites that the account has.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the device id (deviceId or accountId required).
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required).
     * @param {String} [appKey] the app to retrieve the data for, use your application key.
     * @param {String} [appVersion] the application version, used to version the game level data
     * @param {Number} [missionId] the mission id to filter on
     * @param {String} [status] The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED}
     * @param {Number} [lastUpdated] Only return invites that have been updated since this date/time (long)
     * @param {Number} [start] The index into the record set to start with.
     * @param {Number} [limit] The total number of record to return.
     * @param {String} [keyword] the keyword to search on
     * @param {String} [missionTypes] 
     * @param {Boolean} [filterByBillable] filter results by the account's billable
     * @param {Boolean} [includeGameData] Include the game level data with the mission.
     * @param {module:api/MissionInviteApi~searchMissionInvitesCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/MissionResponse>}
     */
    searchMissionInvites(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling searchMissionInvites");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appKey': opts['appKey'],
        'appVersion': opts['appVersion'],
        'missionId': opts['missionId'],
        'status': opts['status'],
        'lastUpdated': opts['lastUpdated'],
        'start': opts['start'],
        'limit': opts['limit'],
        'keyword': opts['keyword'],
        'missionTypes': opts['missionTypes'],
        'filterByBillable': opts['filterByBillable'],
        'includeGameData': opts['includeGameData']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [MissionResponse];
      return this.apiClient.callApi(
        '/api/{version}/mission/invite/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the updateMissionInvite operation.
     * @callback module:api/MissionInviteApi~updateMissionInviteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/MissionResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update Mission Invite
     * Update the mission invite status. An account can only be invited to a mission one time. For missions that require user submission and reviewing the permissionableType and permissionableId need to be provided.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the device id (deviceId or accountId required).
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required).
     * @param {String} [appKey] the application key
     * @param {Number} [missionId] The mission to find the invite for (missionId or missionInviteId requried).
     * @param {Number} [missionInviteId] The mission invite id. This checks on the user's billable for permission (missionId or missionInviteId requried).
     * @param {Number} [packId] the pack id to find the invite for
     * @param {Number} [gameLevelId] the game level id to find the invite for
     * @param {String} [status] The invite status to filter for, comma separated list {AVAILABLE, PENDING, UPDATED, ACCEPTED, ACTIVE, QUIT, COMPLETED, PENDING_REVIEW, REJECTED}
     * @param {String} [permissionableType] This is the content type for missions that require user submission. Note that user submitted content must require a status of PENDING_REVIEW to be accpeted. {ACCOUNT, GAMELEVEL, COLLECTION, ALBUM_CONTEST, THEME_DESCRIPTOR, ALBUM}
     * @param {Number} [permissionableId] The id of the content being submitted.
     * @param {Boolean} [includeGameData] Include the game level data with the mission.
     * @param {module:api/MissionInviteApi~updateMissionInviteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/MissionResponse}
     */
    updateMissionInvite(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling updateMissionInvite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appKey': opts['appKey'],
        'missionId': opts['missionId'],
        'missionInviteId': opts['missionInviteId'],
        'packId': opts['packId'],
        'gameLevelId': opts['gameLevelId'],
        'status': opts['status'],
        'permissionableType': opts['permissionableType'],
        'permissionableId': opts['permissionableId'],
        'includeGameData': opts['includeGameData']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = MissionResponse;
      return this.apiClient.callApi(
        '/api/{version}/mission/invite/update', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
