/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import ScoreResponse from '../model/ScoreResponse';

/**
* Score service.
* @module api/ScoreApi
* @version 3.16
*/
export default class ScoreApi {

    /**
    * Constructs a new ScoreApi. 
    * @alias module:api/ScoreApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the createScore operation.
     * @callback module:api/ScoreApi~createScoreCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ScoreResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Score
     * Create a score.  The response object will contain a series of   coded messages detailing what items were completed, the score registered,   and any tickets allocated.  Scoring a  level could complete the pack it   is in, completing that pack could complete the game, which  in turn could   complete the mission.  This completion chain is indicated to the client   via  a list of {@link MessageResponse}.
     * @param {Number} accountId The logged in user.
     * @param {String} appKey The game application key to save the score for.
     * @param {Number} points The score
     * @param {Object} opts Optional parameters
     * @param {Number} [missionId] The missionId to score for, ignore if not playing mission.
     * @param {Number} [gameId] The gameId to score for, ignore if not playing mission.
     * @param {Number} [packId] The packId to score for, send -2 if playing community levels.
     * @param {Number} [gameLevelId] The gameLevelId to score for.
     * @param {Number} [gameObjectId] The gameObjectId to score for, ignore if level based scoring.
     * @param {Number} [timeTaken] The time taken to complete task
     * @param {Boolean} [highest] 
     * @param {module:api/ScoreApi~createScoreCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ScoreResponse}
     */
    createScore(accountId, appKey, points, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling createScore");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling createScore");
      }
      // verify the required parameter 'points' is set
      if (points === undefined || points === null) {
        throw new Error("Missing the required parameter 'points' when calling createScore");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'appKey': appKey,
        'missionId': opts['missionId'],
        'gameId': opts['gameId'],
        'packId': opts['packId'],
        'gameLevelId': opts['gameLevelId'],
        'gameObjectId': opts['gameObjectId'],
        'points': points,
        'timeTaken': opts['timeTaken'],
        'highest': opts['highest']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = ScoreResponse;
      return this.apiClient.callApi(
        '/score/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getScore operation.
     * @callback module:api/ScoreApi~getScoreCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ScoreResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Score
     * Get the high score for an item.  Pass in the full path IDs for the score.
     * @param {Number} accountId The logged in user.
     * @param {String} appKey The game application key to get the level for.
     * @param {Object} opts Optional parameters
     * @param {Number} [missionId] The missionId to score for, null if not playing mission.
     * @param {Number} [gameId] The gameId to score for, null if not playing mission.
     * @param {Number} [packId] The packId to score for, null if playing community levels.
     * @param {Number} [gameLevelId] The gameLevelId to score for.
     * @param {Number} [gameObjectId] The gameObjectId to score for, null if level based scoring.
     * @param {String} [scoreObjectType] The object type to filter scores by (TicketObjectType)
     * @param {String} [scoreStatus] The status of the score to filter (ScoreStatus)
     * @param {module:api/ScoreApi~getScoreCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ScoreResponse}
     */
    getScore(accountId, appKey, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling getScore");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling getScore");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'appKey': appKey,
        'missionId': opts['missionId'],
        'gameId': opts['gameId'],
        'packId': opts['packId'],
        'gameLevelId': opts['gameLevelId'],
        'gameObjectId': opts['gameObjectId'],
        'scoreObjectType': opts['scoreObjectType'],
        'scoreStatus': opts['scoreStatus']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = ScoreResponse;
      return this.apiClient.callApi(
        '/score/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchScores operation.
     * @callback module:api/ScoreApi~searchScoresCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/ScoreResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Score
     * Search the scores for an item.  Pass in the full path IDs for the scores.
     * @param {Number} accountId The logged in user.
     * @param {String} appKey The game application key to get the level for.
     * @param {Object} opts Optional parameters
     * @param {Number} [missionId] The missionId to score for, null if not playing mission.
     * @param {Number} [gameId] The gameId to score for, null if not playing mission.
     * @param {Number} [packId] The packId to score for, null if playing community levels.
     * @param {Number} [gameLevelId] The gameLevelId to score for.
     * @param {Number} [gameObjectId] The gameObjectId to score for, null if level based scoring.
     * @param {module:api/ScoreApi~searchScoresCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/ScoreResponse>}
     */
    searchScores(accountId, appKey, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling searchScores");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling searchScores");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'appKey': appKey,
        'missionId': opts['missionId'],
        'gameId': opts['gameId'],
        'packId': opts['packId'],
        'gameLevelId': opts['gameLevelId'],
        'gameObjectId': opts['gameObjectId']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [ScoreResponse];
      return this.apiClient.callApi(
        '/score/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
