/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import MissionFormatResponse from '../model/MissionFormatResponse';
import MissionResponse from '../model/MissionResponse';
import SirqulResponse from '../model/SirqulResponse';

/**
* Mission service.
* @module api/MissionApi
* @version 3.16
*/
export default class MissionApi {

    /**
    * Constructs a new MissionApi. 
    * @alias module:api/MissionApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the createMission operation.
     * @callback module:api/MissionApi~createMissionCallback
     * @param {String} error Error message, if any.
     * @param {module:model/MissionResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Mission
     * Create a user defined mission.
     * @param {Number} version 
     * @param {Number} accountId The logged in user.
     * @param {String} title The title of the mission
     * @param {Object} opts Optional parameters
     * @param {String} [description] The description of the mission
     * @param {String} [subType] Custom string client apps can use for searching/filtering missions
     * @param {Number} [startDate] The date/time to start the mission, send 0 to unset
     * @param {Number} [endDate] The date/time to end the mission, send 0 to unset
     * @param {Boolean} [active] Activate/deactivate the mission
     * @param {String} [gameLevelIds] the game level ids to include in the mission, comma separated
     * @param {String} [creativeIds] creatives to associate with the mission
     * @param {String} [audienceIds] audiences to associate with the mission
     * @param {String} [missionTask] This parameter is deprecated.
     * @param {String} [formatType] The string identifier for a mission format (this is not the missionFormatId)
     * @param {Number} [offerId] The offerId to give as a reward
     * @param {Number} [balance] Set the amount of money available to spend, once 0 deactivate mission. Set to a negative value for unlimited.
     * @param {Boolean} [advancedReporting] Flag to mark the mission as eligible for advanced reporting
     * @param {Boolean} [allocateTickets] Flag to indicate owner should receive tickets for completed missions
     * @param {Number} [ticketCount] The number of tickets to reward
     * @param {String} [ticketType] The type of ticket to reward, null means default type
     * @param {Number} [points] The number of points to award for completing a mission
     * @param {String} [metaData] external custom client defined data
     * @param {String} [applicationIds] Comma separated list of application ids
     * @param {String} [devices] Deprecated parameter for device names
     * @param {String} [deviceIds] Comma separated list of device ids
     * @param {String} [deviceVersions] Comma separated list of device version ranges
     * @param {String} [locations] List of lat/long pairs for mission locations
     * @param {String} [radius] Comma separated list of radii for locations
     * @param {module:api/MissionApi~createMissionCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/MissionResponse}
     */
    createMission(version, accountId, title, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling createMission");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling createMission");
      }
      // verify the required parameter 'title' is set
      if (title === undefined || title === null) {
        throw new Error("Missing the required parameter 'title' when calling createMission");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'title': title,
        'description': opts['description'],
        'subType': opts['subType'],
        'startDate': opts['startDate'],
        'endDate': opts['endDate'],
        'active': opts['active'],
        'gameLevelIds': opts['gameLevelIds'],
        'creativeIds': opts['creativeIds'],
        'audienceIds': opts['audienceIds'],
        'missionTask': opts['missionTask'],
        'formatType': opts['formatType'],
        'offerId': opts['offerId'],
        'balance': opts['balance'],
        'advancedReporting': opts['advancedReporting'],
        'allocateTickets': opts['allocateTickets'],
        'ticketCount': opts['ticketCount'],
        'ticketType': opts['ticketType'],
        'points': opts['points'],
        'metaData': opts['metaData'],
        'applicationIds': opts['applicationIds'],
        'devices': opts['devices'],
        'deviceIds': opts['deviceIds'],
        'deviceVersions': opts['deviceVersions'],
        'locations': opts['locations'],
        'radius': opts['radius']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = MissionResponse;
      return this.apiClient.callApi(
        '/api/{version}/mission/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the deleteMission operation.
     * @callback module:api/MissionApi~deleteMissionCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete Mission
     * Delete a mission.
     * @param {Number} version 
     * @param {Number} accountId the id of the logged in user
     * @param {Number} missionId the id of the mission to delete
     * @param {module:api/MissionApi~deleteMissionCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    deleteMission(version, accountId, missionId, callback) {
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling deleteMission");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling deleteMission");
      }
      // verify the required parameter 'missionId' is set
      if (missionId === undefined || missionId === null) {
        throw new Error("Missing the required parameter 'missionId' when calling deleteMission");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'missionId': missionId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/api/{version}/mission/delete', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the findMissions operation.
     * @callback module:api/MissionApi~findMissionsCallback
     * @param {String} error Error message, if any.
     * @param {module:model/MissionResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Find Missions
     * Get a set of ad filtered by the parameters provided.
     * @param {Number} version 
     * @param {String} appKey The application key, if provided return missions specific for the app. Will always return mission levels that are app agnostic.
     * @param {Object} opts Optional parameters
     * @param {String} [suffix] The type of mission to get, possible values are: click_banner, click_leaderboard, click_skyscraper, click_full, click_video, or click_zip
     * @param {String} [type] The type of ads to get, possible values are: BANNER, LEADERBOARD, SKYSCRAPER, FULL, VIDEO, ZIP, CONFIG. Use this instead of suffix.
     * @param {Number} [accountId] The logged in user.
     * @param {String} [appVersion] The version of the application, will not return levels newer than the appVersion.
     * @param {Number} [latitude] The current location of the requesting device
     * @param {Number} [longitude] The current location of the requesting device
     * @param {String} [device] Should use deviceId if possible. The name of the requesting device; possible values are: Android, iPhone, iPad, iPod, etc. use /audience/devices for list.
     * @param {Number} [deviceIdentifier] The device ID of the requesting device, use /audience/devices for list
     * @param {String} [deviceVersion] The requesting device version; examples are: 2.3, 5.1.4, 6.1.4. Supports X, X.X, or X.X.X formated string.
     * @param {Number} [start] The index into the record set to start with.
     * @param {Number} [limit] The total number of record to return.
     * @param {Boolean} [includeGameData] Include the game level data with the mission.
     * @param {Boolean} [includeAudiences] If true then return the audience data in the response. Default is false.
     * @param {Boolean} [allocatesTickets] If true/false only return missions whose game levels allocate (or don't allocate) tickets. Do not provide a value to return both.
     * @param {Boolean} [randomize] return a random set of results, default is true. If false returns in natural order.
     * @param {Boolean} [targetedAdsOnly] return only ads targeted to the specific app, no global ads.
     * @param {String} [missionIds] return only ads from the specified campaigns.
     * @param {String} [audienceOperator] will return the items that have at least 1 or all of their audiences exist in the logged in user’s audiences, depending if the value is OR or AND
     * @param {module:api/MissionApi~findMissionsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/MissionResponse}
     */
    findMissions(version, appKey, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling findMissions");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling findMissions");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'appKey': appKey,
        'suffix': opts['suffix'],
        'type': opts['type'],
        'accountId': opts['accountId'],
        'appVersion': opts['appVersion'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'device': opts['device'],
        'deviceIdentifier': opts['deviceIdentifier'],
        'deviceVersion': opts['deviceVersion'],
        'start': opts['start'],
        'limit': opts['limit'],
        'includeGameData': opts['includeGameData'],
        'includeAudiences': opts['includeAudiences'],
        'allocatesTickets': opts['allocatesTickets'],
        'randomize': opts['randomize'],
        'targetedAdsOnly': opts['targetedAdsOnly'],
        'missionIds': opts['missionIds'],
        'audienceOperator': opts['audienceOperator']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = MissionResponse;
      return this.apiClient.callApi(
        '/api/{version}/mission/find', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getMission operation.
     * @callback module:api/MissionApi~getMissionCallback
     * @param {String} error Error message, if any.
     * @param {module:model/MissionResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Mission
     * Get a mission.
     * @param {Number} version 
     * @param {Number} accountId The logged in user.
     * @param {Number} missionId The id of the mission to return.
     * @param {Object} opts Optional parameters
     * @param {Boolean} [returnCreative] Return creatives associated with the mission when true
     * @param {module:api/MissionApi~getMissionCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/MissionResponse}
     */
    getMission(version, accountId, missionId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling getMission");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling getMission");
      }
      // verify the required parameter 'missionId' is set
      if (missionId === undefined || missionId === null) {
        throw new Error("Missing the required parameter 'missionId' when calling getMission");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'missionId': missionId,
        'returnCreative': opts['returnCreative']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = MissionResponse;
      return this.apiClient.callApi(
        '/api/{version}/mission/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the importMission operation.
     * @callback module:api/MissionApi~importMissionCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Import Mission
     * Create a mission using a source item such as an offer location.
     * @param {Number} version 
     * @param {Number} accountId The logged in user.
     * @param {Number} latitude The current location of the requesting device
     * @param {Number} longitude The current location of the requesting device
     * @param {String} appKey the application key
     * @param {Object} opts Optional parameters
     * @param {String} [keyword] the keyword of the mission
     * @param {Number} [start] The starting index in the result set to return. Default is 0.
     * @param {Number} [limit] The total number of records to return. Default is 20.
     * @param {module:model/String} [adSize] the size of the ad
     * @param {module:api/MissionApi~importMissionCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    importMission(version, accountId, latitude, longitude, appKey, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling importMission");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling importMission");
      }
      // verify the required parameter 'latitude' is set
      if (latitude === undefined || latitude === null) {
        throw new Error("Missing the required parameter 'latitude' when calling importMission");
      }
      // verify the required parameter 'longitude' is set
      if (longitude === undefined || longitude === null) {
        throw new Error("Missing the required parameter 'longitude' when calling importMission");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling importMission");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'latitude': latitude,
        'longitude': longitude,
        'keyword': opts['keyword'],
        'start': opts['start'],
        'limit': opts['limit'],
        'appKey': appKey,
        'adSize': opts['adSize']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/api/{version}/mission/import', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchMissionFormats operation.
     * @callback module:api/MissionApi~searchMissionFormatsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/MissionFormatResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Mission Formats
     * Searches on pre-defined mission formats
     * @param {Number} version 
     * @param {Number} start The starting index in the result set to return. Default is 0.
     * @param {Number} limit The total number of records to return. Default is 20.
     * @param {Boolean} activeOnly Determines whether to return only active results. Default is false.
     * @param {module:api/MissionApi~searchMissionFormatsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/MissionFormatResponse>}
     */
    searchMissionFormats(version, start, limit, activeOnly, callback) {
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling searchMissionFormats");
      }
      // verify the required parameter 'start' is set
      if (start === undefined || start === null) {
        throw new Error("Missing the required parameter 'start' when calling searchMissionFormats");
      }
      // verify the required parameter 'limit' is set
      if (limit === undefined || limit === null) {
        throw new Error("Missing the required parameter 'limit' when calling searchMissionFormats");
      }
      // verify the required parameter 'activeOnly' is set
      if (activeOnly === undefined || activeOnly === null) {
        throw new Error("Missing the required parameter 'activeOnly' when calling searchMissionFormats");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'start': start,
        'limit': limit,
        'activeOnly': activeOnly
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [MissionFormatResponse];
      return this.apiClient.callApi(
        '/api/{version}/mission/format/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchMissions operation.
     * @callback module:api/MissionApi~searchMissionsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/MissionResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Missions
     * Get the list missions available to the account.  
     * @param {Number} version 
     * @param {Number} accountId The logged in user.
     * @param {Object} opts Optional parameters
     * @param {String} [keyword] Filter by keyword
     * @param {String} [subType] Custom string client apps can use for searching/filtering missions
     * @param {Number} [start] The index into the record set to start with. Default is 0.
     * @param {Number} [limit] The total number of record to return. Default is 20, maximum is 100.
     * @param {Boolean} [includeGameData] If true then return the game level data in the response. Default is false.
     * @param {Boolean} [includeAudiences] If true then return the audience data in the response. Default is false.
     * @param {Boolean} [includeInactive] If true then include inactive missions. Default is false.
     * @param {String} [suffix] A list of game types to include; click_leaderboard, click_banner, click_skyscraper, click_full, click_video, click_zip, create, vote, question, invite, share, json, xml
     * @param {String} [sortField] The field to sort the search on (for example TITLE)
     * @param {Boolean} [descending] Whether to sort in descending order (default true)
     * @param {module:api/MissionApi~searchMissionsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/MissionResponse>}
     */
    searchMissions(version, accountId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling searchMissions");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling searchMissions");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'keyword': opts['keyword'],
        'subType': opts['subType'],
        'start': opts['start'],
        'limit': opts['limit'],
        'includeGameData': opts['includeGameData'],
        'includeAudiences': opts['includeAudiences'],
        'includeInactive': opts['includeInactive'],
        'suffix': opts['suffix'],
        'sortField': opts['sortField'],
        'descending': opts['descending']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [MissionResponse];
      return this.apiClient.callApi(
        '/api/{version}/mission/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchMissionsByBillableEntity operation.
     * @callback module:api/MissionApi~searchMissionsByBillableEntityCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/MissionResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Missions by Billable Entity
     * Use the accountId to determine the associated BillableEntity.  From there get a list of all accounts associated as managers.  Get the list missions owned by all associated managers.
     * @param {Number} version 
     * @param {Number} accountId The logged in user.
     * @param {Object} opts Optional parameters
     * @param {String} [keyword] Filter by keyword
     * @param {Number} [start] The index into the record set to start with. Default is 0.
     * @param {Number} [limit] The total number of record to return. Default id 20.
     * @param {Boolean} [includeGameData] If true then return the game level data in the response. Default is false.
     * @param {Boolean} [includeAudiences] If true then return the audience data in the response. Default is false.
     * @param {Boolean} [includeInactive] If true then include inactive missions. Default is false.
     * @param {String} [suffix] A list of game types to include; click_leaderboard, click_banner, click_skyscraper, click_full, click_video, click_zip, create, vote, question, invite, share, json, xml
     * @param {String} [sortField] The field to sort the search on (for example TITLE)
     * @param {Boolean} [descending] Whether to sort in descending order (default true)
     * @param {module:api/MissionApi~searchMissionsByBillableEntityCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/MissionResponse>}
     */
    searchMissionsByBillableEntity(version, accountId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling searchMissionsByBillableEntity");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling searchMissionsByBillableEntity");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'keyword': opts['keyword'],
        'start': opts['start'],
        'limit': opts['limit'],
        'includeGameData': opts['includeGameData'],
        'includeAudiences': opts['includeAudiences'],
        'includeInactive': opts['includeInactive'],
        'suffix': opts['suffix'],
        'sortField': opts['sortField'],
        'descending': opts['descending']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [MissionResponse];
      return this.apiClient.callApi(
        '/api/{version}/mission/searchByBillableEntity', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the updateMission operation.
     * @callback module:api/MissionApi~updateMissionCallback
     * @param {String} error Error message, if any.
     * @param {module:model/MissionResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update Mission
     * Update a mission.
     * @param {Number} version 
     * @param {Number} accountId The logged in user.
     * @param {Number} missionId The id of the mission to update.
     * @param {Object} opts Optional parameters
     * @param {String} [title] The title of the mission
     * @param {String} [description] The description of the mission
     * @param {String} [subType] Custom string client apps can use for searching/filtering missions
     * @param {String} [metaData] external custom client defined data
     * @param {Number} [startDate] The date/time to start the mission, send 0 to unset
     * @param {Number} [endDate] The date/time to end the mission, send 0 to unset
     * @param {Boolean} [active] Activate/deactivate the mission
     * @param {String} [gameLevelIds] the game level ids to include in the mission, comma separated
     * @param {String} [creativeIds] 
     * @param {String} [audienceIds] 
     * @param {Number} [offerId] The offerId to give as a reward
     * @param {Number} [balance] Replace the amount of money available to spend, once 0 deactivate mission. Set to a negative value for unlimited.
     * @param {Boolean} [advancedReporting] Flag to mark the mission as eligible for advanced reporting
     * @param {Boolean} [allocateTickets] Flag to indicate owner should receive tickets for completed missions
     * @param {Number} [ticketCount] The number of tickets to reward
     * @param {String} [ticketType] The type of ticket to reward, null means default type
     * @param {Number} [points] The number of points to award for completing a mission
     * @param {String} [applicationIds] Comma separated list of application ids
     * @param {String} [devices] Deprecated parameter for device names
     * @param {String} [deviceIds] Comma separated list of device ids
     * @param {String} [deviceVersions] Comma separated list of device version ranges
     * @param {String} [locations] List of lat/long pairs for mission locations
     * @param {String} [radius] Comma separated list of radii for locations
     * @param {module:api/MissionApi~updateMissionCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/MissionResponse}
     */
    updateMission(version, accountId, missionId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling updateMission");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling updateMission");
      }
      // verify the required parameter 'missionId' is set
      if (missionId === undefined || missionId === null) {
        throw new Error("Missing the required parameter 'missionId' when calling updateMission");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'accountId': accountId,
        'missionId': missionId,
        'title': opts['title'],
        'description': opts['description'],
        'subType': opts['subType'],
        'metaData': opts['metaData'],
        'startDate': opts['startDate'],
        'endDate': opts['endDate'],
        'active': opts['active'],
        'gameLevelIds': opts['gameLevelIds'],
        'creativeIds': opts['creativeIds'],
        'audienceIds': opts['audienceIds'],
        'offerId': opts['offerId'],
        'balance': opts['balance'],
        'advancedReporting': opts['advancedReporting'],
        'allocateTickets': opts['allocateTickets'],
        'ticketCount': opts['ticketCount'],
        'ticketType': opts['ticketType'],
        'points': opts['points'],
        'applicationIds': opts['applicationIds'],
        'devices': opts['devices'],
        'deviceIds': opts['deviceIds'],
        'deviceVersions': opts['deviceVersions'],
        'locations': opts['locations'],
        'radius': opts['radius']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = MissionResponse;
      return this.apiClient.callApi(
        '/api/{version}/mission/update', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
