/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import RatingIndexResponse from '../model/RatingIndexResponse';
import RatingResponse from '../model/RatingResponse';
import SirqulResponse from '../model/SirqulResponse';

/**
* Rating service.
* @module api/RatingApi
* @version 3.16
*/
export default class RatingApi {

    /**
    * Constructs a new RatingApi. 
    * @alias module:api/RatingApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the createRating operation.
     * @callback module:api/RatingApi~createRatingCallback
     * @param {String} error Error message, if any.
     * @param {module:model/RatingResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Rating
     * This is used to leave rating on a ratable object (i.e. retailer locations). Each user can only rate on a ratable object once per category. If a user rates on the same object and category, the previous rating will be overwritten. Leaving a rating on a ratable object will be visible to everyone who has access to view the object.
     * @param {Number} version 
     * @param {String} ratableType The ratable object type {RETAILER_LOCATION}
     * @param {Number} ratableId The id of the ratable object
     * @param {Number} ratingValue The integer value of 0-100
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The unique device identifier that made the request (either deviceId or accountId must be used)
     * @param {Number} [accountId] The unique accountId that made the request (either deviceId or accountId must be used)
     * @param {Number} [categoryId] 
     * @param {String} [display] A subject title for the user rating (limited to 255 characters)
     * @param {String} [description] The description of the rating
     * @param {String} [locationDescription] The description of the location
     * @param {Number} [latitude] The current location of the user
     * @param {Number} [longitude] The current location of the user
     * @param {module:api/RatingApi~createRatingCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/RatingResponse}
     */
    createRating(version, ratableType, ratableId, ratingValue, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling createRating");
      }
      // verify the required parameter 'ratableType' is set
      if (ratableType === undefined || ratableType === null) {
        throw new Error("Missing the required parameter 'ratableType' when calling createRating");
      }
      // verify the required parameter 'ratableId' is set
      if (ratableId === undefined || ratableId === null) {
        throw new Error("Missing the required parameter 'ratableId' when calling createRating");
      }
      // verify the required parameter 'ratingValue' is set
      if (ratingValue === undefined || ratingValue === null) {
        throw new Error("Missing the required parameter 'ratingValue' when calling createRating");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'ratableType': ratableType,
        'ratableId': ratableId,
        'ratingValue': ratingValue,
        'categoryId': opts['categoryId'],
        'display': opts['display'],
        'description': opts['description'],
        'locationDescription': opts['locationDescription'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = RatingResponse;
      return this.apiClient.callApi(
        '/api/{version}/rating/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the deleteRating operation.
     * @callback module:api/RatingApi~deleteRatingCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete Rating
     * Sets a rating as deleted.
     * @param {Number} version 
     * @param {Number} ratingId The ID of the rating to delete
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The unique device identifier that made the request (either deviceId or accountId must be used)
     * @param {Number} [accountId] The unique accountId that made the request (either deviceId or accountId must be used)
     * @param {module:api/RatingApi~deleteRatingCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    deleteRating(version, ratingId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling deleteRating");
      }
      // verify the required parameter 'ratingId' is set
      if (ratingId === undefined || ratingId === null) {
        throw new Error("Missing the required parameter 'ratingId' when calling deleteRating");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'ratingId': ratingId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/api/{version}/rating/delete', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchLocationRatingIndexes operation.
     * @callback module:api/RatingApi~searchLocationRatingIndexesCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/RatingIndexResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Location Rating Indexes
     * Search for retailer locations by averages near you.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [categoryIds] Comma separated list of category ids to filter the results by
     * @param {String} [keyword] The keyword used to search
     * @param {String} [locationType] The type of location to filter the results by
     * @param {module:model/String} [sortField] The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, COUNT, SUMMATION, AVERAGE, VALUE}
     * @param {Boolean} [descending] The order to return the search results
     * @param {Number} [start] The record to begin the return set on
     * @param {Number} [limit] The number of records to return
     * @param {Number} [searchRange] The search radius in kilometers to filter results
     * @param {Number} [latitude] The current location of the user
     * @param {Number} [longitude] The current location of the user
     * @param {Boolean} [returnOverallRating] Determines whether to return the overall rating record instead
     * @param {module:model/String} [distanceUnit] 
     * @param {Boolean} [returnRetailer] whether to return the retailer or not
     * @param {Boolean} [returnAssets] whether to return the assets or not
     * @param {Boolean} [returnOffers] whether to return the offers or not
     * @param {Boolean} [returnCategories] whether to return the categories or not
     * @param {Boolean} [returnFilters] whether to return the filters or not
     * @param {module:api/RatingApi~searchLocationRatingIndexesCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/RatingIndexResponse>}
     */
    searchLocationRatingIndexes(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling searchLocationRatingIndexes");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'categoryIds': opts['categoryIds'],
        'keyword': opts['keyword'],
        'locationType': opts['locationType'],
        'sortField': opts['sortField'],
        'descending': opts['descending'],
        'start': opts['start'],
        'limit': opts['limit'],
        'searchRange': opts['searchRange'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'returnOverallRating': opts['returnOverallRating'],
        'distanceUnit': opts['distanceUnit'],
        'returnRetailer': opts['returnRetailer'],
        'returnAssets': opts['returnAssets'],
        'returnOffers': opts['returnOffers'],
        'returnCategories': opts['returnCategories'],
        'returnFilters': opts['returnFilters']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [RatingIndexResponse];
      return this.apiClient.callApi(
        '/api/{version}/location/rating/index/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchRatingIndexes operation.
     * @callback module:api/RatingApi~searchRatingIndexesCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/RatingIndexResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Rating Indexes
     * Search for ratable items by averages.
     * @param {Number} version 
     * @param {module:model/String} ratableType Filter results by a ratable type {RETAILER_LOCATION}
     * @param {Object} opts Optional parameters
     * @param {String} [ratableIds] Comma separated list of ratable ids to filter the resuts by
     * @param {String} [categoryIds] Comma separated list of category ids to filter the results by
     * @param {String} [secondaryType] 
     * @param {String} [keyword] The keyword used to search
     * @param {module:model/String} [sortField] The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, COUNT, SUMMATION, AVERAGE, VALUE}
     * @param {Boolean} [descending] The order to return the search results
     * @param {Number} [start] The record to begin the return set on
     * @param {Number} [limit] The number of records to return
     * @param {Number} [latitude] 
     * @param {Number} [longitude] 
     * @param {Boolean} [returnRatable] Determines whether to return the ratable object in the response
     * @param {Boolean} [returnOverallRating] Determines whether to return the overall rating record instead
     * @param {module:api/RatingApi~searchRatingIndexesCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/RatingIndexResponse>}
     */
    searchRatingIndexes(version, ratableType, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling searchRatingIndexes");
      }
      // verify the required parameter 'ratableType' is set
      if (ratableType === undefined || ratableType === null) {
        throw new Error("Missing the required parameter 'ratableType' when calling searchRatingIndexes");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'ratableType': ratableType,
        'ratableIds': opts['ratableIds'],
        'categoryIds': opts['categoryIds'],
        'secondaryType': opts['secondaryType'],
        'keyword': opts['keyword'],
        'sortField': opts['sortField'],
        'descending': opts['descending'],
        'start': opts['start'],
        'limit': opts['limit'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'returnRatable': opts['returnRatable'],
        'returnOverallRating': opts['returnOverallRating']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [RatingIndexResponse];
      return this.apiClient.callApi(
        '/api/{version}/rating/index/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchRatings operation.
     * @callback module:api/RatingApi~searchRatingsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/RatingResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Ratings
     * Search for ratings on a ratable object.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The device id (deviceId or accountId required)
     * @param {Number} [accountId] The account id of the user (deviceId or accountId required)
     * @param {Number} [filterAccountId] Filter results for a particular account
     * @param {String} [ratableType] The ratable object type {RETAILER_LOCATION}
     * @param {Number} [ratableId] The id of the ratable object
     * @param {String} [categoryIds] Comma separated list of category ids to filter the results by
     * @param {String} [keyword] The keyword used to search
     * @param {module:model/String} [sortField] The column to sort the search on {ID, CREATED, UPDATED, ACTIVE, OWNER_DISPLAY, RATABLE_TYPE, RATABLE_ID, RATABLE_DISPLAY, CATEGORY_ID, CATEGORY_NAME, CATEGORY_SHORT_NAME, CATEGORY_DISPLAY, VALUE}
     * @param {Boolean} [descending] The order to return the search results
     * @param {Number} [start] The record to begin the return set on
     * @param {Number} [limit] The number of records to return
     * @param {module:api/RatingApi~searchRatingsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/RatingResponse>}
     */
    searchRatings(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling searchRatings");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'filterAccountId': opts['filterAccountId'],
        'ratableType': opts['ratableType'],
        'ratableId': opts['ratableId'],
        'categoryIds': opts['categoryIds'],
        'keyword': opts['keyword'],
        'sortField': opts['sortField'],
        'descending': opts['descending'],
        'start': opts['start'],
        'limit': opts['limit']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [RatingResponse];
      return this.apiClient.callApi(
        '/api/{version}/rating/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the updateRating operation.
     * @callback module:api/RatingApi~updateRatingCallback
     * @param {String} error Error message, if any.
     * @param {module:model/RatingResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update Rating
     * Update an existing rating. Only the creator of the rating have permission to update.
     * @param {Number} version 
     * @param {Number} ratingId The id of the rating (Note: this is not the ratable object id)
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The unique device identifier that made the request (either deviceId or accountId must be used)
     * @param {Number} [accountId] The unique accountId that made the request (either deviceId or accountId must be used)
     * @param {Number} [ratingValue] The rating value to update
     * @param {Number} [categoryId] 
     * @param {String} [display] A subject title for the user rating (limited to 255 characters)
     * @param {String} [description] The description of the rating
     * @param {String} [locationDescription] The description of the location
     * @param {Number} [latitude] The current location of the user
     * @param {Number} [longitude] The current location of the user
     * @param {module:api/RatingApi~updateRatingCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/RatingResponse}
     */
    updateRating(version, ratingId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling updateRating");
      }
      // verify the required parameter 'ratingId' is set
      if (ratingId === undefined || ratingId === null) {
        throw new Error("Missing the required parameter 'ratingId' when calling updateRating");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'ratingId': ratingId,
        'ratingValue': opts['ratingValue'],
        'categoryId': opts['categoryId'],
        'display': opts['display'],
        'description': opts['description'],
        'locationDescription': opts['locationDescription'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = RatingResponse;
      return this.apiClient.callApi(
        '/api/{version}/rating/update', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
