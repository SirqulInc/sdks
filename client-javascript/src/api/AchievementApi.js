/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import AchievementProgressResponse from '../model/AchievementProgressResponse';
import AchievementResponse from '../model/AchievementResponse';
import AchievementShortResponse from '../model/AchievementShortResponse';
import AchievementTierResponse from '../model/AchievementTierResponse';
import SirqulResponse from '../model/SirqulResponse';

/**
* Achievement service.
* @module api/AchievementApi
* @version 3.16
*/
export default class AchievementApi {

    /**
    * Constructs a new AchievementApi. 
    * @alias module:api/AchievementApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the achievementTierSearchPost operation.
     * @callback module:api/AchievementApi~achievementTierSearchPostCallback
     * @param {String} error Error message, if any.
     * @param {module:model/AchievementTierResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Searches an Achievement Tier
     * Searches a tier of an achievement (for developer/retailer use). User must have permissions to the application the achievement is created for.
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique id given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {String} [appKey] the application key
     * @param {String} [keyword] the keyword used to filter resutls with (this returns results that have the keyword in the title or the description of the achievement tier)
     * @param {Number} [achievementType] filter results by achievementType (these are exact case sensitive matches)
     * @param {String} [rankType] filter results by the rankType (these are the exact case sensitive matches)
     * @param {String} [sortField] the field to sort by. See {@link AchievementApiMap}
     * @param {Boolean} [descending] determines whether the sort list is in descending or ascending order (of the achievement)
     * @param {Boolean} [descendingGoal] determines whether the results are in descending or ascending order by the tier goal count (after the initial sort on the achievement)
     * @param {Number} [start] The start of the index for pagination
     * @param {Number} [limit] the limit for pagination (has a hard limit of 1000)
     * @param {module:api/AchievementApi~achievementTierSearchPostCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/AchievementTierResponse}
     */
    achievementTierSearchPost(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appKey': opts['appKey'],
        'keyword': opts['keyword'],
        'achievementType': opts['achievementType'],
        'rankType': opts['rankType'],
        'sortField': opts['sortField'],
        'descending': opts['descending'],
        'descendingGoal': opts['descendingGoal'],
        'start': opts['start'],
        'limit': opts['limit']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = AchievementTierResponse;
      return this.apiClient.callApi(
        '/achievement/tier/search', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the createAchievement operation.
     * @callback module:api/AchievementApi~createAchievementCallback
     * @param {String} error Error message, if any.
     * @param {module:model/AchievementResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Achievement
     * Updates an achievement (for developer/retailer use). User must have permissions to the application the achievement is created for.
     * @param {String} appKey the application key the achievement is for
     * @param {String} title the title of the achievement (255 character limit)
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique id given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {String} [analyticsTag] the analytics tag that will trigger when a user's achievement count gets updated
     * @param {String} [description] the description of the achievement
     * @param {String} [rankType] the rank type for updating leader boards
     * @param {Number} [rankIncrement] determines how much the rank count is incremented
     * @param {Number} [minIncrement] restrict scores to be above or equal to this minimum value
     * @param {Number} [maxIncrement] restrict scores to be below or equal to this maximum value
     * @param {Boolean} [validate] determines whether the customId on analytics are used to validate a user's achievement progress.
     * @param {Boolean} [active] achievement is active or inactive
     * @param {String} [triggerDefinition] if provided will define what triggers to run after a tier is completed
     * @param {module:api/AchievementApi~createAchievementCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/AchievementResponse}
     */
    createAchievement(appKey, title, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling createAchievement");
      }
      // verify the required parameter 'title' is set
      if (title === undefined || title === null) {
        throw new Error("Missing the required parameter 'title' when calling createAchievement");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appKey': appKey,
        'analyticsTag': opts['analyticsTag'],
        'title': title,
        'description': opts['description'],
        'rankType': opts['rankType'],
        'rankIncrement': opts['rankIncrement'],
        'minIncrement': opts['minIncrement'],
        'maxIncrement': opts['maxIncrement'],
        'validate': opts['validate'],
        'active': opts['active'],
        'triggerDefinition': opts['triggerDefinition']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = AchievementResponse;
      return this.apiClient.callApi(
        '/achievement/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the createAchievementTier operation.
     * @callback module:api/AchievementApi~createAchievementTierCallback
     * @param {String} error Error message, if any.
     * @param {module:model/AchievementTierResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Achievement Tier
     * Create a tier of an achievement (for developer/retailer use). User must have permissions to the application the achievement is created for.
     * @param {Number} achievementId the achievement id for adding a new tier
     * @param {Boolean} scoreAllInstances score all instances
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique id given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {File} [icon] achievement tier icon image file
     * @param {Number} [iconAssetId] the icon assetId, if icon is provided, icon will overrule
     * @param {String} [title] the title of the achievement tier
     * @param {String} [description] the description of the achievement tier
     * @param {Number} [goalCount] the count requirement for completing the achievement tier
     * @param {Number} [missionId] The ID of the mission to associate with the achievement
     * @param {Number} [gameId] The ID of the game to associate with the achievement
     * @param {Number} [packId] The ID of the pack to associate with the achievement
     * @param {Number} [gameLevelId] The ID of the game level to associate with the achievement
     * @param {Number} [gameObjectId] The ID of the game object to associate with the achievement
     * @param {module:api/AchievementApi~createAchievementTierCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/AchievementTierResponse}
     */
    createAchievementTier(achievementId, scoreAllInstances, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'achievementId' is set
      if (achievementId === undefined || achievementId === null) {
        throw new Error("Missing the required parameter 'achievementId' when calling createAchievementTier");
      }
      // verify the required parameter 'scoreAllInstances' is set
      if (scoreAllInstances === undefined || scoreAllInstances === null) {
        throw new Error("Missing the required parameter 'scoreAllInstances' when calling createAchievementTier");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'achievementId': achievementId,
        'icon': opts['icon'],
        'iconAssetId': opts['iconAssetId'],
        'title': opts['title'],
        'description': opts['description'],
        'goalCount': opts['goalCount'],
        'missionId': opts['missionId'],
        'gameId': opts['gameId'],
        'packId': opts['packId'],
        'gameLevelId': opts['gameLevelId'],
        'gameObjectId': opts['gameObjectId'],
        'scoreAllInstances': scoreAllInstances
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = AchievementTierResponse;
      return this.apiClient.callApi(
        '/achievement/tier/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the deleteAchievement operation.
     * @callback module:api/AchievementApi~deleteAchievementCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete Achievement
     * Deletes an achievement (for developer/retailer use). User must have permissions to the application the achievement was created for.
     * @param {Number} achievementId The ID of the achievement
     * @param {Object} opts Optional parameters
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {module:api/AchievementApi~deleteAchievementCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    deleteAchievement(achievementId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'achievementId' is set
      if (achievementId === undefined || achievementId === null) {
        throw new Error("Missing the required parameter 'achievementId' when calling deleteAchievement");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': opts['accountId'],
        'achievementId': achievementId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/achievement/delete', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the deleteAchievementTier operation.
     * @callback module:api/AchievementApi~deleteAchievementTierCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete Achievement Tier
     * Deletes an achievement tier (for developer/retailer use). User must have permissions to the application the achievement was created for.
     * @param {Number} achievementTierId the achievement id for deletion
     * @param {Object} opts Optional parameters
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required).
     * @param {module:api/AchievementApi~deleteAchievementTierCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    deleteAchievementTier(achievementTierId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'achievementTierId' is set
      if (achievementTierId === undefined || achievementTierId === null) {
        throw new Error("Missing the required parameter 'achievementTierId' when calling deleteAchievementTier");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': opts['accountId'],
        'achievementTierId': achievementTierId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/achievement/tier/delete', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getAchievement operation.
     * @callback module:api/AchievementApi~getAchievementCallback
     * @param {String} error Error message, if any.
     * @param {module:model/AchievementTierResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Achievement
     * Get an achievement (for developer/retailer use). User must have permissions to the application the achievement is created for.
     * @param {Number} achievementId The ID of the achievement
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique id given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {String} [achievementType] achievementType
     * @param {module:api/AchievementApi~getAchievementCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/AchievementTierResponse}
     */
    getAchievement(achievementId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'achievementId' is set
      if (achievementId === undefined || achievementId === null) {
        throw new Error("Missing the required parameter 'achievementId' when calling getAchievement");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'achievementId': achievementId,
        'achievementType': opts['achievementType']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = AchievementTierResponse;
      return this.apiClient.callApi(
        '/achievement/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getAchievementTier operation.
     * @callback module:api/AchievementApi~getAchievementTierCallback
     * @param {String} error Error message, if any.
     * @param {module:model/AchievementTierResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Gets an achievement tier
     * Gets an achievement tier (for developer/retailer use). User must have permissions to the application the achievement is created for.
     * @param {Number} accountId the account id of the user (deviceId or accountId required)
     * @param {Number} achievementTierId the achievement tier id that is being retrieved
     * @param {module:api/AchievementApi~getAchievementTierCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/AchievementTierResponse}
     */
    getAchievementTier(accountId, achievementTierId, callback) {
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling getAchievementTier");
      }
      // verify the required parameter 'achievementTierId' is set
      if (achievementTierId === undefined || achievementTierId === null) {
        throw new Error("Missing the required parameter 'achievementTierId' when calling getAchievementTier");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'achievementTierId': achievementTierId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = AchievementTierResponse;
      return this.apiClient.callApi(
        '/achievement/tier/get', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getUserAchievements operation.
     * @callback module:api/AchievementApi~getUserAchievementsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/AchievementProgressResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Achievement Progress
     * Gets a list of user achievements.
     * @param {Boolean} returnNulls determines whether to return null fields in the response
     * @param {String} appKey the application key for filtering results by application
     * @param {Boolean} includeUndiscovered determines whether to return achievements that the user has not discovered yet
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique id given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {String} [connectionAccountEmail] the email of the account to view achievements
     * @param {Number} [connectionAccountId] the id of the account to view achievements
     * @param {String} [rankType] filter results by achievement rankType
     * @param {String} [achievementType] filter results by achievement type
     * @param {Number} [latitude] the current latitude of the user
     * @param {Number} [longitude] the current longitude of the user
     * @param {module:api/AchievementApi~getUserAchievementsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/AchievementProgressResponse>}
     */
    getUserAchievements(returnNulls, appKey, includeUndiscovered, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'returnNulls' is set
      if (returnNulls === undefined || returnNulls === null) {
        throw new Error("Missing the required parameter 'returnNulls' when calling getUserAchievements");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling getUserAchievements");
      }
      // verify the required parameter 'includeUndiscovered' is set
      if (includeUndiscovered === undefined || includeUndiscovered === null) {
        throw new Error("Missing the required parameter 'includeUndiscovered' when calling getUserAchievements");
      }

      let pathParams = {
      };
      let queryParams = {
        'returnNulls': returnNulls,
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'connectionAccountEmail': opts['connectionAccountEmail'],
        'connectionAccountId': opts['connectionAccountId'],
        'appKey': appKey,
        'rankType': opts['rankType'],
        'achievementType': opts['achievementType'],
        'includeUndiscovered': includeUndiscovered,
        'latitude': opts['latitude'],
        'longitude': opts['longitude']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [AchievementProgressResponse];
      return this.apiClient.callApi(
        '/achievement/progress/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listAchievementTags operation.
     * @callback module:api/AchievementApi~listAchievementTagsCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * List Achievement Tags
     * List achievement tags by application
     * @param {Object} opts Optional parameters
     * @param {String} [appKey] filter results by application key
     * @param {module:api/AchievementApi~listAchievementTagsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    listAchievementTags(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'appKey': opts['appKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/achievement/tag/list', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listAchievements operation.
     * @callback module:api/AchievementApi~listAchievementsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/AchievementShortResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * List Achievements
     * List achievements by billable.
     * @param {module:model/String} sortField the field to sort by. See AchievementApiMap
     * @param {Boolean} descending determines whether the sorted list is in descending or ascending order
     * @param {Number} start the start index for pagination
     * @param {Number} limit the limit for pagination (has a hard limit of 1000)
     * @param {Boolean} activeOnly Filter results to only return active achievements
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique id given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {String} [appKey] the application key
     * @param {String} [keyword] the keyword used to filter results with (this returns results that have the keyword in the title or description of the achievement)
     * @param {String} [achievementType] filter results by the achievementType (these are exact case sensitive matches)
     * @param {String} [rankType] filter results by the rankType (these are exact case sensitive matches)
     * @param {module:api/AchievementApi~listAchievementsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/AchievementShortResponse>}
     */
    listAchievements(sortField, descending, start, limit, activeOnly, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'sortField' is set
      if (sortField === undefined || sortField === null) {
        throw new Error("Missing the required parameter 'sortField' when calling listAchievements");
      }
      // verify the required parameter 'descending' is set
      if (descending === undefined || descending === null) {
        throw new Error("Missing the required parameter 'descending' when calling listAchievements");
      }
      // verify the required parameter 'start' is set
      if (start === undefined || start === null) {
        throw new Error("Missing the required parameter 'start' when calling listAchievements");
      }
      // verify the required parameter 'limit' is set
      if (limit === undefined || limit === null) {
        throw new Error("Missing the required parameter 'limit' when calling listAchievements");
      }
      // verify the required parameter 'activeOnly' is set
      if (activeOnly === undefined || activeOnly === null) {
        throw new Error("Missing the required parameter 'activeOnly' when calling listAchievements");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appKey': opts['appKey'],
        'keyword': opts['keyword'],
        'achievementType': opts['achievementType'],
        'rankType': opts['rankType'],
        'sortField': sortField,
        'descending': descending,
        'start': start,
        'limit': limit,
        'activeOnly': activeOnly
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [AchievementShortResponse];
      return this.apiClient.callApi(
        '/achievement/list', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchAchievements operation.
     * @callback module:api/AchievementApi~searchAchievementsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/AchievementShortResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Achievements
     * Searches achievements by application for consumers.
     * @param {String} appKey the application key
     * @param {module:model/String} sortField the field to sort by. See AchievementApiMap
     * @param {Boolean} descending determines whether the sorted list is in descending or ascending order
     * @param {Boolean} includeTiers return tiers, only applicable for version >3.18
     * @param {Boolean} includeInactiveTiers return inactive tiers, only applicable when includeTiers is true, only applicable for version >3.18
     * @param {Number} start the start index for pagination
     * @param {Number} limit the limit for pagination (has a hard limit of 1000)
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique id given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {String} [keyword] the keyword used to filter results with (this returns results that have the keyword in the title or description of the achievement)
     * @param {String} [achievementType] filter results by the achievementType (these are exact case sensitive matches)
     * @param {String} [rankType] filter results by the rankType (these are exact case sensitive matches)
     * @param {module:api/AchievementApi~searchAchievementsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/AchievementShortResponse>}
     */
    searchAchievements(appKey, sortField, descending, includeTiers, includeInactiveTiers, start, limit, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling searchAchievements");
      }
      // verify the required parameter 'sortField' is set
      if (sortField === undefined || sortField === null) {
        throw new Error("Missing the required parameter 'sortField' when calling searchAchievements");
      }
      // verify the required parameter 'descending' is set
      if (descending === undefined || descending === null) {
        throw new Error("Missing the required parameter 'descending' when calling searchAchievements");
      }
      // verify the required parameter 'includeTiers' is set
      if (includeTiers === undefined || includeTiers === null) {
        throw new Error("Missing the required parameter 'includeTiers' when calling searchAchievements");
      }
      // verify the required parameter 'includeInactiveTiers' is set
      if (includeInactiveTiers === undefined || includeInactiveTiers === null) {
        throw new Error("Missing the required parameter 'includeInactiveTiers' when calling searchAchievements");
      }
      // verify the required parameter 'start' is set
      if (start === undefined || start === null) {
        throw new Error("Missing the required parameter 'start' when calling searchAchievements");
      }
      // verify the required parameter 'limit' is set
      if (limit === undefined || limit === null) {
        throw new Error("Missing the required parameter 'limit' when calling searchAchievements");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appKey': appKey,
        'keyword': opts['keyword'],
        'achievementType': opts['achievementType'],
        'rankType': opts['rankType'],
        'sortField': sortField,
        'descending': descending,
        'includeTiers': includeTiers,
        'includeInactiveTiers': includeInactiveTiers,
        'start': start,
        'limit': limit
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [AchievementShortResponse];
      return this.apiClient.callApi(
        '/achievement/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the updateAchievement operation.
     * @callback module:api/AchievementApi~updateAchievementCallback
     * @param {String} error Error message, if any.
     * @param {module:model/AchievementResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update Achievement
     * Updates an achievement (for developer/retailer use). User must have permissions to the application the achievement is created for.
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique id given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {Number} [achievementId] the achievement ID for updating an existing achievement
     * @param {String} [analyticsTag] the analytics tag that will trigger when a user's achievement count gets updated
     * @param {String} [title] the title of the achievement (255 character limit)
     * @param {String} [description] the description of the achievement
     * @param {String} [rankType] the rank type for updating leader boards
     * @param {Number} [rankIncrement] determines how much the rank count is incremented
     * @param {Number} [minIncrement] restrict scores to be above or equal to this minimum value
     * @param {Boolean} [nullMinIncrement] enable to ignore usage of minIncrement
     * @param {Number} [maxIncrement] restrict scores to be below or equal to this maximum value
     * @param {Boolean} [nullMaxIncrement] enable to ignore usage of maxIncrement
     * @param {Boolean} [validate] determines whether the customId on analytics are used to validate a user's achievement progress.
     * @param {Boolean} [active] if it's active or inactive
     * @param {String} [triggerDefinition] if provided will define what triggers to run after a tier is completed
     * @param {module:api/AchievementApi~updateAchievementCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/AchievementResponse}
     */
    updateAchievement(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'achievementId': opts['achievementId'],
        'analyticsTag': opts['analyticsTag'],
        'title': opts['title'],
        'description': opts['description'],
        'rankType': opts['rankType'],
        'rankIncrement': opts['rankIncrement'],
        'minIncrement': opts['minIncrement'],
        'nullMinIncrement': opts['nullMinIncrement'],
        'maxIncrement': opts['maxIncrement'],
        'nullMaxIncrement': opts['nullMaxIncrement'],
        'validate': opts['validate'],
        'active': opts['active'],
        'triggerDefinition': opts['triggerDefinition']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = AchievementResponse;
      return this.apiClient.callApi(
        '/achievement/update', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the updateAchievementTier operation.
     * @callback module:api/AchievementApi~updateAchievementTierCallback
     * @param {String} error Error message, if any.
     * @param {module:model/AchievementTierResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update Achievement Tier
     * Updates a tier of an achievement (for developer/retailer use). User must have permissions to the application the achievement is created for.
     * @param {Number} achievementTierId the achievement tier id for updating
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] a unique id given by the device (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {File} [icon] achievement tier icon image file
     * @param {Number} [iconAssetId] the icon assetId, if icon is provided, icon will overrule
     * @param {String} [title] the title of the achievement tier
     * @param {String} [description] the description of the achievement tier
     * @param {Number} [goalCount] the count requirement for completing the achievement tier
     * @param {Number} [missionId] The ID of the mission to associate with the achievement
     * @param {Number} [gameId] The ID of the game to associate with the achievement
     * @param {Number} [packId] The ID of the pack to associate with the achievement
     * @param {Number} [gameLevelId] The ID of the game level to associate with the achievement
     * @param {Number} [gameObjectId] The ID of the game object to associate with the achievement
     * @param {Boolean} [scoreAllInstances] score all instances
     * @param {module:api/AchievementApi~updateAchievementTierCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/AchievementTierResponse}
     */
    updateAchievementTier(achievementTierId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'achievementTierId' is set
      if (achievementTierId === undefined || achievementTierId === null) {
        throw new Error("Missing the required parameter 'achievementTierId' when calling updateAchievementTier");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'achievementTierId': achievementTierId,
        'icon': opts['icon'],
        'iconAssetId': opts['iconAssetId'],
        'title': opts['title'],
        'description': opts['description'],
        'goalCount': opts['goalCount'],
        'missionId': opts['missionId'],
        'gameId': opts['gameId'],
        'packId': opts['packId'],
        'gameLevelId': opts['gameLevelId'],
        'gameObjectId': opts['gameObjectId'],
        'scoreAllInstances': opts['scoreAllInstances']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = AchievementTierResponse;
      return this.apiClient.callApi(
        '/achievement/tier/update', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the updateUserAchievement operation.
     * @callback module:api/AchievementApi~updateUserAchievementCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update Achievement Progress
     * Update user achievement progress.
     * @param {Number} accountId the account id of the user
     * @param {Object} opts Optional parameters
     * @param {Number} [achievementId] the achievement id (achievementId or tag required)
     * @param {String} [tag] the analytic tag to identify an achievement (achievementId or tag required)
     * @param {Number} [customId] a custom identifier used for validation
     * @param {Number} [increment] the amount to increment an achievement progress by (if no increment is sent in, the server defaults to using the achievement's rankIncrement value)
     * @param {Number} [startDate] a custom start date that the client can set (not yet used in server logic)
     * @param {Number} [endDate] a custom end date that the client can set (not yet used in server logic)
     * @param {Boolean} [returnProgress] determines whether to return the achievement progress response
     * @param {module:api/AchievementApi~updateUserAchievementCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    updateUserAchievement(accountId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling updateUserAchievement");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'achievementId': opts['achievementId'],
        'tag': opts['tag'],
        'customId': opts['customId'],
        'increment': opts['increment'],
        'startDate': opts['startDate'],
        'endDate': opts['endDate'],
        'returnProgress': opts['returnProgress']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/achievement/progress/update', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
