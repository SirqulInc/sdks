/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import CountResponse from '../model/CountResponse';
import ProfileResponse from '../model/ProfileResponse';
import SirqulResponse from '../model/SirqulResponse';
import TicketListResponse from '../model/TicketListResponse';
import TicketOfferResponse from '../model/TicketOfferResponse';

/**
* Ticket service.
* @module api/TicketApi
* @version 3.16
*/
export default class TicketApi {

    /**
    * Constructs a new TicketApi. 
    * @alias module:api/TicketApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the getTicketCount operation.
     * @callback module:api/TicketApi~getTicketCountCallback
     * @param {String} error Error message, if any.
     * @param {module:model/CountResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Ticket Count
     * Gets the ticket count.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the id of the device that owns the tickets
     * @param {Number} [accountId] the id of the account that owns the tickets
     * @param {String} [gameType] this is deprecated.
     * @param {String} [appKey] the applicationkey
     * @param {String} [ticketType] the type of ticket
     * @param {module:api/TicketApi~getTicketCountCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/CountResponse}
     */
    getTicketCount(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling getTicketCount");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'gameType': opts['gameType'],
        'appKey': opts['appKey'],
        'ticketType': opts['ticketType']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = CountResponse;
      return this.apiClient.callApi(
        '/api/{version}/ticket/count', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getTicketList operation.
     * @callback module:api/TicketApi~getTicketListCallback
     * @param {String} error Error message, if any.
     * @param {module:model/TicketListResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Ticket List
     * Gets the list of tickets.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the id of the device that owns the tickets
     * @param {Number} [accountId] the id of the account that owns the tickets
     * @param {String} [ticketObjectType] comma separated list of TicketObjectType
     * @param {String} [actionType] comma separated list of TicketActionType
     * @param {String} [ticketIds] the ids of the tickets to get
     * @param {String} [objectIds] the ids of the objects to get
     * @param {String} [receiptTokens] 
     * @param {String} [gameType] 
     * @param {String} [appKey] the application key
     * @param {module:api/TicketApi~getTicketListCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/TicketListResponse}
     */
    getTicketList(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling getTicketList");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'ticketObjectType': opts['ticketObjectType'],
        'actionType': opts['actionType'],
        'ticketIds': opts['ticketIds'],
        'objectIds': opts['objectIds'],
        'receiptTokens': opts['receiptTokens'],
        'gameType': opts['gameType'],
        'appKey': opts['appKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = TicketListResponse;
      return this.apiClient.callApi(
        '/api/{version}/ticket/getList', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the giftPurchase operation.
     * @callback module:api/TicketApi~giftPurchaseCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Gift Tickets
     * Gift tickets to another user.
     * @param {Number} version 
     * @param {Number} receiverAccountId the id of the account receiving the tickets
     * @param {Number} ticketId the id of the tickets
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the id of the device
     * @param {Number} [accountId] the id of the gift owner
     * @param {Number} [assetId] the id of the asset
     * @param {String} [customMessage] a message that can be written to go along with the gift
     * @param {String} [gameType] the type of game associated with the tickets
     * @param {String} [appKey] the application key
     * @param {module:api/TicketApi~giftPurchaseCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    giftPurchase(version, receiverAccountId, ticketId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling giftPurchase");
      }
      // verify the required parameter 'receiverAccountId' is set
      if (receiverAccountId === undefined || receiverAccountId === null) {
        throw new Error("Missing the required parameter 'receiverAccountId' when calling giftPurchase");
      }
      // verify the required parameter 'ticketId' is set
      if (ticketId === undefined || ticketId === null) {
        throw new Error("Missing the required parameter 'ticketId' when calling giftPurchase");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'receiverAccountId': receiverAccountId,
        'ticketId': ticketId,
        'assetId': opts['assetId'],
        'customMessage': opts['customMessage'],
        'gameType': opts['gameType'],
        'appKey': opts['appKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/api/{version}/purchase/gift', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the saveTicket operation.
     * @callback module:api/TicketApi~saveTicketCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProfileResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Save Ticket
     * Allow user to acquire a purchase item and generate a ticket record. Used to redeem tickets or add tickets to the system.
     * @param {Number} version 
     * @param {String} actionType the action being performed, values: COMPLETED, // ADD TICKETS FOR COMPLETING A MISSION, CHALLENGE, GAME, PACK, LEVEL, LEVEL OBJECT REDEEMED, // REMOVE TICKETS FOR BUYING PACKS, HINTS, AND PEN TOOLS OPTIONS, ETC USERS_PLAYED, // ADD TICKETS FOR LEVELS PLAYED BY OTHER USERS TOURNAMENT_OWNER, // ADD TICKETS FOR TOURNAMENTS BY OTHER USERS PURCHASED, // ADD TICKET VIA IN APP PURCHASING SUMATION, // SUMATION OF TICKETS EARNED FROM CHILDREN GIFTED, // TRANSFERING OF PURCHASE ITEMS TO OTHER PEOPLE REFUNDED // FOR REFUNDING TICKETS BACK TO THE USER
     * @param {String} ticketObjectType the type of object being purchased, values: GAME_OBJECT, GAME_LEVEL, PACK, GAME, MISSION, PROFILE, APPLICATION, TICKETS, ASSET, CUSTOM
     * @param {Object} opts Optional parameters
     * @param {Boolean} [returnNulls] whether to return nulls or not
     * @param {String} [deviceId] the device id that owns the tickets
     * @param {Number} [accountId] the account id that owns the tickets
     * @param {String} [gameType] This parameter is deprecated. deprecated, use the appKey
     * @param {String} [appKey] the application key
     * @param {Number} [objectId] the ID of the item being purchased
     * @param {String} [purchaseCode] a unique string identifier defined by the application owner or Executive
     * @param {String} [receiptToken] a unique transaction identifier from the purchase provider (i.e. the transaction ID from iTunes, Google Play, etc)
     * @param {String} [receiptData] the receipt/transaction data for validating a purchase via iTunes/Gooogle/etc. This should be in string format.
     * @param {Number} [count] the count of tickets
     * @param {String} [ticketType] the ticket type
     * @param {String} [purchaseProvider] the provider for making the purchase {ITUNES, ITUNES_SANDBOX, GOOGLE}
     * @param {String} [purchaseType] a comma separated list of purchase types { SIRQUL, // purchasing from the sirqul store using tickets IOS, // purchasing from the itunes store for iPhone, iPod, iPod Touch GOOGLE, // purchasing from the google android store AMAZON, // purchasing from the amazon android store MAC, // purchasing from the itunes store for OSX WP8, // purhasing from the windows phone 8 store FREE }
     * @param {Boolean} [returnProfileResponse] returns a ProfileResponse if true, otherwise will return an AppResponse
     * @param {Boolean} [includeProfileResponse] if returnProfileResponse is false, will return an AppResponse with profile data if true
     * @param {String} [appVersion] the application version
     * @param {module:api/TicketApi~saveTicketCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ProfileResponse}
     */
    saveTicket(version, actionType, ticketObjectType, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling saveTicket");
      }
      // verify the required parameter 'actionType' is set
      if (actionType === undefined || actionType === null) {
        throw new Error("Missing the required parameter 'actionType' when calling saveTicket");
      }
      // verify the required parameter 'ticketObjectType' is set
      if (ticketObjectType === undefined || ticketObjectType === null) {
        throw new Error("Missing the required parameter 'ticketObjectType' when calling saveTicket");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'returnNulls': opts['returnNulls'],
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'gameType': opts['gameType'],
        'appKey': opts['appKey'],
        'actionType': actionType,
        'ticketObjectType': ticketObjectType,
        'objectId': opts['objectId'],
        'purchaseCode': opts['purchaseCode'],
        'receiptToken': opts['receiptToken'],
        'receiptData': opts['receiptData'],
        'count': opts['count'],
        'ticketType': opts['ticketType'],
        'purchaseProvider': opts['purchaseProvider'],
        'purchaseType': opts['purchaseType'],
        'returnProfileResponse': opts['returnProfileResponse'],
        'includeProfileResponse': opts['includeProfileResponse'],
        'appVersion': opts['appVersion']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = ProfileResponse;
      return this.apiClient.callApi(
        '/api/{version}/ticket/save', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the saveTicketViaFileUpload operation.
     * @callback module:api/TicketApi~saveTicketViaFileUploadCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProfileResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Save Ticket with Reciept
     * Similar to the Save Ticket endpoint but allows the receiptData to be in binary format. This must be a multi-part post
     * @param {Number} version 
     * @param {String} actionType the action being performed { COMPLETED, // ADD TICKETS FOR COMPLETING A MISSION, CHALLENGE, GAME, PACK, LEVEL, LEVEL OBJECT REDEEMED, // REMOVE TICKETS FOR BUYING PACKS, HINTS, AND PEN TOOLS OPTIONS, ETC USERS_PLAYED, // ADD TICKETS FOR LEVELS PLAYED BY OTHER USERS TOURNAMENT_OWNER, // ADD TICKETS FOR TOURNAMENTS BY OTHER USERS PURCHASED, // ADD TICKET VIA IN APP PURCHASING SUMATION, // SUMATION OF TICKETS EARNED FROM CHILDREN GIFTED, // TRANSFERING OF PURCHASE ITEMS TO OTHER PEOPLE REFUNDED // FOR REFUNDING TICKETS BACK TO THE USER }
     * @param {String} ticketObjectType the type of object being purchased {GAME_OBJECT, GAME_LEVEL, PACK, GAME, MISSION, PROFILE, APPLICATION, TICKETS, ASSET, CUSTOM}
     * @param {File} receiptData the receipt/transaction data for validating a purchase via iTunes/Gooogle/etc. This should be in binary format.
     * @param {Object} opts Optional parameters
     * @param {Boolean} [returnNulls] whether to return nulls or not
     * @param {String} [deviceId] the device id
     * @param {Number} [accountId] the account id
     * @param {String} [gameType] This parameter is deprecated. deprecated, use the appKey
     * @param {String} [appKey] the application key
     * @param {Number} [objectId] the ID of the item being purchased
     * @param {String} [purchaseCode] a unique string identifier defined by the application owner or Executive
     * @param {String} [receiptToken] a unique transaction identifier from the purchase provider (i.e. the transaction ID from iTunes, Google Play, etc)
     * @param {Number} [count] the count of tickets
     * @param {String} [ticketType] the ticket type
     * @param {String} [purchaseProvider] the provider for making the purchase {ITUNES, ITUNES_SANDBOX, GOOGLE}
     * @param {String} [purchaseType] a comma separated list of purchase types { SIRQUL, // purchasing from the sirqul store using tickets IOS, // purchasing from the itunes store for iPhone, iPod, iPod Touch GOOGLE, // purchasing from the google android store AMAZON, // purchasing from the amazon android store MAC, // purchasing from the itunes store for OSX WP8, // purhasing from the windows phone 8 store FREE }
     * @param {Boolean} [returnProfileResponse] returns a ProfileResponse if true, otherwise will return an AppResponse
     * @param {Boolean} [includeProfileResponse] if returnProfileResponse is false, will return an AppResponse with profile data if true
     * @param {String} [appVersion] the application version
     * @param {module:api/TicketApi~saveTicketViaFileUploadCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ProfileResponse}
     */
    saveTicketViaFileUpload(version, actionType, ticketObjectType, receiptData, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling saveTicketViaFileUpload");
      }
      // verify the required parameter 'actionType' is set
      if (actionType === undefined || actionType === null) {
        throw new Error("Missing the required parameter 'actionType' when calling saveTicketViaFileUpload");
      }
      // verify the required parameter 'ticketObjectType' is set
      if (ticketObjectType === undefined || ticketObjectType === null) {
        throw new Error("Missing the required parameter 'ticketObjectType' when calling saveTicketViaFileUpload");
      }
      // verify the required parameter 'receiptData' is set
      if (receiptData === undefined || receiptData === null) {
        throw new Error("Missing the required parameter 'receiptData' when calling saveTicketViaFileUpload");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'returnNulls': opts['returnNulls'],
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'gameType': opts['gameType'],
        'appKey': opts['appKey'],
        'actionType': actionType,
        'ticketObjectType': ticketObjectType,
        'objectId': opts['objectId'],
        'purchaseCode': opts['purchaseCode'],
        'receiptToken': opts['receiptToken'],
        'receiptData': receiptData,
        'count': opts['count'],
        'ticketType': opts['ticketType'],
        'purchaseProvider': opts['purchaseProvider'],
        'purchaseType': opts['purchaseType'],
        'returnProfileResponse': opts['returnProfileResponse'],
        'includeProfileResponse': opts['includeProfileResponse'],
        'appVersion': opts['appVersion']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = ProfileResponse;
      return this.apiClient.callApi(
        '/api/{version}/ticket/save/fileUpload', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the ticketOffers operation.
     * @callback module:api/TicketApi~ticketOffersCallback
     * @param {String} error Error message, if any.
     * @param {module:model/TicketOfferResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Ticket Offers
     * Get a list offers for tickets owned by sirqul.  Purchasing these will add the number of tickets to the account specified by the offer.
     * @param {Number} version 
     * @param {module:api/TicketApi~ticketOffersCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/TicketOfferResponse}
     */
    ticketOffers(version, callback) {
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling ticketOffers");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = TicketOfferResponse;
      return this.apiClient.callApi(
        '/api/{version}/ticket/ticketoffers', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
