/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import AccountMiniResponse from '../model/AccountMiniResponse';
import Leg from '../model/Leg';
import LegResponse from '../model/LegResponse';
import PredictedLocationResponse from '../model/PredictedLocationResponse';
import PreferredLocationResponse from '../model/PreferredLocationResponse';
import SirqulResponse from '../model/SirqulResponse';
import StepResponse from '../model/StepResponse';

/**
* Tracking service.
* @module api/TrackingApi
* @version 3.16
*/
export default class TrackingApi {

    /**
    * Constructs a new TrackingApi. 
    * @alias module:api/TrackingApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the batchSaveTracking operation.
     * @callback module:api/TrackingApi~batchSaveTrackingCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/Leg>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Batch Tracking
     * Batch create tracking legs
     * @param {String} data JSON array of tracking legs ```json [   \"distance\": \"0.08\",   \"duration\": \"10000\",   \"startLatitude\": \"47.614603\",   \"startLongitude\": \"-122.350518\",   \"endLatitude\": \"47.614384\",   \"endLongitude\": \"-122.349161\",   \"startDate\": \"1361924010000\",   \"endDate\": \"1361924020000\",   \"steps\": [     {       \"distance\": \"0.03\",       \"duration\": \"5000\",       \"startLat\": \"47.614603\",       \"startLng\": \"-122.350518\",       \"startDate\": \"1361924010000\",       \"endLat\": \"47.614941\",       \"endLng\": \"-122.350062\",       \"endDate\": \"1361924015000\"     },{       \"distance\": \"0.05\",       \"duration\": \"5000\",       \"startLat\": \"47.614941\",       \"startLng\": \"-122.350062\",       \"startDate\": \"1361924015000\",       \"endLat\": \"47.614384\",       \"endLng\": \"-122.349161\",       \"endDate\": \"1361924020000\"     }   ] ] ``` 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the device id (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {Boolean} [generateAccounts] Whether to generate accounts for tracking entries when the owner does not exist
     * @param {Boolean} [updateAccountLocations] Whether to update the account's current location from the incoming tracking data
     * @param {String} [defaultTag = 'PASSIVE')] The default tag to apply to incoming legs when no tag is provided
     * @param {String} [slaveUID] 
     * @param {module:api/TrackingApi~batchSaveTrackingCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/Leg>}
     */
    batchSaveTracking(data, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'data' is set
      if (data === undefined || data === null) {
        throw new Error("Missing the required parameter 'data' when calling batchSaveTracking");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'data': data,
        'generateAccounts': opts['generateAccounts'],
        'updateAccountLocations': opts['updateAccountLocations'],
        'defaultTag': opts['defaultTag'],
        'slaveUID': opts['slaveUID']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [Leg];
      return this.apiClient.callApi(
        '/tracking/batch/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getPredictedLocations operation.
     * @callback module:api/TrackingApi~getPredictedLocationsCallback
     * @param {String} error Error message, if any.
     * @param {module:model/PredictedLocationResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Predicted Locations
     * Get the predicted location for a customer based on previous behavior.  If a customer resides in a place for a period of time this is marked as a preferred location.  We look back over the previous few days and the previous days of the week from the day specified.  If for instance the day was a Wednesday then this would check the days before, including: Tuesday, Monday, Sunday, etc. It will also check some number of previous Wednesdays in the past few weeks.
     * @param {Number} accountId The account id of the customer
     * @param {Object} opts Optional parameters
     * @param {Number} [latitude] latitude to return a more likely result set based on the user's current location
     * @param {Number} [longitude] longitude to return a more likely result set based on the user's current location
     * @param {Number} [dateCheck] Used to specify which day to get predicted locations for. So if you want to look at where they might be tomorrow, pass in tomorrow's date (timestamp in milliseconds). If no value is passed in, the current date will be used.
     * @param {String} [hourCheck] Comma separated list of hours that will filter the results on the hours specified. For example, if you want to see predicted locations for the mornings and evening you can pass in \"8,9,10,11,17,18,19,20\".
     * @param {Number} [threshold = 1)] The minimum number matches in 1 hour to be considered a likely location.
     * @param {module:model/String} [distanceUnit = 'MILES')] Determines which unit of measurement gets returned for distances: {MILES, KILOMETERS}
     * @param {Number} [searchRange = 0)] Filter results so only locations within the specified radius will be returned. The distance can either be in miles or kilometers as specified in the distanceUnit parameter. A value of \"0\" (zero) will ignore the radius restriction.
     * @param {module:model/String} [sortOrder = 'MATCHES')] The ordering algorithm for sorting the returned results: {MATCHES, DISTANCE, WEIGHTED}
     * @param {module:api/TrackingApi~getPredictedLocationsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/PredictedLocationResponse}
     */
    getPredictedLocations(accountId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling getPredictedLocations");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'dateCheck': opts['dateCheck'],
        'hourCheck': opts['hourCheck'],
        'threshold': opts['threshold'],
        'distanceUnit': opts['distanceUnit'],
        'searchRange': opts['searchRange'],
        'sortOrder': opts['sortOrder']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = PredictedLocationResponse;
      return this.apiClient.callApi(
        '/tracking/predicted/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getPredictedPath operation.
     * @callback module:api/TrackingApi~getPredictedPathCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/StepResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Tracking Path
     * Get the path (lat/long coordinates) between 2 steps previously logged for a customer.
     * @param {Number} accountId The account id of the customer
     * @param {Number} startStepId The stepId to begin from
     * @param {Number} endStepId The stepId to end with
     * @param {module:api/TrackingApi~getPredictedPathCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/StepResponse>}
     */
    getPredictedPath(accountId, startStepId, endStepId, callback) {
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling getPredictedPath");
      }
      // verify the required parameter 'startStepId' is set
      if (startStepId === undefined || startStepId === null) {
        throw new Error("Missing the required parameter 'startStepId' when calling getPredictedPath");
      }
      // verify the required parameter 'endStepId' is set
      if (endStepId === undefined || endStepId === null) {
        throw new Error("Missing the required parameter 'endStepId' when calling getPredictedPath");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'startStepId': startStepId,
        'endStepId': endStepId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [StepResponse];
      return this.apiClient.callApi(
        '/tracking/path/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getPreferredLocations operation.
     * @callback module:api/TrackingApi~getPreferredLocationsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/PreferredLocationResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Preferred Locations
     * Search on preferred locations for a user, which is created when a customer resides in a place for a period of time.
     * @param {Number} accountId The account id of the customer
     * @param {Object} opts Optional parameters
     * @param {Number} [latitude] latitude to return a more likely result set based on the user's current location
     * @param {Number} [longitude] longitude to return a more likely result set based on the user's current location
     * @param {Number} [dateCheck] Used to specify which day to get preferred locations for. So if you want to look at where they might be tomorrow, pass in tomorrow's date (timestamp in milliseconds). If no value is passed in, results from all time will be returned.
     * @param {String} [hourCheck] Comma separated list of hours that will filter the results on the hours specified. For example, if you want to see preferred locations for the mornings and evening you can pass in \"8,9,10,11,17,18,19,20\".
     * @param {String} [sortField = 'PREFERRED_DATE')] Specifies how the results will be ordered. Supported values include: CREATED - the time of when the preferred location data was processed. PREFERRED_DATE - the time of when the user sent in the tracking data. HOUR - the hour of when the user sent in the tracking data. DURATION - the duration of the preferred location
     * @param {Boolean} [descending = true)] Determines whether the sorted list is in descending or ascending order
     * @param {Number} [start = 0)] The start index for pagination
     * @param {Number} [limit = 20)] The limit for pagination
     * @param {Number} [searchRange = 0)] Filter results so only locations within the specified radius will be returned. The distance can either be in miles or kilometers as specified in the distanceUnit parameter. A value of \"0\" (zero) will ignore the radius restriction.
     * @param {module:model/String} [distanceUnit = 'MILES')] Determines which unit of measurement gets returned for distances: {MILES, KILOMETERS}
     * @param {module:api/TrackingApi~getPreferredLocationsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/PreferredLocationResponse>}
     */
    getPreferredLocations(accountId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling getPreferredLocations");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'dateCheck': opts['dateCheck'],
        'hourCheck': opts['hourCheck'],
        'sortField': opts['sortField'],
        'descending': opts['descending'],
        'start': opts['start'],
        'limit': opts['limit'],
        'searchRange': opts['searchRange'],
        'distanceUnit': opts['distanceUnit']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [PreferredLocationResponse];
      return this.apiClient.callApi(
        '/tracking/preferred/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getTrackingLegs operation.
     * @callback module:api/TrackingApi~getTrackingLegsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/LegResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Tracking
     * Retrieve tracking data to be able to show where a user has been.
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the device id (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {Number} [ownerId] the account id of the person the user wants to tracking data for
     * @param {String} [trackingDeviceId] the id of the tracking device
     * @param {Number} [startDate] the start date in (UTC milliseconds) to filter the tracking results. If no startDate is passed in, the last 30 days will be returned.
     * @param {Number} [endDate] the end date in (UTC milliseconds) to filter the tracking results
     * @param {String} [tags] filter results by tag
     * @param {Boolean} [getLastPoint = false)] gets the last known location of the user
     * @param {module:api/TrackingApi~getTrackingLegsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/LegResponse>}
     */
    getTrackingLegs(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'ownerId': opts['ownerId'],
        'trackingDeviceId': opts['trackingDeviceId'],
        'startDate': opts['startDate'],
        'endDate': opts['endDate'],
        'tags': opts['tags'],
        'getLastPoint': opts['getLastPoint']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [LegResponse];
      return this.apiClient.callApi(
        '/tracking/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the saveTrackingLeg operation.
     * @callback module:api/TrackingApi~saveTrackingLegCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Tracking Leg
     * Send tracking points to be able to generate pathing data
     * @param {Number} startLat the latitude of the first point
     * @param {Number} startLng the longitude of the first point
     * @param {Number} startDate the start date (in UTC milliseconds) of the first point
     * @param {Number} endLat the latitude of the last point
     * @param {Number} endLng the longitude of the last point
     * @param {Number} endDate the end date (in UTC milliseconds) of the last point
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the device id (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {Number} [distance] the total distance
     * @param {Number} [duration] the total duration
     * @param {String} [steps] JSON array of tracking vectors used for smoother pathing data. If null then the leg data will be used to generate a single step, if an empty array then no steps will be generated. ```json [{   \"distance\": \"0.03\",   \"duration\": \"5000\",   \"startLat\": \"47.614603\",   \"startLng\": \"-122.350518\",   \"startDate\": \"1361924010000\",   \"endLat\": \"47.614941\",   \"endLng\": \"-122.350062\",   \"endDate\": \"1361924015000\" }] ``` 
     * @param {String} [tags] name the leg for searching
     * @param {module:api/TrackingApi~saveTrackingLegCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    saveTrackingLeg(startLat, startLng, startDate, endLat, endLng, endDate, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'startLat' is set
      if (startLat === undefined || startLat === null) {
        throw new Error("Missing the required parameter 'startLat' when calling saveTrackingLeg");
      }
      // verify the required parameter 'startLng' is set
      if (startLng === undefined || startLng === null) {
        throw new Error("Missing the required parameter 'startLng' when calling saveTrackingLeg");
      }
      // verify the required parameter 'startDate' is set
      if (startDate === undefined || startDate === null) {
        throw new Error("Missing the required parameter 'startDate' when calling saveTrackingLeg");
      }
      // verify the required parameter 'endLat' is set
      if (endLat === undefined || endLat === null) {
        throw new Error("Missing the required parameter 'endLat' when calling saveTrackingLeg");
      }
      // verify the required parameter 'endLng' is set
      if (endLng === undefined || endLng === null) {
        throw new Error("Missing the required parameter 'endLng' when calling saveTrackingLeg");
      }
      // verify the required parameter 'endDate' is set
      if (endDate === undefined || endDate === null) {
        throw new Error("Missing the required parameter 'endDate' when calling saveTrackingLeg");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'distance': opts['distance'],
        'duration': opts['duration'],
        'startLat': startLat,
        'startLng': startLng,
        'startDate': startDate,
        'endLat': endLat,
        'endLng': endLng,
        'endDate': endDate,
        'steps': opts['steps'],
        'tags': opts['tags']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/tracking/leg/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the saveTrackingStep operation.
     * @callback module:api/TrackingApi~saveTrackingStepCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Tracking Step
     * Send tracking points to be able to generate pathing data
     * @param {Number} legId the leg to add the step to
     * @param {Number} startLat the latitude of the first point
     * @param {Number} startLng the longitude of the first point
     * @param {Number} startDate the start date (in UTC milliseconds) of the first point
     * @param {Number} endLat the latitude of the last point
     * @param {Number} endLng the longitude of the last point
     * @param {Number} endDate the end date (in UTC milliseconds) of the last point
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] the device id (deviceId or accountId required)
     * @param {Number} [accountId] the account id of the user (deviceId or accountId required)
     * @param {Number} [distance] the total distance
     * @param {Number} [duration] the total duration
     * @param {module:api/TrackingApi~saveTrackingStepCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    saveTrackingStep(legId, startLat, startLng, startDate, endLat, endLng, endDate, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'legId' is set
      if (legId === undefined || legId === null) {
        throw new Error("Missing the required parameter 'legId' when calling saveTrackingStep");
      }
      // verify the required parameter 'startLat' is set
      if (startLat === undefined || startLat === null) {
        throw new Error("Missing the required parameter 'startLat' when calling saveTrackingStep");
      }
      // verify the required parameter 'startLng' is set
      if (startLng === undefined || startLng === null) {
        throw new Error("Missing the required parameter 'startLng' when calling saveTrackingStep");
      }
      // verify the required parameter 'startDate' is set
      if (startDate === undefined || startDate === null) {
        throw new Error("Missing the required parameter 'startDate' when calling saveTrackingStep");
      }
      // verify the required parameter 'endLat' is set
      if (endLat === undefined || endLat === null) {
        throw new Error("Missing the required parameter 'endLat' when calling saveTrackingStep");
      }
      // verify the required parameter 'endLng' is set
      if (endLng === undefined || endLng === null) {
        throw new Error("Missing the required parameter 'endLng' when calling saveTrackingStep");
      }
      // verify the required parameter 'endDate' is set
      if (endDate === undefined || endDate === null) {
        throw new Error("Missing the required parameter 'endDate' when calling saveTrackingStep");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'legId': legId,
        'distance': opts['distance'],
        'duration': opts['duration'],
        'startLat': startLat,
        'startLng': startLng,
        'startDate': startDate,
        'endLat': endLat,
        'endLng': endLng,
        'endDate': endDate
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/tracking/step/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchAccountsWithTrackingLegs operation.
     * @callback module:api/TrackingApi~searchAccountsWithTrackingLegsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/AccountMiniResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * List Tracking
     * Search for all accounts that have tracking legs data by the given constraints.
     * @param {Number} accountId The account id of the user
     * @param {Object} opts Optional parameters
     * @param {String} [keyword] Used for LIKE search of first or last name on the acocunt
     * @param {Number} [startDate] Range to begin in UTC milliseconds
     * @param {Number} [endDate] Range to end in UTC milliseconds
     * @param {String} [tags] Exact match on tag field of Legs's searchTag
     * @param {String} [audienceIds] 
     * @param {Number} [latitude] Origin latitude to perform searching constraints with given range
     * @param {Number} [longitude] Origin longitude to perform searching constraints with given range
     * @param {Number} [range = 5)] The radius, in miles, to perform the search for
     * @param {String} [sortField = 'LEG_START_DATE')] The column to sort the search on. Possible values include: {LEG_START_DATE, ACCOUNT_DISPLAY}
     * @param {Boolean} [descending = true)] The order to return the results. Default is false, which will return the results in ascending order.
     * @param {Number} [start = 0)] The index into the record set to start with. Default is 0.
     * @param {Number} [limit = 20)] The total number of records to return. Default is 20.
     * @param {Boolean} [activeOnly = false)] Determines whether to return only active results. Default is false.
     * @param {module:api/TrackingApi~searchAccountsWithTrackingLegsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/AccountMiniResponse>}
     */
    searchAccountsWithTrackingLegs(accountId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling searchAccountsWithTrackingLegs");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'keyword': opts['keyword'],
        'startDate': opts['startDate'],
        'endDate': opts['endDate'],
        'tags': opts['tags'],
        'audienceIds': opts['audienceIds'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'range': opts['range'],
        'sortField': opts['sortField'],
        'descending': opts['descending'],
        'start': opts['start'],
        'limit': opts['limit'],
        'activeOnly': opts['activeOnly']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [AccountMiniResponse];
      return this.apiClient.callApi(
        '/tracking/list', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchTrackingLegs operation.
     * @callback module:api/TrackingApi~searchTrackingLegsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/LegResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Tracking (Billable)
     * Retrieve tracking data for billable/account scoped queries.
     * @param {Number} accountId The account id to search tracking for
     * @param {String} appKey The application key
     * @param {Object} opts Optional parameters
     * @param {String} [trackingDeviceId] The id of the tracking device
     * @param {Number} [startDate] The start date in (UTC milliseconds) to filter the tracking results
     * @param {Number} [endDate] The end date in (UTC milliseconds) to filter the tracking results
     * @param {String} [tags] Filter results by tag
     * @param {Number} [start = 0)] The start index for pagination
     * @param {Number} [limit = 100)] The limit for pagination
     * @param {module:api/TrackingApi~searchTrackingLegsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/LegResponse>}
     */
    searchTrackingLegs(accountId, appKey, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling searchTrackingLegs");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling searchTrackingLegs");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'appKey': appKey,
        'trackingDeviceId': opts['trackingDeviceId'],
        'startDate': opts['startDate'],
        'endDate': opts['endDate'],
        'tags': opts['tags'],
        'start': opts['start'],
        'limit': opts['limit']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [LegResponse];
      return this.apiClient.callApi(
        '/tracking/searchByBillable', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
