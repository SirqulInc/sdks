/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import QueueResponse from '../model/QueueResponse';
import SirqulResponse from '../model/SirqulResponse';

/**
* AMQP service.
* @module api/AMQPApi
* @version 3.16
*/
export default class AMQPApi {

    /**
    * Constructs a new AMQPApi. 
    * @alias module:api/AMQPApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the consumerCreate operation.
     * @callback module:api/AMQPApi~consumerCreateCallback
     * @param {String} error Error message, if any.
     * @param {module:model/QueueResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Consumer
     * Create a connection to an existing amqp queue and register as a consumer.
     * @param {String} appKey The application key to use when creating an analytic or service request. The account needs to have permissions to the applicaton or it will be denied.
     * @param {String} name The name of the queue to connect to
     * @param {String} hostname The hostname of the server the queue is hosted on
     * @param {String} username The username to access the server the queue is hosted on
     * @param {String} password The password to access the queue to connect to
     * @param {String} dataMapping The data mapping information in the format of AMQPRequest
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The client deviceID
     * @param {Number} [accountId] The logged in user ID
     * @param {Number} [port = 5672)] The port of the server the queue is hosted on
     * @param {String} [virtualHost] The virtual host defined on the server the queue is associated on
     * @param {String} [exchanger] The exchanger of the queue to connect to
     * @param {String} [exchangerType] The exchanger type of the queue to connect to
     * @param {Number} [workers = 1)] The number of workers to generate 
     * @param {Boolean} [useSSL] Use SSL
     * @param {module:api/AMQPApi~consumerCreateCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/QueueResponse}
     */
    consumerCreate(appKey, name, hostname, username, password, dataMapping, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling consumerCreate");
      }
      // verify the required parameter 'name' is set
      if (name === undefined || name === null) {
        throw new Error("Missing the required parameter 'name' when calling consumerCreate");
      }
      // verify the required parameter 'hostname' is set
      if (hostname === undefined || hostname === null) {
        throw new Error("Missing the required parameter 'hostname' when calling consumerCreate");
      }
      // verify the required parameter 'username' is set
      if (username === undefined || username === null) {
        throw new Error("Missing the required parameter 'username' when calling consumerCreate");
      }
      // verify the required parameter 'password' is set
      if (password === undefined || password === null) {
        throw new Error("Missing the required parameter 'password' when calling consumerCreate");
      }
      // verify the required parameter 'dataMapping' is set
      if (dataMapping === undefined || dataMapping === null) {
        throw new Error("Missing the required parameter 'dataMapping' when calling consumerCreate");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appKey': appKey,
        'name': name,
        'hostname': hostname,
        'port': opts['port'],
        'username': username,
        'password': password,
        'virtualHost': opts['virtualHost'],
        'exchanger': opts['exchanger'],
        'exchangerType': opts['exchangerType'],
        'workers': opts['workers'],
        'dataMapping': dataMapping,
        'useSSL': opts['useSSL']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = QueueResponse;
      return this.apiClient.callApi(
        '/queue/consumer/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the consumerUpdate operation.
     * @callback module:api/AMQPApi~consumerUpdateCallback
     * @param {String} error Error message, if any.
     * @param {module:model/QueueResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update Consumer
     * Update an existing amqp queue's data mapping.
     * @param {String} appKey The application key to use when creating an analytic or service request. The account needs to have permissions to the applicaton or it will be denied.
     * @param {Number} queueId The queue to update
     * @param {String} dataMapping The data mapping information in the format of AMQPRequest
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The client deviceID
     * @param {Number} [accountId] The logged in user ID
     * @param {Boolean} [useSSL] Use SSL
     * @param {module:api/AMQPApi~consumerUpdateCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/QueueResponse}
     */
    consumerUpdate(appKey, queueId, dataMapping, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling consumerUpdate");
      }
      // verify the required parameter 'queueId' is set
      if (queueId === undefined || queueId === null) {
        throw new Error("Missing the required parameter 'queueId' when calling consumerUpdate");
      }
      // verify the required parameter 'dataMapping' is set
      if (dataMapping === undefined || dataMapping === null) {
        throw new Error("Missing the required parameter 'dataMapping' when calling consumerUpdate");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appKey': appKey,
        'queueId': queueId,
        'dataMapping': dataMapping,
        'useSSL': opts['useSSL']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = QueueResponse;
      return this.apiClient.callApi(
        '/queue/consumer/update', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the queueCreate operation.
     * @callback module:api/AMQPApi~queueCreateCallback
     * @param {String} error Error message, if any.
     * @param {module:model/QueueResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Queue
     * Create a basic AMQP queue. If the username and password and virtual host is not sepcified, the queue will be created on the virtual host assigned to the application.
     * @param {String} appKey The application key unique to each application.
     * @param {String} name The name of the queue to create
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The client deviceID
     * @param {Number} [accountId] The logged in user ID
     * @param {Number} [workers = 1)] The number of workers to generate 
     * @param {String} [analyticTags] If provided the analytic processing will publsih to this queue instead of the default one for the provided list of tags
     * @param {String} [hostname] The hostname of the server the queue is hosted on
     * @param {Number} [port] The port of the server the queue is hosted on
     * @param {String} [username] The username to access the server that the queue is on
     * @param {String} [password] The password to access the queue to connect to
     * @param {String} [virtualHost] The virtual host defined on the server to queue
     * @param {Boolean} [useSSL] Use SSL
     * @param {module:api/AMQPApi~queueCreateCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/QueueResponse}
     */
    queueCreate(appKey, name, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling queueCreate");
      }
      // verify the required parameter 'name' is set
      if (name === undefined || name === null) {
        throw new Error("Missing the required parameter 'name' when calling queueCreate");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appKey': appKey,
        'name': name,
        'workers': opts['workers'],
        'analyticTags': opts['analyticTags'],
        'hostname': opts['hostname'],
        'port': opts['port'],
        'username': opts['username'],
        'password': opts['password'],
        'virtualHost': opts['virtualHost'],
        'useSSL': opts['useSSL']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = QueueResponse;
      return this.apiClient.callApi(
        '/queue/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the queueDelete operation.
     * @callback module:api/AMQPApi~queueDeleteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete Queue
     * Delete the stored queue record and close any active connections to the AMQP servers.
     * @param {Number} queueId The id of the queue to find
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The client device ID
     * @param {Number} [accountId] The logged in user ID
     * @param {module:api/AMQPApi~queueDeleteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    queueDelete(queueId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'queueId' is set
      if (queueId === undefined || queueId === null) {
        throw new Error("Missing the required parameter 'queueId' when calling queueDelete");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'queueId': queueId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/queue/delete', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the queueGet operation.
     * @callback module:api/AMQPApi~queueGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/QueueResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Queue
     * Get the stored queue record. Must supply the queueId, or the name and hostname and virtualHost, or the name and appKey to find the record.
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The client device ID
     * @param {Number} [accountId] The logged in user ID
     * @param {Number} [queueId] The id of the queue to find
     * @param {String} [appKey] The application key the queue was assigned to
     * @param {String} [name] The name of the queue to find
     * @param {String} [hostname] The hostname of the queue to find
     * @param {String} [virtualHost] The virtual host of the queue to find
     * @param {module:api/AMQPApi~queueGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/QueueResponse}
     */
    queueGet(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'queueId': opts['queueId'],
        'appKey': opts['appKey'],
        'name': opts['name'],
        'hostname': opts['hostname'],
        'virtualHost': opts['virtualHost']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = QueueResponse;
      return this.apiClient.callApi(
        '/queue/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the queuePublish operation.
     * @callback module:api/AMQPApi~queuePublishCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Publish Queue
     * Publish a message to a stored queue. Must supply the queueId, or the name and hostname and virtualHost, or the name and appKey to find the record.
     * @param {String} message The payload to send to the queue
     * @param {Object} opts Optional parameters
     * @param {Number} [queueId] The id of the queue to publish to
     * @param {String} [appKey] The application key the queue was assigned to
     * @param {String} [name] The name of the queue to publish to or the analytic tag to handle if the analytic param is true
     * @param {String} [hostname] The hostname of the server the queue is hosted on
     * @param {String} [virtualHost] The virtual host defined on the server to queue
     * @param {module:api/AMQPApi~queuePublishCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    queuePublish(message, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'message' is set
      if (message === undefined || message === null) {
        throw new Error("Missing the required parameter 'message' when calling queuePublish");
      }

      let pathParams = {
      };
      let queryParams = {
        'queueId': opts['queueId'],
        'appKey': opts['appKey'],
        'name': opts['name'],
        'hostname': opts['hostname'],
        'virtualHost': opts['virtualHost'],
        'message': message
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/queue/publish', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the queueSearch operation.
     * @callback module:api/AMQPApi~queueSearchCallback
     * @param {String} error Error message, if any.
     * @param {module:model/QueueResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Queue
     * Get the queues setup for the BillableEntity's applications.
     * @param {Object} opts Optional parameters
     * @param {Number} [queueId] The id of the queue to find
     * @param {String} [deviceId] The client device ID
     * @param {Number} [accountId] The logged in user ID
     * @param {String} [name] The name of the queue to find
     * @param {Number} [start = 0)] Start of the index
     * @param {Number} [limit = 10)] Limit of the index
     * @param {module:api/AMQPApi~queueSearchCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/QueueResponse}
     */
    queueSearch(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'queueId': opts['queueId'],
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'name': opts['name'],
        'start': opts['start'],
        'limit': opts['limit']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = QueueResponse;
      return this.apiClient.callApi(
        '/queue/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the queueUpdate operation.
     * @callback module:api/AMQPApi~queueUpdateCallback
     * @param {String} error Error message, if any.
     * @param {module:model/QueueResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update Queue
     * Update the basic AMQP queue.
     * @param {Number} queueId The id of the queue to update
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The client deviceID
     * @param {Number} [accountId] The logged in user ID
     * @param {String} [appKey] The application key unique to each application.
     * @param {Number} [workers] The number of workers to generate
     * @param {String} [analyticTags] If provided the analytic processing will publsih to this queue instead of the default one for the provided list of tags
     * @param {String} [hostname] The hostname of the server the queue is hosted on
     * @param {Number} [port] The port of the server the queue is hosted on
     * @param {String} [username] The username to access the server that the queue is on
     * @param {String} [password] The password to access the queue to connect to
     * @param {String} [virtualHost] The virtual host defined on the server to queue
     * @param {Boolean} [useSSL] the SSL to use
     * @param {module:api/AMQPApi~queueUpdateCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/QueueResponse}
     */
    queueUpdate(queueId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'queueId' is set
      if (queueId === undefined || queueId === null) {
        throw new Error("Missing the required parameter 'queueId' when calling queueUpdate");
      }

      let pathParams = {
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'appKey': opts['appKey'],
        'queueId': queueId,
        'workers': opts['workers'],
        'analyticTags': opts['analyticTags'],
        'hostname': opts['hostname'],
        'port': opts['port'],
        'username': opts['username'],
        'password': opts['password'],
        'virtualHost': opts['virtualHost'],
        'useSSL': opts['useSSL']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = QueueResponse;
      return this.apiClient.callApi(
        '/queue/update', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
