/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import AccountResponse from '../model/AccountResponse';
import SearchResponse from '../model/SearchResponse';
import SirqulResponse from '../model/SirqulResponse';
import WrappedResponse from '../model/WrappedResponse';

/**
* Favorite service.
* @module api/FavoriteApi
* @version 3.16
*/
export default class FavoriteApi {

    /**
    * Constructs a new FavoriteApi. 
    * @alias module:api/FavoriteApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the addFavorite operation.
     * @callback module:api/FavoriteApi~addFavoriteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/WrappedResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Favorite
     * Adds an offer, offer location, retailer location, or category to your favorites.
     * @param {Number} version 
     * @param {Number} favoritableId The ID of the object to favorite {offerId, offerLocationId, retailerLocationId, categoryId}
     * @param {String} favoritableType The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY, ALBUM}
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The unique ID given by the device (deviceId or accountId required)
     * @param {Number} [accountId] The account ID of the user (deviceId or accountId required)
     * @param {Number} [latitude] The current latitude of the user
     * @param {Number} [longitude] The current longitude of the user
     * @param {module:api/FavoriteApi~addFavoriteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/WrappedResponse}
     */
    addFavorite(version, favoritableId, favoritableType, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling addFavorite");
      }
      // verify the required parameter 'favoritableId' is set
      if (favoritableId === undefined || favoritableId === null) {
        throw new Error("Missing the required parameter 'favoritableId' when calling addFavorite");
      }
      // verify the required parameter 'favoritableType' is set
      if (favoritableType === undefined || favoritableType === null) {
        throw new Error("Missing the required parameter 'favoritableType' when calling addFavorite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'favoritableId': favoritableId,
        'favoritableType': favoritableType,
        'latitude': opts['latitude'],
        'longitude': opts['longitude']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = WrappedResponse;
      return this.apiClient.callApi(
        '/api/{version}/favorite/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the deleteFavorite operation.
     * @callback module:api/FavoriteApi~deleteFavoriteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete Favorite
     * Removes a favorited item from the user's favorites list.
     * @param {Number} version 
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The unique ID given by the device (deviceId or accountId required)
     * @param {Number} [accountId] The account ID of the user (deviceId or accountId required)
     * @param {Number} [favoriteId] The ID of the favorite reference record (only optional if favoritableId & favoritableType is pass in instead)
     * @param {Number} [favoritableId] The ID of the object to un-favorite {offerId, offerLocationId, retailerLocationId, categoryId} (this is required if favoriteId is NOT passed in)
     * @param {String} [favoritableType] The type of the object to un-favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY} (this is required if favoriteId is NOT passed in)
     * @param {module:api/FavoriteApi~deleteFavoriteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    deleteFavorite(version, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling deleteFavorite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'favoriteId': opts['favoriteId'],
        'favoritableId': opts['favoritableId'],
        'favoritableType': opts['favoritableType']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/api/{version}/favorite/delete', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getFavorite operation.
     * @callback module:api/FavoriteApi~getFavoriteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/WrappedResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Favorite
     * Retrieves a single favorited item.
     * @param {Number} version 
     * @param {Number} favoriteId The ID of the favorite reference record
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The unique ID given by the device (deviceId or accountId required)
     * @param {Number} [accountId] The account ID of the user (deviceId or accountId required)
     * @param {Number} [latitude] The current latitude of the user
     * @param {Number} [longitude] The current longitude of the user
     * @param {module:api/FavoriteApi~getFavoriteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/WrappedResponse}
     */
    getFavorite(version, favoriteId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling getFavorite");
      }
      // verify the required parameter 'favoriteId' is set
      if (favoriteId === undefined || favoriteId === null) {
        throw new Error("Missing the required parameter 'favoriteId' when calling getFavorite");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'favoriteId': favoriteId,
        'latitude': opts['latitude'],
        'longitude': opts['longitude']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = WrappedResponse;
      return this.apiClient.callApi(
        '/api/{version}/favorite/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchFavorites operation.
     * @callback module:api/FavoriteApi~searchFavoritesCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SearchResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Favorites
     * Searches on the user's favorites.
     * @param {Number} version 
     * @param {String} favoritableType The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY}
     * @param {module:model/String} sortField Determines what to sort the results by {CREATED, UPDATED, DISPLAY}
     * @param {Boolean} descending Determines whether the results are in descending order
     * @param {Number} start The start index for pagination
     * @param {Number} limit The limit for pagination (there is a hard limit of 1000)
     * @param {Boolean} activeOnly Determines whether to only return active favorites
     * @param {Boolean} returnFullResponse Determines whether to return a detailed version of the response list
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The unique ID given by the device (deviceId or accountId required)
     * @param {Number} [accountId] The account ID of the user (deviceId or accountId required)
     * @param {Number} [connectionAccountId] The ID of an account the user would like to view favorites for
     * @param {String} [secondaryType] 
     * @param {String} [keyword] The keyword to search for
     * @param {Number} [latitude] The current latitude of the user
     * @param {Number} [longitude] The current longitude of the user
     * @param {module:api/FavoriteApi~searchFavoritesCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SearchResponse}
     */
    searchFavorites(version, favoritableType, sortField, descending, start, limit, activeOnly, returnFullResponse, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling searchFavorites");
      }
      // verify the required parameter 'favoritableType' is set
      if (favoritableType === undefined || favoritableType === null) {
        throw new Error("Missing the required parameter 'favoritableType' when calling searchFavorites");
      }
      // verify the required parameter 'sortField' is set
      if (sortField === undefined || sortField === null) {
        throw new Error("Missing the required parameter 'sortField' when calling searchFavorites");
      }
      // verify the required parameter 'descending' is set
      if (descending === undefined || descending === null) {
        throw new Error("Missing the required parameter 'descending' when calling searchFavorites");
      }
      // verify the required parameter 'start' is set
      if (start === undefined || start === null) {
        throw new Error("Missing the required parameter 'start' when calling searchFavorites");
      }
      // verify the required parameter 'limit' is set
      if (limit === undefined || limit === null) {
        throw new Error("Missing the required parameter 'limit' when calling searchFavorites");
      }
      // verify the required parameter 'activeOnly' is set
      if (activeOnly === undefined || activeOnly === null) {
        throw new Error("Missing the required parameter 'activeOnly' when calling searchFavorites");
      }
      // verify the required parameter 'returnFullResponse' is set
      if (returnFullResponse === undefined || returnFullResponse === null) {
        throw new Error("Missing the required parameter 'returnFullResponse' when calling searchFavorites");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'connectionAccountId': opts['connectionAccountId'],
        'favoritableType': favoritableType,
        'secondaryType': opts['secondaryType'],
        'keyword': opts['keyword'],
        'sortField': sortField,
        'descending': descending,
        'start': start,
        'limit': limit,
        'activeOnly': activeOnly,
        'returnFullResponse': returnFullResponse,
        'latitude': opts['latitude'],
        'longitude': opts['longitude']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SearchResponse;
      return this.apiClient.callApi(
        '/api/{version}/favorite/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the whoHasFavorited operation.
     * @callback module:api/FavoriteApi~whoHasFavoritedCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/AccountResponse>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Who has Favorited
     * Searches for everyone that has favorited an item
     * @param {Number} version 
     * @param {Number} favoritableId The ID of the favoritableType to search on
     * @param {String} favoritableType The type of the object to favorite {OFFER, OFFER_LOCATION, RETAILER_LOCATION, CATEGORY}
     * @param {Number} start The start index for pagination
     * @param {Number} limit The limit for pagination
     * @param {Object} opts Optional parameters
     * @param {String} [deviceId] The unique ID given by the device (deviceId or accountId required)
     * @param {Number} [accountId] The account ID of the user (deviceId or accountId required)
     * @param {Number} [latitude] The current latitude of the user
     * @param {Number} [longitude] The current longitude of the user
     * @param {String} [keyword] The keyword to limit that account list
     * @param {module:api/FavoriteApi~whoHasFavoritedCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/AccountResponse>}
     */
    whoHasFavorited(version, favoritableId, favoritableType, start, limit, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'version' is set
      if (version === undefined || version === null) {
        throw new Error("Missing the required parameter 'version' when calling whoHasFavorited");
      }
      // verify the required parameter 'favoritableId' is set
      if (favoritableId === undefined || favoritableId === null) {
        throw new Error("Missing the required parameter 'favoritableId' when calling whoHasFavorited");
      }
      // verify the required parameter 'favoritableType' is set
      if (favoritableType === undefined || favoritableType === null) {
        throw new Error("Missing the required parameter 'favoritableType' when calling whoHasFavorited");
      }
      // verify the required parameter 'start' is set
      if (start === undefined || start === null) {
        throw new Error("Missing the required parameter 'start' when calling whoHasFavorited");
      }
      // verify the required parameter 'limit' is set
      if (limit === undefined || limit === null) {
        throw new Error("Missing the required parameter 'limit' when calling whoHasFavorited");
      }

      let pathParams = {
        'version': version
      };
      let queryParams = {
        'deviceId': opts['deviceId'],
        'accountId': opts['accountId'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'favoritableId': favoritableId,
        'favoritableType': favoritableType,
        'keyword': opts['keyword'],
        'start': start,
        'limit': limit
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = [AccountResponse];
      return this.apiClient.callApi(
        '/api/{version}/favorite/whois', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
