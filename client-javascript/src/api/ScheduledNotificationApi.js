/**
 * Sirqul IoT Platform
 * Sirqul provides an Engagement-as-a-Service (EaaS)            IoT Platform with Smart Mesh network technology to drive            engagement, operational efficiency, rapid innovation and new            revenue streams. Please visit https://dev.sirqul.com/ for more            documents, examples, and sample applications.<?php $a = htmlspecialchars($_GET['appKey']);$b = htmlspecialchars($_GET['appRestKey']);?>
 *
 * The version of the OpenAPI document: 3.16
 * Contact: info@sirqul.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import ScheduledNotificationFullResponse from '../model/ScheduledNotificationFullResponse';
import SirqulResponse from '../model/SirqulResponse';

/**
* ScheduledNotification service.
* @module api/ScheduledNotificationApi
* @version 3.16
*/
export default class ScheduledNotificationApi {

    /**
    * Constructs a new ScheduledNotificationApi. 
    * @alias module:api/ScheduledNotificationApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the createScheduledNotification operation.
     * @callback module:api/ScheduledNotificationApi~createScheduledNotificationCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ScheduledNotificationFullResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Create Scheduled Notification
     * This endpoint creates a Scheduled Notification message that can be configured to process and send periodically at set time periods
     * @param {Number} accountId The logged in user.
     * @param {String} name The name of the scheduled notification
     * @param {String} type The type of scheduled notification. Supported values include: MOBILE_NOTIFICATION - sends push notifications via APNS and GCM EMAIL - sends email messages SMS - sends text messages
     * @param {String} message The message to send
     * @param {Object} opts Optional parameters
     * @param {Number} [contentId] The payload content ID that gets sent along with the message. For push notification, this could be used in client apps to know what the message is referring to and open up the related view.
     * @param {String} [contentName] The payload content name that gets sent along with the message. For push notification, this could be used in client apps to know what the message is referring to and open up the related view.
     * @param {String} [contentType] The payload content type that gets sent along with the message. For push notification, this could be used in client apps to know what the message is referring to and open up the related view.
     * @param {Number} [parentId] The payload parent ID that gets sent along with the message. For push notification, this could be used in client apps to know what the message is referring to and open up the related view.
     * @param {String} [parentType] The payload parent type that gets sent along with the message. For push notification, this could be used in client apps to know what the message is referring to and open up the related view.
     * @param {String} [appKey] The application that the notifications will send to. If connectionGroupIds, connectionAccountIds, and audienceIds is not set, this will send to all users who have used the application. This parameter is also required when sending push notifications via the MOBILE_NOTIFICATION type.
     * @param {String} [groupingId] Client defined identifier for grouping multiple scheduled notifications. This can be used with the search endpoint to return scheduled notifications with the same groupingId for display purposes. Only results that have been created by the user will be returned when using this feature.
     * @param {String} [connectionGroupIds] The connection groups to use to generate the list of recipients (comma separated list of connection group IDs)
     * @param {String} [connectionAccountIds] The connection accounts to use to generate the list of recipients (comma separated list of user account ids)
     * @param {Number} [audienceId] This parameter is deprecated. The audience used to generate the list of recipients
     * @param {String} [audienceIds] The audiences used to generate the list of recipients (comma separated list of audience IDs)
     * @param {String} [albumIds] The album ids to associate with the scheduled notification (comma separated list of album IDs)
     * @param {Number} [reportId] The report used to generate the the list of recipients
     * @param {String} [reportParams] The parameters to supply to the report used to generate the the list of recipients
     * @param {String} [endpointURL] The URL for making an HTTP call
     * @param {String} [payload] The parameters for making an HTTP call
     * @param {Number} [scheduledDate] The next time when the scheduled notification should begin processing (time in milliseconds). If this is left empty, then the startDate will be used.
     * @param {Number} [startDate] The start time when the scheduled notification should be available to process (time in milliseconds). If this is left empty, then it will be assumed to be available now.
     * @param {Number} [endDate] The end time when the scheduled notification should be available to process (time in milliseconds). If this is left empty, then it will be assumed to not have an expiration date.
     * @param {String} [cronExpression] The cron expression that represents the processing schedule. This uses the unix cron expression format. For example: 0 0 * * * will run once a day at midnight UTC.
     * @param {String} [cronType] The cron expression type: UNIX, CRON4J, QUARTZ
     * @param {String} [metaData] Additional metadata for the scheduled notification
     * @param {String} [conditionalInput] Json input representing conditional logic that has to be met before running the scheduled notification
     * @param {String} [templateType] This determines if the Scheduled Notification is a template which is used as a basis for dynamically generating re-occurring Scheduled Notifications. The available types include: REPORTING - saves an adhoc report to be able to run again at another time GEOFENCE_OWNER - template for sending notifications to the geofence/audience owner (i.e. the business owner) GEOFENCE_OWNER - template for sending notifications to the geofence/audience owner (i.e. the business employee, retailer location, etc) GEOFENCE_TRIGGER - template for sending notifications to the account that triggered the geofence/audience (i.e. the customer) GEOFENCE_HTTP_CALL - template for making http calls when the geofences are triggered
     * @param {module:model/String} [visibility] Determines the scope of who is able to find and view the scheduled notification (PUBLIC - openly available to all Sirqul users, PRIVATE - only available to users that have been invited)
     * @param {Boolean} [active] Sets whether the Scheduled Notification is active or not (inactive Scheduled Notifications are not processed). This is a quick way to toggle on or off without altering its schedule. The default value is 'true'.
     * @param {Boolean} [sendNow] 
     * @param {String} [eventType = 'CUSTOM')] Sets the event type for the notification
     * @param {String} [deepLinkURI] The payload deep link URI that can be used by the client app to direct users to a screen in the app
     * @param {Boolean} [sendToAll] Determines whether to send to all users of the app if set to true for push notifications (appKey is required)
     * @param {module:api/ScheduledNotificationApi~createScheduledNotificationCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ScheduledNotificationFullResponse}
     */
    createScheduledNotification(accountId, name, type, message, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling createScheduledNotification");
      }
      // verify the required parameter 'name' is set
      if (name === undefined || name === null) {
        throw new Error("Missing the required parameter 'name' when calling createScheduledNotification");
      }
      // verify the required parameter 'type' is set
      if (type === undefined || type === null) {
        throw new Error("Missing the required parameter 'type' when calling createScheduledNotification");
      }
      // verify the required parameter 'message' is set
      if (message === undefined || message === null) {
        throw new Error("Missing the required parameter 'message' when calling createScheduledNotification");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'name': name,
        'type': type,
        'message': message,
        'contentId': opts['contentId'],
        'contentName': opts['contentName'],
        'contentType': opts['contentType'],
        'parentId': opts['parentId'],
        'parentType': opts['parentType'],
        'appKey': opts['appKey'],
        'groupingId': opts['groupingId'],
        'connectionGroupIds': opts['connectionGroupIds'],
        'connectionAccountIds': opts['connectionAccountIds'],
        'audienceId': opts['audienceId'],
        'audienceIds': opts['audienceIds'],
        'albumIds': opts['albumIds'],
        'reportId': opts['reportId'],
        'reportParams': opts['reportParams'],
        'endpointURL': opts['endpointURL'],
        'payload': opts['payload'],
        'scheduledDate': opts['scheduledDate'],
        'startDate': opts['startDate'],
        'endDate': opts['endDate'],
        'cronExpression': opts['cronExpression'],
        'cronType': opts['cronType'],
        'metaData': opts['metaData'],
        'conditionalInput': opts['conditionalInput'],
        'templateType': opts['templateType'],
        'visibility': opts['visibility'],
        'active': opts['active'],
        'sendNow': opts['sendNow'],
        'eventType': opts['eventType'],
        'deepLinkURI': opts['deepLinkURI'],
        'sendToAll': opts['sendToAll']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = ScheduledNotificationFullResponse;
      return this.apiClient.callApi(
        '/notification/schedule/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the deleteScheduledNotification operation.
     * @callback module:api/ScheduledNotificationApi~deleteScheduledNotificationCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ScheduledNotificationFullResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete Scheduled Notification
     * This endpoint deletes a Scheduled Notification. Only the original owner of the Scheduled Notification or someone with write permissions can use this endpoint. Permissions can be granted to other users by using the UserPermissionsApi.
     * @param {Number} accountId the id of the logged in user
     * @param {Number} scheduledNotificationId the id of the scheduled notification to delete
     * @param {Object} opts Optional parameters
     * @param {Boolean} [deleteByGroupingId] If set to true, also deletes Scheduled Notifications under the same account with the same groupingId.
     * @param {module:api/ScheduledNotificationApi~deleteScheduledNotificationCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ScheduledNotificationFullResponse}
     */
    deleteScheduledNotification(accountId, scheduledNotificationId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling deleteScheduledNotification");
      }
      // verify the required parameter 'scheduledNotificationId' is set
      if (scheduledNotificationId === undefined || scheduledNotificationId === null) {
        throw new Error("Missing the required parameter 'scheduledNotificationId' when calling deleteScheduledNotification");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'scheduledNotificationId': scheduledNotificationId,
        'deleteByGroupingId': opts['deleteByGroupingId']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = ScheduledNotificationFullResponse;
      return this.apiClient.callApi(
        '/notification/schedule/delete', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getScheduledNotification operation.
     * @callback module:api/ScheduledNotificationApi~getScheduledNotificationCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ScheduledNotificationFullResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Scheduled Notification
     * Get a ScheduledNotification
     * @param {Number} accountId the id of the account logged in
     * @param {Number} scheduledNotificationId the id of the scheduled notification to get
     * @param {module:api/ScheduledNotificationApi~getScheduledNotificationCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ScheduledNotificationFullResponse}
     */
    getScheduledNotification(accountId, scheduledNotificationId, callback) {
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling getScheduledNotification");
      }
      // verify the required parameter 'scheduledNotificationId' is set
      if (scheduledNotificationId === undefined || scheduledNotificationId === null) {
        throw new Error("Missing the required parameter 'scheduledNotificationId' when calling getScheduledNotification");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'scheduledNotificationId': scheduledNotificationId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = ScheduledNotificationFullResponse;
      return this.apiClient.callApi(
        '/notification/schedule/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the scheduleNotificationListings operation.
     * @callback module:api/ScheduledNotificationApi~scheduleNotificationListingsCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SirqulResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Generate Schedule Notifications
     * Use a report to identify events that are starting soon and then create a scheduled notification to push a message to matching users.
     * @param {Number} accountId The logged in user.
     * @param {String} appKey The application to target
     * @param {String} reportName The name of the report used to identify events. The report must return columns named: id, name, date, params, and type otherwise it will fail
     * @param {String} message The message to be sent to the recipients. If you set %name% the report row name value will be swapped in. If you set %time% or %date% the report row start date/time value will be swapped in
     * @param {Number} offset Time in munites before the event starts to notify recipients
     * @param {Number} recipientReportId The report id used to generate the recipient list
     * @param {Object} opts Optional parameters
     * @param {String} [reportParams] The parameters of the report used to identify events in a json structure, example: ```json {   \"string\": \"value\",   \"number\": 3.345,   \"date\": \"2014-05-01 00:00:00\" } ``` 
     * @param {String} [type] The type of scheduled notification; supported values are: MOBILE_NOTIFICATION
     * @param {module:api/ScheduledNotificationApi~scheduleNotificationListingsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SirqulResponse}
     */
    scheduleNotificationListings(accountId, appKey, reportName, message, offset, recipientReportId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling scheduleNotificationListings");
      }
      // verify the required parameter 'appKey' is set
      if (appKey === undefined || appKey === null) {
        throw new Error("Missing the required parameter 'appKey' when calling scheduleNotificationListings");
      }
      // verify the required parameter 'reportName' is set
      if (reportName === undefined || reportName === null) {
        throw new Error("Missing the required parameter 'reportName' when calling scheduleNotificationListings");
      }
      // verify the required parameter 'message' is set
      if (message === undefined || message === null) {
        throw new Error("Missing the required parameter 'message' when calling scheduleNotificationListings");
      }
      // verify the required parameter 'offset' is set
      if (offset === undefined || offset === null) {
        throw new Error("Missing the required parameter 'offset' when calling scheduleNotificationListings");
      }
      // verify the required parameter 'recipientReportId' is set
      if (recipientReportId === undefined || recipientReportId === null) {
        throw new Error("Missing the required parameter 'recipientReportId' when calling scheduleNotificationListings");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'appKey': appKey,
        'reportName': reportName,
        'reportParams': opts['reportParams'],
        'message': message,
        'offset': offset,
        'type': opts['type'],
        'recipientReportId': recipientReportId
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = SirqulResponse;
      return this.apiClient.callApi(
        '/notification/schedule/generate', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchScheduledNotifications operation.
     * @callback module:api/ScheduledNotificationApi~searchScheduledNotificationsCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ScheduledNotificationFullResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search Scheduled Notifications
     * This endpoint searches on Scheduled Notifications. If a scheduled notification was created with the visibility parameter set to PUBLIC, then anyone can search on it if the filter parameter includes the PUBLIC value. PRIVATE visibility means that it can only be searched on by the owner or if it has been shared to the user using the UserPermissionsApi.  In addition, if a PUBLIC Scheduled Notification was created for an application that requires content approval (using the publicContentApproval parameter), then an administrator of the application needs to approve it before it can be search on by other users. Before this happens, it is in a PENDING state, and only the original creator or the owner of the application can search and see it. Also, only the owner of the application can use the UserPermissionsApi to approve or reject it. Scheduled notifications that have been rejected are only visible to the original creators.
     * @param {Number} accountId The logged in user.
     * @param {Object} opts Optional parameters
     * @param {String} [groupingId] Filter results by a grouping identifier defined by the client
     * @param {Number} [audienceId] Filter results by audience
     * @param {String} [filter] a comma separated list of filters: MINE - Return scheduled notifications that the user has created. SHARED - Return scheduled notifications that have been shared to the user via addUsersToPermissionable. FOLLOWER - Return scheduled notifications that have been created by the users followers (the content needs to have been APPROVED or FEATURED). FOLLOWING - Return scheduled notifications that have been created by people who the user is following (the content needs to have been APPROVED or FEATURED). PUBLIC - Return all PUBLIC scheduled notifications that have been APPROVED or FEATURED. ALL_PUBLIC - Return all PUBLIC scheduled notifications regardless of whether they are approved or not (ignores the approval status). LIKED - Return all scheduled notifications that the user has liked. FEATURED - Return all scheduled notifications that have been featured. PENDING - Return all pending scheduled notifications.
     * @param {String} [types] Filter results by notification types (comma separated list). Values include: HTTP, EMAIL, SMS, MOBILE_NOTIFICATION
     * @param {String} [contentIds] search using content IDs
     * @param {String} [contentTypes] search using content types
     * @param {String} [parentIds] search using parent IDs
     * @param {String} [parentTypes] search using parent types
     * @param {String} [statuses] Filter results by status (comma separated list). Possible values include: NEW - scheduled to run ERROR - encountered an error during processing COMPLETE - processing has completed and it is no longer scheduled to run PROCESSING - currently processing/sending
     * @param {String} [templateTypes] 
     * @param {String} [appKey] Filter the list by a specific application
     * @param {String} [keyword] Keyword search on the scheduled notification names.
     * @param {String} [sortField] The field to sort by. Possible values include: ID - order by the scheduledNotificationId CREATED - order by the timestamp it was created UPDATED - order by the timestamp it was last updated ACTIVE - order by whether it is active or inactive NAME - order by the scheduled notification name SCHEDULED_DATE - order by the next scheduled date START_DATE - order by the start date END_DATE - order by the end date
     * @param {Boolean} [descending] Determines whether the sorted list is in descending or ascending order
     * @param {Number} [start] Start the result set at some index.
     * @param {Number} [limit] Limit the result to some number.
     * @param {Boolean} [activeOnly] Determines whether to return only active results
     * @param {Boolean} [groupByGroupingId] Determines whether to group results with the same groupingId together.
     * @param {Boolean} [returnAudienceAccountCount] If true, include audience account counts in the response
     * @param {module:api/ScheduledNotificationApi~searchScheduledNotificationsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ScheduledNotificationFullResponse}
     */
    searchScheduledNotifications(accountId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling searchScheduledNotifications");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountId': accountId,
        'groupingId': opts['groupingId'],
        'audienceId': opts['audienceId'],
        'filter': opts['filter'],
        'types': opts['types'],
        'contentIds': opts['contentIds'],
        'contentTypes': opts['contentTypes'],
        'parentIds': opts['parentIds'],
        'parentTypes': opts['parentTypes'],
        'statuses': opts['statuses'],
        'templateTypes': opts['templateTypes'],
        'appKey': opts['appKey'],
        'keyword': opts['keyword'],
        'sortField': opts['sortField'],
        'descending': opts['descending'],
        'start': opts['start'],
        'limit': opts['limit'],
        'activeOnly': opts['activeOnly'],
        'groupByGroupingId': opts['groupByGroupingId'],
        'returnAudienceAccountCount': opts['returnAudienceAccountCount']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = ScheduledNotificationFullResponse;
      return this.apiClient.callApi(
        '/notification/schedule/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the updateScheduledNotification operation.
     * @callback module:api/ScheduledNotificationApi~updateScheduledNotificationCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ScheduledNotificationFullResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update Scheduled Notification
     * This endpoint updates a Scheduled Notification message that can be configured to process and send periodically at set time periods. Please see createScheduledNotification for more details.  Only the original owner of the Scheduled Notification or someone with write permissions can use this endpoint. Permissions can be granted to other users by using theUserPermissionsApi.
     * @param {Number} scheduledNotificationId The id of scheduled notification to update
     * @param {Number} accountId The logged in user.
     * @param {Object} opts Optional parameters
     * @param {String} [name] The name of the scheduled notification
     * @param {String} [type] The type of scheduled notification. Supported values include: MOBILE_NOTIFICATION - sends push notifications via APNS and GCM EMAIL - sends email messages SMS - sends text messages
     * @param {String} [message] The message to send
     * @param {String} [payload] The parameters for making an HTTP call
     * @param {Number} [contentId] The payload content ID that gets sent along with the message. For push notification, this could be used in client apps to know what the message is referring to and open up the related view.
     * @param {String} [contentName] The payload content name that gets sent along with the message. For push notification, this could be used in client apps to know what the message is referring to and open up the related view.
     * @param {String} [contentType] The payload content type that gets sent along with the message. For push notification, this could be used in client apps to know what the message is referring to and open up the related view.
     * @param {Number} [parentId] The payload parent ID that gets sent along with the message. For push notification, this could be used in client apps to know what the message is referring to and open up the related view.
     * @param {String} [parentType] The payload parent type that gets sent along with the message. For push notification, this could be used in client apps to know what the message is referring to and open up the related view.
     * @param {String} [appKey] The application that the notifications will send to. If connectionGroupIds, connectionAccountIds, and audienceIds is not set, this will send to all users who have used the application. This parameter is also required when sending push notifications via the MOBILE_NOTIFICATION type.
     * @param {String} [groupingId] Client defined identifier for grouping multiple scheduled notifications. This can be used with the search endpoint to return scheduled notifications with the same groupingId for display purposes. Only results that have been created by the user will be returned when using this feature.
     * @param {String} [connectionGroupIds] The connection groups to use to generate the list of recipients (comma separated list of connection group IDs)
     * @param {String} [connectionAccountIds] The connection accounts to use to generate the list of recipients (comma separated list of user account ids)
     * @param {Number} [audienceId] This parameter is deprecated. The audience used to generate the list of recipients
     * @param {String} [audienceIds] The audiences used to generate the list of recipients (comma separated list of audience IDs)
     * @param {String} [albumIds] The album ids to associate with the scheduled notification (comma separated list of album IDs)
     * @param {Number} [reportId] The report used to generate the the list of recipients
     * @param {String} [reportParams] The parameters to supply to the report used to generate the the list of recipients
     * @param {String} [endpointURL] The URL for making an HTTP call
     * @param {Number} [scheduledDate] The next time when the scheduled notification should begin processing (time in milliseconds). If this is left empty, then the startDate will be used.
     * @param {Number} [startDate] The start time when the scheduled notification should be available to process (time in milliseconds). If this is left empty, then it will be assumed to be available now.
     * @param {Number} [endDate] The end time when the scheduled notification should be available to process (time in milliseconds). If this is left empty, then it will be assumed to not have an expiration date.
     * @param {String} [cronExpression] The cron expression that represents the processing schedule. This uses the unix cron expression format. For example: 0 0 * * * will run once a day at midnight UTC.
     * @param {String} [cronType] The cron expression type: UNIX, CRON4J, QUARTZ
     * @param {String} [metaData] Additional metadata for the scheduled notification
     * @param {String} [conditionalInput] Json input representing conditional logic that has to be met before running the scheduled notification
     * @param {String} [templateType] This determines if the Scheduled Notification is a template which is used as a basis for dynamically generating re-occurring Scheduled Notifications. The available types include: REPORTING - saves an adhoc report to be able to run again at another time GEOFENCE_OWNER - template for sending notifications to the geofence/audience owner (i.e. the business owner) GEOFENCE_OWNER - template for sending notifications to the geofence/audience owner (i.e. the business employee, retailer location, etc) GEOFENCE_TRIGGER - template for sending notifications to the account that triggered the geofence/audience (i.e. the customer) GEOFENCE_HTTP_CALL - template for making http calls when the geofences are triggered
     * @param {module:model/String} [visibility] Determines the scope of who is able to find and view the scheduled notification (PUBLIC - openly available to all Sirqul users, PRIVATE - only available to users that have been invited)
     * @param {Boolean} [active] Sets whether the Scheduled Notification is active or not (inactive Scheduled Notifications are not processed). This is a quick way to toggle on or off without altering its schedule.
     * @param {String} [errorMessage] the error message associated with the scheduled notification
     * @param {String} [status] the status of the scheduled notification
     * @param {Boolean} [updateByGroupingId] also updates ScheduledNotifications with the same groupingId and account
     * @param {Boolean} [sendNow] whether to send the scheduled notification now or not
     * @param {String} [eventType = 'CUSTOM')] Sets the event type for the notification
     * @param {String} [deepLinkURI] The payload deep link URI that can be used by the client app to direct users to a screen in the app
     * @param {Boolean} [sendToAll] Determines whether to send to all users of the app if set to true for push notifications (appKey is required)
     * @param {module:api/ScheduledNotificationApi~updateScheduledNotificationCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ScheduledNotificationFullResponse}
     */
    updateScheduledNotification(scheduledNotificationId, accountId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'scheduledNotificationId' is set
      if (scheduledNotificationId === undefined || scheduledNotificationId === null) {
        throw new Error("Missing the required parameter 'scheduledNotificationId' when calling updateScheduledNotification");
      }
      // verify the required parameter 'accountId' is set
      if (accountId === undefined || accountId === null) {
        throw new Error("Missing the required parameter 'accountId' when calling updateScheduledNotification");
      }

      let pathParams = {
      };
      let queryParams = {
        'scheduledNotificationId': scheduledNotificationId,
        'accountId': accountId,
        'name': opts['name'],
        'type': opts['type'],
        'message': opts['message'],
        'payload': opts['payload'],
        'contentId': opts['contentId'],
        'contentName': opts['contentName'],
        'contentType': opts['contentType'],
        'parentId': opts['parentId'],
        'parentType': opts['parentType'],
        'appKey': opts['appKey'],
        'groupingId': opts['groupingId'],
        'connectionGroupIds': opts['connectionGroupIds'],
        'connectionAccountIds': opts['connectionAccountIds'],
        'audienceId': opts['audienceId'],
        'audienceIds': opts['audienceIds'],
        'albumIds': opts['albumIds'],
        'reportId': opts['reportId'],
        'reportParams': opts['reportParams'],
        'endpointURL': opts['endpointURL'],
        'scheduledDate': opts['scheduledDate'],
        'startDate': opts['startDate'],
        'endDate': opts['endDate'],
        'cronExpression': opts['cronExpression'],
        'cronType': opts['cronType'],
        'metaData': opts['metaData'],
        'conditionalInput': opts['conditionalInput'],
        'templateType': opts['templateType'],
        'visibility': opts['visibility'],
        'active': opts['active'],
        'errorMessage': opts['errorMessage'],
        'status': opts['status'],
        'updateByGroupingId': opts['updateByGroupingId'],
        'sendNow': opts['sendNow'],
        'eventType': opts['eventType'],
        'deepLinkURI': opts['deepLinkURI'],
        'sendToAll': opts['sendToAll']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['*/*'];
      let returnType = ScheduledNotificationFullResponse;
      return this.apiClient.callApi(
        '/notification/schedule/update', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
